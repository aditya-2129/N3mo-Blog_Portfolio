---
title: "Find Subsequence of Length K With the Largest Sum"
summary: "Find a subsequence of length k with maximum sum from an array, maintaining original order."
date: "Mar 13, 2025"
tags:
  - Array
  - Hash Table
  - Sorting
  - Heap
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 57.40% | **Paid:** No
> **Topics:** Array, Hash Table, Sorting, Heap (Priority Queue)

You are given an integer array nums and an integer k. You want to find a subsequence of nums of length k that has the maximum sum.

Return any subsequence of nums of length k with the maximum sum.

A subsequence is an array that can be derived from another array by deleting some or no elements without changing the order of the remaining elements.

- Examples
- Constraints
- [Sorting with Indices](#sorting-with-indices)
- [Heap (Priority Queue)](#heap-priority-queue)
- [Brute Force](#brute-force)

## Examples

```text
Input: nums = [2,1,3,3], k = 2
Output: [3,3]
Explanation:
The subsequence has the maximum sum of 3 + 3 = 6.
```

```text
Input: nums = [-1,-2,3,4], k = 3
Output: [-1,3,4]
Explanation:
The subsequence has a maximum sum of -1 + 3 + 4 = 6.
```

```text
Input: nums = [3,4,3,3], k = 2
Output: [3,4]
Explanation:
The subsequence has the maximum sum of 3 + 4 = 7. 
The other subsequence [3,3] has a sum of 3 + 3 = 6.
```

## Constraints

```text
1 <= nums.length <= 1000
-10⁵ <= nums[i] <= 10⁵
1 <= k <= nums.length
```

---
## Sorting with Indices

**Intuition**
Pair each element with its index, sort by value to find the k largest elements, then sort those by index to restore original order.

**Steps**
- Create pairs of (value, index) for all elements
- Sort pairs by value in descending order
- Take the top k pairs
- Sort these k pairs by their original indices
- Extract and return the values

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maxSubsequence(self, nums: List[int], k: int) -&gt; List[int]:\n        indexed = [(nums[i], i) for i in range(len(nums))]\n        indexed.sort(key=lambda x: -x[0])\n        top_k = indexed[:k]\n        top_k.sort(key=lambda x: x[1])\n        return [val for val, idx in top_k]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] maxSubsequence(int[] nums, int k) {\n        int n = nums.length;\n        int[][] indexed = new int[n][2];\n        for (int i = 0; i &lt; n; i++) {\n            indexed[i][0] = nums[i];\n            indexed[i][1] = i;\n        }\n        Arrays.sort(indexed, (a, b) -&gt; b[0] - a[0]);\n        int[][] topK = Arrays.copyOf(indexed, k);\n        Arrays.sort(topK, (a, b) -&gt; a[1] - b[1]);\n        int[] result = new int[k];\n        for (int i = 0; i &lt; k; i++) {\n            result[i] = topK[i][0];\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; maxSubsequence(vector&lt;int&gt;& nums, int k) {\n        int n = nums.size();\n        vector&lt;pair&lt;int, int&gt;&gt; indexed;\n        for (int i = 0; i &lt; n; i++) {\n            indexed.push_back({nums[i], i});\n        }\n        sort(indexed.begin(), indexed.end(), [](const pair&lt;int, int&gt;& a, const pair&lt;int, int&gt;& b) {\n            return a.first &gt; b.first;\n        });\n        vector&lt;pair&lt;int, int&gt;&gt; topK(indexed.begin(), indexed.begin() + k);\n        sort(topK.begin(), topK.end(), [](const pair&lt;int, int&gt;& a, const pair&lt;int, int&gt;& b) {\n            return a.second &lt; b.second;\n        });\n        vector&lt;int&gt; result;\n        for (auto& p : topK) {\n            result.push_back(p.first);\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    maxSubsequence(nums, k) {\n        const indexed = nums.map((val, idx) =&gt; ({ val, idx }));\n        indexed.sort((a, b) =&gt; b.val - a.val);\n        const topK = indexed.slice(0, k);\n        topK.sort((a, b) =&gt; a.idx - b.idx);\n        return topK.map(item =&gt; item.val);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    maxSubsequence(nums: number[], k: number): number[] {\n        const indexed = nums.map((val, idx) =&gt; ({ val, idx }));\n        indexed.sort((a, b) =&gt; b.val - a.val);\n        const topK = indexed.slice(0, k);\n        topK.sort((a, b) =&gt; a.idx - b.idx);\n        return topK.map(item =&gt; item.val);\n    }\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(n)
- Notes: Simple and intuitive, but requires full sorting

---
## Heap (Priority Queue)

**Intuition**
Use a min-heap of size k to track the k largest elements while iterating through the array, then sort by index to maintain order.

**Steps**
- Create a min-heap of (value, index) pairs
- For each element, push to heap
- If heap size exceeds k, remove the smallest element
- After processing all elements, sort remaining k elements by index
- Return the values in order

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import heapq\n\nclass Solution:\n    def maxSubsequence(self, nums: List[int], k: int) -&gt; List[int]:\n        heap = []\n        for i, val in enumerate(nums):\n            heapq.heappush(heap, (val, i))\n            if len(heap) &gt; k:\n                heapq.heappop(heap)\n        heap.sort(key=lambda x: x[1])\n        return [val for val, idx in heap]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] maxSubsequence(int[] nums, int k) {\n        PriorityQueue&lt;int[]&gt; heap = new PriorityQueue&lt;&gt;((a, b) -&gt; a[0] - b[0]);\n        for (int i = 0; i &lt; nums.length; i++) {\n            heap.offer(new int[]{nums[i], i});\n            if (heap.size() &gt; k) {\n                heap.poll();\n            }\n        }\n        int[][] result = new int[k][2];\n        int idx = 0;\n        while (!heap.isEmpty()) {\n            result[idx++] = heap.poll();\n        }\n        Arrays.sort(result, (a, b) -&gt; a[1] - b[1]);\n        int[] ans = new int[k];\n        for (int i = 0; i &lt; k; i++) {\n            ans[i] = result[i][0];\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; maxSubsequence(vector&lt;int&gt;& nums, int k) {\n        priority_queue&lt;pair&lt;int, int&gt;, vector&lt;pair&lt;int, int&gt;&gt;, greater&lt;pair&lt;int, int&gt;&gt;&gt; heap;\n        for (int i = 0; i &lt; nums.size(); i++) {\n            heap.push({nums[i], i});\n            if (heap.size() &gt; k) {\n                heap.pop();\n            }\n        }\n        vector&lt;pair&lt;int, int&gt;&gt; result;\n        while (!heap.empty()) {\n            result.push_back(heap.top());\n            heap.pop();\n        }\n        sort(result.begin(), result.end(), [](const pair&lt;int, int&gt;& a, const pair&lt;int, int&gt;& b) {\n            return a.second &lt; b.second;\n        });\n        vector&lt;int&gt; ans;\n        for (auto& p : result) {\n            ans.push_back(p.first);\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    maxSubsequence(nums, k) {\n        class MinHeap {\n            constructor() {\n                this.heap = [];\n            }\n            push(item) {\n                this.heap.push(item);\n                this.bubbleUp(this.heap.length - 1);\n            }\n            bubbleUp(idx) {\n                while (idx &gt; 0) {\n                    const parentIdx = Math.floor((idx - 1) / 2);\n                    if (this.heap[parentIdx][0] &lt;= this.heap[idx][0]) break;\n                    [this.heap[parentIdx], this.heap[idx]] = [this.heap[idx], this.heap[parentIdx]];\n                    idx = parentIdx;\n                }\n            }\n            pop() {\n                const min = this.heap[0];\n                const last = this.heap.pop();\n                if (this.heap.length &gt; 0) {\n                    this.heap[0] = last;\n                    this.bubbleDown(0);\n                }\n                return min;\n            }\n            bubbleDown(idx) {\n                while (true) {\n                    const leftChild = 2 * idx + 1;\n                    const rightChild = 2 * idx + 2;\n                    let smallest = idx;\n                    if (leftChild &lt; this.heap.length && this.heap[leftChild][0] &lt; this.heap[smallest][0]) {\n                        smallest = leftChild;\n                    }\n                    if (rightChild &lt; this.heap.length && this.heap[rightChild][0] &lt; this.heap[smallest][0]) {\n                        smallest = rightChild;\n                    }\n                    if (smallest === idx) break;\n                    [this.heap[smallest], this.heap[idx]] = [this.heap[idx], this.heap[smallest]];\n                    idx = smallest;\n                }\n            }\n            size() {\n                return this.heap.length;\n            }\n            toArray() {\n                return [...this.heap];\n            }\n        }\n        const heap = new MinHeap();\n        for (let i = 0; i &lt; nums.length; i++) {\n            heap.push([nums[i], i]);\n            if (heap.size() &gt; k) {\n                heap.pop();\n            }\n        }\n        const result = heap.toArray();\n        result.sort((a, b) =&gt; a[1] - b[1]);\n        return result.map(item =&gt; item[0]);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    maxSubsequence(nums: number[], k: number): number[] {\n        class MinHeap {\n            heap: [number, number][];\n            constructor() {\n                this.heap = [];\n            }\n            push(item: [number, number]): void {\n                this.heap.push(item);\n                this.bubbleUp(this.heap.length - 1);\n            }\n            bubbleUp(idx: number): void {\n                while (idx &gt; 0) {\n                    const parentIdx = Math.floor((idx - 1) / 2);\n                    if (this.heap[parentIdx][0] &lt;= this.heap[idx][0]) break;\n                    [this.heap[parentIdx], this.heap[idx]] = [this.heap[idx], this.heap[parentIdx]];\n                    idx = parentIdx;\n                }\n            }\n            pop(): [number, number] {\n                const min = this.heap[0];\n                const last = this.heap.pop()!;\n                if (this.heap.length &gt; 0) {\n                    this.heap[0] = last;\n                    this.bubbleDown(0);\n                }\n                return min;\n            }\n            bubbleDown(idx: number): void {\n                while (true) {\n                    const leftChild = 2 * idx + 1;\n                    const rightChild = 2 * idx + 2;\n                    let smallest = idx;\n                    if (leftChild &lt; this.heap.length && this.heap[leftChild][0] &lt; this.heap[smallest][0]) {\n                        smallest = leftChild;\n                    }\n                    if (rightChild &lt; this.heap.length && this.heap[rightChild][0] &lt; this.heap[smallest][0]) {\n                        smallest = rightChild;\n                    }\n                    if (smallest === idx) break;\n                    [this.heap[smallest], this.heap[idx]] = [this.heap[idx], this.heap[smallest]];\n                    idx = smallest;\n                }\n            }\n            size(): number {\n                return this.heap.length;\n            }\n            toArray(): [number, number][] {\n                return [...this.heap];\n            }\n        }\n        const heap = new MinHeap();\n        for (let i = 0; i &lt; nums.length; i++) {\n            heap.push([nums[i], i]);\n            if (heap.size() &gt; k) {\n                heap.pop();\n            }\n        }\n        const result = heap.toArray();\n        result.sort((a, b) =&gt; a[1] - b[1]);\n        return result.map(item =&gt; item[0]);\n    }\n}" },]} />

**Complexity**
- Time: O(n log k)
- Space: O(n)
- Notes: More efficient than sorting when k is much smaller than n

---
## Brute Force

**Intuition**
Generate all possible subsequences of length k, calculate their sums, and return the one with maximum sum.

**Steps**
- Generate all combinations of k indices from the array
- For each combination, calculate the sum of elements at those indices
- Track the combination with maximum sum
- Return the elements of that combination in order

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from itertools import combinations\n\nclass Solution:\n    def maxSubsequence(self, nums: List[int], k: int) -&gt; List[int]:\n        n = len(nums)\n        max_sum = float('-inf')\n        result = []\n        \n        for indices in combinations(range(n), k):\n            current_sum = sum(nums[i] for i in indices)\n            if current_sum &gt; max_sum:\n                max_sum = current_sum\n                result = [nums[i] for i in indices]\n        \n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    private int[] nums;\n    private int k;\n    private int maxSum;\n    private int[] result;\n    \n    public int[] maxSubsequence(int[] nums, int k) {\n        this.nums = nums;\n        this.k = k;\n        this.maxSum = Integer.MIN_VALUE;\n        this.result = new int[k];\n        \n        generateCombinations(0, new ArrayList&lt;&gt;());\n        return result;\n    }\n    \n    private void generateCombinations(int start, List&lt;Integer&gt; current) {\n        if (current.size() == k) {\n            int sum = 0;\n            for (int idx : current) {\n                sum += nums[idx];\n            }\n            if (sum &gt; maxSum) {\n                maxSum = sum;\n                for (int i = 0; i &lt; k; i++) {\n                    result[i] = nums[current.get(i)];\n                }\n            }\n            return;\n        }\n        \n        for (int i = start; i &lt; nums.length; i++) {\n            current.add(i);\n            generateCombinations(i + 1, current);\n            current.remove(current.size() - 1);\n        }\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\nprivate:\n    vector&lt;int&gt; nums;\n    int k;\n    int maxSum;\n    vector&lt;int&gt; result;\n    \npublic:\n    vector&lt;int&gt; maxSubsequence(vector&lt;int&gt;& nums, int k) {\n        this-&gt;nums = nums;\n        this-&gt;k = k;\n        this-&gt;maxSum = INT_MIN;\n        this-&gt;result = vector&lt;int&gt;(k);\n        \n        vector&lt;int&gt; current;\n        generateCombinations(0, current);\n        return result;\n    }\n    \nprivate:\n    void generateCombinations(int start, vector&lt;int&gt;& current) {\n        if (current.size() == k) {\n            int sum = 0;\n            for (int idx : current) {\n                sum += nums[idx];\n            }\n            if (sum &gt; maxSum) {\n                maxSum = sum;\n                for (int i = 0; i &lt; k; i++) {\n                    result[i] = nums[current[i]];\n                }\n            }\n            return;\n        }\n        \n        for (int i = start; i &lt; nums.size(); i++) {\n            current.push_back(i);\n            generateCombinations(i + 1, current);\n            current.pop_back();\n        }\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    maxSubsequence(nums, k) {\n        const n = nums.length;\n        let maxSum = -Infinity;\n        let result = [];\n        \n        const generateCombinations = (start, current) =&gt; {\n            if (current.length === k) {\n                const sum = current.reduce((acc, idx) =&gt; acc + nums[idx], 0);\n                if (sum &gt; maxSum) {\n                    maxSum = sum;\n                    result = current.map(idx =&gt; nums[idx]);\n                }\n                return;\n            }\n            \n            for (let i = start; i &lt; n; i++) {\n                current.push(i);\n                generateCombinations(i + 1, current);\n                current.pop();\n            }\n        };\n        \n        generateCombinations(0, []);\n        return result;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    maxSubsequence(nums: number[], k: number): number[] {\n        const n = nums.length;\n        let maxSum = -Infinity;\n        let result: number[] = [];\n        \n        const generateCombinations = (start: number, current: number[]): void =&gt; {\n            if (current.length === k) {\n                const sum = current.reduce((acc, idx) =&gt; acc + nums[idx], 0);\n                if (sum &gt; maxSum) {\n                    maxSum = sum;\n                    result = current.map(idx =&gt; nums[idx]);\n                }\n                return;\n            }\n            \n            for (let i = start; i &lt; n; i++) {\n                current.push(i);\n                generateCombinations(i + 1, current);\n                current.pop();\n            }\n        };\n        \n        generateCombinations(0, []);\n        return result;\n    }\n}" },]} />

**Complexity**
- Time: O(n choose k)
- Space: O(k)
- Notes: Exponential time complexity, only suitable for very small inputs

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find Subsequence of Length K With the Largest Sum Solutions","datePublished":"2025-03-13","description":"Find a subsequence of length k with maximum sum from an array, maintaining original order.","keywords":"LeetCode, Find Subsequence of Length K With the Largest Sum, Easy, Array, Hash Table, Sorting, Heap (Priority Queue)"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Sorting with Indices approach?","acceptedAnswer":{"@type":"Answer","text":"The Sorting with Indices approach has a time complexity of O(n log n) where n is the length of the input array."}},{"@type":"Question","name":"What is the time complexity of the Heap approach?","acceptedAnswer":{"@type":"Answer","text":"The Heap approach has a time complexity of O(n log k) where n is the length of the input array and k is the subsequence length."}},{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach has a time complexity of O(n choose k) which is exponential and not suitable for large inputs."}},{"@type":"Question","name":"Which approach is most efficient for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Heap approach is most efficient when k is much smaller than n, with O(n log k) time complexity. The Sorting approach is simpler and also efficient with O(n log n)."}}]}).replace(/</g,'\u003c')}
</script>