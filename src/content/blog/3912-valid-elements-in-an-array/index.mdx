---
title: "Valid Elements in an Array"
summary: "Count the number of elements in an array that are strictly greater than the sum of all elements to their left."
date: "Apr 19, 2025"
tags:
  - Array
  - Prefix Sum
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 57.00% | **Paid:** No
> **Topics:** N/A

You are given an integer array `nums`. An element `nums[i]` is valid if it is strictly greater than the sum of all elements to its left. Return the number of valid elements in the array.

- Examples
- Constraints
- [Approach 1: Iterative Prefix Sum](#approach-1-iterative-prefix-sum)
- [Approach 2: Brute Force Summation](#approach-2-brute-force-summation)

## Examples

**Example 1:**
```text
Input: nums = [1, 2, 3, 4]
Output: 2
Explanation: 
- nums[0] = 1 is valid because sum of elements to its left is 0.
- nums[1] = 2 is valid because 2 > 1.
- nums[2] = 3 is not valid because 3 is not greater than 1 + 2 = 3.
- nums[3] = 4 is not valid because 4 is not greater than 1 + 2 + 3 = 6.
```

**Example 2:**
```text
Input: nums = [10, 2, 5, 3]
Output: 1
Explanation:
- nums[0] = 10 is valid.
- nums[1] = 2 is not valid because 2 is not greater than 10.
- nums[2] = 5 is not valid because 5 is not greater than 10 + 2 = 12.
- nums[3] = 3 is not valid because 3 is not greater than 10 + 2 + 5 = 17.
```

**Example 3:**
```text
Input: nums = [1, 1, 1]
Output: 1
Explanation:
- nums[0] = 1 is valid.
- nums[1] = 1 is not valid because 1 is not greater than 1.
- nums[2] = 1 is not valid because 1 is not greater than 1 + 1 = 2.
```

## Constraints

```text
1 <= nums.length <= 100
1 <= nums[i] <= 100
```

---
## Approach 1: Iterative Prefix Sum

**Intuition**
We can iterate through the array while maintaining a running sum of all elements encountered so far. For each element, we simply check if it is greater than this running sum.

**Steps**
- Initialize a variable `total_sum` to 0 and `count` to 0.
- Iterate through each number `num` in the array `nums`.
- If `num` is strictly greater than `total_sum`, increment `count`.
- Add `num` to `total_sum`.
- Return `count`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countValidElements(self, nums: list[int]) -&gt; int:\n        total_sum = 0\n        count = 0\n        for num in nums:\n            if num &gt; total_sum:\n                count += 1\n            total_sum += num\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int countValidElements(int[] nums) {\n        int totalSum = 0;\n        int count = 0;\n        for (int num : nums) {\n            if (num &gt; totalSum) {\n                count++;\n            }\n            totalSum += num;\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countValidElements(vector&lt;int&gt;& nums) {\n        int totalSum = 0;\n        int count = 0;\n        for (int num : nums) {\n            if (num &gt; totalSum) {\n                count++;\n            }\n            totalSum += num;\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar countValidElements = function(nums) {\n    let totalSum = 0;\n    let count = 0;\n    for (const num of nums) {\n        if (num &gt; totalSum) {\n            count++;\n        }\n        totalSum += num;\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countValidElements(nums: number[]): number {\n    let totalSum = 0;\n    let count = 0;\n    for (const num of nums) {\n        if (num &gt; totalSum) {\n            count++;\n        }\n        totalSum += num;\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: This is the optimal solution as it requires a single pass through the array.

---
## Approach 2: Brute Force Summation

**Intuition**
For every element at index `i`, explicitly calculate the sum of all elements from index `0` to `i-1` and compare it with the current element.

**Steps**
- Initialize `count` to 0.
- Iterate through the array with index `i`.
- For each `i`, calculate the sum of elements from `0` to `i-1`.
- If `nums[i]` is greater than this sum, increment `count`.
- Return `count`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countValidElements(self, nums: list[int]) -&gt; int:\n        count = 0\n        n = len(nums)\n        for i in range(n):\n            left_sum = sum(nums[:i])\n            if nums[i] &gt; left_sum:\n                count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int countValidElements(int[] nums) {\n        int count = 0;\n        int n = nums.length;\n        for (int i = 0; i &lt; n; i++) {\n            int leftSum = 0;\n            for (int j = 0; j &lt; i; j++) {\n                leftSum += nums[j];\n            }\n            if (nums[i] &gt; leftSum) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countValidElements(vector&lt;int&gt;& nums) {\n        int count = 0;\n        int n = nums.size();\n        for (int i = 0; i &lt; n; i++) {\n            int leftSum = 0;\n            for (int j = 0; j &lt; i; j++) {\n                leftSum += nums[j];\n            }\n            if (nums[i] &gt; leftSum) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar countValidElements = function(nums) {\n    let count = 0;\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        let leftSum = 0;\n        for (let j = 0; j &lt; i; j++) {\n            leftSum += nums[j];\n        }\n        if (nums[i] &gt; leftSum) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countValidElements(nums: number[]): number {\n    let count = 0;\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        let leftSum = 0;\n        for (let j = 0; j &lt; i; j++) {\n            leftSum += nums[j];\n        }\n        if (nums[i] &gt; leftSum) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1)
- Notes: This approach is inefficient for larger arrays but demonstrates the brute force logic clearly.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Valid Elements in an Array Solutions","datePublished":"2025-04-19","description":"Count the number of elements in an array that are strictly greater than the sum of all elements to their left.","keywords":"LeetCode, Valid Elements in an Array, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Prefix Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of the Iterative Prefix Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a few extra variables."}},{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n²) because for each element, we sum all previous elements."}}]}).replace(/</g,'\u003c')}
</script>