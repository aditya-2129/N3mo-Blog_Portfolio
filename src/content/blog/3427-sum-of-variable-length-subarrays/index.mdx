---
title: "Sum of Variable Length Subarrays"
summary: "Calculate the total sum of subarrays defined by a corresponding lengths array."
date: "Nov 09, 2024"
tags:
  - Array
  - Prefix Sum
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 85.60% | **Paid:** No
> **Topics:** Array, Prefix Sum

You are given a 0-indexed integer array `nums` and an array `lengths` of the same length.

For each index `i`, you need to consider the subarray of `nums` starting at index `i` with length `lengths[i]`. If the length `lengths[i]` causes the subarray to go beyond the bounds of `nums`, consider only the elements up to the last index of `nums`.

Return the sum of all elements in all these subarrays.

- Examples
- Constraints
- [Brute Force Simulation](#brute-force-simulation)
- [Prefix Sum Optimization](#prefix-sum-optimization)
- [Difference Array (Contribution Counting)](#difference-array-contribution-counting)

## Examples

Example 1:
```text
Input: nums = [1,2,3,4], lengths = [1,3,1,2]
Output: 17
Explanation:
- Subarray at index 0 with length 1: [1]. Sum = 1.
- Subarray at index 1 with length 3: [2, 3, 4]. Sum = 9.
- Subarray at index 2 with length 1: [3]. Sum = 3.
- Subarray at index 3 with length 2: [4]. (Only 1 element remains). Sum = 4.
Total sum = 1 + 9 + 3 + 4 = 17.
```

Example 2:
```text
Input: nums = [1,2,3], lengths = [3,3,3]
Output: 14
Explanation:
- Subarray at index 0 with length 3: [1, 2, 3]. Sum = 6.
- Subarray at index 1 with length 3: [2, 3]. Sum = 5.
- Subarray at index 2 with length 3: [3]. Sum = 3.
Total sum = 6 + 5 + 3 = 14.
```

## Constraints

```text
- 1 <= n == nums.length <= 100
- 1 <= nums[i] <= 1000
```

---

## Brute Force Simulation

**Intuition**
Iterate through each starting index `i`, determine the end of the subarray based on `lengths[i]`, and sum the elements directly by iterating through the subarray.

**Steps**
- Initialize `total` to 0.
- Iterate through each index `i` from 0 to `n-1`.
- Calculate the end index `j` as `min(n-1, i + lengths[i] - 1)`.
- Iterate from `i` to `j` and add `nums[k]` to `total`.
- Return `total`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfSubarrays(self, nums: list[int], lengths: list[int]) -&gt; int:\n        n = len(nums)\n        total = 0\n        for i in range(n):\n            end = min(n - 1, i + lengths[i] - 1)\n            for j in range(i, end + 1):\n                total += nums[j]\n        return total\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public long sumOfSubarrays(int[] nums, int[] lengths) {\n        int n = nums.length;\n        long total = 0;\n        for (int i = 0; i &lt; n; i++) {\n            int end = Math.min(n - 1, i + lengths[i] - 1);\n            for (int j = i; j &lt;= end; j++) {\n                total += nums[j];\n            }\n        }\n        return total;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    long long sumOfSubarrays(vector&lt;int&gt;& nums, vector&lt;int&gt;& lengths) {\n        int n = nums.size();\n        long long total = 0;\n        for (int i = 0; i &lt; n; ++i) {\n            int end = min(n - 1, i + lengths[i] - 1);\n            for (int j = i; j &lt;= end; ++j) {\n                total += nums[j];\n            }\n        }\n        return total;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var sumOfSubarrays = function(nums, lengths) {\n    const n = nums.length;\n    let total = 0;\n    for (let i = 0; i &lt; n; i++) {\n        const end = Math.min(n - 1, i + lengths[i] - 1);\n        for (let j = i; j &lt;= end; j++) {\n            total += nums[j];\n        }\n    }\n    return total;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfSubarrays(nums: number[], lengths: number[]): number {\n    const n = nums.length;\n    let total = 0;\n    for (let i = 0; i &lt; n; i++) {\n        const end = Math.min(n - 1, i + lengths[i] - 1);\n        for (let j = i; j &lt;= end; j++) {\n            total += nums[j];\n        }\n    }\n    return total;\n}\n" },]} />

**Complexity**
- Time: O(n * L) where L is the average length. In the worst case, this is O(n²).
- Space: O(1)
- Notes: Simple to implement but inefficient for large arrays or large lengths.

---

## Prefix Sum Optimization

**Intuition**
Precompute the prefix sums of the array to allow calculating the sum of any subarray in constant time. This avoids the inner loop of the brute force approach.

**Steps**
- Create a `prefix` array where `prefix[i]` is the sum of elements from `nums[0]` to `nums[i-1]`.
- Iterate through each index `i`.
- Calculate the end index `j` as `min(n-1, i + lengths[i] - 1)`.
- Add `prefix[j+1] - prefix[i]` to `total`.
- Return `total`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfSubarrays(self, nums: list[int], lengths: list[int]) -&gt; int:\n        n = len(nums)\n        prefix = [0] * (n + 1)\n        for i in range(n):\n            prefix[i + 1] = prefix[i] + nums[i]\n        \n        total = 0\n        for i in range(n):\n            end = min(n - 1, i + lengths[i] - 1)\n            total += prefix[end + 1] - prefix[i]\n        return total\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public long sumOfSubarrays(int[] nums, int[] lengths) {\n        int n = nums.length;\n        long[] prefix = new long[n + 1];\n        for (int i = 0; i &lt; n; i++) {\n            prefix[i + 1] = prefix[i] + nums[i];\n        }\n        \n        long total = 0;\n        for (int i = 0; i &lt; n; i++) {\n            int end = Math.min(n - 1, i + lengths[i] - 1);\n            total += prefix[end + 1] - prefix[i];\n        }\n        return total;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    long long sumOfSubarrays(vector&lt;int&gt;& nums, vector&lt;int&gt;& lengths) {\n        int n = nums.size();\n        vector&lt;long long&gt; prefix(n + 1, 0);\n        for (int i = 0; i &lt; n; ++i) {\n            prefix[i + 1] = prefix[i] + nums[i];\n        }\n        \n        long long total = 0;\n        for (int i = 0; i &lt; n; ++i) {\n            int end = min(n - 1, i + lengths[i] - 1);\n            total += prefix[end + 1] - prefix[i];\n        }\n        return total;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var sumOfSubarrays = function(nums, lengths) {\n    const n = nums.length;\n    const prefix = new Array(n + 1).fill(0);\n    for (let i = 0; i &lt; n; i++) {\n        prefix[i + 1] = prefix[i] + nums[i];\n    }\n    \n    let total = 0;\n    for (let i = 0; i &lt; n; i++) {\n        const end = Math.min(n - 1, i + lengths[i] - 1);\n        total += prefix[end + 1] - prefix[i];\n    }\n    return total;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfSubarrays(nums: number[], lengths: number[]): number {\n    const n = nums.length;\n    const prefix = new Array(n + 1).fill(0);\n    for (let i = 0; i &lt; n; i++) {\n        prefix[i + 1] = prefix[i] + nums[i];\n    }\n    \n    let total = 0;\n    for (let i = 0; i &lt; n; i++) {\n        const end = Math.min(n - 1, i + lengths[i] - 1);\n        total += prefix[end + 1] - prefix[i];\n    }\n    return total;\n}\n" },]} />

**Complexity**
- Time: O(n) for building prefix sum and O(n) for calculating total, resulting in O(n).
- Space: O(n) for the prefix array.
- Notes: Efficient time complexity, optimal for this problem type.

---

## Difference Array (Contribution Counting)

**Intuition**
Instead of summing subarrays, determine how many times each element `nums[i]` contributes to the final total. An element `nums[i]` is included in the subarray starting at `j` if `j &lt;= i` and `j + lengths[j] &gt; i`. We can use a difference array to mark the ranges covered by each subarray.

**Steps**
- Initialize a difference array `diff` of size `n+1` with zeros.
- Iterate through each index `i` (start of subarray).
- Calculate the end index `j` as `min(n-1, i + lengths[i] - 1)`.
- Increment `diff[i]` by 1 and decrement `diff[j+1]` by 1.
- Compute the prefix sum of the `diff` array to get the count of how many subarrays cover each index `i`.
- Multiply the count at index `i` by `nums[i]` and add to `total`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfSubarrays(self, nums: list[int], lengths: list[int]) -&gt; int:\n        n = len(nums)\n        diff = [0] * (n + 1)\n        \n        for i in range(n):\n            end = min(n - 1, i + lengths[i] - 1)\n            diff[i] += 1\n            diff[end + 1] -= 1\n        \n        total = 0\n        count = 0\n        for i in range(n):\n            count += diff[i]\n            total += count * nums[i]\n        return total\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public long sumOfSubarrays(int[] nums, int[] lengths) {\n        int n = nums.length;\n        long[] diff = new long[n + 1];\n        \n        for (int i = 0; i &lt; n; i++) {\n            int end = Math.min(n - 1, i + lengths[i] - 1);\n            diff[i]++;\n            diff[end + 1]--;\n        }\n        \n        long total = 0;\n        long count = 0;\n        for (int i = 0; i &lt; n; i++) {\n            count += diff[i];\n            total += count * nums[i];\n        }\n        return total;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    long long sumOfSubarrays(vector&lt;int&gt;& nums, vector&lt;int&gt;& lengths) {\n        int n = nums.size();\n        vector&lt;long long&gt; diff(n + 1, 0);\n        \n        for (int i = 0; i &lt; n; ++i) {\n            int end = min(n - 1, i + lengths[i] - 1);\n            diff[i]++;\n            diff[end + 1]--;\n        }\n        \n        long long total = 0;\n        long long count = 0;\n        for (int i = 0; i &lt; n; ++i) {\n            count += diff[i];\n            total += count * nums[i];\n        }\n        return total;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var sumOfSubarrays = function(nums, lengths) {\n    const n = nums.length;\n    const diff = new Array(n + 1).fill(0);\n    \n    for (let i = 0; i &lt; n; i++) {\n        const end = Math.min(n - 1, i + lengths[i] - 1);\n        diff[i]++;\n        diff[end + 1]--;\n    }\n    \n    let total = 0;\n    let count = 0;\n    for (let i = 0; i &lt; n; i++) {\n        count += diff[i];\n        total += count * nums[i];\n    }\n    return total;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfSubarrays(nums: number[], lengths: number[]): number {\n    const n = nums.length;\n    const diff = new Array(n + 1).fill(0);\n    \n    for (let i = 0; i &lt; n; i++) {\n        const end = Math.min(n - 1, i + lengths[i] - 1);\n        diff[i]++;\n        diff[end + 1]--;\n    }\n    \n    let total = 0;\n    let count = 0;\n    for (let i = 0; i &lt; n; i++) {\n        count += diff[i];\n        total += count * nums[i];\n    }\n    return total;\n}\n" },]} />

**Complexity**
- Time: O(n) to process ranges and O(n) to calculate the total, resulting in O(n).
- Space: O(n) for the difference array.
- Notes: A clever approach that transforms the problem into a range update query.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Sum of Variable Length Subarrays Solutions","datePublished":"2024-11-09","description":"Calculate the total sum of subarrays defined by a corresponding lengths array.","keywords":"LeetCode, Sum of Variable Length Subarrays, Easy, Array, Prefix Sum"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n * L), where n is the length of the array and L is the average length of the subarrays. In the worst case, it is O(n²)."}},{"@type":"Question","name":"How does the Prefix Sum approach optimize the solution?","acceptedAnswer":{"@type":"Answer","text":"The Prefix Sum approach precomputes cumulative sums, allowing the sum of any subarray to be calculated in O(1) time. This reduces the overall time complexity to O(n)."}},{"@type":"Question","name":"What is the main idea behind the Difference Array approach?","acceptedAnswer":{"@type":"Answer","text":"The Difference Array approach counts how many times each element in the original array appears in all the variable-length subarrays by marking ranges, then sums the contributions of each element based on its count."}}]}).replace(/</g,'\u003c')}
</script>