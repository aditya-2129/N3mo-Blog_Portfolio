---
title: "Smallest Pair With Different Frequencies"
summary: "Find the lexicographically smallest pair of indices where the frequencies of the corresponding elements differ."
date: "Apr 07, 2026"
tags:
  - Array
  - Hash Table
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 69.50% | **Paid:** No
> **Topics:** Array, Hash Table, Counting

You are given a 0-indexed integer array `nums`. A pair of indices `(i, j)` is called valid if `i &lt; j` and the frequency of `nums[i]` in the array is not equal to the frequency of `nums[j]` in the array.

Return the lexicographically smallest valid pair. If no such pair exists, return `[-1, -1]`.

A pair `(i1, j1)` is lexicographically smaller than `(i2, j2)` if either `i1 &lt; i2` or `i1 == i2` and `j1 &lt; j2`.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Hash Map + Nested Loops](#hash-map--nested-loops)
- [Linear Scan](#linear-scan)

## Examples

### Example 1

**Input:**
```text
nums = [1,1,2,2,3,4]
```

**Output:**
```text
[1,3]
```

**Explanation:**
The smallest value is 1 with a frequency of 2, and the smallest value greater than 1 that has a different frequency from 1 is 3 with a frequency of 1. Thus, the answer is [1, 3].

### Example 2

**Input:**
```text
nums = [1,5]
```

**Output:**
```text
[-1,-1]
```

**Explanation:**
Both values have the same frequency, so no valid pair exists. Return [-1, -1].

### Example 3

**Input:**
```text
nums = [7]
```

**Output:**
```text
[-1,-1]
```

**Explanation:**
There is only one value in the array, so no valid pair exists. Return [-1, -1].

## Constraints

```text
2 <= nums.length <= 100
1 <= nums[i] <= 100
```

---
## Brute Force

**Intuition**
Check every possible pair `(i, j)` in the array. For each pair, count the occurrences of `nums[i]` and `nums[j]` in the entire array to see if they are different.

**Steps**
- Iterate `i` from `0` to `n-2`.
- Iterate `j` from `i+1` to `n-1`.
- For each pair, scan the array to count frequency of `nums[i]` and `nums[j]`.
- If frequencies differ, return `[i, j]`.
- If loop finishes, return `[-1, -1]`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestPair(self, nums: list[int]) -&gt; list[int]:\n        n = len(nums)\n        for i in range(n):\n            for j in range(i + 1, n):\n                # Count frequencies on the fly\n                count_i = 0\n                count_j = 0\n                for k in range(n):\n                    if nums[k] == nums[i]:\n                        count_i += 1\n                    if nums[k] == nums[j]:\n                        count_j += 1\n                \n                if count_i != count_j:\n                    return [i, j]\n        return [-1, -1]\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] smallestPair(int[] nums) {\n        int n = nums.length;\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int countI = 0;\n                int countJ = 0;\n                for (int k = 0; k &lt; n; k++) {\n                    if (nums[k] == nums[i]) countI++;\n                    if (nums[k] == nums[j]) countJ++;\n                }\n                if (countI != countJ) {\n                    return new int[]{i, j};\n                }\n            }\n        }\n        return new int[]{-1, -1};\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; smallestPair(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        for (int i = 0; i &lt; n; ++i) {\n            for (int j = i + 1; j &lt; n; ++j) {\n                int countI = 0;\n                int countJ = 0;\n                for (int k = 0; k &lt; n; ++k) {\n                    if (nums[k] == nums[i]) countI++;\n                    if (nums[k] == nums[j]) countJ++;\n                }\n                if (countI != countJ) {\n                    return {i, j};\n                }\n            }\n        }\n        return {-1, -1};\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar smallestPair = function(nums) {\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            let countI = 0;\n            let countJ = 0;\n            for (let k = 0; k &lt; n; k++) {\n                if (nums[k] === nums[i]) countI++;\n                if (nums[k] === nums[j]) countJ++;\n            }\n            if (countI !== countJ) {\n                return [i, j];\n            }\n        }\n    }\n    return [-1, -1];\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function smallestPair(nums: number[]): number[] {\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            let countI = 0;\n            let countJ = 0;\n            for (let k = 0; k &lt; n; k++) {\n                if (nums[k] === nums[i]) countI++;\n                if (nums[k] === nums[j]) countJ++;\n            }\n            if (countI !== countJ) {\n                return [i, j];\n            }\n        }\n    }\n    return [-1, -1];\n}\n" },]} />

**Complexity**
- Time: O(n³)
- Space: O(1)
- Notes: Inefficient for larger arrays due to repeated counting.

---
## Hash Map + Nested Loops

**Intuition**
Optimize the frequency counting by using a Hash Map to store frequencies of all elements first. Then, check every pair to find the first valid one.

**Steps**
- Create a frequency map of `nums`.
- Iterate `i` from `0` to `n-2`.
- Iterate `j` from `i+1` to `n-1`.
- Check if `freq[nums[i]] != freq[nums[j]]`.
- If true, return `[i, j]`.
- If loop finishes, return `[-1, -1]`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from collections import Counter\n\nclass Solution:\n    def smallestPair(self, nums: list[int]) -&gt; list[int]:\n        freq = Counter(nums)\n        n = len(nums)\n        for i in range(n):\n            for j in range(i + 1, n):\n                if freq[nums[i]] != freq[nums[j]]:\n                    return [i, j]\n        return [-1, -1]\n" },
{ name: "Java", lang: "java", code: "import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public int[] smallestPair(int[] nums) {\n        Map&lt;Integer, Integer&gt; freq = new HashMap&lt;&gt;();\n        for (int num : nums) {\n            freq.put(num, freq.getOrDefault(num, 0) + 1);\n        }\n        int n = nums.length;\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                if (freq.get(nums[i]) != freq.get(nums[j])) {\n                    return new int[]{i, j};\n                }\n            }\n        }\n        return new int[]{-1, -1};\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_map&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; smallestPair(vector&lt;int&gt;& nums) {\n        unordered_map&lt;int, int&gt; freq;\n        for (int num : nums) {\n            freq[num]++;\n        }\n        int n = nums.size();\n        for (int i = 0; i &lt; n; ++i) {\n            for (int j = i + 1; j &lt; n; ++j) {\n                if (freq[nums[i]] != freq[nums[j]]) {\n                    return {i, j};\n                }\n            }\n        }\n        return {-1, -1};\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar smallestPair = function(nums) {\n    const freq = new Map();\n    for (const num of nums) {\n        freq.set(num, (freq.get(num) || 0) + 1);\n    }\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            if (freq.get(nums[i]) !== freq.get(nums[j])) {\n                return [i, j];\n            }\n        }\n    }\n    return [-1, -1];\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function smallestPair(nums: number[]): number[] {\n    const freq = new Map&lt;number, number&gt;();\n    for (const num of nums) {\n        freq.set(num, (freq.get(num) || 0) + 1);\n    }\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            if (freq.get(nums[i]) !== freq.get(nums[j])) {\n                return [i, j];\n            }\n        }\n    }\n    return [-1, -1];\n}\n" },]} />

**Complexity**
- Time: O(n²)
- Space: O(n)
- Notes: Much better than brute force, but still quadratic.

---
## Linear Scan

**Intuition**
We want the lexicographically smallest pair `(i, j)`. This means we want the smallest possible `i`. The smallest possible `i` is `0`. If there exists any `j &gt; 0` such that the frequency of `nums[j]` is different from `nums[0]`, then `(0, j)` is the answer (specifically the smallest such `j`). If no such `j` exists, it means all elements have the same frequency, so no valid pair exists.

**Steps**
- Calculate the frequency of all elements in `nums`.
- Get the frequency of the first element `nums[0]`, call it `target_freq`.
- Iterate `j` from `1` to `n-1`.
- If `freq[nums[j]] != target_freq`, return `[0, j]`.
- If the loop completes, return `[-1, -1]`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from collections import Counter\n\nclass Solution:\n    def smallestPair(self, nums: list[int]) -&gt; list[int]:\n        freq = Counter(nums)\n        n = len(nums)\n        target_freq = freq[nums[0]]\n        \n        for j in range(1, n):\n            if freq[nums[j]] != target_freq:\n                return [0, j]\n                \n        return [-1, -1]\n" },
{ name: "Java", lang: "java", code: "import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public int[] smallestPair(int[] nums) {\n        Map&lt;Integer, Integer&gt; freq = new HashMap&lt;&gt;();\n        for (int num : nums) {\n            freq.put(num, freq.getOrDefault(num, 0) + 1);\n        }\n        int n = nums.length;\n        int targetFreq = freq.get(nums[0]);\n        \n        for (int j = 1; j &lt; n; j++) {\n            if (freq.get(nums[j]) != targetFreq) {\n                return new int[]{0, j};\n            }\n        }\n        return new int[]{-1, -1};\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_map&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; smallestPair(vector&lt;int&gt;& nums) {\n        unordered_map&lt;int, int&gt; freq;\n        for (int num : nums) {\n            freq[num]++;\n        }\n        int n = nums.size();\n        int targetFreq = freq[nums[0]];\n        \n        for (int j = 1; j &lt; n; ++j) {\n            if (freq[nums[j]] != targetFreq) {\n                return {0, j};\n            }\n        }\n        return {-1, -1};\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar smallestPair = function(nums) {\n    const freq = new Map();\n    for (const num of nums) {\n        freq.set(num, (freq.get(num) || 0) + 1);\n    }\n    const n = nums.length;\n    const targetFreq = freq.get(nums[0]);\n    \n    for (let j = 1; j &lt; n; j++) {\n        if (freq.get(nums[j]) !== targetFreq) {\n            return [0, j];\n        }\n    }\n    return [-1, -1];\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function smallestPair(nums: number[]): number[] {\n    const freq = new Map&lt;number, number&gt;();\n    for (const num of nums) {\n        freq.set(num, (freq.get(num) || 0) + 1);\n    }\n    const n = nums.length;\n    const targetFreq = freq.get(nums[0]);\n    \n    for (let j = 1; j &lt; n; j++) {\n        if (freq.get(nums[j]) !== targetFreq) {\n            return [0, j];\n        }\n    }\n    return [-1, -1];\n}\n" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Optimal solution. We only need to check pairs starting with index 0.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Smallest Pair With Different Frequencies Solutions","datePublished":"2026-04-07","description":"Find the lexicographically smallest pair of indices where the frequencies of the corresponding elements differ.","keywords":"LeetCode, Smallest Pair With Different Frequencies, Easy, Array, Hash Table, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n³), where n is the length of the array."}},{"@type":"Question","name":"What is the time complexity of the Hash Map + Nested Loops approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Hash Map + Nested Loops approach is O(n²)."}},{"@type":"Question","name":"What is the time complexity of the optimal Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the optimal Linear Scan approach is O(n)."}}]}).replace(/</g,'\u003c')}
</script>