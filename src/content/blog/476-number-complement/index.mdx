---
title: "Number Complement"
summary: "Find the complement of an integer by flipping all bits in its binary representation."
date: "May 24, 2025"
tags:
  - Bit Manipulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 70.40% | **Paid:** No
> **Topics:** Bit Manipulation

The complement of an integer is the integer you get when you flip all the 0's to 1's and all the 1's to 0's in its binary representation.

For example, The integer 5 is "101" in binary, its complement is "010" which is the integer 2.

Given an integer num, return its complement.

- Examples
- Constraints
- [Bit Manipulation with Mask](#bit-manipulation-with-mask)
- [Bit by Bit Construction](#bit-by-bit-construction)
- [String Manipulation](#string-manipulation)
- [Mathematical Approach](#mathematical-approach)

## Examples

```text
Input: num = 5
Output: 2
Explanation: 5 is "101" in binary, with complement "010" in binary, which is 2 in base-10.
```

```text
Input: num = 1
Output: 0
Explanation: 1 is "1" in binary, with complement "0" in binary, which is 0 in base-10.
```

## Constraints

```text
1 <= num < 2^31
```

---
## Bit Manipulation with Mask

**Intuition**
Create a mask with all 1s from the most significant bit of num to the least significant bit, then XOR with num to flip only those bits.

**Steps**
- Find the position of the most significant bit of num
- Create a mask with all 1s from MSB to LSB
- XOR num with the mask to get the complement

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def findComplement(self, num: int) -> int:
        mask = 1
        while mask &lt;= num:
            mask &lt;&lt;= 1
        return num ^ (mask - 1)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int findComplement(int num) {
        int mask = 1;
        while (mask &lt;= num) {
            mask &lt;&lt;= 1;
        }
        return num ^ (mask - 1);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int findComplement(int num) {
        int mask = 1;
        while (mask &lt;= num) {
            mask &lt;&lt;= 1;
        }
        return num ^ (mask - 1);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} num
 * @return {number}
 */
var findComplement = function(num) {
    let mask = 1;
    while (mask &lt;= num) {
        mask &lt;&lt;= 1;
    }
    return num ^ (mask - 1);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function findComplement(num: number): number {
    let mask = 1;
    while (mask &lt;= num) {
        mask &lt;&lt;= 1;
    }
    return num ^ (mask - 1);
}
` },]} />

**Complexity**
- Time: O(log n) - we iterate through each bit position
- Space: O(1) - only using constant extra space
- Notes: Efficient bit manipulation approach

---
## Bit by Bit Construction

**Intuition**
Build the complement bit by bit by iterating through each bit position and flipping the bit.

**Steps**
- Initialize result to 0
- Iterate through each bit position
- For each position, check if the bit is 0 or 1
- Add the flipped bit to the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def findComplement(self, num: int) -> int:
        result = 0
        bit = 0
        while num &gt; 0:
            if num & 1 == 0:
                result |= 1 &lt;&lt; bit
            num &gt;&gt;= 1
            bit += 1
        return result
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int findComplement(int num) {
        int result = 0;
        int bit = 0;
        while (num &gt; 0) {
            if ((num & 1) == 0) {
                result |= 1 &lt;&lt; bit;
            }
            num &gt;&gt;= 1;
            bit++;
        }
        return result;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int findComplement(int num) {
        int result = 0;
        int bit = 0;
        while (num &gt; 0) {
            if ((num & 1) == 0) {
                result |= 1 &lt;&lt; bit;
            }
            num &gt;&gt;= 1;
            bit++;
        }
        return result;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} num
 * @return {number}
 */
var findComplement = function(num) {
    let result = 0;
    let bit = 0;
    while (num &gt; 0) {
        if ((num & 1) === 0) {
            result |= 1 &lt;&lt; bit;
        }
        num &gt;&gt;= 1;
        bit++;
    }
    return result;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function findComplement(num: number): number {
    let result = 0;
    let bit = 0;
    while (num &gt; 0) {
        if ((num & 1) === 0) {
            result |= 1 &lt;&lt; bit;
        }
        num &gt;&gt;= 1;
        bit++;
    }
    return result;
}
` },]} />

**Complexity**
- Time: O(log n) - we iterate through each bit position
- Space: O(1) - only using constant extra space
- Notes: Straightforward but slightly more verbose than mask approach

---
## String Manipulation

**Intuition**
Convert the number to binary string, flip all bits, and convert back to integer.

**Steps**
- Convert num to binary string
- Flip each bit (0 to 1, 1 to 0)
- Convert the flipped binary string back to integer

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def findComplement(self, num: int) -> int:
        binary = bin(num)[2:]
        complement = ''.join('1' if b == '0' else '0' for b in binary)
        return int(complement, 2)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int findComplement(int num) {
        String binary = Integer.toBinaryString(num);
        StringBuilder complement = new StringBuilder();
        for (char c : binary.toCharArray()) {
            complement.append(c == '0' ? '1' : '0');
        }
        return Integer.parseInt(complement.toString(), 2);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int findComplement(int num) {
        string binary;
        int temp = num;
        while (temp &gt; 0) {
            binary = (char)('0' + (temp & 1)) + binary;
            temp &gt;&gt;= 1;
        }
        for (char& c : binary) {
            c = c == '0' ? '1' : '0';
        }
        return stoi(binary, nullptr, 2);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} num
 * @return {number}
 */
var findComplement = function(num) {
    const binary = num.toString(2);
    let complement = '';
    for (const bit of binary) {
        complement += bit === '0' ? '1' : '0';
    }
    return parseInt(complement, 2);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function findComplement(num: number): number {
    const binary = num.toString(2);
    let complement = '';
    for (const bit of binary) {
        complement += bit === '0' ? '1' : '0';
    }
    return parseInt(complement, 2);
}
` },]} />

**Complexity**
- Time: O(log n) - converting to binary and processing each bit
- Space: O(log n) - storing the binary string
- Notes: Less efficient due to string operations but more readable

---
## Mathematical Approach

**Intuition**
The complement of num is equal to (2ᵏ - 1) - num, where k is the number of bits in num.

**Steps**
- Find the smallest power of 2 greater than num
- Subtract 1 to get a mask of all 1s
- Subtract num from this mask to get the complement

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def findComplement(self, num: int) -> int:
        power_of_2 = 1
        while power_of_2 &lt;= num:
            power_of_2 &lt;&lt;= 1
        return power_of_2 - 1 - num
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int findComplement(int num) {
        int powerOf2 = 1;
        while (powerOf2 &lt;= num) {
            powerOf2 &lt;&lt;= 1;
        }
        return powerOf2 - 1 - num;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int findComplement(int num) {
        int powerOf2 = 1;
        while (powerOf2 &lt;= num) {
            powerOf2 &lt;&lt;= 1;
        }
        return powerOf2 - 1 - num;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} num
 * @return {number}
 */
var findComplement = function(num) {
    let powerOf2 = 1;
    while (powerOf2 &lt;= num) {
        powerOf2 &lt;&lt;= 1;
    }
    return powerOf2 - 1 - num;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function findComplement(num: number): number {
    let powerOf2 = 1;
    while (powerOf2 &lt;= num) {
        powerOf2 &lt;&lt;= 1;
    }
    return powerOf2 - 1 - num;
}
` },]} />

**Complexity**
- Time: O(log n) - finding the next power of 2
- Space: O(1) - only using constant extra space
- Notes: Mathematically elegant, equivalent to the mask approach

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Number Complement Solutions","datePublished":"2025-05-24","description":"Find the complement of an integer by flipping all bits in its binary representation.","keywords":"LeetCode, Number Complement, Easy, Bit Manipulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Bit Manipulation with Mask approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(log n) where n is the input number, as we iterate through each bit position to find the most significant bit."}},{"@type":"Question","name":"What is the space complexity of the String Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(log n) because we store the binary string representation of the number."}},{"@type":"Question","name":"Which approach is the most efficient for Number Complement?","acceptedAnswer":{"@type":"Answer","text":"The Bit Manipulation with Mask and Mathematical Approach are the most efficient with O(log n) time and O(1) space complexity."}}]}).replace(/</g,'\u003c')}
</script>