---
title: "1-bit and 2-bit Characters"
summary: "Determine if the last character in a binary array must be a 1-bit character, where 0 is 1-bit and 10/11 are 2-bit."
date: "Aug 31, 2024"
tags:
  - Array
  - Greedy
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 49.60% | **Paid:** No
> **Topics:** Array

We have two special characters:

The first character can be represented by one bit 0.
The second character can be represented by two bits (10 or 11).

Given a binary array bits that ends with 0, return true if the last character must be a one-bit character.

- Examples
- Constraints
- [Linear Scan (Greedy)](#linear-scan-greedy)
- [Count from End](#count-from-end)

## Examples

### Example 1

**Input:**
```text
bits = [1,0,0]
```

**Output:**
```text
true
```

**Explanation:**
The only way to decode it is two-bit character and one-bit character.
So the last character is one-bit character.

### Example 2

**Input:**
```text
bits = [1,1,1,0]
```

**Output:**
```text
false
```

**Explanation:**
The only way to decode it is two-bit character and two-bit character.
So the last character is not one-bit character.

## Constraints

```text
1 <= bits.length <= 1000
bits[i] is either 0 or 1.
```

---

## Linear Scan (Greedy)

**Intuition**
Parse the array from left to right, consuming 1 bit for 0 and 2 bits for 1, then check if we land exactly on the last index.

**Steps**
- Initialize pointer at index 0
- While pointer is before the last element:
  - If current bit is 0, move pointer by 1 (1-bit character)
  - If current bit is 1, move pointer by 2 (2-bit character)
- Return true if pointer equals the last index

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isOneBitCharacter(self, bits: list[int]) -&gt; bool:\n        i = 0\n        n = len(bits)\n        while i &lt; n - 1:\n            if bits[i] == 0:\n                i += 1\n            else:\n                i += 2\n        return i == n - 1\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isOneBitCharacter(int[] bits) {\n        int i = 0;\n        int n = bits.length;\n        while (i &lt; n - 1) {\n            if (bits[i] == 0) {\n                i++;\n            } else {\n                i += 2;\n            }\n        }\n        return i == n - 1;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isOneBitCharacter(vector&lt;int&gt;& bits) {\n        int i = 0;\n        int n = bits.size();\n        while (i &lt; n - 1) {\n            if (bits[i] == 0) {\n                i++;\n            } else {\n                i += 2;\n            }\n        }\n        return i == n - 1;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} bits\n * @return {boolean}\n */\nvar isOneBitCharacter = function(bits) {\n    let i = 0;\n    const n = bits.length;\n    while (i &lt; n - 1) {\n        if (bits[i] === 0) {\n            i++;\n        } else {\n            i += 2;\n        }\n    }\n    return i === n - 1;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function isOneBitCharacter(bits: number[]): boolean {\n    let i = 0;\n    const n = bits.length;\n    while (i &lt; n - 1) {\n        if (bits[i] === 0) {\n            i++;\n        } else {\n            i += 2;\n        }\n    }\n    return i === n - 1;\n}\n" },]} />

**Complexity**
- Time: O(n) - single pass through the array
- Space: O(1) - only using a pointer variable
- Notes: Simple and intuitive approach

---

## Count from End

**Intuition**
Count consecutive 1s before the last 0. If count is even, last 0 is a 1-bit character; if odd, it pairs with a preceding 1.

**Steps**
- Start from second-to-last element and move backward
- Count consecutive 1s until hitting a 0
- Return true if count is even, false if odd

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isOneBitCharacter(self, bits: list[int]) -&gt; bool:\n        n = len(bits)\n        ones = 0\n        for i in range(n - 2, -1, -1):\n            if bits[i] == 1:\n                ones += 1\n            else:\n                break\n        return ones % 2 == 0\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isOneBitCharacter(int[] bits) {\n        int n = bits.length;\n        int ones = 0;\n        for (int i = n - 2; i &gt;= 0; i--) {\n            if (bits[i] == 1) {\n                ones++;\n            } else {\n                break;\n            }\n        }\n        return ones % 2 == 0;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isOneBitCharacter(vector&lt;int&gt;& bits) {\n        int n = bits.size();\n        int ones = 0;\n        for (int i = n - 2; i &gt;= 0; i--) {\n            if (bits[i] == 1) {\n                ones++;\n            } else {\n                break;\n            }\n        }\n        return ones % 2 == 0;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} bits\n * @return {boolean}\n */\nvar isOneBitCharacter = function(bits) {\n    const n = bits.length;\n    let ones = 0;\n    for (let i = n - 2; i &gt;= 0; i--) {\n        if (bits[i] === 1) {\n            ones++;\n        } else {\n            break;\n        }\n    }\n    return ones % 2 === 0;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function isOneBitCharacter(bits: number[]): boolean {\n    const n = bits.length;\n    let ones = 0;\n    for (let i = n - 2; i &gt;= 0; i--) {\n        if (bits[i] === 1) {\n            ones++;\n        } else {\n            break;\n        }\n    }\n    return ones % 2 === 0;\n}\n" },]} />

**Complexity**
- Time: O(n) - worst case scans entire array
- Space: O(1) - only using a counter
- Notes: Elegant mathematical insight; often faster in practice as it may exit early

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode 1-bit and 2-bit Characters Solutions","datePublished":"2024-08-31","description":"Determine if the last character in a binary array must be a 1-bit character, where 0 is 1-bit and 10/11 are 2-bit.","keywords":"LeetCode, 1-bit and 2-bit Characters, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) - single pass through the array"}},{"@type":"Question","name":"What is the space complexity of Count from End approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - only using a counter variable"}},{"@type":"Question","name":"Why does counting consecutive 1s work?","acceptedAnswer":{"@type":"Answer","text":"Each 1 must pair with the next bit. Even count means all 1s pair with each other, leaving the last 0 as a 1-bit character. Odd count means the last 1 pairs with the final 0."}}]}).replace(/</g,'\u003c')}
</script>