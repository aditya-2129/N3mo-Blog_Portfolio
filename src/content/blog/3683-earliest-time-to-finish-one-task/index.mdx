---
title: "Earliest Time to Finish One Task"
summary: "You are given an integer array `tasks`, where `tasks[i]` represents the time required to complete the i-th task."
date: "May 12, 2026"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 84.60% | **Paid:** No

> **Topics:** Array

You are given an integer array `tasks`, where `tasks[i]` represents the time required to complete the i-th task. Your goal is to determine the minimum possible time to finish any single task from the given list. In other words, you need to find the smallest value present in the `tasks` array.

- Examples
- Constraints
- [Linear Scan (Iterative Minimum Finding)](#linear-scan-iterative-minimum-finding)
- [Sorting](#sorting)

## Examples

### Example 1

**Input:**
```text
tasks = [[1,6],[2,3]]
```

**Output:**
```text
5
```

**Explanation:**
The first task starts at time t = 1 and finishes at time 1 + 6 = 7. The second task finishes at time 2 + 3 = 5. You can finish one task at time 5.

### Example 2

**Input:**
```text
tasks = [[100,100],[100,100],[100,100]]
```

**Output:**
```text
200
```

**Explanation:**
All three tasks finish at time 100 + 100 = 200.

## Constraints

```text
- 1 <= tasks.length <= 10⁵
- 1 <= tasks[i] <= 10⁹
```
---

---

## Linear Scan (Iterative Minimum Finding)

**Intuition**

To find the smallest value in an array, we can iterate through each element, keeping track of the minimum value encountered so far.

**Steps**

- Initialize a variable, say `minTime`, with the value of the first element in the `tasks` array. This is safe because the constraints guarantee `tasks.length &gt;= 1`.
- Iterate through the `tasks` array starting from the second element (index 1) up to the end.
- In each iteration, compare the current task's duration (`tasks[i]`) with `minTime`. If `tasks[i]` is smaller than `minTime`, update `minTime` to `tasks[i]`.
- After the loop completes, `minTime` will hold the smallest duration found in the array, which is the earliest time to finish one task.
- Return `minTime`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def earliestTimeToFinishOneTask(self, tasks: list[int]) -> int:
        # Constraints guarantee tasks.length >= 1, so tasks is never empty.
        min_time = tasks[0]
        for i in range(1, len(tasks)):
            if tasks[i] < min_time:
                min_time = tasks[i]
        return min_time` },
{ name: "Java", lang: "java", code: `class Solution {
    public int earliestTimeToFinishOneTask(int[] tasks) {
        // Constraints guarantee tasks.length >= 1, so tasks is never empty.
        int minTime = tasks[0];
        for (int i = 1; i < tasks.length; i++) {
            if (tasks[i] < minTime) {
                minTime = tasks[i];
            }
        }
        return minTime;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <algorithm> // Not strictly needed for manual loop, but useful for std::min

class Solution {
public:
    int earliestTimeToFinishOneTask(std::vector<int>& tasks) {
        // Constraints guarantee tasks.length >= 1, so tasks is never empty.
        int minTime = tasks[0];
        for (size_t i = 1; i < tasks.size(); ++i) {
            if (tasks[i] < minTime) {
                minTime = tasks[i];
            }
        }
        return minTime;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} tasks
 * @return {number}
 */
class Solution {
    earliestTimeToFinishOneTask(tasks) {
        // Constraints guarantee tasks.length >= 1, so tasks is never empty.
        let minTime = tasks[0];
        for (let i = 1; i < tasks.length; i++) {
            if (tasks[i] < minTime) {
                minTime = tasks[i];
            }
        }
        return minTime;
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    earliestTimeToFinishOneTask(tasks: number[]): number {
        // Constraints guarantee tasks.length >= 1, so tasks is never empty.
        let minTime: number = tasks[0];
        for (let i = 1; i < tasks.length; i++) {
            if (tasks[i] < minTime) {
                minTime = tasks[i];
            }
        }
        return minTime;
    }
}` }]} />

**Complexity**

- Time: O(N), where N is the number of tasks. We iterate through the array once to find the minimum element.
- Space: O(1). We use a constant amount of extra space for variables like `minTime` and the loop index.
- Notes: This approach is optimal in terms of time complexity because we must examine every task at least once to guarantee finding the minimum value.

---

## Sorting

**Intuition**

If the array of task durations is sorted in non-decreasing order, the smallest duration will always be the first element.

**Steps**

- Sort the `tasks` array in non-decreasing (ascending) order.
- After sorting, the earliest time to finish one task will be the element at index 0 of the sorted array.
- Return `tasks[0]`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def earliestTimeToFinishOneTask(self, tasks: list[int]) -> int:
        # Constraints guarantee tasks.length >= 1, so tasks is never empty.
        tasks.sort() # Sorts the list in-place
        return tasks[0]` },
{ name: "Java", lang: "java", code: `import java.util.Arrays;

class Solution {
    public int earliestTimeToFinishOneTask(int[] tasks) {
        // Constraints guarantee tasks.length >= 1, so tasks is never empty.
        Arrays.sort(tasks);
        return tasks[0];
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <algorithm> // For std::sort

class Solution {
public:
    int earliestTimeToFinishOneTask(std::vector<int>& tasks) {
        // Constraints guarantee tasks.length >= 1, so tasks is never empty.
        std::sort(tasks.begin(), tasks.end());
        return tasks[0];
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} tasks
 * @return {number}
 */
class Solution {
    earliestTimeToFinishOneTask(tasks) {
        // Constraints guarantee tasks.length >= 1, so tasks is never empty.
        // The default sort converts elements to strings and compares them.
        // For numbers, a custom comparator is needed to sort numerically.
        tasks.sort((a, b) => a - b);
        return tasks[0];
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    earliestTimeToFinishOneTask(tasks: number[]): number {
        // Constraints guarantee tasks.length >= 1, so tasks is never empty.
        // The default sort converts elements to strings and compares them.
        // For numbers, a custom comparator is needed to sort numerically.
        tasks.sort((a, b) => a - b);
        return tasks[0];
    }
}` }]} />

**Complexity**

- Time: O(N log N), where N is the number of tasks. This is due to the time complexity of most comparison-based sorting algorithms.
- Space: O(log N) or O(N), depending on the specific sorting algorithm implementation. Some in-place sorts might use O(log N) for recursion stack, while others like Timsort (used in Python) or MergeSort might use O(N) auxiliary space in the worst case.
- Notes: While correct and straightforward, this approach is generally less efficient than the linear scan for this particular problem. Sorting performs more operations than strictly necessary, as we only need the minimum element, not the full sorted order.


<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Earliest Time to Finish One Task Solutions","datePublished":"2026-05-12T23:08:16.774336","description":"You are given an integer array `tasks`, where `tasks[i]` represents the time required to complete the i-th task.","keywords":"LeetCode, Earliest Time to Finish One Task, Easy, Array, Hash Map, Brute Force, Array"}).replace(/</g,'\\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"Linear Scan (Iterative Minimum Finding) complexity?","acceptedAnswer":{"@type":"Answer","text":"Time: O(N), where N is the number of tasks. We iterate through the array once to find the minimum element.; Space: O(1). We use a constant amount of extra space for variables like `minTime` and the loop index.. This approach is optimal in terms of time complexity because we must examine every task at least once to guarantee finding the minimum value."}},{"@type":"Question","name":"Sorting complexity?","acceptedAnswer":{"@type":"Answer","text":"Time: O(N log N), where N is the number of tasks. This is due to the time complexity of most comparison-based sorting algorithms.; Space: O(log N) or O(N), depending on the specific sorting algorithm implementation. Some in-place sorts might use O(log N) for recursion stack, while others like Timsort (used in Python) or MergeSort might use O(N) auxiliary space in the worst case.. While correct and straightforward, this approach is generally less efficient than the linear scan for this particular problem. Sorting performs more operations than strictly necessary, as we only need the minimum element, not the full sorted order."}}]}).replace(/</g,'\\u003c')}
</script>
