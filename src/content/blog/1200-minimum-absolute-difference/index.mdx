---
title: "Minimum Absolute Difference"
summary: "Given an array of distinct integers, find all pairs of elements with the minimum absolute difference."
date: "Nov 10, 2024"
tags:
  - Array
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 75.10% | **Paid:** No
> **Topics:** Array, Sorting

Given an array of distinct integers arr, find all pairs of elements with the minimum absolute difference of any two elements.

Return a list of pairs in ascending order (with respect to each pair) and sorted in ascending order by the first element.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Sorting](#sorting)

## Examples

**Example 1:**

```text
Input: arr = [4,2,1,3]
Output: [[1,2],[2,3],[3,4]]
Explanation: The minimum absolute difference is 1. List all pairs with difference 1 in ascending order.
```

**Example 2:**

```text
Input: arr = [1,3,6,10,15]
Output: [[1,3]]
```

**Example 3:**

```text
Input: arr = [3,8,-10,23,19,-4,-14,27]
Output: [[-14,-10],[19,23],[23,27]]
```

## Constraints

```text
2 <= arr.length <= 10^5
-10^6 <= arr[i] <= 10^6
```

---

## Brute Force

**Intuition**
Compare every possible pair of elements in the array to find the smallest absolute difference. Then, iterate through all pairs again to collect those that match this minimum difference.

**Steps**
- Initialize `min_diff` to infinity.
- Use two nested loops to calculate the absolute difference between every pair `(i, j)` where `i &lt; j`. Update `min_diff` if a smaller difference is found.
- Initialize an empty result list.
- Use two nested loops again to find all pairs where the absolute difference equals `min_diff`.
- Sort the result list to satisfy the output ordering requirements.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumAbsDifference(self, arr: list[int]) -&gt; list[list[int]]:\n        n = len(arr)\n        min_diff = float('inf')\n        \n        # Find the minimum absolute difference\n        for i in range(n):\n            for j in range(i + 1, n):\n                diff = abs(arr[i] - arr[j])\n                if diff &lt; min_diff:\n                    min_diff = diff\n        \n        res = []\n        # Collect all pairs with the minimum difference\n        for i in range(n):\n            for j in range(i + 1, n):\n                if abs(arr[i] - arr[j]) == min_diff:\n                    if arr[i] &lt; arr[j]:\n                        res.append([arr[i], arr[j]])\n                    else:\n                        res.append([arr[j], arr[i]])\n        \n        res.sort()\n        return res" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public List&lt;List&lt;Integer&gt;&gt; minimumAbsDifference(int[] arr) {\n        int n = arr.length;\n        int minDiff = Integer.MAX_VALUE;\n        \n        // Find the minimum absolute difference\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int diff = Math.abs(arr[i] - arr[j]);\n                if (diff &lt; minDiff) {\n                    minDiff = diff;\n                }\n            }\n        }\n        \n        List&lt;List&lt;Integer&gt;&gt; res = new ArrayList&lt;&gt;();\n        // Collect all pairs with the minimum difference\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                if (Math.abs(arr[i] - arr[j]) == minDiff) {\n                    List&lt;Integer&gt; pair = new ArrayList&lt;&gt;();\n                    pair.add(Math.min(arr[i], arr[j]));\n                    pair.add(Math.max(arr[i], arr[j]));\n                    res.add(pair);\n                }\n            }\n        }\n        \n        res.sort((a, b) -&gt; a.get(0) - b.get(0));\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n#include &lt;climits&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;vector&lt;int&gt;&gt; minimumAbsDifference(vector&lt;int&gt;& arr) {\n        int n = arr.size();\n        int minDiff = INT_MAX;\n        \n        // Find the minimum absolute difference\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int diff = abs(arr[i] - arr[j]);\n                if (diff &lt; minDiff) {\n                    minDiff = diff;\n                }\n            }\n        }\n        \n        vector&lt;vector&lt;int&gt;&gt; res;\n        // Collect all pairs with the minimum difference\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                if (abs(arr[i] - arr[j]) == minDiff) {\n                    if (arr[i] &lt; arr[j]) {\n                        res.push_back({arr[i], arr[j]});\n                    } else {\n                        res.push_back({arr[j], arr[i]});\n                    }\n                }\n            }\n        }\n        \n        sort(res.begin(), res.end());\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var minimumAbsDifference = function(arr) {\n    let n = arr.length;\n    let minDiff = Infinity;\n    \n    // Find the minimum absolute difference\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            let diff = Math.abs(arr[i] - arr[j]);\n            if (diff &lt; minDiff) {\n                minDiff = diff;\n            }\n        }\n    }\n    \n    let res = [];\n    // Collect all pairs with the minimum difference\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            if (Math.abs(arr[i] - arr[j]) === minDiff) {\n                let pair = [Math.min(arr[i], arr[j]), Math.max(arr[i], arr[j])];\n                res.push(pair);\n            }\n        }\n    }\n    \n    res.sort((a, b) =&gt; a[0] - b[0]);\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumAbsDifference(arr: number[]): number[][] {\n    let n = arr.length;\n    let minDiff = Infinity;\n    \n    // Find the minimum absolute difference\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            let diff = Math.abs(arr[i] - arr[j]);\n            if (diff &lt; minDiff) {\n                minDiff = diff;\n            }\n        }\n    }\n    \n    let res: number[][] = [];\n    // Collect all pairs with the minimum difference\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            if (Math.abs(arr[i] - arr[j]) === minDiff) {\n                let pair = [Math.min(arr[i], arr[j]), Math.max(arr[i], arr[j])];\n                res.push(pair);\n            }\n        }\n    }\n    \n    res.sort((a, b) =&gt; a[0] - b[0]);\n    return res;\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1) (excluding output space)
- Notes: This approach is simple but inefficient for large inputs due to the nested loops.

---

## Sorting

**Intuition**
If the array is sorted, the minimum absolute difference between any two elements must occur between adjacent elements. This allows us to find the minimum difference in a single pass.

**Steps**
- Sort the array in ascending order.
- Initialize `min_diff` to infinity.
- Iterate through the sorted array once to find the minimum difference between adjacent elements (`arr[i+1] - arr[i]`).
- Iterate through the array a second time. If the difference between adjacent elements equals `min_diff`, add the pair `[arr[i], arr[i+1]]` to the result list.
- Since the array is sorted, the result list will automatically be in the correct order.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumAbsDifference(self, arr: list[int]) -&gt; list[list[int]]:\n        arr.sort()\n        min_diff = float('inf')\n        \n        # Find the minimum difference between adjacent elements\n        for i in range(len(arr) - 1):\n            diff = arr[i+1] - arr[i]\n            if diff &lt; min_diff:\n                min_diff = diff\n        \n        res = []\n        # Collect pairs with the minimum difference\n        for i in range(len(arr) - 1):\n            if arr[i+1] - arr[i] == min_diff:\n                res.append([arr[i], arr[i+1]])\n        \n        return res" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public List&lt;List&lt;Integer&gt;&gt; minimumAbsDifference(int[] arr) {\n        Arrays.sort(arr);\n        int minDiff = Integer.MAX_VALUE;\n        \n        // Find the minimum difference between adjacent elements\n        for (int i = 0; i &lt; arr.length - 1; i++) {\n            int diff = arr[i + 1] - arr[i];\n            if (diff &lt; minDiff) {\n                minDiff = diff;\n            }\n        }\n        \n        List&lt;List&lt;Integer&gt;&gt; res = new ArrayList&lt;&gt;();\n        // Collect pairs with the minimum difference\n        for (int i = 0; i &lt; arr.length - 1; i++) {\n            if (arr[i + 1] - arr[i] == minDiff) {\n                res.add(Arrays.asList(arr[i], arr[i + 1]));\n            }\n        }\n        \n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n#include &lt;climits&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;vector&lt;int&gt;&gt; minimumAbsDifference(vector&lt;int&gt;& arr) {\n        sort(arr.begin(), arr.end());\n        int minDiff = INT_MAX;\n        \n        // Find the minimum difference between adjacent elements\n        for (int i = 0; i &lt; arr.size() - 1; i++) {\n            int diff = arr[i + 1] - arr[i];\n            if (diff &lt; minDiff) {\n                minDiff = diff;\n            }\n        }\n        \n        vector&lt;vector&lt;int&gt;&gt; res;\n        // Collect pairs with the minimum difference\n        for (int i = 0; i &lt; arr.size() - 1; i++) {\n            if (arr[i + 1] - arr[i] == minDiff) {\n                res.push_back({arr[i], arr[i + 1]});\n            }\n        }\n        \n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var minimumAbsDifference = function(arr) {\n    arr.sort((a, b) =&gt; a - b);\n    let minDiff = Infinity;\n    \n    // Find the minimum difference between adjacent elements\n    for (let i = 0; i &lt; arr.length - 1; i++) {\n        let diff = arr[i + 1] - arr[i];\n        if (diff &lt; minDiff) {\n            minDiff = diff;\n        }\n    }\n    \n    let res = [];\n    // Collect pairs with the minimum difference\n    for (let i = 0; i &lt; arr.length - 1; i++) {\n        if (arr[i + 1] - arr[i] === minDiff) {\n            res.push([arr[i], arr[i + 1]]);\n        }\n    }\n    \n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumAbsDifference(arr: number[]): number[][] {\n    arr.sort((a, b) =&gt; a - b);\n    let minDiff = Infinity;\n    \n    // Find the minimum difference between adjacent elements\n    for (let i = 0; i &lt; arr.length - 1; i++) {\n        let diff = arr[i + 1] - arr[i];\n        if (diff &lt; minDiff) {\n            minDiff = diff;\n        }\n    }\n    \n    let res: number[][] = [];\n    // Collect pairs with the minimum difference\n    for (let i = 0; i &lt; arr.length - 1; i++) {\n        if (arr[i + 1] - arr[i] === minDiff) {\n            res.push([arr[i], arr[i + 1]]);\n        }\n    }\n    \n    return res;\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(1) or O(n) depending on the sorting algorithm's space complexity.
- Notes: This is the optimal approach for this problem.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Absolute Difference Solutions","datePublished":"2024-11-10","description":"Given an array of distinct integers, find all pairs of elements with the minimum absolute difference.","keywords":"LeetCode, Minimum Absolute Difference, Easy, Array, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n²), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the time complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Sorting approach is O(n log n) due to the sorting step."}},{"@type":"Question","name":"Why is sorting the array helpful for this problem?","acceptedAnswer":{"@type":"Answer","text":"In a sorted array, the minimum absolute difference between any two elements must occur between adjacent elements, allowing us to find the solution in linear time after sorting."}}]}).replace(/</g,'\u003c')}
</script>