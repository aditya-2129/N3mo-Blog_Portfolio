---
title: "Climbing Stairs"
summary: "Count distinct ways to climb n steps taking 1 or 2 steps at a time."
date: "May 12, 2026"
tags:
  - Math
  - Dynamic Programming
  - Memoization
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 54.10% | **Paid:** No
> **Topics:** Math, Dynamic Programming, Memoization

You are climbing a staircase. It takes n steps to reach the top.

Each time you can either climb 1 or 2 steps. In how many distinct ways can you climb to the top?

- Examples
- Constraints
- [Brute Force Recursion](#brute-force-recursion)
- [Recursion with Memoization](#recursion-with-memoization)
- [Dynamic Programming](#dynamic-programming)
- [Space Optimized DP](#space-optimized-dp)
- [Matrix Exponentiation](#matrix-exponentiation)
- [Binet's Formula](#binets-formula)

## Examples

Example 1:
```text
Input: n = 2
Output: 2
Explanation: There are two ways to climb to the top.
1. 1 step + 1 step
2. 2 steps
```

Example 2:
```text
Input: n = 3
Output: 3
Explanation: There are three ways to climb to the top.
1. 1 step + 1 step + 1 step
2. 1 step + 2 steps
3. 2 steps + 1 step
```

## Constraints
```text
1 <= n <= 45
```

---
## Brute Force Recursion

**Intuition**
At each step, we have two choices: take 1 step or take 2 steps. We recursively explore both paths and sum the results.

**Steps**
- Base case: if n is 1 or 2, return n (only 1 way for 1 step, 2 ways for 2 steps)
- Recursive case: return climbStairs(n-1) + climbStairs(n-2)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def climbStairs(self, n: int) -&gt; int:\n        if n &lt;= 2:\n            return n\n        return self.climbStairs(n - 1) + self.climbStairs(n - 2)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int climbStairs(int n) {\n        if (n &lt;= 2) {\n            return n;\n        }\n        return climbStairs(n - 1) + climbStairs(n - 2);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int climbStairs(int n) {\n        if (n &lt;= 2) {\n            return n;\n        }\n        return climbStairs(n - 1) + climbStairs(n - 2);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar climbStairs = function(n) {\n    if (n &lt;= 2) {\n        return n;\n    }\n    return climbStairs(n - 1) + climbStairs(n - 2);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function climbStairs(n: number): number {\n    if (n &lt;= 2) {\n        return n;\n    }\n    return climbStairs(n - 1) + climbStairs(n - 2);\n}" },]} />

**Complexity**
- Time: O(2ⁿ) - exponential due to repeated subproblems
- Space: O(n) - recursion stack depth
- Notes: Simple but inefficient for larger n due to redundant calculations

---
## Recursion with Memoization

**Intuition**
Store computed results to avoid redundant calculations. Each subproblem is solved only once.

**Steps**
- Create a memoization structure (array/hashmap)
- Before computing, check if result exists in memo
- After computing, store result in memo before returning

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def climbStairs(self, n: int) -&gt; int:\n        memo = {}\n        \n        def dfs(k):\n            if k &lt;= 2:\n                return k\n            if k in memo:\n                return memo[k]\n            memo[k] = dfs(k - 1) + dfs(k - 2)\n            return memo[k]\n        \n        return dfs(n)" },
{ name: "Java", lang: "java", code: "class Solution {\n    private int[] memo;\n    \n    public int climbStairs(int n) {\n        memo = new int[n + 1];\n        return dfs(n);\n    }\n    \n    private int dfs(int k) {\n        if (k &lt;= 2) {\n            return k;\n        }\n        if (memo[k] != 0) {\n            return memo[k];\n        }\n        memo[k] = dfs(k - 1) + dfs(k - 2);\n        return memo[k];\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\nprivate:\n    vector&lt;int&gt; memo;\n    \n    int dfs(int k) {\n        if (k &lt;= 2) {\n            return k;\n        }\n        if (memo[k] != 0) {\n            return memo[k];\n        }\n        memo[k] = dfs(k - 1) + dfs(k - 2);\n        return memo[k];\n    }\n    \npublic:\n    int climbStairs(int n) {\n        memo = vector&lt;int&gt;(n + 1, 0);\n        return dfs(n);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar climbStairs = function(n) {\n    const memo = {};\n    \n    function dfs(k) {\n        if (k &lt;= 2) {\n            return k;\n        }\n        if (memo[k] !== undefined) {\n            return memo[k];\n        }\n        memo[k] = dfs(k - 1) + dfs(k - 2);\n        return memo[k];\n    }\n    \n    return dfs(n);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function climbStairs(n: number): number {\n    const memo: Record&lt;number, number&gt; = {};\n    \n    function dfs(k: number): number {\n        if (k &lt;= 2) {\n            return k;\n        }\n        if (memo[k] !== undefined) {\n            return memo[k];\n        }\n        memo[k] = dfs(k - 1) + dfs(k - 2);\n        return memo[k];\n    }\n    \n    return dfs(n);\n}" },]} />

**Complexity**
- Time: O(n) - each subproblem computed once
- Space: O(n) - memo array + recursion stack
- Notes: Top-down DP approach, eliminates redundant work

---
## Dynamic Programming

**Intuition**
Build solution bottom-up. Each step's ways depend only on the previous two steps.

**Steps**
- Initialize dp array with base cases: dp[1] = 1, dp[2] = 2
- Iterate from 3 to n, computing dp[i] = dp[i-1] + dp[i-2]
- Return dp[n]

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def climbStairs(self, n: int) -&gt; int:\n        if n &lt;= 2:\n            return n\n        dp = [0] * (n + 1)\n        dp[1] = 1\n        dp[2] = 2\n        for i in range(3, n + 1):\n            dp[i] = dp[i - 1] + dp[i - 2]\n        return dp[n]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int climbStairs(int n) {\n        if (n &lt;= 2) {\n            return n;\n        }\n        int[] dp = new int[n + 1];\n        dp[1] = 1;\n        dp[2] = 2;\n        for (int i = 3; i &lt;= n; i++) {\n            dp[i] = dp[i - 1] + dp[i - 2];\n        }\n        return dp[n];\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int climbStairs(int n) {\n        if (n &lt;= 2) {\n            return n;\n        }\n        vector&lt;int&gt; dp(n + 1, 0);\n        dp[1] = 1;\n        dp[2] = 2;\n        for (int i = 3; i &lt;= n; i++) {\n            dp[i] = dp[i - 1] + dp[i - 2];\n        }\n        return dp[n];\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar climbStairs = function(n) {\n    if (n &lt;= 2) {\n        return n;\n    }\n    const dp = new Array(n + 1).fill(0);\n    dp[1] = 1;\n    dp[2] = 2;\n    for (let i = 3; i &lt;= n; i++) {\n        dp[i] = dp[i - 1] + dp[i - 2];\n    }\n    return dp[n];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function climbStairs(n: number): number {\n    if (n &lt;= 2) {\n        return n;\n    }\n    const dp: number[] = new Array(n + 1).fill(0);\n    dp[1] = 1;\n    dp[2] = 2;\n    for (let i = 3; i &lt;= n; i++) {\n        dp[i] = dp[i - 1] + dp[i - 2];\n    }\n    return dp[n];\n}" },]} />

**Complexity**
- Time: O(n) - single pass through all steps
- Space: O(n) - dp array stores all intermediate results
- Notes: Bottom-up DP, intuitive and easy to understand

---
## Space Optimized DP

**Intuition**
Only the last two values are needed to compute the next one. Eliminate the array entirely.

**Steps**
- Handle base cases directly
- Maintain two variables for previous two results
- Update variables iteratively, shifting values forward

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def climbStairs(self, n: int) -&gt; int:\n        if n &lt;= 2:\n            return n\n        prev1 = 1\n        prev2 = 2\n        for i in range(3, n + 1):\n            curr = prev1 + prev2\n            prev1 = prev2\n            prev2 = curr\n        return prev2" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int climbStairs(int n) {\n        if (n &lt;= 2) {\n            return n;\n        }\n        int prev1 = 1;\n        int prev2 = 2;\n        for (int i = 3; i &lt;= n; i++) {\n            int curr = prev1 + prev2;\n            prev1 = prev2;\n            prev2 = curr;\n        }\n        return prev2;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int climbStairs(int n) {\n        if (n &lt;= 2) {\n            return n;\n        }\n        int prev1 = 1;\n        int prev2 = 2;\n        for (int i = 3; i &lt;= n; i++) {\n            int curr = prev1 + prev2;\n            prev1 = prev2;\n            prev2 = curr;\n        }\n        return prev2;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar climbStairs = function(n) {\n    if (n &lt;= 2) {\n        return n;\n    }\n    let prev1 = 1;\n    let prev2 = 2;\n    for (let i = 3; i &lt;= n; i++) {\n        let curr = prev1 + prev2;\n        prev1 = prev2;\n        prev2 = curr;\n    }\n    return prev2;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function climbStairs(n: number): number {\n    if (n &lt;= 2) {\n        return n;\n    }\n    let prev1: number = 1;\n    let prev2: number = 2;\n    for (let i = 3; i &lt;= n; i++) {\n        let curr: number = prev1 + prev2;\n        prev1 = prev2;\n        prev2 = curr;\n    }\n    return prev2;\n}" },]} />

**Complexity**
- Time: O(n) - single pass through all steps
- Space: O(1) - only two variables used
- Notes: Optimal for this problem, most commonly used in interviews

---
## Matrix Exponentiation

**Intuition**
The recurrence relation can be represented as matrix multiplication. Using fast exponentiation, we compute the result in logarithmic time.

**Steps**
- Represent the recurrence as [[1,1],[1,0]] matrix
- Use binary exponentiation to raise matrix to power n
- Extract result from matrix[0][0]

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def climbStairs(self, n: int) -&gt; int:\n        if n &lt;= 2:\n            return n\n        \n        def multiply(a, b):\n            return [\n                [a[0][0] * b[0][0] + a[0][1] * b[1][0], a[0][0] * b[0][1] + a[0][1] * b[1][1]],\n                [a[1][0] * b[0][0] + a[1][1] * b[1][0], a[1][0] * b[0][1] + a[1][1] * b[1][1]]\n            ]\n        \n        def power(matrix, exp):\n            result = [[1, 0], [0, 1]]\n            while exp &gt; 0:\n                if exp % 2 == 1:\n                    result = multiply(result, matrix)\n                matrix = multiply(matrix, matrix)\n                exp //= 2\n            return result\n        \n        base = [[1, 1], [1, 0]]\n        result = power(base, n)\n        return result[0][0]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int climbStairs(int n) {\n        if (n &lt;= 2) {\n            return n;\n        }\n        long[][] result = power(new long[][]{{1, 1}, {1, 0}}, n);\n        return (int) result[0][0];\n    }\n    \n    private long[][] multiply(long[][] a, long[][] b) {\n        return new long[][]{\n            {a[0][0] * b[0][0] + a[0][1] * b[1][0], a[0][0] * b[0][1] + a[0][1] * b[1][1]},\n            {a[1][0] * b[0][0] + a[1][1] * b[1][0], a[1][0] * b[0][1] + a[1][1] * b[1][1]}\n        };\n    }\n    \n    private long[][] power(long[][] matrix, int exp) {\n        long[][] result = {{1, 0}, {0, 1}};\n        while (exp &gt; 0) {\n            if ((exp & 1) == 1) {\n                result = multiply(result, matrix);\n            }\n            matrix = multiply(matrix, matrix);\n            exp &gt;&gt;= 1;\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int climbStairs(int n) {\n        if (n &lt;= 2) {\n            return n;\n        }\n        vector&lt;vector&lt;long long&gt;&gt; result = power({{1, 1}, {1, 0}}, n);\n        return (int) result[0][0];\n    }\n    \nprivate:\n    vector&lt;vector&lt;long long&gt;&gt; multiply(vector&lt;vector&lt;long long&gt;&gt;& a, vector&lt;vector&lt;long long&gt;&gt;& b) {\n        return {\n            {a[0][0] * b[0][0] + a[0][1] * b[1][0], a[0][0] * b[0][1] + a[0][1] * b[1][1]},\n            {a[1][0] * b[0][0] + a[1][1] * b[1][0], a[1][0] * b[0][1] + a[1][1] * b[1][1]}\n        };\n    }\n    \n    vector&lt;vector&lt;long long&gt;&gt; power(vector&lt;vector&lt;long long&gt;&gt; matrix, int exp) {\n        vector&lt;vector&lt;long long&gt;&gt; result = {{1, 0}, {0, 1}};\n        while (exp &gt; 0) {\n            if (exp & 1) {\n                result = multiply(result, matrix);\n            }\n            matrix = multiply(matrix, matrix);\n            exp &gt;&gt;= 1;\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar climbStairs = function(n) {\n    if (n &lt;= 2) {\n        return n;\n    }\n    \n    function multiply(a, b) {\n        return [\n            [a[0][0] * b[0][0] + a[0][1] * b[1][0], a[0][0] * b[0][1] + a[0][1] * b[1][1]],\n            [a[1][0] * b[0][0] + a[1][1] * b[1][0], a[1][0] * b[0][1] + a[1][1] * b[1][1]]\n        ];\n    }\n    \n    function power(matrix, exp) {\n        let result = [[1, 0], [0, 1]];\n        while (exp &gt; 0) {\n            if (exp % 2 === 1) {\n                result = multiply(result, matrix);\n            }\n            matrix = multiply(matrix, matrix);\n            exp = Math.floor(exp / 2);\n        }\n        return result;\n    }\n    \n    const base = [[1, 1], [1, 0]];\n    const result = power(base, n);\n    return result[0][0];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function climbStairs(n: number): number {\n    if (n &lt;= 2) {\n        return n;\n    }\n    \n    function multiply(a: number[][], b: number[][]): number[][] {\n        return [\n            [a[0][0] * b[0][0] + a[0][1] * b[1][0], a[0][0] * b[0][1] + a[0][1] * b[1][1]],\n            [a[1][0] * b[0][0] + a[1][1] * b[1][0], a[1][0] * b[0][1] + a[1][1] * b[1][1]]\n        ];\n    }\n    \n    function power(matrix: number[][], exp: number): number[][] {\n        let result: number[][] = [[1, 0], [0, 1]];\n        while (exp &gt; 0) {\n            if (exp % 2 === 1) {\n                result = multiply(result, matrix);\n            }\n            matrix = multiply(matrix, matrix);\n            exp = Math.floor(exp / 2);\n        }\n        return result;\n    }\n    \n    const base: number[][] = [[1, 1], [1, 0]];\n    const result: number[][] = power(base, n);\n    return result[0][0];\n}" },]} />

**Complexity**
- Time: O(log n) - binary exponentiation halves exponent each step
- Space: O(1) - constant space for matrix operations
- Notes: Overkill for n ≤ 45, but useful for very large n constraints

---
## Binet's Formula

**Intuition**
Fibonacci numbers have a closed-form solution using the golden ratio. Direct computation yields O(1) time.

**Steps**
- Calculate golden ratio φ = (1 + √5) / 2
- Apply Binet's formula: result = round(φ^(n+1) / √5)
- Return the rounded integer

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def climbStairs(self, n: int) -&gt; int:\n        import math\n        sqrt5 = math.sqrt(5)\n        phi = (1 + sqrt5) / 2\n        psi = (1 - sqrt5) / 2\n        return int(round((phi ** (n + 1) - psi ** (n + 1)) / sqrt5))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int climbStairs(int n) {\n        double sqrt5 = Math.sqrt(5);\n        double phi = (1 + sqrt5) / 2;\n        double psi = (1 - sqrt5) / 2;\n        return (int) Math.round((Math.pow(phi, n + 1) - Math.pow(psi, n + 1)) / sqrt5);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int climbStairs(int n) {\n        double sqrt5 = sqrt(5);\n        double phi = (1 + sqrt5) / 2;\n        double psi = (1 - sqrt5) / 2;\n        return (int) round((pow(phi, n + 1) - pow(psi, n + 1)) / sqrt5);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar climbStairs = function(n) {\n    const sqrt5 = Math.sqrt(5);\n    const phi = (1 + sqrt5) / 2;\n    const psi = (1 - sqrt5) / 2;\n    return Math.round((Math.pow(phi, n + 1) - Math.pow(psi, n + 1)) / sqrt5);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function climbStairs(n: number): number {\n    const sqrt5: number = Math.sqrt(5);\n    const phi: number = (1 + sqrt5) / 2;\n    const psi: number = (1 - sqrt5) / 2;\n    return Math.round((Math.pow(phi, n + 1) - Math.pow(psi, n + 1)) / sqrt5);\n}" },]} />

**Complexity**
- Time: O(1) - constant time arithmetic operations
- Space: O(1) - no additional storage
- Notes: Elegant mathematical solution, but floating-point precision issues for very large n

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Climbing Stairs Solutions","datePublished":"2026-05-12","description":"Count distinct ways to climb n steps taking 1 or 2 steps at a time.","keywords":"LeetCode, Climbing Stairs, Easy, Math, Dynamic Programming, Memoization"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Brute Force Recursion?","acceptedAnswer":{"@type":"Answer","text":"O(2ⁿ) - exponential due to repeated subproblems"}},{"@type":"Question","name":"What is the time complexity of Recursion with Memoization?","acceptedAnswer":{"@type":"Answer","text":"O(n) - each subproblem computed once"}},{"@type":"Question","name":"What is the time complexity of Dynamic Programming?","acceptedAnswer":{"@type":"Answer","text":"O(n) - single pass through all steps"}},{"@type":"Question","name":"What is the time complexity of Space Optimized DP?","acceptedAnswer":{"@type":"Answer","text":"O(n) - single pass through all steps"}},{"@type":"Question","name":"What is the time complexity of Matrix Exponentiation?","acceptedAnswer":{"@type":"Answer","text":"O(log n) - binary exponentiation halves exponent each step"}},{"@type":"Question","name":"What is the time complexity of Binet's Formula?","acceptedAnswer":{"@type":"Answer","text":"O(1) - constant time arithmetic operations"}}]}).replace(/</g,'\u003c')}
</script>