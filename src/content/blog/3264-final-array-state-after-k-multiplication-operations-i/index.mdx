---
title: "Final Array State After K Multiplication Operations I"
summary: "Perform k operations replacing the smallest element with its product by multiplier."
date: "Oct 07, 2025"
tags:
  - Array
  - Math
  - Heap (Priority Queue)
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 86.90% | **Paid:** No
> **Topics:** Array, Math, Heap (Priority Queue), Simulation

You are given an integer array `nums`, an integer `multiplier`, and an integer `k`.

You can perform the following operation `k` times:

*   Find the minimum value `x` in `nums`. If there are multiple occurrences of the minimum value, select the one with the smallest index.
*   Replace the selected element with `x * multiplier`.

Return the final state of `nums` after performing all `k` operations.

- Examples
- Constraints
- [Simulation](#simulation)
- [Min-Heap](#min-heap)

## Examples

```text
Input: nums = [2,1,3,5,6], k = 5, multiplier = 2
Output: [8,4,6,5,6]
Explanation:
Operation 1: Select 1 (index 1) -> [2, 2, 3, 5, 6]
Operation 2: Select 2 (index 0) -> [4, 2, 3, 5, 6]
Operation 3: Select 2 (index 1) -> [4, 4, 3, 5, 6]
Operation 4: Select 3 (index 2) -> [4, 4, 6, 5, 6]
Operation 5: Select 4 (index 0) -> [8, 4, 6, 5, 6]
```

```text
Input: nums = [1,2], k = 3, multiplier = 4
Output: [16,8]
Explanation:
Operation 1: Select 1 (index 0) -> [4, 2]
Operation 2: Select 2 (index 1) -> [4, 8]
Operation 3: Select 4 (index 0) -> [16, 8]
```

## Constraints

```text
- 1 <= nums.length <= 100
- 1 <= nums[i] <= 100
- 1 <= k <= 10
- 1 <= multiplier <= 5
```

---
## Simulation

**Intuition**
Since the constraints for this specific problem (I) are very small (length and k are at most 50), we can simply simulate the process step-by-step. We iterate `k` times, and in each iteration, we scan the array to find the minimum element and its index, then update it.

**Steps**
- Loop `k` times.
- Inside the loop, iterate through `nums` to find the smallest value. If values are equal, keep the one with the smaller index.
- Multiply the found value by `multiplier`.
- Return the modified `nums`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def getFinalState(self, nums: List[int], k: int, multiplier: int) -&gt; List[int]:\r\n        n = len(nums)\r\n        for _ in range(k):\r\n            min_val = float('inf')\r\n            min_idx = -1\r\n            for i in range(n):\r\n                if nums[i] &lt; min_val:\r\n                    min_val = nums[i]\r\n                    min_idx = i\r\n            nums[min_idx] *= multiplier\r\n        return nums" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int[] getFinalState(int[] nums, int k, int multiplier) {\r\n        int n = nums.length;\r\n        for (int i = 0; i &lt; k; i++) {\r\n            int minVal = Integer.MAX_VALUE;\r\n            int minIdx = -1;\r\n            for (int j = 0; j &lt; n; j++) {\r\n                if (nums[j] &lt; minVal) {\r\n                    minVal = nums[j];\r\n                    minIdx = j;\r\n                }\r\n            }\r\n            nums[minIdx] *= multiplier;\r\n        }\r\n        return nums;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;climits&gt;\r\n\r\nusing namespace std;\r\n\r\nclass Solution {\r\npublic:\r\n    vector&lt;int&gt; getFinalState(vector&lt;int&gt;& nums, int k, int multiplier) {\r\n        int n = nums.size();\r\n        for (int i = 0; i &lt; k; ++i) {\r\n            int minVal = INT_MAX;\r\n            int minIdx = -1;\r\n            for (int j = 0; j &lt; n; ++j) {\r\n                if (nums[j] &lt; minVal) {\r\n                    minVal = nums[j];\r\n                    minIdx = j;\r\n                }\r\n            }\r\n            nums[minIdx] *= multiplier;\r\n        }\r\n        return nums;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "var getFinalState = function(nums, k, multiplier) {\r\n    const n = nums.length;\r\n    for (let i = 0; i &lt; k; i++) {\r\n        let minVal = Infinity;\r\n        let minIdx = -1;\r\n        for (let j = 0; j &lt; n; j++) {\r\n            if (nums[j] &lt; minVal) {\r\n                minVal = nums[j];\r\n                minIdx = j;\r\n            }\r\n        }\r\n        nums[minIdx] *= multiplier;\r\n    }\r\n    return nums;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function getFinalState(nums: number[], k: number, multiplier: number): number[] {\r\n    const n = nums.length;\r\n    for (let i = 0; i &lt; k; i++) {\r\n        let minVal = Infinity;\r\n        let minIdx = -1;\r\n        for (let j = 0; j &lt; n; j++) {\r\n            if (nums[j] &lt; minVal) {\r\n                minVal = nums[j];\r\n                minIdx = j;\r\n            }\r\n        }\r\n        nums[minIdx] *= multiplier;\r\n    }\r\n    return nums;\r\n}" },]} />

**Complexity**
- Time: O(K * N)
- Space: O(1)
- Notes: Efficient enough for the given constraints of problem I.

---
## Min-Heap

**Intuition**
To efficiently retrieve the minimum element in each step, we can use a Min-Heap (Priority Queue). This approach is more scalable for larger inputs (like in problem II). We store pairs of `(value, index)` in the heap. The heap automatically sorts by value, and for ties, by index.

**Steps**
- Initialize a min-heap and push all `(value, index)` pairs from `nums` into it.
- Loop `k` times.
- Pop the top element `(val, idx)` from the heap.
- Push `(val * multiplier, idx)` back into the heap.
- After `k` operations, the heap contains the final values. Pop all elements and place them back into their respective indices in `nums` to return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import heapq\r\nfrom typing import List\r\n\r\nclass Solution:\r\n    def getFinalState(self, nums: List[int], k: int, multiplier: int) -&gt; List[int]:\r\n        heap = [(val, i) for i, val in enumerate(nums)]\r\n        heapq.heapify(heap)\r\n        \r\n        for _ in range(k):\r\n            val, idx = heapq.heappop(heap)\r\n            heapq.heappush(heap, (val * multiplier, idx))\r\n            \r\n        res = [0] * len(nums)\r\n        while heap:\r\n            val, idx = heapq.heappop(heap)\r\n            res[idx] = val\r\n        return res" },
{ name: "Java", lang: "java", code: "import java.util.*;\r\n\r\nclass Solution {\r\n    public int[] getFinalState(int[] nums, int k, int multiplier) {\r\n        PriorityQueue&lt;int[]&gt; pq = new PriorityQueue&lt;&gt;((a, b) -&gt; {\r\n            if (a[0] != b[0]) return a[0] - b[0];\r\n            return a[1] - b[1];\r\n        });\r\n        \r\n        for (int i = 0; i &lt; nums.length; i++) {\r\n            pq.offer(new int[]{nums[i], i});\r\n        }\r\n        \r\n        for (int i = 0; i &lt; k; i++) {\r\n            int[] curr = pq.poll();\r\n            curr[0] *= multiplier;\r\n            pq.offer(curr);\r\n        }\r\n        \r\n        while (!pq.isEmpty()) {\r\n            int[] curr = pq.poll();\r\n            nums[curr[1]] = curr[0];\r\n        }\r\n        return nums;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;queue&gt;\r\n\r\nusing namespace std;\r\n\r\nclass Solution {\r\npublic:\r\n    vector&lt;int&gt; getFinalState(vector&lt;int&gt;& nums, int k, int multiplier) {\r\n        priority_queue&lt;pair&lt;int, int&gt;, vector&lt;pair&lt;int, int&gt;&gt;, greater&lt;pair&lt;int, int&gt;&gt;&gt; pq;\r\n        \r\n        for (int i = 0; i &lt; nums.size(); i++) {\r\n            pq.push({nums[i], i});\r\n        }\r\n        \r\n        for (int i = 0; i &lt; k; i++) {\r\n            auto [val, idx] = pq.top();\r\n            pq.pop();\r\n            pq.push({val * multiplier, idx});\r\n        }\r\n        \r\n        while (!pq.empty()) {\r\n            auto [val, idx] = pq.top();\r\n            pq.pop();\r\n            nums[idx] = val;\r\n        }\r\n        return nums;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "class MinHeap {\r\n    constructor() {\r\n        this.heap = [];\r\n    }\r\n    \r\n    push(val) {\r\n        this.heap.push(val);\r\n        this.bubbleUp(this.heap.length - 1);\r\n    }\r\n    \r\n    bubbleUp(i) {\r\n        while (i &gt; 0) {\r\n            let p = Math.floor((i - 1) / 2);\r\n            if (this.heap[i][0] &lt; this.heap[p][0] || \r\n                (this.heap[i][0] === this.heap[p][0] && this.heap[i][1] &lt; this.heap[p][1])) {\r\n                [this.heap[i], this.heap[p]] = [this.heap[p], this.heap[i]];\r\n                i = p;\r\n            } else {\r\n                break;\r\n            }\r\n        }\r\n    }\r\n    \r\n    pop() {\r\n        if (this.heap.length === 0) return null;\r\n        const top = this.heap[0];\r\n        const end = this.heap.pop();\r\n        if (this.heap.length &gt; 0) {\r\n            this.heap[0] = end;\r\n            this.sinkDown(0);\r\n        }\r\n        return top;\r\n    }\r\n    \r\n    sinkDown(i) {\r\n        const len = this.heap.length;\r\n        while (true) {\r\n            let l = 2 * i + 1, r = 2 * i + 2, swap = null;\r\n            if (l &lt; len && (this.heap[l][0] &lt; this.heap[i][0] || \r\n                (this.heap[l][0] === this.heap[i][0] && this.heap[l][1] &lt; this.heap[i][1]))) {\r\n                swap = l;\r\n            }\r\n            if (r &lt; len) {\r\n                if ((swap === null && (this.heap[r][0] &lt; this.heap[i][0] || \r\n                    (this.heap[r][0] === this.heap[i][0] && this.heap[r][1] &lt; this.heap[i][1]))) ||\r\n                    (swap !== null && (this.heap[r][0] &lt; this.heap[l][0] || \r\n                    (this.heap[r][0] === this.heap[l][0] && this.heap[r][1] &lt; this.heap[l][1])))) {\r\n                    swap = r;\r\n                }\r\n            }\r\n            if (swap === null) break;\r\n            [this.heap[i], this.heap[swap]] = [this.heap[swap], this.heap[i]];\r\n            i = swap;\r\n        }\r\n    }\r\n    \r\n    size() {\r\n        return this.heap.length;\r\n    }\r\n}\r\n\r\nvar getFinalState = function(nums, k, multiplier) {\r\n    const pq = new MinHeap();\r\n    for (let i = 0; i &lt; nums.length; i++) {\r\n        pq.push([nums[i], i]);\r\n    }\r\n    \r\n    for (let i = 0; i &lt; k; i++) {\r\n        const [val, idx] = pq.pop();\r\n        pq.push([val * multiplier, idx]);\r\n    }\r\n    \r\n    while (pq.size() &gt; 0) {\r\n        const [val, idx] = pq.pop();\r\n        nums[idx] = val;\r\n    }\r\n    return nums;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "class MinHeap {\r\n    heap: [number, number][];\r\n    constructor() {\r\n        this.heap = [];\r\n    }\r\n    \r\n    push(val: [number, number]): void {\r\n        this.heap.push(val);\r\n        this.bubbleUp(this.heap.length - 1);\r\n    }\r\n    \r\n    bubbleUp(i: number): void {\r\n        while (i &gt; 0) {\r\n            let p = Math.floor((i - 1) / 2);\r\n            if (this.heap[i][0] &lt; this.heap[p][0] || \r\n                (this.heap[i][0] === this.heap[p][0] && this.heap[i][1] &lt; this.heap[p][1])) {\r\n                [this.heap[i], this.heap[p]] = [this.heap[p], this.heap[i]];\r\n                i = p;\r\n            } else {\r\n                break;\r\n            }\r\n        }\r\n    }\r\n    \r\n    pop(): [number, number] | null {\r\n        if (this.heap.length === 0) return null;\r\n        const top = this.heap[0];\r\n        const end = this.heap.pop()!;\r\n        if (this.heap.length &gt; 0) {\r\n            this.heap[0] = end;\r\n            this.sinkDown(0);\r\n        }\r\n        return top;\r\n    }\r\n    \r\n    sinkDown(i: number): void {\r\n        const len = this.heap.length;\r\n        while (true) {\r\n            let l = 2 * i + 1, r = 2 * i + 2, swap: number | null = null;\r\n            if (l &lt; len && (this.heap[l][0] &lt; this.heap[i][0] || \r\n                (this.heap[l][0] === this.heap[i][0] && this.heap[l][1] &lt; this.heap[i][1]))) {\r\n                swap = l;\r\n            }\r\n            if (r &lt; len) {\r\n                if ((swap === null && (this.heap[r][0] &lt; this.heap[i][0] || \r\n                    (this.heap[r][0] === this.heap[i][0] && this.heap[r][1] &lt; this.heap[i][1]))) ||\r\n                    (swap !== null && (this.heap[r][0] &lt; this.heap[l][0] || \r\n                    (this.heap[r][0] === this.heap[l][0] && this.heap[r][1] &lt; this.heap[l][1])))) {\r\n                    swap = r;\r\n                }\r\n            }\r\n            if (swap === null) break;\r\n            [this.heap[i], this.heap[swap]] = [this.heap[swap], this.heap[i]];\r\n            i = swap;\r\n        }\r\n    }\r\n    \r\n    size(): number {\r\n        return this.heap.length;\r\n    }\r\n}\r\n\r\nfunction getFinalState(nums: number[], k: number, multiplier: number): number[] {\r\n    const pq = new MinHeap();\r\n    for (let i = 0; i &lt; nums.length; i++) {\r\n        pq.push([nums[i], i]);\r\n    }\r\n    \r\n    for (let i = 0; i &lt; k; i++) {\r\n        const [val, idx] = pq.pop()!;\r\n        pq.push([val * multiplier, idx]);\r\n    }\r\n    \r\n    while (pq.size() &gt; 0) {\r\n        const [val, idx] = pq.pop()!;\r\n        nums[idx] = val;\r\n    }\r\n    return nums;\r\n}" },]} />

**Complexity**
- Time: O(K log N)
- Space: O(N)
- Notes: More efficient for larger arrays or higher values of K.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Final Array State After K Multiplication Operations I Solutions","datePublished":"2025-10-07","description":"Perform k operations replacing the smallest element with its product by multiplier.","keywords":"LeetCode, Final Array State After K Multiplication Operations I, Easy, Array, Math, Heap (Priority Queue), Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Simulation approach is O(K * N), where K is the number of operations and N is the length of the array."}},{"@type":"Question","name":"What is the time complexity of the Min-Heap approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Min-Heap approach is O(K log N) for the operations and O(N log N) to reconstruct the array, which simplifies to O((K + N) log N)."}},{"@type":"Question","name":"Why use a Min-Heap for this problem?","acceptedAnswer":{"@type":"Answer","text":"A Min-Heap allows efficient retrieval of the minimum element in O(log N) time per operation, which is significantly faster than the O(N) scan required by the simulation approach, especially when K is large."}}]}).replace(/</g,'\u003c')}
</script>