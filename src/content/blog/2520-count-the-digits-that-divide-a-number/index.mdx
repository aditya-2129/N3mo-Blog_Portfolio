---
title: "Count the Digits That Divide a Number"
summary: "Count the number of digits in a number that divide the number evenly."
date: "Jul 08, 2025"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 85.90% | **Paid:** No
> **Topics:** Math

Given an integer num, return the number of digits in num that divide num.

An integer val divides nums if nums % val == 0.

- Examples
- Constraints
- [Iterative Approach](#iterative-approach)
- [String Conversion Approach](#string-conversion-approach)

## Examples

Example 1:
```text
Input: num = 7
Output: 1
Explanation: 7 divides 7, so the answer is 1.
```

Example 2:
```text
Input: num = 121
Output: 2
Explanation: 121 is divisible by 1, but not 2. Since 1 occurs twice as a digit, we return 2.
```

Example 3:
```text
Input: num = 1248
Output: 4
Explanation: 1248 is divisible by all of its digits, so the answer is 4.
```

## Constraints
```text
1 <= num <= 10⁹
```

---
## Iterative Approach

**Intuition**
Extract each digit from the number using modulo and division operations, then check if each digit divides the original number.

**Steps**
- Store the original number in a temporary variable
- Iterate through each digit by taking modulo 10
- Check if the digit divides the original number
- Divide the temporary number by 10 to move to the next digit
- Count the digits that divide the number

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def countDigits(self, num: int) -> int:
        original = num
        count = 0
        while num &gt; 0:
            digit = num % 10
            if original % digit == 0:
                count += 1
            num //= 10
        return count` },
{ name: "Java", lang: "java", code: `class Solution {
    public int countDigits(int num) {
        int original = num;
        int count = 0;
        while (num &gt; 0) {
            int digit = num % 10;
            if (original % digit == 0) {
                count++;
            }
            num /= 10;
        }
        return count;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int countDigits(int num) {
        int original = num;
        int count = 0;
        while (num &gt; 0) {
            int digit = num % 10;
            if (original % digit == 0) {
                count++;
            }
            num /= 10;
        }
        return count;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} num
 * @return {number}
 */
var countDigits = function(num) {
    const original = num;
    let count = 0;
    while (num &gt; 0) {
        const digit = num % 10;
        if (original % digit === 0) {
            count++;
        }
        num = Math.floor(num / 10);
    }
    return count;
};` },
{ name: "TypeScript", lang: "typescript", code: `function countDigits(num: number): number {
    const original = num;
    let count = 0;
    while (num &gt; 0) {
        const digit = num % 10;
        if (original % digit === 0) {
            count++;
        }
        num = Math.floor(num / 10);
    }
    return count;
}` },]} />

**Complexity**
- Time: O(log n) where n is the number of digits in num
- Space: O(1)
- Notes: This is the most efficient approach with constant space complexity.

---
## String Conversion Approach

**Intuition**
Convert the number to a string and iterate through each character, converting it back to an integer to check divisibility.

**Steps**
- Convert the number to a string
- Iterate through each character in the string
- Convert each character back to an integer
- Check if the digit divides the original number
- Count the digits that divide the number

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def countDigits(self, num: int) -> int:
        count = 0
        for digit in str(num):
            if num % int(digit) == 0:
                count += 1
        return count` },
{ name: "Java", lang: "java", code: `class Solution {
    public int countDigits(int num) {
        int count = 0;
        String numStr = String.valueOf(num);
        for (int i = 0; i &lt; numStr.length(); i++) {
            int digit = numStr.charAt(i) - '0';
            if (num % digit == 0) {
                count++;
            }
        }
        return count;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int countDigits(int num) {
        int count = 0;
        string numStr = to_string(num);
        for (char c : numStr) {
            int digit = c - '0';
            if (num % digit == 0) {
                count++;
            }
        }
        return count;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} num
 * @return {number}
 */
var countDigits = function(num) {
    let count = 0;
    const numStr = num.toString();
    for (let i = 0; i &lt; numStr.length; i++) {
        const digit = parseInt(numStr[i]);
        if (num % digit === 0) {
            count++;
        }
    }
    return count;
};` },
{ name: "TypeScript", lang: "typescript", code: `function countDigits(num: number): number {
    let count = 0;
    const numStr = num.toString();
    for (let i = 0; i &lt; numStr.length; i++) {
        const digit = parseInt(numStr[i]);
        if (num % digit === 0) {
            count++;
        }
    }
    return count;
}` },]} />

**Complexity**
- Time: O(log n) where n is the number of digits in num
- Space: O(log n) for storing the string representation
- Notes: Slightly less space-efficient than the iterative approach but more readable.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Count the Digits That Divide a Number Solutions","datePublished":"2025-07-08","description":"Count the number of digits in a number that divide the number evenly.","keywords":"LeetCode, Count the Digits That Divide a Number, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(log n) where n is the number of digits in num."}},{"@type":"Question","name":"What is the space complexity of the String Conversion Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(log n) for storing the string representation of the number."}},{"@type":"Question","name":"Which approach is more space-efficient?","acceptedAnswer":{"@type":"Answer","text":"The Iterative Approach is more space-efficient with O(1) space complexity compared to O(log n) for the String Conversion Approach."}}]}).replace(/</g,'\u003c')}
</script>