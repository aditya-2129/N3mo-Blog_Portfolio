---
title: "Replace Elements with Greatest Element on Right Side"
summary: "Replace each element in an array with the greatest element to its right, and the last element with -1."
date: "Nov 25, 2024"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 72.20% | **Paid:** No
> **Topics:** Array

Given an array arr, replace every element in that array with the greatest element among the elements to its right, and replace the last element with -1.

After doing so, return the array.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Reverse Iteration](#reverse-iteration)

## Examples

Input: arr = [17,18,5,4,6,1]
Output: [18,6,6,6,1,-1]
Explanation:
- Index 0: the greatest element to the right of index 0 is index 1 (18).
- Index 1: the greatest element to the right of index 1 is index 4 (6).
- Index 2: the greatest element to the right of index 2 is index 4 (6).
- Index 3: the greatest element to the right of index 3 is index 4 (6).
- Index 4: the greatest element to the right of index 4 is index 5 (1).
- Index 5: there are no elements to the right of index 5, so we put -1.

Input: arr = [400]
Output: [-1]
Explanation: There are no elements to the right of index 0.

## Constraints

```text
- 1 <= arr.length <= 10^4
- 1 <= arr[i] <= 10^5
```

---
## Brute Force

**Intuition**
For each element in the array, we can scan all the elements to its right to find the maximum value.

**Steps**
- Iterate through the array from the first element to the second to last element.
- For each element at index `i`, initialize a `max_val` to -1.
- Iterate through the subarray starting from `i + 1` to the end of the array.
- Update `max_val` if a larger element is found.
- Replace the element at index `i` with `max_val`.
- Set the last element of the array to -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def replaceElements(self, arr: List[int]) -&gt; List[int]:\n        n = len(arr)\n        for i in range(n):\n            max_val = -1\n            for j in range(i + 1, n):\n                if arr[j] &gt; max_val:\n                    max_val = arr[j]\n            arr[i] = max_val\n        return arr" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] replaceElements(int[] arr) {\n        int n = arr.length;\n        for (int i = 0; i &lt; n; i++) {\n            int max = -1;\n            for (int j = i + 1; j &lt; n; j++) {\n                if (arr[j] &gt; max) {\n                    max = arr[j];\n                }\n            }\n            arr[i] = max;\n        }\n        return arr;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; replaceElements(vector&lt;int&gt;& arr) {\n        int n = arr.size();\n        for (int i = 0; i &lt; n; i++) {\n            int maxVal = -1;\n            for (int j = i + 1; j &lt; n; j++) {\n                if (arr[j] &gt; maxVal) {\n                    maxVal = arr[j];\n                }\n            }\n            arr[i] = maxVal;\n        }\n        return arr;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var replaceElements = function(arr) {\n    const n = arr.length;\n    for (let i = 0; i &lt; n; i++) {\n        let max = -1;\n        for (let j = i + 1; j &lt; n; j++) {\n            if (arr[j] &gt; max) {\n                max = arr[j];\n            }\n        }\n        arr[i] = max;\n    }\n    return arr;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function replaceElements(arr: number[]): number[] {\n    const n = arr.length;\n    for (let i = 0; i &lt; n; i++) {\n        let max = -1;\n        for (let j = i + 1; j &lt; n; j++) {\n            if (arr[j] &gt; max) {\n                max = arr[j];\n            }\n        }\n        arr[i] = max;\n    }\n    return arr;\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1)
- Notes: Simple to implement but inefficient for large arrays.

---
## Reverse Iteration

**Intuition**
If we traverse the array from right to left, we can keep track of the maximum element encountered so far. This allows us to update each element in a single pass.

**Steps**
- Initialize a variable `max_so_far` to -1.
- Iterate through the array from the last element down to the first.
- Store the current element in a temporary variable.
- Replace the current element with `max_so_far`.
- Update `max_so_far` to be the maximum of `max_so_far` and the temporary variable.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def replaceElements(self, arr: List[int]) -&gt; List[int]:\n        n = len(arr)\n        max_val = -1\n        for i in range(n - 1, -1, -1):\n            temp = arr[i]\n            arr[i] = max_val\n            if temp &gt; max_val:\n                max_val = temp\n        return arr" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] replaceElements(int[] arr) {\n        int n = arr.length;\n        int max = -1;\n        for (int i = n - 1; i &gt;= 0; i--) {\n            int temp = arr[i];\n            arr[i] = max;\n            if (temp &gt; max) {\n                max = temp;\n            }\n        }\n        return arr;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; replaceElements(vector&lt;int&gt;& arr) {\n        int n = arr.size();\n        int maxVal = -1;\n        for (int i = n - 1; i &gt;= 0; i--) {\n            int temp = arr[i];\n            arr[i] = maxVal;\n            if (temp &gt; maxVal) {\n                maxVal = temp;\n            }\n        }\n        return arr;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var replaceElements = function(arr) {\n    let max = -1;\n    for (let i = arr.length - 1; i &gt;= 0; i--) {\n        const temp = arr[i];\n        arr[i] = max;\n        if (temp &gt; max) {\n            max = temp;\n        }\n    }\n    return arr;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function replaceElements(arr: number[]): number[] {\n    let max = -1;\n    for (let i = arr.length - 1; i &gt;= 0; i--) {\n        const temp = arr[i];\n        arr[i] = max;\n        if (temp &gt; max) {\n            max = temp;\n        }\n    }\n    return arr;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal solution with linear time complexity.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Replace Elements with Greatest Element on Right Side Solutions","datePublished":"2024-11-25","description":"Replace each element in an array with the greatest element to its right, and the last element with -1.","keywords":"LeetCode, Replace Elements with Greatest Element on Right Side, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n²), where n is the length of the array."}},{"@type":"Question","name":"What is the time complexity of the Reverse Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Reverse Iteration approach is O(n), as we traverse the array only once."}},{"@type":"Question","name":"What is the space complexity of the optimal approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity of the optimal approach is O(1), as we only use a constant amount of extra space."}}]}).replace(/</g,'\u003c')}
</script>