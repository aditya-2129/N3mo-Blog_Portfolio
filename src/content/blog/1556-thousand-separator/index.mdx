---
title: "Thousand Separator"
summary: "Given an integer n, add a dot (.) as the thousands separator and return it in string format."
date: "Dec 24, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 53.70% | **Paid:** No
> **Topics:** String

Given an integer n, add a dot (".") as the thousands separator and return it in string format.

- Examples
- Constraints
- [String Iteration](#string-iteration)
- [Mathematical Extraction](#mathematical-extraction)
- [Recursion](#recursion)

## Examples

### Example 1

**Input:**
```text
n = 987
```

**Output:**
```text
"987"
```

### Example 2

**Input:**
```text
n = 1234
```

**Output:**
```text
"1.234"
```

## Constraints

```text
0 <= n <= 2³¹ - 1
```

---
## String Iteration

**Intuition**
Convert the number to a string and iterate from the end, inserting a dot every three characters.

**Steps**
- Convert the integer to a string.
- Iterate through the string from right to left.
- Append characters to a result list.
- Insert a dot every time the count of processed characters is a multiple of 3 (and not the last character).
- Reverse the result list and join it into a string.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def thousandSeparator(self, n: int) -&gt; str:\n        s = str(n)\n        res = []\n        for i, ch in enumerate(reversed(s)):\n            if i &gt; 0 and i % 3 == 0:\n                res.append('.')\n            res.append(ch)\n        return ''.join(reversed(res))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String thousandSeparator(int n) {\n        String s = Integer.toString(n);\n        StringBuilder sb = new StringBuilder();\n        int count = 0;\n        for (int i = s.length() - 1; i &gt;= 0; i--) {\n            if (count &gt; 0 && count % 3 == 0) {\n                sb.append('.');\n            }\n            sb.append(s.charAt(i));\n            count++;\n        }\n        return sb.reverse().toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string thousandSeparator(int n) {\n        string s = to_string(n);\n        string res = \"\";\n        int count = 0;\n        for (int i = s.length() - 1; i &gt;= 0; i--) {\n            if (count &gt; 0 && count % 3 == 0) {\n                res += \".\";\n            }\n            res += s[i];\n            count++;\n        }\n        reverse(res.begin(), res.end());\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {string}\n */\nvar thousandSeparator = function(n) {\n    let s = n.toString();\n    let res = [];\n    let count = 0;\n    for (let i = s.length - 1; i &gt;= 0; i--) {\n        if (count &gt; 0 && count % 3 === 0) {\n            res.push('.');\n        }\n        res.push(s[i]);\n        count++;\n    }\n    return res.reverse().join('');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function thousandSeparator(n: number): string {\n    let s: string = n.toString();\n    let res: string[] = [];\n    let count: number = 0;\n    for (let i = s.length - 1; i &gt;= 0; i--) {\n        if (count &gt; 0 && count % 3 === 0) {\n            res.push('.');\n        }\n        res.push(s[i]);\n        count++;\n    }\n    return res.reverse().join('');\n}" },]} />

**Complexity**
- Time: O(log₁₀ n) - We iterate over the number of digits.
- Space: O(log₁₀ n) - To store the result string.
- Notes: Simple and readable, uses string manipulation.

---
## Mathematical Extraction

**Intuition**
Extract chunks of three digits from the right using modulo 1000 and integer division, then combine them.

**Steps**
- Handle the edge case where n is 0.
- While n is greater than 0, take n % 1000 to get the last three digits.
- Prepend this chunk to the result string.
- If there are remaining digits, prepend a dot.
- Divide n by 1000 to process the next chunk.
- Handle padding with zeros for chunks that are not the most significant (e.g., 1234 -> 1.234).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def thousandSeparator(self, n: int) -&gt; str:\n        if n == 0:\n            return \"0\"\n        res = []\n        while n &gt; 0:\n            chunk = n % 1000\n            n //= 1000\n            if n &gt; 0:\n                res.append(str(chunk).zfill(3))\n            else:\n                res.append(str(chunk))\n        return '.'.join(reversed(res))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String thousandSeparator(int n) {\n        if (n == 0) return \"0\";\n        StringBuilder sb = new StringBuilder();\n        int count = 0;\n        while (n &gt; 0) {\n            if (count &gt; 0 && count % 3 == 0) {\n                sb.insert(0, '.');\n            }\n            sb.insert(0, n % 10);\n            n /= 10;\n            count++;\n        }\n        return sb.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string thousandSeparator(int n) {\n        if (n == 0) return \"0\";\n        string res = \"\";\n        int count = 0;\n        while (n &gt; 0) {\n            if (count &gt; 0 && count % 3 == 0) {\n                res = \".\" + res;\n            }\n            res = char('0' + n % 10) + res;\n            n /= 10;\n            count++;\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {string}\n */\nvar thousandSeparator = function(n) {\n    if (n === 0) return '0';\n    let res = '';\n    let count = 0;\n    while (n &gt; 0) {\n        if (count &gt; 0 && count % 3 === 0) {\n            res = '.' + res;\n        }\n        res = (n % 10) + res;\n        n = Math.floor(n / 10);\n        count++;\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function thousandSeparator(n: number): string {\n    if (n === 0) return '0';\n    let res: string = '';\n    let count: number = 0;\n    while (n &gt; 0) {\n        if (count &gt; 0 && count % 3 === 0) {\n            res = '.' + res;\n        }\n        res = (n % 10) + res;\n        n = Math.floor(n / 10);\n        count++;\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(log₁₀ n) - We process the number digit by digit.
- Space: O(log₁₀ n) - To store the result string.
- Notes: Avoids converting the entire number to a string initially, working with math operations.

---
## Recursion

**Intuition**
Recursively process the number by removing the last three digits and appending them with a separator.

**Steps**
- Base case: if n is less than 1000, return the string representation of n.
- Recursive step: call the function on n / 1000.
- Append a dot and the last three digits (padded with zeros if necessary) to the result of the recursive call.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def thousandSeparator(self, n: int) -&gt; str:\n        if n &lt; 1000:\n            return str(n)\n        return self.thousandSeparator(n // 1000) + '.' + str(n % 1000).zfill(3)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String thousandSeparator(int n) {\n        if (n &lt; 1000) return String.valueOf(n);\n        return thousandSeparator(n / 1000) + \".\" + String.format(\"%03d\", n % 1000);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string thousandSeparator(int n) {\n        if (n &lt; 1000) return to_string(n);\n        string suffix = to_string(n % 1000);\n        while (suffix.length() &lt; 3) suffix = \"0\" + suffix;\n        return thousandSeparator(n / 1000) + \".\" + suffix;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {string}\n */\nvar thousandSeparator = function(n) {\n    if (n &lt; 1000) return n.toString();\n    return thousandSeparator(Math.floor(n / 1000)) + '.' + (n % 1000).toString().padStart(3, '0');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function thousandSeparator(n: number): string {\n    if (n &lt; 1000) return n.toString();\n    return thousandSeparator(Math.floor(n / 1000)) + '.' + (n % 1000).toString().padStart(3, '0');\n}" },]} />

**Complexity**
- Time: O(log₁₀ n) - Each recursive call reduces the number of digits.
- Space: O(log₁₀ n) - Stack space for recursion and result string.
- Notes: Elegant solution, but uses implicit stack space.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Thousand Separator Solutions","datePublished":"2024-12-24","description":"Given an integer n, add a dot (.) as the thousands separator and return it in string format.","keywords":"LeetCode, Thousand Separator, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the String Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(log₁₀ n), where n is the input integer, as we iterate through each digit."}},{"@type":"Question","name":"What is the space complexity of the Mathematical Extraction approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(log₁₀ n) to store the resulting string."}},{"@type":"Question","name":"How does the Recursive approach handle padding?","acceptedAnswer":{"@type":"Answer","text":"The recursive approach formats the last three digits with leading zeros (e.g., using zfill or padStart) before appending them to the result."}}]}).replace(/</g,'\u003c')}
</script>