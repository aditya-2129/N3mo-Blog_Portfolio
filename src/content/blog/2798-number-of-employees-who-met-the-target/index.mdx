---
title: "Number of Employees Who Met the Target"
summary: "Count how many employees worked at least the target number of hours."
date: "Dec 31, 2025"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 87.80% | **Paid:** No
> **Topics:** Array

There are n employees in a company, numbered from 0 to n - 1. Each employee i has worked for hours[i] hours in the company.

The company requires each employee to work for at least target hours.

You are given a 0-indexed integer array hours of length n and an integer target.

Return the number of employees who worked at least target hours.

## Table of Contents

- Examples
- Constraints
- [Linear Iteration](#linear-iteration)
- [Functional Approach](#functional-approach)

## Examples

```text
Input: hours = [0,1,2,3,4], target = 2
Output: 3
Explanation: The company requires employees to work at least 2 hours.
- Employee 0 worked 0 hours and did not meet the target.
- Employee 1 worked 1 hour and did not meet the target.
- Employee 2 worked 2 hours and met the target.
- Employee 3 worked 3 hours and met the target.
- Employee 4 worked 4 hours and met the target.
There are 3 employees who met the target.
```

```text
Input: hours = [5,1,4,2,2], target = 6
Output: 0
Explanation: The company requires employees to work at least 6 hours.
There are 0 employees who met the target.
```

## Constraints

```text
n == hours.length
1 <= n <= 50
0 <= hours[i], target <= 10⁵
```

---
## Linear Iteration

**Intuition**
We iterate through the array once, checking each employee's hours against the target. If the hours are greater than or equal to the target, we increment our counter.

**Steps**
- Initialize a counter variable to 0.
- Loop through each element in the hours array.
- For each element, check if it is greater than or equal to the target.
- If the condition is true, increment the counter.
- Return the counter after the loop finishes.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numberOfEmployeesWhoMetTarget(self, hours: List[int], target: int) -&gt; int:\n        count = 0\n        for h in hours:\n            if h &gt;= target:\n                count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numberOfEmployeesWhoMetTarget(int[] hours, int target) {\n        int count = 0;\n        for (int h : hours) {\n            if (h &gt;= target) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numberOfEmployeesWhoMetTarget(vector&lt;int&gt;& hours, int target) {\n        int count = 0;\n        for (int h : hours) {\n            if (h &gt;= target) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var numberOfEmployeesWhoMetTarget = function(hours, target) {\n    let count = 0;\n    for (let h of hours) {\n        if (h &gt;= target) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfEmployeesWhoMetTarget(hours: number[], target: number): number {\n    let count = 0;\n    for (let h of hours) {\n        if (h &gt;= target) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: This is the most efficient approach with minimal space usage.

---
## Functional Approach

**Intuition**
We utilize built-in array methods to filter the list for employees meeting the target and then count the resulting elements. This abstracts the loop logic.

**Steps**
- Use the language's filter or stream mechanism to select elements &gt;= target.
- Return the count or length of the filtered collection.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numberOfEmployeesWhoMetTarget(self, hours: List[int], target: int) -&gt; int:\n        return sum(1 for h in hours if h &gt;= target)" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int numberOfEmployeesWhoMetTarget(int[] hours, int target) {\n        return (int) Arrays.stream(hours).filter(h -&gt; h &gt;= target).count();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n\nclass Solution {\npublic:\n    int numberOfEmployeesWhoMetTarget(vector&lt;int&gt;& hours, int target) {\n        return std::count_if(hours.begin(), hours.end(), [target](int h) {\n            return h &gt;= target;\n        });\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var numberOfEmployeesWhoMetTarget = function(hours, target) {\n    return hours.filter(h =&gt; h &gt;= target).length;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfEmployeesWhoMetTarget(hours: number[], target: number): number {\n    return hours.filter(h =&gt; h &gt;= target).length;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n) or O(1) depending on implementation (filter creates new array, count_if is O(1) extra)
- Notes: More concise but may have slightly higher constant factors or memory overhead depending on the language implementation.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Number of Employees Who Met the Target Solutions","datePublished":"2025-12-31","description":"Count how many employees worked at least the target number of hours.","keywords":"LeetCode, Number of Employees Who Met the Target, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of employees, as we iterate through the array once."}},{"@type":"Question","name":"What is the space complexity of the Linear Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use a single counter variable regardless of the input size."}},{"@type":"Question","name":"How does the Functional Approach work?","acceptedAnswer":{"@type":"Answer","text":"It uses built-in methods like filter or streams to select elements that meet the condition (hours &gt;= target) and returns the count of those elements."}}]}).replace(/</g,'\u003c')}
</script>