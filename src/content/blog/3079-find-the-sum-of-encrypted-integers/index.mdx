---
title: "Find the Sum of Encrypted Integers"
summary: "Calculate the sum of integers where each digit is replaced by the maximum digit found in that integer."
date: "Jun 05, 2025"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 74.90% | **Paid:** No
> **Topics:** Array, Math

You are given an integer array nums. The encryption of an integer is performed as follows:

Replace each digit x of the integer with the maximum digit of the integer.

Return the sum of the elements of nums after encryption.

- Examples
- Constraints
- [String Manipulation](#string-manipulation)
- [Mathematical Calculation](#mathematical-calculation)

## Examples

Example 1:
```text
Input: nums = [1,2,3]
Output: 6
Explanation: 1 is encrypted as 1, 2 is encrypted as 2, 3 is encrypted as 3. 1 + 2 + 3 = 6.
```

Example 2:
```text
Input: nums = [10,21,31]
Output: 66
Explanation: 10 is encrypted as 11, 21 is encrypted as 22, 31 is encrypted as 33. 11 + 22 + 33 = 66.
```

## Constraints

```text
1 <= nums.length <= 50
1 <= nums[i] <= 1000
```

---
## String Manipulation

**Intuition**
Convert each number to a string to easily iterate over its digits, find the maximum digit, and construct the encrypted number by repeating that maximum digit.

**Steps**
- Initialize a variable `total` to 0.
- Iterate through each number in the input array `nums`.
- Convert the current number to a string.
- Find the maximum character (digit) in the string.
- Create a new string consisting of this maximum character repeated for the length of the original string.
- Convert this new string back to an integer and add it to `total`.
- Return `total`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfEncryptedInt(self, nums: list[int]) -&gt; int:\n        total = 0\n        for num in nums:\n            s = str(num)\n            max_digit = max(s)\n            encrypted = int(max_digit * len(s))\n            total += encrypted\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfEncryptedInt(int[] nums) {\n        int total = 0;\n        for (int num : nums) {\n            String s = String.valueOf(num);\n            char maxDigit = '0';\n            for (char c : s.toCharArray()) {\n                if (c &gt; maxDigit) {\n                    maxDigit = c;\n                }\n            }\n            String encryptedStr = new String(new char[s.length()]).replace('\\0', String.valueOf(maxDigit));\n            total += Integer.parseInt(encryptedStr);\n        }\n        return total;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfEncryptedInt(vector&lt;int&gt;& nums) {\n        int total = 0;\n        for (int num : nums) {\n            string s = to_string(num);\n            char maxDigit = *max_element(s.begin(), s.end());\n            string encrypted(s.length(), maxDigit);\n            total += stoi(encrypted);\n        }\n        return total;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar sumOfEncryptedInt = function(nums) {\n    let total = 0;\n    for (const num of nums) {\n        const s = num.toString();\n        const maxDigit = Math.max(...s);\n        const encrypted = parseInt(maxDigit.repeat(s.length));\n        total += encrypted;\n    }\n    return total;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfEncryptedInt(nums: number[]): number {\n    let total = 0;\n    for (const num of nums) {\n        const s = num.toString();\n        const maxDigit = Math.max(...s.split('').map(Number));\n        const encrypted = parseInt(String(maxDigit).repeat(s.length));\n        total += encrypted;\n    }\n    return total;\n}" },]} />

**Complexity**
- Time: O(N * L), where N is the number of elements and L is the maximum number of digits in an element.
- Space: O(L) to store the string representation of the number.
- Notes: This approach is very readable and easy to implement but involves string allocation overhead.

---
## Mathematical Calculation

**Intuition**
Process each number mathematically by extracting digits to find the maximum value and the total number of digits. The encrypted number can be calculated using the geometric series sum formula: `max_digit * (10^len - 1) / 9`.

**Steps**
- Initialize `total` to 0.
- Iterate through each number in `nums`.
- Initialize `max_digit` to 0 and `length` to 0.
- Use a loop to extract digits from the number using modulo 10 and division by 10.
- Update `max_digit` with the maximum value found and increment `length`.
- Calculate the encrypted value using the formula `max_digit * (10^length - 1) / 9`.
- Add the encrypted value to `total`.
- Return `total`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfEncryptedInt(self, nums: list[int]) -&gt; int:\n        total = 0\n        for num in nums:\n            max_digit = 0\n            length = 0\n            temp = num\n            while temp &gt; 0:\n                max_digit = max(max_digit, temp % 10)\n                length += 1\n                temp //= 10\n            encrypted = max_digit * (10**length - 1) // 9\n            total += encrypted\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfEncryptedInt(int[] nums) {\n        int total = 0;\n        for (int num : nums) {\n            int maxDigit = 0;\n            int length = 0;\n            int temp = num;\n            while (temp &gt; 0) {\n                maxDigit = Math.max(maxDigit, temp % 10);\n                length++;\n                temp /= 10;\n            }\n            int encrypted = maxDigit * ((int)Math.pow(10, length) - 1) / 9;\n            total += encrypted;\n        }\n        return total;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfEncryptedInt(vector&lt;int&gt;& nums) {\n        int total = 0;\n        for (int num : nums) {\n            int maxDigit = 0;\n            int length = 0;\n            int temp = num;\n            while (temp &gt; 0) {\n                maxDigit = max(maxDigit, temp % 10);\n                length++;\n                temp /= 10;\n            }\n            int encrypted = maxDigit * ((int)pow(10, length) - 1) / 9;\n            total += encrypted;\n        }\n        return total;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar sumOfEncryptedInt = function(nums) {\n    let total = 0;\n    for (const num of nums) {\n        let maxDigit = 0;\n        let length = 0;\n        let temp = num;\n        while (temp &gt; 0) {\n            maxDigit = Math.max(maxDigit, temp % 10);\n            length++;\n            temp = Math.floor(temp / 10);\n        }\n        const encrypted = maxDigit * (Math.pow(10, length) - 1) / 9;\n        total += encrypted;\n    }\n    return total;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfEncryptedInt(nums: number[]): number {\n    let total = 0;\n    for (const num of nums) {\n        let maxDigit = 0;\n        let length = 0;\n        let temp = num;\n        while (temp &gt; 0) {\n            maxDigit = Math.max(maxDigit, temp % 10);\n            length++;\n            temp = Math.floor(temp / 10);\n        }\n        const encrypted = maxDigit * (Math.pow(10, length) - 1) / 9;\n        total += encrypted;\n    }\n    return total;\n}" },]} />

**Complexity**
- Time: O(N * L), where N is the number of elements and L is the maximum number of digits.
- Space: O(1), as we only use a few variables for calculation.
- Notes: This approach avoids string allocation and is generally more performant in environments where string operations are expensive.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find the Sum of Encrypted Integers Solutions","datePublished":"2025-06-05","description":"Calculate the sum of integers where each digit is replaced by the maximum digit found in that integer.","keywords":"LeetCode, Find the Sum of Encrypted Integers, Easy, Array, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the String Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * L), where N is the number of elements in the array and L is the maximum number of digits in any element."}},{"@type":"Question","name":"What is the space complexity of the Mathematical Calculation approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use a constant amount of extra space for variables."}},{"@type":"Question","name":"How is the encrypted number calculated mathematically?","acceptedAnswer":{"@type":"Answer","text":"It is calculated using the formula max_digit * (10^length - 1) / 9, which creates a number consisting of the max_digit repeated 'length' times."}}]}).replace(/</g,'\u003c')}
</script>