---
title: "Decompress Run-Length Encoded List"
summary: "Decompress a run-length encoded list by expanding frequency-value pairs into a flat array."
date: "Sep 24, 2025"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 86.20% | **Paid:** No
> **Topics:** Array

We are given a list nums of integers representing a list run-length encoded.

Consider each adjacent pair of elements [freq, val] = [nums[2*i], nums[2*i+1]] (with i &gt;= 0). For each such pair, there are freq elements with value val concatenated in a sublist. Concatenate all the sublists from left to right to generate the decompressed list.

Return the decompressed list.

- Examples
- Constraints
- [Iterative Approach](#iterative-approach)
- [Functional Approach](#functional-approach)
- [Two Pointer Approach](#two-pointer-approach)

## Examples

Example 1:
```text
Input: nums = [1,2,3,4]
Output: [2,4,4,4]
Explanation: The first pair [1,2] means we have freq = 1 and val = 2 so we generate [2].
The second pair [3,4] means we have freq = 3 and val = 4 so we generate [4,4,4].
At the end we concatenate [2] + [4,4,4] = [2,4,4,4].
```

Example 2:
```text
Input: nums = [1,1,2,3]
Output: [1,3,3]
```

## Constraints
```text
2 <= nums.length <= 100
nums.length % 2 == 0
1 <= nums[i] <= 100
```

---
## Iterative Approach

**Intuition**
Iterate through the array in steps of 2, treating each adjacent pair as (frequency, value), and extend the result list with the value repeated frequency times.

**Steps**
- Initialize an empty result list
- Loop through nums with step size of 2
- For each pair (freq, val), extend result with val repeated freq times
- Return the result list

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def decompressRLElist(self, nums: List[int]) -&gt; List[int]:\n        result = []\n        for i in range(0, len(nums), 2):\n            freq = nums[i]\n            val = nums[i + 1]\n            result.extend([val] * freq)\n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] decompressRLElist(int[] nums) {\n        int totalLength = 0;\n        for (int i = 0; i &lt; nums.length; i += 2) {\n            totalLength += nums[i];\n        }\n        \n        int[] result = new int[totalLength];\n        int index = 0;\n        \n        for (int i = 0; i &lt; nums.length; i += 2) {\n            int freq = nums[i];\n            int val = nums[i + 1];\n            for (int j = 0; j &lt; freq; j++) {\n                result[index++] = val;\n            }\n        }\n        \n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; decompressRLElist(vector&lt;int&gt;& nums) {\n        vector&lt;int&gt; result;\n        for (int i = 0; i &lt; nums.size(); i += 2) {\n            int freq = nums[i];\n            int val = nums[i + 1];\n            for (int j = 0; j &lt; freq; j++) {\n                result.push_back(val);\n            }\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar decompressRLElist = function(nums) {\n    const result = [];\n    for (let i = 0; i &lt; nums.length; i += 2) {\n        const freq = nums[i];\n        const val = nums[i + 1];\n        for (let j = 0; j &lt; freq; j++) {\n            result.push(val);\n        }\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function decompressRLElist(nums: number[]): number[] {\n    const result: number[] = [];\n    for (let i = 0; i &lt; nums.length; i += 2) {\n        const freq = nums[i];\n        const val = nums[i + 1];\n        for (let j = 0; j &lt; freq; j++) {\n            result.push(val);\n        }\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(n) where n is the total length of the decompressed list
- Space: O(n) for storing the result
- Notes: Simple and straightforward approach with clear logic

---
## Functional Approach

**Intuition**
Use language-specific functional constructs like list comprehensions, Array.fill, or vector insert to build the result more concisely.

**Steps**
- Initialize an empty result list
- For each pair (freq, val), use functional methods to add val repeated freq times
- Return the result list

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def decompressRLElist(self, nums: List[int]) -&gt; List[int]:\n        return [val for i in range(0, len(nums), 2) for val in [nums[i + 1]] * nums[i]]" },
{ name: "Java", lang: "java", code: "import java.util.ArrayList;\nimport java.util.List;\n\nclass Solution {\n    public int[] decompressRLElist(int[] nums) {\n        List&lt;Integer&gt; resultList = new ArrayList&lt;&gt;();\n        \n        for (int i = 0; i &lt; nums.length; i += 2) {\n            int freq = nums[i];\n            int val = nums[i + 1];\n            for (int j = 0; j &lt; freq; j++) {\n                resultList.add(val);\n            }\n        }\n        \n        int[] result = new int[resultList.size()];\n        for (int i = 0; i &lt; resultList.size(); i++) {\n            result[i] = resultList.get(i);\n        }\n        \n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; decompressRLElist(vector&lt;int&gt;& nums) {\n        vector&lt;int&gt; result;\n        for (int i = 0; i &lt; nums.size(); i += 2) {\n            int freq = nums[i];\n            int val = nums[i + 1];\n            result.insert(result.end(), freq, val);\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar decompressRLElist = function(nums) {\n    const result = [];\n    for (let i = 0; i &lt; nums.length; i += 2) {\n        const freq = nums[i];\n        const val = nums[i + 1];\n        result.push(...Array(freq).fill(val));\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function decompressRLElist(nums: number[]): number[] {\n    const result: number[] = [];\n    for (let i = 0; i &lt; nums.length; i += 2) {\n        const freq = nums[i];\n        const val = nums[i + 1];\n        result.push(...Array(freq).fill(val));\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(n) where n is the total length of the decompressed list
- Space: O(n) for storing the result
- Notes: More concise code using built-in methods; Python list comprehension is particularly elegant

---
## Two Pointer Approach

**Intuition**
Use two pointers - one to track position in the input array and another to track position in the result array, processing pairs sequentially.

**Steps**
- Initialize result list and set input pointer to 0
- While input pointer is within bounds, read freq and val
- Add val to result freq times, incrementing result pointer
- Move input pointer by 2 to next pair
- Return result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def decompressRLElist(self, nums: List[int]) -&gt; List[int]:\n        result = []\n        n = len(nums)\n        i = 0\n        while i &lt; n:\n            freq = nums[i]\n            val = nums[i + 1]\n            result.extend([val] * freq)\n            i += 2\n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] decompressRLElist(int[] nums) {\n        int totalLength = 0;\n        for (int i = 0; i &lt; nums.length; i += 2) {\n            totalLength += nums[i];\n        }\n        \n        int[] result = new int[totalLength];\n        int resultIndex = 0;\n        int numsIndex = 0;\n        \n        while (numsIndex &lt; nums.length) {\n            int freq = nums[numsIndex];\n            int val = nums[numsIndex + 1];\n            for (int j = 0; j &lt; freq; j++) {\n                result[resultIndex++] = val;\n            }\n            numsIndex += 2;\n        }\n        \n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; decompressRLElist(vector&lt;int&gt;& nums) {\n        vector&lt;int&gt; result;\n        int i = 0;\n        while (i &lt; nums.size()) {\n            int freq = nums[i];\n            int val = nums[i + 1];\n            for (int j = 0; j &lt; freq; j++) {\n                result.push_back(val);\n            }\n            i += 2;\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar decompressRLElist = function(nums) {\n    const result = [];\n    let i = 0;\n    while (i &lt; nums.length) {\n        const freq = nums[i];\n        const val = nums[i + 1];\n        for (let j = 0; j &lt; freq; j++) {\n            result.push(val);\n        }\n        i += 2;\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function decompressRLElist(nums: number[]): number[] {\n    const result: number[] = [];\n    let i = 0;\n    while (i &lt; nums.length) {\n        const freq = nums[i];\n        const val = nums[i + 1];\n        for (let j = 0; j &lt; freq; j++) {\n            result.push(val);\n        }\n        i += 2;\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(n) where n is the total length of the decompressed list
- Space: O(n) for storing the result
- Notes: Similar to iterative approach but uses while loop with explicit pointer management

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Decompress Run-Length Encoded List Solutions","datePublished":"2025-09-24","description":"Decompress a run-length encoded list by expanding frequency-value pairs into a flat array.","keywords":"LeetCode, Decompress Run-Length Encoded List, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the total length of the decompressed list."}},{"@type":"Question","name":"What is the space complexity of the Functional Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) for storing the result array."}},{"@type":"Question","name":"What is the time complexity of the Two Pointer Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the total length of the decompressed list."}}]}).replace(/</g,'\u003c')}
</script>