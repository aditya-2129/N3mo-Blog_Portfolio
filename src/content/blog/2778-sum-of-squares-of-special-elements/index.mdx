---
title: "Sum of Squares of Special Elements"
summary: "Calculate the sum of squares of elements whose 1-indexed positions divide the array length."
date: "Nov 14, 2024"
tags:
  - Array
  - Enumeration
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.10% | **Paid:** No
> **Topics:** Array, Enumeration

You are given a 0-indexed integer array nums and a positive integer k.

We call an index i of nums special if i divides n, where n is the length of the array.

Return the sum of the squares of all special elements of nums.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Optimized Enumeration](#optimized-enumeration)

## Examples

Example 1:
```text
Input: nums = [1,2,3,4]
Output: 21
Explanation: Special elements are nums[0] = 1, nums[1] = 2, and nums[3] = 4.
Their squares are 1² = 1, 2² = 4, and 4² = 16.
The sum of squares is 1 + 4 + 16 = 21.
```

Example 2:
```text
Input: nums = [2,7,1,19,18,3]
Output: 63
Explanation: Special elements are nums[0] = 2, nums[1] = 7, nums[2] = 1, and nums[5] = 3.
Their squares are 2² = 4, 7² = 49, 1² = 1, and 3² = 9.
The sum of squares is 4 + 49 + 1 + 9 = 63.
```

## Constraints
```text
1 <= nums.length == n <= 50
1 <= nums[i] <= 50
```

---
## Brute Force

**Intuition**
Iterate through all indices from 1 to n, check if each index divides n, and accumulate the square of elements at special positions.

**Steps**
- Get the length n of the array
- Initialize total sum to 0
- Loop through indices 1 to n (1-indexed)
- If n is divisible by current index, add square of element at that index to total
- Return the total sum

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfSquares(self, nums: List[int]) -&gt; int:\n        n = len(nums)\n        total = 0\n        for i in range(1, n + 1):\n            if n % i == 0:\n                total += nums[i - 1] ** 2\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfSquares(int[] nums) {\n        int n = nums.length;\n        int total = 0;\n        for (int i = 1; i &lt;= n; i++) {\n            if (n % i == 0) {\n                total += nums[i - 1] * nums[i - 1];\n            }\n        }\n        return total;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfSquares(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        int total = 0;\n        for (int i = 1; i &lt;= n; i++) {\n            if (n % i == 0) {\n                total += nums[i - 1] * nums[i - 1];\n            }\n        }\n        return total;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar sumOfSquares = function(nums) {\n    const n = nums.length;\n    let total = 0;\n    for (let i = 1; i &lt;= n; i++) {\n        if (n % i === 0) {\n            total += nums[i - 1] * nums[i - 1];\n        }\n    }\n    return total;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfSquares(nums: number[]): number {\n    const n = nums.length;\n    let total = 0;\n    for (let i = 1; i &lt;= n; i++) {\n        if (n % i === 0) {\n            total += nums[i - 1] * nums[i - 1];\n        }\n    }\n    return total;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Simple and straightforward approach suitable for the given constraints.

---
## Optimized Enumeration

**Intuition**
Instead of checking every index, directly find all divisors of n and only process those positions, reducing iterations to O(√n).

**Steps**
- Get the length n of the array
- Initialize total sum to 0
- Iterate from 1 to √n
- If i divides n, add square of element at index i-1
- If i ≠ n/i, also add square of element at index (n/i)-1
- Return the total sum

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfSquares(self, nums: List[int]) -&gt; int:\n        n = len(nums)\n        total = 0\n        for i in range(1, int(n ** 0.5) + 1):\n            if n % i == 0:\n                total += nums[i - 1] ** 2\n                if i != n // i:\n                    total += nums[n // i - 1] ** 2\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfSquares(int[] nums) {\n        int n = nums.length;\n        int total = 0;\n        for (int i = 1; i * i &lt;= n; i++) {\n            if (n % i == 0) {\n                total += nums[i - 1] * nums[i - 1];\n                if (i != n / i) {\n                    total += nums[n / i - 1] * nums[n / i - 1];\n                }\n            }\n        }\n        return total;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfSquares(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        int total = 0;\n        for (int i = 1; i * i &lt;= n; i++) {\n            if (n % i == 0) {\n                total += nums[i - 1] * nums[i - 1];\n                if (i != n / i) {\n                    total += nums[n / i - 1] * nums[n / i - 1];\n                }\n            }\n        }\n        return total;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar sumOfSquares = function(nums) {\n    const n = nums.length;\n    let total = 0;\n    for (let i = 1; i * i &lt;= n; i++) {\n        if (n % i === 0) {\n            total += nums[i - 1] * nums[i - 1];\n            if (i !== n / i) {\n                total += nums[n / i - 1] * nums[n / i - 1];\n            }\n        }\n    }\n    return total;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfSquares(nums: number[]): number {\n    const n = nums.length;\n    let total = 0;\n    for (let i = 1; i * i &lt;= n; i++) {\n        if (n % i === 0) {\n            total += nums[i - 1] * nums[i - 1];\n            if (i !== n / i) {\n                total += nums[n / i - 1] * nums[n / i - 1];\n            }\n        }\n    }\n    return total;\n}" },]} />

**Complexity**
- Time: O(√n)
- Space: O(1)
- Notes: More efficient for larger n values, though both approaches work well within given constraints.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Sum of Squares of Special Elements  Solutions","datePublished":"2024-11-14","description":"Calculate the sum of squares of elements whose 1-indexed positions divide the array length.","keywords":"LeetCode, Sum of Squares of Special Elements , Easy, Array, Enumeration"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the array."}},{"@type":"Question","name":"What is the time complexity of the Optimized Enumeration approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(√n) where n is the length of the array."}},{"@type":"Question","name":"What is the space complexity for both approaches?","acceptedAnswer":{"@type":"Answer","text":"Both approaches have O(1) space complexity as we only use a constant amount of extra space."}}]}).replace(/</g,'\u003c')}
</script>