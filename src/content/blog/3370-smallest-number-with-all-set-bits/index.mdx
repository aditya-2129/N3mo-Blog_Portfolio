---
title: "Smallest Number With All Set Bits"
summary: "Find the smallest number with all bits set for a given number of bits n."
date: "Nov 21, 2024"
tags:
  - Math
  - Bit Manipulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 80.30% | **Paid:** No
> **Topics:** Math, Bit Manipulation

You are given a positive integer n. Return the smallest number x such that x has all its bits set and has exactly n bits.

A number has all its bits set if every bit in its binary representation is 1. For example, 7 (binary 111) has all its bits set, while 5 (binary 101) does not.

- Examples
- Constraints
- [Bit Manipulation Approach](#bit-manipulation)
- [Math Formula Approach](#math-formula)

## Examples

```text
Input: n = 3
Output: 7
Explanation: 7 in binary is 111, which has all 3 bits set.
```

```text
Input: n = 1
Output: 1
Explanation: 1 in binary is 1, which has all 1 bit set.
```

```text
Input: n = 4
Output: 15
Explanation: 15 in binary is 1111, which has all 4 bits set.
```

## Constraints

```text
1 <= n <= 20
```

---
## Bit Manipulation

**Intuition**
A number with all n bits set is simply 2ⁿ - 1, which can be efficiently computed using the left shift operator.

**Steps**
- Left shift 1 by n positions to get 2ⁿ
- Subtract 1 to get the number with all n bits set

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestNumber(self, n: int) -&gt; int:\n        return (1 &lt;&lt; n) - 1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int smallestNumber(int n) {\n        return (1 &lt;&lt; n) - 1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int smallestNumber(int n) {\n        return (1 &lt;&lt; n) - 1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    smallestNumber(n) {\n        return (1 &lt;&lt; n) - 1;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    smallestNumber(n: number): number {\n        return (1 &lt;&lt; n) - 1;\n    }\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Most efficient approach using bitwise operations

---
## Math Formula

**Intuition**
The smallest number with all n bits set follows the mathematical formula 2ⁿ - 1.

**Steps**
- Calculate 2 raised to the power of n
- Subtract 1 from the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestNumber(self, n: int) -&gt; int:\n        return 2 ** n - 1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int smallestNumber(int n) {\n        return (int)Math.pow(2, n) - 1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int smallestNumber(int n) {\n        return (int)pow(2, n) - 1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    smallestNumber(n) {\n        return Math.pow(2, n) - 1;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    smallestNumber(n: number): number {\n        return Math.pow(2, n) - 1;\n    }\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Uses math library functions, slightly less efficient than bit manipulation

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Smallest Number With All Set Bits Solutions","datePublished":"2024-11-21","description":"Find the smallest number with all bits set for a given number of bits n.","keywords":"LeetCode, Smallest Number With All Set Bits, Easy, Math, Bit Manipulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Bit Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) as it involves a single left shift operation and subtraction."}},{"@type":"Question","name":"What is the time complexity of the Math Formula approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) as it involves computing a power and subtraction."}},{"@type":"Question","name":"Which approach is more efficient?","acceptedAnswer":{"@type":"Answer","text":"The Bit Manipulation approach is more efficient as it uses bitwise operations directly without calling math library functions."}}]}).replace(/</g,'\u003c')}
</script>