---
title: "Big Countries"
summary: "Find the name, population, and area of countries with an area of at least three million square kilometers or a population of at least twenty-five million."
date: "Dec 30, 2024"
tags:
  - Database
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 68.50% | **Paid:** No
> **Topics:** Database

Table: World

+-------------+-----------+
| Column Name | Type      |
+-------------+-----------+
| name        | varchar   |
| continent   | varchar   |
| area        | int       |
| population  | int       |
| gdp         | bigint    |
+-------------+-----------+
name is the primary key for this table.

A country is big if:

it has an area of at least three million (i.e., 3000000), or
it has a population of at least twenty-five million (i.e., 25000000).

Write a solution to find the name, population, and area of the big countries.

Return the result table in any order.

The result format is in the following example.

- Examples
- Constraints
- [Approach 1: Direct Filtering](#approach-1-direct-filtering)
- [Approach 2: Set Union](#approach-2-set-union)

## Examples

**Example 1:**

```text
Input:
World table:
+-------------+-----------+---------+------------+--------------+
| name        | continent | area    | population | gdp          |
+-------------+-----------+---------+------------+--------------+
| Afghanistan | Asia      | 652230  | 25500100   | 20343000000  |
| Albania     | Europe    | 28748   | 2831741    | 12960000000  |
| Algeria     | Africa    | 2381741 | 37100000   | 188681000000 |
| Andorra     | Europe    | 468     | 78115      | 3712000000   |
| Angola      | Africa    | 1246700 | 20609294   | 100360000000 |
+-------------+-----------+---------+------------+--------------+
Output:
+-------------+------------+---------+
| name        | population | area    |
+-------------+------------+---------+
| Afghanistan | 25500100   | 652230  |
| Algeria     | 37100000   | 2381741 |
+-------------+------------+---------+
Explanation:
Afghanistan has a population of 25500100, which is greater than 25000000.
Algeria has a population of 37100000, which is greater than 25000000.
```

## Constraints

```text
There are no specific constraints listed for this problem other than the table schema.
```

---
## Approach 1: Direct Filtering

**Intuition**
We can directly filter the `World` table using a `WHERE` clause with an `OR` condition to select countries that satisfy either the area or the population requirement.

**Steps**
- Select the columns `name`, `population`, and `area` from the `World` table.
- Filter the rows where `area` is greater than or equal to 3,000,000 (3×10⁶).
- Alternatively, filter the rows where `population` is greater than or equal to 25,000,000 (2.5×10⁷).
- Combine these conditions using the `OR` operator.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def query(self) -&gt; str:\n        return 'SELECT name, population, area FROM World WHERE area &gt;= 3000000 OR population &gt;= 25000000'" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String query() {\n        return \"SELECT name, population, area FROM World WHERE area &gt;= 3000000 OR population &gt;= 25000000\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    string query() {\n        return \"SELECT name, population, area FROM World WHERE area &gt;= 3000000 OR population &gt;= 25000000\";\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var query = function() {\n    return 'SELECT name, population, area FROM World WHERE area &gt;= 3000000 OR population &gt;= 25000000';\n};" },
{ name: "TypeScript", lang: "typescript", code: "function query(): string {\n    return 'SELECT name, population, area FROM World WHERE area &gt;= 3000000 OR population &gt;= 25000000';\n}" },]} />

**Complexity**
- Time: O(N), where N is the number of rows in the `World` table. The database engine scans the table once.
- Space: O(1), excluding the space required for the output result.
- Notes: This is the most efficient approach as it requires a single pass over the data.

---
## Approach 2: Set Union

**Intuition**
We can treat the problem as finding the union of two sets: countries with large area and countries with large population. We can use the `UNION` operator to combine the results of two separate queries.

**Steps**
- Select `name`, `population`, and `area` from `World` where `area &gt;= 3000000`.
- Select `name`, `population`, and `area` from `World` where `population &gt;= 25000000`.
- Use `UNION` to combine these two result sets, which automatically removes duplicate rows (countries that meet both criteria).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def query(self) -&gt; str:\n        return 'SELECT name, population, area FROM World WHERE area &gt;= 3000000 UNION SELECT name, population, area FROM World WHERE population &gt;= 25000000'" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String query() {\n        return \"SELECT name, population, area FROM World WHERE area &gt;= 3000000 UNION SELECT name, population, area FROM World WHERE population &gt;= 25000000\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    string query() {\n        return \"SELECT name, population, area FROM World WHERE area &gt;= 3000000 UNION SELECT name, population, area FROM World WHERE population &gt;= 25000000\";\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var query = function() {\n    return 'SELECT name, population, area FROM World WHERE area &gt;= 3000000 UNION SELECT name, population, area FROM World WHERE population &gt;= 25000000';\n};" },
{ name: "TypeScript", lang: "typescript", code: "function query(): string {\n    return 'SELECT name, population, area FROM World WHERE area &gt;= 3000000 UNION SELECT name, population, area FROM World WHERE population &gt;= 25000000';\n}" },]} />

**Complexity**
- Time: O(N) or O(N log N), depending on the database implementation. `UNION` involves deduplication, which typically requires sorting or hashing.
- Space: O(N) in the worst case to store the intermediate results for the union operation.
- Notes: While logically correct, this approach is generally less efficient than the single `WHERE` clause with `OR` because it may require scanning the table twice or performing additional processing for deduplication.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Big Countries Solutions","datePublished":"2024-12-30","description":"Find the name, population, and area of countries with an area of at least three million square kilometers or a population of at least twenty-five million.","keywords":"LeetCode, Big Countries, Easy, Database"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Direct Filtering approach?","acceptedAnswer":{"@type":"Answer","text":"O(N), where N is the number of rows in the World table."}},{"@type":"Question","name":"What is the time complexity of the Set Union approach?","acceptedAnswer":{"@type":"Answer","text":"O(N) or O(N log N), depending on the database engine's implementation of UNION (deduplication)."}}]}).replace(/</g,'\u003c')}
</script>