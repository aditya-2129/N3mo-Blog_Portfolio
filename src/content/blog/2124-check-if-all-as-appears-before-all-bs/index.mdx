---
title: "Check if All A's Appears Before All B's"
summary: "Given a string s consisting of only characters 'a' and 'b', return true if every 'a' appears before every 'b'."
date: "Jan 10, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 73.20% | **Paid:** No
> **Topics:** String

Given a string s consisting of only characters 'a' and 'b', return true if every 'a' appears before every 'b', otherwise return false.

## Table of Contents
- Examples
- Constraints
- [Approach 1: Find Last A and First B](#approach-1-find-last-a-and-first-b)
- [Approach 2: Single Pass Scan](#approach-2-single-pass-scan)
- [Approach 3: Substring Check](#approach-3-substring-check)

## Examples

### Example 1
```text
Input: s = "aaabbb"
Output: true
Explanation:
The 'a's are at indices 0, 1, 2, while the 'b's are at indices 3, 4, 5.
Hence, every 'a' appears before every 'b'.
```

### Example 2
```text
Input: s = "abab"
Output: false
Explanation:
There is an 'a' at index 3 and a 'b' at index 2.
Hence, not every 'a' appears before every 'b'.
```

### Example 3
```text
Input: s = "bbb"
Output: true
Explanation:
There are no 'a's, so the condition holds vacuously.
```

## Constraints

```text
1 <= s.length <= 100
s[i] is either 'a' or 'b'.
```

---
## Approach 1: Find Last A and First B

**Intuition**
If the last occurrence of 'a' happens before the first occurrence of 'b', then all 'a's are before all 'b's. We can find these two indices and compare them.

**Steps**
- Iterate through the string to find the index of the last 'a'. If no 'a' exists, treat its index as -1.
- Iterate through the string to find the index of the first 'b'. If no 'b' exists, treat its index as `s.length`.
- Return true if the last index of 'a' is less than the first index of 'b', otherwise return false.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkString(self, s: str) -&gt; bool:\n        last_a = -1\n        first_b = len(s)\n        \n        for i, char in enumerate(s):\n            if char == 'a':\n                last_a = i\n            \n        for i, char in enumerate(s):\n            if char == 'b':\n                first_b = i\n                break\n                \n        return last_a &lt; first_b\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkString(String s) {\n        int lastA = -1;\n        int firstB = s.length();\n        \n        for (int i = 0; i &lt; s.length(); i++) {\n            if (s.charAt(i) == 'a') {\n                lastA = i;\n            }\n        }\n        \n        for (int i = 0; i &lt; s.length(); i++) {\n            if (s.charAt(i) == 'b') {\n                firstB = i;\n                break;\n            }\n        }\n        \n        return lastA &lt; firstB;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkString(string s) {\n        int lastA = -1;\n        int firstB = s.length();\n        \n        for (int i = 0; i &lt; s.length(); i++) {\n            if (s[i] == 'a') {\n                lastA = i;\n            }\n        }\n        \n        for (int i = 0; i &lt; s.length(); i++) {\n            if (s[i] == 'b') {\n                firstB = i;\n                break;\n            }\n        }\n        \n        return lastA &lt; firstB;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    checkString(s) {\n        let lastA = -1;\n        let firstB = s.length;\n        \n        for (let i = 0; i &lt; s.length; i++) {\n            if (s[i] === 'a') {\n                lastA = i;\n            }\n        }\n        \n        for (let i = 0; i &lt; s.length; i++) {\n            if (s[i] === 'b') {\n                firstB = i;\n                break;\n            }\n        }\n        \n        return lastA &lt; firstB;\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    checkString(s: string): boolean {\n        let lastA = -1;\n        let firstB = s.length;\n        \n        for (let i = 0; i &lt; s.length; i++) {\n            if (s[i] === 'a') {\n                lastA = i;\n            }\n        }\n        \n        for (let i = 0; i &lt; s.length; i++) {\n            if (s[i] === 'b') {\n                firstB = i;\n                break;\n            }\n        }\n        \n        return lastA &lt; firstB;\n    }\n}\n" },]} />

**Complexity**
- Time: O(n) - We iterate through the string twice.
- Space: O(1) - We only use a few variables for indices.
- Notes: This approach is intuitive but requires two passes over the string.

---
## Approach 2: Single Pass Scan

**Intuition**
We can iterate through the string once. If we encounter a 'b', we know that no 'a' should appear after it. We can use a flag to track if we have seen a 'b'.

**Steps**
- Initialize a boolean flag `seenB` to false.
- Iterate through each character in the string:
  - If the character is 'b', set `seenB` to true.
  - If the character is 'a' and `seenB` is true, it means we found an 'a' after a 'b', so return false immediately.
- If the loop finishes without returning false, return true.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkString(self, s: str) -&gt; bool:\n        seen_b = False\n        for char in s:\n            if char == 'b':\n                seen_b = True\n            elif char == 'a' and seen_b:\n                return False\n        return True\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkString(String s) {\n        boolean seenB = false;\n        for (int i = 0; i &lt; s.length(); i++) {\n            char c = s.charAt(i);\n            if (c == 'b') {\n                seenB = true;\n            } else if (c == 'a' && seenB) {\n                return false;\n            }\n        }\n        return true;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkString(string s) {\n        bool seenB = false;\n        for (char c : s) {\n            if (c == 'b') {\n                seenB = true;\n            } else if (c == 'a' && seenB) {\n                return false;\n            }\n        }\n        return true;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    checkString(s) {\n        let seenB = false;\n        for (let i = 0; i &lt; s.length; i++) {\n            const c = s[i];\n            if (c === 'b') {\n                seenB = true;\n            } else if (c === 'a' && seenB) {\n                return false;\n            }\n        }\n        return true;\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    checkString(s: string): boolean {\n        let seenB = false;\n        for (let i = 0; i &lt; s.length; i++) {\n            const c = s[i];\n            if (c === 'b') {\n                seenB = true;\n            } else if (c === 'a' && seenB) {\n                return false;\n            }\n        }\n        return true;\n    }\n}\n" },]} />

**Complexity**
- Time: O(n) - We iterate through the string once.
- Space: O(1) - We only use a boolean flag.
- Notes: This is more efficient than the two-pass approach as it can terminate early upon finding a violation.

---
## Approach 3: Substring Check

**Intuition**
The only pattern that violates the condition is an 'a' appearing after a 'b'. This corresponds to the substring "ba". If the string contains "ba", it is invalid; otherwise, it is valid.

**Steps**
- Check if the string "ba" exists within the input string `s`.
- Return true if "ba" is not found, and false otherwise.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkString(self, s: str) -&gt; bool:\n        return 'ba' not in s\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkString(String s) {\n        return s.indexOf(\"ba\") == -1;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkString(string s) {\n        return s.find(\"ba\") == string::npos;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    checkString(s) {\n        return !s.includes(\"ba\");\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    checkString(s: string): boolean {\n        return !s.includes(\"ba\");\n    }\n}\n" },]} />

**Complexity**
- Time: O(n) - The underlying implementation of substring search (e.g., KMP or Boyer-Moore in standard libraries) is linear.
- Space: O(1) - No extra space is used proportional to input size.
- Notes: This is the most concise solution and leverages built-in library functions effectively.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check if All A's Appears Before All B's Solutions","datePublished":"2024-01-10","description":"Given a string s consisting of only characters 'a' and 'b', return true if every 'a' appears before every 'b'.","keywords":"LeetCode, Check if All A's Appears Before All B's, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Single Pass Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string, as we iterate through the string once."}},{"@type":"Question","name":"What is the space complexity of the Substring Check approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only check for the existence of a substring without allocating significant extra memory."}},{"@type":"Question","name":"Why does checking for the substring 'ba' work?","acceptedAnswer":{"@type":"Answer","text":"The condition 'all a's before all b's' is violated if and only if there exists at least one 'b' followed by an 'a'. This specific sequence is exactly the substring 'ba'."}}]}).replace(/</g,'\u003c')}
</script>