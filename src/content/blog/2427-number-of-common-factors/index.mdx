---
title: "Number of Common Factors"
summary: "Given two positive integers a and b, return the number of common factors of a and b."
date: "Nov 18, 2025"
tags:
  - Math
  - Enumeration
  - Number Theory
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 80.10% | **Paid:** No
> **Topics:** Math, Enumeration, Number Theory

Given two positive integers a and b, return the number of common factors of a and b.

An integer x is a common factor of a and b if x divides both a and b.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Using GCD](#using-gcd)
- [Optimized Divisor Counting](#optimized-divisor-counting)

## Examples

### Example 1:
```text
Input: a = 12, b = 6
Output: 4
Explanation: The common factors of 12 and 6 are 1, 2, 3, 6.
```

### Example 2:
```text
Input: a = 25, b = 30
Output: 2
Explanation: The common factors of 25 and 30 are 1, 5.
```

## Constraints

```text
1 <= a, b <= 1000
```

---
## Brute Force

**Intuition**
Check every number from 1 to the minimum of a and b, counting those that divide both numbers.

**Steps**
- Iterate from 1 to min(a, b)
- For each number i, check if a % i == 0 and b % i == 0
- Count such numbers

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def commonFactors(self, a: int, b: int) -> int:
        count = 0
        for i in range(1, min(a, b) + 1):
            if a % i == 0 and b % i == 0:
                count += 1
        return count` },
{ name: "Java", lang: "java", code: `class Solution {
    public int commonFactors(int a, int b) {
        int count = 0;
        int min = Math.min(a, b);
        for (int i = 1; i &lt;= min; i++) {
            if (a % i == 0 && b % i == 0) {
                count++;
            }
        }
        return count;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int commonFactors(int a, int b) {
        int count = 0;
        int minVal = min(a, b);
        for (int i = 1; i &lt;= minVal; i++) {
            if (a % i == 0 && b % i == 0) {
                count++;
            }
        }
        return count;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} a
 * @param {number} b
 * @return {number}
 */
var commonFactors = function(a, b) {
    let count = 0;
    const min = Math.min(a, b);
    for (let i = 1; i &lt;= min; i++) {
        if (a % i === 0 && b % i === 0) {
            count++;
        }
    }
    return count;
};` },
{ name: "TypeScript", lang: "typescript", code: `function commonFactors(a: number, b: number): number {
    let count = 0;
    const min = Math.min(a, b);
    for (let i = 1; i &lt;= min; i++) {
        if (a % i === 0 && b % i === 0) {
            count++;
        }
    }
    return count;
}` },]} />

**Complexity**
- Time: O(min(a, b))
- Space: O(1)
- Notes: Simple but can be slow for large inputs

---
## Using GCD

**Intuition**
The common factors of a and b are exactly the divisors of their greatest common divisor (GCD).

**Steps**
- Compute GCD of a and b using Euclidean algorithm
- Count divisors of the GCD

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `import math

class Solution:
    def commonFactors(self, a: int, b: int) -> int:
        gcd = math.gcd(a, b)
        count = 0
        for i in range(1, gcd + 1):
            if gcd % i == 0:
                count += 1
        return count` },
{ name: "Java", lang: "java", code: `class Solution {
    public int commonFactors(int a, int b) {
        int gcd = gcd(a, b);
        int count = 0;
        for (int i = 1; i &lt;= gcd; i++) {
            if (gcd % i == 0) {
                count++;
            }
        }
        return count;
    }
    
    private int gcd(int a, int b) {
        while (b != 0) {
            int temp = b;
            b = a % b;
            a = temp;
        }
        return a;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <algorithm>
using namespace std;

class Solution {
public:
    int commonFactors(int a, int b) {
        int gcdVal = __gcd(a, b);
        int count = 0;
        for (int i = 1; i &lt;= gcdVal; i++) {
            if (gcdVal % i == 0) {
                count++;
            }
        }
        return count;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} a
 * @param {number} b
 * @return {number}
 */
var commonFactors = function(a, b) {
    const gcd = (x, y) =&gt; {
        while (y !== 0) {
            const temp = y;
            y = x % y;
            x = temp;
        }
        return x;
    };
    
    const gcdVal = gcd(a, b);
    let count = 0;
    for (let i = 1; i &lt;= gcdVal; i++) {
        if (gcdVal % i === 0) {
            count++;
        }
    }
    return count;
};` },
{ name: "TypeScript", lang: "typescript", code: `function commonFactors(a: number, b: number): number {
    const gcd = (x: number, y: number): number =&gt; {
        while (y !== 0) {
            const temp = y;
            y = x % y;
            x = temp;
        }
        return x;
    };
    
    const gcdVal = gcd(a, b);
    let count = 0;
    for (let i = 1; i &lt;= gcdVal; i++) {
        if (gcdVal % i === 0) {
            count++;
        }
    }
    return count;
}` },]} />

**Complexity**
- Time: O(gcd(a, b) + log(max(a, b)))
- Space: O(1)
- Notes: More efficient than brute force when a and b are very different

---
## Optimized Divisor Counting

**Intuition**
Divisors come in pairs. If i divides n, then n/i also divides n. We only need to check up to sqrt(n).

**Steps**
- Compute GCD of a and b
- Iterate from 1 to sqrt(gcd)
- For each divisor i, count both i and gcd/i (if different)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `import math

class Solution:
    def commonFactors(self, a: int, b: int) -> int:
        gcd = math.gcd(a, b)
        count = 0
        i = 1
        while i * i &lt;= gcd:
            if gcd % i == 0:
                if i * i == gcd:
                    count += 1
                else:
                    count += 2
            i += 1
        return count` },
{ name: "Java", lang: "java", code: `class Solution {
    public int commonFactors(int a, int b) {
        int gcd = gcd(a, b);
        int count = 0;
        for (int i = 1; (long) i * i &lt;= gcd; i++) {
            if (gcd % i == 0) {
                if (i * i == gcd) {
                    count++;
                } else {
                    count += 2;
                }
            }
        }
        return count;
    }
    
    private int gcd(int a, int b) {
        while (b != 0) {
            int temp = b;
            b = a % b;
            a = temp;
        }
        return a;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <algorithm>
using namespace std;

class Solution {
public:
    int commonFactors(int a, int b) {
        int gcdVal = __gcd(a, b);
        int count = 0;
        for (int i = 1; (long long) i * i &lt;= gcdVal; i++) {
            if (gcdVal % i == 0) {
                if (i * i == gcdVal) {
                    count++;
                } else {
                    count += 2;
                }
            }
        }
        return count;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} a
 * @param {number} b
 * @return {number}
 */
var commonFactors = function(a, b) {
    const gcd = (x, y) =&gt; {
        while (y !== 0) {
            const temp = y;
            y = x % y;
            x = temp;
        }
        return x;
    };
    
    const gcdVal = gcd(a, b);
    let count = 0;
    for (let i = 1; i * i &lt;= gcdVal; i++) {
        if (gcdVal % i === 0) {
            if (i * i === gcdVal) {
                count++;
            } else {
                count += 2;
            }
        }
    }
    return count;
};` },
{ name: "TypeScript", lang: "typescript", code: `function commonFactors(a: number, b: number): number {
    const gcd = (x: number, y: number): number =&gt; {
        while (y !== 0) {
            const temp = y;
            y = x % y;
            x = temp;
        }
        return x;
    };
    
    const gcdVal = gcd(a, b);
    let count = 0;
    for (let i = 1; i * i &lt;= gcdVal; i++) {
        if (gcdVal % i === 0) {
            if (i * i === gcdVal) {
                count++;
            } else {
                count += 2;
            }
        }
    }
    return count;
}` },]} />

**Complexity**
- Time: O(√gcd(a, b) + log(max(a, b)))
- Space: O(1)
- Notes: Most efficient approach, especially for large numbers

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Number of Common Factors Solutions","datePublished":"2025-11-18","description":"Given two positive integers a and b, return the number of common factors of a and b.","keywords":"LeetCode, Number of Common Factors, Easy, Math, Enumeration, Number Theory"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(min(a, b))."}},{"@type":"Question","name":"What is the time complexity of the Using GCD approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Using GCD approach is O(gcd(a, b) + log(max(a, b)))."}},{"@type":"Question","name":"What is the time complexity of the Optimized Divisor Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Optimized Divisor Counting approach is O(√gcd(a, b) + log(max(a, b)))."}}]}).replace(/</g,'\u003c')}
</script>