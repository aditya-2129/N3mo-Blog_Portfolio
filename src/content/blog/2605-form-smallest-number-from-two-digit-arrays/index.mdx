---
title: "Form Smallest Number From Two Digit Arrays"
summary: "Given two digit arrays, find the smallest number containing at least one digit from each array."
date: "Jun 21, 2025"
tags:
  - Array
  - Hash Table
  - Enumeration
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 55.20% | **Paid:** No
> **Topics:** Array, Hash Table, Enumeration

Given two arrays of integers `nums1` and `nums2`, return the smallest number that contains at least one digit from each array.

You are given two integer arrays `nums1` and `nums2` consisting of digits (0-9). You need to form a number by selecting digits from these arrays. The number must contain at least one digit from `nums1` and at least one digit from `nums2`.

Return the smallest such number. If there are multiple answers, return the smallest one. If no such number exists, return -1.

- Examples
- Constraints
- [Approach 1: Hash Set](#approach-1-hash-set)
- [Approach 2: Sorting](#approach-2-sorting)
- [Approach 3: Brute Force](#approach-3-brute-force)

## Examples

### Example 1

**Input:**
```text
nums1 = [4,1,3], nums2 = [5,7]
```

**Output:**
```text
15
```

**Explanation:**
The number 15 contains the digit 1 from nums1 and the digit 5 from nums2. It can be proven that 15 is the smallest number we can have.

### Example 2

**Input:**
```text
nums1 = [3,5,2,6], nums2 = [3,1,7]
```

**Output:**
```text
3
```

**Explanation:**
The number 3 contains the digit 3 which exists in both arrays.

## Constraints

```text
1 <= nums1.length, nums2.length <= 9
1 <= nums1[i], nums2[i] <= 9
All digits in each array are unique.
```

---

## Approach 1: Hash Set

**Intuition**
The smallest possible number is a single digit if there is a common digit between the two arrays. If no common digit exists, the smallest number must be a two-digit number formed by the smallest digit from each array.

**Steps**
- Convert `nums1` into a hash set for O(1) lookups.
- Iterate through `nums2` to find the smallest digit that exists in the set of `nums1`.
- If a common digit is found, return it immediately as it is the smallest possible answer.
- If no common digit is found, find the minimum digit in `nums1` (`min1`) and the minimum digit in `nums2` (`min2`).
- Return the smaller of the two possible two-digit numbers: `min1 * 10 + min2` or `min2 * 10 + min1`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minNumber(self, nums1: list[int], nums2: list[int]) -&gt; int:\n        set1 = set(nums1)\n        min_common = float('inf')\n        \n        for x in nums2:\n            if x in set1:\n                if x &lt; min_common:\n                    min_common = x\n        \n        if min_common != float('inf'):\n            return min_common\n        \n        min1 = min(nums1)\n        min2 = min(nums2)\n        \n        return min(min1 * 10 + min2, min2 * 10 + min1)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minNumber(int[] nums1, int[] nums2) {\n        java.util.Set&lt;Integer&gt; set = new java.util.HashSet&lt;&gt;();\n        for (int n : nums1) {\n            set.add(n);\n        }\n        \n        int minCommon = Integer.MAX_VALUE;\n        for (int n : nums2) {\n            if (set.contains(n)) {\n                minCommon = Math.min(minCommon, n);\n            }\n        }\n        \n        if (minCommon != Integer.MAX_VALUE) {\n            return minCommon;\n        }\n        \n        int min1 = 10;\n        int min2 = 10;\n        for (int n : nums1) min1 = Math.min(min1, n);\n        for (int n : nums2) min2 = Math.min(min2, n);\n        \n        return Math.min(min1 * 10 + min2, min2 * 10 + min1);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minNumber(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        unordered_set&lt;int&gt; s(nums1.begin(), nums1.end());\n        int minCommon = INT_MAX;\n        \n        for (int n : nums2) {\n            if (s.count(n)) {\n                minCommon = min(minCommon, n);\n            }\n        }\n        \n        if (minCommon != INT_MAX) {\n            return minCommon;\n        }\n        \n        int min1 = *min_element(nums1.begin(), nums1.end());\n        int min2 = *min_element(nums2.begin(), nums2.end());\n        \n        return min(min1 * 10 + min2, min2 * 10 + min1);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var minNumber = function(nums1, nums2) {\n    const set = new Set(nums1);\n    let minCommon = Infinity;\n    \n    for (const x of nums2) {\n        if (set.has(x)) {\n            minCommon = Math.min(minCommon, x);\n        }\n    }\n    \n    if (minCommon !== Infinity) {\n        return minCommon;\n    }\n    \n    const min1 = Math.min(...nums1);\n    const min2 = Math.min(...nums2);\n    \n    return Math.min(min1 * 10 + min2, min2 * 10 + min1);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minNumber(nums1: number[], nums2: number[]): number {\n    const set = new Set(nums1);\n    let minCommon = Infinity;\n    \n    for (const x of nums2) {\n        if (set.has(x)) {\n            minCommon = Math.min(minCommon, x);\n        }\n    }\n    \n    if (minCommon !== Infinity) {\n        return minCommon;\n    }\n    \n    const min1 = Math.min(...nums1);\n    const min2 = Math.min(...nums2);\n    \n    return Math.min(min1 * 10 + min2, min2 * 10 + min1);\n}" },]} />

**Complexity**
- Time: O(n + m), where n and m are the lengths of the arrays.
- Space: O(n) to store the hash set.
- Notes: This is the most optimal approach for time complexity.

---

## Approach 2: Sorting

**Intuition**
Sorting both arrays allows us to easily find the smallest digits and check for common digits using a two-pointer technique.

**Steps**
- Sort both `nums1` and `nums2` in ascending order.
- Use two pointers to iterate through both arrays simultaneously to find the first common digit.
- If a common digit is found, return it.
- If no common digit is found, the smallest digits are at index 0 of both arrays.
- Construct the two possible two-digit numbers and return the smaller one.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minNumber(self, nums1: list[int], nums2: list[int]) -&gt; int:\n        nums1.sort()\n        nums2.sort()\n        \n        i, j = 0, 0\n        while i &lt; len(nums1) and j &lt; len(nums2):\n            if nums1[i] == nums2[j]:\n                return nums1[i]\n            elif nums1[i] &lt; nums2[j]:\n                i += 1\n            else:\n                j += 1\n        \n        min1, min2 = nums1[0], nums2[0]\n        return min(min1 * 10 + min2, min2 * 10 + min1)" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int minNumber(int[] nums1, int[] nums2) {\n        Arrays.sort(nums1);\n        Arrays.sort(nums2);\n        \n        int i = 0, j = 0;\n        while (i &lt; nums1.length && j &lt; nums2.length) {\n            if (nums1[i] == nums2[j]) {\n                return nums1[i];\n            } else if (nums1[i] &lt; nums2[j]) {\n                i++;\n            } else {\n                j++;\n            }\n        }\n        \n        int min1 = nums1[0];\n        int min2 = nums2[0];\n        return Math.min(min1 * 10 + min2, min2 * 10 + min1);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int minNumber(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        sort(nums1.begin(), nums1.end());\n        sort(nums2.begin(), nums2.end());\n        \n        int i = 0, j = 0;\n        while (i &lt; nums1.size() && j &lt; nums2.size()) {\n            if (nums1[i] == nums2[j]) {\n                return nums1[i];\n            } else if (nums1[i] &lt; nums2[j]) {\n                i++;\n            } else {\n                j++;\n            }\n        }\n        \n        int min1 = nums1[0];\n        int min2 = nums2[0];\n        return min(min1 * 10 + min2, min2 * 10 + min1);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var minNumber = function(nums1, nums2) {\n    nums1.sort((a, b) =&gt; a - b);\n    nums2.sort((a, b) =&gt; a - b);\n    \n    let i = 0, j = 0;\n    while (i &lt; nums1.length && j &lt; nums2.length) {\n        if (nums1[i] === nums2[j]) {\n            return nums1[i];\n        } else if (nums1[i] &lt; nums2[j]) {\n            i++;\n        } else {\n            j++;\n        }\n    }\n    \n    const min1 = nums1[0];\n    const min2 = nums2[0];\n    return Math.min(min1 * 10 + min2, min2 * 10 + min1);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minNumber(nums1: number[], nums2: number[]): number {\n    nums1.sort((a, b) =&gt; a - b);\n    nums2.sort((a, b) =&gt; a - b);\n    \n    let i = 0, j = 0;\n    while (i &lt; nums1.length && j &lt; nums2.length) {\n        if (nums1[i] === nums2[j]) {\n            return nums1[i];\n        } else if (nums1[i] &lt; nums2[j]) {\n            i++;\n        } else {\n            j++;\n        }\n    }\n    \n    const min1 = nums1[0];\n    const min2 = nums2[0];\n    return Math.min(min1 * 10 + min2, min2 * 10 + min1);\n}" },]} />

**Complexity**
- Time: O(n log n + m log m) due to sorting.
- Space: O(1) or O(log n) depending on the sorting algorithm's stack space.
- Notes: Sorting is slightly slower than the hash set approach but avoids extra space for the set.

---

## Approach 3: Brute Force

**Intuition**
Since the constraints are very small (arrays of length at most 9), we can simply check every possible pair of digits to find commonalities and minimums.

**Steps**
- Initialize `min_common` to a value larger than 9 (e.g., 10).
- Initialize `min1` and `min2` to 10.
- Iterate through every digit `a` in `nums1` and every digit `b` in `nums2`.
- Update `min1` and `min2` with the minimum values found so far.
- If `a == b`, update `min_common` with the minimum of itself and `a`.
- If `min_common` is less than 10, return `min_common`.
- Otherwise, return the smaller of the two combinations of `min1` and `min2`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minNumber(self, nums1: list[int], nums2: list[int]) -&gt; int:\n        min_common = 10\n        min1 = 10\n        min2 = 10\n        \n        for a in nums1:\n            if a &lt; min1:\n                min1 = a\n            for b in nums2:\n                if b &lt; min2:\n                    min2 = b\n                if a == b and a &lt; min_common:\n                    min_common = a\n        \n        if min_common &lt; 10:\n            return min_common\n        \n        return min(min1 * 10 + min2, min2 * 10 + min1)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minNumber(int[] nums1, int[] nums2) {\n        int minCommon = 10;\n        int min1 = 10;\n        int min2 = 10;\n        \n        for (int a : nums1) {\n            min1 = Math.min(min1, a);\n            for (int b : nums2) {\n                min2 = Math.min(min2, b);\n                if (a == b) {\n                    minCommon = Math.min(minCommon, a);\n                }\n            }\n        }\n        \n        if (minCommon &lt; 10) {\n            return minCommon;\n        }\n        \n        return Math.min(min1 * 10 + min2, min2 * 10 + min1);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minNumber(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        int minCommon = 10;\n        int min1 = 10;\n        int min2 = 10;\n        \n        for (int a : nums1) {\n            min1 = min(min1, a);\n            for (int b : nums2) {\n                min2 = min(min2, b);\n                if (a == b) {\n                    minCommon = min(minCommon, a);\n                }\n            }\n        }\n        \n        if (minCommon &lt; 10) {\n            return minCommon;\n        }\n        \n        return min(min1 * 10 + min2, min2 * 10 + min1);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var minNumber = function(nums1, nums2) {\n    let minCommon = 10;\n    let min1 = 10;\n    let min2 = 10;\n    \n    for (const a of nums1) {\n        min1 = Math.min(min1, a);\n        for (const b of nums2) {\n            min2 = Math.min(min2, b);\n            if (a === b) {\n                minCommon = Math.min(minCommon, a);\n            }\n        }\n    }\n    \n    if (minCommon &lt; 10) {\n        return minCommon;\n    }\n    \n    return Math.min(min1 * 10 + min2, min2 * 10 + min1);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minNumber(nums1: number[], nums2: number[]): number {\n    let minCommon = 10;\n    let min1 = 10;\n    let min2 = 10;\n    \n    for (const a of nums1) {\n        min1 = Math.min(min1, a);\n        for (const b of nums2) {\n            min2 = Math.min(min2, b);\n            if (a === b) {\n                minCommon = Math.min(minCommon, a);\n            }\n        }\n    }\n    \n    if (minCommon &lt; 10) {\n        return minCommon;\n    }\n    \n    return Math.min(min1 * 10 + min2, min2 * 10 + min1);\n}" },]} />

**Complexity**
- Time: O(n * m), where n and m are the lengths of the arrays.
- Space: O(1).
- Notes: This approach is simple and acceptable given the small constraints, but less efficient than the hash set approach for larger inputs.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Form Smallest Number From Two Digit Arrays Solutions","datePublished":"2025-06-21","description":"Given two digit arrays, find the smallest number containing at least one digit from each array.","keywords":"LeetCode, Form Smallest Number From Two Digit Arrays, Easy, Array, Hash Table, Enumeration"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n + m), where n and m are the lengths of the two arrays."}},{"@type":"Question","name":"What is the space complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) or O(log n) depending on the sorting algorithm's implementation."}},{"@type":"Question","name":"Why is the Brute Force approach acceptable here?","acceptedAnswer":{"@type":"Answer","text":"The constraints are very small (arrays of length at most 9), so O(n * m) operations are negligible."}}]}).replace(/</g,'\u003c')}
</script>