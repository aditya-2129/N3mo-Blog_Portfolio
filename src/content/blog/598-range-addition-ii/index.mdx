---
title: "Range Addition II"
summary: "Given an m x n matrix and operations incrementing submatrices from (0,0), find the count of maximum integers."
date: "Apr 11, 2024"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 58.70% | **Paid:** No
> **Topics:** Array, Math

You are given an m x n matrix M initialized with all 0's and an array of operations ops, where ops[i] = [ai, bi] means increment all elements M[x][y] for 0 &lt;= x &lt; ai and 0 &lt;= y &lt; bi.

Count and return the number of maximum integers in the matrix after performing all the operations.

- Examples
- Constraints
- [Brute Force Simulation](#brute-force-simulation)
- [Mathematical Intersection](#mathematical-intersection)

## Examples

**Example 1:**

```text
Input: m = 3, n = 3, ops = [[2,2],[3,3]]
Output: 4
Explanation: The maximum integer in M is 2, and there are four of it in M. So return 4.
```

**Example 2:**

```text
Input: m = 3, n = 3, ops = [[2,2],[3,3],[3,3],[3,3],[2,2],[3,3],[3,3],[3,3],[2,2],[3,3],[3,3],[3,3]]
Output: 4
```

**Example 3:**

```text
Input: m = 3, n = 3, ops = []
Output: 9
```

## Constraints

```text
1 <= m, n <= 4 * 10⁴
0 <= ops.length <= 10⁴
ops[i].length == 2
1 <= ai <= m
1 <= bi <= n
```

---
## Brute Force Simulation

**Intuition**
Simulate the process exactly as described by creating the matrix and applying each operation to the relevant submatrix.

**Steps**
- Initialize an m x n matrix with all zeros.
- Iterate through each operation in the ops array.
- For each operation [a, b], iterate through rows 0 to a-1 and columns 0 to b-1, incrementing each cell by 1.
- After all operations, find the maximum value in the matrix.
- Count how many times this maximum value appears and return the count.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def maxCount(self, m: int, n: int, ops: list[list[int]]) -> int:\n        M = [[0] * n for _ in range(m)]\n        for a, b in ops:\n            for i in range(a):\n                for j in range(b):\n                    M[i][j] += 1\n        max_val = 0\n        count = 0\n        for i in range(m):\n            for j in range(n):\n                if M[i][j] > max_val:\n                    max_val = M[i][j]\n                    count = 1\n                elif M[i][j] == max_val:\n                    count += 1\n        return count' },
{ name: "Java", lang: "java", code: 'class Solution {\n    public int maxCount(int m, int n, int[][] ops) {\n        int[][] M = new int[m][n];\n        for (int[] op : ops) {\n            int a = op[0];\n            int b = op[1];\n            for (int i = 0; i &lt; a; i++) {\n                for (int j = 0; j &lt; b; j++) {\n                    M[i][j]++;\n                }\n            }\n        }\n        int maxVal = 0;\n        int count = 0;\n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (M[i][j] &gt; maxVal) {\n                    maxVal = M[i][j];\n                    count = 1;\n                } else if (M[i][j] == maxVal) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n}' },
{ name: "C++", lang: "cpp", code: 'class Solution {\npublic:\n    int maxCount(int m, int n, vector<vector<int>&gt;& ops) {\n        vector<vector<int>&gt; M(m, vector<int>(n, 0));\n        for (const auto& op : ops) {\n            int a = op[0];\n            int b = op[1];\n            for (int i = 0; i &lt; a; ++i) {\n                for (int j = 0; j &lt; b; ++j) {\n                    M[i][j]++;\n                }\n            }\n        }\n        int maxVal = 0;\n        int count = 0;\n        for (int i = 0; i &lt; m; ++i) {\n            for (int j = 0; j &lt; n; ++j) {\n                if (M[i][j] &gt; maxVal) {\n                    maxVal = M[i][j];\n                    count = 1;\n                } else if (M[i][j] == maxVal) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n};' },
{ name: "JavaScript", lang: "javascript", code: 'var maxCount = function(m, n, ops) {\n    let M = Array.from({length: m}, () =&gt; new Array(n).fill(0));\n    for (const [a, b] of ops) {\n        for (let i = 0; i &lt; a; i++) {\n            for (let j = 0; j &lt; b; j++) {\n                M[i][j]++;\n            }\n        }\n    }\n    let maxVal = 0;\n    let count = 0;\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (M[i][j] &gt; maxVal) {\n                maxVal = M[i][j];\n                count = 1;\n            } else if (M[i][j] === maxVal) {\n                count++;\n            }\n        }\n    }\n    return count;\n};' },
{ name: "TypeScript", lang: "typescript", code: 'function maxCount(m: number, n: number, ops: number[][]): number {\n    let M: number[][] = Array.from({length: m}, () =&gt; new Array(n).fill(0));\n    for (const [a, b] of ops) {\n        for (let i = 0; i &lt; a; i++) {\n            for (let j = 0; j &lt; b; j++) {\n                M[i][j]++;\n            }\n        }\n    }\n    let maxVal: number = 0;\n    let count: number = 0;\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (M[i][j] &gt; maxVal) {\n                maxVal = M[i][j];\n                count = 1;\n            } else if (M[i][j] === maxVal) {\n                count++;\n            }\n        }\n    }\n    return count;\n}' },]} />

**Complexity**
- Time: O(K * M * N) where K is the number of operations. In the worst case, this is very slow.
- Space: O(M * N) to store the matrix.
- Notes: This approach is not efficient for large inputs but demonstrates the literal interpretation of the problem.

---
## Mathematical Intersection

**Intuition**
Since every operation starts at the top-left corner (0, 0), the cells that are incremented the most times are the ones common to all operation rectangles. The maximum value is determined by the overlap of all operations.

**Steps**
- Initialize `min_row` to `m` and `min_col` to `n`.
- Iterate through each operation `[a, b]`.
- Update `min_row` to be the minimum of current `min_row` and `a`.
- Update `min_col` to be the minimum of current `min_col` and `b`.
- The number of maximum integers is the area of the overlapping rectangle: `min_row * min_col`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def maxCount(self, m: int, n: int, ops: list[list[int]]) -> int:\n        min_a = m\n        min_b = n\n        for a, b in ops:\n            min_a = min(min_a, a)\n            min_b = min(min_b, b)\n        return min_a * min_b' },
{ name: "Java", lang: "java", code: 'class Solution {\n    public int maxCount(int m, int n, int[][] ops) {\n        int minA = m;\n        int minB = n;\n        for (int[] op : ops) {\n            minA = Math.min(minA, op[0]);\n            minB = Math.min(minB, op[1]);\n        }\n        return minA * minB;\n    }\n}' },
{ name: "C++", lang: "cpp", code: 'class Solution {\npublic:\n    int maxCount(int m, int n, vector<vector<int>&gt;& ops) {\n        int minA = m;\n        int minB = n;\n        for (const auto& op : ops) {\n            minA = min(minA, op[0]);\n            minB = min(minB, op[1]);\n        }\n        return minA * minB;\n    }\n};' },
{ name: "JavaScript", lang: "javascript", code: 'var maxCount = function(m, n, ops) {\n    let minA = m;\n    let minB = n;\n    for (const [a, b] of ops) {\n        minA = Math.min(minA, a);\n        minB = Math.min(minB, b);\n    }\n    return minA * minB;\n};' },
{ name: "TypeScript", lang: "typescript", code: 'function maxCount(m: number, n: number, ops: number[][]): number {\n    let minA: number = m;\n    let minB: number = n;\n    for (const [a, b] of ops) {\n        minA = Math.min(minA, a);\n        minB = Math.min(minB, b);\n    }\n    return minA * minB;\n}' },]} />

**Complexity**
- Time: O(K) where K is the number of operations.
- Space: O(1) as we only store a few variables.
- Notes: This is the optimal solution, avoiding the need for matrix simulation.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Range Addition II Solutions","datePublished":"2024-04-11","description":"Given an m x n matrix and operations incrementing submatrices from (0,0), find the count of maximum integers.","keywords":"LeetCode, Range Addition II, Easy, Array, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(K * M * N), where K is the number of operations, and M and N are the dimensions of the matrix."}},{"@type":"Question","name":"What is the time complexity of the Mathematical Intersection approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(K), where K is the number of operations, as we only need to find the minimum dimensions."}},{"@type":"Question","name":"Why is the Mathematical Intersection approach optimal?","acceptedAnswer":{"@type":"Answer","text":"It avoids simulating the matrix updates by recognizing that the maximum values are always located in the overlapping region of all operations starting from (0,0)."}}]}).replace(/</g,'\u003c')}
</script>