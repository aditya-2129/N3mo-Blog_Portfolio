---
title: "Find All K-Distant Indices in an Array"
summary: "Find all indices i in an array such that there exists an index j where nums[j] equals key and the absolute difference between i and j is at most k."
date: "Dec 12, 2024"
tags:
  - Array
  - Two Pointers
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 77.30% | **Paid:** No
> **Topics:** Array, Two Pointers

You are given a 0-indexed integer array nums and two integers key and k. A k-distant index is an index i of nums for which there exists an index j such that nums[j] == key and abs(i - j) &lt;= k.

Return the list of all k-distant indices sorted in increasing order.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Boolean Array Marking](#boolean-array-marking)
- [Precompute Nearest Key](#precompute-nearest-key)

## Examples

```text
Input: nums = [3,4,9,1,3,9,2], key = 9, k = 1
Output: [1,2,3,4,5,6]
Explanation: The k-distant indices with key = 9 are:
- nums[2] == 9 and abs(0 - 2) > 1
- nums[2] == 9 and abs(1 - 2) <= 1
- nums[2] == 9 and abs(2 - 2) <= 1
- nums[5] == 9 and abs(3 - 5) > 1
- nums[5] == 9 and abs(4 - 5) <= 1
- nums[5] == 9 and abs(5 - 5) <= 1
- nums[5] == 9 and abs(6 - 5) <= 1
Hence, the k-distant indices are [1,2,3,4,5,6].
```

```text
Input: nums = [2,2,2,2,2], key = 2, k = 2
Output: [0,1,2,3,4]
Explanation: All indices are k-distant indices because every element is equal to key and the distance is at most 2.
```

## Constraints

```text
1 <= nums.length <= 1000
1 <= nums[i] <= 1000
key is an element in nums.
1 <= k <= nums.length
```

---
## Brute Force

**Intuition**
Check every index `i` against every index `j` to see if the condition `nums[j] == key` and `abs(i - j) &lt;= k` holds true.

**Steps**
- Initialize an empty list `result`.
- Iterate through each index `i` from 0 to `n-1`.
- For each `i`, iterate through each index `j` from 0 to `n-1`.
- If `nums[j] == key` and `abs(i - j) &lt;= k`, add `i` to `result` and break the inner loop (since we only need one valid `j` per `i`).
- Return `result`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findKDistantIndices(self, nums: list[int], key: int, k: int) -&gt; list[int]:\n        n = len(nums)\n        res = []\n        for i in range(n):\n            for j in range(n):\n                if nums[j] == key and abs(i - j) &lt;= k:\n                    res.append(i)\n                    break\n        return res" },
{ name: "Java", lang: "java", code: "class Solution {\n    public List&lt;Integer&gt; findKDistantIndices(int[] nums, int key, int k) {\n        int n = nums.length;\n        List&lt;Integer&gt; res = new ArrayList&lt;&gt;();\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (nums[j] == key && Math.abs(i - j) &lt;= k) {\n                    res.add(i);\n                    break;\n                }\n            }\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; findKDistantIndices(vector&lt;int&gt;& nums, int key, int k) {\n        int n = nums.size();\n        vector&lt;int&gt; res;\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (nums[j] == key && abs(i - j) &lt;= k) {\n                    res.push_back(i);\n                    break;\n                }\n            }\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var findKDistantIndices = function(nums, key, k) {\n    const n = nums.length;\n    const res = [];\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (nums[j] === key && Math.abs(i - j) &lt;= k) {\n                res.push(i);\n                break;\n            }\n        }\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findKDistantIndices(nums: number[], key: number, k: number): number[] {\n    const n = nums.length;\n    const res: number[] = [];\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (nums[j] === key && Math.abs(i - j) &lt;= k) {\n                res.push(i);\n                break;\n            }\n        }\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1) (excluding output)
- Notes: Simple to implement but inefficient for large arrays.

---
## Boolean Array Marking

**Intuition**
Instead of checking every `i`, find the valid ranges for `i` based on where `key` is located. For every index `j` where `nums[j] == key`, all indices `i` in the range `[j-k, j+k]` are valid.

**Steps**
- Create a boolean array `visited` of size `n`, initialized to false.
- Iterate through the array to find indices `j` where `nums[j] == key`.
- For each such `j`, mark all indices `i` from `max(0, j-k)` to `min(n-1, j+k)` as true in the `visited` array.
- Collect all indices `i` where `visited[i]` is true.
- Return the collected indices.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findKDistantIndices(self, nums: list[int], key: int, k: int) -&gt; list[int]:\n        n = len(nums)\n        visited = [False] * n\n        for j in range(n):\n            if nums[j] == key:\n                start = max(0, j - k)\n                end = min(n - 1, j + k)\n                for i in range(start, end + 1):\n                    visited[i] = True\n        return [i for i in range(n) if visited[i]]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public List&lt;Integer&gt; findKDistantIndices(int[] nums, int key, int k) {\n        int n = nums.length;\n        boolean[] visited = new boolean[n];\n        for (int j = 0; j &lt; n; j++) {\n            if (nums[j] == key) {\n                int start = Math.max(0, j - k);\n                int end = Math.min(n - 1, j + k);\n                for (int i = start; i &lt;= end; i++) {\n                    visited[i] = true;\n                }\n            }\n        }\n        List&lt;Integer&gt; res = new ArrayList&lt;&gt;();\n        for (int i = 0; i &lt; n; i++) {\n            if (visited[i]) {\n                res.add(i);\n            }\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; findKDistantIndices(vector&lt;int&gt;& nums, int key, int k) {\n        int n = nums.size();\n        vector&lt;bool&gt; visited(n, false);\n        for (int j = 0; j &lt; n; j++) {\n            if (nums[j] == key) {\n                int start = max(0, j - k);\n                int end = min(n - 1, j + k);\n                for (int i = start; i &lt;= end; i++) {\n                    visited[i] = true;\n                }\n            }\n        }\n        vector&lt;int&gt; res;\n        for (int i = 0; i &lt; n; i++) {\n            if (visited[i]) {\n                res.push_back(i);\n            }\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var findKDistantIndices = function(nums, key, k) {\n    const n = nums.length;\n    const visited = new Array(n).fill(false);\n    for (let j = 0; j &lt; n; j++) {\n        if (nums[j] === key) {\n            const start = Math.max(0, j - k);\n            const end = Math.min(n - 1, j + k);\n            for (let i = start; i &lt;= end; i++) {\n                visited[i] = true;\n            }\n        }\n    }\n    const res = [];\n    for (let i = 0; i &lt; n; i++) {\n        if (visited[i]) {\n            res.push(i);\n        }\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findKDistantIndices(nums: number[], key: number, k: number): number[] {\n    const n = nums.length;\n    const visited: boolean[] = new Array(n).fill(false);\n    for (let j = 0; j &lt; n; j++) {\n        if (nums[j] === key) {\n            const start = Math.max(0, j - k);\n            const end = Math.min(n - 1, j + k);\n            for (let i = start; i &lt;= end; i++) {\n                visited[i] = true;\n            }\n        }\n    }\n    const res: number[] = [];\n    for (let i = 0; i &lt; n; i++) {\n        if (visited[i]) {\n            res.push(i);\n        }\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(n * k)
- Space: O(n)
- Notes: More efficient than brute force when k is small.

---
## Precompute Nearest Key

**Intuition**
For each index `i`, we only need to know the closest `key` to the left and right. We can precompute these distances in two passes.

**Steps**
- Create arrays `left` and `right` of size `n`.
- Iterate left to right to fill `left` with the index of the nearest `key` seen so far.
- Iterate right to left to fill `right` with the index of the nearest `key` seen so far.
- Iterate `i` from 0 to `n-1`. Check if `abs(i - left[i]) &lt;= k` or `abs(i - right[i]) &lt;= k`.
- If yes, add `i` to result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findKDistantIndices(self, nums: list[int], key: int, k: int) -&gt; list[int]:\n        n = len(nums)\n        left = [-1] * n\n        right = [-1] * n\n        \n        last = -1\n        for i in range(n):\n            if nums[i] == key:\n                last = i\n            left[i] = last\n            \n        last = -1\n        for i in range(n - 1, -1, -1):\n            if nums[i] == key:\n                last = i\n            right[i] = last\n            \n        res = []\n        for i in range(n):\n            if (left[i] != -1 and abs(i - left[i]) &lt;= k) or (right[i] != -1 and abs(i - right[i]) &lt;= k):\n                res.append(i)\n        return res" },
{ name: "Java", lang: "java", code: "class Solution {\n    public List&lt;Integer&gt; findKDistantIndices(int[] nums, int key, int k) {\n        int n = nums.length;\n        int[] left = new int[n];\n        int[] right = new int[n];\n        \n        int last = -1;\n        for (int i = 0; i &lt; n; i++) {\n            if (nums[i] == key) {\n                last = i;\n            }\n            left[i] = last;\n        }\n        \n        last = -1;\n        for (int i = n - 1; i &gt;= 0; i--) {\n            if (nums[i] == key) {\n                last = i;\n            }\n            right[i] = last;\n        }\n        \n        List&lt;Integer&gt; res = new ArrayList&lt;&gt;();\n        for (int i = 0; i &lt; n; i++) {\n            if ((left[i] != -1 && Math.abs(i - left[i]) &lt;= k) || (right[i] != -1 && Math.abs(i - right[i]) &lt;= k)) {\n                res.add(i);\n            }\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; findKDistantIndices(vector&lt;int&gt;& nums, int key, int k) {\n        int n = nums.size();\n        vector&lt;int&gt; left(n, -1);\n        vector&lt;int&gt; right(n, -1);\n        \n        int last = -1;\n        for (int i = 0; i &lt; n; i++) {\n            if (nums[i] == key) {\n                last = i;\n            }\n            left[i] = last;\n        }\n        \n        last = -1;\n        for (int i = n - 1; i &gt;= 0; i--) {\n            if (nums[i] == key) {\n                last = i;\n            }\n            right[i] = last;\n        }\n        \n        vector&lt;int&gt; res;\n        for (int i = 0; i &lt; n; i++) {\n            if ((left[i] != -1 && abs(i - left[i]) &lt;= k) || (right[i] != -1 && abs(i - right[i]) &lt;= k)) {\n                res.push_back(i);\n            }\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var findKDistantIndices = function(nums, key, k) {\n    const n = nums.length;\n    const left = new Array(n).fill(-1);\n    const right = new Array(n).fill(-1);\n    \n    let last = -1;\n    for (let i = 0; i &lt; n; i++) {\n        if (nums[i] === key) {\n            last = i;\n        }\n        left[i] = last;\n    }\n    \n    last = -1;\n    for (let i = n - 1; i &gt;= 0; i--) {\n        if (nums[i] === key) {\n            last = i;\n        }\n        right[i] = last;\n    }\n    \n    const res = [];\n    for (let i = 0; i &lt; n; i++) {\n        if ((left[i] !== -1 && Math.abs(i - left[i]) &lt;= k) || (right[i] !== -1 && Math.abs(i - right[i]) &lt;= k)) {\n            res.push(i);\n        }\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findKDistantIndices(nums: number[], key: number, k: number): number[] {\n    const n = nums.length;\n    const left: number[] = new Array(n).fill(-1);\n    const right: number[] = new Array(n).fill(-1);\n    \n    let last = -1;\n    for (let i = 0; i &lt; n; i++) {\n        if (nums[i] === key) {\n            last = i;\n        }\n        left[i] = last;\n    }\n    \n    last = -1;\n    for (let i = n - 1; i &gt;= 0; i--) {\n        if (nums[i] === key) {\n            last = i;\n        }\n        right[i] = last;\n    }\n    \n    const res: number[] = [];\n    for (let i = 0; i &lt; n; i++) {\n        if ((left[i] !== -1 && Math.abs(i - left[i]) &lt;= k) || (right[i] !== -1 && Math.abs(i - right[i]) &lt;= k)) {\n            res.push(i);\n        }\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Most efficient time complexity, but uses extra space for the `left` and `right` arrays.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find All K-Distant Indices in an Array Solutions","datePublished":"2024-12-12","description":"Find all indices i in an array such that there exists an index j where nums[j] equals key and the absolute difference between i and j is at most k.","keywords":"LeetCode, Find All K-Distant Indices in an Array, Easy, Array, Two Pointers"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n²)."}},{"@type":"Question","name":"What is the time complexity of the Boolean Array Marking approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Boolean Array Marking approach is O(n * k)."}},{"@type":"Question","name":"What is the time complexity of the Precompute Nearest Key approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Precompute Nearest Key approach is O(n)."}}]}).replace(/</g,'\u003c')}
</script>