---
title: "Counting Words With a Given Prefix"
summary: "Return the number of strings in an array that start with a given prefix."
date: "Oct 14, 2024"
tags:
  - Array
  - String
  - String Matching
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 84.40% | **Paid:** No
> **Topics:** Array, String, String Matching

You are given an array of strings `words` and a string `pref`.

Return the number of strings in `words` that start with `pref`.

A string `s` starts with `pref` if `s` is equal to `pref` concatenated with some string (possibly empty).

- Examples
- Constraints
- [Approach 1: Iterative Comparison](#approach-1-iterative-comparison)
- [Approach 2: Built-in String Methods](#approach-2-built-in-string-methods)

## Examples

**Example 1:**

```text
Input: words = ["pay","attention","practice","attend"], pref = "at"
Output: 2
Explanation: "pay" does not start with "at", so it is not counted.
"attention" and "attend" start with "at", so they are counted.
```

**Example 2:**

```text
Input: words = ["leetcode","win","loops","success"], pref = "code"
Output: 0
Explanation: None of the strings start with "code".
```

## Constraints

```text
1 <= words.length <= 100
1 <= words[i].length, pref.length <= 100
words[i] and pref consist of lowercase English letters.
```

---

## Approach 1: Iterative Comparison

**Intuition**
We can manually check each word by comparing characters one by one to see if they match the prefix.

**Steps**
- Initialize a counter to 0.
- Iterate through each word in the array.
- If the word's length is less than the prefix length, skip it.
- Loop through the characters of the prefix and compare them with the corresponding characters in the word.
- If all characters match, increment the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def prefixCount(self, words: list[str], pref: str) -&gt; int:\n        count = 0\n        p_len = len(pref)\n        for w in words:\n            if len(w) &gt;= p_len:\n                match = True\n                for i in range(p_len):\n                    if w[i] != pref[i]:\n                        match = False\n                        break\n                if match:\n                    count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int prefixCount(String[] words, String pref) {\n        int count = 0;\n        int pLen = pref.length();\n        for (String w : words) {\n            if (w.length() &gt;= pLen) {\n                boolean match = true;\n                for (int i = 0; i &lt; pLen; i++) {\n                    if (w.charAt(i) != pref.charAt(i)) {\n                        match = false;\n                        break;\n                    }\n                }\n                if (match) count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int prefixCount(vector&lt;string&gt;& words, string pref) {\n        int count = 0;\n        int pLen = pref.length();\n        for (const string& w : words) {\n            if (w.length() &gt;= pLen) {\n                bool match = true;\n                for (int i = 0; i &lt; pLen; i++) {\n                    if (w[i] != pref[i]) {\n                        match = false;\n                        break;\n                    }\n                }\n                if (match) count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var prefixCount = function(words, pref) {\n    let count = 0;\n    const pLen = pref.length;\n    for (const w of words) {\n        if (w.length &gt;= pLen) {\n            let match = true;\n            for (let i = 0; i &lt; pLen; i++) {\n                if (w[i] !== pref[i]) {\n                    match = false;\n                    break;\n                }\n            }\n            if (match) count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function prefixCount(words: string[], pref: string): number {\n    let count = 0;\n    const pLen = pref.length;\n    for (const w of words) {\n        if (w.length &gt;= pLen) {\n            let match = true;\n            for (let i = 0; i &lt; pLen; i++) {\n                if (w[i] !== pref[i]) {\n                    match = false;\n                    break;\n                }\n            }\n            if (match) count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(N * M), where N is the number of words and M is the length of the prefix.
- Space: O(1)
- Notes: This approach is fundamental and works in any programming environment without relying on standard library helpers.

---

## Approach 2: Built-in String Methods

**Intuition**
Most modern programming languages provide optimized built-in methods to check if a string starts with a specific substring.

**Steps**
- Initialize a counter to 0.
- Iterate through each word in the array.
- Use the language's built-in "starts with" function to check if the word begins with the prefix.
- If true, increment the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def prefixCount(self, words: list[str], pref: str) -&gt; int:\n        return sum(1 for w in words if w.startswith(pref))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int prefixCount(String[] words, String pref) {\n        int count = 0;\n        for (String w : words) {\n            if (w.startsWith(pref)) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int prefixCount(vector&lt;string&gt;& words, string pref) {\n        int count = 0;\n        for (const string& w : words) {\n            if (w.rfind(pref, 0) == 0) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var prefixCount = function(words, pref) {\n    let count = 0;\n    for (const w of words) {\n        if (w.startsWith(pref)) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function prefixCount(words: string[], pref: string): number {\n    let count = 0;\n    for (const w of words) {\n        if (w.startsWith(pref)) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(N * M), where N is the number of words and M is the length of the prefix.
- Space: O(1)
- Notes: While the time complexity is theoretically the same as the iterative approach, built-in methods are often highly optimized and result in cleaner, more readable code.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Counting Words With a Given Prefix Solutions","datePublished":"2024-10-14","description":"Return the number of strings in an array that start with a given prefix.","keywords":"LeetCode, Counting Words With a Given Prefix, Easy, Array, String, String Matching"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Comparison approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * M), where N is the number of words and M is the length of the prefix."}},{"@type":"Question","name":"What is the time complexity of the Built-in String Methods approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * M), where N is the number of words and M is the length of the prefix."}}]}).replace(/</g,'\u003c')}
</script>