---
title: "Convert Date to Binary"
summary: "Convert a date string from decimal format to binary format by converting year, month, and day separately."
date: "Jan 16, 2024"
tags:
  - Math
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 88.80% | **Paid:** No
> **Topics:** Math, String

You are given a string date in the format YYYY-MM-DD (for example, 2024-02-16).

Convert the date string to the format of binary representation and return the result.

The binary representation of a number is the representation of that number in base-2. For example, the binary representation of 5 is "101", and the binary representation of 10 is "1010".

- Examples
- Constraints
- [Approach 1: Built-in Conversion](#approach-1-built-in-conversion)
- [Approach 2: Manual Binary Conversion](#approach-2-manual-binary-conversion)

## Examples

**Example 1:**

```text
Input: date = "2080-02-10"
Output: "100000100000-10-1010"
Explanation:
2080 in binary is "100000100000".
02 in binary is "10".
10 in binary is "1010".
```

**Example 2:**

```text
Input: date = "1900-01-01"
Output: "11101101100-1-1"
Explanation:
1900 in binary is "11101101100".
01 in binary is "1".
01 in binary is "1".
```

## Constraints

```text
- date.length == 10
- date[4] == date[7] == '-', and all other date[i]'s are digits.
- The input is generated such that date represents a valid Gregorian calendar date between Jan 1^st, 1900 and Dec 31^st, 2100 (both inclusive).
```

---
## Approach 1: Built-in Conversion

**Intuition**
Most modern programming languages provide built-in utilities to convert integers to binary strings. We can leverage these standard library functions to parse the date components and convert them directly.

**Steps**
- Split the input string by the delimiter '-' to separate year, month, and day.
- Parse the resulting substrings into integers.
- Use the language's built-in method (e.g., `bin()`, `Integer.toBinaryString()`, `toString(2)`) to convert each integer to its binary string representation.
- Join the three binary strings with '-' and return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def convertDateToBinary(self, date: str) -&gt; str:\n        y, m, d = date.split('-')\n        return bin(int(y))[2:] + '-' + bin(int(m))[2:] + '-' + bin(int(d))[2:]\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String convertDateToBinary(String date) {\n        String[] parts = date.split(\"-\");\n        return Integer.toBinaryString(Integer.parseInt(parts[0])) + \"-\" +\n               Integer.toBinaryString(Integer.parseInt(parts[1])) + \"-\" +\n               Integer.toBinaryString(Integer.parseInt(parts[2]));\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    string toBinary(int n) {\n        if (n == 0) return \"0\";\n        string res = \"\";\n        while (n &gt; 0) {\n            res = (char)('0' + (n % 2)) + res;\n            n /= 2;\n        }\n        return res;\n    }\n\n    string convertDateToBinary(string date) {\n        int year = stoi(date.substr(0, 4));\n        int month = stoi(date.substr(5, 2));\n        int day = stoi(date.substr(8, 2));\n        return toBinary(year) + \"-\" + toBinary(month) + \"-\" + toBinary(day);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    convertDateToBinary(date) {\n        const [y, m, d] = date.split('-');\n        const toBin = (n) =&gt; parseInt(n).toString(2);\n        return toBin(y) + '-' + toBin(m) + '-' + toBin(d);\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    convertDateToBinary(date: string): string {\n        const [y, m, d] = date.split('-');\n        const toBin = (n: string) =&gt; parseInt(n, 10).toString(2);\n        return toBin(y) + '-' + toBin(m) + '-' + toBin(d);\n    }\n}\n" },]} />

**Complexity**
- Time: O(1) - The number of operations is constant and bounded by the maximum value of the year (2100), which has a fixed number of bits.
- Space: O(1) - The space used for the output string is constant in size relative to the input constraints.
- Notes: This approach is the most concise and readable for production code.

---
## Approach 2: Manual Binary Conversion

**Intuition**
We can implement the mathematical process of converting a decimal number to binary manually. This involves repeatedly dividing the number by 2 and recording the remainders.

**Steps**
- Parse the year, month, and day from the string.
- For each number, initialize an empty string to store the binary result.
- While the number is greater than 0:
    - Prepend the remainder of the number divided by 2 (n % 2) to the result string.
    - Update the number by integer division by 2 (n /= 2).
- Join the resulting binary strings with '-'.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def convertDateToBinary(self, date: str) -&gt; str:\n        def to_bin(n):\n            if n == 0: return '0'\n            res = ''\n            while n &gt; 0:\n                res = str(n % 2) + res\n                n //= 2\n            return res\n\n        y, m, d = date.split('-')\n        return to_bin(int(y)) + '-' + to_bin(int(m)) + '-' + to_bin(int(d))\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String convertDateToBinary(String date) {\n        String[] parts = date.split(\"-\");\n        return toBinary(Integer.parseInt(parts[0])) + \"-\" +\n               toBinary(Integer.parseInt(parts[1])) + \"-\" +\n               toBinary(Integer.parseInt(parts[2]));\n    }\n\n    private String toBinary(int n) {\n        if (n == 0) return \"0\";\n        StringBuilder sb = new StringBuilder();\n        while (n &gt; 0) {\n            sb.insert(0, n % 2);\n            n /= 2;\n        }\n        return sb.toString();\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    string toBinary(int n) {\n        if (n == 0) return \"0\";\n        string res = \"\";\n        while (n &gt; 0) {\n            res = (char)('0' + (n % 2)) + res;\n            n /= 2;\n        }\n        return res;\n    }\n\n    string convertDateToBinary(string date) {\n        int year = stoi(date.substr(0, 4));\n        int month = stoi(date.substr(5, 2));\n        int day = stoi(date.substr(8, 2));\n        return toBinary(year) + \"-\" + toBinary(month) + \"-\" + toBinary(day);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    convertDateToBinary(date) {\n        const toBin = (n) =&gt; {\n            if (n === 0) return '0';\n            let res = '';\n            while (n &gt; 0) {\n                res = (n % 2) + res;\n                n = Math.floor(n / 2);\n            }\n            return res;\n        };\n\n        const [y, m, d] = date.split('-').map(Number);\n        return toBin(y) + '-' + toBin(m) + '-' + toBin(d);\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    convertDateToBinary(date: string): string {\n        const toBin = (n: number): string =&gt; {\n            if (n === 0) return '0';\n            let res = '';\n            while (n &gt; 0) {\n                res = (n % 2) + res;\n                n = Math.floor(n / 2);\n            }\n            return res;\n        };\n\n        const [y, m, d] = date.split('-').map(Number);\n        return toBin(y) + '-' + toBin(m) + '-' + toBin(d);\n    }\n}\n" },]} />

**Complexity**
- Time: O(1) - The loop runs for log₂(n) iterations, where n is at most 2100. This is a constant upper bound.
- Space: O(1) - Excluding the output string, we use only a few variables for computation.
- Notes: This approach demonstrates the underlying algorithm and is useful if built-in functions are restricted.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Convert Date to Binary Solutions","datePublished":"2024-01-16","description":"Convert a date string from decimal format to binary format by converting year, month, and day separately.","keywords":"LeetCode, Convert Date to Binary, Easy, Math, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Built-in Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) because the number of operations is constant and bounded by the maximum value of the year (2100)."}},{"@type":"Question","name":"What is the space complexity of the Manual Binary Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1), excluding the space required for the output string, as we only use a few variables for computation."}}]}).replace(/</g,'\u003c')}
</script>