---
title: "Crawler Log Folder"
summary: "Calculate the minimum steps to return to the main folder from the current folder after performing a series of move operations."
date: "Apr 26, 2025"
tags:
  - Array
  - String
  - Stack
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 71.70% | **Paid:** No
> **Topics:** Array, String, Stack

The Leetcode file system keeps a log each time some user performs a change folder operation.

The operations are described below:

"../" : Move to the parent folder of the current folder. (If you are already in the main folder, remain in the same folder).
"./" : Remain in the same folder.
"x/" : Move to the child folder named x (This folder is guaranteed to always exist).

You are given a list of strings logs where logs[i] is the operation performed by the user at the ith step.

The file system starts in the main folder, then the operations in logs are performed.

Return the minimum number of operations to go back to the main folder after the change folder operations.

- Examples
- Constraints
- [Simulation with Counter](#simulation-with-counter)
- [Stack Simulation](#stack-simulation)

## Examples

```text
Input: logs = ["d1/","d2/","../","d21/","./"]
Output: 2
Explanation: Use this change folder operation "../" 2 times and go back to the main folder.
```

```text
Input: logs = ["d1/","d2/","./","d3/","../","d31/"]
Output: 3
```

```text
Input: logs = ["d1/","../","../","../"]
Output: 0
```

## Constraints

```text
1 <= logs.length <= 10³
2 <= logs[i].length <= 10
logs[i] contains lowercase English letters, digits, '.', and '/'.
logs[i] follows the format described in the problem.
```

---
## Simulation with Counter

**Intuition**
Since we only need to know the depth relative to the main folder, we can simulate the process using a single integer counter representing the current depth.

**Steps**
- Initialize a variable `depth` to 0.
- Iterate through each operation in the `logs` array.
- If the operation is `"../"`, decrement `depth` by 1, but ensure it does not go below 0.
- If the operation is `"./"`, do nothing.
- For any other operation (moving into a child folder), increment `depth` by 1.
- Return the final value of `depth`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def minOperations(self, logs: List[str]) -&gt; int:\n        depth = 0\n        for log in logs:\n            if log == \"../\":\n                if depth &gt; 0:\n                    depth -= 1\n            elif log == \"./\":\n                continue\n            else:\n                depth += 1\n        return depth" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minOperations(String[] logs) {\n        int depth = 0;\n        for (String log : logs) {\n            if (log.equals(\"../\")) {\n                if (depth &gt; 0) {\n                    depth--;\n                }\n            } else if (log.equals(\"./\")) {\n                continue;\n            } else {\n                depth++;\n            }\n        }\n        return depth;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int minOperations(vector&lt;string&gt;& logs) {\n        int depth = 0;\n        for (const string& log : logs) {\n            if (log == \"../\") {\n                if (depth &gt; 0) {\n                    depth--;\n                }\n            } else if (log == \"./\") {\n                continue;\n            } else {\n                depth++;\n            }\n        }\n        return depth;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string[]} logs\n * @return {number}\n */\nvar minOperations = function(logs) {\n    let depth = 0;\n    for (let i = 0; i &lt; logs.length; i++) {\n        const log = logs[i];\n        if (log === \"../\") {\n            if (depth &gt; 0) {\n                depth--;\n            }\n        } else if (log === \"./\") {\n            continue;\n        } else {\n            depth++;\n        }\n    }\n    return depth;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minOperations(logs: string[]): number {\n    let depth = 0;\n    for (let i = 0; i &lt; logs.length; i++) {\n        const log = logs[i];\n        if (log === \"../\") {\n            if (depth &gt; 0) {\n                depth--;\n            }\n        } else if (log === \"./\") {\n            continue;\n        } else {\n            depth++;\n        }\n    }\n    return depth;\n}" },]} />

**Complexity**
- Time: O(n), where n is the number of logs.
- Space: O(1), as we only use a single variable for tracking depth.
- Notes: This is the most space-efficient approach.

---
## Stack Simulation

**Intuition**
We can explicitly simulate the folder structure using a stack. Each time we enter a folder, we push it onto the stack, and each time we move up, we pop from the stack.

**Steps**
- Initialize an empty stack.
- Iterate through each operation in the `logs` array.
- If the operation is `"../"`, check if the stack is not empty and pop the top element.
- If the operation is `"./"`, do nothing.
- For any other operation, push the folder name (or a placeholder) onto the stack.
- The size of the stack represents the distance from the main folder. Return the stack size.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def minOperations(self, logs: List[str]) -&gt; int:\n        stack = []\n        for log in logs:\n            if log == \"../\":\n                if stack:\n                    stack.pop()\n            elif log == \"./\":\n                continue\n            else:\n                stack.append(log)\n        return len(stack)" },
{ name: "Java", lang: "java", code: "import java.util.ArrayDeque;\nimport java.util.Deque;\n\nclass Solution {\n    public int minOperations(String[] logs) {\n        Deque&lt;String&gt; stack = new ArrayDeque&lt;&gt;();\n        for (String log : logs) {\n            if (log.equals(\"../\")) {\n                if (!stack.isEmpty()) {\n                    stack.pop();\n                }\n            } else if (log.equals(\"./\")) {\n                continue;\n            } else {\n                stack.push(log);\n            }\n        }\n        return stack.size();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int minOperations(vector&lt;string&gt;& logs) {\n        vector&lt;string&gt; stack;\n        for (const string& log : logs) {\n            if (log == \"../\") {\n                if (!stack.empty()) {\n                    stack.pop_back();\n                }\n            } else if (log == \"./\") {\n                continue;\n            } else {\n                stack.push_back(log);\n            }\n        }\n        return stack.size();\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string[]} logs\n * @return {number}\n */\nvar minOperations = function(logs) {\n    let stack = [];\n    for (let i = 0; i &lt; logs.length; i++) {\n        const log = logs[i];\n        if (log === \"../\") {\n            if (stack.length &gt; 0) {\n                stack.pop();\n            }\n        } else if (log === \"./\") {\n            continue;\n        } else {\n            stack.push(log);\n        }\n    }\n    return stack.length;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minOperations(logs: string[]): number {\n    let stack: string[] = [];\n    for (let i = 0; i &lt; logs.length; i++) {\n        const log = logs[i];\n        if (log === \"../\") {\n            if (stack.length &gt; 0) {\n                stack.pop();\n            }\n        } else if (log === \"./\") {\n            continue;\n        } else {\n            stack.push(log);\n        }\n    }\n    return stack.length;\n}" },]} />

**Complexity**
- Time: O(n), where n is the number of logs.
- Space: O(n), in the worst case where we only move into child folders.
- Notes: While conceptually clear, this uses more memory than the counter approach.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Crawler Log Folder Solutions","datePublished":"2025-04-26","description":"Calculate the minimum steps to return to the main folder from the current folder after performing a series of move operations.","keywords":"LeetCode, Crawler Log Folder, Easy, Array, String, Stack"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Simulation with Counter approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of logs, as we iterate through the list once."}},{"@type":"Question","name":"What is the space complexity of the Simulation with Counter approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use a single integer variable to track depth."}},{"@type":"Question","name":"What is the time complexity of the Stack Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of logs, as we process each log exactly once."}},{"@type":"Question","name":"What is the space complexity of the Stack Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) in the worst case, as the stack may store all folder names if we only move deeper."}}]}).replace(/</g,'\u003c')}
</script>