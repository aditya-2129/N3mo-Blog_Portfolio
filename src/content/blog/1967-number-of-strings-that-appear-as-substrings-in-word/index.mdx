---
title: "Number of Strings That Appear as Substrings in Word"
summary: "Given an array of strings patterns and a string word, return the number of strings in patterns that are substrings of word."
date: "Aug 13, 2025"
tags:
  - Array
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.50% | **Paid:** No
> **Topics:** Array, String

Given an array of strings patterns and a string word, return the number of strings in patterns that are substrings of word.

A substring is a contiguous sequence of characters within a string.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Built-in Methods](#built-in-methods)

## Examples

**Example 1:**
```text
Input: patterns = ["a","abc","bc","d"], word = "abc"
Output: 3
Explanation:
- "a" appears as a substring of "abc".
- "abc" appears as a substring of "abc".
- "bc" appears as a substring of "abc".
- "d" does not appear as a substring of "abc".
Thus, 3 strings in patterns appear as substrings of word.
```

**Example 2:**
```text
Input: patterns = ["a","b","c"], word = "aaaaabbbbb"
Output: 2
Explanation:
- "a" appears as a substring of "aaaaabbbbb".
- "b" appears as a substring of "aaaaabbbbb".
- "c" does not appear as a substring of "aaaaabbbbb".
Thus, 2 strings in patterns appear as substrings of word.
```

**Example 3:**
```text
Input: patterns = ["a","a","a"], word = "ab"
Output: 3
Explanation: All 3 strings in patterns appear as substrings of word.
```

## Constraints

```text
1 <= patterns.length <= 100
1 <= patterns[i].length <= 100
1 <= word.length <= 100
patterns[i] and word consist of lowercase English letters.
```

---
## Brute Force

**Intuition**
We manually check each pattern against the word by iterating through all possible starting positions in the word and comparing characters one by one.

**Steps**
- Initialize a counter to 0.
- Iterate through each string `p` in the `patterns` array.
- For each `p`, iterate through the `word` from index 0 to `len(word) - len(p)`.
- At each starting index `i` in `word`, check if the substring of `word` starting at `i` with length `len(p)` matches `p`.
- If a match is found, increment the counter and stop checking the current pattern.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numOfStrings(self, patterns: list[str], word: str) -&gt; int:\n        count = 0\n        for p in patterns:\n            found = False\n            n = len(word)\n            m = len(p)\n            for i in range(n - m + 1):\n                if word[i:i+m] == p:\n                    found = True\n                    break\n            if found:\n                count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numOfStrings(String[] patterns, String word) {\n        int count = 0;\n        for (String p : patterns) {\n            if (isSubstring(p, word)) {\n                count++;\n            }\n        }\n        return count;\n    }\n\n    private boolean isSubstring(String p, String word) {\n        int n = word.length();\n        int m = p.length();\n        for (int i = 0; i &lt;= n - m; i++) {\n            boolean match = true;\n            for (int j = 0; j &lt; m; j++) {\n                if (word.charAt(i + j) != p.charAt(j)) {\n                    match = false;\n                    break;\n                }\n            }\n            if (match) return true;\n        }\n        return false;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numOfStrings(vector&lt;string&gt;& patterns, string word) {\n        int count = 0;\n        for (const string& p : patterns) {\n            if (isSubstring(p, word)) {\n                count++;\n            }\n        }\n        return count;\n    }\n\nprivate:\n    bool isSubstring(const string& p, const string& word) {\n        int n = word.size();\n        int m = p.size();\n        for (int i = 0; i &lt;= n - m; ++i) {\n            bool match = true;\n            for (int j = 0; j &lt; m; ++j) {\n                if (word[i + j] != p[j]) {\n                    match = false;\n                    break;\n                }\n            }\n            if (match) return true;\n        }\n        return false;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var numOfStrings = function(patterns, word) {\n    let count = 0;\n    for (const p of patterns) {\n        if (isSubstring(p, word)) {\n            count++;\n        }\n    }\n    return count;\n};\n\nfunction isSubstring(p, word) {\n    const n = word.length;\n    const m = p.length;\n    for (let i = 0; i &lt;= n - m; i++) {\n        let match = true;\n        for (let j = 0; j &lt; m; j++) {\n            if (word[i + j] !== p[j]) {\n                match = false;\n                break;\n            }\n        }\n        if (match) return true;\n    }\n    return false;\n}" },
{ name: "TypeScript", lang: "typescript", code: "function numOfStrings(patterns: string[], word: string): number {\n    let count = 0;\n    for (const p of patterns) {\n        if (isSubstring(p, word)) {\n            count++;\n        }\n    }\n    return count;\n}\n\nfunction isSubstring(p: string, word: string): boolean {\n    const n = word.length;\n    const m = p.length;\n    for (let i = 0; i &lt;= n - m; i++) {\n        let match = true;\n        for (let j = 0; j &lt; m; j++) {\n            if (word[i + j] !== p[j]) {\n                match = false;\n                break;\n            }\n        }\n        if (match) return true;\n    }\n    return false;\n}" },]} />

**Complexity**
- Time: O(N * M * L) where N is the number of patterns, M is the length of the word, and L is the average length of a pattern.
- Space: O(1)
- Notes: This approach manually implements the substring check logic without relying on library functions.

---
## Built-in Methods

**Intuition**
Most programming languages provide optimized built-in methods to check if a string contains another string. We can leverage these for cleaner and more concise code.

**Steps**
- Initialize a counter to 0.
- Iterate through each string `p` in the `patterns` array.
- Use the language's built-in method (e.g., `in` for Python, `contains` for Java, `find` for C++, `includes` for JavaScript/TypeScript) to check if `word` contains `p`.
- If the result is true, increment the counter.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numOfStrings(self, patterns: list[str], word: str) -&gt; int:\n        return sum(1 for p in patterns if p in word)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numOfStrings(String[] patterns, String word) {\n        int count = 0;\n        for (String p : patterns) {\n            if (word.contains(p)) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numOfStrings(vector&lt;string&gt;& patterns, string word) {\n        int count = 0;\n        for (const string& p : patterns) {\n            if (word.find(p) != string::npos) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var numOfStrings = function(patterns, word) {\n    let count = 0;\n    for (const p of patterns) {\n        if (word.includes(p)) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numOfStrings(patterns: string[], word: string): number {\n    let count = 0;\n    for (const p of patterns) {\n        if (word.includes(p)) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(N * M * L) where N is the number of patterns, M is the length of the word, and L is the average length of a pattern. Built-in methods are often highly optimized (e.g., using Boyer-Moore or similar algorithms), but the worst-case complexity remains similar.
- Space: O(1)
- Notes: This is the preferred approach in production due to readability and reliance on standard library optimizations.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Number of Strings That Appear as Substrings in Word Solutions","datePublished":"2025-08-13","description":"Given an array of strings patterns and a string word, return the number of strings in patterns that are substrings of word.","keywords":"LeetCode, Number of Strings That Appear as Substrings in Word, Easy, Array, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * M * L), where N is the number of patterns, M is the length of the word, and L is the average length of a pattern."}},{"@type":"Question","name":"What is the space complexity of the Built-in Methods approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for the counter and loop variables."}},{"@type":"Question","name":"Which method is recommended for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Built-in Methods approach is recommended for its readability and reliance on optimized standard library functions."}}]}).replace(/</g,'\u003c')}
</script>