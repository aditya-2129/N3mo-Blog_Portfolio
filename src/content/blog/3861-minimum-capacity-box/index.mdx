---
title: "Minimum Capacity Box"
summary: "Given an array of box capacities and an item size, return the index of the box with the minimum capacity that can store the item, or -1 if none can."
date: "May 13, 2026"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 72.60% | **Paid:** No
> **Topics:** Array

You are given an integer array `capacity`, where `capacity[i]` represents the capacity of the `i`th box, and an integer `itemSize` representing the size of an item.

The `i`th box can store the item if `capacity[i] >= itemSize`.

Return an integer denoting the index of the box with the **minimum** capacity that can store the item. If multiple such boxes exist, return the smallest index.

If no box can store the item, return `-1`.

- Examples
- Constraints
- [Linear Scan](#linear-scan)
- [Two Pass](#two-pass)

## Examples

### Example 1

**Input:**
```text
capacity = [1,5,3,7], itemSize = 3
```

**Output:**
```text
2
```

**Explanation:**
The box at index 2 has a capacity of 3, which is the minimum capacity that can store the item. Thus, the answer is 2.

### Example 2

**Input:**
```text
capacity = [3,5,4,3], itemSize = 2
```

**Output:**
```text
0
```

**Explanation:**
The minimum capacity that can store the item is 3, and it appears at indices 0 and 3. Thus, the answer is 0.

### Example 3

**Input:**
```text
capacity = [4], itemSize = 5
```

**Output:**
```text
-1
```

**Explanation:**
No box has enough capacity to store the item, so the answer is -1.

## Constraints

```text
- 1 <= capacity.length <= 100
- 1 <= capacity[i] <= 100
- 1 <= itemSize <= 100
```
---

## Linear Scan

**Intuition**

Iterate through the array once, tracking the minimum valid capacity seen so far and its index. A box is valid if its capacity is at least `itemSize`. Because we scan left to right, the first occurrence of any capacity value is naturally the smallest index.

**Steps**

- Initialize `minCap` to infinity and `result` to -1.
- For each index `i`, if `capacity[i] >= itemSize` and `capacity[i] < minCap`, update `minCap` and `result`.
- Return `result`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumBox(self, capacity: List[int], itemSize: int) -&gt; int:\n        result = -1\n        min_cap = float('inf')\n        for i, c in enumerate(capacity):\n            if c &gt;= itemSize and c &lt; min_cap:\n                min_cap = c\n                result = i\n        return result\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimumBox(int[] capacity, int itemSize) {\n        int result = -1;\n        int minCap = Integer.MAX_VALUE;\n        for (int i = 0; i &lt; capacity.length; i++) {\n            if (capacity[i] &gt;= itemSize &amp;&amp; capacity[i] &lt; minCap) {\n                minCap = capacity[i];\n                result = i;\n            }\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimumBox(vector&lt;int&gt;& capacity, int itemSize) {\n        int result = -1;\n        int minCap = INT_MAX;\n        for (int i = 0; i &lt; (int)capacity.size(); i++) {\n            if (capacity[i] &gt;= itemSize &amp;&amp; capacity[i] &lt; minCap) {\n                minCap = capacity[i];\n                result = i;\n            }\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var minimumBox = function(capacity, itemSize) {\n    let result = -1;\n    let minCap = Infinity;\n    for (let i = 0; i &lt; capacity.length; i++) {\n        if (capacity[i] &gt;= itemSize &amp;&amp; capacity[i] &lt; minCap) {\n            minCap = capacity[i];\n            result = i;\n        }\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumBox(capacity: number[], itemSize: number): number {\n    let result = -1;\n    let minCap = Infinity;\n    for (let i = 0; i &lt; capacity.length; i++) {\n        if (capacity[i] &gt;= itemSize &amp;&amp; capacity[i] &lt; minCap) {\n            minCap = capacity[i];\n            result = i;\n        }\n    }\n    return result;\n}" }]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Single pass with constant extra space. The strict `<` comparison ensures ties resolve to the leftmost index automatically.

---

## Two Pass

**Intuition**

Separate the problem into two clear steps: first find the minimum capacity among all valid boxes, then find the first index that has exactly that capacity.

**Steps**

- Pass 1: scan all boxes and track the minimum capacity value that is `>= itemSize`.
- If no valid capacity found, return -1.
- Pass 2: scan again and return the first index whose capacity equals the minimum found.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumBox(self, capacity: List[int], itemSize: int) -&gt; int:\n        min_cap = min((c for c in capacity if c &gt;= itemSize), default=None)\n        if min_cap is None:\n            return -1\n        return capacity.index(min_cap)\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimumBox(int[] capacity, int itemSize) {\n        int minCap = Integer.MAX_VALUE;\n        for (int c : capacity) {\n            if (c &gt;= itemSize) minCap = Math.min(minCap, c);\n        }\n        if (minCap == Integer.MAX_VALUE) return -1;\n        for (int i = 0; i &lt; capacity.length; i++) {\n            if (capacity[i] == minCap) return i;\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimumBox(vector&lt;int&gt;& capacity, int itemSize) {\n        int minCap = INT_MAX;\n        for (int c : capacity) {\n            if (c &gt;= itemSize) minCap = min(minCap, c);\n        }\n        if (minCap == INT_MAX) return -1;\n        for (int i = 0; i &lt; (int)capacity.size(); i++) {\n            if (capacity[i] == minCap) return i;\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var minimumBox = function(capacity, itemSize) {\n    let minCap = Infinity;\n    for (const c of capacity) {\n        if (c &gt;= itemSize) minCap = Math.min(minCap, c);\n    }\n    if (minCap === Infinity) return -1;\n    return capacity.indexOf(minCap);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumBox(capacity: number[], itemSize: number): number {\n    let minCap = Infinity;\n    for (const c of capacity) {\n        if (c &gt;= itemSize) minCap = Math.min(minCap, c);\n    }\n    if (minCap === Infinity) return -1;\n    return capacity.indexOf(minCap);\n}" }]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Two passes over the array, still linear time and constant space. Slightly more readable by separating the two concerns.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Capacity Box Solutions","datePublished":"2026-05-13","description":"Given an array of box capacities and an item size, return the index of the box with the minimum capacity that can store the item, or -1 if none can.","keywords":"LeetCode, Minimum Capacity Box, Easy, Array"}).replace(/</g,'<')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) — a single pass through the array with O(1) extra space."}},{"@type":"Question","name":"What is the time complexity of the Two Pass approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) — two linear passes, still O(1) extra space."}}]}).replace(/</g,'<')}
</script>
