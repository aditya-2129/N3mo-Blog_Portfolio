---
title: "Check if Strings Can be Made Equal With Operations I"
summary: "Determine if two strings can be made equal by swapping characters at indices with the same parity."
date: "Mar 06, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 61.90% | **Paid:** No
> **Topics:** String

You are given two strings s1 and s2 of equal length consisting of lowercase English letters.

In one operation, you can choose two indices i and j (0-indexed) such that i % 2 == j % 2 and swap s1[i] with s1[j].

Return true if you can make the strings s1 and s2 equal, otherwise, return false.

- Examples
- Constraints
- [Frequency Counting](#frequency-counting)
- [Sorting](#sorting)

## Examples

```text
Input: s1 = "abcd", s2 = "cdab"
Output: true
Explanation: One possible sequence of operations is:
- Swap indices 0 and 2, and indices 1 and 3. The string becomes "cdab".
```

```text
Input: s1 = "abcd", s2 = "dacb"
Output: false
Explanation: It is impossible to make s1 equal to s2.
```

## Constraints

```text
1 <= s1.length == s2.length <= 100
s1 and s2 consist of lowercase English letters.
```

---
## Frequency Counting

**Intuition**
Since we can only swap characters at indices with the same parity (even with even, odd with odd), the multiset of characters at even indices in `s1` must match the multiset of characters at even indices in `s2`. The same applies to odd indices.

**Steps**
- Initialize two frequency arrays (or hash maps) for even indices and two for odd indices.
- Iterate through the strings. For each index, increment the corresponding frequency counter based on whether the index is even or odd.
- Compare the frequency arrays for `s1` and `s2` for both even and odd indices.
- Return true if both comparisons match, otherwise false.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def canBeEqual(self, s1: str, s2: str) -&gt; bool:\n        if len(s1) != len(s2):\n            return False\n        n = len(s1)\n        # Even indices\n        count_even_s1 = [0] * 26\n        count_even_s2 = [0] * 26\n        # Odd indices\n        count_odd_s1 = [0] * 26\n        count_odd_s2 = [0] * 26\n\n        for i in range(n):\n            char_code = ord(s1[i]) - ord('a')\n            if i % 2 == 0:\n                count_even_s1[char_code] += 1\n            else:\n                count_odd_s1[char_code] += 1\n\n            char_code = ord(s2[i]) - ord('a')\n            if i % 2 == 0:\n                count_even_s2[char_code] += 1\n            else:\n                count_odd_s2[char_code] += 1\n\n        return count_even_s1 == count_even_s2 and count_odd_s1 == count_odd_s2\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean canBeEqual(String s1, String s2) {\n        int n = s1.length();\n        int[] evenS1 = new int[26];\n        int[] oddS1 = new int[26];\n        int[] evenS2 = new int[26];\n        int[] oddS2 = new int[26];\n\n        for (int i = 0; i &lt; n; i++) {\n            char c1 = s1.charAt(i);\n            char c2 = s2.charAt(i);\n            if (i % 2 == 0) {\n                evenS1[c1 - 'a']++;\n                evenS2[c2 - 'a']++;\n            } else {\n                oddS1[c1 - 'a']++;\n                oddS2[c2 - 'a']++;\n            }\n        }\n\n        for (int i = 0; i &lt; 26; i++) {\n            if (evenS1[i] != evenS2[i] || oddS1[i] != oddS2[i]) {\n                return false;\n            }\n        }\n        return true;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool canBeEqual(string s1, string s2) {\n        int n = s1.size();\n        int evenS1[26] = {0};\n        int oddS1[26] = {0};\n        int evenS2[26] = {0};\n        int oddS2[26] = {0};\n\n        for (int i = 0; i &lt; n; ++i) {\n            if (i % 2 == 0) {\n                evenS1[s1[i] - 'a']++;\n                evenS2[s2[i] - 'a']++;\n            } else {\n                oddS1[s1[i] - 'a']++;\n                oddS2[s2[i] - 'a']++;\n            }\n        }\n\n        for (int i = 0; i &lt; 26; ++i) {\n            if (evenS1[i] != evenS2[i] || oddS1[i] != oddS2[i]) {\n                return false;\n            }\n        }\n        return true;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var canBeEqual = function(s1, s2) {\n    const n = s1.length;\n    const evenS1 = new Array(26).fill(0);\n    const oddS1 = new Array(26).fill(0);\n    const evenS2 = new Array(26).fill(0);\n    const oddS2 = new Array(26).fill(0);\n\n    for (let i = 0; i &lt; n; i++) {\n        const code1 = s1.charCodeAt(i) - 97;\n        const code2 = s2.charCodeAt(i) - 97;\n        if (i % 2 === 0) {\n            evenS1[code1]++;\n            evenS2[code2]++;\n        } else {\n            oddS1[code1]++;\n            oddS2[code2]++;\n        }\n    }\n\n    for (let i = 0; i &lt; 26; i++) {\n        if (evenS1[i] !== evenS2[i] || oddS1[i] !== oddS2[i]) {\n            return false;\n        }\n    }\n    return true;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function canBeEqual(s1: string, s2: string): boolean {\n    const n = s1.length;\n    const evenS1: number[] = new Array(26).fill(0);\n    const oddS1: number[] = new Array(26).fill(0);\n    const evenS2: number[] = new Array(26).fill(0);\n    const oddS2: number[] = new Array(26).fill(0);\n\n    for (let i = 0; i &lt; n; i++) {\n        const code1 = s1.charCodeAt(i) - 97;\n        const code2 = s2.charCodeAt(i) - 97;\n        if (i % 2 === 0) {\n            evenS1[code1]++;\n            evenS2[code2]++;\n        } else {\n            oddS1[code1]++;\n            oddS2[code2]++;\n        }\n    }\n\n    for (let i = 0; i &lt; 26; i++) {\n        if (evenS1[i] !== evenS2[i] || oddS1[i] !== oddS2[i]) {\n            return false;\n        }\n    }\n    return true;\n};\n" },]} />

**Complexity**
- Time: O(n)
- Space: O(1) (since the alphabet size is fixed at 26)
- Notes: This is the most optimal approach as it only requires a single pass through the strings.

---
## Sorting

**Intuition**
If we can rearrange characters at even indices freely, then sorting the characters at even indices of `s1` should result in the same sequence as sorting the characters at even indices of `s2`. The same logic applies to odd indices.

**Steps**
- Extract characters at even indices from `s1` and `s2` into separate lists/arrays.
- Extract characters at odd indices from `s1` and `s2` into separate lists/arrays.
- Sort all four lists/arrays.
- Compare the sorted even lists and the sorted odd lists.
- Return true if both pairs are identical, otherwise false.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def canBeEqual(self, s1: str, s2: str) -&gt; bool:\n        # Extract even and odd indices\n        s1_even = sorted(s1[::2])\n        s2_even = sorted(s2[::2])\n        s1_odd = sorted(s1[1::2])\n        s2_odd = sorted(s2[1::2])\n        \n        return s1_even == s2_even and s1_odd == s2_odd\n" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public boolean canBeEqual(String s1, String s2) {\n        char[] s1Even = new char[(s1.length() + 1) / 2];\n        char[] s1Odd = new char[s1.length() / 2];\n        char[] s2Even = new char[(s2.length() + 1) / 2];\n        char[] s2Odd = new char[s2.length() / 2];\n\n        for (int i = 0; i &lt; s1.length(); i++) {\n            if (i % 2 == 0) {\n                s1Even[i / 2] = s1.charAt(i);\n                s2Even[i / 2] = s2.charAt(i);\n            } else {\n                s1Odd[i / 2] = s1.charAt(i);\n                s2Odd[i / 2] = s2.charAt(i);\n            }\n        }\n\n        Arrays.sort(s1Even);\n        Arrays.sort(s1Odd);\n        Arrays.sort(s2Even);\n        Arrays.sort(s2Odd);\n\n        return Arrays.equals(s1Even, s2Even) && Arrays.equals(s1Odd, s2Odd);\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nclass Solution {\npublic:\n    bool canBeEqual(string s1, string s2) {\n        std::vector&lt;char&gt; s1Even, s1Odd, s2Even, s2Odd;\n        \n        for (int i = 0; i &lt; s1.size(); ++i) {\n            if (i % 2 == 0) {\n                s1Even.push_back(s1[i]);\n                s2Even.push_back(s2[i]);\n            } else {\n                s1Odd.push_back(s1[i]);\n                s2Odd.push_back(s2[i]);\n            }\n        }\n        \n        std::sort(s1Even.begin(), s1Even.end());\n        std::sort(s1Odd.begin(), s1Odd.end());\n        std::sort(s2Even.begin(), s2Even.end());\n        std::sort(s2Odd.begin(), s2Odd.end());\n        \n        return s1Even == s2Even && s1Odd == s2Odd;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var canBeEqual = function(s1, s2) {\n    const getChars = (s, start) =&gt; {\n        let res = [];\n        for (let i = start; i &lt; s.length; i += 2) {\n            res.push(s[i]);\n        }\n        return res.sort().join('');\n    };\n\n    return getChars(s1, 0) === getChars(s2, 0) && \n           getChars(s1, 1) === getChars(s2, 1);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function canBeEqual(s1: string, s2: string): boolean {\n    const getChars = (s: string, start: number): string =&gt; {\n        let res: string[] = [];\n        for (let i = start; i &lt; s.length; i += 2) {\n            res.push(s[i]);\n        }\n        return res.sort().join('');\n    };\n\n    return getChars(s1, 0) === getChars(s2, 0) && \n           getChars(s1, 1) === getChars(s2, 1);\n};\n" },]} />

**Complexity**
- Time: O(n log n) due to sorting.
- Space: O(n) to store the extracted characters.
- Notes: While intuitive, this is slightly less efficient than frequency counting due to the sorting step, but perfectly acceptable given the constraint n &lt;= 100.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check if Strings Can be Made Equal With Operations I Solutions","datePublished":"2025-03-06","description":"Determine if two strings can be made equal by swapping characters at indices with the same parity.","keywords":"LeetCode, Check if Strings Can be Made Equal With Operations I, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Frequency Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the strings, as we iterate through the strings once and then through the fixed-size alphabet array."}},{"@type":"Question","name":"What is the space complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) to store the extracted characters for sorting."}}]}).replace(/</g,'\u003c')}
</script>