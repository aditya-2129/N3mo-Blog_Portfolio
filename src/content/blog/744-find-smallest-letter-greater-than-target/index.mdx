---
title: "Find Smallest Letter Greater Than Target"
summary: "Given a sorted characters array and a target character, find the smallest character in the array that is larger than the target."
date: "Nov 12, 2024"
tags:
  - Array
  - Binary Search
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 59.10% | **Paid:** No
> **Topics:** Array, Binary Search

Given a characters array letters that is sorted in non-decreasing order and a character target, return the smallest character in the array that is larger than target.

Note that the letters wrap around. For example, if target == 'z' and letters == ['a', 'b'], the answer is 'a'.

- Examples
- Constraints
- [Linear Scan](#linear-scan)
- [Binary Search](#binary-search)

## Examples

### Example 1
```text
Input: letters = ["c","f","j"], target = "a"
Output: "c"
```

### Example 2
```text
Input: letters = ["c","f","j"], target = "c"
Output: "f"
```

### Example 3
```text
Input: letters = ["x","x","y","y"], target = "z"
Output: "x"
```

## Constraints

```text
2 <= letters.length <= 10⁴
letters[i] is a lowercase English letter.
letters is sorted in non-decreasing order.
letters contains at least two different characters.
target is a lowercase English letter.
```

---
## Linear Scan

**Intuition**
Since the array is sorted, we can simply iterate through the array and return the first character that is greater than the target. If no such character exists, we return the first character (wrap around).

**Steps**
- Iterate through each character in the letters array
- If the current character is greater than the target, return it
- If we reach the end of the array without finding a greater character, return the first character

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def nextGreatestLetter(self, letters: list[str], target: str) -> str:
        for letter in letters:
            if letter &gt; target:
                return letter
        return letters[0]` },
{ name: "Java", lang: "java", code: `class Solution {
    public char nextGreatestLetter(char[] letters, char target) {
        for (char letter : letters) {
            if (letter &gt; target) {
                return letter;
            }
        }
        return letters[0];
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    char nextGreatestLetter(vector<char>& letters, char target) {
        for (char letter : letters) {
            if (letter &gt; target) {
                return letter;
            }
        }
        return letters[0];
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {character[]} letters
 * @param {character} target
 * @return {character}
 */
var nextGreatestLetter = function(letters, target) {
    for (let letter of letters) {
        if (letter &gt; target) {
            return letter;
        }
    }
    return letters[0];
};` },
{ name: "TypeScript", lang: "typescript", code: `function nextGreatestLetter(letters: string[], target: string): string {
    for (let letter of letters) {
        if (letter &gt; target) {
            return letter;
        }
    }
    return letters[0];
}` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Simple but not optimal for large arrays

---
## Binary Search

**Intuition**
Since the array is sorted, we can use binary search to find the smallest character greater than the target in O(log n) time. We're looking for the leftmost character that is strictly greater than the target.

**Steps**
- Initialize left = 0 and right = n - 1
- While left &lt;= right, calculate mid = left + (right - left) / 2
- If letters[mid] &lt;= target, move left to mid + 1 (we need a character strictly greater than target)
- Otherwise, move right to mid - 1
- After the loop, left will point to the smallest character greater than target
- If left is out of bounds (equal to n), return letters[0] (wrap around)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def nextGreatestLetter(self, letters: list[str], target: str) -> str:
        left, right = 0, len(letters) - 1
        
        while left &lt;= right:
            mid = left + (right - left) // 2
            if letters[mid] &lt;= target:
                left = mid + 1
            else:
                right = mid - 1
        
        return letters[left] if left &lt; len(letters) else letters[0]` },
{ name: "Java", lang: "java", code: `class Solution {
    public char nextGreatestLetter(char[] letters, char target) {
        int left = 0, right = letters.length - 1;
        
        while (left &lt;= right) {
            int mid = left + (right - left) / 2;
            if (letters[mid] &lt;= target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        
        return left &lt; letters.length ? letters[left] : letters[0];
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    char nextGreatestLetter(vector<char>& letters, char target) {
        int left = 0, right = letters.size() - 1;
        
        while (left &lt;= right) {
            int mid = left + (right - left) / 2;
            if (letters[mid] &lt;= target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }
        
        return left &lt; letters.size() ? letters[left] : letters[0];
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {character[]} letters
 * @param {character} target
 * @return {character}
 */
var nextGreatestLetter = function(letters, target) {
    let left = 0, right = letters.length - 1;
    
    while (left &lt;= right) {
        let mid = left + Math.floor((right - left) / 2);
        if (letters[mid] &lt;= target) {
            left = mid + 1;
        } else {
            right = mid - 1;
        }
    }
    
    return left &lt; letters.length ? letters[left] : letters[0];
};` },
{ name: "TypeScript", lang: "typescript", code: `function nextGreatestLetter(letters: string[], target: string): string {
    let left = 0, right = letters.length - 1;
    
    while (left &lt;= right) {
        let mid = left + Math.floor((right - left) / 2);
        if (letters[mid] &lt;= target) {
            left = mid + 1;
        } else {
            right = mid - 1;
        }
    }
    
    return left &lt; letters.length ? letters[left] : letters[0];
}` },]} />

**Complexity**
- Time: O(log n)
- Space: O(1)
- Notes: Optimal solution for large sorted arrays

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find Smallest Letter Greater Than Target Solutions","datePublished":"2024-11-12","description":"Given a sorted characters array and a target character, find the smallest character in the array that is larger than the target.","keywords":"LeetCode, Find Smallest Letter Greater Than Target, Easy, Array, Binary Search"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Linear Scan approach is O(n), where n is the length of the letters array."}},{"@type":"Question","name":"What is the time complexity of the Binary Search approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Binary Search approach is O(log n), where n is the length of the letters array."}},{"@type":"Question","name":"What is the space complexity of both approaches?","acceptedAnswer":{"@type":"Answer","text":"Both the Linear Scan and Binary Search approaches have O(1) space complexity as they only use a constant amount of extra space."}}]}).replace(/</g,'\u003c')}
</script>