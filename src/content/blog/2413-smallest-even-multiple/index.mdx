---
title: "Smallest Even Multiple"
summary: "Find the smallest positive multiple of n that is also even."
date: "Mar 17, 2025"
tags:
  - Math
  - Number Theory
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 88.30% | **Paid:** No
> **Topics:** Math, Number Theory

Given a positive integer n, return the smallest positive multiple of n that is also even.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Mathematical (LCM)](#mathematical-lcm)
- [Direct Formula](#direct-formula)
- [Bit Manipulation](#bit-manipulation)

## Examples

### Example 1
```text
Input: n = 5
Output: 10
Explanation: The smallest positive multiple of 5 that is also even is 10.
```

### Example 2
```text
Input: n = 6
Output: 6
Explanation: The smallest positive multiple of 6 that is also even is 6 itself.
```

## Constraints

```text
1 <= n <= 150
```

---
## Brute Force

**Intuition**
Start from n and keep adding n until we find an even number.

**Steps**
- If n is even, return n
- Otherwise, keep adding n to the result until it becomes even

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def smallestEvenMultiple(self, n: int) -> int:
        result = n
        while result % 2 != 0:
            result += n
        return result
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int smallestEvenMultiple(int n) {
        int result = n;
        while (result % 2 != 0) {
            result += n;
        }
        return result;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int smallestEvenMultiple(int n) {
        int result = n;
        while (result % 2 != 0) {
            result += n;
        }
        return result;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var smallestEvenMultiple = function(n) {
    let result = n;
    while (result % 2 !== 0) {
        result += n;
    }
    return result;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function smallestEvenMultiple(n: number): number {
    let result = n;
    while (result % 2 !== 0) {
        result += n;
    }
    return result;
}
` },]} />

**Complexity**
- Time: O(1) - at most one iteration needed
- Space: O(1)
- Notes: Simple but not the most efficient

---
## Mathematical (LCM)

**Intuition**
The smallest even multiple of n is the LCM of n and 2.

**Steps**
- Calculate LCM(n, 2) = (n * 2) / GCD(n, 2)
- If n is even, GCD(n, 2) = 2, so LCM = n
- If n is odd, GCD(n, 2) = 1, so LCM = 2n

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `import math

class Solution:
    def smallestEvenMultiple(self, n: int) -> int:
        return (n * 2) // math.gcd(n, 2)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int smallestEvenMultiple(int n) {
        return (n * 2) / gcd(n, 2);
    }
    
    private int gcd(int a, int b) {
        while (b != 0) {
            int temp = b;
            b = a % b;
            a = temp;
        }
        return a;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int smallestEvenMultiple(int n) {
        return (n * 2) / __gcd(n, 2);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var smallestEvenMultiple = function(n) {
    const gcd = (a, b) =&gt; {
        while (b !== 0) {
            let temp = b;
            b = a % b;
            a = temp;
        }
        return a;
    };
    return (n * 2) / gcd(n, 2);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function smallestEvenMultiple(n: number): number {
    const gcd = (a: number, b: number): number =&gt; {
        while (b !== 0) {
            let temp = b;
            b = a % b;
            a = temp;
        }
        return a;
    };
    return Math.floor((n * 2) / gcd(n, 2));
}
` },]} />

**Complexity**
- Time: O(log(min(n, 2))) = O(1) for GCD calculation
- Space: O(1)
- Notes: Uses the mathematical relationship between LCM and GCD

---
## Direct Formula

**Intuition**
If n is even, n itself is the answer. If n is odd, we need to multiply by 2.

**Steps**
- Check if n is even using modulo operation
- If even, return n
- If odd, return 2 * n

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def smallestEvenMultiple(self, n: int) -> int:
        return n if n % 2 == 0 else 2 * n
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int smallestEvenMultiple(int n) {
        return n % 2 == 0 ? n : 2 * n;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int smallestEvenMultiple(int n) {
        return n % 2 == 0 ? n : 2 * n;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var smallestEvenMultiple = function(n) {
    return n % 2 === 0 ? n : 2 * n;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function smallestEvenMultiple(n: number): number {
    return n % 2 === 0 ? n : 2 * n;
}
` },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Most efficient and readable approach

---
## Bit Manipulation

**Intuition**
Check the least significant bit to determine if n is even or odd.

**Steps**
- Use bitwise AND with 1 to check if n is odd
- If n & 1 is 0, n is even, return n
- If n & 1 is 1, n is odd, return n &lt;&lt; 1 (n * 2)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def smallestEvenMultiple(self, n: int) -> int:
        return n if (n & 1) == 0 else n &lt;&lt; 1
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int smallestEvenMultiple(int n) {
        return (n & 1) == 0 ? n : n &lt;&lt; 1;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int smallestEvenMultiple(int n) {
        return (n & 1) == 0 ? n : n &lt;&lt; 1;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var smallestEvenMultiple = function(n) {
    return (n & 1) === 0 ? n : n &lt;&lt; 1;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function smallestEvenMultiple(n: number): number {
    return (n & 1) === 0 ? n : n &lt;&lt; 1;
}
` },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Uses bitwise operations which can be slightly faster than modulo

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Smallest Even Multiple Solutions","datePublished":"2025-03-17","description":"Find the smallest positive multiple of n that is also even.","keywords":"LeetCode, Smallest Even Multiple, Easy, Math, Number Theory"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - at most one iteration is needed since adding n to an odd n always results in an even number."}},{"@type":"Question","name":"What is the time complexity of the Direct Formula approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - constant time with a single modulo operation."}},{"@type":"Question","name":"What is the most efficient approach for Smallest Even Multiple?","acceptedAnswer":{"@type":"Answer","text":"The Direct Formula or Bit Manipulation approach, both with O(1) time and space complexity."}},{"@type":"Question","name":"How does the Bit Manipulation approach work?","acceptedAnswer":{"@type":"Answer","text":"It checks the least significant bit using (n & 1). If 0, n is even. If 1, n is odd and we return n &lt;&lt; 1 (n * 2)."}}]}).replace(/</g,'\u003c')}
</script>