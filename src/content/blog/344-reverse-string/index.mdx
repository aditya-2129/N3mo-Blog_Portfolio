---
title: "Reverse String"
summary: "Write a function that reverses a string. The input string is given as an array of characters s. You must do this by modifying the input array in-place with O(1) extra memory."
date: "Jan 20, 2025"
tags:
  - Two Pointers
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 80.80% | **Paid:** No
> **Topics:** Two Pointers, String

Write a function that reverses a string. The input string is given as an array of characters s. You must do this by modifying the input array in-place with O(1) extra memory.

- Examples
- Constraints
- [Approach 1: Two Pointers](#approach-1-two-pointers)
- [Approach 2: Recursion](#approach-2-recursion)
- [Approach 3: Brute Force (New Array)](#approach-3-brute-force-new-array)

## Examples

Example 1:
```text
Input: s = ["h","e","l","l","o"]
Output: ["o","l","l","e","h"]
```

Example 2:
```text
Input: s = ["H","a","n","n","a","h"]
Output: ["h","a","n","n","a","H"]
```

## Constraints

```text
1 <= s.length <= 10⁵
s[i] is a printable ascii character.
```

---
## Approach 1: Two Pointers

**Intuition**
Use two pointers starting from the beginning and end of the array, swapping elements until they meet in the middle.

**Steps**
- Initialize a left pointer at index 0 and a right pointer at the last index.
- While left is less than right, swap the characters at these pointers.
- Increment the left pointer and decrement the right pointer.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def reverseString(self, s: list[str]) -&gt; None:\n        left, right = 0, len(s) - 1\n        while left &lt; right:\n            s[left], s[right] = s[right], s[left]\n            left += 1\n            right -= 1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public void reverseString(char[] s) {\n        int left = 0, right = s.length - 1;\n        while (left &lt; right) {\n            char temp = s[left];\n            s[left] = s[right];\n            s[right] = temp;\n            left++;\n            right--;\n        }\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    void reverseString(vector&lt;char&gt;& s) {\n        int left = 0, right = s.size() - 1;\n        while (left &lt; right) {\n            swap(s[left], s[right]);\n            left++;\n            right--;\n        }\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var reverseString = function(s) {\n    let left = 0, right = s.length - 1;\n    while (left &lt; right) {\n        let temp = s[left];\n        s[left] = s[right];\n        s[right] = temp;\n        left++;\n        right--;\n    }\n};" },
{ name: "TypeScript", lang: "typescript", code: "function reverseString(s: string[]): void {\n    let left = 0, right = s.length - 1;\n    while (left &lt; right) {\n        let temp = s[left];\n        s[left] = s[right];\n        s[right] = temp;\n        left++;\n        right--;\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Most optimal solution for in-place reversal.

---
## Approach 2: Recursion

**Intuition**
Recursively swap the outermost characters and then process the inner substring.

**Steps**
- Define a helper function that takes the array and two indices.
- Base case: if the left index is greater than or equal to the right index, return.
- Swap the characters at the left and right indices.
- Recursively call the helper with left + 1 and right - 1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def reverseString(self, s: list[str]) -&gt; None:\n        def helper(left, right):\n            if left &gt;= right:\n                return\n            s[left], s[right] = s[right], s[left]\n            helper(left + 1, right - 1)\n        helper(0, len(s) - 1)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public void reverseString(char[] s) {\n        helper(s, 0, s.length - 1);\n    }\n    \n    private void helper(char[] s, int left, int right) {\n        if (left &gt;= right) return;\n        char temp = s[left];\n        s[left] = s[right];\n        s[right] = temp;\n        helper(s, left + 1, right - 1);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    void reverseString(vector&lt;char&gt;& s) {\n        helper(s, 0, s.size() - 1);\n    }\n    \nprivate:\n    void helper(vector&lt;char&gt;& s, int left, int right) {\n        if (left &gt;= right) return;\n        swap(s[left], s[right]);\n        helper(s, left + 1, right - 1);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var reverseString = function(s) {\n    const helper = (left, right) =&gt; {\n        if (left &gt;= right) return;\n        let temp = s[left];\n        s[left] = s[right];\n        s[right] = temp;\n        helper(left + 1, right - 1);\n    };\n    helper(0, s.length - 1);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function reverseString(s: string[]): void {\n    const helper = (left: number, right: number): void =&gt; {\n        if (left &gt;= right) return;\n        let temp = s[left];\n        s[left] = s[right];\n        s[right] = temp;\n        helper(left + 1, right - 1);\n    };\n    helper(0, s.length - 1);\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Uses stack space for recursion, which is less efficient than the iterative approach for large inputs.

---
## Approach 3: Brute Force (New Array)

**Intuition**
Create a new array containing the elements in reverse order, then copy them back to the original array.

**Steps**
- Initialize a new array of the same length.
- Iterate through the original array from the end to the beginning.
- Place each element into the new array starting from the beginning.
- Copy the new array back into the original array.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def reverseString(self, s: list[str]) -&gt; None:\n        n = len(s)\n        reversed_s = [0] * n\n        for i in range(n):\n            reversed_s[i] = s[n - 1 - i]\n        for i in range(n):\n            s[i] = reversed_s[i]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public void reverseString(char[] s) {\n        int n = s.length;\n        char[] reversed = new char[n];\n        for (int i = 0; i &lt; n; i++) {\n            reversed[i] = s[n - 1 - i];\n        }\n        for (int i = 0; i &lt; n; i++) {\n            s[i] = reversed[i];\n        }\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    void reverseString(vector&lt;char&gt;& s) {\n        int n = s.size();\n        vector&lt;char&gt; reversed(n);\n        for (int i = 0; i &lt; n; i++) {\n            reversed[i] = s[n - 1 - i];\n        }\n        for (int i = 0; i &lt; n; i++) {\n            s[i] = reversed[i];\n        }\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var reverseString = function(s) {\n    const n = s.length;\n    const reversed = new Array(n);\n    for (let i = 0; i &lt; n; i++) {\n        reversed[i] = s[n - 1 - i];\n    }\n    for (let i = 0; i &lt; n; i++) {\n        s[i] = reversed[i];\n    }\n};" },
{ name: "TypeScript", lang: "typescript", code: "function reverseString(s: string[]): void {\n    const n = s.length;\n    const reversed: string[] = new Array(n);\n    for (let i = 0; i &lt; n; i++) {\n        reversed[i] = s[n - 1 - i];\n    }\n    for (let i = 0; i &lt; n; i++) {\n        s[i] = reversed[i];\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Violates the O(1) extra memory constraint but demonstrates a straightforward logic.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Reverse String Solutions","datePublished":"2025-01-20","description":"Write a function that reverses a string. The input string is given as an array of characters s. You must do this by modifying the input array in-place with O(1) extra memory.","keywords":"LeetCode, Reverse String, Easy, Two Pointers, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string, as we iterate through half of the array."}},{"@type":"Question","name":"What is the space complexity of the Recursion approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) due to the recursion stack depth, which can go as deep as n/2."}},{"@type":"Question","name":"Why is the Brute Force approach not optimal?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach uses O(n) extra space to create a new array, violating the problem constraint of O(1) extra memory."}}]}).replace(/</g,'\u003c')}
</script>