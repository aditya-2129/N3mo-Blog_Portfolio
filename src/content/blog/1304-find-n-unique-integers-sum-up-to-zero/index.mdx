---
title: "Find N Unique Integers Sum up to Zero"
summary: "Given an integer n, return any array of n unique integers that sum up to zero."
date: "Feb 10, 2025"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 78.40% | **Paid:** No
> **Topics:** Array, Math

Given an integer n, return any array containing n unique integers such that they add up to 0.

- Examples
- Constraints
- [Symmetric Pairs](#symmetric-pairs)
- [Arithmetic Progression](#arithmetic-progression)
- [Sum of First N-1](#sum-of-first-n-1)

## Examples

```text
Input: n = 5
Output: [-7,-1,1,3,4]
Explanation: These arrays also are accepted [-5,-1,1,2,3] , [-3,-1,2,-2,4].
```

```text
Input: n = 3
Output: [-1,0,1]
```

```text
Input: n = 1
Output: [0]
```

## Constraints

```text
1 <= n <= 1000
```

---

## Symmetric Pairs

**Intuition**
We can generate pairs of positive and negative integers (e.g., 1 and -1, 2 and -2). These pairs naturally sum to zero. If n is odd, we simply add 0 to the array.

**Steps**
- Initialize an empty list to store the result.
- Iterate from 1 to n // 2.
- In each iteration, add the current integer i and its negative -i to the list.
- If n is odd, append 0 to the list to handle the remaining element.
- Return the list.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumZero(self, n: int) -&gt; list[int]:\n        res = []\n        for i in range(1, n // 2 + 1):\n            res.append(i)\n            res.append(-i)\n        if n % 2 != 0:\n            res.append(0)\n        return res" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] sumZero(int n) {\n        int[] res = new int[n];\n        int left = 0, right = n - 1;\n        int start = 1;\n        while (left &lt; right) {\n            res[left++] = start;\n            res[right--] = -start;\n            start++;\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; sumZero(int n) {\n        vector&lt;int&gt; res;\n        for (int i = 1; i &lt;= n / 2; ++i) {\n            res.push_back(i);\n            res.push_back(-i);\n        }\n        if (n % 2 != 0) {\n            res.push_back(0);\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var sumZero = function(n) {\n    let res = [];\n    for (let i = 1; i &lt;= n / 2; i++) {\n        res.push(i);\n        res.push(-i);\n    }\n    if (n % 2 !== 0) {\n        res.push(0);\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumZero(n: number): number[] {\n    let res: number[] = [];\n    for (let i = 1; i &lt;= n / 2; i++) {\n        res.push(i);\n        res.push(-i);\n    }\n    if (n % 2 !== 0) {\n        res.push(0);\n    }\n    return res;\n}" }]} />

**Complexity**
- Time: O(n)
- Space: O(1) (excluding the output array)
- Notes: This approach is very intuitive and efficient.

---

## Arithmetic Progression

**Intuition**
A sequence of consecutive integers centered around zero will always sum to zero. For example, for n=3, we can use -1, 0, 1. For n=4, we can use -3, -1, 1, 3 (step of 2) or simply -1, 0, 1, 2 offset by a constant. A simpler way is to generate numbers from -(n-1) to (n-1) with a step of 2.

**Steps**
- Calculate the starting value as -(n - 1).
- Loop n times.
- In each iteration, add the current value to the result and increment the value by 2.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumZero(self, n: int) -&gt; list[int]:\n        return [i - (n - 1) // 2 for i in range(n)]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] sumZero(int n) {\n        int[] res = new int[n];\n        int start = -(n - 1) / 2;\n        for (int i = 0; i &lt; n; i++) {\n            res[i] = start + i;\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; sumZero(int n) {\n        vector&lt;int&gt; res(n);\n        int start = -(n - 1) / 2;\n        for (int i = 0; i &lt; n; ++i) {\n            res[i] = start + i;\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var sumZero = function(n) {\n    let res = [];\n    let start = -Math.floor((n - 1) / 2);\n    for (let i = 0; i &lt; n; i++) {\n        res.push(start + i);\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumZero(n: number): number[] {\n    let res: number[] = [];\n    let start = -Math.floor((n - 1) / 2);\n    for (let i = 0; i &lt; n; i++) {\n        res.push(start + i);\n    }\n    return res;\n}" }]} />

**Complexity**
- Time: O(n)
- Space: O(1) (excluding the output array)
- Notes: This approach generates a sorted array, which might be desirable in some contexts.

---

## Sum of First N-1

**Intuition**
We can generate the first n-1 unique integers (e.g., 1, 2, ..., n-1). The sum of these is S. To make the total sum zero, the nth integer must be -S. This guarantees uniqueness because -S will be distinct from the positive integers 1 to n-1.

**Steps**
- Initialize an empty list.
- Iterate from 1 to n-1, adding each number to the list and keeping a running sum.
- Append the negative of the running sum to the list.
- Handle the edge case where n=1 by returning [0].

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumZero(self, n: int) -&gt; list[int]:\n        res = list(range(1, n))\n        res.append(-sum(res))\n        return res" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] sumZero(int n) {\n        int[] res = new int[n];\n        int sum = 0;\n        for (int i = 0; i &lt; n - 1; i++) {\n            res[i] = i + 1;\n            sum += res[i];\n        }\n        res[n - 1] = -sum;\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; sumZero(int n) {\n        vector&lt;int&gt; res(n);\n        int sum = 0;\n        for (int i = 0; i &lt; n - 1; ++i) {\n            res[i] = i + 1;\n            sum += res[i];\n        }\n        res[n - 1] = -sum;\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var sumZero = function(n) {\n    let res = [];\n    let sum = 0;\n    for (let i = 1; i &lt; n; i++) {\n        res.push(i);\n        sum += i;\n    }\n    res.push(-sum);\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumZero(n: number): number[] {\n    let res: number[] = [];\n    let sum = 0;\n    for (let i = 1; i &lt; n; i++) {\n        res.push(i);\n        sum += i;\n    }\n    res.push(-sum);\n    return res;\n}" }]} />

**Complexity**
- Time: O(n)
- Space: O(1) (excluding the output array)
- Notes: This approach is simple but results in a larger range of numbers compared to the symmetric approach.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find N Unique Integers Sum up to Zero Solutions","datePublished":"2025-02-10","description":"Given an integer n, return any array of n unique integers that sum up to zero.","keywords":"LeetCode, Find N Unique Integers Sum up to Zero, Easy, Array, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Symmetric Pairs approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) as we iterate n/2 times."}},{"@type":"Question","name":"What is the space complexity of the Arithmetic Progression approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) if we exclude the output array, as we only use a few variables for calculation."}},{"@type":"Question","name":"How does the Sum of First N-1 approach ensure uniqueness?","acceptedAnswer":{"@type":"Answer","text":"It generates positive integers from 1 to n-1. The last number is the negative sum of these, which is guaranteed to be negative and thus distinct from the previous positive numbers."}}]}).replace(/</g,'\u003c')}
</script>