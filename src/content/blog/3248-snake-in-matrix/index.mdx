---
title: "Snake in Matrix"
summary: "Simulate a snake moving in an n x n matrix based on a list of directional commands and return its final position."
date: "Mar 08, 2025"
tags:
  - Array
  - String
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.30% | **Paid:** No
> **Topics:** Array, String, Simulation

You are given a positive integer n, indicating that we have a square matrix of size n x n. You are also given a 0-indexed string array commands of length m, where commands[i] describes the ith command to perform.

The snake starts at cell (0, 0) of the matrix. Each command is one of the following strings:

"UP": Move the snake one cell up.
"DOWN": Move the snake one cell down.
"LEFT": Move the snake one cell left.
"RIGHT": Move the snake one cell right.

The snake will not move outside the boundaries of the matrix. Return the final position of the snake after performing all commands.

- Examples
- Constraints
- [Direct Simulation](#direct-simulation)
- [Direction Vector Map](#direction-vector-map)

## Examples

Example 1:
```text
Input: n = 2, commands = ["RIGHT","DOWN"]
Output: [1,1]
Explanation:
(0,0) -> (0,1) -> (1,1)
```

Example 2:
```text
Input: n = 3, commands = ["DOWN","RIGHT","UP"]
Output: [0,1]
Explanation:
(0,0) -> (1,0) -> (1,1) -> (0,1)
```

## Constraints

```text
2 <= n <= 10
1 <= commands.length <= 100
commands[i] is one of "UP", "DOWN", "LEFT", "RIGHT".
The snake will not move outside the boundaries of the matrix.
```

---
## Direct Simulation

**Intuition**
We can iterate through the list of commands and update the snake's row and column coordinates based on the current command string.

**Steps**
- Initialize row `r` and column `c` to 0.
- Iterate through each command in the `commands` array.
- If the command is "UP", decrement `r`. If "DOWN", increment `r`.
- If the command is "LEFT", decrement `c`. If "RIGHT", increment `c`.
- Return the final coordinates `[r, c]`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def finalPositionOfSnake(self, n: int, commands: list[str]) -&gt; list[int]:\n        r, c = 0, 0\n        for cmd in commands:\n            if cmd == \"UP\":\n                r -= 1\n            elif cmd == \"DOWN\":\n                r += 1\n            elif cmd == \"LEFT\":\n                c -= 1\n            elif cmd == \"RIGHT\":\n                c += 1\n        return [r, c]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] finalPositionOfSnake(int n, List&lt;String&gt; commands) {\n        int r = 0, c = 0;\n        for (String cmd : commands) {\n            switch (cmd) {\n                case \"UP\":\n                    r--;\n                    break;\n                case \"DOWN\":\n                    r++;\n                    break;\n                case \"LEFT\":\n                    c--;\n                    break;\n                case \"RIGHT\":\n                    c++;\n                    break;\n            }\n        }\n        return new int[] { r, c };\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; finalPositionOfSnake(int n, vector&lt;string&gt;& commands) {\n        int r = 0, c = 0;\n        for (const string& cmd : commands) {\n            if (cmd == \"UP\") r--;\n            else if (cmd == \"DOWN\") r++;\n            else if (cmd == \"LEFT\") c--;\n            else if (cmd == \"RIGHT\") c++;\n        }\n        return {r, c};\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var finalPositionOfSnake = function(n, commands) {\n    let r = 0, c = 0;\n    for (const cmd of commands) {\n        if (cmd === \"UP\") r--;\n        else if (cmd === \"DOWN\") r++;\n        else if (cmd === \"LEFT\") c--;\n        else if (cmd === \"RIGHT\") c++;\n    }\n    return [r, c];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function finalPositionOfSnake(n: number, commands: string[]): number[] {\n    let r = 0, c = 0;\n    for (const cmd of commands) {\n        if (cmd === \"UP\") r--;\n        else if (cmd === \"DOWN\") r++;\n        else if (cmd === \"LEFT\") c--;\n        else if (cmd === \"RIGHT\") c++;\n    }\n    return [r, c];\n}" },]} />

**Complexity**
- Time: O(m), where m is the number of commands.
- Space: O(1), only using a few variables for coordinates.
- Notes: Simple and readable, but requires multiple conditional checks per command.

---
## Direction Vector Map

**Intuition**
Instead of using multiple conditional statements, we can map each command string to a coordinate change vector (delta row, delta column). This makes the code cleaner and easier to extend.

**Steps**
- Create a map/dictionary where keys are command strings ("UP", "DOWN", etc.) and values are pairs representing row and column changes (e.g., "UP" -> [-1, 0]).
- Initialize `r` and `c` to 0.
- Iterate through commands. For each command, look up the vector and add the values to `r` and `c`.
- Return `[r, c]`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def finalPositionOfSnake(self, n: int, commands: list[str]) -&gt; list[int]:\n        moves = {\n            \"UP\": (-1, 0),\n            \"DOWN\": (1, 0),\n            \"LEFT\": (0, -1),\n            \"RIGHT\": (0, 1)\n        }\n        r, c = 0, 0\n        for cmd in commands:\n            dr, dc = moves[cmd]\n            r += dr\n            c += dc\n        return [r, c]" },
{ name: "Java", lang: "java", code: "import java.util.Map;\nimport java.util.HashMap;\n\nclass Solution {\n    public int[] finalPositionOfSnake(int n, List&lt;String&gt; commands) {\n        Map&lt;String, int[]&gt; moves = new HashMap&lt;&gt;();\n        moves.put(\"UP\", new int[]{-1, 0});\n        moves.put(\"DOWN\", new int[]{1, 0});\n        moves.put(\"LEFT\", new int[]{0, -1});\n        moves.put(\"RIGHT\", new int[]{0, 1});\n        \n        int r = 0, c = 0;\n        for (String cmd : commands) {\n            int[] delta = moves.get(cmd);\n            r += delta[0];\n            c += delta[1];\n        }\n        return new int[] { r, c };\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;unordered_map&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; finalPositionOfSnake(int n, vector&lt;string&gt;& commands) {\n        unordered_map&lt;string, vector&lt;int&gt;&gt; moves = {\n            {\"UP\", {-1, 0}},\n            {\"DOWN\", {1, 0}},\n            {\"LEFT\", {0, -1}},\n            {\"RIGHT\", {0, 1}}\n        };\n        int r = 0, c = 0;\n        for (const string& cmd : commands) {\n            r += moves[cmd][0];\n            c += moves[cmd][1];\n        }\n        return {r, c};\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var finalPositionOfSnake = function(n, commands) {\n    const moves = {\n        \"UP\": [-1, 0],\n        \"DOWN\": [1, 0],\n        \"LEFT\": [0, -1],\n        \"RIGHT\": [0, 1]\n    };\n    let r = 0, c = 0;\n    for (const cmd of commands) {\n        r += moves[cmd][0];\n        c += moves[cmd][1];\n    }\n    return [r, c];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function finalPositionOfSnake(n: number, commands: string[]): number[] {\n    const moves: Record&lt;string, number[]&gt; = {\n        \"UP\": [-1, 0],\n        \"DOWN\": [1, 0],\n        \"LEFT\": [0, -1],\n        \"RIGHT\": [0, 1]\n    };\n    let r = 0, c = 0;\n    for (const cmd of commands) {\n        r += moves[cmd][0];\n        c += moves[cmd][1];\n    }\n    return [r, c];\n}" },]} />

**Complexity**
- Time: O(m), where m is the number of commands.
- Space: O(1), the map size is constant (4 entries).
- Notes: More elegant than if-else chains, reduces boilerplate code.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Snake in Matrix Solutions","datePublished":"2025-03-08","description":"Simulate a snake moving in an n x n matrix based on a list of directional commands and return its final position.","keywords":"LeetCode, Snake in Matrix, Easy, Array, String, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Direct Simulation?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(m), where m is the number of commands, as we process each command exactly once."}},{"@type":"Question","name":"What is the space complexity of Direction Vector Map?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because the map stores a constant number of entries (4 directions) regardless of input size."}}]}).replace(/</g,'\u003c')}
</script>