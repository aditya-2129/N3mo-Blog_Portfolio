---
title: "Check If N and Its Double Exist"
summary: "Given an array of integers, return true if there exist indices i and j such that i != j and arr[i] == 2 * arr[j]."
date: "Nov 02, 2024"
tags:
  - Array
  - Hash Table
  - Two Pointers
  - Binary Search
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 41.80% | **Paid:** No
> **Topics:** Array, Hash Table, Two Pointers, Binary Search, Sorting

Given an array arr of integers, check if there exists two indices i and j such that :

i != j
and
arr[i] == 2 * arr[j].

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Hash Set](#hash-set)
- [Sorting + Binary Search](#sorting--binary-search)

## Examples

### Example 1

**Input:**
```text
arr = [10,2,5,3]
```

**Output:**
```text
true
```

**Explanation:**
For i = 0 and j = 2, arr[i] == 10 == 2 * 5 == 2 * arr[j]

### Example 2

**Input:**
```text
arr = [3,1,7,11]
```

**Output:**
```text
false
```

**Explanation:**
There is no i and j that satisfy the conditions.

## Constraints

```text
2 <= arr.length <= 500
-10³ <= arr[i] <= 10³
```

---

## Brute Force

**Intuition**
Check every possible pair of indices (i, j) in the array to see if the condition arr[i] == 2 * arr[j] is met, ensuring i != j.

**Steps**
- Iterate through the array with index i.
- For each i, iterate through the array with index j.
- If i != j and arr[i] == 2 * arr[j], return true.
- If loops finish without finding a match, return false.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkIfExist(self, arr):\n        n = len(arr)\n        for i in range(n):\n            for j in range(n):\n                if i != j and arr[i] == 2 * arr[j]:\n                    return True\n        return False" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkIfExist(int[] arr) {\n        int n = arr.length;\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (i != j && arr[i] == 2 * arr[j]) {\n                    return true;\n                }\n            }\n        }\n        return false;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkIfExist(vector&lt;int&gt;& arr) {\n        int n = arr.size();\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (i != j && arr[i] == 2 * arr[j]) {\n                    return true;\n                }\n            }\n        }\n        return false;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var checkIfExist = function(arr) {\n    const n = arr.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (i !== j && arr[i] === 2 * arr[j]) {\n                return true;\n            }\n        }\n    }\n    return false;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkIfExist(arr: number[]): boolean {\n    const n = arr.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (i !== j && arr[i] === 2 * arr[j]) {\n                return true;\n            }\n        }\n    }\n    return false;\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1)
- Notes: Simple but inefficient for large arrays.

## Hash Set

**Intuition**
Use a hash set to store numbers we have seen so far. For each number x, check if 2*x or x/2 (if x is even) already exists in the set.

**Steps**
- Initialize an empty hash set.
- Iterate through each number x in the array.
- Check if 2*x is in the set, or if x is even and x/2 is in the set.
- If yes, return true.
- Add x to the set.
- If the loop finishes, return false.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkIfExist(self, arr):\n        seen = set()\n        for n in arr:\n            if 2 * n in seen or (n % 2 == 0 and n // 2 in seen):\n                return True\n            seen.add(n)\n        return False" },
{ name: "Java", lang: "java", code: "import java.util.HashSet;\nimport java.util.Set;\n\nclass Solution {\n    public boolean checkIfExist(int[] arr) {\n        Set&lt;Integer&gt; seen = new HashSet&lt;&gt;();\n        for (int n : arr) {\n            if (seen.contains(2 * n) || (n % 2 == 0 && seen.contains(n / 2))) {\n                return true;\n            }\n            seen.add(n);\n        }\n        return false;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    bool checkIfExist(vector&lt;int&gt;& arr) {\n        unordered_set&lt;int&gt; seen;\n        for (int n : arr) {\n            if (seen.count(2 * n) || (n % 2 == 0 && seen.count(n / 2))) {\n                return true;\n            }\n            seen.insert(n);\n        }\n        return false;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var checkIfExist = function(arr) {\n    const seen = new Set();\n    for (const n of arr) {\n        if (seen.has(2 * n) || (n % 2 === 0 && seen.has(n / 2))) {\n            return true;\n        }\n        seen.add(n);\n    }\n    return false;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkIfExist(arr: number[]): boolean {\n    const seen = new Set&lt;number&gt;();\n    for (const n of arr) {\n        if (seen.has(2 * n) || (n % 2 === 0 && seen.has(n / 2))) {\n            return true;\n        }\n        seen.add(n);\n    }\n    return false;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Most efficient time complexity, uses extra space for the set.

## Sorting + Binary Search

**Intuition**
Sort the array first. For each element arr[i], perform a binary search for 2 * arr[i] in the rest of the array (i+1 to end).

**Steps**
- Sort the array in ascending order.
- Iterate through the array with index i.
- Calculate target = 2 * arr[i].
- Perform binary search for target in the subarray arr[i+1...n-1].
- If found, return true.
- If loop finishes, return false.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkIfExist(self, arr):\n        arr.sort()\n        n = len(arr)\n        for i in range(n):\n            target = 2 * arr[i]\n            left, right = i + 1, n - 1\n            while left &lt;= right:\n                mid = (left + right) // 2\n                if arr[mid] == target:\n                    return True\n                elif arr[mid] &lt; target:\n                    left = mid + 1\n                else:\n                    right = mid - 1\n        return False" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public boolean checkIfExist(int[] arr) {\n        Arrays.sort(arr);\n        int n = arr.length;\n        for (int i = 0; i &lt; n; i++) {\n            int target = 2 * arr[i];\n            int left = i + 1, right = n - 1;\n            while (left &lt;= right) {\n                int mid = left + (right - left) / 2;\n                if (arr[mid] == target) {\n                    return true;\n                } else if (arr[mid] &lt; target) {\n                    left = mid + 1;\n                } else {\n                    right = mid - 1;\n                }\n            }\n        }\n        return false;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    bool checkIfExist(vector&lt;int&gt;& arr) {\n        sort(arr.begin(), arr.end());\n        int n = arr.size();\n        for (int i = 0; i &lt; n; i++) {\n            int target = 2 * arr[i];\n            int left = i + 1, right = n - 1;\n            while (left &lt;= right) {\n                int mid = left + (right - left) / 2;\n                if (arr[mid] == target) {\n                    return true;\n                } else if (arr[mid] &lt; target) {\n                    left = mid + 1;\n                } else {\n                    right = mid - 1;\n                }\n            }\n        }\n        return false;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var checkIfExist = function(arr) {\n    arr.sort((a, b) =&gt; a - b);\n    const n = arr.length;\n    for (let i = 0; i &lt; n; i++) {\n        const target = 2 * arr[i];\n        let left = i + 1, right = n - 1;\n        while (left &lt;= right) {\n            const mid = Math.floor((left + right) / 2);\n            if (arr[mid] === target) {\n                return true;\n            } else if (arr[mid] &lt; target) {\n                left = mid + 1;\n            } else {\n                right = mid - 1;\n            }\n        }\n    }\n    return false;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkIfExist(arr: number[]): boolean {\n    arr.sort((a, b) =&gt; a - b);\n    const n = arr.length;\n    for (let i = 0; i &lt; n; i++) {\n        const target = 2 * arr[i];\n        let left = i + 1, right = n - 1;\n        while (left &lt;= right) {\n            const mid = Math.floor((left + right) / 2);\n            if (arr[mid] === target) {\n                return true;\n            } else if (arr[mid] &lt; target) {\n                left = mid + 1;\n            } else {\n                right = mid - 1;\n            }\n        }\n    }\n    return false;\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(1) or O(n) depending on sorting implementation
- Notes: Good balance if space is constrained, though slower than Hash Set.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check If N and Its Double Exist Solutions","datePublished":"2024-11-02","description":"Given an array of integers, return true if there exist indices i and j such that i != j and arr[i] == 2 * arr[j].","keywords":"LeetCode, Check If N and Its Double Exist, Easy, Array, Hash Table, Two Pointers, Binary Search, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n²), where n is the length of the array."}},{"@type":"Question","name":"What is the time complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Hash Set approach is O(n), as we iterate through the array once and set operations are O(1) on average."}},{"@type":"Question","name":"What is the space complexity of the Sorting + Binary Search approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) if we ignore the space used by the sorting algorithm, or O(n) if the sorting algorithm requires additional space."}}]}).replace(/</g,'\u003c')}
</script>