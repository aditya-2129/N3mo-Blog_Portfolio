---
title: "Filter Elements from Array"
summary: "Given an array and a function, return a new array containing only the elements for which the function returns true."
date: "Apr 02, 2026"
tags:
  - Array
  - Functional Programming
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 85.50% | **Paid:** No
> **Topics:** N/A

Given an integer array arr and a filtering function fn, return a filtered array filteredArr.

The fn function takes an element and an index and returns a boolean. The filtered array should only contain elements where fn returned true.

- Examples
- Constraints
- [Iterative Approach](#iterative-approach)
- [Built-in Methods](#built-in-methods)

## Examples

### Example 1
```text
Input: arr = [0,10,20,30], fn = function greaterThan10(n) { return n > 10; }
Output: [20,30]
Explanation:
const newArray = filter(arr, greaterThan10);
The function filters out values that are less than or equal to 10.
```

### Example 2
```text
Input: arr = [1,2,3], fn = function firstIndex(n, i) { return i === 0; }
Output: [1]
Explanation:
fn takes the index of the element as an argument.
Since the index of 1 is 0, it passes the check.
```

### Example 3
```text
Input: arr = [-2,-1,0,1,2], fn = function plusOne(n) { return n + 1; }
Output: [-2,0,1,2]
Explanation:
Falsy values like 0 are filtered out.
```

## Constraints

```text
0 <= arr.length <= 1000
-10⁹ <= arr[i] <= 10⁹
fn returns a boolean
```

---

## Iterative Approach

**Intuition**
The most straightforward way to solve this problem is to iterate through the input array, evaluate the filtering function for each element, and collect the elements that satisfy the condition into a new result array.

**Steps**
- Initialize an empty array (or list) to store the filtered elements.
- Iterate through the input array using a standard loop, keeping track of the index.
- For each element, call the provided function `fn` with the element and its index.
- If `fn` returns a truthy value, append the current element to the result array.
- Return the result array after the loop completes.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List, Callable\r\n\r\nclass Solution:\r\n    def filter(self, arr: List[int], fn: Callable[[int, int], bool]) -&gt; List[int]:\r\n        filtered_arr = []\r\n        for i in range(len(arr)):\r\n            if fn(arr[i], i):\r\n                filtered_arr.append(arr[i])\r\n        return filtered_arr" },
{ name: "Java", lang: "java", code: "import java.util.ArrayList;\r\nimport java.util.List;\r\nimport java.util.function.IntPredicate;\r\n\r\nclass Solution {\r\n    public int[] filter(int[] arr, IntPredicate fn) {\r\n        List&lt;Integer&gt; res = new ArrayList&lt;&gt;();\r\n        for (int i = 0; i &lt; arr.length; i++) {\r\n            if (fn.test(arr[i])) {\r\n                res.add(arr[i]);\r\n            }\r\n        }\r\n        int[] ans = new int[res.size()];\r\n        for (int i = 0; i &lt; res.size(); i++) {\r\n            ans[i] = res.get(i);\r\n        }\r\n        return ans;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;functional&gt;\r\n\r\nusing namespace std;\r\n\r\nclass Solution {\r\npublic:\r\n    vector&lt;int&gt; filter(vector&lt;int&gt;& arr, function&lt;bool(int, int)&gt; fn) {\r\n        vector&lt;int&gt; res;\r\n        for (int i = 0; i &lt; arr.size(); i++) {\r\n            if (fn(arr[i], i)) {\r\n                res.push_back(arr[i]);\r\n            }\r\n        }\r\n        return res;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "var filter = function(arr, fn) {\r\n    var filteredArr = [];\r\n    for (var i = 0; i &lt; arr.length; i++) {\r\n        if (fn(arr[i], i)) {\r\n            filteredArr.push(arr[i]);\r\n        }\r\n    }\r\n    return filteredArr;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "type Fn = (n: number, i: number) =&gt; any;\r\n\r\nfunction filter(arr: number[], fn: Fn): number[] {\r\n    const filteredArr: number[] = [];\r\n    for (let i = 0; i &lt; arr.length; i++) {\r\n        if (fn(arr[i], i)) {\r\n            filteredArr.push(arr[i]);\r\n        }\r\n    }\r\n    return filteredArr;\r\n}" },]} />

**Complexity**
- Time: O(n) - We iterate through the array once.
- Space: O(n) - In the worst case, we store all elements in the new array.
- Notes: This is the most compatible approach across different programming paradigms.

---

## Built-in Methods

**Intuition**
Most modern programming languages provide built-in methods for functional array operations like `filter`. Using these methods leads to more concise and declarative code.

**Steps**
- Call the native `filter` method (or equivalent stream operation) on the input array.
- Pass the provided function `fn` as the predicate.
- Return the resulting array directly.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List, Callable\r\n\r\nclass Solution:\r\n    def filter(self, arr: List[int], fn: Callable[[int, int], bool]) -&gt; List[int]:\r\n        return [x for i, x in enumerate(arr) if fn(x, i)]" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\r\nimport java.util.function.IntPredicate;\r\n\r\nclass Solution {\r\n    public int[] filter(int[] arr, IntPredicate fn) {\r\n        return IntStream.of(arr).filter(fn).toArray();\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;algorithm&gt;\r\n#include &lt;iterator&gt;\r\n#include &lt;functional&gt;\r\n\r\nusing namespace std;\r\n\r\nclass Solution {\r\npublic:\r\n    vector&lt;int&gt; filter(vector&lt;int&gt;& arr, function&lt;bool(int, int)&gt; fn) {\r\n        vector&lt;int&gt; res;\r\n        copy_if(arr.begin(), arr.end(), back_inserter(res), [&fn, &arr](int val) {\r\n            int idx = &val - &arr[0];\r\n            return fn(val, idx);\r\n        });\r\n        return res;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "var filter = function(arr, fn) {\r\n    return arr.filter(fn);\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "type Fn = (n: number, i: number) =&gt; any;\r\n\r\nfunction filter(arr: number[], fn: Fn): number[] {\r\n    return arr.filter(fn);\r\n}" },]} />

**Complexity**
- Time: O(n) - The built-in methods iterate through the array internally.
- Space: O(n) - A new array is created to store the results.
- Notes: While concise, built-in methods may have slight overhead compared to a manual loop, but they improve readability.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Filter Elements from Array Solutions","datePublished":"2026-04-02","description":"Given an array and a function, return a new array containing only the elements for which the function returns true.","keywords":"LeetCode, Filter Elements from Array, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of filtering an array?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of elements in the array, as each element must be checked once."}},{"@type":"Question","name":"What is the space complexity of the iterative approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) in the worst case to store the filtered result if all elements pass the condition."}}]}).replace(/</g,'\u003c')}
</script>