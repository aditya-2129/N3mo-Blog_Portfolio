---
title: "Array Wrapper"
summary: "Create a class that wraps an array and provides string representation and addition operations."
date: "Dec 02, 2025"
tags:
  - Class Design
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 89.10% | **Paid:** No
> **Topics:** N/A

Create a class ArrayWrapper that wraps an array of integers and provides the following methods:

- ArrayWrapper(int[] nums): Initializes the object with the array nums.
- String toString(): Returns a string representation of the wrapped array in the format "[1,2,3]".
- int add(ArrayWrapper other): Returns the sum of all elements in the current array and the other array.

- Examples
- Constraints
- [Direct Implementation](#direct-implementation)
- [Using Built-in Methods](#using-built-in-methods)

## Examples

### Example 1

**Input:**
```text
nums = [[1,2],[3,4]], operation = "Add"
```

**Output:**
```text
10
```

**Explanation:**
const obj1 = new ArrayWrapper([1,2]);
const obj2 = new ArrayWrapper([3,4]);
obj1 + obj2; // 10

### Example 2

**Input:**
```text
nums = [[23,98,42,70]], operation = "String"
```

**Output:**
```text
"[23,98,42,70]"
```

**Explanation:**
const obj = new ArrayWrapper([23,98,42,70]);
String(obj); // "[23,98,42,70]"

### Example 3

**Input:**
```text
nums = [[],[]], operation = "Add"
```

**Output:**
```text
0
```

**Explanation:**
const obj1 = new ArrayWrapper([]);
const obj2 = new ArrayWrapper([]);
obj1 + obj2; // 0

## Constraints

```
0 <= nums.length <= 1000
0 <= nums[i] <= 1000
```

---
## Direct Implementation

**Intuition**
Store the array in the class and implement each method by directly iterating through the elements.

**Steps**
- Store the array in the constructor
- For toString, iterate through elements and build the string representation
- For add, iterate through both arrays and sum all elements

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class ArrayWrapper:
    def __init__(self, nums):
        self.nums = nums
    
    def add(self, other):
        total = 0
        for num in self.nums:
            total += num
        for num in other.nums:
            total += num
        return total
    
    def __str__(self):
        result = "["
        for i, num in enumerate(self.nums):
            if i &gt; 0:
                result += ","
            result += str(num)
        result += "]"
        return result` },
{ name: "Java", lang: "java", code: `class ArrayWrapper {
    private int[] nums;
    
    public ArrayWrapper(int[] nums) {
        this.nums = nums;
    }
    
    public int add(ArrayWrapper other) {
        int total = 0;
        for (int num : this.nums) {
            total += num;
        }
        for (int num : other.nums) {
            total += num;
        }
        return total;
    }
    
    @Override
    public String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append("[");
        for (int i = 0; i &lt; nums.length; i++) {
            if (i &gt; 0) {
                sb.append(",");
            }
            sb.append(nums[i]);
        }
        sb.append("]");
        return sb.toString();
    }
}` },
{ name: "C++", lang: "cpp", code: `class ArrayWrapper {
private:
    vector<int> nums;
public:
    ArrayWrapper(vector<int>& nums) {
        this->nums = nums;
    }
    
    int add(ArrayWrapper& other) {
        int total = 0;
        for (int num : nums) {
            total += num;
        }
        for (int num : other.nums) {
            total += num;
        }
        return total;
    }
    
    string toString() {
        string result = "[";
        for (int i = 0; i &lt; nums.size(); i++) {
            if (i &gt; 0) {
                result += ",";
            }
            result += to_string(nums[i]);
        }
        result += "]";
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class ArrayWrapper {
    constructor(nums) {
        this.nums = nums;
    }
    
    add(other) {
        let total = 0;
        for (const num of this.nums) {
            total += num;
        }
        for (const num of other.nums) {
            total += num;
        }
        return total;
    }
    
    toString() {
        let result = "[";
        for (let i = 0; i &lt; this.nums.length; i++) {
            if (i &gt; 0) {
                result += ",";
            }
            result += this.nums[i];
        }
        result += "]";
        return result;
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class ArrayWrapper {
    private nums: number[];
    
    constructor(nums: number[]) {
        this.nums = nums;
    }
    
    add(other: ArrayWrapper): number {
        let total = 0;
        for (const num of this.nums) {
            total += num;
        }
        for (const num of other.nums) {
            total += num;
        }
        return total;
    }
    
    toString(): string {
        let result = "[";
        for (let i = 0; i &lt; this.nums.length; i++) {
            if (i &gt; 0) {
                result += ",";
            }
            result += this.nums[i];
        }
        result += "]";
        return result;
    }
}` },]} />

**Complexity**
- Time: O(n + m) for add, O(n) for toString where n and m are the lengths of the arrays
- Space: O(n) for toString (to store the result string), O(1) for add
- Notes: Simple and straightforward implementation

---
## Using Built-in Methods

**Intuition**
Leverage language-specific built-in methods for array operations to simplify the code.

**Steps**
- Store the array in the constructor
- Use built-in join/toString methods for string representation
- Use built-in sum/reduce methods for addition

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class ArrayWrapper:
    def __init__(self, nums):
        self.nums = nums
    
    def add(self, other):
        return sum(self.nums) + sum(other.nums)
    
    def __str__(self):
        return "[" + ",".join(str(num) for num in self.nums) + "]"` },
{ name: "Java", lang: "java", code: `import java.util.Arrays;

class ArrayWrapper {
    private int[] nums;
    
    public ArrayWrapper(int[] nums) {
        this.nums = nums;
    }
    
    public int add(ArrayWrapper other) {
        return Arrays.stream(this.nums).sum() + Arrays.stream(other.nums).sum();
    }
    
    @Override
    public String toString() {
        return Arrays.toString(nums);
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <numeric>
#include <sstream>

class ArrayWrapper {
private:
    vector<int> nums;
public:
    ArrayWrapper(vector<int>& nums) {
        this->nums = nums;
    }
    
    int add(ArrayWrapper& other) {
        return accumulate(nums.begin(), nums.end(), 0) + 
               accumulate(other.nums.begin(), other.nums.end(), 0);
    }
    
    string toString() {
        if (nums.empty()) return "[]";
        stringstream ss;
        ss &lt;&lt; "[";
        for (size_t i = 0; i &lt; nums.size(); i++) {
            if (i &gt; 0) ss &lt;&lt; ",";
            ss &lt;&lt; nums[i];
        }
        ss &lt;&lt; "]";
        return ss.str();
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class ArrayWrapper {
    constructor(nums) {
        this.nums = nums;
    }
    
    add(other) {
        return this.nums.reduce((a, b) =&gt; a + b, 0) + 
               other.nums.reduce((a, b) =&gt; a + b, 0);
    }
    
    toString() {
        return "[" + this.nums.join(",") + "]";
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class ArrayWrapper {
    private nums: number[];
    
    constructor(nums: number[]) {
        this.nums = nums;
    }
    
    add(other: ArrayWrapper): number {
        return this.nums.reduce((a, b) =&gt; a + b, 0) + 
               other.nums.reduce((a, b) =&gt; a + b, 0);
    }
    
    toString(): string {
        return "[" + this.nums.join(",") + "]";
    }
}` },]} />

**Complexity**
- Time: O(n + m) for add, O(n) for toString where n and m are the lengths of the arrays
- Space: O(n) for toString (to store the result string), O(1) for add
- Notes: More concise code using built-in functions

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Array Wrapper Solutions","datePublished":"2025-12-02","description":"Create a class that wraps an array and provides string representation and addition operations.","keywords":"LeetCode, Array Wrapper, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the add method?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the add method is O(n + m) where n and m are the lengths of the two arrays being added."}},{"@type":"Question","name":"What is the time complexity of the toString method?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the toString method is O(n) where n is the length of the array being converted to string."}},{"@type":"Question","name":"What is the space complexity of the add method?","acceptedAnswer":{"@type":"Answer","text":"The space complexity of the add method is O(1) as we only use a constant amount of extra space for the sum."}},{"@type":"Question","name":"What is the space complexity of the toString method?","acceptedAnswer":{"@type":"Answer","text":"The space complexity of the toString method is O(n) where n is the length of the array, as we need to store the resulting string."}}]}).replace(/</g,'\u003c')}
</script>