---
title: "Minimize String Length"
summary: "Given a string s, return the minimum possible length after deleting characters that appear more than once."
date: "Jan 05, 2024"
tags:
  - Hash Table
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 79.20% | **Paid:** No
> **Topics:** Hash Table, String

Given a string s, you can perform two types of operations on the string:

Remove any character from the string if it appears at least twice.

Return the minimum length of the string after performing the above operations.

## Table of Contents

- Examples
- Constraints
- [Hash Set](#hash-set)
- [Boolean Array](#boolean-array)
- [Bit Manipulation](#bit-manipulation)

## Examples

```text
Input: s = "aaabc"
Output: 3
Explanation: Remove two 'a' characters to get "abc".
```

```text
Input: s = "cbbd"
Output: 3
Explanation: Remove one 'b' character to get "cbd".
```

```text
Input: s = "dddaaa"
Output: 2
Explanation: Remove two 'd' and two 'a' characters to get "da".
```

## Constraints

```text
1 <= s.length <= 100
s consists of lowercase English letters.
```

---
## Hash Set

**Intuition**
The minimum length is achieved by keeping only one instance of every unique character. A Hash Set automatically handles uniqueness.

**Steps**
- Initialize an empty Hash Set.
- Iterate through each character in the string and add it to the set.
- The size of the set represents the count of unique characters, which is the answer.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimizedStringLength(self, s: str) -&gt; int:\n        return len(set(s))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimizedStringLength(String s) {\n        return (int) s.chars().distinct().count();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimizedStringLength(string s) {\n        unordered_set&lt;char&gt; st(s.begin(), s.end());\n        return st.size();\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    minimizedStringLength(s) {\n        return new Set(s).size;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    minimizedStringLength(s: string): number {\n        return new Set(s).size;\n    }\n}" },]} />

**Complexity**
- Time: O(N)
- Space: O(1) (Since the alphabet size is constant 26)
- Notes: High-level abstraction, very readable.

---
## Boolean Array

**Intuition**
Since the input consists only of lowercase English letters, we can use a fixed-size boolean array of length 26 to track seen characters more efficiently than a Hash Set.

**Steps**
- Create a boolean array of size 26 initialized to false.
- Iterate through the string. For each character, calculate its index (0-25).
- If the character hasn't been seen, mark it as seen and increment a counter.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimizedStringLength(self, s: str) -&gt; int:\n        seen = [0] * 26\n        for c in s:\n            seen[ord(c) - 97] = 1\n        return sum(seen)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimizedStringLength(String s) {\n        boolean[] seen = new boolean[26];\n        int count = 0;\n        for (char c : s.toCharArray()) {\n            if (!seen[c - 'a']) {\n                seen[c - 'a'] = true;\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimizedStringLength(string s) {\n        int seen[26] = {0};\n        int count = 0;\n        for (char c : s) {\n            if (!seen[c - 'a']) {\n                seen[c - 'a'] = 1;\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    minimizedStringLength(s) {\n        let seen = new Array(26).fill(0);\n        let count = 0;\n        for (let i = 0; i &lt; s.length; i++) {\n            let idx = s.charCodeAt(i) - 97;\n            if (seen[idx] === 0) {\n                seen[idx] = 1;\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    minimizedStringLength(s: string): number {\n        let seen: number[] = new Array(26).fill(0);\n        let count = 0;\n        for (let i = 0; i &lt; s.length; i++) {\n            let idx = s.charCodeAt(i) - 97;\n            if (seen[idx] === 0) {\n                seen[idx] = 1;\n                count++;\n            }\n        }\n        return count;\n    }\n}" },]} />

**Complexity**
- Time: O(N)
- Space: O(1)
- Notes: Slightly faster than Hash Set due to lower overhead.

---
## Bit Manipulation

**Intuition**
We can use a single integer as a bitmask to track the 26 possible letters. Each bit in the integer corresponds to a letter in the alphabet.

**Steps**
- Initialize an integer `mask` to 0.
- Iterate through the string. For each character, calculate the bit position (0-25).
- Use the bitwise OR operator to set the corresponding bit in the mask.
- Finally, count the number of set bits (1s) in the mask.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimizedStringLength(self, s: str) -&gt; int:\n        mask = 0\n        for c in s:\n            mask |= 1 &lt;&lt; (ord(c) - 97)\n        return bin(mask).count('1')" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimizedStringLength(String s) {\n        int mask = 0;\n        for (char c : s.toCharArray()) {\n            mask |= 1 &lt;&lt; (c - 'a');\n        }\n        return Integer.bitCount(mask);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimizedStringLength(string s) {\n        int mask = 0;\n        for (char c : s) {\n            mask |= 1 &lt;&lt; (c - 'a');\n        }\n        return __builtin_popcount(mask);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    minimizedStringLength(s) {\n        let mask = 0;\n        for (let i = 0; i &lt; s.length; i++) {\n            mask |= 1 &lt;&lt; (s.charCodeAt(i) - 97);\n        }\n        let count = 0;\n        while (mask &gt; 0) {\n            count += mask & 1;\n            mask &gt;&gt;= 1;\n        }\n        return count;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    minimizedStringLength(s: string): number {\n        let mask = 0;\n        for (let i = 0; i &lt; s.length; i++) {\n            mask |= 1 &lt;&lt; (s.charCodeAt(i) - 97);\n        }\n        let count = 0;\n        while (mask &gt; 0) {\n            count += mask & 1;\n            mask &gt;&gt;= 1;\n        }\n        return count;\n    }\n}" },]} />

**Complexity**
- Time: O(N)
- Space: O(1)
- Notes: Extremely space-efficient, uses bitwise operations for speed.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimize String Length Solutions","datePublished":"2024-01-05","description":"Given a string s, return the minimum possible length after deleting characters that appear more than once.","keywords":"LeetCode, Minimize String Length, Easy, Hash Table, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N), where N is the length of the string, as we iterate through the string once."}},{"@type":"Question","name":"Why is the space complexity O(1) for the Boolean Array approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because the array size is fixed at 26, regardless of the input string length."}},{"@type":"Question","name":"How does Bit Manipulation optimize space?","acceptedAnswer":{"@type":"Answer","text":"Bit manipulation uses a single integer to store the presence of 26 characters, reducing space usage to a constant 32 bits (or 64 bits)."}}]}).replace(/</g,'\u003c')}
</script>