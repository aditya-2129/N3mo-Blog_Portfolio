---
title: "Count Odd Numbers in an Interval Range"
summary: "Given two non-negative integers low and high, return the count of odd numbers between low and high (inclusive)."
date: "Jun 28, 2024"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 54.50% | **Paid:** No
> **Topics:** Math

Given two non-negative integers low and high, return the count of odd numbers between low and high (inclusive).

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Mathematical Formula](#mathematical-formula)

## Examples

### Example 1
```text
Input: low = 3, high = 7
Output: 3
Explanation: The odd numbers between 3 and 7 are [3,5,7].
```

### Example 2
```text
Input: low = 0, high = 10
Output: 5
Explanation: The odd numbers between 0 and 10 are [1,3,5,7,9].
```

## Constraints

```text
0 <= low <= high <= 10⁹
```

## Brute Force

**Intuition**
Iterate through all numbers from low to high and count how many are odd.

**Steps**
- Initialize a counter to 0
- Loop from low to high (inclusive)
- For each number, check if it's odd using modulo operator
- Increment counter if odd
- Return the counter

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countOdds(self, low: int, high: int) -&gt; int:\n        count = 0\n        for num in range(low, high + 1):\n            if num % 2 == 1:\n                count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int countOdds(int low, int high) {\n        int count = 0;\n        for (int num = low; num &lt;= high; num++) {\n            if (num % 2 == 1) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countOdds(int low, int high) {\n        int count = 0;\n        for (int num = low; num &lt;= high; num++) {\n            if (num % 2 == 1) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} low\n * @param {number} high\n * @return {number}\n */\nvar countOdds = function(low, high) {\n    let count = 0;\n    for (let num = low; num &lt;= high; num++) {\n        if (num % 2 === 1) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countOdds(low: number, high: number): number {\n    let count = 0;\n    for (let num = low; num &lt;= high; num++) {\n        if (num % 2 === 1) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n) where n = high - low + 1
- Space: O(1)
- Notes: Simple but inefficient for large ranges

## Mathematical Formula

**Intuition**
Use a mathematical formula to directly calculate the count of odd numbers without iteration. The count of odd numbers from 0 to n is (n + 1) / 2 using integer division. So the count from low to high is the count from 0 to high minus the count from 0 to low - 1.

**Steps**
- Calculate count of odd numbers from 0 to high: (high + 1) / 2
- Calculate count of odd numbers from 0 to low - 1: low / 2
- Return the difference

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countOdds(self, low: int, high: int) -&gt; int:\n        return (high + 1) // 2 - low // 2" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int countOdds(int low, int high) {\n        return (high + 1) / 2 - low / 2;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countOdds(int low, int high) {\n        return (high + 1) / 2 - low / 2;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} low\n * @param {number} high\n * @return {number}\n */\nvar countOdds = function(low, high) {\n    return Math.floor((high + 1) / 2) - Math.floor(low / 2);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countOdds(low: number, high: number): number {\n    return Math.floor((high + 1) / 2) - Math.floor(low / 2);\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Optimal solution with constant time complexity

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Count Odd Numbers in an Interval Range Solutions","datePublished":"2024-06-28","description":"Given two non-negative integers low and high, return the count of odd numbers between low and high (inclusive).","keywords":"LeetCode, Count Odd Numbers in an Interval Range, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the brute force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the brute force approach is O(n) where n = high - low + 1."}},{"@type":"Question","name":"What is the time complexity of the mathematical formula approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the mathematical formula approach is O(1) as it uses a direct calculation without iteration."}},{"@type":"Question","name":"What is the formula to count odd numbers in a range?","acceptedAnswer":{"@type":"Answer","text":"The formula is (high + 1) / 2 - low / 2 using integer division, which gives the count of odd numbers from low to high inclusive."}}]}).replace(/</g,'\u003c')}
</script>