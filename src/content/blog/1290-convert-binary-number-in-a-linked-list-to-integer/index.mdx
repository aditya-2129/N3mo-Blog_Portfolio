---
title: "Convert Binary Number in a Linked List to Integer"
summary: "Given head which is a reference node to a singly-linked list, return the decimal value of the binary number represented by the linked list."
date: "Dec 31, 2024"
tags:
  - Linked List
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.30% | **Paid:** No
> **Topics:** Linked List, Math

Given head which is a reference node to a singly-linked list. The values of each node in the linked-list is either 0 or 1. The linked list holds the binary representation of a number.

Return the decimal value of the number in the linked list.

The most significant bit (MSB) is at the head of the linked list.

- Examples
- Constraints
- [Bit Manipulation Approach](#bit-manipulation-approach)
- [String Conversion Approach](#string-conversion-approach)
- [Recursive Approach](#recursive-approach)
- [Array Approach](#array-approach)

## Examples

Example 1:

```text
Input: head = [1,0,1]
Output: 5
Explanation: (101) in base 2 equals (5) in base 10.
```

Example 2:

```text
Input: head = [0]
Output: 0
```

## Constraints

```text
The number of nodes in the linked list is in the range [1, 30].
Node.val is either 0 or 1.
```

---

## Bit Manipulation Approach

**Intuition**
Traverse the linked list while building the integer by left-shifting the current result and adding the current node's value.

**Steps**
- Initialize result to 0
- Traverse the linked list
- For each node, left-shift result by 1 (multiply by 2) and add the node's value
- Return the final result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def getDecimalValue(self, head: ListNode) -> int:
        result = 0
        current = head
        while current:
            result = (result &lt;&lt; 1) | current.val
            current = current.next
        return result` },
{ name: "Java", lang: "java", code: `/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public int getDecimalValue(ListNode head) {
        int result = 0;
        ListNode current = head;
        while (current != null) {
            result = (result &lt;&lt; 1) | current.val;
            current = current.next;
        }
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    int getDecimalValue(ListNode* head) {
        int result = 0;
        ListNode* current = head;
        while (current != nullptr) {
            result = (result &lt;&lt; 1) | current->val;
            current = current->next;
        }
        return result;
    }
}` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} head
 * @return {number}
 */
var getDecimalValue = function(head) {
    let result = 0;
    let current = head;
    while (current !== null) {
        result = (result &lt;&lt; 1) | current.val;
        current = current.next;
    }
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `/**
 * Definition for singly-linked list.
 * class ListNode {
 *     val: number
 *     next: ListNode | null
 *     constructor(val?: number, next?: ListNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.next = (next===undefined ? null : next)
 *     }
 * }
 */

function getDecimalValue(head: ListNode | null): number {
    let result = 0;
    let current = head;
    while (current !== null) {
        result = (result &lt;&lt; 1) | current.val;
        current = current.next;
    }
    return result;
}` },]} />

**Complexity**
- Time: O(n) where n is the number of nodes in the linked list
- Space: O(1) constant extra space
- Notes: Most efficient approach with optimal time and space complexity

---

## String Conversion Approach

**Intuition**
Convert the linked list to a string representation of the binary number, then parse it to an integer.

**Steps**
- Traverse the linked list and build a string of binary digits
- Parse the binary string to an integer
- Return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def getDecimalValue(self, head: ListNode) -> int:
        binary_str = ""
        current = head
        while current:
            binary_str += str(current.val)
            current = current.next
        return int(binary_str, 2)` },
{ name: "Java", lang: "java", code: `/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public int getDecimalValue(ListNode head) {
        StringBuilder binaryStr = new StringBuilder();
        ListNode current = head;
        while (current != null) {
            binaryStr.append(current.val);
            current = current.next;
        }
        return Integer.parseInt(binaryStr.toString(), 2);
    }
}` },
{ name: "C++", lang: "cpp", code: `/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    int getDecimalValue(ListNode* head) {
        string binaryStr = "";
        ListNode* current = head;
        while (current != nullptr) {
            binaryStr += to_string(current->val);
            current = current->next;
        }
        return stoi(binaryStr, nullptr, 2);
    }
}` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} head
 * @return {number}
 */
var getDecimalValue = function(head) {
    let binaryStr = "";
    let current = head;
    while (current !== null) {
        binaryStr += current.val;
        current = current.next;
    }
    return parseInt(binaryStr, 2);
};` },
{ name: "TypeScript", lang: "typescript", code: `/**
 * Definition for singly-linked list.
 * class ListNode {
 *     val: number
 *     next: ListNode | null
 *     constructor(val?: number, next?: ListNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.next = (next===undefined ? null : next)
 *     }
 * }
 */

function getDecimalValue(head: ListNode | null): number {
    let binaryStr = "";
    let current = head;
    while (current !== null) {
        binaryStr += current.val;
        current = current.next;
    }
    return parseInt(binaryStr, 2);
}` },]} />

**Complexity**
- Time: O(n) where n is the number of nodes in the linked list
- Space: O(n) for storing the binary string
- Notes: Less space efficient than bit manipulation but more intuitive

---

## Recursive Approach

**Intuition**
Recursively process the linked list, building the integer from the most significant bit to the least significant bit.

**Steps**
- Base case: if head is null, return 0
- Recursively process the rest of the list
- Combine the result by left-shifting and adding the current node's value

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def getDecimalValue(self, head: ListNode) -> int:
        def helper(node: ListNode, result: int) -> int:
            if node is None:
                return result
            return helper(node.next, (result &lt;&lt; 1) | node.val)
        return helper(head, 0)` },
{ name: "Java", lang: "java", code: `/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public int getDecimalValue(ListNode head) {
        return helper(head, 0);
    }
    
    private int helper(ListNode node, int result) {
        if (node == null) {
            return result;
        }
        return helper(node.next, (result &lt;&lt; 1) | node.val);
    }
}` },
{ name: "C++", lang: "cpp", code: `/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    int getDecimalValue(ListNode* head) {
        return helper(head, 0);
    }
    
private:
    int helper(ListNode* node, int result) {
        if (node == nullptr) {
            return result;
        }
        return helper(node->next, (result &lt;&lt; 1) | node->val);
    }
}` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} head
 * @return {number}
 */
var getDecimalValue = function(head) {
    function helper(node, result) {
        if (node === null) {
            return result;
        }
        return helper(node.next, (result &lt;&lt; 1) | node.val);
    }
    return helper(head, 0);
};` },
{ name: "TypeScript", lang: "typescript", code: `/**
 * Definition for singly-linked list.
 * class ListNode {
 *     val: number
 *     next: ListNode | null
 *     constructor(val?: number, next?: ListNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.next = (next===undefined ? null : next)
 *     }
 * }
 */

function getDecimalValue(head: ListNode | null): number {
    function helper(node: ListNode | null, result: number): number {
        if (node === null) {
            return result;
        }
        return helper(node.next, (result &lt;&lt; 1) | node.val);
    }
    return helper(head, 0);
}` },]} />

**Complexity**
- Time: O(n) where n is the number of nodes in the linked list
- Space: O(n) for the recursion stack
- Notes: Elegant but uses O(n) stack space due to recursion

---

## Array Approach

**Intuition**
Store all values in an array, then compute the integer by iterating from the most significant bit.

**Steps**
- Traverse the linked list and store all values in an array
- Iterate through the array, computing the decimal value using powers of 2
- Return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def getDecimalValue(self, head: ListNode) -> int:
        values = []
        current = head
        while current:
            values.append(current.val)
            current = current.next
        
        result = 0
        n = len(values)
        for i in range(n):
            result += values[i] * (1 &lt;&lt; (n - 1 - i))
        return result` },
{ name: "Java", lang: "java", code: `/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public int getDecimalValue(ListNode head) {
        List<Integer> values = new ArrayList&lt;&gt;();
        ListNode current = head;
        while (current != null) {
            values.add(current.val);
            current = current.next;
        }
        
        int result = 0;
        int n = values.size();
        for (int i = 0; i &lt; n; i++) {
            result += values.get(i) * (1 &lt;&lt; (n - 1 - i));
        }
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    int getDecimalValue(ListNode* head) {
        vector<int> values;
        ListNode* current = head;
        while (current != nullptr) {
            values.push_back(current->val);
            current = current->next;
        }
        
        int result = 0;
        int n = values.size();
        for (int i = 0; i &lt; n; i++) {
            result += values[i] * (1 &lt;&lt; (n - 1 - i));
        }
        return result;
    }
}` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} head
 * @return {number}
 */
var getDecimalValue = function(head) {
    const values = [];
    let current = head;
    while (current !== null) {
        values.push(current.val);
        current = current.next;
    }
    
    let result = 0;
    const n = values.length;
    for (let i = 0; i &lt; n; i++) {
        result += values[i] * (1 &lt;&lt; (n - 1 - i));
    }
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `/**
 * Definition for singly-linked list.
 * class ListNode {
 *     val: number
 *     next: ListNode | null
 *     constructor(val?: number, next?: ListNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.next = (next===undefined ? null : next)
 *     }
 * }
 */

function getDecimalValue(head: ListNode | null): number {
    const values: number[] = [];
    let current = head;
    while (current !== null) {
        values.push(current.val);
        current = current.next;
    }
    
    let result = 0;
    const n = values.length;
    for (let i = 0; i &lt; n; i++) {
        result += values[i] * (1 &lt;&lt; (n - 1 - i));
    }
    return result;
}` },]} />

**Complexity**
- Time: O(n) where n is the number of nodes in the linked list
- Space: O(n) for storing the array
- Notes: Less efficient than bit manipulation but demonstrates the mathematical concept clearly

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Convert Binary Number in a Linked List to Integer Solutions","datePublished":"2024-12-31","description":"Given head which is a reference node to a singly-linked list, return the decimal value of the binary number represented by the linked list.","keywords":"LeetCode, Convert Binary Number in a Linked List to Integer, Easy, Linked List, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Bit Manipulation Approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of nodes in the linked list"}},{"@type":"Question","name":"What is the space complexity of the Bit Manipulation Approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) constant extra space"}},{"@type":"Question","name":"What is the time complexity of the String Conversion Approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of nodes in the linked list"}},{"@type":"Question","name":"What is the space complexity of the String Conversion Approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for storing the binary string"}},{"@type":"Question","name":"What is the time complexity of the Recursive Approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of nodes in the linked list"}},{"@type":"Question","name":"What is the space complexity of the Recursive Approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for the recursion stack"}},{"@type":"Question","name":"What is the time complexity of the Array Approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of nodes in the linked list"}},{"@type":"Question","name":"What is the space complexity of the Array Approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for storing the array"}}]}).replace(/</g,'\u003c')}
</script>