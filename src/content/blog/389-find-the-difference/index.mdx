---
title: "Find the Difference"
summary: "You are given two strings s and t. String t is generated by random shuffling string s and then adding one more letter at a random position. Return the letter that was added to t."
date: "Nov 15, 2025"
tags:
  - Hash Table
  - String
  - Bit Manipulation
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 60.30% | **Paid:** No
> **Topics:** Hash Table, String, Bit Manipulation, Sorting

You are given two strings s and t.
String t is generated by random shuffling string s and then adding one more letter at a random position.
Return the letter that was added to t.

- Examples
- Constraints
- [Approach 1: Hash Map (Frequency Count)](#hash-map-frequency-count)
- [Approach 2: Sorting](#sorting)
- [Approach 3: Bit Manipulation (XOR)](#bit-manipulation-xor)
- [Approach 4: Summation](#summation)

## Examples

Example 1:
```text
Input: s = "abcd", t = "abcde"
Output: "e"
Explanation: 'e' is the letter that was added to t.
```

Example 2:
```text
Input: s = "", t = "y"
Output: "y"
```

## Constraints

```text
0 <= s.length <= 1000
t.length == s.length + 1
s and t consist of lowercase English letters.
```

---
## Hash Map (Frequency Count)

**Intuition**
We can count the frequency of each character in string `s` and then decrement the count while iterating through string `t`. The character that causes the count to drop below zero (or is missing from the map) is the added letter.

**Steps**
- Initialize an array or hash map of size 26 (for lowercase English letters) with zeros.
- Iterate through string `s` and increment the count for each character.
- Iterate through string `t` and decrement the count for each character.
- If the count for a character becomes negative, return that character immediately.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findTheDifference(self, s: str, t: str) -&gt; str:\n        cnt = [0] * 26\n        for ch in s:\n            cnt[ord(ch) - ord('a')] += 1\n        for ch in t:\n            cnt[ord(ch) - ord('a')] -= 1\n            if cnt[ord(ch) - ord('a')] &lt; 0:\n                return ch\n        return ''" },
{ name: "Java", lang: "java", code: "class Solution {\n    public char findTheDifference(String s, String t) {\n        int[] cnt = new int[26];\n        for (int i = 0; i &lt; s.length(); i++) {\n            cnt[s.charAt(i) - 'a']++;\n        }\n        for (int i = 0; i &lt; t.length(); i++) {\n            cnt[t.charAt(i) - 'a']--;\n            if (cnt[t.charAt(i) - 'a'] &lt; 0) {\n                return t.charAt(i);\n            }\n        }\n        return ' ';\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    char findTheDifference(string s, string t) {\n        vector&lt;int&gt; cnt(26, 0);\n        for (char c : s) {\n            cnt[c - 'a']++;\n        }\n        for (char c : t) {\n            cnt[c - 'a']--;\n            if (cnt[c - 'a'] &lt; 0) {\n                return c;\n            }\n        }\n        return ' ';\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var findTheDifference = function(s, t) {\n    let cnt = new Array(26).fill(0);\n    for (let i = 0; i &lt; s.length; i++) {\n        cnt[s.charCodeAt(i) - 97]++;\n    }\n    for (let i = 0; i &lt; t.length; i++) {\n        cnt[t.charCodeAt(i) - 97]--;\n        if (cnt[t.charCodeAt(i) - 97] &lt; 0) {\n            return t[i];\n        }\n    }\n    return '';\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findTheDifference(s: string, t: string): string {\n    let cnt = new Array(26).fill(0);\n    for (let i = 0; i &lt; s.length; i++) {\n        cnt[s.charCodeAt(i) - 97]++;\n    }\n    for (let i = 0; i &lt; t.length; i++) {\n        cnt[t.charCodeAt(i) - 97]--;\n        if (cnt[t.charCodeAt(i) - 97] &lt; 0) {\n            return t[i];\n        }\n    }\n    return '';\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: We use a fixed-size array of 26 integers, so space complexity is constant regardless of input size.

---
## Sorting

**Intuition**
If we sort both strings, the characters will be in order. The extra character in `t` will either be the first character that doesn't match `s`, or it will be the very last character of `t` if all previous characters match.

**Steps**
- Convert strings to character arrays (if necessary) and sort them.
- Iterate through the length of the shorter string `s`.
- If characters at index `i` differ between `s` and `t`, return the character from `t`.
- If the loop completes without finding a difference, return the last character of `t`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findTheDifference(self, s: str, t: str) -&gt; str:\n        sorted_s = sorted(s)\n        sorted_t = sorted(t)\n        for i in range(len(s)):\n            if sorted_s[i] != sorted_t[i]:\n                return sorted_t[i]\n        return sorted_t[-1]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public char findTheDifference(String s, String t) {\n        char[] sArr = s.toCharArray();\n        char[] tArr = t.toCharArray();\n        Arrays.sort(sArr);\n        Arrays.sort(tArr);\n        for (int i = 0; i &lt; s.length(); i++) {\n            if (sArr[i] != tArr[i]) {\n                return tArr[i];\n            }\n        }\n        return tArr[t.length() - 1];\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    char findTheDifference(string s, string t) {\n        sort(s.begin(), s.end());\n        sort(t.begin(), t.end());\n        for (int i = 0; i &lt; s.size(); i++) {\n            if (s[i] != t[i]) {\n                return t[i];\n            }\n        }\n        return t.back();\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var findTheDifference = function(s, t) {\n    s = s.split('').sort().join('');\n    t = t.split('').sort().join('');\n    for (let i = 0; i &lt; s.length; i++) {\n        if (s[i] !== t[i]) {\n            return t[i];\n        }\n    }\n    return t[t.length - 1];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findTheDifference(s: string, t: string): string {\n    s = s.split('').sort().join('');\n    t = t.split('').sort().join('');\n    for (let i = 0; i &lt; s.length; i++) {\n        if (s[i] !== t[i]) {\n            return t[i];\n        }\n    }\n    return t[t.length - 1];\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(n)
- Notes: Sorting dominates the time complexity. In languages where strings are immutable, converting to arrays requires O(n) space.

---
## Bit Manipulation (XOR)

**Intuition**
The XOR operation has a property that `a ^ a = 0` and `a ^ 0 = a`. If we XOR all characters from both strings together, the characters appearing in both `s` and `t` will cancel each other out (result in 0). The result will be the ASCII value of the extra character.

**Steps**
- Initialize a variable `res` to 0.
- Iterate through string `s` and XOR each character's ASCII value with `res`.
- Iterate through string `t` and XOR each character's ASCII value with `res`.
- Convert the final integer value of `res` back to a character and return it.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findTheDifference(self, s: str, t: str) -&gt; str:\n        res = 0\n        for ch in s:\n            res ^= ord(ch)\n        for ch in t:\n            res ^= ord(ch)\n        return chr(res)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public char findTheDifference(String s, String t) {\n        int res = 0;\n        for (int i = 0; i &lt; s.length(); i++) {\n            res ^= s.charAt(i);\n        }\n        for (int i = 0; i &lt; t.length(); i++) {\n            res ^= t.charAt(i);\n        }\n        return (char) res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    char findTheDifference(string s, string t) {\n        int res = 0;\n        for (char c : s) {\n            res ^= c;\n        }\n        for (char c : t) {\n            res ^= c;\n        }\n        return (char) res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var findTheDifference = function(s, t) {\n    let res = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        res ^= s.charCodeAt(i);\n    }\n    for (let i = 0; i &lt; t.length; i++) {\n        res ^= t.charCodeAt(i);\n    }\n    return String.fromCharCode(res);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findTheDifference(s: string, t: string): string {\n    let res = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        res ^= s.charCodeAt(i);\n    }\n    for (let i = 0; i &lt; t.length; i++) {\n        res ^= t.charCodeAt(i);\n    }\n    return String.fromCharCode(res);\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: This is the most optimal approach in terms of space and operation count, as it only requires a single integer variable.

---
## Summation

**Intuition**
Since `t` contains all characters of `s` plus one extra, the difference between the sum of ASCII values of characters in `t` and the sum of ASCII values of characters in `s` will be the ASCII value of the added character.

**Steps**
- Calculate the sum of ASCII values of all characters in `s`.
- Calculate the sum of ASCII values of all characters in `t`.
- Subtract the sum of `s` from the sum of `t`.
- Convert the resulting difference to a character and return it.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findTheDifference(self, s: str, t: str) -&gt; str:\n        sum_s = sum(ord(ch) for ch in s)\n        sum_t = sum(ord(ch) for ch in t)\n        return chr(sum_t - sum_s)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public char findTheDifference(String s, String t) {\n        int sumS = 0;\n        int sumT = 0;\n        for (int i = 0; i &lt; s.length(); i++) {\n            sumS += s.charAt(i);\n        }\n        for (int i = 0; i &lt; t.length(); i++) {\n            sumT += t.charAt(i);\n        }\n        return (char) (sumT - sumS);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    char findTheDifference(string s, string t) {\n        int sumS = 0;\n        int sumT = 0;\n        for (char c : s) sumS += c;\n        for (char c : t) sumT += c;\n        return (char) (sumT - sumS);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var findTheDifference = function(s, t) {\n    let sumS = 0;\n    let sumT = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        sumS += s.charCodeAt(i);\n    }\n    for (let i = 0; i &lt; t.length; i++) {\n        sumT += t.charCodeAt(i);\n    }\n    return String.fromCharCode(sumT - sumS);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findTheDifference(s: string, t: string): string {\n    let sumS = 0;\n    let sumT = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        sumS += s.charCodeAt(i);\n    }\n    for (let i = 0; i &lt; t.length; i++) {\n        sumT += t.charCodeAt(i);\n    }\n    return String.fromCharCode(sumT - sumS);\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Similar to XOR, this uses O(1) space. However, in extreme cases with very long strings, the sum could theoretically overflow standard integer types, though with the constraint `t.length &lt;= 1001`, this is not an issue for 32-bit integers.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find the Difference Solutions","datePublished":"2025-11-15","description":"You are given two strings s and t. String t is generated by random shuffling string s and then adding one more letter at a random position. Return the letter that was added to t.","keywords":"LeetCode, Find the Difference, Easy, Hash Table, String, Bit Manipulation, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Map approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) and the space complexity is O(1)."}},{"@type":"Question","name":"What is the time complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n log n) and the space complexity is O(n)."}},{"@type":"Question","name":"What is the time complexity of the Bit Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) and the space complexity is O(1)."}},{"@type":"Question","name":"What is the time complexity of the Summation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) and the space complexity is O(1)."}}]}).replace(/</g,'\u003c')}
</script>