---
title: "Make Three Strings Equal"
summary: "Find minimum operations to make three strings equal by deleting from the end."
date: "Dec 06, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 44.90% | **Paid:** No
> **Topics:** String

You are given three strings s1, s2, and s3 of the same length.

In one operation, you can choose one of the strings and delete its rightmost character.

Return the minimum number of operations required to make the strings equal. If it is impossible to make them equal, return -1.

- Examples
- Constraints
- [Iterative Approach](#iterative-approach)
- [While Loop Approach](#while-loop-approach)

## Examples

```text
Input: s1 = "abc", s2 = "abb", s3 = "ab"
Output: 2
Explanation: Delete the rightmost character from s1 and s2 to get "ab" and "ab".
```

```text
Input: s1 = "dac", s2 = "bac", s3 = "cac"
Output: -1
Explanation: The first characters of all strings are different, so it's impossible.
```

## Constraints

```text
1 <= s1.length, s2.length, s3.length <= 100
s1, s2, and s3 consist only of lowercase English letters.
```

---
## Iterative Approach

**Intuition**
Since we can only delete from the end, the resulting string must be a common prefix of all three strings. We find the longest common prefix and calculate deletions needed.

**Steps**
- Check if first characters match; if not, return -1
- Iterate through characters while all three match
- Calculate total operations as sum of (length - common_length)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def findMinimumOperations(self, s1: str, s2: str, s3: str) -> int:
        if not s1 or not s2 or not s3:
            return -1
        
        if s1[0] != s2[0] or s2[0] != s3[0]:
            return -1
        
        common = 0
        min_len = min(len(s1), len(s2), len(s3))
        
        for i in range(min_len):
            if s1[i] == s2[i] == s3[i]:
                common += 1
            else:
                break
        
        return (len(s1) - common) + (len(s2) - common) + (len(s3) - common)` },
{ name: "Java", lang: "java", code: `class Solution {
    public int findMinimumOperations(String s1, String s2, String s3) {
        if (s1.isEmpty() || s2.isEmpty() || s3.isEmpty()) {
            return -1;
        }
        
        if (s1.charAt(0) != s2.charAt(0) || s2.charAt(0) != s3.charAt(0)) {
            return -1;
        }
        
        int common = 0;
        int minLen = Math.min(Math.min(s1.length(), s2.length()), s3.length());
        
        for (int i = 0; i &lt; minLen; i++) {
            if (s1.charAt(i) == s2.charAt(i) && s2.charAt(i) == s3.charAt(i)) {
                common++;
            } else {
                break;
            }
        }
        
        return (s1.length() - common) + (s2.length() - common) + (s3.length() - common);
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int findMinimumOperations(string s1, string s2, string s3) {
        if (s1.empty() || s2.empty() || s3.empty()) {
            return -1;
        }
        
        if (s1[0] != s2[0] || s2[0] != s3[0]) {
            return -1;
        }
        
        int common = 0;
        int minLen = min({s1.length(), s2.length(), s3.length()});
        
        for (int i = 0; i &lt; minLen; i++) {
            if (s1[i] == s2[i] && s2[i] == s3[i]) {
                common++;
            } else {
                break;
            }
        }
        
        return (s1.length() - common) + (s2.length() - common) + (s3.length() - common);
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    findMinimumOperations(s1, s2, s3) {
        if (!s1 || !s2 || !s3) {
            return -1;
        }
        
        if (s1[0] !== s2[0] || s2[0] !== s3[0]) {
            return -1;
        }
        
        let common = 0;
        const minLen = Math.min(s1.length, s2.length, s3.length);
        
        for (let i = 0; i &lt; minLen; i++) {
            if (s1[i] === s2[i] && s2[i] === s3[i]) {
                common++;
            } else {
                break;
            }
        }
        
        return (s1.length - common) + (s2.length - common) + (s3.length - common);
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    findMinimumOperations(s1: string, s2: string, s3: string): number {
        if (!s1 || !s2 || !s3) {
            return -1;
        }
        
        if (s1[0] !== s2[0] || s2[0] !== s3[0]) {
            return -1;
        }
        
        let common = 0;
        const minLen = Math.min(s1.length, s2.length, s3.length);
        
        for (let i = 0; i &lt; minLen; i++) {
            if (s1[i] === s2[i] && s2[i] === s3[i]) {
                common++;
            } else {
                break;
            }
        }
        
        return (s1.length - common) + (s2.length - common) + (s3.length - common);
    }
}` },]} />

**Complexity**
- Time: O(n) where n is the length of the shortest string
- Space: O(1)
- Notes: Simple and efficient, single pass through the strings

---
## While Loop Approach

**Intuition**
Same logic as the iterative approach, but using a while loop for cleaner index-based iteration.

**Steps**
- Check if first characters match; if not, return -1
- Use while loop to find matching prefix length
- Calculate total operations as sum of (length - common_length)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def findMinimumOperations(self, s1: str, s2: str, s3: str) -> int:
        if not s1 or not s2 or not s3:
            return -1
        
        if s1[0] != s2[0] or s2[0] != s3[0]:
            return -1
        
        i = 0
        min_len = min(len(s1), len(s2), len(s3))
        
        while i &lt; min_len and s1[i] == s2[i] == s3[i]:
            i += 1
        
        return (len(s1) - i) + (len(s2) - i) + (len(s3) - i)` },
{ name: "Java", lang: "java", code: `class Solution {
    public int findMinimumOperations(String s1, String s2, String s3) {
        if (s1.isEmpty() || s2.isEmpty() || s3.isEmpty()) {
            return -1;
        }
        
        if (s1.charAt(0) != s2.charAt(0) || s2.charAt(0) != s3.charAt(0)) {
            return -1;
        }
        
        int i = 0;
        int minLen = Math.min(Math.min(s1.length(), s2.length()), s3.length());
        
        while (i &lt; minLen && s1.charAt(i) == s2.charAt(i) && s2.charAt(i) == s3.charAt(i)) {
            i++;
        }
        
        return (s1.length() - i) + (s2.length() - i) + (s3.length() - i);
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int findMinimumOperations(string s1, string s2, string s3) {
        if (s1.empty() || s2.empty() || s3.empty()) {
            return -1;
        }
        
        if (s1[0] != s2[0] || s2[0] != s3[0]) {
            return -1;
        }
        
        int i = 0;
        int minLen = min({s1.length(), s2.length(), s3.length()});
        
        while (i &lt; minLen && s1[i] == s2[i] && s2[i] == s3[i]) {
            i++;
        }
        
        return (s1.length() - i) + (s2.length() - i) + (s3.length() - i);
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    findMinimumOperations(s1, s2, s3) {
        if (!s1 || !s2 || !s3) {
            return -1;
        }
        
        if (s1[0] !== s2[0] || s2[0] !== s3[0]) {
            return -1;
        }
        
        let i = 0;
        const minLen = Math.min(s1.length, s2.length, s3.length);
        
        while (i &lt; minLen && s1[i] === s2[i] && s2[i] === s3[i]) {
            i++;
        }
        
        return (s1.length - i) + (s2.length - i) + (s3.length - i);
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    findMinimumOperations(s1: string, s2: string, s3: string): number {
        if (!s1 || !s2 || !s3) {
            return -1;
        }
        
        if (s1[0] !== s2[0] || s2[0] !== s3[0]) {
            return -1;
        }
        
        let i = 0;
        const minLen = Math.min(s1.length, s2.length, s3.length);
        
        while (i &lt; minLen && s1[i] === s2[i] && s2[i] === s3[i]) {
            i++;
        }
        
        return (s1.length - i) + (s2.length - i) + (s3.length - i);
    }
}` },]} />

**Complexity**
- Time: O(n) where n is the length of the shortest string
- Space: O(1)
- Notes: Same efficiency as for loop, slightly different style

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Make Three Strings Equal Solutions","datePublished":"2025-12-06","description":"Find minimum operations to make three strings equal by deleting from the end.","keywords":"LeetCode, Make Three Strings Equal, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of the shortest string."}},{"@type":"Question","name":"What is the space complexity of the While Loop Approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) as we only use a constant amount of extra space."}}]}).replace(/</g,'\u003c')}
</script>