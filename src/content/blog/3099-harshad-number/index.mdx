---
title: "Harshad Number"
summary: "Check if a number is divisible by the sum of its digits and return the sum if true, otherwise -1."
date: "Apr 19, 2024"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 83.30% | **Paid:** No
> **Topics:** Math

An integer divisible by the sum of its digits is said to be a Harshad number. You are given an integer x. Return the sum of the digits of x if x is a Harshad number, otherwise return -1.

- Examples
- Constraints
- [Mathematical Approach](#mathematical-approach)
- [String Conversion Approach](#string-conversion-approach)
- [Recursive Approach](#recursive-approach)

## Examples

```text
Input: x = 18
Output: 9
Explanation: 
The sum of digits of x is 9. 18 is divisible by 9, so 18 is a Harshad number.
```

```text
Input: x = 23
Output: -1
Explanation: 
The sum of digits of x is 5. 23 is not divisible by 5, so 23 is not a Harshad number.
```

## Constraints

```text
1 <= x <= 100
```

---

## Mathematical Approach

**Intuition**
Extract digits using modulo and division operations, sum them, then check if the original number is divisible by this sum.

**Steps**
- Initialize a sum variable to 0
- Extract each digit using modulo 10 and add to sum
- Divide the number by 10 to remove the last digit
- Repeat until number becomes 0
- Check if original number is divisible by the sum

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def sumOfTheDigitsOfHarshadNumber(self, x: int) -> int:
        original = x
        digit_sum = 0
        while x &gt; 0:
            digit_sum += x % 10
            x //= 10
        return digit_sum if original % digit_sum == 0 else -1` },
{ name: "Java", lang: "java", code: `class Solution {
    public int sumOfTheDigitsOfHarshadNumber(int x) {
        int original = x;
        int digitSum = 0;
        while (x &gt; 0) {
            digitSum += x % 10;
            x /= 10;
        }
        return original % digitSum == 0 ? digitSum : -1;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int sumOfTheDigitsOfHarshadNumber(int x) {
        int original = x;
        int digitSum = 0;
        while (x &gt; 0) {
            digitSum += x % 10;
            x /= 10;
        }
        return original % digitSum == 0 ? digitSum : -1;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} x
 * @return {number}
 */
var sumOfTheDigitsOfHarshadNumber = function(x) {
    const original = x;
    let digitSum = 0;
    while (x &gt; 0) {
        digitSum += x % 10;
        x = Math.floor(x / 10);
    }
    return original % digitSum === 0 ? digitSum : -1;
};` },
{ name: "TypeScript", lang: "typescript", code: `function sumOfTheDigitsOfHarshadNumber(x: number): number {
    const original = x;
    let digitSum = 0;
    while (x &gt; 0) {
        digitSum += x % 10;
        x = Math.floor(x / 10);
    }
    return original % digitSum === 0 ? digitSum : -1;
}` },]} />

**Complexity**
- Time: O(log₁₀ x) - number of digits in x
- Space: O(1) - constant extra space
- Notes: Most efficient approach with minimal memory overhead

---

## String Conversion Approach

**Intuition**
Convert the number to a string, iterate through each character, convert back to integer, sum all digits, then check divisibility.

**Steps**
- Convert the number to string representation
- Iterate through each character in the string
- Convert each character back to integer and accumulate sum
- Check if original number is divisible by the sum

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def sumOfTheDigitsOfHarshadNumber(self, x: int) -> int:
        digit_sum = sum(int(d) for d in str(x))
        return digit_sum if x % digit_sum == 0 else -1` },
{ name: "Java", lang: "java", code: `class Solution {
    public int sumOfTheDigitsOfHarshadNumber(int x) {
        String str = String.valueOf(x);
        int digitSum = 0;
        for (char c : str.toCharArray()) {
            digitSum += c - '0';
        }
        return x % digitSum == 0 ? digitSum : -1;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int sumOfTheDigitsOfHarshadNumber(int x) {
        string str = to_string(x);
        int digitSum = 0;
        for (char c : str) {
            digitSum += c - '0';
        }
        return x % digitSum == 0 ? digitSum : -1;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} x
 * @return {number}
 */
var sumOfTheDigitsOfHarshadNumber = function(x) {
    const str = x.toString();
    let digitSum = 0;
    for (const c of str) {
        digitSum += parseInt(c);
    }
    return x % digitSum === 0 ? digitSum : -1;
};` },
{ name: "TypeScript", lang: "typescript", code: `function sumOfTheDigitsOfHarshadNumber(x: number): number {
    const str = x.toString();
    let digitSum = 0;
    for (const c of str) {
        digitSum += parseInt(c);
    }
    return x % digitSum === 0 ? digitSum : -1;
}` },]} />

**Complexity**
- Time: O(log₁₀ x) - number of digits in x
- Space: O(log₁₀ x) - space for string representation
- Notes: More readable but uses extra memory for string conversion

---

## Recursive Approach

**Intuition**
Use recursion to extract and sum digits by repeatedly taking the last digit and processing the remaining number.

**Steps**
- Base case: if number is 0, return 0
- Recursive case: return last digit plus sum of remaining digits
- Check if original number is divisible by the computed sum

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def sumOfTheDigitsOfHarshadNumber(self, x: int) -> int:
        def digit_sum(n):
            if n == 0:
                return 0
            return n % 10 + digit_sum(n // 10)
        
        s = digit_sum(x)
        return s if x % s == 0 else -1` },
{ name: "Java", lang: "java", code: `class Solution {
    public int sumOfTheDigitsOfHarshadNumber(int x) {
        int sum = digitSum(x);
        return x % sum == 0 ? sum : -1;
    }
    
    private int digitSum(int n) {
        if (n == 0) {
            return 0;
        }
        return n % 10 + digitSum(n / 10);
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int sumOfTheDigitsOfHarshadNumber(int x) {
        int sum = digitSum(x);
        return x % sum == 0 ? sum : -1;
    }
    
private:
    int digitSum(int n) {
        if (n == 0) {
            return 0;
        }
        return n % 10 + digitSum(n / 10);
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} x
 * @return {number}
 */
var sumOfTheDigitsOfHarshadNumber = function(x) {
    const digitSum = (n) =&gt; {
        if (n === 0) {
            return 0;
        }
        return n % 10 + digitSum(Math.floor(n / 10));
    };
    
    const sum = digitSum(x);
    return x % sum === 0 ? sum : -1;
};` },
{ name: "TypeScript", lang: "typescript", code: `function sumOfTheDigitsOfHarshadNumber(x: number): number {
    const digitSum = (n: number): number =&gt; {
        if (n === 0) {
            return 0;
        }
        return n % 10 + digitSum(Math.floor(n / 10));
    };
    
    const sum = digitSum(x);
    return x % sum === 0 ? sum : -1;
}` },]} />

**Complexity**
- Time: O(log₁₀ x) - number of digits in x
- Space: O(log₁₀ x) - recursion stack depth
- Notes: Elegant but uses call stack space; may cause stack overflow for very large numbers

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Harshad Number Solutions","datePublished":"2024-04-19","description":"Check if a number is divisible by the sum of its digits and return the sum if true, otherwise -1.","keywords":"LeetCode, Harshad Number, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Mathematical Approach?","acceptedAnswer":{"@type":"Answer","text":"O(log₁₀ x) where x is the input number, as we process each digit once."}},{"@type":"Question","name":"What is the space complexity of the String Conversion Approach?","acceptedAnswer":{"@type":"Answer","text":"O(log₁₀ x) for storing the string representation of the number."}},{"@type":"Question","name":"Which approach is most memory efficient?","acceptedAnswer":{"@type":"Answer","text":"The Mathematical Approach uses O(1) extra space, making it the most memory efficient."}},{"@type":"Question","name":"What is a Harshad Number?","acceptedAnswer":{"@type":"Answer","text":"A Harshad Number (or Niven number) is an integer that is divisible by the sum of its digits."}}]}).replace(/</g,'\u003c')}
</script>