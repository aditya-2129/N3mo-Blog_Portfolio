---
title: "N-Repeated Element in Size 2N Array"
summary: "In an array of size 2N with N+1 unique elements, find the element that is repeated N times."
date: "Oct 18, 2025"
tags:
  - Array
  - Hash Table
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 79.90% | **Paid:** No
> **Topics:** Array, Hash Table

You are given an integer array nums with the following properties:

nums.length == 2 * n
nums contains n + 1 unique elements
Exactly one element of nums is repeated n times
Return the element that is repeated n times.

## Table of Contents

- Examples
- Constraints
- [Hash Map (Frequency Count)](#hash-map-frequency-count)
- [Compare with Previous Elements](#compare-with-previous-elements)

## Examples

### Example 1
```text
Input: nums = [1,2,3,3]
Output: 3
```

### Example 2
```text
Input: nums = [2,1,2,5,3,2]
Output: 2
```

### Example 3
```text
Input: nums = [5,1,5,2,5,3,5,4]
Output: 5
```

## Constraints

```text
2 <= n <= 5000
nums.length == 2 * n
0 <= nums[i] <= 10⁴
nums contains n + 1 unique elements
One element of nums is repeated n times
```

---

## Hash Map (Frequency Count)

**Intuition**
We can iterate through the array and store the frequency of each number in a hash map. The first number we encounter that already exists in the map is the repeated element.

**Steps**
- Initialize an empty hash map (or set).
- Iterate through each number in the array.
- If the number is already in the map, return it immediately.
- Otherwise, add the number to the map.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def repeatedNTimes(self, nums: list[int]) -&gt; int:\n        seen = set()\n        for num in nums:\n            if num in seen:\n                return num\n            seen.add(num)\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int repeatedNTimes(int[] nums) {\n        Set&lt;Integer&gt; seen = new HashSet&lt;&gt;();\n        for (int num : nums) {\n            if (seen.contains(num)) {\n                return num;\n            }\n            seen.add(num);\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int repeatedNTimes(vector&lt;int&gt;& nums) {\n        unordered_set&lt;int&gt; seen;\n        for (int num : nums) {\n            if (seen.count(num)) {\n                return num;\n            }\n            seen.insert(num);\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var repeatedNTimes = function(nums) {\n    const seen = new Set();\n    for (const num of nums) {\n        if (seen.has(num)) {\n            return num;\n        }\n        seen.add(num);\n    }\n};" },
{ name: "TypeScript", lang: "typescript", code: "function repeatedNTimes(nums: number[]): number {\n    const seen = new Set&lt;number&gt;();\n    for (const num of nums) {\n        if (seen.has(num)) {\n            return num;\n        }\n        seen.add(num);\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(N)
- Space: O(N)
- Notes: We use O(N) space to store the seen elements.

---

## Compare with Previous Elements

**Intuition**
Since the array length is 2N and one element appears N times, the repeated element must appear close to itself. Specifically, there must be at least one instance where the repeated element is within 2 indices of another instance (e.g., at positions i and i+1, or i and i+2). We can check the current element against the previous two elements.

**Steps**
- Iterate through the array starting from index 2.
- Check if `nums[i]` is equal to `nums[i-1]` or `nums[i-2]`.
- If a match is found, return that number.
- Also handle the edge cases for the first few elements (indices 0, 1, 2) by checking them against each other.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def repeatedNTimes(self, nums: list[int]) -&gt; int:\n        n = len(nums)\n        # Check the first few elements manually to cover all bases\n        if nums[0] == nums[1] or nums[0] == nums[2]:\n            return nums[0]\n        if nums[1] == nums[2]:\n            return nums[1]\n            \n        # Check the rest of the array\n        for i in range(3, n):\n            if nums[i] == nums[i-1] or nums[i] == nums[i-2]:\n                return nums[i]\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int repeatedNTimes(int[] nums) {\n        int n = nums.length;\n        if (nums[0] == nums[1] || nums[0] == nums[2]) return nums[0];\n        if (nums[1] == nums[2]) return nums[1];\n        \n        for (int i = 3; i &lt; n; i++) {\n            if (nums[i] == nums[i-1] || nums[i] == nums[i-2]) {\n                return nums[i];\n            }\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int repeatedNTimes(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        if (nums[0] == nums[1] || nums[0] == nums[2]) return nums[0];\n        if (nums[1] == nums[2]) return nums[1];\n        \n        for (int i = 3; i &lt; n; ++i) {\n            if (nums[i] == nums[i-1] || nums[i] == nums[i-2]) {\n                return nums[i];\n            }\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var repeatedNTimes = function(nums) {\n    const n = nums.length;\n    if (nums[0] === nums[1] || nums[0] === nums[2]) return nums[0];\n    if (nums[1] === nums[2]) return nums[1];\n    \n    for (let i = 3; i &lt; n; i++) {\n        if (nums[i] === nums[i-1] || nums[i] === nums[i-2]) {\n            return nums[i];\n        }\n    }\n};" },
{ name: "TypeScript", lang: "typescript", code: "function repeatedNTimes(nums: number[]): number {\n    const n: number = nums.length;\n    if (nums[0] === nums[1] || nums[0] === nums[2]) return nums[0];\n    if (nums[1] === nums[2]) return nums[1];\n    \n    for (let i = 3; i &lt; n; i++) {\n        if (nums[i] === nums[i-1] || nums[i] === nums[i-2]) {\n            return nums[i];\n        }\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(N)
- Space: O(1)
- Notes: This approach is optimal in terms of space complexity as it only uses a constant amount of extra memory.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode N-Repeated Element in Size 2N Array Solutions","datePublished":"2025-10-18","description":"In an array of size 2N with N+1 unique elements, find the element that is repeated N times.","keywords":"LeetCode, N-Repeated Element in Size 2N Array, Easy, Array, Hash Table"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Map approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N), where N is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of the Compare with Previous Elements approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because it does not use any extra data structures proportional to the input size."}},{"@type":"Question","name":"Why does checking distance 2 work for finding the repeated element?","acceptedAnswer":{"@type":"Answer","text":"Since the array length is 2N and the repeated element appears N times, the remaining N elements are unique. By the pigeonhole principle, the repeated element must appear at least once within a distance of 2 from another instance of itself."}}]}).replace(/</g,'\u003c')}
</script>