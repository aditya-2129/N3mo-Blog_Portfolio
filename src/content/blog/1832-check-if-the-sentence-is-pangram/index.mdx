---
title: "Check if the Sentence Is Pangram"
summary: "Determine if a given sentence contains every letter of the English alphabet at least once."
date: "May 22, 2025"
tags:
  - Hash Table
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 84.20% | **Paid:** No
> **Topics:** Hash Table, String

A pangram is a sentence where every letter of the English alphabet appears at least once.

Given a string sentence containing only lowercase English letters, return true if sentence is a pangram, or false otherwise.

- Examples
- Constraints
- [HashSet](#hashset)
- [Boolean Array](#boolean-array)
- [Bit Manipulation](#bit-manipulation)

## Examples

Example 1:
```text
Input: sentence = "thequickbrownfoxjumpsoverthelazydog"
Output: true
Explanation: The sentence contains at least one occurrence of every letter of the English alphabet.
```

Example 2:
```text
Input: sentence = "leetcode"
Output: false
```

## Constraints

```text
1 <= sentence.length <= 1000
sentence consists of lowercase English letters.
```

---
## HashSet

**Intuition**
Use a hash set to store unique characters encountered in the sentence. If the size of the set reaches 26, we have found every letter.

**Steps**
- Initialize an empty hash set.
- Iterate through each character in the sentence.
- Add the character to the set.
- After the loop, check if the size of the set is 26.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkIfPangram(self, sentence: str) -&gt; bool:\n        return len(set(sentence)) == 26" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkIfPangram(String sentence) {\n        Set&lt;Character&gt; seen = new HashSet&lt;&gt;();\n        for (int i = 0; i &lt; sentence.length(); i++) {\n            seen.add(sentence.charAt(i));\n        }\n        return seen.size() == 26;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkIfPangram(string sentence) {\n        unordered_set&lt;char&gt; seen;\n        for (char c : sentence) {\n            seen.insert(c);\n        }\n        return seen.size() == 26;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var checkIfPangram = function(sentence) {\n    const seen = new Set();\n    for (const c of sentence) {\n        seen.add(c);\n    }\n    return seen.size === 26;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkIfPangram(sentence: string): boolean {\n    const seen = new Set&lt;string&gt;();\n    for (const c of sentence) {\n        seen.add(c);\n    }\n    return seen.size === 26;\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string.
- Space: O(1) because the set will store at most 26 characters.
- Notes: Very readable and concise, especially in Python.

---
## Boolean Array

**Intuition**
Since the input consists only of lowercase English letters, we can use a fixed-size boolean array of length 26 to track which letters have been seen.

**Steps**
- Initialize a boolean array of size 26 with false values.
- Iterate through the string.
- Calculate the index for the current character (e.g., `index = char - 'a'`).
- Mark the corresponding index in the array as true.
- Finally, check if all values in the array are true.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkIfPangram(self, sentence: str) -&gt; bool:\n        seen = [False] * 26\n        for ch in sentence:\n            seen[ord(ch) - ord('a')] = True\n        return all(seen)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkIfPangram(String sentence) {\n        boolean[] seen = new boolean[26];\n        for (int i = 0; i &lt; sentence.length(); i++) {\n            seen[sentence.charAt(i) - 'a'] = true;\n        }\n        for (boolean b : seen) {\n            if (!b) return false;\n        }\n        return true;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkIfPangram(string sentence) {\n        vector&lt;bool&gt; seen(26, false);\n        for (char c : sentence) {\n            seen[c - 'a'] = true;\n        }\n        for (bool b : seen) {\n            if (!b) return false;\n        }\n        return true;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var checkIfPangram = function(sentence) {\n    const seen = new Array(26).fill(false);\n    for (const c of sentence) {\n        seen[c.charCodeAt(0) - 97] = true;\n    }\n    return seen.every(val =&gt; val === true);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkIfPangram(sentence: string): boolean {\n    const seen: boolean[] = new Array(26).fill(false);\n    for (const c of sentence) {\n        seen[c.charCodeAt(0) - 97] = true;\n    }\n    return seen.every(val =&gt; val === true);\n}" },]} />

**Complexity**
- Time: O(n) to iterate through the string.
- Space: O(1) as the array size is constant (26).
- Notes: Slightly more verbose than a set but very efficient with low overhead.

---
## Bit Manipulation

**Intuition**
We can use a single 32-bit integer as a bitmask to track the presence of letters. Each bit represents a letter (e.g., bit 0 for 'a', bit 1 for 'b').

**Steps**
- Initialize an integer `mask` to 0.
- Iterate through the string.
- Calculate the bit position for the character.
- Use bitwise OR to set the corresponding bit in `mask`.
- After processing all characters, check if `mask` equals `(1 &lt;&lt; 26) - 1` (which is a binary number with the lowest 26 bits set to 1).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkIfPangram(self, sentence: str) -&gt; bool:\n        mask = 0\n        for ch in sentence:\n            mask |= 1 &lt;&lt; (ord(ch) - ord('a'))\n        return mask == (1 &lt;&lt; 26) - 1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkIfPangram(String sentence) {\n        int mask = 0;\n        for (int i = 0; i &lt; sentence.length(); i++) {\n            mask |= 1 &lt;&lt; (sentence.charAt(i) - 'a');\n        }\n        return mask == (1 &lt;&lt; 26) - 1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkIfPangram(string sentence) {\n        int mask = 0;\n        for (char c : sentence) {\n            mask |= 1 &lt;&lt; (c - 'a');\n        }\n        return mask == (1 &lt;&lt; 26) - 1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var checkIfPangram = function(sentence) {\n    let mask = 0;\n    for (const c of sentence) {\n        mask |= 1 &lt;&lt; (c.charCodeAt(0) - 97);\n    }\n    return mask === (1 &lt;&lt; 26) - 1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkIfPangram(sentence: string): boolean {\n    let mask: number = 0;\n    for (const c of sentence) {\n        mask |= 1 &lt;&lt; (c.charCodeAt(0) - 97);\n    }\n    return mask === (1 &lt;&lt; 26) - 1;\n}" },]} />

**Complexity**
- Time: O(n) to iterate through the string.
- Space: O(1) using only a single integer variable.
- Notes: The most space-efficient approach, utilizing bitwise operations for optimal performance.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check if the Sentence Is Pangram Solutions","datePublished":"2025-05-22","description":"Determine if a given sentence contains every letter of the English alphabet at least once.","keywords":"LeetCode, Check if the Sentence Is Pangram, Easy, Hash Table, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the HashSet approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the sentence, as we iterate through the string once."}},{"@type":"Question","name":"What is the space complexity of the Bit Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use a single integer variable to store the bitmask."}},{"@type":"Question","name":"Why is the Boolean Array approach efficient?","acceptedAnswer":{"@type":"Answer","text":"It is efficient because the array size is fixed at 26, making it O(1) space, and direct index access makes it very fast."}}]}).replace(/</g,'\u003c')}
</script>