---
title: "Difference Between Element Sum and Digit Sum of an Array"
summary: "Calculate the absolute difference between the sum of array elements and the sum of their individual digits."
date: "Nov 24, 2025"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 85.30% | **Paid:** No
> **Topics:** Array, Math

You are given a positive integer array nums.

The element sum is the sum of all the elements in nums.
The digit sum is the sum of all the digits that appear in nums.
Return the absolute difference between element sum and digit sum.

Note that the digit sum of an integer is the sum of its digits (e.g., the digit sum of 12 is 1 + 2 = 3).

- Examples
- Constraints
- [Iterative with String Conversion](#iterative-with-string-conversion)
- [Iterative with Modulo Arithmetic](#iterative-with-modulo-arithmetic)
- [Functional One-Pass](#functional-one-pass)

## Examples

### Example 1:
```text
Input: nums = [1,15,6,3]
Output: 9
Explanation: 
The element sum of nums is 1 + 15 + 6 + 3 = 25.
The digit sum of nums is 1 + 1 + 5 + 6 + 3 = 16.
The absolute difference between the element sum and digit sum is |25 - 16| = 9.
```

### Example 2:
```text
Input: nums = [1,2,3,4]
Output: 0
Explanation:
The element sum of nums is 1 + 2 + 3 + 4 = 10.
The digit sum of nums is 1 + 2 + 3 + 4 = 10.
The absolute difference between the element sum and digit sum is |10 - 10| = 0.
```

## Constraints

```text
1 <= nums.length <= 2000
1 <= nums[i] <= 2000
```

---
## Iterative with String Conversion

**Intuition**
Convert each number to a string to easily iterate over its characters, which represent the digits, and sum them up.

**Steps**
- Initialize `element_sum` and `digit_sum` to 0.
- Iterate through each number in the array.
- Add the number to `element_sum`.
- Convert the number to a string, iterate through characters, convert back to integer, and add to `digit_sum`.
- Return the absolute difference between the two sums.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def differenceOfSum(self, nums: list[int]) -&gt; int:\n        element_sum = 0\n        digit_sum = 0\n        for num in nums:\n            element_sum += num\n            for d in str(num):\n                digit_sum += int(d)\n        return abs(element_sum - digit_sum)\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int differenceOfSum(int[] nums) {\n        int elementSum = 0;\n        int digitSum = 0;\n        for (int num : nums) {\n            elementSum += num;\n            String s = String.valueOf(num);\n            for (int i = 0; i &lt; s.length(); i++) {\n                digitSum += s.charAt(i) - '0';\n            }\n        }\n        return Math.abs(elementSum - digitSum);\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int differenceOfSum(vector&lt;int&gt;& nums) {\n        int elementSum = 0;\n        int digitSum = 0;\n        for (int num : nums) {\n            elementSum += num;\n            string s = to_string(num);\n            for (char c : s) {\n                digitSum += c - '0';\n            }\n        }\n        return abs(elementSum - digitSum);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var differenceOfSum = function(nums) {\n    let elementSum = 0;\n    let digitSum = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        let num = nums[i];\n        elementSum += num;\n        let s = num.toString();\n        for (let j = 0; j &lt; s.length; j++) {\n            digitSum += parseInt(s[j]);\n        }\n    }\n    return Math.abs(elementSum - digitSum);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function differenceOfSum(nums: number[]): number {\n    let elementSum = 0;\n    let digitSum = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        let num = nums[i];\n        elementSum += num;\n        let s = num.toString();\n        for (let j = 0; j &lt; s.length; j++) {\n            digitSum += parseInt(s[j]);\n        }\n    }\n    return Math.abs(elementSum - digitSum);\n}\n" },]} />

**Complexity**
- Time: O(N * L) where N is the number of elements and L is the average number of digits.
- Space: O(L) to store the string representation of the number.
- Notes: Readable but incurs overhead for string allocation.

---
## Iterative with Modulo Arithmetic

**Intuition**
Use mathematical operations (modulo 10 and integer division by 10) to extract digits without converting to strings.

**Steps**
- Initialize `element_sum` and `digit_sum` to 0.
- Iterate through each number in the array.
- Add the number to `element_sum`.
- While the number is greater than 0, add `num % 10` to `digit_sum` and divide `num` by 10.
- Return the absolute difference.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def differenceOfSum(self, nums: list[int]) -&gt; int:\n        element_sum = 0\n        digit_sum = 0\n        for num in nums:\n            element_sum += num\n            temp = num\n            while temp &gt; 0:\n                digit_sum += temp % 10\n                temp //= 10\n        return abs(element_sum - digit_sum)\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int differenceOfSum(int[] nums) {\n        int elementSum = 0;\n        int digitSum = 0;\n        for (int num : nums) {\n            elementSum += num;\n            int temp = num;\n            while (temp &gt; 0) {\n                digitSum += temp % 10;\n                temp /= 10;\n            }\n        }\n        return Math.abs(elementSum - digitSum);\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int differenceOfSum(vector&lt;int&gt;& nums) {\n        int elementSum = 0;\n        int digitSum = 0;\n        for (int num : nums) {\n            elementSum += num;\n            int temp = num;\n            while (temp &gt; 0) {\n                digitSum += temp % 10;\n                temp /= 10;\n            }\n        }\n        return abs(elementSum - digitSum);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var differenceOfSum = function(nums) {\n    let elementSum = 0;\n    let digitSum = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        let num = nums[i];\n        elementSum += num;\n        let temp = num;\n        while (temp &gt; 0) {\n            digitSum += temp % 10;\n            temp = Math.floor(temp / 10);\n        }\n    }\n    return Math.abs(elementSum - digitSum);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function differenceOfSum(nums: number[]): number {\n    let elementSum = 0;\n    let digitSum = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        let num = nums[i];\n        elementSum += num;\n        let temp = num;\n        while (temp &gt; 0) {\n            digitSum += temp % 10;\n            temp = Math.floor(temp / 10);\n        }\n    }\n    return Math.abs(elementSum - digitSum);\n}\n" },]} />

**Complexity**
- Time: O(N * L) where N is the number of elements and L is the average number of digits.
- Space: O(1) extra space.
- Notes: More efficient than string conversion as it avoids memory allocation.

---
## Functional One-Pass

**Intuition**
Utilize built-in functional programming constructs like `reduce`, `map`, or `sum` to calculate the sums concisely.

**Steps**
- Calculate the element sum using the language's standard sum/reduce function.
- Calculate the digit sum by mapping each number to its digits and summing them up.
- Return the absolute difference.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def differenceOfSum(self, nums: list[int]) -&gt; int:\n        element_sum = sum(nums)\n        digit_sum = sum(int(d) for n in nums for d in str(n))\n        return abs(element_sum - digit_sum)\n" },
{ name: "Java", lang: "java", code: "import java.util.stream.*;\n\nclass Solution {\n    public int differenceOfSum(int[] nums) {\n        int elementSum = Arrays.stream(nums).sum();\n        int digitSum = Arrays.stream(nums)\n            .flatMap(num -&gt; String.valueOf(num).chars().mapToObj(c -&gt; (char) c))\n            .mapToInt(Character::getNumericValue)\n            .sum();\n        return Math.abs(elementSum - digitSum);\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;numeric&gt;\n\nclass Solution {\npublic:\n    int differenceOfSum(vector&lt;int&gt;& nums) {\n        int elementSum = accumulate(nums.begin(), nums.end(), 0);\n        int digitSum = 0;\n        for (int num : nums) {\n            int temp = num;\n            while (temp &gt; 0) {\n                digitSum += temp % 10;\n                temp /= 10;\n            }\n        }\n        return abs(elementSum - digitSum);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var differenceOfSum = function(nums) {\n    const elementSum = nums.reduce((acc, val) =&gt; acc + val, 0);\n    const digitSum = nums.reduce((acc, val) =&gt; {\n        let sum = 0;\n        let n = val;\n        while (n &gt; 0) {\n            sum += n % 10;\n            n = Math.floor(n / 10);\n        }\n        return acc + sum;\n    }, 0);\n    return Math.abs(elementSum - digitSum);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function differenceOfSum(nums: number[]): number {\n    const elementSum = nums.reduce((acc, val) =&gt; acc + val, 0);\n    const digitSum = nums.reduce((acc, val) =&gt; {\n        let sum = 0;\n        let n = val;\n        while (n &gt; 0) {\n            sum += n % 10;\n            n = Math.floor(n / 10);\n        }\n        return acc + sum;\n    }, 0);\n    return Math.abs(elementSum - digitSum);\n}\n" },]} />

**Complexity**
- Time: O(N * L) where N is the number of elements and L is the average number of digits.
- Space: O(1) or O(N) depending on implementation details (e.g., intermediate streams).
- Notes: Concise and expressive, leveraging standard library features.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Difference Between Element Sum and Digit Sum of an Array Solutions","datePublished":"2025-11-24","description":"Calculate the absolute difference between the sum of array elements and the sum of their individual digits.","keywords":"LeetCode, Difference Between Element Sum and Digit Sum of an Array, Easy, Array, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative with String Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * L), where N is the number of elements in the array and L is the average number of digits per element."}},{"@type":"Question","name":"What is the space complexity of the Iterative with Modulo Arithmetic approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a few integer variables for calculation."}},{"@type":"Question","name":"Which approach is generally more efficient for calculating digit sums?","acceptedAnswer":{"@type":"Answer","text":"The Iterative with Modulo Arithmetic approach is generally more efficient as it avoids the overhead of converting integers to strings."}}]}).replace(/</g,'\u003c')}
</script>