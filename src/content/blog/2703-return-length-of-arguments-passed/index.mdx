---
title: "Return Length of Arguments Passed"
summary: "Write a function that returns the number of arguments passed to it."
date: "Feb 27, 2025"
tags:
  - Array
  - Function
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 94.50% | **Paid:** No
> **Topics:** N/A

Write a function argumentsLength that returns the number of arguments passed to it. The function should accept any number of arguments of any type and return the count of arguments.

- Examples
- Constraints
- [Using Built-in Length Property](#using-built-in-length-property)
- [Manual Counting with Loop](#manual-counting-with-loop)
- [Using Array/List Conversion](#using-arraylist-conversion)

## Examples

### Example 1

**Input:**
```text
args = [5]
```

**Output:**
```text
1
```

**Explanation:**
argumentsLength(5); // 1

One value was passed to the function so it should return 1.

### Example 2

**Input:**
```text
args = [{}, null, "3"]
```

**Output:**
```text
3
```

**Explanation:**
argumentsLength({}, null, "3"); // 3

Three values were passed to the function so it should return 3.

## Constraints
```text
0 <= args.length <= 100
```

---
## Using Built-in Length Property

**Intuition**
Most programming languages provide a built-in property or operator to get the length of a collection or variadic arguments, which is the most efficient way to solve this problem.

**Steps**
- Use the language's built-in length/size property to get the number of arguments
- Return the length

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `def argumentsLength(*args) -> int:
    return len(args)` },
{ name: "Java", lang: "java", code: `class Solution {
    public int argumentsLength(Object... args) {
        return args.length;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    template<typename... Args>
    int argumentsLength(Args... args) {
        return sizeof...(args);
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `var argumentsLength = function(...args) {
    return args.length;
}` },
{ name: "TypeScript", lang: "typescript", code: `function argumentsLength(...args: any[]): number {
    return args.length;
}` },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This is the most efficient approach as it directly accesses the length property without any additional computation.

---
## Manual Counting with Loop

**Intuition**
We can manually count the number of arguments by iterating through them and incrementing a counter for each argument.

**Steps**
- Initialize a counter to 0
- Iterate through each argument
- Increment the counter for each argument
- Return the counter

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `def argumentsLength(*args) -> int:
    count = 0
    for _ in args:
        count += 1
    return count` },
{ name: "Java", lang: "java", code: `class Solution {
    public int argumentsLength(Object... args) {
        int count = 0;
        for (Object arg : args) {
            count++;
        }
        return count;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int argumentsLength() {
        return 0;
    }
    
    template<typename T, typename... Args>
    int argumentsLength(T first, Args... rest) {
        return 1 + argumentsLength(rest...);
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `var argumentsLength = function(...args) {
    let count = 0;
    for (let i = 0; i &lt; args.length; i++) {
        count++;
    }
    return count;
}` },
{ name: "TypeScript", lang: "typescript", code: `function argumentsLength(...args: any[]): number {
    let count = 0;
    for (let i = 0; i &lt; args.length; i++) {
        count++;
    }
    return count;
}` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: This approach is less efficient than using the built-in length property but demonstrates the underlying concept of counting.

---
## Using Array/List Conversion

**Intuition**
We can convert the arguments to an array or list and then use the length/size property of the collection to get the count.

**Steps**
- Convert the arguments to an array or list
- Use the length/size property of the collection
- Return the length

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `def argumentsLength(*args) -> int:
    return len(list(args))` },
{ name: "Java", lang: "java", code: `import java.util.Arrays;

class Solution {
    public int argumentsLength(Object... args) {
        return Arrays.asList(args).size();
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <tuple>

class Solution {
public:
    template<typename... Args>
    int argumentsLength(Args... args) {
        return std::tuple_size<std::tuple<Args...&gt;&gt;::value;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `var argumentsLength = function(...args) {
    return Array.from(args).length;
}` },
{ name: "TypeScript", lang: "typescript", code: `function argumentsLength(...args: any[]): number {
    return args.length;
}` },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: This approach creates an additional data structure, which uses extra space. It's less efficient than the built-in length property approach.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Return Length of Arguments Passed Solutions","datePublished":"2025-02-27","description":"Write a function that returns the number of arguments passed to it.","keywords":"LeetCode, Return Length of Arguments Passed, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of using the built-in length property?","acceptedAnswer":{"@type":"Answer","text":"O(1) - accessing the length property is a constant time operation."}},{"@type":"Question","name":"What is the time complexity of manual counting with a loop?","acceptedAnswer":{"@type":"Answer","text":"O(n) - we iterate through all arguments once."}},{"@type":"Question","name":"What is the time complexity of using array/list conversion?","acceptedAnswer":{"@type":"Answer","text":"O(n) - converting to an array/list requires iterating through all arguments."}}]}).replace(/</g,'\u003c')}
</script>