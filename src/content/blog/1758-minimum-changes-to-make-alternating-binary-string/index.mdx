---
title: "Minimum Changes To Make Alternating Binary String"
summary: "Find minimum operations to make binary string alternating by flipping characters."
date: "May 26, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 67.80% | **Paid:** No
> **Topics:** String

You are given a string s consisting only of the characters '0' and '1'. In one operation, you can change any '0' to '1' or '1' to '0'.

Return the minimum number of operations needed to make s alternating.

A string is alternating if no two adjacent characters are equal. For example, "0101" and "1010" are alternating, while "0100" is not.

- Examples
- Constraints
- [Direct Counting Approach](#direct-counting-approach)
- [Optimized Counting Approach](#optimized-counting-approach)

## Examples

```text
Input: s = "0100"
Output: 1
Explanation: Change the last character to '1' to get "0101".
```

```text
Input: s = "10"
Output: 0
Explanation: The string is already alternating.
```

```text
Input: s = "1111"
Output: 2
Explanation: Change the second and last characters to '0' to get "1010".
```

## Constraints

```text
1 <= s.length <= 10⁴
s[i] is either '0' or '1'.
```

---
## Direct Counting Approach

**Intuition**
There are only two valid alternating patterns for a binary string: starting with '0' (0101...) or starting with '1' (1010...). We count changes needed for each pattern and return the minimum.

**Steps**
- Initialize two counters for changes needed for both patterns
- Iterate through the string and count mismatches for both patterns
- For each position, check if character differs from expected value in both patterns
- Return the minimum of both counts

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minOperations(self, s: str) -&gt; int:\n        n = len(s)\n        count0 = 0  # changes needed for pattern starting with '0'\n        count1 = 0  # changes needed for pattern starting with '1'\n        \n        for i in range(n):\n            expected0 = '0' if i % 2 == 0 else '1'\n            expected1 = '1' if i % 2 == 0 else '0'\n            \n            if s[i] != expected0:\n                count0 += 1\n            if s[i] != expected1:\n                count1 += 1\n        \n        return min(count0, count1)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minOperations(String s) {\n        int n = s.length();\n        int count0 = 0;  // changes needed for pattern starting with '0'\n        int count1 = 0;  // changes needed for pattern starting with '1'\n        \n        for (int i = 0; i &lt; n; i++) {\n            char expected0 = (i % 2 == 0) ? '0' : '1';\n            char expected1 = (i % 2 == 0) ? '1' : '0';\n            \n            if (s.charAt(i) != expected0) {\n                count0++;\n            }\n            if (s.charAt(i) != expected1) {\n                count1++;\n            }\n        }\n        \n        return Math.min(count0, count1);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minOperations(string s) {\n        int n = s.length();\n        int count0 = 0;  // changes needed for pattern starting with '0'\n        int count1 = 0;  // changes needed for pattern starting with '1'\n        \n        for (int i = 0; i &lt; n; i++) {\n            char expected0 = (i % 2 == 0) ? '0' : '1';\n            char expected1 = (i % 2 == 0) ? '1' : '0';\n            \n            if (s[i] != expected0) {\n                count0++;\n            }\n            if (s[i] != expected1) {\n                count1++;\n            }\n        }\n        \n        return min(count0, count1);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {number}\n */\nvar minOperations = function(s) {\n    const n = s.length;\n    let count0 = 0;  // changes needed for pattern starting with '0'\n    let count1 = 0;  // changes needed for pattern starting with '1'\n    \n    for (let i = 0; i &lt; n; i++) {\n        const expected0 = (i % 2 === 0) ? '0' : '1';\n        const expected1 = (i % 2 === 0) ? '1' : '0';\n        \n        if (s[i] !== expected0) {\n            count0++;\n        }\n        if (s[i] !== expected1) {\n            count1++;\n        }\n    }\n    \n    return Math.min(count0, count1);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minOperations(s: string): number {\n    const n = s.length;\n    let count0 = 0;  // changes needed for pattern starting with '0'\n    let count1 = 0;  // changes needed for pattern starting with '1'\n    \n    for (let i = 0; i &lt; n; i++) {\n        const expected0 = (i % 2 === 0) ? '0' : '1';\n        const expected1 = (i % 2 === 0) ? '1' : '0';\n        \n        if (s[i] !== expected0) {\n            count0++;\n        }\n        if (s[i] !== expected1) {\n            count1++;\n        }\n    }\n    \n    return Math.min(count0, count1);\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1) only using constant extra space
- Notes: Simple and straightforward approach with clear logic

---
## Optimized Counting Approach

**Intuition**
We can optimize by noting that count0 + count1 = n (every position matches exactly one of the two patterns). So we only need to count for one pattern and derive the other.

**Steps**
- Count changes needed for pattern starting with '0'
- The changes for pattern starting with '1' is simply n - count0
- Return the minimum of both

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minOperations(self, s: str) -&gt; int:\n        n = len(s)\n        count0 = 0  # changes needed for pattern starting with '0'\n        \n        for i in range(n):\n            expected = '0' if i % 2 == 0 else '1'\n            if s[i] != expected:\n                count0 += 1\n        \n        # count1 = n - count0 (every position matches exactly one pattern)\n        return min(count0, n - count0)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minOperations(String s) {\n        int n = s.length();\n        int count0 = 0;  // changes needed for pattern starting with '0'\n        \n        for (int i = 0; i &lt; n; i++) {\n            char expected = (i % 2 == 0) ? '0' : '1';\n            if (s.charAt(i) != expected) {\n                count0++;\n            }\n        }\n        \n        // count1 = n - count0 (every position matches exactly one pattern)\n        return Math.min(count0, n - count0);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minOperations(string s) {\n        int n = s.length();\n        int count0 = 0;  // changes needed for pattern starting with '0'\n        \n        for (int i = 0; i &lt; n; i++) {\n            char expected = (i % 2 == 0) ? '0' : '1';\n            if (s[i] != expected) {\n                count0++;\n            }\n        }\n        \n        // count1 = n - count0 (every position matches exactly one pattern)\n        return min(count0, n - count0);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {number}\n */\nvar minOperations = function(s) {\n    const n = s.length;\n    let count0 = 0;  // changes needed for pattern starting with '0'\n    \n    for (let i = 0; i &lt; n; i++) {\n        const expected = (i % 2 === 0) ? '0' : '1';\n        if (s[i] !== expected) {\n            count0++;\n        }\n    }\n    \n    // count1 = n - count0 (every position matches exactly one pattern)\n    return Math.min(count0, n - count0);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minOperations(s: string): number {\n    const n = s.length;\n    let count0 = 0;  // changes needed for pattern starting with '0'\n    \n    for (let i = 0; i &lt; n; i++) {\n        const expected = (i % 2 === 0) ? '0' : '1';\n        if (s[i] !== expected) {\n            count0++;\n        }\n    }\n    \n    // count1 = n - count0 (every position matches exactly one pattern)\n    return Math.min(count0, n - count0);\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1) only using constant extra space
- Notes: Slightly more efficient as we only track one counter and use mathematical relationship

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Changes To Make Alternating Binary String Solutions","datePublished":"2024-05-26","description":"Find minimum operations to make binary string alternating by flipping characters.","keywords":"LeetCode, Minimum Changes To Make Alternating Binary String, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Direct Counting Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we iterate through the string once."}},{"@type":"Question","name":"What is the space complexity of the Optimized Counting Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for a single counter."}},{"@type":"Question","name":"Why are there only two possible alternating patterns?","acceptedAnswer":{"@type":"Answer","text":"For a binary string, there are only two valid alternating patterns: starting with '0' (0101...) or starting with '1' (1010...). Any alternating binary string must match one of these patterns."}},{"@type":"Question","name":"How does the optimization work in the second approach?","acceptedAnswer":{"@type":"Answer","text":"Since every position matches exactly one of the two patterns, count0 + count1 = n. We only need to count for one pattern and derive the other as n - count0."}}]}).replace(/</g,'\u003c')}
</script>