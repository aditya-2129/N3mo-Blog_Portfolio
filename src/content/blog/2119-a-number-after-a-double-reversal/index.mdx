---
title: "A Number After a Double Reversal"
summary: "Check if reversing a number twice returns the original number. Leading zeros are dropped during reversal."
date: "Jun 29, 2024"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.20% | **Paid:** No
> **Topics:** Math

Reversing an integer means to reverse all its digits.

For example, reversing 2021 gives 1202. Reversing 12300 gives 321 as the leading zeros are not retained.

Given an integer num, reverse num to get reversed1, then reverse reversed1 to get reversed2. Return true if reversed2 equals num. Otherwise return false.

- Examples
- Constraints
- [Direct Simulation Approach](#direct-simulation-approach)
- [Mathematical Observation Approach](#mathematical-observation-approach)

## Examples

```text
Input: num = 526
Output: true
Explanation: Reverse num to get 625, then reverse 625 to get 526, which equals num.
```

```text
Input: num = 1800
Output: false
Explanation: Reverse num to get 81, then reverse 81 to get 18, which does not equal num.
```

```text
Input: num = 0
Output: true
Explanation: Reverse num to get 0, then reverse 0 to get 0, which equals num.
```

## Constraints

```text
0 <= num <= 10⁶
```

---
## Direct Simulation Approach

**Intuition**
Simulate the reversal process twice and compare the final result with the original number.

**Steps**
- Create a helper function to reverse a number by extracting digits from right to left
- Reverse the original number once to get reversed1
- Reverse reversed1 to get reversed2
- Return true if reversed2 equals the original number, false otherwise

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def isSameAfterReversals(self, num: int) -> bool:
        def reverse(n):
            rev = 0
            while n &gt; 0:
                rev = rev * 10 + n % 10
                n //= 10
            return rev
        
        reversed_once = reverse(num)
        reversed_twice = reverse(reversed_once)
        return reversed_twice == num
` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean isSameAfterReversals(int num) {
        int reversedOnce = reverse(num);
        int reversedTwice = reverse(reversedOnce);
        return reversedTwice == num;
    }
    
    private int reverse(int n) {
        int rev = 0;
        while (n &gt; 0) {
            rev = rev * 10 + n % 10;
            n /= 10;
        }
        return rev;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool isSameAfterReversals(int num) {
        int reversedOnce = reverse(num);
        int reversedTwice = reverse(reversedOnce);
        return reversedTwice == num;
    }
    
private:
    int reverse(int n) {
        int rev = 0;
        while (n &gt; 0) {
            rev = rev * 10 + n % 10;
            n /= 10;
        }
        return rev;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    isSameAfterReversals(num) {
        const reversedOnce = this.reverse(num);
        const reversedTwice = this.reverse(reversedOnce);
        return reversedTwice === num;
    }
    
    reverse(n) {
        let rev = 0;
        while (n &gt; 0) {
            rev = rev * 10 + n % 10;
            n = Math.floor(n / 10);
        }
        return rev;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    isSameAfterReversals(num: number): boolean {
        const reversedOnce = this.reverse(num);
        const reversedTwice = this.reverse(reversedOnce);
        return reversedTwice === num;
    }
    
    private reverse(n: number): number {
        let rev = 0;
        while (n &gt; 0) {
            rev = rev * 10 + n % 10;
            n = Math.floor(n / 10);
        }
        return rev;
    }
}
` },]} />

**Complexity**
- Time: O(log n) - number of digits in num
- Space: O(1) - only using constant extra space
- Notes: Straightforward implementation but does unnecessary work

---
## Mathematical Observation Approach

**Intuition**
A number remains unchanged after double reversal only if it is 0 or doesn't end with 0, since trailing zeros become leading zeros and are dropped.

**Steps**
- Check if the number is 0 (special case that always returns true)
- Check if the last digit is not 0 using modulo operation
- Return true if either condition is met

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def isSameAfterReversals(self, num: int) -> bool:
        # A number is the same after double reversal if:
        # 1. It is 0, or
        # 2. It doesn't end with 0
        return num == 0 or num % 10 != 0
` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean isSameAfterReversals(int num) {
        // A number is the same after double reversal if:
        // 1. It is 0, or
        // 2. It doesn't end with 0
        return num == 0 || num % 10 != 0;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool isSameAfterReversals(int num) {
        // A number is the same after double reversal if:
        // 1. It is 0, or
        // 2. It doesn't end with 0
        return num == 0 || num % 10 != 0;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    isSameAfterReversals(num) {
        // A number is the same after double reversal if:
        // 1. It is 0, or
        // 2. It doesn't end with 0
        return num === 0 || num % 10 !== 0;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    isSameAfterReversals(num: number): boolean {
        // A number is the same after double reversal if:
        // 1. It is 0, or
        // 2. It doesn't end with 0
        return num === 0 || num % 10 !== 0;
    }
}
` },]} />

**Complexity**
- Time: O(1) - constant time operations
- Space: O(1) - no extra space used
- Notes: Optimal solution with minimal computation

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode A Number After a Double Reversal Solutions","datePublished":"2024-06-29","description":"Check if reversing a number twice returns the original number. Leading zeros are dropped during reversal.","keywords":"LeetCode, A Number After a Double Reversal, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Direct Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(log n) where n is the input number, as we process each digit once per reversal."}},{"@type":"Question","name":"What is the time complexity of the Mathematical Observation approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) constant time, as we only perform a modulo operation and comparison."}},{"@type":"Question","name":"Why do numbers ending with 0 fail the double reversal test?","acceptedAnswer":{"@type":"Answer","text":"When reversing a number ending with 0, the trailing zeros become leading zeros which are dropped, so the second reversal cannot restore the original number."}},{"@type":"Question","name":"What is the space complexity of both approaches?","acceptedAnswer":{"@type":"Answer","text":"Both approaches use O(1) extra space, only storing a few integer variables."}}]}).replace(/</g,'\u003c')}
</script>