---
title: "Second Largest Digit in a String"
summary: "Find the second largest unique digit in a string containing alphanumeric characters."
date: "Apr 27, 2025"
tags:
  - Hash Table
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 54.30% | **Paid:** No
> **Topics:** Hash Table, String

Given an alphanumeric string s, return the second largest numerical digit that appears in s, or -1 if it does not exist.

An alphanumeric string is a string consisting of lowercase English letters and uppercase English letters and digits.

- Examples
- Constraints
- [Set + Sort Approach](#set--sort-approach)
- [Two Pass Approach](#two-pass-approach)
- [Single Pass Approach](#single-pass-approach)

## Examples

```text
Input: s = "dfa12321afd"
Output: 2
Explanation: The digits are [1, 2, 3]. The second largest digit is 2.
```

```text
Input: s = "abc1111"
Output: -1
Explanation: The digits are [1]. There is no second largest digit.
```

## Constraints

```text
1 <= s.length <= 500
s consists of lowercase English letters and/or digits.
```

## Set + Sort Approach

**Intuition**
Extract all unique digits using a set, sort them, and return the second largest.

**Steps**
- Iterate through the string and collect all digits in a set
- If the set has fewer than 2 elements, return -1
- Sort the set and return the second largest element

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def secondHighest(self, s: str) -> int:
        digits = set()
        for c in s:
            if c.isdigit():
                digits.add(int(c))
        if len(digits) &lt; 2:
            return -1
        return sorted(digits)[-2]
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int secondHighest(String s) {
        Set<Integer> digits = new HashSet&lt;&gt;();
        for (char c : s.toCharArray()) {
            if (Character.isDigit(c)) {
                digits.add(c - '0');
            }
        }
        if (digits.size() &lt; 2) {
            return -1;
        }
        List<Integer> sorted = new ArrayList&lt;&gt;(digits);
        Collections.sort(sorted);
        return sorted.get(sorted.size() - 2);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int secondHighest(string s) {
        set<int> digits;
        for (char c : s) {
            if (isdigit(c)) {
                digits.insert(c - '0');
            }
        }
        if (digits.size() &lt; 2) {
            return -1;
        }
        auto it = digits.end();
        it--;
        it--;
        return *it;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number}
 */
var secondHighest = function(s) {
    const digits = new Set();
    for (const c of s) {
        if (c &gt;= '0' && c &lt;= '9') {
            digits.add(parseInt(c));
        }
    }
    if (digits.size &lt; 2) {
        return -1;
    }
    const sorted = Array.from(digits).sort((a, b) =&gt; a - b);
    return sorted[sorted.length - 2];
};
` },
{ name: "TypeScript", lang: "typescript", code: `function secondHighest(s: string): number {
    const digits = new Set<number>();
    for (const c of s) {
        if (c &gt;= '0' && c &lt;= '9') {
            digits.add(parseInt(c));
        }
    }
    if (digits.size &lt; 2) {
        return -1;
    }
    const sorted = Array.from(digits).sort((a, b) =&gt; a - b);
    return sorted[sorted.length - 2];
}
` },]} />

**Complexity**
- Time: O(n log n) where n is the length of the string (due to sorting)
- Space: O(1) since there are at most 10 unique digits
- Notes: Simple but sorting is unnecessary for this problem

## Two Pass Approach

**Intuition**
First pass finds the maximum digit, second pass finds the largest digit less than the maximum.

**Steps**
- First pass: find the maximum digit in the string
- Second pass: find the largest digit that is strictly less than the maximum
- Return the result, or -1 if no such digit exists

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def secondHighest(self, s: str) -> int:
        max_digit = -1
        for c in s:
            if c.isdigit():
                max_digit = max(max_digit, int(c))
        
        second_max = -1
        for c in s:
            if c.isdigit():
                d = int(c)
                if d &lt; max_digit:
                    second_max = max(second_max, d)
        
        return second_max
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int secondHighest(String s) {
        int maxDigit = -1;
        for (char c : s.toCharArray()) {
            if (Character.isDigit(c)) {
                maxDigit = Math.max(maxDigit, c - '0');
            }
        }
        
        int secondMax = -1;
        for (char c : s.toCharArray()) {
            if (Character.isDigit(c)) {
                int d = c - '0';
                if (d &lt; maxDigit) {
                    secondMax = Math.max(secondMax, d);
                }
            }
        }
        
        return secondMax;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int secondHighest(string s) {
        int maxDigit = -1;
        for (char c : s) {
            if (isdigit(c)) {
                maxDigit = max(maxDigit, c - '0');
            }
        }
        
        int secondMax = -1;
        for (char c : s) {
            if (isdigit(c)) {
                int d = c - '0';
                if (d &lt; maxDigit) {
                    secondMax = max(secondMax, d);
                }
            }
        }
        
        return secondMax;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number}
 */
var secondHighest = function(s) {
    let maxDigit = -1;
    for (const c of s) {
        if (c &gt;= '0' && c &lt;= '9') {
            maxDigit = Math.max(maxDigit, parseInt(c));
        }
    }
    
    let secondMax = -1;
    for (const c of s) {
        if (c &gt;= '0' && c &lt;= '9') {
            const d = parseInt(c);
            if (d &lt; maxDigit) {
                secondMax = Math.max(secondMax, d);
            }
        }
    }
    
    return secondMax;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function secondHighest(s: string): number {
    let maxDigit = -1;
    for (const c of s) {
        if (c &gt;= '0' && c &lt;= '9') {
            maxDigit = Math.max(maxDigit, parseInt(c));
        }
    }
    
    let secondMax = -1;
    for (const c of s) {
        if (c &gt;= '0' && c &lt;= '9') {
            const d = parseInt(c);
            if (d &lt; maxDigit) {
                secondMax = Math.max(secondMax, d);
            }
        }
    }
    
    return secondMax;
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1)
- Notes: Two passes through the string but no extra space needed

## Single Pass Approach

**Intuition**
Track both the largest and second largest digits in a single pass through the string.

**Steps**
- Initialize first and second to -1
- For each digit in the string:
  - If it's greater than first, update second to first and first to the digit
  - If it's between first and second, update second
- Return second

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def secondHighest(self, s: str) -> int:
        first = -1
        second = -1
        for c in s:
            if c.isdigit():
                d = int(c)
                if d &gt; first:
                    second = first
                    first = d
                elif d &lt; first and d &gt; second:
                    second = d
        return second
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int secondHighest(String s) {
        int first = -1;
        int second = -1;
        for (char c : s.toCharArray()) {
            if (Character.isDigit(c)) {
                int d = c - '0';
                if (d &gt; first) {
                    second = first;
                    first = d;
                } else if (d &lt; first && d &gt; second) {
                    second = d;
                }
            }
        }
        return second;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int secondHighest(string s) {
        int first = -1;
        int second = -1;
        for (char c : s) {
            if (isdigit(c)) {
                int d = c - '0';
                if (d &gt; first) {
                    second = first;
                    first = d;
                } else if (d &lt; first && d &gt; second) {
                    second = d;
                }
            }
        }
        return second;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number}
 */
var secondHighest = function(s) {
    let first = -1;
    let second = -1;
    for (const c of s) {
        if (c &gt;= '0' && c &lt;= '9') {
            const d = parseInt(c);
            if (d &gt; first) {
                second = first;
                first = d;
            } else if (d &lt; first && d &gt; second) {
                second = d;
            }
        }
    }
    return second;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function secondHighest(s: string): number {
    let first = -1;
    let second = -1;
    for (const c of s) {
        if (c &gt;= '0' && c &lt;= '9') {
            const d = parseInt(c);
            if (d &gt; first) {
                second = first;
                first = d;
            } else if (d &lt; first && d &gt; second) {
                second = d;
            }
        }
    }
    return second;
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1)
- Notes: Most efficient approach with single pass and constant space

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Second Largest Digit in a String Solutions","datePublished":"2025-04-27","description":"Find the second largest unique digit in a string containing alphanumeric characters.","keywords":"LeetCode, Second Largest Digit in a String, Easy, Hash Table, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Set + Sort approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n log n) where n is the length of the string, due to the sorting step."}},{"@type":"Question","name":"What is the time complexity of the Two Pass approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we make two passes through the string."}},{"@type":"Question","name":"What is the time complexity of the Single Pass approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we make only one pass through the string."}},{"@type":"Question","name":"What is the space complexity of all approaches?","acceptedAnswer":{"@type":"Answer","text":"All approaches have O(1) space complexity since there are at most 10 unique digits (0-9)."}}]}).replace(/</g,'\u003c')}
</script>