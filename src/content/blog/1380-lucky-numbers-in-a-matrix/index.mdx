---
title: "Lucky Numbers in a Matrix"
summary: "Find all lucky numbers in a matrix where a number is the minimum in its row and maximum in its column."
date: "Nov 02, 2025"
tags:
  - Array
  - Matrix
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 80.00% | **Paid:** No
> **Topics:** Array, Matrix

Given an m x n matrix of distinct numbers, return all lucky numbers in the matrix in any order.

A lucky number is an element of the matrix such that it is the minimum element in its row and maximum in its column.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Pre-computation](#pre-computation)

## Examples

### Example 1
```text
Input: matrix = [[3,7,8],[9,11,13],[15,16,17]]
Output: [15]
Explanation: 15 is the only lucky number since it is the minimum in its row and the maximum in its column.
```

### Example 2
```text
Input: matrix = [[1,10,4,2],[9,3,8,7],[15,16,17,12]]
Output: [12]
Explanation: 12 is the only lucky number since it is the minimum in its row and the maximum in its column.
```

### Example 3
```text
Input: matrix = [[7,8],[1,2]]
Output: [7]
Explanation: 7 is the only lucky number since it is the minimum in its row and the maximum in its column.
```

## Constraints

```text
m == matrix.length
n == matrix[i].length
1 <= m, n <= 50
1 <= matrix[i][j] <= 10⁵
All the elements in the matrix are distinct.
```

---
## Brute Force

**Intuition**
For every element in the matrix, we can explicitly check if it is the minimum element in its row and the maximum element in its column.

**Steps**
- Iterate through every element in the matrix using nested loops.
- For the current element `matrix[i][j]`, iterate through row `i` to verify it is the minimum value.
- Iterate through column `j` to verify it is the maximum value.
- If both conditions are met, add the element to the result list.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def luckyNumbers(self, matrix: list[list[int]]) -&gt; list[int]:\n        m, n = len(matrix), len(matrix[0])\n        lucky_nums = []\n        \n        for i in range(m):\n            for j in range(n):\n                val = matrix[i][j]\n                is_row_min = True\n                is_col_max = True\n                \n                # Check if val is the minimum in row i\n                for k in range(n):\n                    if matrix[i][k] &lt; val:\n                        is_row_min = False\n                        break\n                \n                if not is_row_min:\n                    continue\n                    \n                # Check if val is the maximum in column j\n                for k in range(m):\n                    if matrix[k][j] &gt; val:\n                        is_col_max = False\n                        break\n                \n                if is_col_max:\n                    lucky_nums.append(val)\n                    \n        return lucky_nums" },
{ name: "Java", lang: "java", code: "class Solution {\n    public List&lt;Integer&gt; luckyNumbers(int[][] matrix) {\n        int m = matrix.length;\n        int n = matrix[0].length;\n        List&lt;Integer&gt; luckyNums = new ArrayList&lt;&gt;();\n        \n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                int val = matrix[i][j];\n                boolean isRowMin = true;\n                boolean isColMax = true;\n                \n                // Check if val is the minimum in row i\n                for (int k = 0; k &lt; n; k++) {\n                    if (matrix[i][k] &lt; val) {\n                        isRowMin = false;\n                        break;\n                    }\n                }\n                \n                if (!isRowMin) continue;\n                \n                // Check if val is the maximum in column j\n                for (int k = 0; k &lt; m; k++) {\n                    if (matrix[k][j] &gt; val) {\n                        isColMax = false;\n                        break;\n                    }\n                }\n                \n                if (isColMax) {\n                    luckyNums.add(val);\n                }\n            }\n        }\n        return luckyNums;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; luckyNumbers (vector&lt;vector&lt;int&gt;&gt;& matrix) {\n        int m = matrix.size();\n        int n = matrix[0].size();\n        vector&lt;int&gt; luckyNums;\n        \n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                int val = matrix[i][j];\n                bool isRowMin = true;\n                bool isColMax = true;\n                \n                // Check if val is the minimum in row i\n                for (int k = 0; k &lt; n; k++) {\n                    if (matrix[i][k] &lt; val) {\n                        isRowMin = false;\n                        break;\n                    }\n                }\n                \n                if (!isRowMin) continue;\n                \n                // Check if val is the maximum in column j\n                for (int k = 0; k &lt; m; k++) {\n                    if (matrix[k][j] &gt; val) {\n                        isColMax = false;\n                        break;\n                    }\n                }\n                \n                if (isColMax) {\n                    luckyNums.push_back(val);\n                }\n            }\n        }\n        return luckyNums;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var luckyNumbers = function(matrix) {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    const luckyNums = [];\n    \n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            const val = matrix[i][j];\n            let isRowMin = true;\n            let isColMax = true;\n            \n            // Check if val is the minimum in row i\n            for (let k = 0; k &lt; n; k++) {\n                if (matrix[i][k] &lt; val) {\n                    isRowMin = false;\n                    break;\n                }\n            }\n            \n            if (!isRowMin) continue;\n            \n            // Check if val is the maximum in column j\n            for (let k = 0; k &lt; m; k++) {\n                if (matrix[k][j] &gt; val) {\n                    isColMax = false;\n                    break;\n                }\n            }\n            \n            if (isColMax) {\n                luckyNums.push(val);\n            }\n        }\n    }\n    return luckyNums;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function luckyNumbers(matrix: number[][]): number[] {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    const luckyNums: number[] = [];\n    \n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            const val = matrix[i][j];\n            let isRowMin = true;\n            let isColMax = true;\n            \n            // Check if val is the minimum in row i\n            for (let k = 0; k &lt; n; k++) {\n                if (matrix[i][k] &lt; val) {\n                    isRowMin = false;\n                    break;\n                }\n            }\n            \n            if (!isRowMin) continue;\n            \n            // Check if val is the maximum in column j\n            for (let k = 0; k &lt; m; k++) {\n                if (matrix[k][j] &gt; val) {\n                    isColMax = false;\n                    break;\n                }\n            }\n            \n            if (isColMax) {\n                luckyNums.push(val);\n            }\n        }\n    }\n    return luckyNums;\n};" },]} />

**Complexity**
- Time: O(m * n * (m + n))
- Space: O(1)
- Notes: We scan the row and column for every single element, leading to repeated work.

---
## Pre-computation

**Intuition**
We can optimize by pre-calculating the minimum value for each row and the maximum value for each column in a single pass. Then, we simply check if an element matches both pre-computed values.

**Steps**
- Create an array `row_mins` of size `m` initialized to infinity, and an array `col_maxs` of size `n` initialized to negative infinity.
- Iterate through the matrix once. For each element `matrix[i][j]`, update `row_mins[i]` and `col_maxs[j]`.
- Iterate through the matrix again. If `matrix[i][j]` equals `row_mins[i]` and `col_maxs[j]`, add it to the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def luckyNumbers(self, matrix: list[list[int]]) -&gt; list[int]:\n        m, n = len(matrix), len(matrix[0])\n        row_mins = [float('inf')] * m\n        col_maxs = [float('-inf')] * n\n        \n        # Precompute row minimums and column maximums\n        for i in range(m):\n            for j in range(n):\n                row_mins[i] = min(row_mins[i], matrix[i][j])\n                col_maxs[j] = max(col_maxs[j], matrix[i][j])\n        \n        # Find lucky numbers\n        lucky_nums = []\n        for i in range(m):\n            for j in range(n):\n                if matrix[i][j] == row_mins[i] and matrix[i][j] == col_maxs[j]:\n                    lucky_nums.append(matrix[i][j])\n                    \n        return lucky_nums" },
{ name: "Java", lang: "java", code: "import java.util.ArrayList;\nimport java.util.List;\n\nclass Solution {\n    public List&lt;Integer&gt; luckyNumbers(int[][] matrix) {\n        int m = matrix.length;\n        int n = matrix[0].length;\n        int[] rowMins = new int[m];\n        int[] colMaxs = new int[n];\n        \n        // Initialize arrays\n        for (int i = 0; i &lt; m; i++) rowMins[i] = Integer.MAX_VALUE;\n        for (int j = 0; j &lt; n; j++) colMaxs[j] = Integer.MIN_VALUE;\n        \n        // Precompute row minimums and column maximums\n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                rowMins[i] = Math.min(rowMins[i], matrix[i][j]);\n                colMaxs[j] = Math.max(colMaxs[j], matrix[i][j]);\n            }\n        }\n        \n        // Find lucky numbers\n        List&lt;Integer&gt; luckyNums = new ArrayList&lt;&gt;();\n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (matrix[i][j] == rowMins[i] && matrix[i][j] == colMaxs[j]) {\n                    luckyNums.add(matrix[i][j]);\n                }\n            }\n        }\n        return luckyNums;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;climits&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; luckyNumbers (vector&lt;vector&lt;int&gt;&gt;& matrix) {\n        int m = matrix.size();\n        int n = matrix[0].size();\n        vector&lt;int&gt; rowMins(m, INT_MAX);\n        vector&lt;int&gt; colMaxs(n, INT_MIN);\n        \n        // Precompute row minimums and column maximums\n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                rowMins[i] = min(rowMins[i], matrix[i][j]);\n                colMaxs[j] = max(colMaxs[j], matrix[i][j]);\n            }\n        }\n        \n        // Find lucky numbers\n        vector&lt;int&gt; luckyNums;\n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (matrix[i][j] == rowMins[i] && matrix[i][j] == colMaxs[j]) {\n                    luckyNums.push_back(matrix[i][j]);\n                }\n            }\n        }\n        return luckyNums;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var luckyNumbers = function(matrix) {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    const rowMins = new Array(m).fill(Infinity);\n    const colMaxs = new Array(n).fill(-Infinity);\n    \n    // Precompute row minimums and column maximums\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            rowMins[i] = Math.min(rowMins[i], matrix[i][j]);\n            colMaxs[j] = Math.max(colMaxs[j], matrix[i][j]);\n        }\n    }\n    \n    // Find lucky numbers\n    const luckyNums = [];\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (matrix[i][j] === rowMins[i] && matrix[i][j] === colMaxs[j]) {\n                luckyNums.push(matrix[i][j]);\n            }\n        }\n    }\n    return luckyNums;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function luckyNumbers(matrix: number[][]): number[] {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    const rowMins: number[] = new Array(m).fill(Infinity);\n    const colMaxs: number[] = new Array(n).fill(-Infinity);\n    \n    // Precompute row minimums and column maximums\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            rowMins[i] = Math.min(rowMins[i], matrix[i][j]);\n            colMaxs[j] = Math.max(colMaxs[j], matrix[i][j]);\n        }\n    }\n    \n    // Find lucky numbers\n    const luckyNums: number[] = [];\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (matrix[i][j] === rowMins[i] && matrix[i][j] === colMaxs[j]) {\n                luckyNums.push(matrix[i][j]);\n            }\n        }\n    }\n    return luckyNums;\n};" },]} />

**Complexity**
- Time: O(m * n)
- Space: O(m + n)
- Notes: We trade extra space for row and column storage to achieve linear time complexity.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Lucky Numbers in a Matrix Solutions","datePublished":"2025-11-02","description":"Find all lucky numbers in a matrix where a number is the minimum in its row and maximum in its column.","keywords":"LeetCode, Lucky Numbers in a Matrix, Easy, Array, Matrix"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"O(m * n * (m + n)) where m is the number of rows and n is the number of columns."}},{"@type":"Question","name":"What is the time complexity of the Pre-computation approach?","acceptedAnswer":{"@type":"Answer","text":"O(m * n) where m is the number of rows and n is the number of columns."}},{"@type":"Question","name":"What is the space complexity of the Pre-computation approach?","acceptedAnswer":{"@type":"Answer","text":"O(m + n) to store the minimums of rows and maximums of columns."}}]}).replace(/</g,'\u003c')}
</script>