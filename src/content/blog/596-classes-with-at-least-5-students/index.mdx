---
title: "Classes With at Least 5 Students"
summary: "Find all classes that have at least 5 students enrolled."
date: "Feb 17, 2025"
tags:
  - Database
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 63.70% | **Paid:** No
> **Topics:** Database

Table: Courses
+-------------+---------+
| Column Name | Type    |
+-------------+---------+
| student     | varchar |
| class       | varchar |
+-------------+---------+
(student, class) is the primary key (combination of columns with unique values) of this table.
Each row of this table indicates the name of a student and the class in which they are enrolled.

Write a solution to find all the classes that have at least 5 students.

Return the result table in any order.

The result format is in the following example.

- Examples
- Constraints
- [Hash Map Counting](#hash-map-counting)
- [Sorting and Linear Scan](#sorting-and-linear-scan)
- [Stream / Functional](#stream--functional)

## Examples

Input: 
Courses table:
+---------+----------+
| student | class    |
+---------+----------+
| A       | Math     |
| B       | Math     |
| C       | Math     |
| D       | Math     |
| E       | Math     |
| F       | Math     |
| G       | Physics  |
| H       | Physics  |
| I       | Physics  |
+---------+----------+

Output: 
+---------+
| class   |
+---------+
| Math    |
+---------+

Explanation: 
- Math has 6 students, so we include it.
- Physics has 3 students, so we do not include it.

## Constraints

```text
- The Courses table may have up to 1000 rows.
```

---
## Hash Map Counting

**Intuition**
Use a hash map to count the number of students in each class, then filter classes with at least 5 students.

**Steps**
- Iterate through each course enrollment
- Increment the count for each class in a hash map
- Filter classes with count &gt;= 5

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List
from collections import defaultdict

class Solution:
    def findClasses(self, courses: List[List[str]]) -> List[str]:
        count = defaultdict(int)
        for student, course in courses:
            count[course] += 1
        return [course for course, cnt in count.items() if cnt &gt;= 5]` },
{ name: "Java", lang: "java", code: `import java.util.*;

class Solution {
    public List<String> findClasses(List<List<String>&gt; courses) {
        Map<String, Integer> count = new HashMap&lt;&gt;();
        for (List<String> course : courses) {
            String className = course.get(1);
            count.put(className, count.getOrDefault(className, 0) + 1);
        }
        List<String> result = new ArrayList&lt;&gt;();
        for (Map.Entry<String, Integer> entry : count.entrySet()) {
            if (entry.getValue() &gt;= 5) {
                result.add(entry.getKey());
            }
        }
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <string>
#include <unordered_map>

using namespace std;

class Solution {
public:
    vector<string> findClasses(vector<vector<string>&gt;& courses) {
        unordered_map<string, int> count;
        for (auto& course : courses) {
            count[course[1]]++;
        }
        vector<string> result;
        for (auto& entry : count) {
            if (entry.second &gt;= 5) {
                result.push_back(entry.first);
            }
        }
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string[][]} courses
 * @return {string[]}
 */
var findClasses = function(courses) {
    const count = new Map();
    for (const [student, className] of courses) {
        count.set(className, (count.get(className) || 0) + 1);
    }
    const result = [];
    for (const [className, cnt] of count) {
        if (cnt &gt;= 5) {
            result.push(className);
        }
    }
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function findClasses(courses: string[][]): string[] {
    const count = new Map<string, number>();
    for (const [student, className] of courses) {
        count.set(className, (count.get(className) || 0) + 1);
    }
    const result: string[] = [];
    for (const [className, cnt] of count) {
        if (cnt &gt;= 5) {
            result.push(className);
        }
    }
    return result;
}` },]} />

**Complexity**
- Time: O(n) where n is the number of enrollments
- Space: O(k) where k is the number of unique classes
- Notes: Most efficient approach for this problem

---
## Sorting and Linear Scan

**Intuition**
Sort the enrollments by class name, then count students for each class in a single pass.

**Steps**
- Sort courses by class name
- Iterate through sorted list, counting consecutive entries with the same class
- Add classes with count &gt;= 5 to result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def findClasses(self, courses: List[List[str]]) -> List[str]:
        courses.sort(key=lambda x: x[1])
        result = []
        i = 0
        n = len(courses)
        while i &lt; n:
            current_class = courses[i][1]
            cnt = 0
            while i &lt; n and courses[i][1] == current_class:
                cnt += 1
                i += 1
            if cnt &gt;= 5:
                result.append(current_class)
        return result` },
{ name: "Java", lang: "java", code: `import java.util.*;

class Solution {
    public List<String> findClasses(List<List<String>&gt; courses) {
        courses.sort((a, b) -> a.get(1).compareTo(b.get(1)));
        List<String> result = new ArrayList&lt;&gt;();
        int i = 0;
        int n = courses.size();
        while (i &lt; n) {
            String currentClass = courses.get(i).get(1);
            int cnt = 0;
            while (i &lt; n && courses.get(i).get(1).equals(currentClass)) {
                cnt++;
                i++;
            }
            if (cnt &gt;= 5) {
                result.add(currentClass);
            }
        }
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <string>
#include <algorithm>

using namespace std;

class Solution {
public:
    vector<string> findClasses(vector<vector<string>&gt;& courses) {
        sort(courses.begin(), courses.end(), [](const vector<string>& a, const vector<string>& b) {
            return a[1] &lt; b[1];
        });
        vector<string> result;
        int i = 0;
        int n = courses.size();
        while (i &lt; n) {
            string currentClass = courses[i][1];
            int cnt = 0;
            while (i &lt; n && courses[i][1] == currentClass) {
                cnt++;
                i++;
            }
            if (cnt &gt;= 5) {
                result.push_back(currentClass);
            }
        }
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string[][]} courses
 * @return {string[]}
 */
var findClasses = function(courses) {
    courses.sort((a, b) =&gt; a[1].localeCompare(b[1]));
    const result = [];
    let i = 0;
    const n = courses.length;
    while (i &lt; n) {
        const currentClass = courses[i][1];
        let cnt = 0;
        while (i &lt; n && courses[i][1] === currentClass) {
            cnt++;
            i++;
        }
        if (cnt &gt;= 5) {
            result.push(currentClass);
        }
    }
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function findClasses(courses: string[][]): string[] {
    courses.sort((a, b) =&gt; a[1].localeCompare(b[1]));
    const result: string[] = [];
    let i = 0;
    const n = courses.length;
    while (i &lt; n) {
        const currentClass = courses[i][1];
        let cnt = 0;
        while (i &lt; n && courses[i][1] === currentClass) {
            cnt++;
            i++;
        }
        if (cnt &gt;= 5) {
            result.push(currentClass);
        }
    }
    return result;
}` },]} />

**Complexity**
- Time: O(n log n) due to sorting
- Space: O(1) extra space (or O(n) if sorting is not in-place)
- Notes: Useful when you need sorted output or when hash operations are expensive

---
## Stream / Functional

**Intuition**
Use functional programming constructs like streams, reduce, and filter to count and filter in a declarative style.

**Steps**
- Use stream/reduce to count students per class
- Filter classes with count &gt;= 5
- Collect results

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List
from collections import Counter

class Solution:
    def findClasses(self, courses: List[List[str]]) -> List[str]:
        classes = [course for _, course in courses]
        count = Counter(classes)
        return [course for course, cnt in count.items() if cnt &gt;= 5]` },
{ name: "Java", lang: "java", code: `import java.util.*;
import java.util.stream.Collectors;

class Solution {
    public List<String> findClasses(List<List<String>&gt; courses) {
        return courses.stream()
            .collect(Collectors.groupingBy(c -> c.get(1), Collectors.counting()))
            .entrySet().stream()
            .filter(e -> e.getValue() &gt;= 5)
            .map(Map.Entry::getKey)
            .collect(Collectors.toList());
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <string>
#include <unordered_map>
#include <numeric>

using namespace std;

class Solution {
public:
    vector<string> findClasses(vector<vector<string>&gt;& courses) {
        unordered_map<string, int> count = accumulate(
            courses.begin(), courses.end(),
            unordered_map<string, int>(),
            [](unordered_map<string, int> acc, const vector<string>& course) {
                acc[course[1]]++;
                return acc;
            }
        );
        
        vector<string> result;
        for (auto& entry : count) {
            if (entry.second &gt;= 5) {
                result.push_back(entry.first);
            }
        }
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string[][]} courses
 * @return {string[]}
 */
var findClasses = function(courses) {
    const count = courses.reduce((acc, [_, className]) =&gt; {
        acc[className] = (acc[className] || 0) + 1;
        return acc;
    }, {});
    
    return Object.entries(count)
        .filter(([_, cnt]) =&gt; cnt &gt;= 5)
        .map(([className, _]) =&gt; className);
};` },
{ name: "TypeScript", lang: "typescript", code: `function findClasses(courses: string[][]): string[] {
    const count = courses.reduce((acc: Record<string, number>, [_, className]) =&gt; {
        acc[className] = (acc[className] || 0) + 1;
        return acc;
    }, {});
    
    return Object.entries(count)
        .filter(([_, cnt]) =&gt; cnt &gt;= 5)
        .map(([className, _]) =&gt; className);
}` },]} />

**Complexity**
- Time: O(n) for counting and filtering
- Space: O(k) where k is the number of unique classes
- Notes: More readable and declarative, but may have slight overhead compared to imperative approach

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Classes With at Least 5 Students Solutions","datePublished":"2025-02-17","description":"Find all classes that have at least 5 students enrolled.","keywords":"LeetCode, Classes With at Least 5 Students, Easy, Database"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Hash Map Counting approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of enrollments."}},{"@type":"Question","name":"What is the space complexity of Hash Map Counting approach?","acceptedAnswer":{"@type":"Answer","text":"O(k) where k is the number of unique classes."}},{"@type":"Question","name":"What is the time complexity of Sorting and Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"O(n log n) due to sorting."}},{"@type":"Question","name":"What is the space complexity of Sorting and Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) extra space (or O(n) if sorting is not in-place)."}},{"@type":"Question","name":"What is the time complexity of Stream / Functional approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for counting and filtering."}},{"@type":"Question","name":"What is the space complexity of Stream / Functional approach?","acceptedAnswer":{"@type":"Answer","text":"O(k) where k is the number of unique classes."}}]}).replace(/</g,'\u003c')}
</script>