---
title: "Divide an Array Into Subarrays With Minimum Cost I"
summary: "Divide array into 3 subarrays where cost is first element of each subarray. Find minimum total cost."
date: "Oct 24, 2025"
tags:
  - Array
  - Sorting
  - Enumeration
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 80.60% | **Paid:** No
> **Topics:** Array, Sorting, Enumeration

You are given an integer array nums of length n.

You need to divide the array into exactly three non-empty subarrays.

The cost of a subarray is the first element of that subarray.

Return the minimum possible total cost of dividing the array into three subarrays.

- Examples
- Constraints
- [Single Pass](#single-pass)
- [Sorting](#sorting)
- [Brute Force](#brute-force)

## Examples

```text
Input: nums = [1,2,3,12]
Output: 6
Explanation: The best way to divide the array is [1], [2], [3,12] with a total cost of 1 + 2 + 3 = 6.
```

```text
Input: nums = [5,4,3,2,1]
Output: 8
Explanation: The best way to divide the array is [5,4,3], [2], [1] with a total cost of 5 + 2 + 1 = 8.
```

```text
Input: nums = [10,1,2,3,4,5]
Output: 13
Explanation: The best way to divide the array is [10], [1], [2,3,4,5] with a total cost of 10 + 1 + 2 = 13.
```

## Constraints

```text
3 <= nums.length <= 50
1 <= nums[i] <= 50
```

---
## Single Pass

**Intuition**
The first subarray always starts at index 0, so its cost is fixed at nums[0]. We need to choose two more starting positions for the second and third subarrays from indices 1 to n-1. To minimize total cost, we should pick the two smallest values from nums[1:].

**Steps**
- Initialize nums[0] as the first cost
- Track the two smallest values while iterating through nums[1:]
- Return the sum of nums[0] and the two smallest values

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumCost(self, nums: list[int]) -&gt; int:\n        first = nums[0]\n        min1 = float('inf')\n        min2 = float('inf')\n        for i in range(1, len(nums)):\n            if nums[i] &lt; min1:\n                min2 = min1\n                min1 = nums[i]\n            elif nums[i] &lt; min2:\n                min2 = nums[i]\n        return first + min1 + min2" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimumCost(int[] nums) {\n        int first = nums[0];\n        int min1 = Integer.MAX_VALUE;\n        int min2 = Integer.MAX_VALUE;\n        for (int i = 1; i &lt; nums.length; i++) {\n            if (nums[i] &lt; min1) {\n                min2 = min1;\n                min1 = nums[i];\n            } else if (nums[i] &lt; min2) {\n                min2 = nums[i];\n            }\n        }\n        return first + min1 + min2;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimumCost(vector&lt;int&gt;& nums) {\n        int first = nums[0];\n        int min1 = INT_MAX;\n        int min2 = INT_MAX;\n        for (int i = 1; i &lt; nums.size(); i++) {\n            if (nums[i] &lt; min1) {\n                min2 = min1;\n                min1 = nums[i];\n            } else if (nums[i] &lt; min2) {\n                min2 = nums[i];\n            }\n        }\n        return first + min1 + min2;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "function minimumCost(nums) {\n    const first = nums[0];\n    let min1 = Infinity;\n    let min2 = Infinity;\n    for (let i = 1; i &lt; nums.length; i++) {\n        if (nums[i] &lt; min1) {\n            min2 = min1;\n            min1 = nums[i];\n        } else if (nums[i] &lt; min2) {\n            min2 = nums[i];\n        }\n    }\n    return first + min1 + min2;\n}" },
{ name: "TypeScript", lang: "typescript", code: "function minimumCost(nums: number[]): number {\n    const first = nums[0];\n    let min1 = Infinity;\n    let min2 = Infinity;\n    for (let i = 1; i &lt; nums.length; i++) {\n        if (nums[i] &lt; min1) {\n            min2 = min1;\n            min1 = nums[i];\n        } else if (nums[i] &lt; min2) {\n            min2 = nums[i];\n        }\n    }\n    return first + min1 + min2;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal solution with single pass through the array

---
## Sorting

**Intuition**
Extract elements from index 1 onwards, sort them, and pick the two smallest values. The total cost is nums[0] plus these two smallest values.

**Steps**
- Create a copy of nums[1:]
- Sort the copy
- Return nums[0] + sorted_copy[0] + sorted_copy[1]

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumCost(self, nums: list[int]) -&gt; int:\n        rest = sorted(nums[1:])\n        return nums[0] + rest[0] + rest[1]" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int minimumCost(int[] nums) {\n        int[] rest = Arrays.copyOfRange(nums, 1, nums.length);\n        Arrays.sort(rest);\n        return nums[0] + rest[0] + rest[1];\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nclass Solution {\npublic:\n    int minimumCost(vector&lt;int&gt;& nums) {\n        vector&lt;int&gt; rest(nums.begin() + 1, nums.end());\n        sort(rest.begin(), rest.end());\n        return nums[0] + rest[0] + rest[1];\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "function minimumCost(nums) {\n    const rest = nums.slice(1).sort((a, b) =&gt; a - b);\n    return nums[0] + rest[0] + rest[1];\n}" },
{ name: "TypeScript", lang: "typescript", code: "function minimumCost(nums: number[]): number {\n    const rest = nums.slice(1).sort((a, b) =&gt; a - b);\n    return nums[0] + rest[0] + rest[1];\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(n)
- Notes: Simpler code but less efficient due to sorting overhead

---
## Brute Force

**Intuition**
Try all possible pairs of indices (i, j) where 1 &lt;= i &lt; j &lt; n, representing the start of the second and third subarrays. Calculate the cost for each pair and return the minimum.

**Steps**
- Initialize minimum cost to infinity
- Iterate through all valid pairs (i, j)
- Calculate cost as nums[0] + nums[i] + nums[j]
- Track and return the minimum cost

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumCost(self, nums: list[int]) -&gt; int:\n        n = len(nums)\n        min_cost = float('inf')\n        for i in range(1, n - 1):\n            for j in range(i + 1, n):\n                cost = nums[0] + nums[i] + nums[j]\n                min_cost = min(min_cost, cost)\n        return min_cost" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimumCost(int[] nums) {\n        int n = nums.length;\n        int minCost = Integer.MAX_VALUE;\n        for (int i = 1; i &lt; n - 1; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int cost = nums[0] + nums[i] + nums[j];\n                minCost = Math.min(minCost, cost);\n            }\n        }\n        return minCost;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimumCost(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        int minCost = INT_MAX;\n        for (int i = 1; i &lt; n - 1; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int cost = nums[0] + nums[i] + nums[j];\n                minCost = min(minCost, cost);\n            }\n        }\n        return minCost;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "function minimumCost(nums) {\n    const n = nums.length;\n    let minCost = Infinity;\n    for (let i = 1; i &lt; n - 1; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            const cost = nums[0] + nums[i] + nums[j];\n            minCost = Math.min(minCost, cost);\n        }\n    }\n    return minCost;\n}" },
{ name: "TypeScript", lang: "typescript", code: "function minimumCost(nums: number[]): number {\n    const n = nums.length;\n    let minCost = Infinity;\n    for (let i = 1; i &lt; n - 1; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            const cost = nums[0] + nums[i] + nums[j];\n            minCost = Math.min(minCost, cost);\n        }\n    }\n    return minCost;\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1)
- Notes: Simple but inefficient for larger arrays

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Divide an Array Into Subarrays With Minimum Cost I Solutions","datePublished":"2025-10-24","description":"Divide array into 3 subarrays where cost is first element of each subarray. Find minimum total cost.","keywords":"LeetCode, Divide an Array Into Subarrays With Minimum Cost I, Easy, Array, Sorting, Enumeration"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Single Pass approach?","acceptedAnswer":{"@type":"Answer","text":"The Single Pass approach has O(n) time complexity as we iterate through the array once."}},{"@type":"Question","name":"What is the time complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The Sorting approach has O(n log n) time complexity due to the sorting operation."}},{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach has O(n²) time complexity as we check all possible pairs of indices."}},{"@type":"Question","name":"Which approach is the most efficient?","acceptedAnswer":{"@type":"Answer","text":"The Single Pass approach is the most efficient with O(n) time and O(1) space complexity."}}]}).replace(/</g,'\u003c')}
</script>