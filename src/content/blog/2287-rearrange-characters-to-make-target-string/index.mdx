---
title: "Rearrange Characters to Make Target String"
summary: "Given two strings s and target, find the maximum number of copies of target that can be formed by rearranging characters from s."
date: "Nov 08, 2025"
tags:
  - Hash Table
  - String
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 61.20% | **Paid:** No
> **Topics:** Hash Table, String, Counting

You are given two 0-indexed strings s and target. You can take some letters from s and rearrange them to form new strings.

Return the maximum number of copies of target that can be formed by taking letters from s.

- Examples
- Constraints
- [Hash Map / Counter Approach](#hash-map--counter-approach)
- [Array Counting Approach](#array-counting-approach)

## Examples

**Example 1:**

```text
Input: s = "ilovecodingonleetcode", target = "code"
Output: 2
Explanation:
- "code" can be formed using characters from s twice.
- We can take the characters 'c', 'o', 'd', 'e' from s to form the first copy of "code".
- We can take the characters 'c', 'o', 'd', 'e' from s to form the second copy of "code".
- We cannot form a third copy of "code" because we only have one 'e' left.
```

**Example 2:**

```text
Input: s = "abcba", target = "abc"
Output: 1
Explanation:
- "abc" can be formed using characters from s once.
- We can take the characters 'a', 'b', 'c' from s to form the first copy of "abc".
- We cannot form a second copy of "abc" because we only have one 'a' left.
```

**Example 3:**

```text
Input: s = "abbaccaddaeea", target = "aaaaa"
Output: 1
Explanation:
- "aaaaa" can be formed using characters from s once.
- We can take the characters 'a', 'a', 'a', 'a', 'a' from s to form the first copy of "aaaaa".
- We cannot form a second copy of "aaaaa" because we only have two 'a's left.
```

## Constraints

```text
1 <= s.length <= 100
1 <= target.length <= 10
s and target consist of lowercase English letters.
```

---
## Hash Map / Counter Approach

**Intuition**
Count the frequency of each character in both strings, then find the limiting character that determines how many copies of target can be formed.

**Steps**
- Count the frequency of each character in string s using a hash map
- Count the frequency of each character in string target using a hash map
- For each character in target, calculate how many times we can form it using characters from s
- The answer is the minimum of these ratios across all characters in target

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def rearrangeCharacters(self, s: str, target: str) -&gt; int:\n        from collections import Counter\n        \n        s_count = Counter(s)\n        target_count = Counter(target)\n        \n        result = float('inf')\n        \n        for char in target_count:\n            if char not in s_count:\n                return 0\n            result = min(result, s_count[char] // target_count[char])\n        \n        return result" },
{ name: "Java", lang: "java", code: "import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public int rearrangeCharacters(String s, String target) {\n        Map&lt;Character, Integer&gt; sCount = new HashMap&lt;&gt;();\n        Map&lt;Character, Integer&gt; targetCount = new HashMap&lt;&gt;();\n        \n        for (char c : s.toCharArray()) {\n            sCount.put(c, sCount.getOrDefault(c, 0) + 1);\n        }\n        \n        for (char c : target.toCharArray()) {\n            targetCount.put(c, targetCount.getOrDefault(c, 0) + 1);\n        }\n        \n        int result = Integer.MAX_VALUE;\n        \n        for (char c : targetCount.keySet()) {\n            if (!sCount.containsKey(c)) {\n                return 0;\n            }\n            result = Math.min(result, sCount.get(c) / targetCount.get(c));\n        }\n        \n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;unordered_map&gt;\n#include &lt;string&gt;\n#include &lt;climits&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int rearrangeCharacters(string s, string target) {\n        unordered_map&lt;char, int&gt; sCount;\n        unordered_map&lt;char, int&gt; targetCount;\n        \n        for (char c : s) {\n            sCount[c]++;\n        }\n        \n        for (char c : target) {\n            targetCount[c]++;\n        }\n        \n        int result = INT_MAX;\n        \n        for (auto& [c, count] : targetCount) {\n            if (sCount.find(c) == sCount.end()) {\n                return 0;\n            }\n            result = min(result, sCount[c] / count);\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @param {string} target\n * @return {number}\n */\nvar rearrangeCharacters = function(s, target) {\n    const sCount = new Map();\n    const targetCount = new Map();\n    \n    for (const c of s) {\n        sCount.set(c, (sCount.get(c) || 0) + 1);\n    }\n    \n    for (const c of target) {\n        targetCount.set(c, (targetCount.get(c) || 0) + 1);\n    }\n    \n    let result = Infinity;\n    \n    for (const [c, count] of targetCount) {\n        if (!sCount.has(c)) {\n            return 0;\n        }\n        result = Math.min(result, Math.floor(sCount.get(c) / count));\n    }\n    \n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function rearrangeCharacters(s: string, target: string): number {\n    const sCount = new Map&lt;string, number&gt;();\n    const targetCount = new Map&lt;string, number&gt;();\n    \n    for (const c of s) {\n        sCount.set(c, (sCount.get(c) || 0) + 1);\n    }\n    \n    for (const c of target) {\n        targetCount.set(c, (targetCount.get(c) || 0) + 1);\n    }\n    \n    let result = Infinity;\n    \n    for (const [c, count] of targetCount) {\n        if (!sCount.has(c)) {\n            return 0;\n        }\n        result = Math.min(result, Math.floor(sCount.get(c) / count));\n    }\n    \n    return result;\n}" },]} />

**Complexity**
- Time: O(n + m) where n is the length of s and m is the length of target
- Space: O(k) where k is the number of unique characters (at most 26 for lowercase letters)
- Notes: Hash map approach is flexible and works for any character set, but has slightly higher overhead than array-based approach.

---
## Array Counting Approach

**Intuition**
Since we only deal with lowercase English letters, we can use fixed-size arrays of length 26 instead of hash maps for better performance.

**Steps**
- Create two arrays of size 26 to count character frequencies
- Count characters in s by mapping each character to index 0-25
- Count characters in target using the same mapping
- Find the minimum ratio of available to required characters for each character in target

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def rearrangeCharacters(self, s: str, target: str) -&gt; int:\n        s_count = [0] * 26\n        target_count = [0] * 26\n        \n        for c in s:\n            s_count[ord(c) - ord('a')] += 1\n        \n        for c in target:\n            target_count[ord(c) - ord('a')] += 1\n        \n        result = float('inf')\n        \n        for i in range(26):\n            if target_count[i] &gt; 0:\n                if s_count[i] == 0:\n                    return 0\n                result = min(result, s_count[i] // target_count[i])\n        \n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int rearrangeCharacters(String s, String target) {\n        int[] sCount = new int[26];\n        int[] targetCount = new int[26];\n        \n        for (char c : s.toCharArray()) {\n            sCount[c - 'a']++;\n        }\n        \n        for (char c : target.toCharArray()) {\n            targetCount[c - 'a']++;\n        }\n        \n        int result = Integer.MAX_VALUE;\n        \n        for (int i = 0; i &lt; 26; i++) {\n            if (targetCount[i] &gt; 0) {\n                if (sCount[i] == 0) {\n                    return 0;\n                }\n                result = Math.min(result, sCount[i] / targetCount[i]);\n            }\n        }\n        \n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\n#include &lt;climits&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int rearrangeCharacters(string s, string target) {\n        int sCount[26] = {0};\n        int targetCount[26] = {0};\n        \n        for (char c : s) {\n            sCount[c - 'a']++;\n        }\n        \n        for (char c : target) {\n            targetCount[c - 'a']++;\n        }\n        \n        int result = INT_MAX;\n        \n        for (int i = 0; i &lt; 26; i++) {\n            if (targetCount[i] &gt; 0) {\n                if (sCount[i] == 0) {\n                    return 0;\n                }\n                result = min(result, sCount[i] / targetCount[i]);\n            }\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @param {string} target\n * @return {number}\n */\nvar rearrangeCharacters = function(s, target) {\n    const sCount = new Array(26).fill(0);\n    const targetCount = new Array(26).fill(0);\n    \n    for (const c of s) {\n        sCount[c.charCodeAt(0) - 'a'.charCodeAt(0)]++;\n    }\n    \n    for (const c of target) {\n        targetCount[c.charCodeAt(0) - 'a'.charCodeAt(0)]++;\n    }\n    \n    let result = Infinity;\n    \n    for (let i = 0; i &lt; 26; i++) {\n        if (targetCount[i] &gt; 0) {\n            if (sCount[i] === 0) {\n                return 0;\n            }\n            result = Math.min(result, Math.floor(sCount[i] / targetCount[i]));\n        }\n    }\n    \n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function rearrangeCharacters(s: string, target: string): number {\n    const sCount = new Array(26).fill(0);\n    const targetCount = new Array(26).fill(0);\n    \n    for (const c of s) {\n        sCount[c.charCodeAt(0) - 'a'.charCodeAt(0)]++;\n    }\n    \n    for (const c of target) {\n        targetCount[c.charCodeAt(0) - 'a'.charCodeAt(0)]++;\n    }\n    \n    let result = Infinity;\n    \n    for (let i = 0; i &lt; 26; i++) {\n        if (targetCount[i] &gt; 0) {\n            if (sCount[i] === 0) {\n                return 0;\n            }\n            result = Math.min(result, Math.floor(sCount[i] / targetCount[i]));\n        }\n    }\n    \n    return result;\n}" },]} />

**Complexity**
- Time: O(n + m) where n is the length of s and m is the length of target
- Space: O(1) since we use fixed-size arrays of 26 elements
- Notes: This is the most efficient approach for lowercase English letters, with constant space and direct array access.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Rearrange Characters to Make Target String Solutions","datePublished":"2025-11-08","description":"Given two strings s and target, find the maximum number of copies of target that can be formed by rearranging characters from s.","keywords":"LeetCode, Rearrange Characters to Make Target String, Easy, Hash Table, String, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Map approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n + m) where n is the length of s and m is the length of target, as we iterate through both strings once."}},{"@type":"Question","name":"What is the space complexity of the Array Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) since we use fixed-size arrays of 26 elements regardless of input size."}},{"@type":"Question","name":"Why do we take the minimum of all character ratios?","acceptedAnswer":{"@type":"Answer","text":"We need all characters of target to form one copy. The limiting character (with the smallest ratio of available to required) determines how many complete copies we can make."}},{"@type":"Question","name":"What happens if a character in target doesn't exist in s?","acceptedAnswer":{"@type":"Answer","text":"If any character from target is not present in s, we immediately return 0 since we cannot form even one copy of target."}}]}).replace(/</g,'\u003c')}
</script>