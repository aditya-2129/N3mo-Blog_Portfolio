---
title: "Minimum Cost to Move Chips to The Same Position"
summary: "Find minimum cost to move all chips to the same position where moving 2 positions costs 0 and moving 1 position costs 1."
date: "Feb 21, 2024"
tags:
  - Array
  - Math
  - Greedy
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 72.90% | **Paid:** No
> **Topics:** Array, Math, Greedy

We have n chips, where the position of the ith chip is position[i].

We need to move all the chips to the same position. In one step, we can change the position of the ith chip from position[x] to:

- position[x] + 2 or position[x] - 2 with cost = 0.
- position[x] + 1 or position[x] - 1 with cost = 1.

Return the minimum cost needed to move all the chips to the same position.

- Examples
- Constraints
- [Parity Count Approach](#parity-count-approach)
- [Mathematical Approach](#mathematical-approach)

## Examples

Example 1:
```text
Input: position = [1,2,3]
Output: 1
Explanation: First step: Move the chip at position 3 to position 1 with cost = 0.
Second step: Move the chip at position 2 to position 1 with cost = 1.
Total cost is 1.
```

Example 2:
```text
Input: position = [2,2,2,3,3]
Output: 2
Explanation: We can move the two chips at position 3 to position 2. Each move has cost = 1. The total cost is 2.
```

Example 3:
```text
Input: position = [1,1000000000]
Output: 1
```

## Constraints
```text
1 <= position.length <= 100
1 <= position[i] <= 10^9
```

---
## Parity Count Approach

**Intuition**
Moving chips by 2 positions costs nothing, so all chips at even positions can be grouped together for free, and all chips at odd positions can be grouped together for free. The only cost is moving between parity groups.

**Steps**
- Count the number of chips at even positions
- Count the number of chips at odd positions
- Return the minimum of these two counts (we move the smaller group to the larger group)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def minCostToMoveChips(self, position: list[int]) -> int:
        even_count = 0
        odd_count = 0
        for pos in position:
            if pos % 2 == 0:
                even_count += 1
            else:
                odd_count += 1
        return min(even_count, odd_count)` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minCostToMoveChips(int[] position) {
        int evenCount = 0;
        int oddCount = 0;
        for (int pos : position) {
            if (pos % 2 == 0) {
                evenCount++;
            } else {
                oddCount++;
            }
        }
        return Math.min(evenCount, oddCount);
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minCostToMoveChips(vector<int>& position) {
        int evenCount = 0;
        int oddCount = 0;
        for (int pos : position) {
            if (pos % 2 == 0) {
                evenCount++;
            } else {
                oddCount++;
            }
        }
        return min(evenCount, oddCount);
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} position
 * @return {number}
 */
var minCostToMoveChips = function(position) {
    let evenCount = 0;
    let oddCount = 0;
    for (let pos of position) {
        if (pos % 2 === 0) {
            evenCount++;
        } else {
            oddCount++;
        }
    }
    return Math.min(evenCount, oddCount);
};` },
{ name: "TypeScript", lang: "typescript", code: `function minCostToMoveChips(position: number[]): number {
    let evenCount = 0;
    let oddCount = 0;
    for (let pos of position) {
        if (pos % 2 === 0) {
            evenCount++;
        } else {
            oddCount++;
        }
    }
    return Math.min(evenCount, oddCount);
}` },]} />

**Complexity**
- Time: O(n) where n is the length of position array
- Space: O(1) only using constant extra space
- Notes: This is the optimal solution with linear time complexity.

---
## Mathematical Approach

**Intuition**
Since we only care about parity (even or odd), we can use mathematical operations to count chips at each parity without explicit loops.

**Steps**
- Use reduce or sum to count even and odd positions
- Return the minimum count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def minCostToMoveChips(self, position: list[int]) -> int:
        even_count = sum(1 for pos in position if pos % 2 == 0)
        odd_count = len(position) - even_count
        return min(even_count, odd_count)` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minCostToMoveChips(int[] position) {
        int evenCount = 0;
        for (int pos : position) {
            evenCount += (pos % 2 == 0) ? 1 : 0;
        }
        int oddCount = position.length - evenCount;
        return Math.min(evenCount, oddCount);
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minCostToMoveChips(vector<int>& position) {
        int evenCount = 0;
        for (int pos : position) {
            evenCount += (pos % 2 == 0) ? 1 : 0;
        }
        int oddCount = position.size() - evenCount;
        return min(evenCount, oddCount);
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} position
 * @return {number}
 */
var minCostToMoveChips = function(position) {
    const evenCount = position.reduce((count, pos) =&gt; count + (pos % 2 === 0 ? 1 : 0), 0);
    const oddCount = position.length - evenCount;
    return Math.min(evenCount, oddCount);
};` },
{ name: "TypeScript", lang: "typescript", code: `function minCostToMoveChips(position: number[]): number {
    const evenCount = position.reduce((count, pos) =&gt; count + (pos % 2 === 0 ? 1 : 0), 0);
    const oddCount = position.length - evenCount;
    return Math.min(evenCount, oddCount);
}` },]} />

**Complexity**
- Time: O(n) where n is the length of position array
- Space: O(1) only using constant extra space
- Notes: Similar to the parity count approach but uses functional programming constructs.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Cost to Move Chips to The Same Position Solutions","datePublished":"2024-02-21","description":"Find minimum cost to move all chips to the same position where moving 2 positions costs 0 and moving 1 position costs 1.","keywords":"LeetCode, Minimum Cost to Move Chips to The Same Position, Easy, Array, Math, Greedy"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Parity Count Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the position array."}},{"@type":"Question","name":"What is the space complexity of the Parity Count Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use constant extra space."}},{"@type":"Question","name":"What is the time complexity of the Mathematical Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the position array."}},{"@type":"Question","name":"What is the space complexity of the Mathematical Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use constant extra space."}}]}).replace(/</g,'\u003c')}
</script>