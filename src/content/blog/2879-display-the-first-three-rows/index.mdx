---
title: "Display the First Three Rows"
summary: "Write a solution to display the first three rows of a DataFrame."
date: "Jan 29, 2024"
tags:
  - Pandas
  - DataFrame
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 93.00% | **Paid:** No
> **Topics:** N/A

DataFrame employees
+-------------+--------+
| Column Name | Type   |
+-------------+--------+
| employee_id | int    |
| name        | object |
| department  | object |
| salary      | int    |
+-------------+--------+
Write a solution to display the first 3 rows of this DataFrame.

- Examples
- Constraints
- [Approach 1: Using head()](#approach-1-using-head)
- [Approach 2: Using Slicing](#approach-2-using-slicing)
- [Approach 3: Using iloc](#approach-3-using-iloc)

## Examples

**Example 1:**

Input:
DataFrame employees
+-------------+--------+------------+--------+
| employee_id | name   | department | salary |
+-------------+--------+------------+--------+
| 3           | Bob    | IT         | 72000  |
| 90          | Alice  | Sales      | 90000  |
| 9           | Tatiana| Engineering| 88000  |
| 60          | Ann    | IT         | 79000  |
+-------------+--------+------------+--------+

Output:
+-------------+--------+------------+--------+
| employee_id | name   | department | salary |
+-------------+--------+------------+--------+
| 3           | Bob    | IT         | 72000  |
| 90          | Alice  | Sales      | 90000  |
| 9           | Tatiana| Engineering| 88000  |
+-------------+--------+------------+--------+

Explanation:
The first 3 rows of the DataFrame are displayed.

## Constraints

```text
The DataFrame may have 0 or more rows.
```

---
## Approach 1: Using head()

**Intuition**
The pandas `head()` method is specifically designed to return the first n rows of a DataFrame, making it the most direct and readable solution.

**Steps**
- Call the `head(3)` method on the DataFrame to get the first 3 rows
- Return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import pandas as pd\n\ndef selectFirstRows(employees: pd.DataFrame) -&gt; pd.DataFrame:\n    return employees.head(3)" },
{ name: "Java", lang: "java", code: "import org.apache.spark.sql.Dataset;\nimport org.apache.spark.sql.Row;\n\npublic class Solution {\n    public Dataset&lt;Row&gt; selectFirstRows(Dataset&lt;Row&gt; employees) {\n        return employees.limit(3);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\nusing namespace std;\n\nstruct Employee {\n    int employee_id;\n    string name;\n    string department;\n    int salary;\n};\n\nclass Solution {\npublic:\n    vector&lt;Employee&gt; selectFirstRows(vector&lt;Employee&gt;& employees) {\n        vector&lt;Employee&gt; result;\n        int n = min(3, (int)employees.size());\n        for (int i = 0; i &lt; n; i++) {\n            result.push_back(employees[i]);\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {Array} employees\n * @return {Array}\n */\nvar selectFirstRows = function(employees) {\n    return employees.slice(0, 3);\n};" },
{ name: "TypeScript", lang: "typescript", code: "interface Employee {\n    employee_id: number;\n    name: string;\n    department: string;\n    salary: number;\n}\n\nfunction selectFirstRows(employees: Employee[]): Employee[] {\n    return employees.slice(0, 3);\n}" },]} />

**Complexity**
- Time: O(1) - accessing first 3 rows is constant time
- Space: O(1) - only returns a view of the original data
- Notes: Most readable and idiomatic pandas approach

---
## Approach 2: Using Slicing

**Intuition**
Python slicing syntax `[:3]` can be used to select the first 3 rows of a DataFrame, similar to how it works with lists.

**Steps**
- Use slice notation `[:3]` on the DataFrame
- Return the sliced result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import pandas as pd\n\ndef selectFirstRows(employees: pd.DataFrame) -&gt; pd.DataFrame:\n    return employees[:3]" },
{ name: "Java", lang: "java", code: "import org.apache.spark.sql.Dataset;\nimport org.apache.spark.sql.Row;\n\npublic class Solution {\n    public Dataset&lt;Row&gt; selectFirstRows(Dataset&lt;Row&gt; employees) {\n        return employees.limit(3);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\nusing namespace std;\n\nstruct Employee {\n    int employee_id;\n    string name;\n    string department;\n    int salary;\n};\n\nclass Solution {\npublic:\n    vector&lt;Employee&gt; selectFirstRows(vector&lt;Employee&gt;& employees) {\n        int n = min(3, (int)employees.size());\n        return vector&lt;Employee&gt;(employees.begin(), employees.begin() + n);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {Array} employees\n * @return {Array}\n */\nvar selectFirstRows = function(employees) {\n    return employees.slice(0, 3);\n};" },
{ name: "TypeScript", lang: "typescript", code: "interface Employee {\n    employee_id: number;\n    name: string;\n    department: string;\n    salary: number;\n}\n\nfunction selectFirstRows(employees: Employee[]): Employee[] {\n    return employees.slice(0, 3);\n}" },]} />

**Complexity**
- Time: O(1) - slicing first 3 elements is constant time
- Space: O(1) - creates a view, not a copy
- Notes: Concise Python syntax, works well for small selections

---
## Approach 3: Using iloc

**Intuition**
The `iloc` indexer allows integer-location based indexing, providing explicit row selection by position.

**Steps**
- Use `iloc[0:3]` to select rows at positions 0, 1, and 2
- Return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import pandas as pd\n\ndef selectFirstRows(employees: pd.DataFrame) -&gt; pd.DataFrame:\n    return employees.iloc[0:3]" },
{ name: "Java", lang: "java", code: "import org.apache.spark.sql.Dataset;\nimport org.apache.spark.sql.Row;\n\npublic class Solution {\n    public Dataset&lt;Row&gt; selectFirstRows(Dataset&lt;Row&gt; employees) {\n        return employees.limit(3);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\nusing namespace std;\n\nstruct Employee {\n    int employee_id;\n    string name;\n    string department;\n    int salary;\n};\n\nclass Solution {\npublic:\n    vector&lt;Employee&gt; selectFirstRows(vector&lt;Employee&gt;& employees) {\n        vector&lt;Employee&gt; result;\n        for (int i = 0; i &lt; 3 && i &lt; employees.size(); i++) {\n            result.push_back(employees[i]);\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {Array} employees\n * @return {Array}\n */\nvar selectFirstRows = function(employees) {\n    const result = [];\n    for (let i = 0; i &lt; 3 && i &lt; employees.length; i++) {\n        result.push(employees[i]);\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "interface Employee {\n    employee_id: number;\n    name: string;\n    department: string;\n    salary: number;\n}\n\nfunction selectFirstRows(employees: Employee[]): Employee[] {\n    const result: Employee[] = [];\n    for (let i = 0; i &lt; 3 && i &lt; employees.length; i++) {\n        result.push(employees[i]);\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(1) - selecting fixed number of rows
- Space: O(1) - returns a view of the data
- Notes: More explicit about integer-based indexing, useful when combined with column selection

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Display the First Three Rows Solutions","datePublished":"2024-01-29","description":"Write a solution to display the first three rows of a DataFrame.","keywords":"LeetCode, Display the First Three Rows, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of using head() to get first 3 rows?","acceptedAnswer":{"@type":"Answer","text":"O(1) - accessing the first 3 rows of a DataFrame is a constant time operation as it only needs to return a view of the first 3 elements."}},{"@type":"Question","name":"What is the space complexity of the slicing approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - slicing creates a view of the original DataFrame rather than copying the data, so no additional space is used proportional to the input size."}},{"@type":"Question","name":"Which approach is most recommended for pandas DataFrames?","acceptedAnswer":{"@type":"Answer","text":"The head() method is most recommended as it is the most readable and idiomatic pandas approach specifically designed for this purpose."}}]}).replace(/</g,'\u003c')}
</script>