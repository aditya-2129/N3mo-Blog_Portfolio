---
title: "Minimum Operations to Make the Array Increasing"
summary: "Return the minimum number of operations to make an array strictly increasing by incrementing elements."
date: "Oct 14, 2024"
tags:
  - Array
  - Greedy
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.00% | **Paid:** No
> **Topics:** Array, Greedy

You are given an integer array nums (0-indexed). In one operation, you can choose an element of the array and increment it by 1.

Return the minimum number of operations to make nums strictly increasing.

An array nums is strictly increasing if nums[0] &lt; nums[1] &lt; nums[2] &lt; ... &lt; nums[nums.length - 1].

- Examples
- Constraints
- [Greedy (Single Pass)](#greedy-single-pass)
- [Greedy (Without Modifying Array)](#greedy-without-modifying-array)
- [Brute Force (Simulation)](#brute-force-simulation)

## Examples

**Example 1:**

```text
Input: nums = [1,1,1]
Output: 3
Explanation: You can do the following three operations to make the array strictly increasing:
1. Increment nums[2] by 1. nums becomes [1,1,2].
2. Increment nums[1] by 1. nums becomes [1,2,2].
3. Increment nums[2] by 1. nums becomes [1,2,3].
```

**Example 2:**

```text
Input: nums = [1,5,2,4,1]
Output: 14
```

**Example 3:**

```text
Input: nums = [8]
Output: 0
```

## Constraints

```text
1 <= nums.length <= 5000
1 <= nums[i] <= 10⁴
```

---

## Greedy (Single Pass)

**Intuition**
For each element, if it's not greater than the previous element, we must increment it to exactly one more than the previous value to minimize operations.

**Steps**
- Initialize operations counter to 0
- Iterate through the array starting from index 1
- If current element is less than or equal to previous, calculate needed increments and update both operations and the current element
- Return total operations

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def minOperations(self, nums: List[int]) -> int:
        operations = 0
        for i in range(1, len(nums)):
            if nums[i] &lt;= nums[i-1]:
                needed = nums[i-1] + 1 - nums[i]
                operations += needed
                nums[i] = nums[i-1] + 1
        return operations
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minOperations(int[] nums) {
        int operations = 0;
        for (int i = 1; i &lt; nums.length; i++) {
            if (nums[i] &lt;= nums[i-1]) {
                int needed = nums[i-1] + 1 - nums[i];
                operations += needed;
                nums[i] = nums[i-1] + 1;
            }
        }
        return operations;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minOperations(vector<int>& nums) {
        int operations = 0;
        for (int i = 1; i &lt; nums.size(); i++) {
            if (nums[i] &lt;= nums[i-1]) {
                int needed = nums[i-1] + 1 - nums[i];
                operations += needed;
                nums[i] = nums[i-1] + 1;
            }
        }
        return operations;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `var minOperations = function(nums) {
    let operations = 0;
    for (let i = 1; i &lt; nums.length; i++) {
        if (nums[i] &lt;= nums[i-1]) {
            let needed = nums[i-1] + 1 - nums[i];
            operations += needed;
            nums[i] = nums[i-1] + 1;
        }
    }
    return operations;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function minOperations(nums: number[]): number {
    let operations = 0;
    for (let i = 1; i &lt; nums.length; i++) {
        if (nums[i] &lt;= nums[i-1]) {
            let needed = nums[i-1] + 1 - nums[i];
            operations += needed;
            nums[i] = nums[i-1] + 1;
        }
    }
    return operations;
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Modifies the input array in-place

---

## Greedy (Without Modifying Array)

**Intuition**
Same greedy logic but track the previous value in a separate variable instead of modifying the original array.

**Steps**
- Initialize operations counter and prev variable with first element
- Iterate through remaining elements
- If current element is not greater than prev, calculate needed increments and update prev to prev + 1
- Otherwise, update prev to current element
- Return total operations

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def minOperations(self, nums: List[int]) -> int:
        if not nums:
            return 0
        operations = 0
        prev = nums[0]
        for i in range(1, len(nums)):
            if nums[i] &lt;= prev:
                needed = prev + 1 - nums[i]
                operations += needed
                prev = prev + 1
            else:
                prev = nums[i]
        return operations
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minOperations(int[] nums) {
        if (nums.length == 0) return 0;
        int operations = 0;
        int prev = nums[0];
        for (int i = 1; i &lt; nums.length; i++) {
            if (nums[i] &lt;= prev) {
                int needed = prev + 1 - nums[i];
                operations += needed;
                prev = prev + 1;
            } else {
                prev = nums[i];
            }
        }
        return operations;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minOperations(vector<int>& nums) {
        if (nums.empty()) return 0;
        int operations = 0;
        int prev = nums[0];
        for (int i = 1; i &lt; nums.size(); i++) {
            if (nums[i] &lt;= prev) {
                int needed = prev + 1 - nums[i];
                operations += needed;
                prev = prev + 1;
            } else {
                prev = nums[i];
            }
        }
        return operations;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `var minOperations = function(nums) {
    if (nums.length === 0) return 0;
    let operations = 0;
    let prev = nums[0];
    for (let i = 1; i &lt; nums.length; i++) {
        if (nums[i] &lt;= prev) {
            let needed = prev + 1 - nums[i];
            operations += needed;
            prev = prev + 1;
        } else {
            prev = nums[i];
        }
    }
    return operations;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function minOperations(nums: number[]): number {
    if (nums.length === 0) return 0;
    let operations = 0;
    let prev = nums[0];
    for (let i = 1; i &lt; nums.length; i++) {
        if (nums[i] &lt;= prev) {
            let needed = prev + 1 - nums[i];
            operations += needed;
            prev = prev + 1;
        } else {
            prev = nums[i];
        }
    }
    return operations;
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Preserves the original array

---

## Brute Force (Simulation)

**Intuition**
Simulate each operation by finding the first violation and incrementing that element until the array is strictly increasing.

**Steps**
- Initialize operations counter to 0
- While array is not strictly increasing:
  - Find the first index where nums[i] &lt;= nums[i-1]
  - Increment nums[i] by 1
  - Increment operations counter
- Return total operations

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def minOperations(self, nums: List[int]) -> int:
        operations = 0
        n = len(nums)
        while True:
            is_strictly_increasing = True
            for i in range(1, n):
                if nums[i] &lt;= nums[i-1]:
                    is_strictly_increasing = False
                    nums[i] += 1
                    operations += 1
                    break
            if is_strictly_increasing:
                break
        return operations
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minOperations(int[] nums) {
        int operations = 0;
        int n = nums.length;
        while (true) {
            boolean isStrictlyIncreasing = true;
            for (int i = 1; i &lt; n; i++) {
                if (nums[i] &lt;= nums[i-1]) {
                    isStrictlyIncreasing = false;
                    nums[i]++;
                    operations++;
                    break;
                }
            }
            if (isStrictlyIncreasing) {
                break;
            }
        }
        return operations;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minOperations(vector<int>& nums) {
        int operations = 0;
        int n = nums.size();
        while (true) {
            bool isStrictlyIncreasing = true;
            for (int i = 1; i &lt; n; i++) {
                if (nums[i] &lt;= nums[i-1]) {
                    isStrictlyIncreasing = false;
                    nums[i]++;
                    operations++;
                    break;
                }
            }
            if (isStrictlyIncreasing) {
                break;
            }
        }
        return operations;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `var minOperations = function(nums) {
    let operations = 0;
    const n = nums.length;
    while (true) {
        let isStrictlyIncreasing = true;
        for (let i = 1; i &lt; n; i++) {
            if (nums[i] &lt;= nums[i-1]) {
                isStrictlyIncreasing = false;
                nums[i]++;
                operations++;
                break;
            }
        }
        if (isStrictlyIncreasing) {
            break;
        }
    }
    return operations;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function minOperations(nums: number[]): number {
    let operations = 0;
    const n = nums.length;
    while (true) {
        let isStrictlyIncreasing = true;
        for (let i = 1; i &lt; n; i++) {
            if (nums[i] &lt;= nums[i-1]) {
                isStrictlyIncreasing = false;
                nums[i]++;
                operations++;
                break;
            }
        }
        if (isStrictlyIncreasing) {
            break;
        }
    }
    return operations;
}
` },]} />

**Complexity**
- Time: O(n × max_operations) - can be very slow for large inputs
- Space: O(1)
- Notes: Not recommended for production use, only for understanding the problem

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Operations to Make the Array Increasing Solutions","datePublished":"2024-10-14","description":"Return the minimum number of operations to make an array strictly increasing by incrementing elements.","keywords":"LeetCode, Minimum Operations to Make the Array Increasing, Easy, Array, Greedy"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Greedy approach?","acceptedAnswer":{"@type":"Answer","text":"The Greedy approach has O(n) time complexity where n is the length of the array, as we only need to traverse the array once."}},{"@type":"Question","name":"What is the space complexity of the Greedy approach?","acceptedAnswer":{"@type":"Answer","text":"The Greedy approach has O(1) space complexity as we only use a constant amount of extra space for variables."}},{"@type":"Question","name":"Why is the Brute Force approach inefficient?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach has O(n × max_operations) time complexity because it performs one operation at a time and rechecks the entire array, making it extremely slow for large inputs."}}]}).replace(/</g,'\u003c')}
</script>