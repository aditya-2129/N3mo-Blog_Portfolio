---
title: "Base 7"
summary: "Given an integer, return its base 7 representation as a string."
date: "Apr 19, 2025"
tags:
  - Math
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 54.50% | **Paid:** No
> **Topics:** Math, String

Given an integer num, return a string representing its base 7 representation.

## Examples

```text
Input: num = 100
Output: "202"
Explanation: 202 in base 7 is 2*7² + 0*7 + 2 = 98 + 0 + 2 = 100.
```

```text
Input: num = -7
Output: "-10"
```

## Constraints

```text
-10⁷ <= num <= 10⁷
```

- [Iterative Division](#iterative-division)
- [Recursive Conversion](#recursive-conversion)

## Iterative Division

**Intuition**
To convert a decimal number to base 7, we repeatedly divide the number by 7 and record the remainders. The remainders, read in reverse order, form the base 7 representation.

**Steps**
- Handle the edge case where `num` is 0 by returning "0".
- Determine if the number is negative and store the sign. Work with the absolute value of the number.
- Use a loop to repeatedly divide the number by 7.
- In each iteration, calculate the remainder (`num % 7`) and prepend it to the result string (or append and reverse later).
- Update the number by performing integer division (`num // 7`).
- If the original number was negative, prepend a "-" sign to the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def convertToBase7(self, num: int) -&gt; str:\n        if num == 0:\n            return \"0\"\n        \n        negative = num &lt; 0\n        num = abs(num)\n        res = []\n        \n        while num &gt; 0:\n            res.append(str(num % 7))\n            num //= 7\n        \n        if negative:\n            res.append(\"-\")\n            \n        return \"\".join(reversed(res))\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String convertToBase7(int num) {\n        if (num == 0) {\n            return \"0\";\n        }\n        \n        boolean negative = num &lt; 0;\n        num = Math.abs(num);\n        StringBuilder sb = new StringBuilder();\n        \n        while (num &gt; 0) {\n            sb.append(num % 7);\n            num /= 7;\n        }\n        \n        if (negative) {\n            sb.append(\"-\");\n        }\n        \n        return sb.reverse().toString();\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string convertToBase7(int num) {\n        if (num == 0) return \"0\";\n        \n        bool negative = num &lt; 0;\n        num = abs(num);\n        string res = \"\";\n        \n        while (num &gt; 0) {\n            res = to_string(num % 7) + res;\n            num /= 7;\n        }\n        \n        if (negative) {\n            res = \"-\" + res;\n        }\n        \n        return res;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var convertToBase7 = function(num) {\n    if (num === 0) return \"0\";\n    \n    let negative = num &lt; 0;\n    num = Math.abs(num);\n    let res = \"\";\n    \n    while (num &gt; 0) {\n        res = (num % 7) + res;\n        num = Math.floor(num / 7);\n    }\n    \n    if (negative) {\n        res = \"-\" + res;\n    }\n    \n    return res;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function convertToBase7(num: number): string {\n    if (num === 0) return \"0\";\n    \n    let negative = num &lt; 0;\n    num = Math.abs(num);\n    let res = \"\";\n    \n    while (num &gt; 0) {\n        res = (num % 7) + res;\n        num = Math.floor(num / 7);\n    }\n    \n    if (negative) {\n        res = \"-\" + res;\n    }\n    \n    return res;\n}\n" },]} />

**Complexity**
- Time: O(log₇ n)
- Space: O(log₇ n)
- Notes: The time complexity is logarithmic because we divide the number by 7 in each step. The space complexity is due to storing the result string.

## Recursive Conversion

**Intuition**
Base conversion can be naturally expressed recursively. The base 7 representation of a number `n` is the base 7 representation of `n / 7` followed by the digit `n % 7`.

**Steps**
- Handle the base case: if the absolute value of `num` is less than 7, return its string representation.
- If the number is negative, return "-" concatenated with the result of the recursive call on the positive value.
- Otherwise, recursively call the function on `num / 7` and concatenate the result with `num % 7`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def convertToBase7(self, num: int) -&gt; str:\n        if num &lt; 0:\n            return \"-\" + self.convertToBase7(-num)\n        if num &lt; 7:\n            return str(num)\n        return self.convertToBase7(num // 7) + str(num % 7)\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String convertToBase7(int num) {\n        if (num &lt; 0) {\n            return \"-\" + convertToBase7(-num);\n        }\n        if (num &lt; 7) {\n            return Integer.toString(num);\n        }\n        return convertToBase7(num / 7) + Integer.toString(num % 7);\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string convertToBase7(int num) {\n        if (num &lt; 0) {\n            return \"-\" + convertToBase7(-num);\n        }\n        if (num &lt; 7) {\n            return to_string(num);\n        }\n        return convertToBase7(num / 7) + to_string(num % 7);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var convertToBase7 = function(num) {\n    if (num &lt; 0) {\n        return \"-\" + convertToBase7(-num);\n    }\n    if (num &lt; 7) {\n        return num.toString();\n    }\n    return convertToBase7(Math.floor(num / 7)) + (num % 7).toString();\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function convertToBase7(num: number): string {\n    if (num &lt; 0) {\n        return \"-\" + convertToBase7(-num);\n    }\n    if (num &lt; 7) {\n        return num.toString();\n    }\n    return convertToBase7(Math.floor(num / 7)) + (num % 7).toString();\n}\n" },]} />

**Complexity**
- Time: O(log₇ n)
- Space: O(log₇ n)
- Notes: The space complexity includes the stack space used by the recursion, which is proportional to the number of digits.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Base 7 Solutions","datePublished":"2025-04-19","description":"Given an integer, return its base 7 representation as a string.","keywords":"LeetCode, Base 7, Easy, Math, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Division approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(log₇ n), where n is the value of the input number."}},{"@type":"Question","name":"What is the space complexity of the Recursive Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(log₇ n) to account for the recursion stack space."}}]}).replace(/</g,'\u003c')}
</script>