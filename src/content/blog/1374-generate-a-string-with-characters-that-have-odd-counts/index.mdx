---
title: "Generate a String With Characters That Have Odd Counts"
summary: "Given an integer n, return a string of length n with all lowercase English letters such that each character appears an odd number of times."
date: "Jan 13, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 78.50% | **Paid:** No
> **Topics:** String

Given an integer `n`, return a string of length `n` with all lowercase English letters such that each character appears an odd number of times.

The returned string can contain any of the lowercase English letters. If there are multiple valid strings, return any of them.

## Examples

```text
Input: n = 4
Output: "pppz"
Explanation: "pppz" is a valid string since the character 'p' appears three times and the character 'z' appears once. Note that there are many other valid strings such as "ohhh" and "abaa".
```

```text
Input: n = 2
Output: "xy"
Explanation: "xy" is a valid string since 'x' and 'y' appear once. Note that there are many other valid strings such as "ag" and "ur".
```

```text
Input: n = 7
Output: "holasss"
```

## Constraints

```text
1 <= n <= 500
```

- [Simple Construction](#simple-construction)
- [String Multiplication](#string-multiplication)
- [Greedy Construction](#greedy-construction)

## Simple Construction

**Intuition**
If n is odd, we can use a single character n times. If n is even, we use one character n-1 times and another character once.

**Steps**
- Check if n is odd or even
- If odd, return 'a' repeated n times
- If even, return 'a' repeated (n-1) times followed by 'b'

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def generateTheString(self, n: int) -> str:
        if n % 2 == 1:
            return 'a' * n
        else:
            return 'a' * (n - 1) + 'b'
` },
{ name: "Java", lang: "java", code: `class Solution {
    public String generateTheString(int n) {
        StringBuilder sb = new StringBuilder();
        if (n % 2 == 1) {
            for (int i = 0; i &lt; n; i++) {
                sb.append('a');
            }
        } else {
            for (int i = 0; i &lt; n - 1; i++) {
                sb.append('a');
            }
            sb.append('b');
        }
        return sb.toString();
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    string generateTheString(int n) {
        string result;
        if (n % 2 == 1) {
            result = string(n, 'a');
        } else {
            result = string(n - 1, 'a') + 'b';
        }
        return result;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {string}
 */
var generateTheString = function(n) {
    if (n % 2 === 1) {
        return 'a'.repeat(n);
    } else {
        return 'a'.repeat(n - 1) + 'b';
    }
};
` },
{ name: "TypeScript", lang: "typescript", code: `function generateTheString(n: number): string {
    if (n % 2 === 1) {
        return 'a'.repeat(n);
    } else {
        return 'a'.repeat(n - 1) + 'b';
    }
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Simple and efficient solution with minimal operations

## String Multiplication

**Intuition**
Use string multiplication/repeat operations to construct the result efficiently.

**Steps**
- If n is odd, return 'a' multiplied by n
- If n is even, return 'a' multiplied by (n-1) plus 'b'

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def generateTheString(self, n: int) -> str:
        return 'a' * n if n % 2 else 'a' * (n - 1) + 'b'
` },
{ name: "Java", lang: "java", code: `class Solution {
    public String generateTheString(int n) {
        if (n % 2 == 1) {
            return "a".repeat(n);
        } else {
            return "a".repeat(n - 1) + "b";
        }
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    string generateTheString(int n) {
        if (n % 2 == 1) {
            return string(n, 'a');
        } else {
            return string(n - 1, 'a') + "b";
        }
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {string}
 */
var generateTheString = function(n) {
    return n % 2 === 1 ? 'a'.repeat(n) : 'a'.repeat(n - 1) + 'b';
};
` },
{ name: "TypeScript", lang: "typescript", code: `function generateTheString(n: number): string {
    return n % 2 === 1 ? 'a'.repeat(n) : 'a'.repeat(n - 1) + 'b';
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: More concise code using built-in string operations

## Greedy Construction

**Intuition**
Build the string by adding characters one at a time, ensuring odd counts for all characters.

**Steps**
- Start with an empty string
- Add 'a' n-1 times
- If n is odd, add one more 'a', otherwise add 'b'

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def generateTheString(self, n: int) -> str:
        result = []
        for i in range(n - 1):
            result.append('a')
        if n % 2 == 1:
            result.append('a')
        else:
            result.append('b')
        return ''.join(result)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public String generateTheString(int n) {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i &lt; n - 1; i++) {
            sb.append('a');
        }
        if (n % 2 == 1) {
            sb.append('a');
        } else {
            sb.append('b');
        }
        return sb.toString();
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    string generateTheString(int n) {
        string result;
        for (int i = 0; i &lt; n - 1; i++) {
            result += 'a';
        }
        if (n % 2 == 1) {
            result += 'a';
        } else {
            result += 'b';
        }
        return result;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {string}
 */
var generateTheString = function(n) {
    let result = '';
    for (let i = 0; i &lt; n - 1; i++) {
        result += 'a';
    }
    if (n % 2 === 1) {
        result += 'a';
    } else {
        result += 'b';
    }
    return result;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function generateTheString(n: number): string {
    let result = '';
    for (let i = 0; i &lt; n - 1; i++) {
        result += 'a';
    }
    if (n % 2 === 1) {
        result += 'a';
    } else {
        result += 'b';
    }
    return result;
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: More verbose but demonstrates the greedy approach clearly

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Generate a String With Characters That Have Odd Counts Solutions","datePublished":"2024-01-13","description":"Given an integer n, return a string of length n with all lowercase English letters such that each character appears an odd number of times.","keywords":"LeetCode, Generate a String With Characters That Have Odd Counts, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Simple Construction approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) as we need to create a string of length n."}},{"@type":"Question","name":"What is the space complexity of String Multiplication approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) to store the resulting string."}},{"@type":"Question","name":"What is the time complexity of Greedy Construction approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) as we iterate through n characters to build the string."}}]}).replace(/</g,'\u003c')}
</script>