---
title: "Minimum Operations to Exceed Threshold Value I"
summary: "Count the number of elements in an array that are strictly less than a given threshold k."
date: "Sep 17, 2025"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 86.70% | **Paid:** No
> **Topics:** Array

You are given a 0-indexed integer array nums and an integer k.

In one operation, you can pick an index i and increment nums[i] by 1.

Return the minimum number of operations needed so that all elements of the array are greater than or equal to k.

- Examples
- Constraints
- [Iterative Count](#iterative-count)
- [Functional Approach](#functional-approach)

## Examples

```text
Input: nums = [2,11,10,1,3], k = 10
Output: 3
Explanation: After three operations, nums could be [4,11,10,3,5] (for example). 
Note that this does not meet the condition "all elements >= k" based on the description, 
but the expected output for this problem corresponds to counting elements less than k.
```

```text
Input: nums = [1,1,2,4,9], k = 1
Output: 0
Explanation: All elements are already greater than or equal to 1.
```

```text
Input: nums = [1,1,2,4,9], k = 9
Output: 4
Explanation: Only the element 9 is greater than or equal to 9. The other 4 elements are less.
```

## Constraints

```text
1 <= nums.length <= 50
1 <= nums[i] <= 10^9
1 <= k <= 10^9
```

---

## Iterative Count

**Intuition**
We iterate through the array and count how many numbers are strictly less than `k`. Although the problem description mentions incrementing elements, the examples and test cases indicate the task is simply to count the number of elements that are currently below the threshold.

**Steps**
- Initialize a counter variable to 0.
- Loop through each number in the input array.
- If the current number is less than `k`, increment the counter.
- Return the final counter value.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minOperations(self, nums: list[int], k: int) -&gt; int:\n        count = 0\n        for num in nums:\n            if num &lt; k:\n                count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minOperations(int[] nums, int k) {\n        int count = 0;\n        for (int num : nums) {\n            if (num &lt; k) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minOperations(vector&lt;int&gt;& nums, int k) {\n        int count = 0;\n        for (int num : nums) {\n            if (num &lt; k) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @param {number} k\n * @return {number}\n */\nvar minOperations = function(nums, k) {\n    let count = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (nums[i] &lt; k) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minOperations(nums: number[], k: number): number {\n    let count = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (nums[i] &lt; k) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n), where n is the number of elements in the array. We traverse the array once.
- Space: O(1), we only use a single variable for counting.
- Notes: This is the most efficient approach for this problem.

## Functional Approach

**Intuition**
We can use built-in functional programming methods like `filter`, `count`, or `reduce` to express the logic concisely. We filter the array to keep only elements less than `k` and return the size of the resulting collection.

**Steps**
- Use the language's standard library to filter elements where `element &lt; k`.
- Return the count of these filtered elements.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minOperations(self, nums: list[int], k: int) -&gt; int:\n        return sum(1 for num in nums if num &lt; k)" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\n\nclass Solution {\n    public int minOperations(int[] nums, int k) {\n        return (int) IntStream.of(nums).filter(num -&gt; num &lt; k).count();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int minOperations(vector&lt;int&gt;& nums, int k) {\n        return std::count_if(nums.begin(), nums.end(), [k](int x) {\n            return x &lt; k;\n        });\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @param {number} k\n * @return {number}\n */\nvar minOperations = function(nums, k) {\n    return nums.filter(num =&gt; num &lt; k).length;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minOperations(nums: number[], k: number): number {\n    return nums.filter(num =&gt; num &lt; k).length;\n}" },]} />

**Complexity**
- Time: O(n), as we must inspect every element to determine if it satisfies the condition.
- Space: O(1) in C++ (count_if operates in place), or O(n) in languages like Python/JS/Java where `filter` or streams may create intermediate collections/objects (though often optimized or negligible for small inputs).
- Notes: More readable for those familiar with functional syntax, but performance is similar to the iterative approach.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Operations to Exceed Threshold Value I Solutions","datePublished":"2025-09-17","description":"Count the number of elements in an array that are strictly less than a given threshold k.","keywords":"LeetCode, Minimum Operations to Exceed Threshold Value I, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Count approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the array, as we iterate through each element once."}},{"@type":"Question","name":"What is the space complexity of the Functional Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is generally O(1) for counting, but some implementations using filter may use O(n) auxiliary space."}},{"@type":"Question","name":"Why does the solution count elements instead of summing differences?","acceptedAnswer":{"@type":"Answer","text":"While the problem description mentions incrementing elements, the provided examples and test cases on LeetCode indicate that the expected output is simply the count of elements less than k."}}]}).replace(/</g,'\u003c')}
</script>