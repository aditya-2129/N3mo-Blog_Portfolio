---
title: "Check Divisibility by Digit Sum and Product"
summary: "Determine if an integer is divisible by both the sum and the product of its digits."
date: "Aug 28, 2024"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 69.60% | **Paid:** No
> **Topics:** Math

Given an integer n, return true if n is divisible by the sum of its digits and also divisible by the product of its digits, or false otherwise.

- Examples
- Constraints
- [String Conversion](#string-conversion)
- [Mathematical Modulo](#mathematical-modulo)

## Examples

### Example 1

**Input:**
```text
n = 99
```

**Output:**
```text
true
```

**Explanation:**
Since 99 is divisible by the sum (9 + 9 = 18) plus product (9 * 9 = 81) of its digits (total 99), the output is true.

### Example 2

**Input:**
```text
n = 23
```

**Output:**
```text
false
```

**Explanation:**
Since 23 is not divisible by the sum (2 + 3 = 5) plus product (2 * 3 = 6) of its digits (total 11), the output is false.

## Constraints

```text
1 <= n <= 10^5
```

---
## String Conversion

**Intuition**
Convert the integer to a string to easily iterate over each digit. This allows us to process each character, convert it back to an integer, and calculate the sum and product.

**Steps**
- Convert the integer `n` to a string.
- Initialize `digit_sum` to 0 and `digit_prod` to 1.
- Iterate through each character in the string:
  - Convert the character to an integer `d`.
  - Add `d` to `digit_sum`.
  - Multiply `d` by `digit_prod`.
- If `digit_prod` is 0, return `false` (to avoid division by zero).
- Check if `n % digit_sum == 0` AND `n % digit_prod == 0`.
- Return the result of the check.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def isDivisible(self, n: int) -> bool:
        s = str(n)
        digit_sum = 0
        digit_prod = 1
        for ch in s:
            d = int(ch)
            digit_sum += d
            digit_prod *= d
        
        if digit_prod == 0:
            return False
            
        return n % digit_sum == 0 and n % digit_prod == 0` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean isDivisible(int n) {
        String s = String.valueOf(n);
        int digitSum = 0;
        int digitProd = 1;
        
        for (int i = 0; i &lt; s.length(); i++) {
            int d = s.charAt(i) - '0';
            digitSum += d;
            digitProd *= d;
        }
        
        if (digitProd == 0) {
            return false;
        }
        
        return n % digitSum == 0 && n % digitProd == 0;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool isDivisible(int n) {
        string s = to_string(n);
        int digitSum = 0;
        int digitProd = 1;
        
        for (char c : s) {
            int d = c - '0';
            digitSum += d;
            digitProd *= d;
        }
        
        if (digitProd == 0) {
            return false;
        }
        
        return n % digitSum == 0 && n % digitProd == 0;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {boolean}
 */
var isDivisible = function(n) {
    let s = n.toString();
    let digitSum = 0;
    let digitProd = 1;
    
    for (let i = 0; i &lt; s.length; i++) {
        let d = parseInt(s[i], 10);
        digitSum += d;
        digitProd *= d;
    }
    
    if (digitProd === 0) {
        return false;
    }
    
    return n % digitSum === 0 && n % digitProd === 0;
};` },
{ name: "TypeScript", lang: "typescript", code: `function isDivisible(n: number): boolean {
    let s = n.toString();
    let digitSum = 0;
    let digitProd = 1;
    
    for (let i = 0; i &lt; s.length; i++) {
        let d = parseInt(s[i], 10);
        digitSum += d;
        digitProd *= d;
    }
    
    if (digitProd === 0) {
        return false;
    }
    
    return n % digitSum === 0 && n % digitProd === 0;
}` },]} />

**Complexity**
- Time: O(log n) - The number of digits in n is proportional to log₁₀(n).
- Space: O(log n) - To store the string representation of n.
- Notes: This approach is readable but uses extra space for the string.

---
## Mathematical Modulo

**Intuition**
Use mathematical operations to extract digits without converting the number to a string. This is more space-efficient as it operates directly on the integer.

**Steps**
- Initialize `digit_sum` to 0 and `digit_prod` to 1.
- Create a temporary variable `temp` equal to `n`.
- While `temp` is greater than 0:
  - Get the last digit using `temp % 10`.
  - Add the digit to `digit_sum`.
  - Multiply the digit by `digit_prod`.
  - Remove the last digit by dividing `temp` by 10 (integer division).
- If `digit_prod` is 0, return `false`.
- Check if `n % digit_sum == 0` AND `n % digit_prod == 0`.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def isDivisible(self, n: int) -> bool:
        digit_sum = 0
        digit_prod = 1
        temp = n
        
        while temp &gt; 0:
            d = temp % 10
            digit_sum += d
            digit_prod *= d
            temp //= 10
            
        if digit_prod == 0:
            return False
            
        return n % digit_sum == 0 and n % digit_prod == 0` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean isDivisible(int n) {
        int digitSum = 0;
        int digitProd = 1;
        int temp = n;
        
        while (temp &gt; 0) {
            int d = temp % 10;
            digitSum += d;
            digitProd *= d;
            temp /= 10;
        }
        
        if (digitProd == 0) {
            return false;
        }
        
        return n % digitSum == 0 && n % digitProd == 0;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool isDivisible(int n) {
        int digitSum = 0;
        int digitProd = 1;
        int temp = n;
        
        while (temp &gt; 0) {
            int d = temp % 10;
            digitSum += d;
            digitProd *= d;
            temp /= 10;
        }
        
        if (digitProd == 0) {
            return false;
        }
        
        return n % digitSum == 0 && n % digitProd == 0;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {boolean}
 */
var isDivisible = function(n) {
    let digitSum = 0;
    let digitProd = 1;
    let temp = n;
    
    while (temp &gt; 0) {
        let d = temp % 10;
        digitSum += d;
        digitProd *= d;
        temp = Math.floor(temp / 10);
    }
    
    if (digitProd === 0) {
        return false;
    }
    
    return n % digitSum === 0 && n % digitProd === 0;
};` },
{ name: "TypeScript", lang: "typescript", code: `function isDivisible(n: number): boolean {
    let digitSum = 0;
    let digitProd = 1;
    let temp = n;
    
    while (temp &gt; 0) {
        let d = temp % 10;
        digitSum += d;
        digitProd *= d;
        temp = Math.floor(temp / 10);
    }
    
    if (digitProd === 0) {
        return false;
    }
    
    return n % digitSum === 0 && n % digitProd === 0;
}` },]} />

**Complexity**
- Time: O(log n) - We process each digit once.
- Space: O(1) - We only use a few integer variables for storage.
- Notes: This is the optimal approach for space complexity.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check Divisibility by Digit Sum and Product Solutions","datePublished":"2024-08-28","description":"Determine if an integer is divisible by both the sum and the product of its digits.","keywords":"LeetCode, Check Divisibility by Digit Sum and Product, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the String Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(log n) because we iterate through each digit of the number n."}},{"@type":"Question","name":"What is the space complexity of the Mathematical Modulo approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for variables."}},{"@type":"Question","name":"How do we handle the case where the product of digits is zero?","acceptedAnswer":{"@type":"Answer","text":"If the product of digits is zero, we return false because division by zero is undefined."}}]}).replace(/</g,'\u003c')}
</script>