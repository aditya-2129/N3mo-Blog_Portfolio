---
title: "Largest Number At Least Twice of Others"
summary: "Find the index of the largest integer if it is at least twice as much as every other number in the array."
date: "Nov 01, 2024"
tags:
  - Array
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 52.40% | **Paid:** No
> **Topics:** Array, Sorting

You are given an integer array nums where the largest integer is unique.

Determine whether the largest element in the array is at least twice as much as every other number in the array. If it is, return the index of the largest element, or return -1 otherwise.

- Examples
- Constraints
- [Approach 1: Sorting](#approach-1-sorting)
- [Approach 2: Linear Scan (Two Pass)](#approach-2-linear-scan-two-pass)
- [Approach 3: Linear Scan (One Pass)](#approach-3-linear-scan-one-pass)

## Examples

### Example 1

**Input:**
```text
nums = [3,6,1,0]
```

**Output:**
```text
1
```

**Explanation:**
6 is the largest integer.
For every other number in the array x, 6 is at least twice as big as x.
The index of value 6 is 1, so we return 1.

### Example 2

**Input:**
```text
nums = [1,2,3,4]
```

**Output:**
```text
-1
```

**Explanation:**
4 is less than twice the value of 3, so we return -1.

## Constraints

```text
1 <= nums.length <= 50
0 <= nums[i] <= 100
It is guaranteed that there is a unique largest element in nums.
```

---
## Approach 1: Sorting

**Intuition**
Sort the array to easily access the largest and second largest elements. If the largest is at least twice the second largest, it is automatically at least twice as large as all others.

**Steps**
- Create a list of pairs containing the value and original index.
- Sort this list based on the values.
- Compare the largest value (last element) with the second largest value (second to last element).
- If the condition is met, return the original index of the largest value; otherwise, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def dominantIndex(self, nums: list[int]) -&gt; int:\n        indexed = [(val, i) for i, val in enumerate(nums)]\n        indexed.sort(key=lambda x: x[0])\n        if len(indexed) == 1:\n            return 0\n        if indexed[-1][0] &gt;= 2 * indexed[-2][0]:\n            return indexed[-1][1]\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int dominantIndex(int[] nums) {\n        int n = nums.length;\n        int[][] arr = new int[n][2];\n        for (int i = 0; i &lt; n; i++) {\n            arr[i][0] = nums[i];\n            arr[i][1] = i;\n        }\n        Arrays.sort(arr, (a, b) -&gt; Integer.compare(a[0], b[0]));\n        if (n == 1) return 0;\n        if (arr[n-1][0] &gt;= 2 * arr[n-2][0]) {\n            return arr[n-1][1];\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int dominantIndex(vector&lt;int&gt;& nums) {\n        vector&lt;pair&lt;int, int&gt;&gt; v;\n        for (int i = 0; i &lt; nums.size(); ++i) {\n            v.push_back({nums[i], i});\n        }\n        sort(v.begin(), v.end());\n        if (v.size() == 1) return 0;\n        if (v.back().first &gt;= 2 * v[v.size()-2].first) {\n            return v.back().second;\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    dominantIndex(nums) {\n        let arr = nums.map((val, idx) =&gt; [val, idx]);\n        arr.sort((a, b) =&gt; a[0] - b[0]);\n        if (arr.length === 1) return 0;\n        if (arr[arr.length - 1][0] &gt;= 2 * arr[arr.length - 2][0]) {\n            return arr[arr.length - 1][1];\n        }\n        return -1;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    dominantIndex(nums: number[]): number {\n        let arr = nums.map((val, idx): [number, number] =&gt; [val, idx]);\n        arr.sort((a, b) =&gt; a[0] - b[0]);\n        if (arr.length === 1) return 0;\n        if (arr[arr.length - 1][0] &gt;= 2 * arr[arr.length - 2][0]) {\n            return arr[arr.length - 1][1];\n        }\n        return -1;\n    }\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(n)
- Notes: Sorting takes O(n log n) time. We use O(n) space to store indices.

---
## Approach 2: Linear Scan (Two Pass)

**Intuition**
Find the maximum value and its index in the first pass. Then, iterate through the array again to verify if this maximum is at least twice every other number.

**Steps**
- Iterate through the array to find the maximum value and its index.
- Iterate through the array a second time.
- For each number that is not the maximum, check if the maximum is less than twice that number.
- If any such number exists, return -1. Otherwise, return the index of the maximum.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def dominantIndex(self, nums: list[int]) -&gt; int:\n        if len(nums) == 1:\n            return 0\n        max_val = max(nums)\n        max_idx = nums.index(max_val)\n        for x in nums:\n            if x != max_val and max_val &lt; 2 * x:\n                return -1\n        return max_idx" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int dominantIndex(int[] nums) {\n        if (nums.length == 1) return 0;\n        int maxVal = Integer.MIN_VALUE;\n        int maxIdx = -1;\n        for (int i = 0; i &lt; nums.length; i++) {\n            if (nums[i] &gt; maxVal) {\n                maxVal = nums[i];\n                maxIdx = i;\n            }\n        }\n        for (int x : nums) {\n            if (x != maxVal && maxVal &lt; 2 * x) {\n                return -1;\n            }\n        }\n        return maxIdx;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int dominantIndex(vector&lt;int&gt;& nums) {\n        if (nums.size() == 1) return 0;\n        int maxVal = INT_MIN;\n        int maxIdx = -1;\n        for (int i = 0; i &lt; nums.size(); ++i) {\n            if (nums[i] &gt; maxVal) {\n                maxVal = nums[i];\n                maxIdx = i;\n            }\n        }\n        for (int x : nums) {\n            if (x != maxVal && maxVal &lt; 2 * x) {\n                return -1;\n            }\n        }\n        return maxIdx;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    dominantIndex(nums) {\n        if (nums.length === 1) return 0;\n        let maxVal = -Infinity;\n        let maxIdx = -1;\n        for (let i = 0; i &lt; nums.length; i++) {\n            if (nums[i] &gt; maxVal) {\n                maxVal = nums[i];\n                maxIdx = i;\n            }\n        }\n        for (let x of nums) {\n            if (x !== maxVal && maxVal &lt; 2 * x) {\n                return -1;\n            }\n        }\n        return maxIdx;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    dominantIndex(nums: number[]): number {\n        if (nums.length === 1) return 0;\n        let maxVal = -Infinity;\n        let maxIdx = -1;\n        for (let i = 0; i &lt; nums.length; i++) {\n            if (nums[i] &gt; maxVal) {\n                maxVal = nums[i];\n                maxIdx = i;\n            }\n        }\n        for (let x of nums) {\n            if (x !== maxVal && maxVal &lt; 2 * x) {\n                return -1;\n            }\n        }\n        return maxIdx;\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: We traverse the array twice, which is still linear time. Uses constant extra space.

---
## Approach 3: Linear Scan (One Pass)

**Intuition**
We can find the largest and second largest numbers in a single pass. If the largest is at least twice the second largest, the condition is satisfied for all other numbers (since they are smaller than or equal to the second largest).

**Steps**
- Initialize variables to track the maximum value, its index, and the second maximum value.
- Iterate through the array.
- Update the maximum and second maximum values accordingly when a new maximum is found.
- After the loop, check if the maximum is at least twice the second maximum.
- Return the index of the maximum if true, otherwise -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def dominantIndex(self, nums: list[int]) -&gt; int:\n        max_val = -1\n        second_max = -1\n        max_idx = -1\n        for i, x in enumerate(nums):\n            if x &gt; max_val:\n                second_max = max_val\n                max_val = x\n                max_idx = i\n            elif x &gt; second_max:\n                second_max = x\n        if max_val &gt;= 2 * second_max:\n            return max_idx\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int dominantIndex(int[] nums) {\n        int maxVal = Integer.MIN_VALUE;\n        int secondMax = Integer.MIN_VALUE;\n        int maxIdx = -1;\n        for (int i = 0; i &lt; nums.length; i++) {\n            if (nums[i] &gt; maxVal) {\n                secondMax = maxVal;\n                maxVal = nums[i];\n                maxIdx = i;\n            } else if (nums[i] &gt; secondMax) {\n                secondMax = nums[i];\n            }\n        }\n        if (maxVal &gt;= 2 * secondMax) {\n            return maxIdx;\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int dominantIndex(vector&lt;int&gt;& nums) {\n        int maxVal = INT_MIN;\n        int secondMax = INT_MIN;\n        int maxIdx = -1;\n        for (int i = 0; i &lt; nums.size(); ++i) {\n            if (nums[i] &gt; maxVal) {\n                secondMax = maxVal;\n                maxVal = nums[i];\n                maxIdx = i;\n            } else if (nums[i] &gt; secondMax) {\n                secondMax = nums[i];\n            }\n        }\n        if (maxVal &gt;= 2 * secondMax) {\n            return maxIdx;\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    dominantIndex(nums) {\n        let maxVal = -Infinity;\n        let secondMax = -Infinity;\n        let maxIdx = -1;\n        for (let i = 0; i &lt; nums.length; i++) {\n            if (nums[i] &gt; maxVal) {\n                secondMax = maxVal;\n                maxVal = nums[i];\n                maxIdx = i;\n            } else if (nums[i] &gt; secondMax) {\n                secondMax = nums[i];\n            }\n        }\n        if (maxVal &gt;= 2 * secondMax) {\n            return maxIdx;\n        }\n        return -1;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    dominantIndex(nums: number[]): number {\n        let maxVal = -Infinity;\n        let secondMax = -Infinity;\n        let maxIdx = -1;\n        for (let i = 0; i &lt; nums.length; i++) {\n            if (nums[i] &gt; maxVal) {\n                secondMax = maxVal;\n                maxVal = nums[i];\n                maxIdx = i;\n            } else if (nums[i] &gt; secondMax) {\n                secondMax = nums[i];\n            }\n        }\n        if (maxVal &gt;= 2 * secondMax) {\n            return maxIdx;\n        }\n        return -1;\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: This is the most optimal approach, solving the problem in a single pass with constant space.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Largest Number At Least Twice of Others Solutions","datePublished":"2024-11-01","description":"Find the index of the largest integer if it is at least twice as much as every other number in the array.","keywords":"LeetCode, Largest Number At Least Twice of Others, Easy, Array, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n log n) due to the sorting step."}},{"@type":"Question","name":"What is the most efficient approach for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Linear Scan (One Pass) approach is the most efficient with O(n) time complexity and O(1) space complexity."}},{"@type":"Question","name":"Why do we only need to compare the largest number with the second largest?","acceptedAnswer":{"@type":"Answer","text":"If the largest number is at least twice the second largest, it is automatically at least twice as large as all other numbers since they are smaller than or equal to the second largest."}}]}).replace(/</g,'\u003c')}
</script>