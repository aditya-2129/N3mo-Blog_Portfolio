---
title: "First Matching Character From Both Ends"
summary: "Find the first character that matches when comparing the string from the start and end moving inwards."
date: "Jan 12, 2026"
tags:
  - Two Pointers
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 81.80% | **Paid:** No
> **Topics:** Two Pointers, String

You are given a string `s`. Return the first character that matches when comparing the string from the beginning and the end.

Specifically, you need to compare the character at index `0` with the character at index `n - 1`, then index `1` with index `n - 2`, and so on. The first time you find a pair of characters that are equal, return that character. If no such pair exists after checking all possible pairs, return an empty string.

- Examples
- Constraints
- [Two Pointers](#two-pointers)
- [Reverse String](#reverse-string)

## Examples

Example 1:
```text
Input: s = "abcdcba"
Output: "a"
Explanation: s[0] = 'a' and s[6] = 'a' match. 'a' is returned.
```

Example 2:
```text
Input: s = "abca"
Output: "a"
Explanation: s[0] = 'a' and s[3] = 'a' match. 'a' is returned.
```

Example 3:
```text
Input: s = "abcdef"
Output: ""
Explanation: No characters match from the ends.
```

## Constraints

```text
1 <= s.length <= 100
s consists of lowercase English letters.
```

---
## Two Pointers

**Intuition**
We can solve this efficiently by using two pointers: one starting at the beginning (`left`) and one at the end (`right`) of the string. We move these pointers towards each other, comparing the characters at their respective positions. The first match we encounter is the answer.

**Steps**
- Initialize `left` to 0 and `right` to `s.length - 1`.
- Loop while `left` is less than `right`.
- In each iteration, check if `s[left]` equals `s[right]`.
- If they are equal, return `s[left]` immediately.
- If not, increment `left` and decrement `right`.
- If the loop finishes without finding a match, return an empty string.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def firstMatchingChar(self, s: str) -&gt; str:\n        left, right = 0, len(s) - 1\n        while left &lt; right:\n            if s[left] == s[right]:\n                return s[left]\n            left += 1\n            right -= 1\n        return ''\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String firstMatchingChar(String s) {\n        int left = 0;\n        int right = s.length() - 1;\n        while (left &lt; right) {\n            if (s.charAt(left) == s.charAt(right)) {\n                return String.valueOf(s.charAt(left));\n            }\n            left++;\n            right--;\n        }\n        return \"\";\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string firstMatchingChar(string s) {\n        int left = 0;\n        int right = s.size() - 1;\n        while (left &lt; right) {\n            if (s[left] == s[right]) {\n                return string(1, s[left]);\n            }\n            left++;\n            right--;\n        }\n        return \"\";\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar firstMatchingChar = function(s) {\n    let left = 0;\n    let right = s.length - 1;\n    while (left &lt; right) {\n        if (s[left] === s[right]) {\n            return s[left];\n        }\n        left++;\n        right--;\n    }\n    return \"\";\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function firstMatchingChar(s: string): string {\n    let left = 0;\n    let right = s.length - 1;\n    while (left &lt; right) {\n        if (s[left] === s[right]) {\n            return s[left];\n        }\n        left++;\n        right--;\n    }\n    return \"\";\n}\n" },]} />

**Complexity**
- Time: O(n), where n is the length of the string. In the worst case, we traverse half the string.
- Space: O(1), as we only use two pointers for storage.
- Notes: This is the most optimal approach for this problem.

---
## Reverse String

**Intuition**
Another way to look at the problem is comparing the original string with its reverse. If we iterate through the original string and the reversed string simultaneously, the first index where the characters match corresponds to the first matching character from the ends.

**Steps**
- Create a reversed copy of the string `s`.
- Iterate through the indices of the string.
- Compare `s[i]` with `reversed[i]`.
- If they match, return `s[i]`.
- If the loop ends, return an empty string.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def firstMatchingChar(self, s: str) -&gt; str:\n        reversed_s = s[::-1]\n        for i in range(len(s)):\n            if s[i] == reversed_s[i]:\n                return s[i]\n        return ''\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String firstMatchingChar(String s) {\n        String reversed = new StringBuilder(s).reverse().toString();\n        for (int i = 0; i &lt; s.length(); i++) {\n            if (s.charAt(i) == reversed.charAt(i)) {\n                return String.valueOf(s.charAt(i));\n            }\n        }\n        return \"\";\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string firstMatchingChar(string s) {\n        string reversed = s;\n        reverse(reversed.begin(), reversed.end());\n        for (int i = 0; i &lt; s.size(); i++) {\n            if (s[i] == reversed[i]) {\n                return string(1, s[i]);\n            }\n        }\n        return \"\";\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar firstMatchingChar = function(s) {\n    let reversed = s.split('').reverse().join('');\n    for (let i = 0; i &lt; s.length; i++) {\n        if (s[i] === reversed[i]) {\n            return s[i];\n        }\n    }\n    return \"\";\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function firstMatchingChar(s: string): string {\n    let reversed = s.split('').reverse().join('');\n    for (let i = 0; i &lt; s.length; i++) {\n        if (s[i] === reversed[i]) {\n            return s[i];\n        }\n    }\n    return \"\";\n}\n" },]} />

**Complexity**
- Time: O(n), as reversing a string and iterating both take linear time.
- Space: O(n), because we need to store the reversed copy of the string.
- Notes: While conceptually simple, this uses extra space compared to the Two Pointers approach.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode First Matching Character From Both Ends Solutions","datePublished":"2026-01-12","description":"Find the first character that matches when comparing the string from the start and end moving inwards.","keywords":"LeetCode, First Matching Character From Both Ends, Easy, Two Pointers, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string."}},{"@type":"Question","name":"What is the space complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1), as we only use two pointers for storage."}},{"@type":"Question","name":"What is the space complexity of the Reverse String approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n), because we need to store the reversed copy of the string."}}]}).replace(/</g,'\u003c')}
</script>