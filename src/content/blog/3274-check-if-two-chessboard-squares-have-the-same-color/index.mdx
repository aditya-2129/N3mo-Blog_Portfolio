---
title: "Check if Two Chessboard Squares Have the Same Color"
summary: "Determine if two given chessboard squares have the same color based on their coordinates."
date: "Sep 26, 2024"
tags:
  - Math
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 72.30% | **Paid:** No
> **Topics:** Math, String

You are given two strings, coordinate1 and coordinate2, representing the coordinates of two squares on a chessboard.

A chessboard has 8 rows (ranks) and 8 columns (files). The rows are labeled from 1 to 8, and the columns are labeled from 'a' to 'h'.

The color of a square is determined by the following rules:
- If the sum of the row number and the column number (where 'a' = 1, 'b' = 2, ..., 'h' = 8) is even, the square is white.
- If the sum is odd, the square is black.

Return true if the two squares have the same color, and false otherwise.

- Examples
- Constraints
- [Explicit Index Conversion](#explicit-index-conversion)
- [Direct ASCII Sum](#direct-ascii-sum)

## Examples

### Example 1

```text
Input: coordinate1 = "a1", coordinate2 = "c3"
Output: true
Explanation:
Square "a1" is black, and square "c3" is also black.
```

### Example 2

```text
Input: coordinate1 = "a1", coordinate2 = "h3"
Output: false
Explanation:
Square "a1" is black, while square "h3" is white.
```

## Constraints

```text
coordinate1.length == coordinate2.length == 2
'a' <= coordinate1[0], coordinate2[0] <= 'h'
'1' <= coordinate1[1], coordinate2[1] <= '8'
```

---
## Explicit Index Conversion

**Intuition**
Convert the letter to a column index (0-7) and the number to a row index (0-7), then check if the parity of their sum is the same for both squares.

**Steps**
- Extract the column letter and row number from each coordinate
- Convert the letter to a 0-based index (a=0, b=1, etc.)
- Convert the number to a 0-based index (1=0, 2=1, etc.)
- Check if (row + col) % 2 is equal for both squares

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def checkTwoChessboards(self, coordinate1: str, coordinate2: str) -> bool:
        def get_color(coord):
            col = ord(coord[0]) - ord('a')
            row = int(coord[1]) - 1
            return (row + col) % 2
        
        return get_color(coordinate1) == get_color(coordinate2)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean checkTwoChessboards(String coordinate1, String coordinate2) {
        return getColor(coordinate1) == getColor(coordinate2);
    }
    
    private int getColor(String coord) {
        int col = coord.charAt(0) - 'a';
        int row = coord.charAt(1) - '1';
        return (row + col) % 2;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool checkTwoChessboards(string coordinate1, string coordinate2) {
        return getColor(coordinate1) == getColor(coordinate2);
    }
    
private:
    int getColor(string coord) {
        int col = coord[0] - 'a';
        int row = coord[1] - '1';
        return (row + col) % 2;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} coordinate1
 * @param {string} coordinate2
 * @return {boolean}
 */
var checkTwoChessboards = function(coordinate1, coordinate2) {
    const getColor = (coord) =&gt; {
        const col = coord.charCodeAt(0) - 'a'.charCodeAt(0);
        const row = parseInt(coord[1]) - 1;
        return (row + col) % 2;
    };
    
    return getColor(coordinate1) === getColor(coordinate2);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function checkTwoChessboards(coordinate1: string, coordinate2: string): boolean {
    const getColor = (coord: string): number =&gt; {
        const col = coord.charCodeAt(0) - 'a'.charCodeAt(0);
        const row = parseInt(coord[1]) - 1;
        return (row + col) % 2;
    };
    
    return getColor(coordinate1) === getColor(coordinate2);
}
` },]} />

**Complexity**
- Time: O(1) - Constant time operations
- Space: O(1) - No extra space used
- Notes: Clear and readable approach with explicit conversion

---
## Direct ASCII Sum

**Intuition**
Use ASCII values directly without explicit index conversion. The parity of (ASCII value of letter + ASCII value of digit) determines the color.

**Steps**
- Sum the ASCII value of the letter and the ASCII value of the digit for each coordinate
- Check if the parity of these sums is the same

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def checkTwoChessboards(self, coordinate1: str, coordinate2: str) -> bool:
        return (ord(coordinate1[0]) + ord(coordinate1[1])) % 2 == (ord(coordinate2[0]) + ord(coordinate2[1])) % 2
` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean checkTwoChessboards(String coordinate1, String coordinate2) {
        return (coordinate1.charAt(0) + coordinate1.charAt(1)) % 2 == 
               (coordinate2.charAt(0) + coordinate2.charAt(1)) % 2;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool checkTwoChessboards(string coordinate1, string coordinate2) {
        return (coordinate1[0] + coordinate1[1]) % 2 == 
               (coordinate2[0] + coordinate2[1]) % 2;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} coordinate1
 * @param {string} coordinate2
 * @return {boolean}
 */
var checkTwoChessboards = function(coordinate1, coordinate2) {
    return (coordinate1.charCodeAt(0) + coordinate1.charCodeAt(1)) % 2 === 
           (coordinate2.charCodeAt(0) + coordinate2.charCodeAt(1)) % 2;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function checkTwoChessboards(coordinate1: string, coordinate2: string): boolean {
    return (coordinate1.charCodeAt(0) + coordinate1.charCodeAt(1)) % 2 === 
           (coordinate2.charCodeAt(0) + coordinate2.charCodeAt(1)) % 2;
}
` },]} />

**Complexity**
- Time: O(1) - Constant time operations
- Space: O(1) - No extra space used
- Notes: More concise but slightly less readable

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check if Two Chessboard Squares Have the Same Color Solutions","datePublished":"2024-09-26","description":"Determine if two given chessboard squares have the same color based on their coordinates.","keywords":"LeetCode, Check if Two Chessboard Squares Have the Same Color, Easy, Math, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Explicit Index Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) as we perform constant time operations on fixed-size strings."}},{"@type":"Question","name":"What is the space complexity of the Direct ASCII Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space."}},{"@type":"Question","name":"How do we determine if two chessboard squares have the same color?","acceptedAnswer":{"@type":"Answer","text":"Two squares have the same color if the parity of (row + column) is the same for both squares. We can check this by comparing (row1 + col1) % 2 with (row2 + col2) % 2."}}]}).replace(/</g,'\u003c')}
</script>