---
title: "Compute Alternating Sum"
summary: "Calculate the alternating sum of an integer array where signs alternate between positive and negative starting with positive."
date: "Apr 11, 2024"
tags:
  - Array
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 90.00% | **Paid:** No
> **Topics:** Array, Simulation

You are given an integer array `nums`. Return the alternating sum of `nums`.

The alternating sum is defined as `nums[0] - nums[1] + nums[2] - nums[3] + ...`.

- Examples
- Constraints
- [Iterative Simulation](#iterative-simulation)
- [Functional Reduction](#functional-reduction)
- [Separate Sums](#separate-sums)

## Examples

### Example 1

**Input:**
```text
nums = [1,3,5,7]
```

**Output:**
```text
-4
```

**Explanation:**
Elements at even indices are nums[0] = 1 and nums[2] = 5 because 0 and 2 are even numbers.

Elements at odd indices are nums[1] = 3 and nums[3] = 7 because 1 and 3 are odd numbers.

The alternating sum is nums[0] - nums[1] + nums[2] - nums[3] = 1 - 3 + 5 - 7 = -4.

### Example 2

**Input:**
```text
nums = [100]
```

**Output:**
```text
100
```

**Explanation:**
The only element at even indices is nums[0] = 100 because 0 is an even number.

There are no elements on odd indices.

The alternating sum is nums[0] = 100.

## Constraints

```text
1 <= nums.length <= 50
-100 <= nums[i] <= 100
```

---

## Iterative Simulation

**Intuition**
We iterate through the array once, adding the element to our total if the index is even and subtracting it if the index is odd.

**Steps**
- Initialize a variable `total` to 0.
- Loop through the array with an index `i`.
- Check if `i` is even (`i % 2 == 0`). If so, add `nums[i]` to `total`.
- If `i` is odd, subtract `nums[i]` from `total`.
- Return `total`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def alternateSum(self, nums: list[int]) -&gt; int:\n        total = 0\n        for i in range(len(nums)):\n            if i % 2 == 0:\n                total += nums[i]\n            else:\n                total -= nums[i]\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int alternateSum(int[] nums) {\n        int sum = 0;\n        for (int i = 0; i &lt; nums.length; i++) {\n            if (i % 2 == 0) {\n                sum += nums[i];\n            } else {\n                sum -= nums[i];\n            }\n        }\n        return sum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int alternateSum(vector&lt;int&gt;& nums) {\n        int sum = 0;\n        for (int i = 0; i &lt; nums.size(); i++) {\n            if (i % 2 == 0) {\n                sum += nums[i];\n            } else {\n                sum -= nums[i];\n            }\n        }\n        return sum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar alternateSum = function(nums) {\n    let sum = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (i % 2 === 0) {\n            sum += nums[i];\n        } else {\n            sum -= nums[i];\n        }\n    }\n    return sum;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function alternateSum(nums: number[]): number {\n    let sum = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (i % 2 === 0) {\n            sum += nums[i];\n        } else {\n            sum -= nums[i];\n        }\n    }\n    return sum;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Most straightforward approach.

---

## Functional Reduction

**Intuition**
We can use the built-in reduce (or fold) functions provided by languages to accumulate the result. We pass the index to the reducer to determine the sign.

**Steps**
- Call the reduce function on the array.
- The accumulator starts at 0.
- In the reducer callback, check the index parity.
- Add the value if even, subtract if odd.
- Return the final accumulated value.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from functools import reduce\n\nclass Solution:\n    def alternateSum(self, nums: list[int]) -&gt; int:\n        return reduce(lambda acc, x: acc + x[1] if x[0] % 2 == 0 else acc - x[1], enumerate(nums), 0)" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\n\nclass Solution {\n    public int alternateSum(int[] nums) {\n        return IntStream.range(0, nums.length)\n                .reduce(0, (sum, i) -&gt; i % 2 == 0 ? sum + nums[i] : sum - nums[i]);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;numeric&gt;\n\nclass Solution {\npublic:\n    int alternateSum(vector&lt;int&gt;& nums) {\n        int sum = 0;\n        for (int i = 0; i &lt; nums.size(); ++i) {\n            sum += (i % 2 == 0 ? nums[i] : -nums[i]);\n        }\n        return sum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar alternateSum = function(nums) {\n    return nums.reduce((acc, val, idx) =&gt; {\n        return idx % 2 === 0 ? acc + val : acc - val;\n    }, 0);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function alternateSum(nums: number[]): number {\n    return nums.reduce((acc, val, idx) =&gt; {\n        return idx % 2 === 0 ? acc + val : acc - val;\n    }, 0);\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Concise syntax, though sometimes slightly slower than raw loops due to overhead.

---

## Separate Sums

**Intuition**
The alternating sum is mathematically equivalent to the sum of elements at even indices minus the sum of elements at odd indices. We can calculate these two sums separately.

**Steps**
- Initialize `sumEven` and `sumOdd` to 0.
- Iterate through the array.
- If index is even, add to `sumEven`.
- If index is odd, add to `sumOdd`.
- Return `sumEven - sumOdd`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def alternateSum(self, nums: list[int]) -&gt; int:\n        sum_even = 0\n        sum_odd = 0\n        for i in range(len(nums)):\n            if i % 2 == 0:\n                sum_even += nums[i]\n            else:\n                sum_odd += nums[i]\n        return sum_even - sum_odd" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int alternateSum(int[] nums) {\n        int sumEven = 0;\n        int sumOdd = 0;\n        for (int i = 0; i &lt; nums.length; i++) {\n            if (i % 2 == 0) {\n                sumEven += nums[i];\n            } else {\n                sumOdd += nums[i];\n            }\n        }\n        return sumEven - sumOdd;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int alternateSum(vector&lt;int&gt;& nums) {\n        int sumEven = 0;\n        int sumOdd = 0;\n        for (int i = 0; i &lt; nums.size(); i++) {\n            if (i % 2 == 0) sumEven += nums[i];\n            else sumOdd += nums[i];\n        }\n        return sumEven - sumOdd;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar alternateSum = function(nums) {\n    let sumEven = 0;\n    let sumOdd = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (i % 2 === 0) {\n            sumEven += nums[i];\n        } else {\n            sumOdd += nums[i];\n        }\n    }\n    return sumEven - sumOdd;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function alternateSum(nums: number[]): number {\n    let sumEven = 0;\n    let sumOdd = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (i % 2 === 0) {\n            sumEven += nums[i];\n        } else {\n            sumOdd += nums[i];\n        }\n    }\n    return sumEven - sumOdd;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Uses two variables instead of one, but logically very clear.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Compute Alternating Sum Solutions","datePublished":"2024-04-11","description":"Calculate the alternating sum of an integer array where signs alternate between positive and negative starting with positive.","keywords":"LeetCode, Compute Alternating Sum, Easy, Array, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Iterative Simulation?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of Functional Reduction?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for the accumulator."}},{"@type":"Question","name":"How does the Separate Sums approach work?","acceptedAnswer":{"@type":"Answer","text":"It calculates the sum of elements at even indices and the sum of elements at odd indices separately, then returns the difference between the two sums."}}]}).replace(/</g,'\u003c')}
</script>