---
title: "Find Valid Pair of Adjacent Digits in String"
summary: "Find the first adjacent pair of digits summing to 10, remove them, and return the resulting string."
date: "Jul 04, 2024"
tags:
  - Hash Table
  - String
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 60.90% | **Paid:** No
> **Topics:** Hash Table, String, Counting

You are given a string `num` representing a large integer. A pair of adjacent digits in `num` is called valid if the sum of the two digits equals 10.

Return the resulting string after removing the first occurrence of a valid pair from `num`. If no such pair exists, return `num`.

- Examples
- Constraints
- [Linear Scan](#linear-scan)
- [Regular Expression](#regular-expression)

## Examples

Example 1:
```text
Input: num = "343"
Output: "343"
Explanation: There is no valid pair of adjacent digits summing to 10.
```

Example 2:
```text
Input: num = "191"
Output: "1"
Explanation: The first valid pair is "19" (1 + 9 = 10). Removing "19" results in "1".
```

Example 3:
```text
Input: num = "55"
Output: ""
Explanation: The valid pair is "55" (5 + 5 = 10). Removing "55" results in an empty string.
```

## Constraints

```text
2 <= num.length <= 100
num consists only of digits.
num does not have leading zeros.
```

---
## Linear Scan

**Intuition**
We iterate through the string once, checking every adjacent pair of digits. The first pair we encounter that sums to 10 is the one we must remove. We then construct a new string excluding these two characters.

**Steps**
- Iterate through the string from index `0` to `length - 2`.
- At each index `i`, convert the characters `num[i]` and `num[i+1]` to integers.
- Check if their sum equals 10.
- If it does, return the substring formed by concatenating the part before `i` and the part after `i+2`.
- If the loop completes without finding a valid pair, return the original string.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findValidPair(self, num: str) -&gt; str:\n        for i in range(len(num) - 1):\n            if int(num[i]) + int(num[i+1]) == 10:\n                return num[:i] + num[i+2:]\n        return num" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String findValidPair(String num) {\n        for (int i = 0; i &lt; num.length() - 1; i++) {\n            int sum = (num.charAt(i) - '0') + (num.charAt(i + 1) - '0');\n            if (sum == 10) {\n                return num.substring(0, i) + num.substring(i + 2);\n            }\n        }\n        return num;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string findValidPair(string num) {\n        for (int i = 0; i &lt; num.size() - 1; ++i) {\n            if ((num[i] - '0') + (num[i+1] - '0') == 10) {\n                return num.substr(0, i) + num.substr(i + 2);\n            }\n        }\n        return num;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    findValidPair(num) {\n        for (let i = 0; i &lt; num.length - 1; i++) {\n            if (parseInt(num[i]) + parseInt(num[i+1]) === 10) {\n                return num.slice(0, i) + num.slice(i + 2);\n            }\n        }\n        return num;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    findValidPair(num: string): string {\n        for (let i = 0; i &lt; num.length - 1; i++) {\n            if (parseInt(num[i]) + parseInt(num[i+1]) === 10) {\n                return num.slice(0, i) + num.slice(i + 2);\n            }\n        }\n        return num;\n    }\n}" },]} />

**Complexity**
- Time: O(n), where n is the length of the string. We traverse the string at most once.
- Space: O(n) to store the resulting string (or O(1) auxiliary space if modifying in-place, though strings are often immutable).
- Notes: This is the most efficient approach for this specific problem.

---
## Regular Expression

**Intuition**
We can define a pattern that matches any two adjacent digits summing to 10. Using a regular expression engine, we can search for the first occurrence of this pattern and replace it with an empty string.

**Steps**
- Construct a regex pattern that matches all possible pairs summing to 10: `(19|28|37|46|55|64|73|82|91)`.
- Apply the regex replacement to the input string, replacing only the first match.
- Return the modified string.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import re\n\nclass Solution:\n    def findValidPair(self, num: str) -&gt; str:\n        pattern = r\"(19|28|37|46|55|64|73|82|91)\"\n        return re.sub(pattern, \"\", num, 1)" },
{ name: "Java", lang: "java", code: "import java.util.regex.*;\n\nclass Solution {\n    public String findValidPair(String num) {\n        Pattern p = Pattern.compile(\"(19|28|37|46|55|64|73|82|91)\");\n        Matcher m = p.matcher(num);\n        if (m.find()) {\n            return m.replaceFirst(\"\");\n        }\n        return num;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;regex&gt;\n\nclass Solution {\npublic:\n    string findValidPair(string num) {\n        std::regex r(\"(19|28|37|46|55|64|73|82|91)\");\n        return std::regex_replace(num, r, \"\", std::regex_constants::format_first_only);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    findValidPair(num) {\n        const regex = /(19|28|37|46|55|64|73|82|91)/;\n        return num.replace(regex, \"\");\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    findValidPair(num: string): string {\n        const regex = /(19|28|37|46|55|64|73|82|91)/;\n        return num.replace(regex, \"\");\n    }\n}" },]} />

**Complexity**
- Time: O(n), as the regex engine scans the string.
- Space: O(n), for the regex pattern compilation and the resulting string.
- Notes: While concise, this approach has higher constant overhead compared to a simple linear scan.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find Valid Pair of Adjacent Digits in String Solutions","datePublished":"2024-07-04","description":"Find the first adjacent pair of digits summing to 10, remove them, and return the resulting string.","keywords":"LeetCode, Find Valid Pair of Adjacent Digits in String, Easy, Hash Table, String, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string, as we iterate through the string once."}},{"@type":"Question","name":"What is the space complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) to store the result, or O(1) auxiliary space."}}]}).replace(/</g,'\u003c')}
</script>