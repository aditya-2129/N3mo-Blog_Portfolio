---
title: "Count Prefixes of a Given String"
summary: "Count how many strings in an array are prefixes of a target string."
date: "Jan 30, 2026"
tags:
  - Array
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 74.30% | **Paid:** No
> **Topics:** Array, String

You are given a string array words and a string s.

A string prefix is a string that can be formed by taking the first k characters of s, where k can be any non-negative integer.

Return the number of strings in words that are a prefix of s.

- Examples
- Constraints
- [Built-in String Methods](#built-in-string-methods)
- [Manual Character Comparison](#manual-character-comparison)

## Examples

**Example 1:**

```text
Input: words = ["a","b","c","ab","bc","abc"], s = "abc"
Output: 3
Explanation:
The strings in words which are a prefix of s are "a", "ab", and "abc".
```

**Example 2:**

```text
Input: words = ["a","a"], s = "aa"
Output: 2
Explanation:
Both strings in words are a prefix of s.
```

## Constraints

```text
- 1 <= words.length <= 1000
- 1 <= words[i].length, s.length <= 10
- words[i] and s consist of lowercase English letters only.
```

---
## Built-in String Methods

**Intuition**
Most modern programming languages provide built-in methods to check if a string starts with a specific substring. We can iterate through the list of words and use this method to check each word against the target string.

**Steps**
- Initialize a counter to 0.
- Iterate through each word in the input array.
- For each word, check if the target string starts with the current word.
- If it does, increment the counter.
- Return the counter after processing all words.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def countPrefixes(self, words: List[str], s: str) -&gt; int:\r\n        count = 0\r\n        for w in words:\r\n            if s.startswith(w):\r\n                count += 1\r\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int countPrefixes(String[] words, String s) {\r\n        int count = 0;\r\n        for (String w : words) {\r\n            if (s.startsWith(w)) {\r\n                count++;\r\n            }\r\n        }\r\n        return count;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;string&gt;\r\n\r\nclass Solution {\r\npublic:\r\n    int countPrefixes(std::vector&lt;std::string&gt;& words, std::string s) {\r\n        int count = 0;\r\n        for (const std::string& w : words) {\r\n            if (s.rfind(w, 0) == 0) {\r\n                count++;\r\n            }\r\n        }\r\n        return count;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "var countPrefixes = function(words, s) {\r\n    let count = 0;\r\n    for (let w of words) {\r\n        if (s.startsWith(w)) {\r\n            count++;\r\n        }\r\n    }\r\n    return count;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countPrefixes(words: string[], s: string): number {\r\n    let count = 0;\r\n    for (let w of words) {\r\n        if (s.startsWith(w)) {\r\n            count++;\r\n        }\r\n    }\r\n    return count;\r\n};" },]} />

**Complexity**
- Time: O(N * L) where N is the number of words and L is the maximum length of a word.
- Space: O(1)
- Notes: This approach is concise and leverages optimized standard library functions.

---
## Manual Character Comparison

**Intuition**
If we want to avoid built-in library functions or understand the underlying mechanics, we can manually compare characters. A word is a prefix of s if the word is shorter than or equal to s and every character in the word matches the corresponding character in s.

**Steps**
- Initialize a counter to 0.
- Iterate through each word in the input array.
- If the length of the current word is greater than the length of s, skip it (it cannot be a prefix).
- Otherwise, iterate through the characters of the word.
- Compare each character of the word with the character at the same index in s.
- If any character mismatches, break the loop and move to the next word.
- If all characters match, increment the counter.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def countPrefixes(self, words: List[str], s: str) -&gt; int:\r\n        count = 0\r\n        for w in words:\r\n            if len(w) &gt; len(s):\r\n                continue\r\n            match = True\r\n            for i in range(len(w)):\r\n                if w[i] != s[i]:\r\n                    match = False\r\n                    break\r\n            if match:\r\n                count += 1\r\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int countPrefixes(String[] words, String s) {\r\n        int count = 0;\r\n        for (String w : words) {\r\n            if (w.length() &gt; s.length()) {\r\n                continue;\r\n            }\r\n            boolean match = true;\r\n            for (int i = 0; i &lt; w.length(); i++) {\r\n                if (w.charAt(i) != s.charAt(i)) {\r\n                    match = false;\r\n                    break;\r\n                }\r\n            }\r\n            if (match) {\r\n                count++;\r\n            }\r\n        }\r\n        return count;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;string&gt;\r\n\r\nclass Solution {\r\npublic:\r\n    int countPrefixes(std::vector&lt;std::string&gt;& words, std::string s) {\r\n        int count = 0;\r\n        for (const std::string& w : words) {\r\n            if (w.size() &gt; s.size()) {\r\n                continue;\r\n            }\r\n            bool match = true;\r\n            for (size_t i = 0; i &lt; w.size(); i++) {\r\n                if (w[i] != s[i]) {\r\n                    match = false;\r\n                    break;\r\n                }\r\n            }\r\n            if (match) {\r\n                count++;\r\n            }\r\n        }\r\n        return count;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "var countPrefixes = function(words, s) {\r\n    let count = 0;\r\n    for (let w of words) {\r\n        if (w.length &gt; s.length) {\r\n            continue;\r\n        }\r\n        let match = true;\r\n        for (let i = 0; i &lt; w.length; i++) {\r\n            if (w[i] !== s[i]) {\r\n                match = false;\r\n                break;\r\n            }\r\n        }\r\n        if (match) {\r\n            count++;\r\n        }\r\n    }\r\n    return count;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countPrefixes(words: string[], s: string): number {\r\n    let count = 0;\r\n    for (let w of words) {\r\n        if (w.length &gt; s.length) {\r\n            continue;\r\n        }\r\n        let match = true;\r\n        for (let i = 0; i &lt; w.length; i++) {\r\n            if (w[i] !== s[i]) {\r\n                match = false;\r\n                break;\r\n            }\r\n        }\r\n        if (match) {\r\n            count++;\r\n        }\r\n    }\r\n    return count;\r\n};" },]} />

**Complexity**
- Time: O(N * L) where N is the number of words and L is the maximum length of a word.
- Space: O(1)
- Notes: This approach is fundamental and works in any programming environment without relying on specific library methods.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Count Prefixes of a Given String Solutions","datePublished":"2026-01-30","description":"Count how many strings in an array are prefixes of a target string.","keywords":"LeetCode, Count Prefixes of a Given String, Easy, Array, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Built-in String Methods approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * L), where N is the number of words and L is the maximum length of a word."}},{"@type":"Question","name":"What is the space complexity of the Manual Character Comparison approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a few variables for counting and iteration."}},{"@type":"Question","name":"Why might one choose Manual Character Comparison over built-in methods?","acceptedAnswer":{"@type":"Answer","text":"Manual comparison is useful for understanding the underlying logic or when working in environments where standard library string functions are unavailable or restricted."}}]}).replace(/</g,'\u003c')}
</script>