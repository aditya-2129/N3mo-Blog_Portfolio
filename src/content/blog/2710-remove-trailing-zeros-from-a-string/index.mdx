---
title: "Remove Trailing Zeros From a String"
summary: "Given a positive integer num represented as a string, return the string without any trailing zeros."
date: "Jun 02, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 79.60% | **Paid:** No
> **Topics:** String

Given a positive integer num represented as a string, return the string representation of the integer without any trailing zeros.

## Table of Contents

- Examples
- Constraints
- [Two Pointers Approach](#two-pointers-approach)
- [Built-in String Methods](#built-in-string-methods)
- [Iterative from End](#iterative-from-end)

## Examples

**Example 1:**

```text
Input: num = "51230100"
Output: "512301"
Explanation: Integer "51230100" has 2 trailing zeros, we remove them and return "512301".
```

**Example 2:**

```text
Input: num = "123"
Output: "123"
Explanation: Integer "123" has no trailing zeros, so we return "123".
```

## Constraints

```text
- 1 <= num.length <= 1000
- num consists of only digits.
- num doesn't have any leading zeros.
```

## Two Pointers Approach

**Intuition**
Use two pointers to find the position of the last non-zero character, then extract the substring from start to that position.

**Steps**
- Initialize a pointer at the end of the string
- Move the pointer backwards while the current character is '0'
- Return the substring from index 0 to the pointer position (inclusive)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def removeTrailingZeros(self, num: str) -&gt; str:\n        n = len(num)\n        right = n - 1\n        while right &gt;= 0 and num[right] == '0':\n            right -= 1\n        return num[:right + 1]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String removeTrailingZeros(String num) {\n        int n = num.length();\n        int right = n - 1;\n        while (right &gt;= 0 && num.charAt(right) == '0') {\n            right--;\n        }\n        return num.substring(0, right + 1);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string removeTrailingZeros(string num) {\n        int n = num.length();\n        int right = n - 1;\n        while (right &gt;= 0 && num[right] == '0') {\n            right--;\n        }\n        return num.substr(0, right + 1);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} num\n * @return {string}\n */\nvar removeTrailingZeros = function(num) {\n    let n = num.length;\n    let right = n - 1;\n    while (right &gt;= 0 && num[right] === '0') {\n        right--;\n    }\n    return num.substring(0, right + 1);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function removeTrailingZeros(num: string): string {\n    let n = num.length;\n    let right = n - 1;\n    while (right &gt;= 0 && num[right] === '0') {\n        right--;\n    }\n    return num.substring(0, right + 1);\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for the resulting substring
- Notes: Simple and efficient, uses minimal extra space

## Built-in String Methods

**Intuition**
Leverage built-in string manipulation functions to remove trailing zeros directly.

**Steps**
- Use language-specific methods to strip trailing zeros
- In Python, use rstrip() method
- In Java, use lastIndexOf() or regex
- In JavaScript/TypeScript, use replaceEnd() or regex

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def removeTrailingZeros(self, num: str) -&gt; str:\n        return num.rstrip('0')" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String removeTrailingZeros(String num) {\n        int i = num.length() - 1;\n        while (i &gt;= 0 && num.charAt(i) == '0') {\n            i--;\n        }\n        return num.substring(0, i + 1);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string removeTrailingZeros(string num) {\n        num.erase(find_if(num.rbegin(), num.rend(), \n            [](char c) { return c != '0'; }).base(), num.end());\n        return num;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} num\n * @return {string}\n */\nvar removeTrailingZeros = function(num) {\n    return num.replace(/0+$/, '');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function removeTrailingZeros(num: string): string {\n    return num.replace(/0+$/, '');\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for the resulting string
- Notes: Most concise solution, leverages built-in optimizations

## Iterative from End

**Intuition**
Iterate from the end of the string and build the result by counting trailing zeros, then slice accordingly.

**Steps**
- Count the number of trailing zeros by iterating from the end
- Use the count to determine the end index for slicing
- Return the substring excluding trailing zeros

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def removeTrailingZeros(self, num: str) -&gt; str:\n        count = 0\n        for i in range(len(num) - 1, -1, -1):\n            if num[i] == '0':\n                count += 1\n            else:\n                break\n        return num[:len(num) - count]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String removeTrailingZeros(String num) {\n        int count = 0;\n        for (int i = num.length() - 1; i &gt;= 0; i--) {\n            if (num.charAt(i) == '0') {\n                count++;\n            } else {\n                break;\n            }\n        }\n        return num.substring(0, num.length() - count);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string removeTrailingZeros(string num) {\n        int count = 0;\n        for (int i = num.length() - 1; i &gt;= 0; i--) {\n            if (num[i] == '0') {\n                count++;\n            } else {\n                break;\n            }\n        }\n        return num.substr(0, num.length() - count);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} num\n * @return {string}\n */\nvar removeTrailingZeros = function(num) {\n    let count = 0;\n    for (let i = num.length - 1; i &gt;= 0; i--) {\n        if (num[i] === '0') {\n            count++;\n        } else {\n            break;\n        }\n    }\n    return num.substring(0, num.length - count);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function removeTrailingZeros(num: string): string {\n    let count = 0;\n    for (let i = num.length - 1; i &gt;= 0; i--) {\n        if (num[i] === '0') {\n            count++;\n        } else {\n            break;\n        }\n    }\n    return num.substring(0, num.length - count);\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for the resulting string
- Notes: Clear logic with explicit counting, easy to understand

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Remove Trailing Zeros From a String Solutions","datePublished":"2025-06-02","description":"Given a positive integer num represented as a string, return the string without any trailing zeros.","keywords":"LeetCode, Remove Trailing Zeros From a String, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Two Pointers Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we may traverse the entire string in the worst case."}},{"@type":"Question","name":"What is the space complexity of the Built-in String Methods approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) for the resulting string, as we create a new string without trailing zeros."}},{"@type":"Question","name":"What is the most efficient approach for removing trailing zeros?","acceptedAnswer":{"@type":"Answer","text":"All approaches have O(n) time complexity. The built-in string methods approach is most concise and leverages language-specific optimizations."}}]}).replace(/</g,'\u003c')}
</script>