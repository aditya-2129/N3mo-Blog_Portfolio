---
title: "Most Visited Sector in a Circular Track"
summary: "Given a circular track with n sectors and an array of rounds, find the most visited sectors after completing all rounds."
date: "Jan 08, 2026"
tags:
  - Array
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 59.90% | **Paid:** No
> **Topics:** Array, Simulation

Given an integer n and an integer array rounds. We have a circular track which consists of n sectors labeled from 1 to n. A marathon will be held on this track, the marathon consists of m rounds. The i-th round starts at sector rounds[i - 1] and ends at sector rounds[i]. For example, round 1 starts at sector rounds[0] and ends at sector rounds[1].

Return an array of the most visited sectors sorted in ascending order.

Notice that you circulate the track in ascending order of sector numbers in the counter-clockwise direction (i.e., from sector 1 to sector n, then to sector 1, etc.).

- Examples
- Constraints
- [Mathematical Approach](#mathematical-approach)
- [Simulation Approach](#simulation-approach)

## Examples

### Example 1

**Input:**
```text
n = 4, rounds = [1,3,1,2]
```

**Output:**
```text
[1,2]
```

**Explanation:**
The marathon starts at sector 1. The order of the visited sectors is as follows:
1 --> 2 --> 3 (end of round 1) --> 4 --> 1 (end of round 2) --> 2 (end of round 3 and the marathon)
We can see that both sectors 1 and 2 are visited twice and they are the most visited sectors. Sectors 3 and 4 are visited only once.

### Example 2

**Input:**
```text
n = 2, rounds = [2,1,2,1,2,1,2,1,2]
```

**Output:**
```text
[2]
```

### Example 3

**Input:**
```text
n = 7, rounds = [1,3,5,7]
```

**Output:**
```text
[1,2,3,4,5,6,7]
```

## Constraints

```text
2 <= n <= 100
1 <= m == rounds.length <= 100
1 <= rounds[i] <= n
rounds[i] != rounds[i + 1] for all valid i
```

---

## Mathematical Approach

**Intuition**
The key observation is that the starting sector is visited at the beginning, and the ending sector is visited at the end. All sectors between them (in the direction of travel) are visited exactly once more than other sectors. Therefore, the most visited sectors form a contiguous range from start to end, wrapping around if necessary.

**Steps**
- Get the start sector (rounds[0]) and end sector (rounds[-1])
- If start &lt;= end, return all sectors from start to end
- If start &gt; end, return sectors from 1 to end, then from start to n

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def mostVisited(self, n: int, rounds: List[int]) -&gt; List[int]:\n        start = rounds[0]\n        end = rounds[-1]\n        \n        if start &lt;= end:\n            return list(range(start, end + 1))\n        else:\n            return list(range(1, end + 1)) + list(range(start, n + 1))\n" },
{ name: "Java", lang: "java", code: "import java.util.ArrayList;\nimport java.util.List;\n\nclass Solution {\n    public List&lt;Integer&gt; mostVisited(int n, int[] rounds) {\n        int start = rounds[0];\n        int end = rounds[rounds.length - 1];\n        List&lt;Integer&gt; result = new ArrayList&lt;&gt;();\n        \n        if (start &lt;= end) {\n            for (int i = start; i &lt;= end; i++) {\n                result.add(i);\n            }\n        } else {\n            for (int i = 1; i &lt;= end; i++) {\n                result.add(i);\n            }\n            for (int i = start; i &lt;= n; i++) {\n                result.add(i);\n            }\n        }\n        return result;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; mostVisited(int n, vector&lt;int&gt;& rounds) {\n        int start = rounds[0];\n        int end = rounds.back();\n        vector&lt;int&gt; result;\n        \n        if (start &lt;= end) {\n            for (int i = start; i &lt;= end; i++) {\n                result.push_back(i);\n            }\n        } else {\n            for (int i = 1; i &lt;= end; i++) {\n                result.push_back(i);\n            }\n            for (int i = start; i &lt;= n; i++) {\n                result.push_back(i);\n            }\n        }\n        return result;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    mostVisited(n, rounds) {\n        const start = rounds[0];\n        const end = rounds[rounds.length - 1];\n        const result = [];\n        \n        if (start &lt;= end) {\n            for (let i = start; i &lt;= end; i++) {\n                result.push(i);\n            }\n        } else {\n            for (let i = 1; i &lt;= end; i++) {\n                result.push(i);\n            }\n            for (let i = start; i &lt;= n; i++) {\n                result.push(i);\n            }\n        }\n        return result;\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    mostVisited(n: number, rounds: number[]): number[] {\n        const start = rounds[0];\n        const end = rounds[rounds.length - 1];\n        const result: number[] = [];\n        \n        if (start &lt;= end) {\n            for (let i = start; i &lt;= end; i++) {\n                result.push(i);\n            }\n        } else {\n            for (let i = 1; i &lt;= end; i++) {\n                result.push(i);\n            }\n            for (let i = start; i &lt;= n; i++) {\n                result.push(i);\n            }\n        }\n        return result;\n    }\n}\n" },]} />

**Complexity**
- Time: O(n) for constructing the result array
- Space: O(n) for storing the result
- Notes: This is the optimal solution with constant time logic and linear space for output

---

## Simulation Approach

**Intuition**
Simulate the entire marathon by visiting each sector step by step, counting how many times each sector is visited. Then find all sectors with the maximum visit count.

**Steps**
- Initialize a count array of size n+1
- Start at rounds[0] and increment its count
- For each subsequent round, move sector by sector until reaching the target, incrementing counts
- Find the maximum count and return all sectors with that count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def mostVisited(self, n: int, rounds: List[int]) -&gt; List[int]:\n        count = [0] * (n + 1)\n        current = rounds[0]\n        count[current] += 1\n        \n        for i in range(1, len(rounds)):\n            target = rounds[i]\n            while current != target:\n                current = current % n + 1\n                count[current] += 1\n        \n        max_count = max(count)\n        return [i for i in range(1, n + 1) if count[i] == max_count]\n" },
{ name: "Java", lang: "java", code: "import java.util.ArrayList;\nimport java.util.List;\n\nclass Solution {\n    public List&lt;Integer&gt; mostVisited(int n, int[] rounds) {\n        int[] count = new int[n + 1];\n        int current = rounds[0];\n        count[current]++;\n        \n        for (int i = 1; i &lt; rounds.length; i++) {\n            int target = rounds[i];\n            while (current != target) {\n                current = current % n + 1;\n                count[current]++;\n            }\n        }\n        \n        int maxCount = 0;\n        for (int c : count) {\n            maxCount = Math.max(maxCount, c);\n        }\n        \n        List&lt;Integer&gt; result = new ArrayList&lt;&gt;();\n        for (int i = 1; i &lt;= n; i++) {\n            if (count[i] == maxCount) {\n                result.add(i);\n            }\n        }\n        return result;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; mostVisited(int n, vector&lt;int&gt;& rounds) {\n        vector&lt;int&gt; count(n + 1, 0);\n        int current = rounds[0];\n        count[current]++;\n        \n        for (size_t i = 1; i &lt; rounds.size(); i++) {\n            int target = rounds[i];\n            while (current != target) {\n                current = current % n + 1;\n                count[current]++;\n            }\n        }\n        \n        int maxCount = *max_element(count.begin(), count.end());\n        \n        vector&lt;int&gt; result;\n        for (int i = 1; i &lt;= n; i++) {\n            if (count[i] == maxCount) {\n                result.push_back(i);\n            }\n        }\n        return result;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    mostVisited(n, rounds) {\n        const count = new Array(n + 1).fill(0);\n        let current = rounds[0];\n        count[current]++;\n        \n        for (let i = 1; i &lt; rounds.length; i++) {\n            const target = rounds[i];\n            while (current !== target) {\n                current = current % n + 1;\n                count[current]++;\n            }\n        }\n        \n        const maxCount = Math.max(...count);\n        const result = [];\n        for (let i = 1; i &lt;= n; i++) {\n            if (count[i] === maxCount) {\n                result.push(i);\n            }\n        }\n        return result;\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    mostVisited(n: number, rounds: number[]): number[] {\n        const count: number[] = new Array(n + 1).fill(0);\n        let current = rounds[0];\n        count[current]++;\n        \n        for (let i = 1; i &lt; rounds.length; i++) {\n            const target = rounds[i];\n            while (current !== target) {\n                current = current % n + 1;\n                count[current]++;\n            }\n        }\n        \n        const maxCount = Math.max(...count);\n        const result: number[] = [];\n        for (let i = 1; i &lt;= n; i++) {\n            if (count[i] === maxCount) {\n                result.push(i);\n            }\n        }\n        return result;\n    }\n}\n" },]} />

**Complexity**
- Time: O(m × n) in worst case where m is rounds length
- Space: O(n) for the count array
- Notes: Less efficient than mathematical approach but more intuitive and easier to understand

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Most Visited Sector in  a Circular Track Solutions","datePublished":"2026-01-08","description":"Given a circular track with n sectors and an array of rounds, find the most visited sectors after completing all rounds.","keywords":"LeetCode, Most Visited Sector in  a Circular Track, Easy, Array, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Mathematical Approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for constructing the result array."}},{"@type":"Question","name":"What is the time complexity of the Simulation Approach?","acceptedAnswer":{"@type":"Answer","text":"O(m × n) in worst case where m is the length of rounds array."}},{"@type":"Question","name":"Why is the Mathematical Approach optimal?","acceptedAnswer":{"@type":"Answer","text":"Because it directly computes the result using the observation that most visited sectors form a contiguous range from start to end, avoiding unnecessary simulation."}},{"@type":"Question","name":"What is the space complexity of both approaches?","acceptedAnswer":{"@type":"Answer","text":"O(n) for storing the result array or count array."}}]}).replace(/</g,'\u003c')}
</script>