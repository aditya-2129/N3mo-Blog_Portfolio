---
title: "Concatenation of Array"
summary: "Given an array nums, return an array ans where ans is the concatenation of nums with itself."
date: "Feb 24, 2026"
tags:
  - Array
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 90.40% | **Paid:** No
> **Topics:** Array, Simulation

Given an integer array nums, return an array ans where ans[i] == nums[i] and ans[i + n] == nums[i] for 0 &lt;= i &lt; n, where n is the length of nums. Specifically, ans is the concatenation of two nums arrays.

- Examples
- Constraints
- [Simple Iteration](#simple-iteration)
- [Built-in Concatenation](#built-in-concatenation)
- [Pre-allocation](#pre-allocation)

## Examples

Example 1:
```text
Input: nums = [1,2,1]
Output: [1,2,1,1,2,1]
Explanation: The array ans is formed as follows:
- ans = [nums[0],nums[1],nums[2],nums[0],nums[1],nums[2]]
- ans = [1,2,1,1,2,1]
```

Example 2:
```text
Input: nums = [1,3,2,1]
Output: [1,3,2,1,1,3,2,1]
Explanation: The array ans is formed as follows:
- ans = [nums[0],nums[1],nums[2],nums[3],nums[0],nums[1],nums[2],nums[3]]
- ans = [1,3,2,1,1,3,2,1]
```

## Constraints

```text
n == nums.length
1 <= n <= 1000
1 <= nums[i] <= 1000
```

---
## Simple Iteration

**Intuition**
Iterate through the input array twice and append each element to the result array.

**Steps**
- Create an empty result array
- Loop through nums twice
- Append each element to result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def getConcatenation(self, nums: List[int]) -&gt; List[int]:\n        ans = []\n        for _ in range(2):\n            for num in nums:\n                ans.append(num)\n        return ans" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] getConcatenation(int[] nums) {\n        int n = nums.length;\n        int[] ans = new int[2 * n];\n        for (int i = 0; i &lt; 2 * n; i++) {\n            ans[i] = nums[i % n];\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; getConcatenation(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        vector&lt;int&gt; ans;\n        for (int i = 0; i &lt; 2; i++) {\n            for (int num : nums) {\n                ans.push_back(num);\n            }\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar getConcatenation = function(nums) {\n    let ans = [];\n    for (let i = 0; i &lt; 2; i++) {\n        for (let num of nums) {\n            ans.push(num);\n        }\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function getConcatenation(nums: number[]): number[] {\n    let ans: number[] = [];\n    for (let i = 0; i &lt; 2; i++) {\n        for (let num of nums) {\n            ans.push(num);\n        }\n    }\n    return ans;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Simple and intuitive approach with clear logic

---
## Built-in Concatenation

**Intuition**
Use language-specific built-in methods to concatenate arrays directly.

**Steps**
- Use array concatenation operation
- Return the concatenated result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def getConcatenation(self, nums: List[int]) -&gt; List[int]:\n        return nums + nums" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] getConcatenation(int[] nums) {\n        int n = nums.length;\n        int[] ans = new int[2 * n];\n        System.arraycopy(nums, 0, ans, 0, n);\n        System.arraycopy(nums, 0, ans, n, n);\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; getConcatenation(vector&lt;int&gt;& nums) {\n        vector&lt;int&gt; ans = nums;\n        ans.insert(ans.end(), nums.begin(), nums.end());\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar getConcatenation = function(nums) {\n    return nums.concat(nums);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function getConcatenation(nums: number[]): number[] {\n    return nums.concat(nums);\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Most concise solution using built-in methods

---
## Pre-allocation

**Intuition**
Pre-allocate the result array with the exact required size and fill it in a single pass.

**Steps**
- Create array of size 2n
- Fill first half with nums
- Fill second half with nums

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def getConcatenation(self, nums: List[int]) -&gt; List[int]:\n        n = len(nums)\n        ans = [0] * (2 * n)\n        for i in range(n):\n            ans[i] = nums[i]\n            ans[i + n] = nums[i]\n        return ans" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] getConcatenation(int[] nums) {\n        int n = nums.length;\n        int[] ans = new int[2 * n];\n        for (int i = 0; i &lt; n; i++) {\n            ans[i] = nums[i];\n            ans[i + n] = nums[i];\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; getConcatenation(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        vector&lt;int&gt; ans(2 * n);\n        for (int i = 0; i &lt; n; i++) {\n            ans[i] = nums[i];\n            ans[i + n] = nums[i];\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar getConcatenation = function(nums) {\n    const n = nums.length;\n    const ans = new Array(2 * n);\n    for (let i = 0; i &lt; n; i++) {\n        ans[i] = nums[i];\n        ans[i + n] = nums[i];\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function getConcatenation(nums: number[]): number[] {\n    const n = nums.length;\n    const ans: number[] = new Array(2 * n);\n    for (let i = 0; i &lt; n; i++) {\n        ans[i] = nums[i];\n        ans[i + n] = nums[i];\n    }\n    return ans;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Efficient memory allocation with single loop iteration

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Concatenation of Array Solutions","datePublished":"2026-02-24","description":"Given an array nums, return an array ans where ans is the concatenation of nums with itself.","keywords":"LeetCode, Concatenation of Array, Easy, Array, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Simple Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the input array. We iterate through the array twice."}},{"@type":"Question","name":"What is the space complexity of Built-in Concatenation approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) as we create a new array of size 2n to store the concatenated result."}},{"@type":"Question","name":"What is the time complexity of Pre-allocation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the input array. We fill the pre-allocated array in a single pass."}}]}).replace(/</g,'\u003c')}
</script>