---
title: "Cells with Odd Values in a Matrix"
summary: "Count odd-valued cells in an m x n matrix after incrementing all cells in specified rows and columns."
date: "Mar 19, 2026"
tags:
  - Array
  - Math
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 79.80% | **Paid:** No
> **Topics:** Array, Math, Simulation

There is an m x n matrix that is initialized to all 0's. There is also a 2D array indices where each indices[i] = [ri, ci] represents a 0-indexed location to perform some increment operations on the matrix.

For each location indices[i], do the following:

Increment all the cells on row ri.
Increment all the cells on column ci.
Given m, n, and indices, return the number of odd-valued cells in the matrix after applying the increment to all locations in indices.

- Examples
- Constraints
- [Brute Force Simulation](#brute-force-simulation)
- [Mathematical Approach](#mathematical-approach)

## Examples

```text
Input: m = 2, n = 3, indices = [[0,1],[1,1]]
Output: 6
Explanation: Initial matrix = [[0,0,0],[0,0,0]].
After applying first increment = [[1,2,1],[0,1,0]].
The final matrix is [[1,3,1],[1,3,1]], which contains 6 odd numbers.
```

```text
Input: m = 2, n = 2, indices = [[1,1],[0,0]]
Output: 0
Explanation: Final matrix = [[2,2],[2,2]], which contains 0 odd numbers.
```

## Constraints

```text
1 <= m, n <= 50
1 <= indices.length <= 100
0 <= ri < m
0 <= ci < n
```

---

## Brute Force Simulation

**Intuition**
Simulate each operation directly by creating the matrix and incrementing cells row by row and column by column, then count odd values.

**Steps**
- Initialize an m x n matrix with all zeros
- For each [ri, ci] in indices, increment all cells in row ri and column ci
- Count all cells with odd values and return the count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def oddCells(self, m: int, n: int, indices: list[list[int]]) -&gt; int:\n        matrix = [[0] * n for _ in range(m)]\n        for ri, ci in indices:\n            for j in range(n):\n                matrix[ri][j] += 1\n            for i in range(m):\n                matrix[i][ci] += 1\n        count = 0\n        for i in range(m):\n            for j in range(n):\n                if matrix[i][j] % 2 == 1:\n                    count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int oddCells(int m, int n, int[][] indices) {\n        int[][] matrix = new int[m][n];\n        for (int[] idx : indices) {\n            int ri = idx[0], ci = idx[1];\n            for (int j = 0; j &lt; n; j++) {\n                matrix[ri][j]++;\n            }\n            for (int i = 0; i &lt; m; i++) {\n                matrix[i][ci]++;\n            }\n        }\n        int count = 0;\n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (matrix[i][j] % 2 == 1) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int oddCells(int m, int n, vector&lt;vector&lt;int&gt;&gt;& indices) {\n        vector&lt;vector&lt;int&gt;&gt; matrix(m, vector&lt;int&gt;(n, 0));\n        for (auto& idx : indices) {\n            int ri = idx[0], ci = idx[1];\n            for (int j = 0; j &lt; n; j++) {\n                matrix[ri][j]++;\n            }\n            for (int i = 0; i &lt; m; i++) {\n                matrix[i][ci]++;\n            }\n        }\n        int count = 0;\n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (matrix[i][j] % 2 == 1) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} m\n * @param {number} n\n * @param {number[][]} indices\n * @return {number}\n */\nvar oddCells = function(m, n, indices) {\n    let matrix = Array.from({length: m}, () =&gt; Array(n).fill(0));\n    for (let [ri, ci] of indices) {\n        for (let j = 0; j &lt; n; j++) {\n            matrix[ri][j]++;\n        }\n        for (let i = 0; i &lt; m; i++) {\n            matrix[i][ci]++;\n        }\n    }\n    let count = 0;\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (matrix[i][j] % 2 === 1) {\n                count++;\n            }\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function oddCells(m: number, n: number, indices: number[][]): number {\n    let matrix: number[][] = Array.from({length: m}, () =&gt; Array(n).fill(0));\n    for (let [ri, ci] of indices) {\n        for (let j = 0; j &lt; n; j++) {\n            matrix[ri][j]++;\n        }\n        for (let i = 0; i &lt; m; i++) {\n            matrix[i][ci]++;\n        }\n    }\n    let count = 0;\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (matrix[i][j] % 2 === 1) {\n                count++;\n            }\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(k × (m + n) + m × n) where k is the length of indices
- Space: O(m × n)
- Notes: Simple to understand but uses extra space for the matrix

---

## Mathematical Approach

**Intuition**
A cell (i, j) is odd when the sum of operations on row i and column j is odd. This happens when one count is odd and the other is even.

**Steps**
- Count how many times each row and column is incremented
- Count odd rows and odd columns
- Result = (odd rows × even columns) + (even rows × odd columns)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def oddCells(self, m: int, n: int, indices: list[list[int]]) -&gt; int:\n        row_ops = [0] * m\n        col_ops = [0] * n\n        for ri, ci in indices:\n            row_ops[ri] += 1\n            col_ops[ci] += 1\n        \n        odd_rows = sum(1 for x in row_ops if x % 2 == 1)\n        even_rows = m - odd_rows\n        odd_cols = sum(1 for x in col_ops if x % 2 == 1)\n        even_cols = n - odd_cols\n        \n        return odd_rows * even_cols + even_rows * odd_cols" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int oddCells(int m, int n, int[][] indices) {\n        int[] rowOps = new int[m];\n        int[] colOps = new int[n];\n        for (int[] idx : indices) {\n            rowOps[idx[0]]++;\n            colOps[idx[1]]++;\n        }\n        \n        int oddRows = 0, oddCols = 0;\n        for (int i = 0; i &lt; m; i++) {\n            if (rowOps[i] % 2 == 1) oddRows++;\n        }\n        for (int j = 0; j &lt; n; j++) {\n            if (colOps[j] % 2 == 1) oddCols++;\n        }\n        \n        int evenRows = m - oddRows;\n        int evenCols = n - oddCols;\n        \n        return oddRows * evenCols + evenRows * oddCols;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int oddCells(int m, int n, vector&lt;vector&lt;int&gt;&gt;& indices) {\n        vector&lt;int&gt; rowOps(m, 0);\n        vector&lt;int&gt; colOps(n, 0);\n        for (auto& idx : indices) {\n            rowOps[idx[0]]++;\n            colOps[idx[1]]++;\n        }\n        \n        int oddRows = 0, oddCols = 0;\n        for (int i = 0; i &lt; m; i++) {\n            if (rowOps[i] % 2 == 1) oddRows++;\n        }\n        for (int j = 0; j &lt; n; j++) {\n            if (colOps[j] % 2 == 1) oddCols++;\n        }\n        \n        int evenRows = m - oddRows;\n        int evenCols = n - oddCols;\n        \n        return oddRows * evenCols + evenRows * oddCols;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} m\n * @param {number} n\n * @param {number[][]} indices\n * @return {number}\n */\nvar oddCells = function(m, n, indices) {\n    let rowOps = Array(m).fill(0);\n    let colOps = Array(n).fill(0);\n    for (let [ri, ci] of indices) {\n        rowOps[ri]++;\n        colOps[ci]++;\n    }\n    \n    let oddRows = 0, oddCols = 0;\n    for (let i = 0; i &lt; m; i++) {\n        if (rowOps[i] % 2 === 1) oddRows++;\n    }\n    for (let j = 0; j &lt; n; j++) {\n        if (colOps[j] % 2 === 1) oddCols++;\n    }\n    \n    let evenRows = m - oddRows;\n    let evenCols = n - oddCols;\n    \n    return oddRows * evenCols + evenRows * oddCols;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function oddCells(m: number, n: number, indices: number[][]): number {\n    let rowOps: number[] = Array(m).fill(0);\n    let colOps: number[] = Array(n).fill(0);\n    for (let [ri, ci] of indices) {\n        rowOps[ri]++;\n        colOps[ci]++;\n    }\n    \n    let oddRows = 0, oddCols = 0;\n    for (let i = 0; i &lt; m; i++) {\n        if (rowOps[i] % 2 === 1) oddRows++;\n    }\n    for (let j = 0; j &lt; n; j++) {\n        if (colOps[j] % 2 === 1) oddCols++;\n    }\n    \n    let evenRows = m - oddRows;\n    let evenCols = n - oddCols;\n    \n    return oddRows * evenCols + evenRows * oddCols;\n}" },]} />

**Complexity**
- Time: O(k + m + n) where k is the length of indices
- Space: O(m + n)
- Notes: Optimal solution with minimal space complexity

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Cells with Odd Values in a Matrix Solutions","datePublished":"2026-03-19","description":"Count odd-valued cells in an m x n matrix after incrementing all cells in specified rows and columns.","keywords":"LeetCode, Cells with Odd Values in a Matrix, Easy, Array, Math, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Brute Force Simulation?","acceptedAnswer":{"@type":"Answer","text":"O(k × (m + n) + m × n) where k is the length of indices array."}},{"@type":"Question","name":"What is the time complexity of Mathematical Approach?","acceptedAnswer":{"@type":"Answer","text":"O(k + m + n) where k is the length of indices array."}},{"@type":"Question","name":"What is the space complexity of Mathematical Approach?","acceptedAnswer":{"@type":"Answer","text":"O(m + n) for storing row and column operation counts."}},{"@type":"Question","name":"Why does the mathematical formula work?","acceptedAnswer":{"@type":"Answer","text":"A cell (i,j) is odd when row_ops[i] + col_ops[j] is odd, which happens when one is odd and the other is even."}}]}).replace(/</g,'\u003c')}
</script>