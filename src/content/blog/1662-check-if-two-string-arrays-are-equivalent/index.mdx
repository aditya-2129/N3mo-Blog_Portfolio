---
title: "Check If Two String Arrays are Equivalent"
summary: "Given two string arrays, determine if they represent the same string when concatenated."
date: "Dec 19, 2025"
tags:
  - Array
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 86.10% | **Paid:** No
> **Topics:** Array, String

Given two string arrays word1 and word2, return true if the two arrays represent the same string. A string is represented by an array if the array elements concatenated in order forms the string.

- Examples
- Constraints
- [Concatenation](#concatenation)
- [Two Pointers](#two-pointers)

## Examples

Example 1:
```text
Input: word1 = ["ab", "c"], word2 = ["a", "bc"]
Output: true
Explanation:
word1 represents string "ab" + "c" -> "abc"
word2 represents string "a" + "bc" -> "abc"
The strings are the same, so return true.
```

Example 2:
```text
Input: word1 = ["a", "cb"], word2 = ["ab", "c"]
Output: false
Explanation:
word1 represents string "a" + "cb" -> "acb"
word2 represents string "ab" + "c" -> "abc"
The strings are not the same, so return false.
```

Example 3:
```text
Input: word1  = ["abc", "d", "defg"], word2 = ["abcddefg"]
Output: true
Explanation:
word1 represents string "abc" + "d" + "defg" -> "abcddefg"
word2 represents string "abcddefg"
The strings are the same, so return true.
```

## Constraints

```text
1 <= word1.length, word2.length <= 10³
1 <= word1[i].length, word2[i].length <= 10³
1 <= sum(word1[i].length), sum(word2[i].length) <= 10³
word1[i] and word2[i] consist of lowercase letters.
```

---
## Concatenation

**Intuition**
The most straightforward way to check if two arrays of strings represent the same string is to concatenate all strings in each array into a single string and then compare the results.

**Steps**
- Concatenate all strings in `word1` into a single string `s1`.
- Concatenate all strings in `word2` into a single string `s2`.
- Return `true` if `s1` equals `s2`, otherwise return `false`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def arrayStringsAreEqual(self, word1: list[str], word2: list[str]) -&gt; bool:\n        return \"\".join(word1) == \"\".join(word2)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean arrayStringsAreEqual(String[] word1, String[] word2) {\n        return String.join(\"\", word1).equals(String.join(\"\", word2));\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool arrayStringsAreEqual(vector&lt;string&gt;& word1, vector&lt;string&gt;& word2) {\n        string s1, s2;\n        for (const auto& w : word1) s1 += w;\n        for (const auto& w : word2) s2 += w;\n        return s1 == s2;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var arrayStringsAreEqual = function(word1, word2) {\n    return word1.join(\"\") === word2.join(\"\");\n};" },
{ name: "TypeScript", lang: "typescript", code: "function arrayStringsAreEqual(word1: string[], word2: string[]): boolean {\n    return word1.join(\"\") === word2.join(\"\");\n}" },]} />

**Complexity**
- Time: O(N + M), where N and M are the total number of characters in `word1` and `word2` respectively.
- Space: O(N + M) to store the concatenated strings.
- Notes: This approach is simple and readable but uses extra memory proportional to the input size.

---
## Two Pointers

**Intuition**
To optimize space, we can compare the arrays character by character without constructing the full strings. We use pointers to track the current word and the current character within that word for both arrays.

**Steps**
- Initialize pointers `i1` and `i2` for the word indices in `word1` and `word2`, and `j1` and `j2` for the character indices within the current words.
- While both pointers are within bounds:
    - Compare `word1[i1][j1]` with `word2[i2][j2]`. If they differ, return `false`.
    - Increment `j1` and `j2`.
    - If `j1` reaches the end of `word1[i1]`, move to the next word (`i1++`) and reset `j1` to 0.
    - If `j2` reaches the end of `word2[i2]`, move to the next word (`i2++`) and reset `j2` to 0.
- After the loop, return `true` only if both arrays have been fully traversed (i.e., `i1` equals `word1.length` and `i2` equals `word2.length`).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def arrayStringsAreEqual(self, word1: list[str], word2: list[str]) -&gt; bool:\n        i1 = i2 = 0\n        j1 = j2 = 0\n        while i1 &lt; len(word1) and i2 &lt; len(word2):\n            if word1[i1][j1] != word2[i2][j2]:\n                return False\n            j1 += 1\n            j2 += 1\n            if j1 == len(word1[i1]):\n                i1 += 1\n                j1 = 0\n            if j2 == len(word2[i2]):\n                i2 += 1\n                j2 = 0\n        return i1 == len(word1) and i2 == len(word2)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean arrayStringsAreEqual(String[] word1, String[] word2) {\n        int i1 = 0, i2 = 0, j1 = 0, j2 = 0;\n        while (i1 &lt; word1.length && i2 &lt; word2.length) {\n            if (word1[i1].charAt(j1) != word2[i2].charAt(j2)) return false;\n            j1++;\n            j2++;\n            if (j1 == word1[i1].length()) {\n                i1++;\n                j1 = 0;\n            }\n            if (j2 == word2[i2].length()) {\n                i2++;\n                j2 = 0;\n            }\n        }\n        return i1 == word1.length && i2 == word2.length;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool arrayStringsAreEqual(vector&lt;string&gt;& word1, vector&lt;string&gt;& word2) {\n        int i1 = 0, i2 = 0, j1 = 0, j2 = 0;\n        while (i1 &lt; word1.size() && i2 &lt; word2.size()) {\n            if (word1[i1][j1] != word2[i2][j2]) return false;\n            j1++;\n            j2++;\n            if (j1 == word1[i1].size()) {\n                i1++;\n                j1 = 0;\n            }\n            if (j2 == word2[i2].size()) {\n                i2++;\n                j2 = 0;\n            }\n        }\n        return i1 == word1.size() && i2 == word2.size();\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var arrayStringsAreEqual = function(word1, word2) {\n    let i1 = 0, i2 = 0, j1 = 0, j2 = 0;\n    while (i1 &lt; word1.length && i2 &lt; word2.length) {\n        if (word1[i1][j1] !== word2[i2][j2]) return false;\n        j1++;\n        j2++;\n        if (j1 === word1[i1].length) {\n            i1++;\n            j1 = 0;\n        }\n        if (j2 === word2[i2].length) {\n            i2++;\n            j2 = 0;\n        }\n    }\n    return i1 === word1.length && i2 === word2.length;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function arrayStringsAreEqual(word1: string[], word2: string[]): boolean {\n    let i1 = 0, i2 = 0, j1 = 0, j2 = 0;\n    while (i1 &lt; word1.length && i2 &lt; word2.length) {\n        if (word1[i1][j1] !== word2[i2][j2]) return false;\n        j1++;\n        j2++;\n        if (j1 === word1[i1].length) {\n            i1++;\n            j1 = 0;\n        }\n        if (j2 === word2[i2].length) {\n            i2++;\n            j2 = 0;\n        }\n    }\n    return i1 === word1.length && i2 === word2.length;\n}" },]} />

**Complexity**
- Time: O(N + M), where N and M are the total number of characters in `word1` and `word2` respectively.
- Space: O(1), as we only use a constant amount of extra space for pointers.
- Notes: This is the optimal solution for space efficiency, especially useful when dealing with very large strings.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check If Two String Arrays are Equivalent Solutions","datePublished":"2025-12-19","description":"Given two string arrays, determine if they represent the same string when concatenated.","keywords":"LeetCode, Check If Two String Arrays are Equivalent, Easy, Array, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Concatenation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N + M), where N and M are the total number of characters in word1 and word2 respectively."}},{"@type":"Question","name":"What is the space complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1), as it only uses a constant amount of extra space for pointers."}},{"@type":"Question","name":"Why is the Two Pointers approach considered better?","acceptedAnswer":{"@type":"Answer","text":"The Two Pointers approach is better in terms of space complexity because it avoids creating new strings, using only O(1) extra space compared to O(N + M) for the Concatenation approach."}}]}).replace(/</g,'\u003c')}
</script>