---
title: "Check If a Word Occurs As a Prefix of Any Word in a Sentence"
summary: "Given a sentence and a search word, find if the search word is a prefix of any word in the sentence and return its 1-indexed position."
date: "Jul 10, 2025"
tags:
  - Two Pointers
  - String
  - String Matching
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 68.80% | **Paid:** No
> **Topics:** Two Pointers, String, String Matching

Given a sentence that consists of some words separated by a single space, and a searchWord, check if searchWord is a prefix of any word in sentence.

Return the index of the word in sentence (1-indexed) where searchWord is a prefix of this word. If there is no such word return -1.

A prefix of a string s is any leading contiguous substring of s.

- Examples
- Constraints
- [Split and Compare](#split-and-compare)
- [Two Pointers](#two-pointers)
- [Built-in String Methods](#built-in-string-methods)

## Examples

```text
Input: sentence = "i love eating burger", searchWord = "burg"
Output: 4
Explanation: "burg" is prefix of "burger" which is the 4th word in the sentence.
```

```text
Input: sentence = "this problem is an easy problem", searchWord = "pro"
Output: 2
Explanation: "pro" is prefix of "problem" which is the 2nd and the 6th word in the sentence.
Since there is a 2, we return 2.
```

```text
Input: sentence = "i am tired", searchWord = "you"
Output: -1
Explanation: There is no word in the sentence that has "you" as a prefix.
```

## Constraints

```text
1 <= sentence.length <= 100
1 <= searchWord.length <= 10
sentence consists of lowercase English letters and spaces.
searchWord consists of lowercase English letters.
```

---
## Split and Compare

**Intuition**
Split the sentence into individual words by spaces, then iterate through each word checking if the searchWord is a prefix using string comparison.

**Steps**
- Split the sentence by spaces to get an array of words
- Iterate through each word with its index
- Check if the word starts with searchWord
- Return the 1-indexed position if found, otherwise return -1

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isPrefixOfWord(self, sentence: str, searchWord: str) -&gt; int:\n        words = sentence.split()\n        for i, word in enumerate(words):\n            if word.startswith(searchWord):\n                return i + 1\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int isPrefixOfWord(String sentence, String searchWord) {\n        String[] words = sentence.split(\" \");\n        for (int i = 0; i &lt; words.length; i++) {\n            if (words[i].startsWith(searchWord)) {\n                return i + 1;\n            }\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;sstream&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    int isPrefixOfWord(string sentence, string searchWord) {\n        vector&lt;string&gt; words;\n        string word;\n        stringstream ss(sentence);\n        while (ss &gt;&gt; word) {\n            words.push_back(word);\n        }\n        for (int i = 0; i &lt; words.size(); i++) {\n            if (words[i].find(searchWord) == 0) {\n                return i + 1;\n            }\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} sentence\n * @param {string} searchWord\n * @return {number}\n */\nvar isPrefixOfWord = function(sentence, searchWord) {\n    const words = sentence.split(' ');\n    for (let i = 0; i &lt; words.length; i++) {\n        if (words[i].startsWith(searchWord)) {\n            return i + 1;\n        }\n    }\n    return -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isPrefixOfWord(sentence: string, searchWord: string): number {\n    const words = sentence.split(' ');\n    for (let i = 0; i &lt; words.length; i++) {\n        if (words[i].startsWith(searchWord)) {\n            return i + 1;\n        }\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(n × m) where n is the number of words and m is the length of searchWord
- Space: O(n) for storing the split words
- Notes: Simple and readable, but uses extra space for the array

---
## Two Pointers

**Intuition**
Use two pointers to traverse the sentence without splitting it into an array. Track word boundaries and compare characters directly with the searchWord.

**Steps**
- Initialize word counter and position pointers
- Skip spaces to find word boundaries
- For each word, compare characters with searchWord
- If all characters match, return the current word index
- Otherwise, skip to the next word

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isPrefixOfWord(self, sentence: str, searchWord: str) -&gt; int:\n        word_index = 0\n        i = 0\n        n = len(sentence)\n        m = len(searchWord)\n        \n        while i &lt; n:\n            if sentence[i] == ' ':\n                i += 1\n                continue\n            \n            word_index += 1\n            \n            j = 0\n            while i &lt; n and j &lt; m and sentence[i] == searchWord[j]:\n                i += 1\n                j += 1\n            \n            if j == m:\n                return word_index\n            \n            while i &lt; n and sentence[i] != ' ':\n                i += 1\n        \n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int isPrefixOfWord(String sentence, String searchWord) {\n        int wordIndex = 0;\n        int i = 0;\n        int n = sentence.length();\n        int m = searchWord.length();\n        \n        while (i &lt; n) {\n            if (sentence.charAt(i) == ' ') {\n                i++;\n                continue;\n            }\n            \n            wordIndex++;\n            \n            int j = 0;\n            while (i &lt; n && j &lt; m && sentence.charAt(i) == searchWord.charAt(j)) {\n                i++;\n                j++;\n            }\n            \n            if (j == m) {\n                return wordIndex;\n            }\n            \n            while (i &lt; n && sentence.charAt(i) != ' ') {\n                i++;\n            }\n        }\n        \n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int isPrefixOfWord(string sentence, string searchWord) {\n        int wordIndex = 0;\n        int i = 0;\n        int n = sentence.length();\n        int m = searchWord.length();\n        \n        while (i &lt; n) {\n            if (sentence[i] == ' ') {\n                i++;\n                continue;\n            }\n            \n            wordIndex++;\n            \n            int j = 0;\n            while (i &lt; n && j &lt; m && sentence[i] == searchWord[j]) {\n                i++;\n                j++;\n            }\n            \n            if (j == m) {\n                return wordIndex;\n            }\n            \n            while (i &lt; n && sentence[i] != ' ') {\n                i++;\n            }\n        }\n        \n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} sentence\n * @param {string} searchWord\n * @return {number}\n */\nvar isPrefixOfWord = function(sentence, searchWord) {\n    let wordIndex = 0;\n    let i = 0;\n    const n = sentence.length;\n    const m = searchWord.length;\n    \n    while (i &lt; n) {\n        if (sentence[i] === ' ') {\n            i++;\n            continue;\n        }\n        \n        wordIndex++;\n        \n        let j = 0;\n        while (i &lt; n && j &lt; m && sentence[i] === searchWord[j]) {\n            i++;\n            j++;\n        }\n        \n        if (j === m) {\n            return wordIndex;\n        }\n        \n        while (i &lt; n && sentence[i] !== ' ') {\n            i++;\n        }\n    }\n    \n    return -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isPrefixOfWord(sentence: string, searchWord: string): number {\n    let wordIndex = 0;\n    let i = 0;\n    const n = sentence.length;\n    const m = searchWord.length;\n    \n    while (i &lt; n) {\n        if (sentence[i] === ' ') {\n            i++;\n            continue;\n        }\n        \n        wordIndex++;\n        \n        let j = 0;\n        while (i &lt; n && j &lt; m && sentence[i] === searchWord[j]) {\n            i++;\n            j++;\n        }\n        \n        if (j === m) {\n            return wordIndex;\n        }\n        \n        while (i &lt; n && sentence[i] !== ' ') {\n            i++;\n        }\n    }\n    \n    return -1;\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of sentence
- Space: O(1) constant extra space
- Notes: Optimal space efficiency, avoids creating an array

---
## Built-in String Methods

**Intuition**
Leverage language-specific built-in string methods for prefix checking, which are often optimized internally.

**Steps**
- Split the sentence into words
- Use built-in prefix checking methods (startsWith, indexOf, substring comparison)
- Return the first matching index or -1

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isPrefixOfWord(self, sentence: str, searchWord: str) -&gt; int:\n        for i, word in enumerate(sentence.split(), 1):\n            if word[:len(searchWord)] == searchWord:\n                return i\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int isPrefixOfWord(String sentence, String searchWord) {\n        String[] words = sentence.split(\" \");\n        for (int i = 0; i &lt; words.length; i++) {\n            if (words[i].regionMatches(0, searchWord, 0, searchWord.length())) {\n                return i + 1;\n            }\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int isPrefixOfWord(string sentence, string searchWord) {\n        int wordIndex = 1;\n        int i = 0;\n        int n = sentence.length();\n        int m = searchWord.length();\n        \n        while (i &lt; n) {\n            if (sentence[i] == ' ') {\n                i++;\n                wordIndex++;\n                continue;\n            }\n            \n            bool isPrefix = true;\n            for (int j = 0; j &lt; m && i + j &lt; n; j++) {\n                if (sentence[i + j] != searchWord[j]) {\n                    isPrefix = false;\n                    break;\n                }\n            }\n            \n            if (isPrefix && i + m &lt;= n) {\n                return wordIndex;\n            }\n            \n            while (i &lt; n && sentence[i] != ' ') {\n                i++;\n            }\n        }\n        \n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} sentence\n * @param {string} searchWord\n * @return {number}\n */\nvar isPrefixOfWord = function(sentence, searchWord) {\n    const words = sentence.split(' ');\n    for (let i = 0; i &lt; words.length; i++) {\n        if (words[i].indexOf(searchWord) === 0) {\n            return i + 1;\n        }\n    }\n    return -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isPrefixOfWord(sentence: string, searchWord: string): number {\n    const words = sentence.split(' ');\n    for (let i = 0; i &lt; words.length; i++) {\n        if (words[i].indexOf(searchWord) === 0) {\n            return i + 1;\n        }\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(n × m) where n is the number of words and m is the length of searchWord
- Space: O(n) for storing the split words
- Notes: Clean and idiomatic, uses optimized built-in methods

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check If a Word Occurs As a Prefix of Any Word in a Sentence Solutions","datePublished":"2025-07-10","description":"Given a sentence and a search word, find if the search word is a prefix of any word in the sentence and return its 1-indexed position.","keywords":"LeetCode, Check If a Word Occurs As a Prefix of Any Word in a Sentence, Easy, Two Pointers, String, String Matching"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Split and Compare approach?","acceptedAnswer":{"@type":"Answer","text":"O(n × m) where n is the number of words and m is the length of searchWord"}},{"@type":"Question","name":"What is the space complexity of Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) constant extra space, as it doesn't create an array of words"}},{"@type":"Question","name":"Which approach is most space efficient?","acceptedAnswer":{"@type":"Answer","text":"The Two Pointers approach is most space efficient with O(1) space complexity"}},{"@type":"Question","name":"What built-in methods can be used for prefix checking?","acceptedAnswer":{"@type":"Answer","text":"startsWith in Python/Java/JS/TS, indexOf at position 0 in JS/TS, regionMatches in Java, substring comparison in Python"}}]}).replace(/</g,'\u003c')}
</script>