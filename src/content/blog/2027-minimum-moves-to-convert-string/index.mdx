---
title: "Minimum Moves to Convert String"
summary: "Find the minimum moves to convert all 'X' characters to 'O' by changing three consecutive characters at a time."
date: "Apr 20, 2024"
tags:
  - String
  - Greedy
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 57.90% | **Paid:** No
> **Topics:** String, Greedy

You are given a string s consisting only of the characters 'X' and 'O'.

In one move, you can select exactly three consecutive characters of s and convert them to 'O'. Note that if a character is already 'O', it remains 'O'.

Return the minimum number of moves required so that all the characters of s are converted to 'O'.

- Examples
- Constraints
- [Greedy (Left-to-Right)](#greedy-left-to-right)
- [Greedy (Right-to-Left)](#greedy-right-to-left)
- [Simulation with String Modification](#simulation-with-string-modification)

## Examples

### Example 1
```text
Input: s = "XXX"
Output: 1
Explanation: We can convert the substring "XXX" in one move.
```

### Example 2
```text
Input: s = "XXOX"
Output: 2
Explanation: We can convert "XXO" to "OOO" in the first move. Then convert "OX" to "OO" in the second move.
```

### Example 3
```text
Input: s = "OOOO"
Output: 0
Explanation: There are no 'X's to convert.
```

## Constraints

```text
1 <= s.length <= 1000
s[i] is either 'X' or 'O'.
```

---
## Greedy (Left-to-Right)

**Intuition**
Always start converting from the leftmost 'X' to minimize overlap with future conversions.

**Steps**
- Iterate through the string from left to right
- When encountering an 'X', increment the move count and skip the next two positions
- Continue until the end of the string

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def minimumMoves(self, s: str) -> int:
        moves = 0
        i = 0
        while i &lt; len(s):
            if s[i] == 'X':
                moves += 1
                i += 3
            else:
                i += 1
        return moves` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minimumMoves(String s) {
        int moves = 0;
        int i = 0;
        while (i &lt; s.length()) {
            if (s.charAt(i) == 'X') {
                moves++;
                i += 3;
            } else {
                i++;
            }
        }
        return moves;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minimumMoves(string s) {
        int moves = 0;
        int i = 0;
        while (i &lt; s.length()) {
            if (s[i] == 'X') {
                moves++;
                i += 3;
            } else {
                i++;
            }
        }
        return moves;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number}
 */
var minimumMoves = function(s) {
    let moves = 0;
    let i = 0;
    while (i &lt; s.length) {
        if (s[i] === 'X') {
            moves++;
            i += 3;
        } else {
            i++;
        }
    }
    return moves;
};` },
{ name: "TypeScript", lang: "typescript", code: `function minimumMoves(s: string): number {
    let moves = 0;
    let i = 0;
    while (i &lt; s.length) {
        if (s[i] === 'X') {
            moves++;
            i += 3;
        } else {
            i++;
        }
    }
    return moves;
}` },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1) only using constant extra space
- Notes: Optimal solution with minimal space usage

---
## Greedy (Right-to-Left)

**Intuition**
Similar to left-to-right but traversing from the end, converting 'X' by looking at the three characters ending at current position.

**Steps**
- Iterate through the string from right to left
- When encountering an 'X', increment the move count and skip the previous two positions
- Continue until the beginning of the string

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def minimumMoves(self, s: str) -> int:
        moves = 0
        i = len(s) - 1
        while i &gt;= 0:
            if s[i] == 'X':
                moves += 1
                i -= 3
            else:
                i -= 1
        return moves` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minimumMoves(String s) {
        int moves = 0;
        int i = s.length() - 1;
        while (i &gt;= 0) {
            if (s.charAt(i) == 'X') {
                moves++;
                i -= 3;
            } else {
                i--;
            }
        }
        return moves;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minimumMoves(string s) {
        int moves = 0;
        int i = s.length() - 1;
        while (i &gt;= 0) {
            if (s[i] == 'X') {
                moves++;
                i -= 3;
            } else {
                i--;
            }
        }
        return moves;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number}
 */
var minimumMoves = function(s) {
    let moves = 0;
    let i = s.length - 1;
    while (i &gt;= 0) {
        if (s[i] === 'X') {
            moves++;
            i -= 3;
        } else {
            i--;
        }
    }
    return moves;
};` },
{ name: "TypeScript", lang: "typescript", code: `function minimumMoves(s: string): number {
    let moves = 0;
    let i = s.length - 1;
    while (i &gt;= 0) {
        if (s[i] === 'X') {
            moves++;
            i -= 3;
        } else {
            i--;
        }
    }
    return moves;
}` },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1) only using constant extra space
- Notes: Same complexity as left-to-right, just different traversal direction

---
## Simulation with String Modification

**Intuition**
Actually modify the string as we perform conversions, making the process more explicit and intuitive.

**Steps**
- Convert the string to a mutable data structure
- Iterate through each position
- When finding an 'X', increment moves and convert the current and next two positions to 'O'
- Return the total move count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def minimumMoves(self, s: str) -> int:
        s = list(s)
        moves = 0
        for i in range(len(s)):
            if s[i] == 'X':
                moves += 1
                for j in range(i, min(i + 3, len(s))):
                    s[j] = 'O'
        return moves` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minimumMoves(String s) {
        char[] arr = s.toCharArray();
        int moves = 0;
        for (int i = 0; i &lt; arr.length; i++) {
            if (arr[i] == 'X') {
                moves++;
                for (int j = i; j &lt; Math.min(i + 3, arr.length); j++) {
                    arr[j] = 'O';
                }
            }
        }
        return moves;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minimumMoves(string s) {
        int moves = 0;
        for (int i = 0; i &lt; s.length(); i++) {
            if (s[i] == 'X') {
                moves++;
                for (int j = i; j &lt; min(i + 3, (int)s.length()); j++) {
                    s[j] = 'O';
                }
            }
        }
        return moves;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number}
 */
var minimumMoves = function(s) {
    let arr = s.split('');
    let moves = 0;
    for (let i = 0; i &lt; arr.length; i++) {
        if (arr[i] === 'X') {
            moves++;
            for (let j = i; j &lt; Math.min(i + 3, arr.length); j++) {
                arr[j] = 'O';
            }
        }
    }
    return moves;
};` },
{ name: "TypeScript", lang: "typescript", code: `function minimumMoves(s: string): number {
    let arr = s.split('');
    let moves = 0;
    for (let i = 0; i &lt; arr.length; i++) {
        if (arr[i] === 'X') {
            moves++;
            for (let j = i; j &lt; Math.min(i + 3, arr.length); j++) {
                arr[j] = 'O';
            }
        }
    }
    return moves;
}` },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for the mutable copy of the string
- Notes: More intuitive but uses extra space for string modification

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Moves to Convert String Solutions","datePublished":"2024-04-20","description":"Find the minimum moves to convert all 'X' characters to 'O' by changing three consecutive characters at a time.","keywords":"LeetCode, Minimum Moves to Convert String, Easy, String, Greedy"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Greedy approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we traverse the string once."}},{"@type":"Question","name":"What is the space complexity of the Greedy approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for variables."}},{"@type":"Question","name":"Why is the greedy approach optimal for this problem?","acceptedAnswer":{"@type":"Answer","text":"The greedy approach is optimal because any solution must convert each 'X', and converting from the leftmost 'X' first doesn't prevent us from converting any other 'X' later."}},{"@type":"Question","name":"What is the difference between left-to-right and right-to-left greedy approaches?","acceptedAnswer":{"@type":"Answer","text":"Both approaches have the same time and space complexity. The only difference is the direction of traversal, but both yield the same optimal result."}}]}).replace(/</g,'\u003c')}
</script>