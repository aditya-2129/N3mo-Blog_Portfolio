---
title: "Root Equals Sum of Children"
summary: "Check if the root node's value equals the sum of its left and right child values in a binary tree."
date: "Jul 17, 2025"
tags:
  - Tree
  - Binary Tree
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 84.90% | **Paid:** No
> **Topics:** Tree, Binary Tree

You are given the root of a binary tree that consists of exactly three nodes: the root, its left child, and its right child.

Return true if the value of the root is equal to the sum of the values of its two children, or false otherwise.

- Examples
- Constraints
- [Direct Property Access](#direct-property-access)
- [Recursive Approach](#recursive-approach)

## Examples

**Example 1:**

```text
Input: root = [10,4,6]
Output: true
Explanation: The values of the root, its left child, and its right child are 10, 4, and 6, respectively.
10 is equal to 4 + 6, so we return true.
```

**Example 2:**

```text
Input: root = [5,3,1]
Output: false
Explanation: The values of the root, its left child, and its right child are 5, 3, and 1, respectively.
5 is not equal to 3 + 1, so we return false.
```

## Constraints

```text
The tree consists only of the root, its left child, and its right child.
-100 <= Node.val <= 100
```

---
## Direct Property Access

**Intuition**
Since the problem guarantees the tree consists of exactly three nodes (root, left, right), we can directly access the values of these nodes and perform a simple arithmetic check.

**Steps**
- Access the value of the root node.
- Access the value of the left child node.
- Access the value of the right child node.
- Return the result of the comparison: `root.val == left.val + right.val`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkTree(self, root: Optional[TreeNode]) -&gt; bool:\n        return root.val == root.left.val + root.right.val" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkTree(TreeNode root) {\n        return root.val == root.left.val + root.right.val;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkTree(TreeNode* root) {\n        return root-&gt;val == root-&gt;left-&gt;val + root-&gt;right-&gt;val;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var checkTree = function(root) {\n    return root.val === root.left.val + root.right.val;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkTree(root: TreeNode | null): boolean {\n    return root.val === root.left.val + root.right.val;\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This is the most optimal approach as it performs a constant number of operations.

---
## Recursive Approach

**Intuition**
Although the tree is small, we can treat this as a general tree problem where we check a condition at the current node. This approach demonstrates how one might verify a property for a node in a larger tree structure.

**Steps**
- Define a helper function or use the main function to accept a node.
- Check if the current node's value equals the sum of its children's values.
- Since the tree is guaranteed to have exactly 3 nodes, we only need to perform this check at the root.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkTree(self, root: Optional[TreeNode]) -&gt; bool:\n        # Base case: if node is None (not expected per constraints)\n        if not root:\n            return True\n        # Check the sum condition\n        return root.val == root.left.val + root.right.val" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkTree(TreeNode root) {\n        if (root == null) return true;\n        return root.val == root.left.val + root.right.val;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkTree(TreeNode* root) {\n        if (!root) return true;\n        return root-&gt;val == root-&gt;left-&gt;val + root-&gt;right-&gt;val;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var checkTree = function(root) {\n    if (!root) return true;\n    return root.val === root.left.val + root.right.val;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkTree(root: TreeNode | null): boolean {\n    if (!root) return true;\n    return root.val === root.left.val + root.right.val;\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: While functionally similar to the direct approach, recursion adds overhead to the call stack, though here the depth is only 1.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Root Equals Sum of Children Solutions","datePublished":"2025-07-17","description":"Check if the root node's value equals the sum of its left and right child values in a binary tree.","keywords":"LeetCode, Root Equals Sum of Children, Easy, Tree, Binary Tree"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Direct Property Access approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) because we perform a constant number of arithmetic operations and property accesses regardless of input size."}},{"@type":"Question","name":"What is the space complexity of the Recursive Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) auxiliary space, but O(H) stack space where H is the height of the tree. Since the tree has exactly 3 nodes, H is 1, so it is effectively O(1)."}}]}).replace(/</g,'\u003c')}
</script>