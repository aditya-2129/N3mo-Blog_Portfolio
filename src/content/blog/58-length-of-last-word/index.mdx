---
title: "Length of Last Word"
summary: "Given a string s, return the length of the last word in the string."
date: "Aug 17, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 58.80% | **Paid:** No
> **Topics:** String

Given a string s consisting of words and spaces, return the length of the last word in the string.

A word is a maximal substring consisting of non-space characters only.

- Examples
- Constraints
- [Built-in Functions](#built-in-functions)
- [Reverse Iteration](#reverse-iteration)
- [Two Pointers](#two-pointers)

## Examples

### Example 1
```text
Input: s = "Hello World"
Output: 5
Explanation: The last word is "World" with length 5.
```

### Example 2
```text
Input: s = "   fly me   to   the moon  "
Output: 4
Explanation: The last word is "moon" with length 4.
```

### Example 3
```text
Input: s = "luffy is still joyboy"
Output: 6
Explanation: The last word is "joyboy" with length 6.
```

## Constraints

```text
1 <= s.length <= 10⁴
s consists of only English letters and spaces ' '.
There will be at least one word in s.
```

---
## Built-in Functions

**Intuition**
Use built-in string functions to trim trailing spaces, split the string into words, and return the length of the last word.

**Steps**
- Trim trailing spaces from the string
- Split the string by spaces
- Return the length of the last element in the resulting array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        return len(s.rstrip().split()[-1])
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int lengthOfLastWord(String s) {
        s = s.trim();
        String[] words = s.split(" ");
        return words[words.length - 1].length();
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int lengthOfLastWord(string s) {
        int end = s.find_last_not_of(' ');
        int start = s.find_last_of(' ', end);
        return end - start;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLastWord = function(s) {
    const trimmed = s.trimEnd();
    const words = trimmed.split(' ');
    return words[words.length - 1].length;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function lengthOfLastWord(s: string): number {
    const trimmed = s.trimEnd();
    const words = trimmed.split(' ');
    return words[words.length - 1].length;
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Simple and readable, but uses extra space for the split array.

---
## Reverse Iteration

**Intuition**
Start from the end of the string and count characters until we encounter a space, skipping any trailing spaces first.

**Steps**
- Start from the last character
- Skip all trailing spaces
- Count characters until we hit a space or reach the beginning
- Return the count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        i = len(s) - 1
        while i &gt;= 0 and s[i] == ' ':
            i -= 1
        length = 0
        while i &gt;= 0 and s[i] != ' ':
            length += 1
            i -= 1
        return length
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int lengthOfLastWord(String s) {
        int i = s.length() - 1;
        while (i &gt;= 0 && s.charAt(i) == ' ') {
            i--;
        }
        int length = 0;
        while (i &gt;= 0 && s.charAt(i) != ' ') {
            length++;
            i--;
        }
        return length;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int lengthOfLastWord(string s) {
        int i = s.size() - 1;
        while (i &gt;= 0 && s[i] == ' ') i--;
        int length = 0;
        while (i &gt;= 0 && s[i] != ' ') {
            length++;
            i--;
        }
        return length;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLastWord = function(s) {
    let i = s.length - 1;
    while (i &gt;= 0 && s[i] === ' ') {
        i--;
    }
    let length = 0;
    while (i &gt;= 0 && s[i] !== ' ') {
        length++;
        i--;
    }
    return length;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function lengthOfLastWord(s: string): number {
    let i = s.length - 1;
    while (i &gt;= 0 && s[i] === ' ') {
        i--;
    }
    let length = 0;
    while (i &gt;= 0 && s[i] !== ' ') {
        length++;
        i--;
    }
    return length;
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal space complexity with a single pass from the end.

---
## Two Pointers

**Intuition**
Use two pointers to mark the start and end of the last word, then calculate the difference.

**Steps**
- Move the end pointer to skip trailing spaces
- Move the start pointer from end to find the beginning of the last word
- Return the difference between end and start

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        end = len(s) - 1
        while end &gt;= 0 and s[end] == ' ':
            end -= 1
        start = end
        while start &gt;= 0 and s[start] != ' ':
            start -= 1
        return end - start
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int lengthOfLastWord(String s) {
        int end = s.length() - 1;
        while (end &gt;= 0 && s.charAt(end) == ' ') {
            end--;
        }
        int start = end;
        while (start &gt;= 0 && s.charAt(start) != ' ') {
            start--;
        }
        return end - start;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int lengthOfLastWord(string s) {
        int end = s.size() - 1;
        while (end &gt;= 0 && s[end] == ' ') end--;
        int start = end;
        while (start &gt;= 0 && s[start] != ' ') start--;
        return end - start;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLastWord = function(s) {
    let end = s.length - 1;
    while (end &gt;= 0 && s[end] === ' ') {
        end--;
    }
    let start = end;
    while (start &gt;= 0 && s[start] !== ' ') {
        start--;
    }
    return end - start;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function lengthOfLastWord(s: string): number {
    let end = s.length - 1;
    while (end &gt;= 0 && s[end] === ' ') {
        end--;
    }
    let start = end;
    while (start &gt;= 0 && s[start] !== ' ') {
        start--;
    }
    return end - start;
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Similar to reverse iteration but uses two pointers for clarity.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Length of Last Word Solutions","datePublished":"2025-08-17","description":"Given a string s, return the length of the last word in the string.","keywords":"LeetCode, Length of Last Word, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Built-in Functions approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we need to traverse the string for trimming and splitting."}},{"@type":"Question","name":"What is the space complexity of the Reverse Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for variables."}},{"@type":"Question","name":"Which approach is most optimal for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Reverse Iteration and Two Pointers approaches are most optimal with O(n) time and O(1) space complexity."}}]}).replace(/</g,'\u003c')}
</script>