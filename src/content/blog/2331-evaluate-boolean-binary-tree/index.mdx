---
title: "Evaluate Boolean Binary Tree"
summary: "Evaluate boolean expression represented by a binary tree where leaves are 0/1 and internal nodes are OR/AND operations."
date: "Oct 13, 2024"
tags:
  - Tree
  - Depth-First Search
  - Binary Tree
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.40% | **Paid:** No
> **Topics:** Tree, Depth-First Search, Binary Tree

You are given the root of a full binary tree with the following properties:

Leaf nodes have either value 0 or 1, where 0 represents False and 1 represents True.
Non-leaf nodes have either value 2 or 3, where 2 represents the boolean OR and 3 represents the boolean AND.

The evaluation of a node is as follows:

If the node is a leaf node, the evaluation is the value of the node, i.e., True or False.
Otherwise, evaluate the node's two children and apply the boolean operation of its value with the children's evaluations.

Return the boolean result of evaluating the root node.

A full binary tree is a binary tree where each node has either 0 or 2 children.

- Examples
- Constraints
- [Recursive DFS](#recursive-dfs)
- [Iterative DFS](#iterative-dfs)
- [BFS (Bottom-up)](#bfs-bottom-up)

## Examples

**Example 1:**

```text
Input: root = [2,1,3,null,null,0,1]
```

```text
Output: true
```

```text
Explanation: The AND node evaluates to False AND True = False.
The OR node evaluates to True OR False = True.
The root node evaluates to True, so we return true.
```

**Example 2:**

```text
Input: root = [0]
```

```text
Output: false
```

```text
Explanation: The root node is a leaf node with value false, so we return false.
```

## Constraints

```text
The number of nodes in the tree is in the range [1, 1000].
0 <= Node.val <= 3
Every node has either 0 or 2 children.
Leaf nodes have a value of 0 or 1.
Non-leaf nodes have a value of 2 or 3.
```

---
## Recursive DFS

**Intuition**
Use post-order traversal to evaluate children first, then apply the operation at the current node.

**Steps**
- If the node is a leaf (no children), return its boolean value.
- Recursively evaluate the left and right subtrees.
- Apply the operation (OR for value 2, AND for value 3) to the children's results.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def evaluateTree(self, root: Optional[TreeNode]) -> bool:
        if not root.left and not root.right:
            return root.val == 1
        
        left_val = self.evaluateTree(root.left)
        right_val = self.evaluateTree(root.right)
        
        if root.val == 2:
            return left_val or right_val
        else:  # root.val == 3
            return left_val and right_val
` },
{ name: "Java", lang: "java", code: `/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public boolean evaluateTree(TreeNode root) {
        if (root.left == null && root.right == null) {
            return root.val == 1;
        }
        
        boolean leftVal = evaluateTree(root.left);
        boolean rightVal = evaluateTree(root.right);
        
        if (root.val == 2) {
            return leftVal || rightVal;
        } else { // root.val == 3
            return leftVal && rightVal;
        }
    }
}
` },
{ name: "C++", lang: "cpp", code: `/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    bool evaluateTree(TreeNode* root) {
        if (root->left == nullptr && root->right == nullptr) {
            return root->val == 1;
        }
        
        bool leftVal = evaluateTree(root->left);
        bool rightVal = evaluateTree(root->right);
        
        if (root->val == 2) {
            return leftVal || rightVal;
        } else { // root->val == 3
            return leftVal && rightVal;
        }
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {boolean}
 */
var evaluateTree = function(root) {
    if (root.left === null && root.right === null) {
        return root.val === 1;
    }
    
    let leftVal = evaluateTree(root.left);
    let rightVal = evaluateTree(root.right);
    
    if (root.val === 2) {
        return leftVal || rightVal;
    } else { // root.val === 3
        return leftVal && rightVal;
    }
};
` },
{ name: "TypeScript", lang: "typescript", code: `/**
 * Definition for a binary tree node.
 * class TreeNode {
 *     val: number
 *     left: TreeNode | null
 *     right: TreeNode | null
 *     constructor(val?: number, left?: TreeNode | null, right?: TreeNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.left = (left===undefined ? null : left)
 *         this.right = (right===undefined ? null : right)
 *     }
 * }
 */
function evaluateTree(root: TreeNode | null): boolean {
    if (root.left === null && root.right === null) {
        return root.val === 1;
    }
    
    let leftVal = evaluateTree(root.left);
    let rightVal = evaluateTree(root.right);
    
    if (root.val === 2) {
        return leftVal || rightVal;
    } else { // root.val === 3
        return leftVal && rightVal;
    }
}
` },]} />

**Complexity**
- Time: O(n) where n is the number of nodes
- Space: O(h) where h is the height of the tree (recursion stack)
- Notes: Clean and intuitive, but may cause stack overflow for very deep trees

---
## Iterative DFS

**Intuition**
Simulate the recursive approach using an explicit stack to avoid potential stack overflow.

**Steps**
- Use a stack to perform post-order traversal with a visited flag.
- Store computed values in a map for reuse.
- Process nodes after their children have been evaluated.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def evaluateTree(self, root: Optional[TreeNode]) -> bool:
        stack = [(root, False)]
        values = {}
        
        while stack:
            node, visited = stack.pop()
            
            if not node.left and not node.right:
                values[node] = node.val == 1
            elif visited:
                left_val = values[node.left]
                right_val = values[node.right]
                if node.val == 2:
                    values[node] = left_val or right_val
                else:
                    values[node] = left_val and right_val
            else:
                stack.append((node, True))
                stack.append((node.right, False))
                stack.append((node.left, False))
        
        return values[root]
` },
{ name: "Java", lang: "java", code: `/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
import java.util.*;

class Solution {
    public boolean evaluateTree(TreeNode root) {
        Stack<Object[]&gt; stack = new Stack&lt;&gt;();
        Map<TreeNode, Boolean> values = new HashMap&lt;&gt;();
        
        stack.push(new Object[]{root, false});
        
        while (!stack.isEmpty()) {
            Object[] pair = stack.pop();
            TreeNode node = (TreeNode) pair[0];
            boolean visited = (boolean) pair[1];
            
            if (node.left == null && node.right == null) {
                values.put(node, node.val == 1);
            } else if (visited) {
                boolean leftVal = values.get(node.left);
                boolean rightVal = values.get(node.right);
                if (node.val == 2) {
                    values.put(node, leftVal || rightVal);
                } else {
                    values.put(node, leftVal && rightVal);
                }
            } else {
                stack.push(new Object[]{node, true});
                stack.push(new Object[]{node.right, false});
                stack.push(new Object[]{node.left, false});
            }
        }
        
        return values.get(root);
    }
}
` },
{ name: "C++", lang: "cpp", code: `/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    bool evaluateTree(TreeNode* root) {
        stack<pair<TreeNode*, bool>&gt; st;
        unordered_map<TreeNode*, bool> values;
        
        st.push({root, false});
        
        while (!st.empty()) {
            auto [node, visited] = st.top();
            st.pop();
            
            if (node->left == nullptr && node->right == nullptr) {
                values[node] = node->val == 1;
            } else if (visited) {
                bool leftVal = values[node->left];
                bool rightVal = values[node->right];
                if (node->val == 2) {
                    values[node] = leftVal || rightVal;
                } else {
                    values[node] = leftVal && rightVal;
                }
            } else {
                st.push({node, true});
                st.push({node->right, false});
                st.push({node->left, false});
            }
        }
        
        return values[root];
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {boolean}
 */
var evaluateTree = function(root) {
    let stack = [[root, false]];
    let values = new Map();
    
    while (stack.length &gt; 0) {
        let [node, visited] = stack.pop();
        
        if (node.left === null && node.right === null) {
            values.set(node, node.val === 1);
        } else if (visited) {
            let leftVal = values.get(node.left);
            let rightVal = values.get(node.right);
            if (node.val === 2) {
                values.set(node, leftVal || rightVal);
            } else {
                values.set(node, leftVal && rightVal);
            }
        } else {
            stack.push([node, true]);
            stack.push([node.right, false]);
            stack.push([node.left, false]);
        }
    }
    
    return values.get(root);
};
` },
{ name: "TypeScript", lang: "typescript", code: `/**
 * Definition for a binary tree node.
 * class TreeNode {
 *     val: number
 *     left: TreeNode | null
 *     right: TreeNode | null
 *     constructor(val?: number, left?: TreeNode | null, right?: TreeNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.left = (left===undefined ? null : left)
 *         this.right = (right===undefined ? null : right)
 *     }
 * }
 */
function evaluateTree(root: TreeNode | null): boolean {
    let stack: [TreeNode, boolean][] = [[root, false]];
    let values = new Map<TreeNode, boolean>();
    
    while (stack.length &gt; 0) {
        let [node, visited] = stack.pop();
        
        if (node.left === null && node.right === null) {
            values.set(node, node.val === 1);
        } else if (visited) {
            let leftVal = values.get(node.left);
            let rightVal = values.get(node.right);
            if (node.val === 2) {
                values.set(node, leftVal || rightVal);
            } else {
                values.set(node, leftVal && rightVal);
            }
        } else {
            stack.push([node, true]);
            stack.push([node.right, false]);
            stack.push([node.left, false]);
        }
    }
    
    return values.get(root);
}
` },]} />

**Complexity**
- Time: O(n) where n is the number of nodes
- Space: O(n) for the stack and values map
- Notes: Avoids recursion stack overflow but uses more memory

---
## BFS (Bottom-up)

**Intuition**
Process nodes level by level from bottom to top, evaluating leaves first and propagating values up.

**Steps**
- Perform level-order traversal to collect all levels.
- Process levels from bottom to top.
- For each node, if it's a leaf, store its boolean value; otherwise, compute from children.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
from collections import deque

class Solution:
    def evaluateTree(self, root: Optional[TreeNode]) -> bool:
        if not root.left and not root.right:
            return root.val == 1
        
        levels = []
        queue = deque([root])
        
        while queue:
            level_size = len(queue)
            level = []
            for _ in range(level_size):
                node = queue.popleft()
                level.append(node)
                if node.left:
                    queue.append(node.left)
                if node.right:
                    queue.append(node.right)
            levels.append(level)
        
        values = {}
        
        for level in reversed(levels):
            for node in level:
                if not node.left and not node.right:
                    values[node] = node.val == 1
                else:
                    left_val = values[node.left]
                    right_val = values[node.right]
                    if node.val == 2:
                        values[node] = left_val or right_val
                    else:
                        values[node] = left_val and right_val
        
        return values[root]
` },
{ name: "Java", lang: "java", code: `/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
import java.util.*;

class Solution {
    public boolean evaluateTree(TreeNode root) {
        if (root.left == null && root.right == null) {
            return root.val == 1;
        }
        
        List<List<TreeNode>&gt; levels = new ArrayList&lt;&gt;();
        Queue<TreeNode> queue = new LinkedList&lt;&gt;();
        queue.offer(root);
        
        while (!queue.isEmpty()) {
            int levelSize = queue.size();
            List<TreeNode> level = new ArrayList&lt;&gt;();
            for (int i = 0; i &lt; levelSize; i++) {
                TreeNode node = queue.poll();
                level.add(node);
                if (node.left != null) {
                    queue.offer(node.left);
                }
                if (node.right != null) {
                    queue.offer(node.right);
                }
            }
            levels.add(level);
        }
        
        Map<TreeNode, Boolean> values = new HashMap&lt;&gt;();
        
        for (int i = levels.size() - 1; i &gt;= 0; i--) {
            for (TreeNode node : levels.get(i)) {
                if (node.left == null && node.right == null) {
                    values.put(node, node.val == 1);
                } else {
                    boolean leftVal = values.get(node.left);
                    boolean rightVal = values.get(node.right);
                    if (node.val == 2) {
                        values.put(node, leftVal || rightVal);
                    } else {
                        values.put(node, leftVal && rightVal);
                    }
                }
            }
        }
        
        return values.get(root);
    }
}
` },
{ name: "C++", lang: "cpp", code: `/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    bool evaluateTree(TreeNode* root) {
        if (root->left == nullptr && root->right == nullptr) {
            return root->val == 1;
        }
        
        vector<vector<TreeNode*&gt;&gt; levels;
        queue<TreeNode*&gt; q;
        q.push(root);
        
        while (!q.empty()) {
            int levelSize = q.size();
            vector<TreeNode*&gt; level;
            for (int i = 0; i &lt; levelSize; i++) {
                TreeNode* node = q.front();
                q.pop();
                level.push_back(node);
                if (node->left != nullptr) {
                    q.push(node->left);
                }
                if (node->right != nullptr) {
                    q.push(node->right);
                }
            }
            levels.push_back(level);
        }
        
        unordered_map<TreeNode*, bool> values;
        
        for (int i = levels.size() - 1; i &gt;= 0; i--) {
            for (TreeNode* node : levels[i]) {
                if (node->left == nullptr && node->right == nullptr) {
                    values[node] = node->val == 1;
                } else {
                    bool leftVal = values[node->left];
                    bool rightVal = values[node->right];
                    if (node->val == 2) {
                        values[node] = leftVal || rightVal;
                    } else {
                        values[node] = leftVal && rightVal;
                    }
                }
            }
        }
        
        return values[root];
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {boolean}
 */
var evaluateTree = function(root) {
    if (root.left === null && root.right === null) {
        return root.val === 1;
    }
    
    let levels = [];
    let queue = [root];
    
    while (queue.length &gt; 0) {
        let levelSize = queue.length;
        let level = [];
        for (let i = 0; i &lt; levelSize; i++) {
            let node = queue.shift();
            level.push(node);
            if (node.left !== null) {
                queue.push(node.left);
            }
            if (node.right !== null) {
                queue.push(node.right);
            }
        }
        levels.push(level);
    }
    
    let values = new Map();
    
    for (let i = levels.length - 1; i &gt;= 0; i--) {
        for (let node of levels[i]) {
            if (node.left === null && node.right === null) {
                values.set(node, node.val === 1);
            } else {
                let leftVal = values.get(node.left);
                let rightVal = values.get(node.right);
                if (node.val === 2) {
                    values.set(node, leftVal || rightVal);
                } else {
                    values.set(node, leftVal && rightVal);
                }
            }
        }
    }
    
    return values.get(root);
};
` },
{ name: "TypeScript", lang: "typescript", code: `/**
 * Definition for a binary tree node.
 * class TreeNode {
 *     val: number
 *     left: TreeNode | null
 *     right: TreeNode | null
 *     constructor(val?: number, left?: TreeNode | null, right?: TreeNode | null) {
 *         this.val = (val===undefined ? 0 : val)
 *         this.left = (left===undefined ? null : left)
 *         this.right = (right===undefined ? null : right)
 *     }
 * }
 */
function evaluateTree(root: TreeNode | null): boolean {
    if (root.left === null && root.right === null) {
        return root.val === 1;
    }
    
    let levels: TreeNode[][] = [];
    let queue: TreeNode[] = [root];
    
    while (queue.length &gt; 0) {
        let levelSize = queue.length;
        let level: TreeNode[] = [];
        for (let i = 0; i &lt; levelSize; i++) {
            let node = queue.shift();
            level.push(node);
            if (node.left !== null) {
                queue.push(node.left);
            }
            if (node.right !== null) {
                queue.push(node.right);
            }
        }
        levels.push(level);
    }
    
    let values = new Map<TreeNode, boolean>();
    
    for (let i = levels.length - 1; i &gt;= 0; i--) {
        for (let node of levels[i]) {
            if (node.left === null && node.right === null) {
                values.set(node, node.val === 1);
            } else {
                let leftVal = values.get(node.left);
                let rightVal = values.get(node.right);
                if (node.val === 2) {
                    values.set(node, leftVal || rightVal);
                } else {
                    values.set(node, leftVal && rightVal);
                }
            }
        }
    }
    
    return values.get(root);
}
` },]} />

**Complexity**
- Time: O(n) where n is the number of nodes
- Space: O(n) for storing all levels and values
- Notes: Intuitive level-by-level processing but uses more memory than DFS

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Evaluate Boolean Binary Tree Solutions","datePublished":"2024-10-13","description":"Evaluate boolean expression represented by a binary tree where leaves are 0/1 and internal nodes are OR/AND operations.","keywords":"LeetCode, Evaluate Boolean Binary Tree, Easy, Tree, Depth-First Search, Binary Tree"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Recursive DFS approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of nodes in the tree, as we visit each node exactly once."}},{"@type":"Question","name":"What is the space complexity of the Recursive DFS approach?","acceptedAnswer":{"@type":"Answer","text":"O(h) where h is the height of the tree, due to the recursion stack. In the worst case, this is O(n) for a skewed tree."}},{"@type":"Question","name":"What is the time complexity of the Iterative DFS approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of nodes in the tree, as we visit each node exactly once."}},{"@type":"Question","name":"What is the space complexity of the Iterative DFS approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) in the worst case for the stack and the values map."}},{"@type":"Question","name":"What is the time complexity of the BFS (Bottom-up) approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of nodes in the tree, as we visit each node exactly once."}},{"@type":"Question","name":"What is the space complexity of the BFS (Bottom-up) approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for storing all levels and the values map."}}]}).replace(/</g,'\u003c')}
</script>