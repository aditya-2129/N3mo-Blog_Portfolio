---
title: "Number of Equivalent Domino Pairs"
summary: "Given a list of dominoes, count the number of pairs (i, j) where i &lt; j and dominoes[i] is equivalent to dominoes[j]."
date: "Mar 17, 2024"
tags:
  - Array
  - Hash Table
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 60.70% | **Paid:** No
> **Topics:** Array, Hash Table, Counting

Given a list of dominoes, dominoes[i] = [a, b] equivalent to dominoes[j] = [c, d] if and only if either (a == c and b == d), or (a == d and b == c) - that is, one domino can be rotated to be equal to another.

Return the number of pairs (i, j) for which 0 &lt;= i &lt; j &lt; dominoes.length, and dominoes[i] is equivalent to dominoes[j].

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Hash Map with Sorted Keys](#hash-map-with-sorted-keys)
- [2D Array Counter](#2d-array-counter)

## Examples

### Example 1
```text
Input: dominoes = [[1,2],[2,1],[3,4],[5,6]]
Output: 1
```

### Example 2
```text
Input: dominoes = [[1,2],[1,2],[1,1],[1,2],[2,2]]
Output: 3
```

## Constraints

```text
1 <= dominoes.length <= 4 * 10⁴
dominoes[i].length == 2
1 <= dominoes[i][j] <= 9
```

---
## Brute Force

**Intuition**
Compare every pair of dominoes directly and count the equivalent ones.

**Steps**
- Initialize count to 0
- For each pair (i, j) where i &lt; j
- Check if dominoes[i] is equivalent to dominoes[j]
- Increment count if equivalent
- Return count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numEquivDominoPairs(self, dominoes: list[list[int]]) -&gt; int:\n        n = len(dominoes)\n        count = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                a, b = dominoes[i]\n                c, d = dominoes[j]\n                if (a == c and b == d) or (a == d and b == c):\n                    count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numEquivDominoPairs(int[][] dominoes) {\n        int n = dominoes.length;\n        int count = 0;\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int a = dominoes[i][0], b = dominoes[i][1];\n                int c = dominoes[j][0], d = dominoes[j][1];\n                if ((a == c && b == d) || (a == d && b == c)) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numEquivDominoPairs(vector&lt;vector&lt;int&gt;&gt;& dominoes) {\n        int n = dominoes.size();\n        int count = 0;\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int a = dominoes[i][0], b = dominoes[i][1];\n                int c = dominoes[j][0], d = dominoes[j][1];\n                if ((a == c && b == d) || (a == d && b == c)) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[][]} dominoes\n * @return {number}\n */\nvar numEquivDominoPairs = function(dominoes) {\n    const n = dominoes.length;\n    let count = 0;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            const [a, b] = dominoes[i];\n            const [c, d] = dominoes[j];\n            if ((a === c && b === d) || (a === d && b === c)) {\n                count++;\n            }\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numEquivDominoPairs(dominoes: number[][]): number {\n    const n = dominoes.length;\n    let count = 0;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            const [a, b] = dominoes[i];\n            const [c, d] = dominoes[j];\n            if ((a === c && b === d) || (a === d && b === c)) {\n                count++;\n            }\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1)
- Notes: Simple but inefficient for large inputs

---
## Hash Map with Sorted Keys

**Intuition**
Normalize each domino by sorting its values, then count occurrences using a hash map.

**Steps**
- Create a hash map to count occurrences of each normalized domino
- For each domino [a, b], normalize to [min(a,b), max(a,b)]
- Use this normalized form as the key in the hash map
- For each count k, add k * (k-1) / 2 to the result
- Return the total count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numEquivDominoPairs(self, dominoes: list[list[int]]) -&gt; int:\n        from collections import defaultdict\n        \n        count = defaultdict(int)\n        for a, b in dominoes:\n            key = (min(a, b), max(a, b))\n            count[key] += 1\n        \n        result = 0\n        for v in count.values():\n            result += v * (v - 1) // 2\n        return result" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int numEquivDominoPairs(int[][] dominoes) {\n        Map&lt;String, Integer&gt; count = new HashMap&lt;&gt;();\n        \n        for (int[] domino : dominoes) {\n            int a = domino[0], b = domino[1];\n            int min = Math.min(a, b);\n            int max = Math.max(a, b);\n            String key = min + \",\" + max;\n            count.put(key, count.getOrDefault(key, 0) + 1);\n        }\n        \n        int result = 0;\n        for (int v : count.values()) {\n            result += v * (v - 1) / 2;\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;unordered_map&gt;\n#include &lt;string&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    int numEquivDominoPairs(vector&lt;vector&lt;int&gt;&gt;& dominoes) {\n        unordered_map&lt;string, int&gt; count;\n        \n        for (auto& domino : dominoes) {\n            int a = domino[0], b = domino[1];\n            int mn = min(a, b);\n            int mx = max(a, b);\n            string key = to_string(mn) + \",\" + to_string(mx);\n            count[key]++;\n        }\n        \n        int result = 0;\n        for (auto& p : count) {\n            int v = p.second;\n            result += v * (v - 1) / 2;\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[][]} dominoes\n * @return {number}\n */\nvar numEquivDominoPairs = function(dominoes) {\n    const count = new Map();\n    \n    for (const [a, b] of dominoes) {\n        const min = Math.min(a, b);\n        const max = Math.max(a, b);\n        const key = min + \",\" + max;\n        count.set(key, (count.get(key) || 0) + 1);\n    }\n    \n    let result = 0;\n    for (const v of count.values()) {\n        result += v * (v - 1) / 2;\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numEquivDominoPairs(dominoes: number[][]): number {\n    const count = new Map&lt;string, number&gt;();\n    \n    for (const [a, b] of dominoes) {\n        const min = Math.min(a, b);\n        const max = Math.max(a, b);\n        const key = min + \",\" + max;\n        count.set(key, (count.get(key) || 0) + 1);\n    }\n    \n    let result = 0;\n    for (const v of count.values()) {\n        result += v * (v - 1) / 2;\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Efficient solution using hash map for counting

---
## 2D Array Counter

**Intuition**
Since domino values are limited to 1-9, use a fixed-size 2D array instead of a hash map.

**Steps**
- Create a 10x10 array initialized to 0
- For each domino [a, b], increment count[min(a,b)][max(a,b)]
- Sum up all pairs using the formula k * (k-1) / 2
- Return the total count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numEquivDominoPairs(self, dominoes: list[list[int]]) -&gt; int:\n        count = [[0] * 10 for _ in range(10)]\n        \n        for a, b in dominoes:\n            mn, mx = min(a, b), max(a, b)\n            count[mn][mx] += 1\n        \n        result = 0\n        for i in range(10):\n            for j in range(10):\n                v = count[i][j]\n                result += v * (v - 1) // 2\n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numEquivDominoPairs(int[][] dominoes) {\n        int[][] count = new int[10][10];\n        \n        for (int[] domino : dominoes) {\n            int a = domino[0], b = domino[1];\n            int min = Math.min(a, b);\n            int max = Math.max(a, b);\n            count[min][max]++;\n        }\n        \n        int result = 0;\n        for (int i = 0; i &lt; 10; i++) {\n            for (int j = 0; j &lt; 10; j++) {\n                int v = count[i][j];\n                result += v * (v - 1) / 2;\n            }\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numEquivDominoPairs(vector&lt;vector&lt;int&gt;&gt;& dominoes) {\n        int count[10][10] = {0};\n        \n        for (auto& domino : dominoes) {\n            int a = domino[0], b = domino[1];\n            int mn = min(a, b);\n            int mx = max(a, b);\n            count[mn][mx]++;\n        }\n        \n        int result = 0;\n        for (int i = 0; i &lt; 10; i++) {\n            for (int j = 0; j &lt; 10; j++) {\n                int v = count[i][j];\n                result += v * (v - 1) / 2;\n            }\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[][]} dominoes\n * @return {number}\n */\nvar numEquivDominoPairs = function(dominoes) {\n    const count = Array(10).fill().map(() =&gt; Array(10).fill(0));\n    \n    for (const [a, b] of dominoes) {\n        const min = Math.min(a, b);\n        const max = Math.max(a, b);\n        count[min][max]++;\n    }\n    \n    let result = 0;\n    for (let i = 0; i &lt; 10; i++) {\n        for (let j = 0; j &lt; 10; j++) {\n            const v = count[i][j];\n            result += v * (v - 1) / 2;\n        }\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numEquivDominoPairs(dominoes: number[][]): number {\n    const count: number[][] = Array(10).fill(0).map(() =&gt; Array(10).fill(0));\n    \n    for (const [a, b] of dominoes) {\n        const min = Math.min(a, b);\n        const max = Math.max(a, b);\n        count[min][max]++;\n    }\n    \n    let result = 0;\n    for (let i = 0; i &lt; 10; i++) {\n        for (let j = 0; j &lt; 10; j++) {\n            const v = count[i][j];\n            result += v * (v - 1) / 2;\n        }\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Most space-efficient solution due to fixed array size

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Number of Equivalent Domino Pairs Solutions","datePublished":"2024-03-17","description":"Given a list of dominoes, count the number of pairs (i, j) where i &lt; j and dominoes[i] is equivalent to dominoes[j].","keywords":"LeetCode, Number of Equivalent Domino Pairs, Easy, Array, Hash Table, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach has a time complexity of O(n²) where n is the number of dominoes."}},{"@type":"Question","name":"What is the time complexity of the Hash Map approach?","acceptedAnswer":{"@type":"Answer","text":"The Hash Map with Sorted Keys approach has a time complexity of O(n) where n is the number of dominoes."}},{"@type":"Question","name":"What is the space complexity of the 2D Array Counter approach?","acceptedAnswer":{"@type":"Answer","text":"The 2D Array Counter approach has a space complexity of O(1) since the array size is fixed at 10x10 regardless of input size."}},{"@type":"Question","name":"Why can we use a 2D array instead of a hash map?","acceptedAnswer":{"@type":"Answer","text":"Since domino values are constrained to be between 1 and 9, we can use a fixed-size 10x10 array to count occurrences, which is more efficient than a hash map."}}]}).replace(/</g,'\u003c')}
</script>