---
title: "Intersection of Two Arrays"
summary: "Given two integer arrays, return an array of their intersection. Each element in the result must be unique."
date: "Feb 23, 2025"
tags:
  - Array
  - Hash Table
  - Two Pointers
  - Binary Search
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 77.70% | **Paid:** No
> **Topics:** Array, Hash Table, Two Pointers, Binary Search, Sorting

Given two integer arrays nums1 and nums2, return an array of their intersection. Each element in the result must be unique and you may return the result in any order.

- Examples
- Constraints
- [Hash Set](#hash-set)
- [Sorting + Two Pointers](#sorting--two-pointers)
- [Binary Search](#binary-search)
- [Brute Force](#brute-force)

## Examples

### Example 1

**Input:**
```text
nums1 = [1,2,2,1], nums2 = [2,2]
```

**Output:**
```text
[2]
```

### Example 2

**Input:**
```text
nums1 = [4,9,5], nums2 = [9,4,9,8,4]
```

**Output:**
```text
[9,4]
```

**Explanation:**
[4,9] is also accepted.

## Constraints

```text
1 <= nums1.length, nums2.length <= 1000
0 <= nums1[i], nums2[i] <= 1000
```

---

## Hash Set

**Intuition**
Use hash sets to store unique elements from both arrays, then find their intersection using set operations.

**Steps**
- Convert both arrays to sets to remove duplicates
- Find the intersection of both sets
- Convert the result back to an array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def intersection(self, nums1: List[int], nums2: List[int]) -&gt; List[int]:\n        set1 = set(nums1)\n        set2 = set(nums2)\n        return list(set1 & set2)" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int[] intersection(int[] nums1, int[] nums2) {\n        Set&lt;Integer&gt; set1 = new HashSet&lt;&gt;();\n        Set&lt;Integer&gt; set2 = new HashSet&lt;&gt;();\n        \n        for (int num : nums1) {\n            set1.add(num);\n        }\n        \n        for (int num : nums2) {\n            set2.add(num);\n        }\n        \n        set1.retainAll(set2);\n        \n        int[] result = new int[set1.size()];\n        int i = 0;\n        for (int num : set1) {\n            result[i++] = num;\n        }\n        \n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; intersection(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        unordered_set&lt;int&gt; set1(nums1.begin(), nums1.end());\n        unordered_set&lt;int&gt; set2(nums2.begin(), nums2.end());\n        \n        vector&lt;int&gt; result;\n        for (int num : set1) {\n            if (set2.count(num)) {\n                result.push_back(num);\n            }\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums1\n * @param {number[]} nums2\n * @return {number[]}\n */\nvar intersection = function(nums1, nums2) {\n    const set1 = new Set(nums1);\n    const set2 = new Set(nums2);\n    \n    const result = [];\n    for (const num of set1) {\n        if (set2.has(num)) {\n            result.push(num);\n        }\n    }\n    \n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function intersection(nums1: number[], nums2: number[]): number[] {\n    const set1 = new Set(nums1);\n    const set2 = new Set(nums2);\n    \n    const result: number[] = [];\n    for (const num of set1) {\n        if (set2.has(num)) {\n            result.push(num);\n        }\n    }\n    \n    return result;\n}" },]} />

**Complexity**
- Time: O(n + m) where n and m are the lengths of the arrays
- Space: O(n + m) for storing both sets
- Notes: Most intuitive approach with optimal time complexity

---

## Sorting + Two Pointers

**Intuition**
Sort both arrays and use two pointers to traverse them simultaneously, finding common elements while skipping duplicates.

**Steps**
- Sort both arrays in ascending order
- Use two pointers starting at the beginning of each array
- Move the pointer pointing to the smaller value
- When values match, add to result if not already added
- Skip duplicates by checking the last element in result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def intersection(self, nums1: List[int], nums2: List[int]) -&gt; List[int]:\n        nums1.sort()\n        nums2.sort()\n        \n        i, j = 0, 0\n        result = []\n        \n        while i &lt; len(nums1) and j &lt; len(nums2):\n            if nums1[i] &lt; nums2[j]:\n                i += 1\n            elif nums1[i] &gt; nums2[j]:\n                j += 1\n            else:\n                if not result or result[-1] != nums1[i]:\n                    result.append(nums1[i])\n                i += 1\n                j += 1\n        \n        return result" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int[] intersection(int[] nums1, int[] nums2) {\n        Arrays.sort(nums1);\n        Arrays.sort(nums2);\n        \n        int i = 0, j = 0;\n        List&lt;Integer&gt; resultList = new ArrayList&lt;&gt;();\n        \n        while (i &lt; nums1.length && j &lt; nums2.length) {\n            if (nums1[i] &lt; nums2[j]) {\n                i++;\n            } else if (nums1[i] &gt; nums2[j]) {\n                j++;\n            } else {\n                if (resultList.isEmpty() || resultList.get(resultList.size() - 1) != nums1[i]) {\n                    resultList.add(nums1[i]);\n                }\n                i++;\n                j++;\n            }\n        }\n        \n        int[] result = new int[resultList.size()];\n        for (int k = 0; k &lt; result.length; k++) {\n            result[k] = resultList.get(k);\n        }\n        \n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; intersection(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        sort(nums1.begin(), nums1.end());\n        sort(nums2.begin(), nums2.end());\n        \n        int i = 0, j = 0;\n        vector&lt;int&gt; result;\n        \n        while (i &lt; nums1.size() && j &lt; nums2.size()) {\n            if (nums1[i] &lt; nums2[j]) {\n                i++;\n            } else if (nums1[i] &gt; nums2[j]) {\n                j++;\n            } else {\n                if (result.empty() || result.back() != nums1[i]) {\n                    result.push_back(nums1[i]);\n                }\n                i++;\n                j++;\n            }\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums1\n * @param {number[]} nums2\n * @return {number[]}\n */\nvar intersection = function(nums1, nums2) {\n    nums1.sort((a, b) =&gt; a - b);\n    nums2.sort((a, b) =&gt; a - b);\n    \n    let i = 0, j = 0;\n    const result = [];\n    \n    while (i &lt; nums1.length && j &lt; nums2.length) {\n        if (nums1[i] &lt; nums2[j]) {\n            i++;\n        } else if (nums1[i] &gt; nums2[j]) {\n            j++;\n        } else {\n            if (result.length === 0 || result[result.length - 1] !== nums1[i]) {\n                result.push(nums1[i]);\n            }\n            i++;\n            j++;\n        }\n    }\n    \n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function intersection(nums1: number[], nums2: number[]): number[] {\n    nums1.sort((a, b) =&gt; a - b);\n    nums2.sort((a, b) =&gt; a - b);\n    \n    let i = 0, j = 0;\n    const result: number[] = [];\n    \n    while (i &lt; nums1.length && j &lt; nums2.length) {\n        if (nums1[i] &lt; nums2[j]) {\n            i++;\n        } else if (nums1[i] &gt; nums2[j]) {\n            j++;\n        } else {\n            if (result.length === 0 || result[result.length - 1] !== nums1[i]) {\n                result.push(nums1[i]);\n            }\n            i++;\n            j++;\n        }\n    }\n    \n    return result;\n}" },]} />

**Complexity**
- Time: O(n log n + m log m) for sorting
- Space: O(1) extra space (excluding result array)
- Notes: Good when space is constrained, but modifies input arrays

---

## Binary Search

**Intuition**
Sort the smaller array and use binary search to check if each element of the larger array exists in the sorted array.

**Steps**
- Sort the smaller array for binary search
- For each unique element in the larger array, binary search in the sorted array
- Use a set to track already found elements to avoid duplicates

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def intersection(self, nums1: List[int], nums2: List[int]) -&gt; List[int]:\n        if len(nums1) &gt; len(nums2):\n            nums1, nums2 = nums2, nums1\n        \n        nums1.sort()\n        nums2_set = set()\n        result = []\n        \n        for num in nums2:\n            if num not in nums2_set and self.binarySearch(nums1, num):\n                result.append(num)\n                nums2_set.add(num)\n        \n        return result\n    \n    def binarySearch(self, nums: List[int], target: int) -&gt; bool:\n        left, right = 0, len(nums) - 1\n        while left &lt;= right:\n            mid = left + (right - left) // 2\n            if nums[mid] == target:\n                return True\n            elif nums[mid] &lt; target:\n                left = mid + 1\n            else:\n                right = mid - 1\n        return False" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int[] intersection(int[] nums1, int[] nums2) {\n        if (nums1.length &gt; nums2.length) {\n            int[] temp = nums1;\n            nums1 = nums2;\n            nums2 = temp;\n        }\n        \n        Arrays.sort(nums1);\n        Set&lt;Integer&gt; seen = new HashSet&lt;&gt;();\n        List&lt;Integer&gt; resultList = new ArrayList&lt;&gt;();\n        \n        for (int num : nums2) {\n            if (!seen.contains(num) && binarySearch(nums1, num)) {\n                resultList.add(num);\n                seen.add(num);\n            }\n        }\n        \n        int[] result = new int[resultList.size()];\n        for (int i = 0; i &lt; result.length; i++) {\n            result[i] = resultList.get(i);\n        }\n        \n        return result;\n    }\n    \n    private boolean binarySearch(int[] nums, int target) {\n        int left = 0, right = nums.length - 1;\n        while (left &lt;= right) {\n            int mid = left + (right - left) / 2;\n            if (nums[mid] == target) {\n                return true;\n            } else if (nums[mid] &lt; target) {\n                left = mid + 1;\n            } else {\n                right = mid - 1;\n            }\n        }\n        return false;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\n#include &lt;algorithm&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; intersection(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        if (nums1.size() &gt; nums2.size()) {\n            swap(nums1, nums2);\n        }\n        \n        sort(nums1.begin(), nums1.end());\n        unordered_set&lt;int&gt; seen;\n        vector&lt;int&gt; result;\n        \n        for (int num : nums2) {\n            if (seen.find(num) == seen.end() && binarySearch(nums1, num)) {\n                result.push_back(num);\n                seen.insert(num);\n            }\n        }\n        \n        return result;\n    }\n    \nprivate:\n    bool binarySearch(vector&lt;int&gt;& nums, int target) {\n        int left = 0, right = nums.size() - 1;\n        while (left &lt;= right) {\n            int mid = left + (right - left) / 2;\n            if (nums[mid] == target) {\n                return true;\n            } else if (nums[mid] &lt; target) {\n                left = mid + 1;\n            } else {\n                right = mid - 1;\n            }\n        }\n        return false;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums1\n * @param {number[]} nums2\n * @return {number[]}\n */\nvar intersection = function(nums1, nums2) {\n    if (nums1.length &gt; nums2.length) {\n        [nums1, nums2] = [nums2, nums1];\n    }\n    \n    nums1.sort((a, b) =&gt; a - b);\n    const seen = new Set();\n    const result = [];\n    \n    for (const num of nums2) {\n        if (!seen.has(num) && binarySearch(nums1, num)) {\n            result.push(num);\n            seen.add(num);\n        }\n    }\n    \n    return result;\n};\n\nfunction binarySearch(nums, target) {\n    let left = 0, right = nums.length - 1;\n    while (left &lt;= right) {\n        const mid = left + Math.floor((right - left) / 2);\n        if (nums[mid] === target) {\n            return true;\n        } else if (nums[mid] &lt; target) {\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n    return false;\n}" },
{ name: "TypeScript", lang: "typescript", code: "function intersection(nums1: number[], nums2: number[]): number[] {\n    if (nums1.length &gt; nums2.length) {\n        [nums1, nums2] = [nums2, nums1];\n    }\n    \n    nums1.sort((a, b) =&gt; a - b);\n    const seen = new Set&lt;number&gt;();\n    const result: number[] = [];\n    \n    for (const num of nums2) {\n        if (!seen.has(num) && binarySearch(nums1, num)) {\n            result.push(num);\n            seen.add(num);\n        }\n    }\n    \n    return result;\n}\n\nfunction binarySearch(nums: number[], target: number): boolean {\n    let left = 0, right = nums.length - 1;\n    while (left &lt;= right) {\n        const mid = left + Math.floor((right - left) / 2);\n        if (nums[mid] === target) {\n            return true;\n        } else if (nums[mid] &lt; target) {\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n    return false;\n}" },]} />

**Complexity**
- Time: O(min(n, m) log min(n, m) + max(n, m)) for sorting and searching
- Space: O(min(n, m)) for the sorted array and seen set
- Notes: Efficient when one array is significantly smaller than the other

---

## Brute Force

**Intuition**
For each element in the first array, check if it exists in the second array using nested loops.

**Steps**
- Iterate through each element in nums1
- For each element, search through nums2
- If found and not already in result, add it
- Use a set to track already added elements

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def intersection(self, nums1: List[int], nums2: List[int]) -&gt; List[int]:\n        result = []\n        seen = set()\n        \n        for num in nums1:\n            if num not in seen:\n                for target in nums2:\n                    if num == target:\n                        result.append(num)\n                        seen.add(num)\n                        break\n        \n        return result" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int[] intersection(int[] nums1, int[] nums2) {\n        Set&lt;Integer&gt; seen = new HashSet&lt;&gt;();\n        List&lt;Integer&gt; resultList = new ArrayList&lt;&gt;();\n        \n        for (int num : nums1) {\n            if (!seen.contains(num)) {\n                for (int target : nums2) {\n                    if (num == target) {\n                        resultList.add(num);\n                        seen.add(num);\n                        break;\n                    }\n                }\n            }\n        }\n        \n        int[] result = new int[resultList.size()];\n        for (int i = 0; i &lt; result.length; i++) {\n            result[i] = resultList.get(i);\n        }\n        \n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; intersection(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        unordered_set&lt;int&gt; seen;\n        vector&lt;int&gt; result;\n        \n        for (int num : nums1) {\n            if (seen.find(num) == seen.end()) {\n                for (int target : nums2) {\n                    if (num == target) {\n                        result.push_back(num);\n                        seen.insert(num);\n                        break;\n                    }\n                }\n            }\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums1\n * @param {number[]} nums2\n * @return {number[]}\n */\nvar intersection = function(nums1, nums2) {\n    const seen = new Set();\n    const result = [];\n    \n    for (const num of nums1) {\n        if (!seen.has(num)) {\n            for (const target of nums2) {\n                if (num === target) {\n                    result.push(num);\n                    seen.add(num);\n                    break;\n                }\n            }\n        }\n    }\n    \n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function intersection(nums1: number[], nums2: number[]): number[] {\n    const seen = new Set&lt;number&gt;();\n    const result: number[] = [];\n    \n    for (const num of nums1) {\n        if (!seen.has(num)) {\n            for (const target of nums2) {\n                if (num === target) {\n                    result.push(num);\n                    seen.add(num);\n                    break;\n                }\n            }\n        }\n    }\n    \n    return result;\n}" },]} />

**Complexity**
- Time: O(n × m) where n and m are the lengths of the arrays
- Space: O(min(n, m)) for the result and seen set
- Notes: Simple but inefficient for large arrays, useful for understanding the problem

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Intersection of Two Arrays Solutions","datePublished":"2025-02-23","description":"Given two integer arrays, return an array of their intersection. Each element in the result must be unique.","keywords":"LeetCode, Intersection of Two Arrays, Easy, Array, Hash Table, Two Pointers, Binary Search, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The Hash Set approach has O(n + m) time complexity where n and m are the lengths of the arrays."}},{"@type":"Question","name":"Which approach uses the least extra space?","acceptedAnswer":{"@type":"Answer","text":"The Sorting + Two Pointers approach uses O(1) extra space (excluding the result array), making it the most space-efficient."}},{"@type":"Question","name":"When should I use the Binary Search approach?","acceptedAnswer":{"@type":"Answer","text":"Use Binary Search when one array is significantly smaller than the other, as it sorts only the smaller array."}},{"@type":"Question","name":"Is the Brute Force approach recommended for production?","acceptedAnswer":{"@type":"Answer","text":"No, the Brute Force approach with O(n × m) time complexity is not recommended for large inputs due to its inefficiency."}}]}).replace(/</g,'\u003c')}
</script>