---
title: "Add Strings"
summary: "Given two non-negative integers num1 and num2 represented as strings, return the sum of num1 and num2 as a string."
date: "Mar 04, 2026"
tags:
  - Math
  - String
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 52.20% | **Paid:** No
> **Topics:** Math, String, Simulation

Given two non-negative integers, num1 and num2 represented as string, return the sum of num1 and num2 as a string.

You must solve the problem without using any built-in library for handling large integers (such as BigInteger). You must also not convert the inputs to integers directly.

- Examples
- Constraints
- [Digit-by-Digit Addition (Two Pointers)](#digit-by-digit-addition-two-pointers)
- [Recursive Approach](#recursive-approach)
- [Padding and Addition]((#padding-and-addition)

## Examples

### Example 1:
```text
Input: num1 = "11", num2 = "123"
Output: "134"
```

### Example 2:
```text
Input: num1 = "456", num2 = "77"
Output: "533"
```

### Example 3:
```text
Input: num1 = "0", num2 = "0"
Output: "0"
```

## Constraints
```text
1 <= num1.length, num2.length <= 10^4
num1 and num2 consist of only digits.
num1 and num2 do not contain any leading zeros except for the zero itself.
```

---
## Digit-by-Digit Addition (Two Pointers)

**Intuition**
Simulate manual addition by processing digits from right to left, maintaining a carry value, and building the result string.

**Steps**
- Initialize two pointers at the end of both strings and a carry variable
- Loop while either pointer is valid or carry exists
- Extract digits (0 if pointer is out of bounds), sum them with carry
- Append the remainder to result and update carry
- Reverse the result and return

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def addStrings(self, num1: str, num2: str) -&gt; str:\n        i, j = len(num1) - 1, len(num2) - 1\n        carry = 0\n        result = []\n        \n        while i &gt;= 0 or j &gt;= 0 or carry:\n            digit1 = int(num1[i]) if i &gt;= 0 else 0\n            digit2 = int(num2[j]) if j &gt;= 0 else 0\n            \n            total = digit1 + digit2 + carry\n            carry = total // 10\n            result.append(str(total % 10))\n            \n            i -= 1\n            j -= 1\n        \n        return ''.join(reversed(result))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String addStrings(String num1, String num2) {\n        int i = num1.length() - 1;\n        int j = num2.length() - 1;\n        int carry = 0;\n        StringBuilder result = new StringBuilder();\n        \n        while (i &gt;= 0 || j &gt;= 0 || carry &gt; 0) {\n            int digit1 = (i &gt;= 0) ? num1.charAt(i) - '0' : 0;\n            int digit2 = (j &gt;= 0) ? num2.charAt(j) - '0' : 0;\n            \n            int total = digit1 + digit2 + carry;\n            carry = total / 10;\n            result.append(total % 10);\n            \n            i--;\n            j--;\n        }\n        \n        return result.reverse().toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string addStrings(string num1, string num2) {\n        int i = num1.length() - 1;\n        int j = num2.length() - 1;\n        int carry = 0;\n        string result = \"\";\n        \n        while (i &gt;= 0 || j &gt;= 0 || carry &gt; 0) {\n            int digit1 = (i &gt;= 0) ? num1[i] - '0' : 0;\n            int digit2 = (j &gt;= 0) ? num2[j] - '0' : 0;\n            \n            int total = digit1 + digit2 + carry;\n            carry = total / 10;\n            result.push_back('0' + (total % 10));\n            \n            i--;\n            j--;\n        }\n        \n        reverse(result.begin(), result.end());\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} num1\n * @param {string} num2\n * @return {string}\n */\nvar addStrings = function(num1, num2) {\n    let i = num1.length - 1;\n    let j = num2.length - 1;\n    let carry = 0;\n    let result = [];\n    \n    while (i &gt;= 0 || j &gt;= 0 || carry &gt; 0) {\n        let digit1 = (i &gt;= 0) ? num1.charCodeAt(i) - 48 : 0;\n        let digit2 = (j &gt;= 0) ? num2.charCodeAt(j) - 48 : 0;\n        \n        let total = digit1 + digit2 + carry;\n        carry = Math.floor(total / 10);\n        result.push(String.fromCharCode(48 + (total % 10)));\n        \n        i--;\n        j--;\n    }\n    \n    return result.reverse().join('');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function addStrings(num1: string, num2: string): string {\n    let i = num1.length - 1;\n    let j = num2.length - 1;\n    let carry = 0;\n    let result: string[] = [];\n    \n    while (i &gt;= 0 || j &gt;= 0 || carry &gt; 0) {\n        let digit1 = (i &gt;= 0) ? num1.charCodeAt(i) - 48 : 0;\n        let digit2 = (j &gt;= 0) ? num2.charCodeAt(j) - 48 : 0;\n        \n        let total = digit1 + digit2 + carry;\n        carry = Math.floor(total / 10);\n        result.push(String.fromCharCode(48 + (total % 10)));\n        \n        i--;\n        j--;\n    }\n    \n    return result.reverse().join('');\n}" },]} />

**Complexity**
- Time: O(max(m, n)) where m and n are lengths of input strings
- Space: O(max(m, n)) for the result string
- Notes: Most efficient approach with minimal operations

---
## Recursive Approach

**Intuition**
Use recursion to process digits from right to left, building the result string through recursive calls.

**Steps**
- Base case: when both pointers are out of bounds and carry is 0, return empty string
- Get current digits (0 if pointer out of bounds), calculate sum and new carry
- Recursively process remaining digits and append current digit

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def addStrings(self, num1: str, num2: str) -&gt; str:\n        def helper(i: int, j: int, carry: int) -&gt; str:\n            if i &lt; 0 and j &lt; 0 and carry == 0:\n                return \"\"\n            \n            digit1 = int(num1[i]) if i &gt;= 0 else 0\n            digit2 = int(num2[j]) if j &gt;= 0 else 0\n            \n            total = digit1 + digit2 + carry\n            new_carry = total // 10\n            \n            return helper(i - 1, j - 1, new_carry) + str(total % 10)\n        \n        return helper(len(num1) - 1, len(num2) - 1, 0)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String addStrings(String num1, String num2) {\n        return helper(num1, num2, num1.length() - 1, num2.length() - 1, 0);\n    }\n    \n    private String helper(String num1, String num2, int i, int j, int carry) {\n        if (i &lt; 0 && j &lt; 0 && carry == 0) {\n            return \"\";\n        }\n        \n        int digit1 = (i &gt;= 0) ? num1.charAt(i) - '0' : 0;\n        int digit2 = (j &gt;= 0) ? num2.charAt(j) - '0' : 0;\n        \n        int total = digit1 + digit2 + carry;\n        int newCarry = total / 10;\n        \n        return helper(num1, num2, i - 1, j - 1, newCarry) + (total % 10);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string addStrings(string num1, string num2) {\n        return helper(num1, num2, num1.length() - 1, num2.length() - 1, 0);\n    }\n    \nprivate:\n    string helper(string& num1, string& num2, int i, int j, int carry) {\n        if (i &lt; 0 && j &lt; 0 && carry == 0) {\n            return \"\";\n        }\n        \n        int digit1 = (i &gt;= 0) ? num1[i] - '0' : 0;\n        int digit2 = (j &gt;= 0) ? num2[j] - '0' : 0;\n        \n        int total = digit1 + digit2 + carry;\n        int newCarry = total / 10;\n        \n        return helper(num1, num2, i - 1, j - 1, newCarry) + string(1, '0' + (total % 10));\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} num1\n * @param {string} num2\n * @return {string}\n */\nvar addStrings = function(num1, num2) {\n    function helper(i, j, carry) {\n        if (i &lt; 0 && j &lt; 0 && carry === 0) {\n            return \"\";\n        }\n        \n        let digit1 = (i &gt;= 0) ? num1.charCodeAt(i) - 48 : 0;\n        let digit2 = (j &gt;= 0) ? num2.charCodeAt(j) - 48 : 0;\n        \n        let total = digit1 + digit2 + carry;\n        let newCarry = Math.floor(total / 10);\n        \n        return helper(i - 1, j - 1, newCarry) + String.fromCharCode(48 + (total % 10));\n    }\n    \n    return helper(num1.length - 1, num2.length - 1, 0);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function addStrings(num1: string, num2: string): string {\n    function helper(i: number, j: number, carry: number): string {\n        if (i &lt; 0 && j &lt; 0 && carry === 0) {\n            return \"\";\n        }\n        \n        let digit1 = (i &gt;= 0) ? num1.charCodeAt(i) - 48 : 0;\n        let digit2 = (j &gt;= 0) ? num2.charCodeAt(j) - 48 : 0;\n        \n        let total = digit1 + digit2 + carry;\n        let newCarry = Math.floor(total / 10);\n        \n        return helper(i - 1, j - 1, newCarry) + String.fromCharCode(48 + (total % 10));\n    }\n    \n    return helper(num1.length - 1, num2.length - 1, 0);\n}" },]} />

**Complexity**
- Time: O(max(m, n)) where m and n are lengths of input strings
- Space: O(max(m, n)) for recursion stack
- Notes: Elegant but may cause stack overflow for very large inputs

---
## Padding and Addition

**Intuition**
Pad the shorter string with leading zeros to make both strings equal length, then add digit by digit.

**Steps**
- Find the maximum length of both strings
- Pad the shorter string with leading zeros
- Iterate from right to left, adding corresponding digits
- Handle final carry if present

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def addStrings(self, num1: str, num2: str) -&gt; str:\n        max_len = max(len(num1), len(num2))\n        num1 = num1.zfill(max_len)\n        num2 = num2.zfill(max_len)\n        \n        carry = 0\n        result = []\n        \n        for i in range(max_len - 1, -1, -1):\n            digit1 = ord(num1[i]) - ord('0')\n            digit2 = ord(num2[i]) - ord('0')\n            \n            total = digit1 + digit2 + carry\n            carry = total // 10\n            result.append(str(total % 10))\n        \n        if carry:\n            result.append(str(carry))\n        \n        return ''.join(reversed(result))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String addStrings(String num1, String num2) {\n        int maxLen = Math.max(num1.length(), num2.length());\n        \n        while (num1.length() &lt; maxLen) {\n            num1 = \"0\" + num1;\n        }\n        while (num2.length() &lt; maxLen) {\n            num2 = \"0\" + num2;\n        }\n        \n        int carry = 0;\n        StringBuilder result = new StringBuilder();\n        \n        for (int i = maxLen - 1; i &gt;= 0; i--) {\n            int digit1 = num1.charAt(i) - '0';\n            int digit2 = num2.charAt(i) - '0';\n            \n            int total = digit1 + digit2 + carry;\n            carry = total / 10;\n            result.append(total % 10);\n        }\n        \n        if (carry &gt; 0) {\n            result.append(carry);\n        }\n        \n        return result.reverse().toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string addStrings(string num1, string num2) {\n        int maxLen = max(num1.length(), num2.length());\n        \n        while (num1.length() &lt; maxLen) {\n            num1 = \"0\" + num1;\n        }\n        while (num2.length() &lt; maxLen) {\n            num2 = \"0\" + num2;\n        }\n        \n        int carry = 0;\n        string result = \"\";\n        \n        for (int i = maxLen - 1; i &gt;= 0; i--) {\n            int digit1 = num1[i] - '0';\n            int digit2 = num2[i] - '0';\n            \n            int total = digit1 + digit2 + carry;\n            carry = total / 10;\n            result.push_back('0' + (total % 10));\n        }\n        \n        if (carry &gt; 0) {\n            result.push_back('0' + carry);\n        }\n        \n        reverse(result.begin(), result.end());\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} num1\n * @param {string} num2\n * @return {string}\n */\nvar addStrings = function(num1, num2) {\n    let maxLen = Math.max(num1.length, num2.length);\n    \n    while (num1.length &lt; maxLen) {\n        num1 = \"0\" + num1;\n    }\n    while (num2.length &lt; maxLen) {\n        num2 = \"0\" + num2;\n    }\n    \n    let carry = 0;\n    let result = [];\n    \n    for (let i = maxLen - 1; i &gt;= 0; i--) {\n        let digit1 = num1.charCodeAt(i) - 48;\n        let digit2 = num2.charCodeAt(i) - 48;\n        \n        let total = digit1 + digit2 + carry;\n        carry = Math.floor(total / 10);\n        result.push(String.fromCharCode(48 + (total % 10)));\n    }\n    \n    if (carry &gt; 0) {\n        result.push(String.fromCharCode(48 + carry));\n    }\n    \n    return result.reverse().join('');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function addStrings(num1: string, num2: string): string {\n    let maxLen = Math.max(num1.length, num2.length);\n    \n    while (num1.length &lt; maxLen) {\n        num1 = \"0\" + num1;\n    }\n    while (num2.length &lt; maxLen) {\n        num2 = \"0\" + num2;\n    }\n    \n    let carry = 0;\n    let result: string[] = [];\n    \n    for (let i = maxLen - 1; i &gt;= 0; i--) {\n        let digit1 = num1.charCodeAt(i) - 48;\n        let digit2 = num2.charCodeAt(i) - 48;\n        \n        let total = digit1 + digit2 + carry;\n        carry = Math.floor(total / 10);\n        result.push(String.fromCharCode(48 + (total % 10)));\n    }\n    \n    if (carry &gt; 0) {\n        result.push(String.fromCharCode(48 + carry));\n    }\n    \n    return result.reverse().join('');\n}" },]} />

**Complexity**
- Time: O(max(m, n)) where m and n are lengths of input strings
- Space: O(max(m, n)) for the result string
- Notes: Simpler logic but uses extra space for padding

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Add Strings Solutions","datePublished":"2026-03-04","description":"Given two non-negative integers num1 and num2 represented as strings, return the sum of num1 and num2 as a string.","keywords":"LeetCode, Add Strings, Easy, Math, String, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Digit-by-Digit Addition approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(max(m, n)) where m and n are the lengths of the input strings, as we process each digit exactly once."}},{"@type":"Question","name":"What is the space complexity of the Recursive Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(max(m, n)) for the recursion stack, which may cause stack overflow for very large inputs."}},{"@type":"Question","name":"Why can't we convert the strings to integers directly?","acceptedAnswer":{"@type":"Answer","text":"The problem explicitly prohibits direct conversion because the numbers can be very large (up to 10^4 digits), exceeding the maximum value of standard integer types."}},{"@type":"Question","name":"What is the trade-off of the Padding and Addition approach?","acceptedAnswer":{"@type":"Answer","text":"This approach has simpler logic since both strings have equal length, but it uses extra space for padding the shorter string with leading zeros."}}]}).replace(/</g,'\u003c')}
</script>