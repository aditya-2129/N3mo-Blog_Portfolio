---
title: "Find X-Sum of All K-Long Subarrays I"
summary: "Calculate the x-sum for every subarray of length k, where x-sum is the sum of top x most frequent elements."
date: "Dec 13, 2024"
tags:
  - Array
  - Hash Table
  - Sliding Window
  - Heap
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 76.10% | **Paid:** No
> **Topics:** Array, Hash Table, Sliding Window, Heap (Priority Queue)

## Problem Description

You are given an array of integers `nums`, and two integers `k` and `x`.

The x-sum of a subarray is calculated as follows:
1. Count the frequency of each element in the subarray.
2. Take the `x` most frequent elements. If there are ties in frequency, prefer larger elements.
3. Sum these `x` elements, each multiplied by its frequency.

Return an array containing the x-sum of every subarray of length `k`.

## Table of Contents

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Sliding Window with Hash Map](#sliding-window-with-hash-map)
- [Sliding Window with Counter](#sliding-window-with-counter)

## Examples

### Example 1

```text
Input: nums = [1,1,2,2,3,4,2,3], k = 6, x = 2
Output: [6,10,12]
Explanation: 
- Subarray [1,1,2,2,3,4]: Frequencies are {1:2, 2:2, 3:1, 4:1}. Top 2 by (freq, value) are 2 and 1. X-sum = 2*2 + 1*2 = 6.
- Subarray [1,2,2,3,4,2]: Frequencies are {1:1, 2:3, 3:1, 4:1}. Top 2 by (freq, value) are 2 and 4. X-sum = 2*3 + 4*1 = 10.
- Subarray [2,2,3,4,2,3]: Frequencies are {2:3, 3:2, 4:1}. Top 2 by (freq, value) are 2 and 3. X-sum = 2*3 + 3*2 = 12.
```

### Example 2

```text
Input: nums = [3,8,7,8,7,5], k = 2, x = 2
Output: [11,15,15,15,12]
Explanation:
- Subarray [3,8]: Frequencies are {3:1, 8:1}. Top 2 are 8 and 3. X-sum = 8*1 + 3*1 = 11.
- Subarray [8,7]: Frequencies are {8:1, 7:1}. Top 2 are 8 and 7. X-sum = 8*1 + 7*1 = 15.
- Subarray [7,8]: Frequencies are {7:1, 8:1}. Top 2 are 8 and 7. X-sum = 8*1 + 7*1 = 15.
- Subarray [8,7]: Frequencies are {8:1, 7:1}. Top 2 are 8 and 7. X-sum = 8*1 + 7*1 = 15.
- Subarray [7,5]: Frequencies are {7:1, 5:1}. Top 2 are 7 and 5. X-sum = 7*1 + 5*1 = 12.
```

## Constraints

```text
1 <= nums.length <= 50
1 <= nums[i] <= 50
1 <= k <= nums.length
1 <= x <= number of distinct elements in the subarray
```

## Brute Force

**Intuition**
For each subarray of length k, count frequencies using a hash map, sort elements by (frequency, value) in descending order, and sum the top x elements.

**Steps**
- Iterate through all starting positions of subarrays of length k
- For each subarray, build a frequency map
- Convert the map to a list of (value, frequency) pairs
- Sort by (frequency, value) in descending order
- Sum the top x elements multiplied by their frequencies

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List
from collections import defaultdict

class Solution:
    def findXSum(self, nums: List[int], k: int, x: int) -> List[int]:
        n = len(nums)
        result = []
        
        for i in range(n - k + 1):
            freq = defaultdict(int)
            for j in range(i, i + k):
                freq[nums[j]] += 1
            
            items = list(freq.items())
            items.sort(key=lambda p: (p[1], p[0]), reverse=True)
            
            x_sum = 0
            for j in range(min(x, len(items))):
                x_sum += items[j][0] * items[j][1]
            
            result.append(x_sum)
        
        return result` },
{ name: "Java", lang: "java", code: `import java.util.*;

class Solution {
    public int[] findXSum(int[] nums, int k, int x) {
        int n = nums.length;
        int[] result = new int[n - k + 1];
        
        for (int i = 0; i &lt;= n - k; i++) {
            Map<Integer, Integer> freq = new HashMap&lt;&gt;();
            for (int j = i; j &lt; i + k; j++) {
                freq.put(nums[j], freq.getOrDefault(nums[j], 0) + 1);
            }
            
            List<int[]&gt; items = new ArrayList&lt;&gt;();
            for (Map.Entry<Integer, Integer> entry : freq.entrySet()) {
                items.add(new int[]{entry.getKey(), entry.getValue()});
            }
            
            items.sort((a, b) -> {
                if (b[1] != a[1]) return b[1] - a[1];
                return b[0] - a[0];
            });
            
            int xSum = 0;
            for (int j = 0; j &lt; Math.min(x, items.size()); j++) {
                xSum += items.get(j)[0] * items.get(j)[1];
            }
            
            result[i] = xSum;
        }
        
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <unordered_map>
#include <algorithm>

using namespace std;

class Solution {
public:
    vector<int> findXSum(vector<int>& nums, int k, int x) {
        int n = nums.size();
        vector<int> result;
        
        for (int i = 0; i &lt;= n - k; i++) {
            unordered_map<int, int> freq;
            for (int j = i; j &lt; i + k; j++) {
                freq[nums[j]]++;
            }
            
            vector<pair<int, int>&gt; items;
            for (auto& entry : freq) {
                items.push_back({entry.first, entry.second});
            }
            
            sort(items.begin(), items.end(), [](const pair<int, int>& a, const pair<int, int>& b) {
                if (a.second != b.second) return a.second &gt; b.second;
                return a.first &gt; b.first;
            });
            
            int xSum = 0;
            for (int j = 0; j &lt; min((int)x, (int)items.size()); j++) {
                xSum += items[j].first * items[j].second;
            }
            
            result.push_back(xSum);
        }
        
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @param {number} k
 * @param {number} x
 * @return {number[]}
 */
var findXSum = function(nums, k, x) {
    const n = nums.length;
    const result = [];
    
    for (let i = 0; i &lt;= n - k; i++) {
        const freq = new Map();
        for (let j = i; j &lt; i + k; j++) {
            freq.set(nums[j], (freq.get(nums[j]) || 0) + 1);
        }
        
        const items = Array.from(freq.entries());
        items.sort((a, b) =&gt; {
            if (b[1] !== a[1]) return b[1] - a[1];
            return b[0] - a[0];
        });
        
        let xSum = 0;
        for (let j = 0; j &lt; Math.min(x, items.length); j++) {
            xSum += items[j][0] * items[j][1];
        }
        
        result.push(xSum);
    }
    
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function findXSum(nums: number[], k: number, x: number): number[] {
    const n = nums.length;
    const result: number[] = [];
    
    for (let i = 0; i &lt;= n - k; i++) {
        const freq = new Map<number, number>();
        for (let j = i; j &lt; i + k; j++) {
            freq.set(nums[j], (freq.get(nums[j]) || 0) + 1);
        }
        
        const items = Array.from(freq.entries());
        items.sort((a, b) =&gt; {
            if (b[1] !== a[1]) return b[1] - a[1];
            return b[0] - a[0];
        });
        
        let xSum = 0;
        for (let j = 0; j &lt; Math.min(x, items.length); j++) {
            xSum += items[j][0] * items[j][1];
        }
        
        result.push(xSum);
    }
    
    return result;
}` },]} />

**Complexity**
- Time: O(n × k × log(k)) where n is the length of nums
- Space: O(k) for the frequency map
- Notes: Simple but not optimal for large inputs

## Sliding Window with Hash Map

**Intuition**
Use a sliding window to maintain frequencies efficiently. As we move the window, we update the frequency map by removing the leftmost element and adding the new rightmost element.

**Steps**
- Initialize the frequency map for the first window
- Calculate the x-sum for the first window
- Slide the window one position at a time:
  - Decrement frequency of the element leaving the window
  - Remove it from the map if frequency becomes 0
  - Increment frequency of the new element entering the window
  - Calculate the x-sum for the current window

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List
from collections import defaultdict

class Solution:
    def findXSum(self, nums: List[int], k: int, x: int) -> List[int]:
        n = len(nums)
        result = []
        freq = defaultdict(int)
        
        for i in range(k):
            freq[nums[i]] += 1
        
        def calculate_x_sum():
            items = list(freq.items())
            items.sort(key=lambda p: (p[1], p[0]), reverse=True)
            x_sum = 0
            for j in range(min(x, len(items))):
                x_sum += items[j][0] * items[j][1]
            return x_sum
        
        result.append(calculate_x_sum())
        
        for i in range(k, n):
            left = nums[i - k]
            freq[left] -= 1
            if freq[left] == 0:
                del freq[left]
            
            freq[nums[i]] += 1
            result.append(calculate_x_sum())
        
        return result` },
{ name: "Java", lang: "java", code: `import java.util.*;

class Solution {
    public int[] findXSum(int[] nums, int k, int x) {
        int n = nums.length;
        int[] result = new int[n - k + 1];
        Map<Integer, Integer> freq = new HashMap&lt;&gt;();
        
        for (int i = 0; i &lt; k; i++) {
            freq.put(nums[i], freq.getOrDefault(nums[i], 0) + 1);
        }
        
        result[0] = calculateXSum(freq, x);
        
        for (int i = k; i &lt; n; i++) {
            int left = nums[i - k];
            freq.put(left, freq.get(left) - 1);
            if (freq.get(left) == 0) {
                freq.remove(left);
            }
            
            freq.put(nums[i], freq.getOrDefault(nums[i], 0) + 1);
            result[i - k + 1] = calculateXSum(freq, x);
        }
        
        return result;
    }
    
    private int calculateXSum(Map<Integer, Integer> freq, int x) {
        List<int[]&gt; items = new ArrayList&lt;&gt;();
        for (Map.Entry<Integer, Integer> entry : freq.entrySet()) {
            items.add(new int[]{entry.getKey(), entry.getValue()});
        }
        
        items.sort((a, b) -> {
            if (b[1] != a[1]) return b[1] - a[1];
            return b[0] - a[0];
        });
        
        int xSum = 0;
        for (int j = 0; j &lt; Math.min(x, items.size()); j++) {
            xSum += items.get(j)[0] * items.get(j)[1];
        }
        
        return xSum;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <unordered_map>
#include <algorithm>

using namespace std;

class Solution {
private:
    int calculateXSum(unordered_map<int, int>& freq, int x) {
        vector<pair<int, int>&gt; items;
        for (auto& entry : freq) {
            items.push_back({entry.first, entry.second});
        }
        
        sort(items.begin(), items.end(), [](const pair<int, int>& a, const pair<int, int>& b) {
            if (a.second != b.second) return a.second &gt; b.second;
            return a.first &gt; b.first;
        });
        
        int xSum = 0;
        for (int j = 0; j &lt; min((int)x, (int)items.size()); j++) {
            xSum += items[j].first * items[j].second;
        }
        
        return xSum;
    }

public:
    vector<int> findXSum(vector<int>& nums, int k, int x) {
        int n = nums.size();
        vector<int> result;
        unordered_map<int, int> freq;
        
        for (int i = 0; i &lt; k; i++) {
            freq[nums[i]]++;
        }
        
        result.push_back(calculateXSum(freq, x));
        
        for (int i = k; i &lt; n; i++) {
            int left = nums[i - k];
            freq[left]--;
            if (freq[left] == 0) {
                freq.erase(left);
            }
            
            freq[nums[i]]++;
            result.push_back(calculateXSum(freq, x));
        }
        
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @param {number} k
 * @param {number} x
 * @return {number[]}
 */
var findXSum = function(nums, k, x) {
    const n = nums.length;
    const result = [];
    const freq = new Map();
    
    const calculateXSum = () =&gt; {
        const items = Array.from(freq.entries());
        items.sort((a, b) =&gt; {
            if (b[1] !== a[1]) return b[1] - a[1];
            return b[0] - a[0];
        });
        
        let xSum = 0;
        for (let j = 0; j &lt; Math.min(x, items.length); j++) {
            xSum += items[j][0] * items[j][1];
        }
        
        return xSum;
    };
    
    for (let i = 0; i &lt; k; i++) {
        freq.set(nums[i], (freq.get(nums[i]) || 0) + 1);
    }
    
    result.push(calculateXSum());
    
    for (let i = k; i &lt; n; i++) {
        const left = nums[i - k];
        freq.set(left, freq.get(left) - 1);
        if (freq.get(left) === 0) {
            freq.delete(left);
        }
        
        freq.set(nums[i], (freq.get(nums[i]) || 0) + 1);
        result.push(calculateXSum());
    }
    
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function findXSum(nums: number[], k: number, x: number): number[] {
    const n = nums.length;
    const result: number[] = [];
    const freq = new Map<number, number>();
    
    const calculateXSum = (): number =&gt; {
        const items = Array.from(freq.entries());
        items.sort((a, b) =&gt; {
            if (b[1] !== a[1]) return b[1] - a[1];
            return b[0] - a[0];
        });
        
        let xSum = 0;
        for (let j = 0; j &lt; Math.min(x, items.length); j++) {
            xSum += items[j][0] * items[j][1];
        }
        
        return xSum;
    };
    
    for (let i = 0; i &lt; k; i++) {
        freq.set(nums[i], (freq.get(nums[i]) || 0) + 1);
    }
    
    result.push(calculateXSum());
    
    for (let i = k; i &lt; n; i++) {
        const left = nums[i - k];
        freq.set(left, freq.get(left)! - 1);
        if (freq.get(left) === 0) {
            freq.delete(left);
        }
        
        freq.set(nums[i], (freq.get(nums[i]) || 0) + 1);
        result.push(calculateXSum());
    }
    
    return result;
}` },]} />

**Complexity**
- Time: O(n × k × log(k)) where n is the length of nums
- Space: O(k) for the frequency map
- Notes: More efficient than brute force for window updates, but still requires sorting for each window

## Sliding Window with Counter

**Intuition**
Use Python's Counter for efficient frequency counting and heapq's nlargest to find the top x elements without full sorting.

**Steps**
- Use a Counter to maintain frequencies for the sliding window
- For each window position, use heapq.nlargest to get the top x elements
- Calculate the x-sum from these top elements

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List
from collections import Counter
import heapq

class Solution:
    def findXSum(self, nums: List[int], k: int, x: int) -> List[int]:
        n = len(nums)
        result = []
        counter = Counter(nums[:k])
        
        def calculate_x_sum():
            top_x = heapq.nlargest(x, counter.items(), key=lambda p: (p[1], p[0]))
            return sum(val * freq for val, freq in top_x)
        
        result.append(calculate_x_sum())
        
        for i in range(k, n):
            left = nums[i - k]
            counter[left] -= 1
            if counter[left] == 0:
                del counter[left]
            
            counter[nums[i]] += 1
            result.append(calculate_x_sum())
        
        return result` },
{ name: "Java", lang: "java", code: `import java.util.*;

class Solution {
    public int[] findXSum(int[] nums, int k, int x) {
        int n = nums.length;
        int[] result = new int[n - k + 1];
        Map<Integer, Integer> freq = new HashMap&lt;&gt;();
        
        for (int i = 0; i &lt; k; i++) {
            freq.put(nums[i], freq.getOrDefault(nums[i], 0) + 1);
        }
        
        result[0] = calculateXSum(freq, x);
        
        for (int i = k; i &lt; n; i++) {
            int left = nums[i - k];
            freq.put(left, freq.get(left) - 1);
            if (freq.get(left) == 0) {
                freq.remove(left);
            }
            
            freq.put(nums[i], freq.getOrDefault(nums[i], 0) + 1);
            result[i - k + 1] = calculateXSum(freq, x);
        }
        
        return result;
    }
    
    private int calculateXSum(Map<Integer, Integer> freq, int x) {
        PriorityQueue<int[]&gt; pq = new PriorityQueue&lt;&gt;((a, b) -> {
            if (b[1] != a[1]) return b[1] - a[1];
            return b[0] - a[0];
        });
        
        for (Map.Entry<Integer, Integer> entry : freq.entrySet()) {
            pq.offer(new int[]{entry.getKey(), entry.getValue()});
        }
        
        int xSum = 0;
        for (int j = 0; j &lt; x && !pq.isEmpty(); j++) {
            int[] item = pq.poll();
            xSum += item[0] * item[1];
        }
        
        return xSum;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <unordered_map>
#include <queue>

using namespace std;

class Solution {
private:
    int calculateXSum(unordered_map<int, int>& freq, int x) {
        auto cmp = [](const pair<int, int>& a, const pair<int, int>& b) {
            if (a.second != b.second) return a.second &lt; b.second;
            return a.first &lt; b.first;
        };
        
        priority_queue<pair<int, int>, vector<pair<int, int>&gt;, decltype(cmp)&gt; pq(cmp);
        
        for (auto& entry : freq) {
            pq.push(entry);
        }
        
        int xSum = 0;
        for (int j = 0; j &lt; x && !pq.empty(); j++) {
            auto item = pq.top();
            pq.pop();
            xSum += item.first * item.second;
        }
        
        return xSum;
    }

public:
    vector<int> findXSum(vector<int>& nums, int k, int x) {
        int n = nums.size();
        vector<int> result;
        unordered_map<int, int> freq;
        
        for (int i = 0; i &lt; k; i++) {
            freq[nums[i]]++;
        }
        
        result.push_back(calculateXSum(freq, x));
        
        for (int i = k; i &lt; n; i++) {
            int left = nums[i - k];
            freq[left]--;
            if (freq[left] == 0) {
                freq.erase(left);
            }
            
            freq[nums[i]]++;
            result.push_back(calculateXSum(freq, x));
        }
        
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @param {number} k
 * @param {number} x
 * @return {number[]}
 */
var findXSum = function(nums, k, x) {
    const n = nums.length;
    const result = [];
    const freq = new Map();
    
    const calculateXSum = () =&gt; {
        const items = Array.from(freq.entries());
        items.sort((a, b) =&gt; {
            if (b[1] !== a[1]) return b[1] - a[1];
            return b[0] - a[0];
        });
        
        let xSum = 0;
        for (let j = 0; j &lt; Math.min(x, items.length); j++) {
            xSum += items[j][0] * items[j][1];
        }
        
        return xSum;
    };
    
    for (let i = 0; i &lt; k; i++) {
        freq.set(nums[i], (freq.get(nums[i]) || 0) + 1);
    }
    
    result.push(calculateXSum());
    
    for (let i = k; i &lt; n; i++) {
        const left = nums[i - k];
        freq.set(left, freq.get(left) - 1);
        if (freq.get(left) === 0) {
            freq.delete(left);
        }
        
        freq.set(nums[i], (freq.get(nums[i]) || 0) + 1);
        result.push(calculateXSum());
    }
    
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function findXSum(nums: number[], k: number, x: number): number[] {
    const n = nums.length;
    const result: number[] = [];
    const freq = new Map<number, number>();
    
    const calculateXSum = (): number =&gt; {
        const items = Array.from(freq.entries());
        items.sort((a, b) =&gt; {
            if (b[1] !== a[1]) return b[1] - a[1];
            return b[0] - a[0];
        });
        
        let xSum = 0;
        for (let j = 0; j &lt; Math.min(x, items.length); j++) {
            xSum += items[j][0] * items[j][1];
        }
        
        return xSum;
    };
    
    for (let i = 0; i &lt; k; i++) {
        freq.set(nums[i], (freq.get(nums[i]) || 0) + 1);
    }
    
    result.push(calculateXSum());
    
    for (let i = k; i &lt; n; i++) {
        const left = nums[i - k];
        freq.set(left, freq.get(left)! - 1);
        if (freq.get(left) === 0) {
            freq.delete(left);
        }
        
        freq.set(nums[i], (freq.get(nums[i]) || 0) + 1);
        result.push(calculateXSum());
    }
    
    return result;
}` },]} />

**Complexity**
- Time: O(n × k × log(k)) where n is the length of nums
- Space: O(k) for the frequency map
- Notes: Using Counter/heap can be more efficient in practice for finding top x elements

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find X-Sum of All K-Long Subarrays I Solutions","datePublished":"2024-12-13","description":"Calculate the x-sum for every subarray of length k, where x-sum is the sum of top x most frequent elements.","keywords":"LeetCode, Find X-Sum of All K-Long Subarrays I, Easy, Array, Hash Table, Sliding Window, Heap (Priority Queue)"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n × k × log(k)) where n is the length of nums."}},{"@type":"Question","name":"What is the time complexity of the Sliding Window approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n × k × log(k)) where n is the length of nums."}},{"@type":"Question","name":"What is the space complexity of all approaches?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(k) for the frequency map in all approaches."}}]}).replace(/</g,'\u003c')}
</script>