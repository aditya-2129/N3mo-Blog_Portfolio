---
title: "Maximum Number of Words Found in Sentences"
summary: "Find the sentence with the most words by counting spaces in each string."
date: "Sep 03, 2024"
tags:
  - Array
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 86.70% | **Paid:** No
> **Topics:** Array, String

A sentence is a list of words that are separated by a single space with no leading or trailing spaces.

Each word consists of lowercase and uppercase English letters.

Given an array of strings sentences, return the maximum number of words that appear in a single sentence.

- Examples
- Constraints
- [Approach 1: Split and Count](#split-and-count)
- [Approach 2: Count Spaces](#count-spaces)

## Examples

```text
Input: sentences = ["alice and bob love leetcode", "i think so too", "this is great thanks very much"]
Output: 6
Explanation: 
- "alice and bob love leetcode" has 5 words.
- "i think so too" has 4 words.
- "this is great thanks very much" has 6 words.
The maximum number of words is 6.
```

```text
Input: sentences = ["please wait", "continue to fight", "continue to win"]
Output: 3
Explanation: 
- "please wait" has 2 words.
- "continue to fight" has 3 words.
- "continue to win" has 3 words.
The maximum number of words is 3.
```

## Constraints

```text
1 <= sentences.length <= 100
1 <= sentences[i].length <= 100
sentences[i] consists only of lowercase and uppercase English letters and ' '.
All the words in sentences[i] are separated by a single space.
There are no leading or trailing spaces.
```

---
## Split and Count

**Intuition**
The most straightforward way to count words in a string is to split the string by the space delimiter and count the resulting elements.

**Steps**
- Initialize a variable `maxWords` to 0.
- Iterate through each sentence in the `sentences` array.
- For each sentence, split the string by the space character " ".
- The length of the resulting array is the number of words in that sentence.
- Update `maxWords` if the current sentence has more words.
- Return `maxWords`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def mostWordsFound(self, sentences: List[str]) -&gt; int:\r\n        max_words = 0\r\n        for s in sentences:\r\n            # Split by space and count the elements\r\n            word_count = len(s.split(\" \"))\r\n            max_words = max(max_words, word_count)\r\n        return max_words\r\n" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int mostWordsFound(String[] sentences) {\r\n        int maxWords = 0;\r\n        for (String s : sentences) {\r\n            // Split by space and count the elements\r\n            String[] words = s.split(\" \");\r\n            maxWords = Math.max(maxWords, words.length);\r\n        }\r\n        return maxWords;\r\n    }\r\n}\r\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;string&gt;\r\n#include &lt;sstream&gt;\r\n#include &lt;algorithm&gt;\r\n\r\nusing namespace std;\r\n\r\nclass Solution {\r\npublic:\r\n    int mostWordsFound(vector&lt;string&gt;& sentences) {\r\n        int maxWords = 0;\r\n        for (const string& s : sentences) {\r\n            // Use stringstream to split by space\r\n            stringstream ss(s);\r\n            string word;\r\n            int count = 0;\r\n            while (ss &gt;&gt; word) {\r\n                count++;\r\n            }\r\n            maxWords = max(maxWords, count);\r\n        }\r\n        return maxWords;\r\n    }\r\n};\r\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {string[]} sentences\r\n * @return {number}\r\n */\r\nvar mostWordsFound = function(sentences) {\r\n    let maxWords = 0;\r\n    for (const s of sentences) {\r\n        // Split by space and count the elements\r\n        const wordCount = s.split(\" \").length;\r\n        maxWords = Math.max(maxWords, wordCount);\r\n    }\r\n    return maxWords;\r\n};\r\n" },
{ name: "TypeScript", lang: "typescript", code: "function mostWordsFound(sentences: string[]): number {\r\n    let maxWords = 0;\r\n    for (const s of sentences) {\r\n        // Split by space and count the elements\r\n        const wordCount = s.split(\" \").length;\r\n        maxWords = Math.max(maxWords, wordCount);\r\n    }\r\n    return maxWords;\r\n}\r\n" },]} />

**Complexity**
- Time: O(N * L) where N is the number of sentences and L is the average length of a sentence.
- Space: O(L) to store the split array for each sentence.
- Notes: This approach is very readable but allocates extra memory for the split arrays.

---
## Count Spaces

**Intuition**
Since words are separated by exactly one space, the number of words in a sentence is equal to the number of spaces plus one. We can simply count the space characters in each string.

**Steps**
- Initialize a variable `maxWords` to 0.
- Iterate through each sentence in the `sentences` array.
- For each sentence, iterate through its characters and count how many spaces (" ") are present.
- The word count is `spaceCount + 1`.
- Update `maxWords` if the current sentence has more words.
- Return `maxWords`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def mostWordsFound(self, sentences: List[str]) -&gt; int:\r\n        max_words = 0\r\n        for s in sentences:\r\n            # Count spaces and add 1 for the word count\r\n            word_count = s.count(\" \") + 1\r\n            max_words = max(max_words, word_count)\r\n        return max_words\r\n" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int mostWordsFound(String[] sentences) {\r\n        int maxWords = 0;\r\n        for (String s : sentences) {\r\n            int count = 1; // Start at 1 because words = spaces + 1\r\n            for (int i = 0; i &lt; s.length(); i++) {\r\n                if (s.charAt(i) == ' ') {\r\n                    count++;\r\n                }\r\n            }\r\n            maxWords = Math.max(maxWords, count);\r\n        }\r\n        return maxWords;\r\n    }\r\n}\r\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;string&gt;\r\n#include &lt;algorithm&gt;\r\n\r\nusing namespace std;\r\n\r\nclass Solution {\r\npublic:\r\n    int mostWordsFound(vector&lt;string&gt;& sentences) {\r\n        int maxWords = 0;\r\n        for (const string& s : sentences) {\r\n            // Count spaces using std::count\r\n            int spaces = count(s.begin(), s.end(), ' ');\r\n            maxWords = max(maxWords, spaces + 1);\r\n        }\r\n        return maxWords;\r\n    }\r\n};\r\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {string[]} sentences\r\n * @return {number}\r\n */\r\nvar mostWordsFound = function(sentences) {\r\n    let maxWords = 0;\r\n    for (const s of sentences) {\r\n        let count = 1; // Start at 1 because words = spaces + 1\r\n        for (let i = 0; i &lt; s.length; i++) {\r\n            if (s[i] === \" \") {\r\n                count++;\r\n            }\r\n        }\r\n        maxWords = Math.max(maxWords, count);\r\n    }\r\n    return maxWords;\r\n};\r\n" },
{ name: "TypeScript", lang: "typescript", code: "function mostWordsFound(sentences: string[]): number {\r\n    let maxWords = 0;\r\n    for (const s of sentences) {\r\n        let count = 1; // Start at 1 because words = spaces + 1\r\n        for (let i = 0; i &lt; s.length; i++) {\r\n            if (s[i] === \" \") {\r\n                count++;\r\n            }\r\n        }\r\n        maxWords = Math.max(maxWords, count);\r\n    }\r\n    return maxWords;\r\n}\r\n" },]} />

**Complexity**
- Time: O(N * L) where N is the number of sentences and L is the average length of a sentence.
- Space: O(1) as we only use a few variables for counting.
- Notes: This is the most optimal approach as it avoids allocating extra memory for string arrays.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Maximum Number of Words Found in Sentences Solutions","datePublished":"2024-09-03","description":"Find the sentence with the most words by counting spaces in each string.","keywords":"LeetCode, Maximum Number of Words Found in Sentences, Easy, Array, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Split and Count approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * L), where N is the number of sentences and L is the average length of a sentence, as we must traverse every character to split the strings."}},{"@type":"Question","name":"What is the space complexity of the Count Spaces approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use a few integer variables for counting and do not allocate any additional data structures proportional to the input size."}}]}).replace(/</g,'\u003c')}
</script>