---
title: "Special Array I"
summary: "Determine if an array is special by checking if every pair of adjacent elements has different parity."
date: "Feb 14, 2024"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 81.60% | **Paid:** No
> **Topics:** Array

An array is considered special if every pair of adjacent elements of the array have different parity.

Return true if the array is special, otherwise return false.

- Examples
- Constraints
- [Iterative Check](#iterative-check)
- [Functional Approach](#functional-approach)

## Examples

```text
Input: nums = [1]
Output: true
Explanation:
There is only one element, so the array is special.
```

```text
Input: nums = [2, 1, 4]
Output: true
Explanation:
All adjacent pairs have different parity:
- (2, 1): 2 is even, 1 is odd.
- (1, 4): 1 is odd, 4 is even.
```

```text
Input: nums = [4, 3, 1, 6]
Output: false
Explanation:
The pair (1, 6) has the same parity (both are odd), so the array is not special.
```

## Constraints

```text
1 <= nums.length <= 100
1 <= nums[i] <= 100
```

---

## Iterative Check

**Intuition**
We can iterate through the array once, comparing the parity of each element with the next one. If any adjacent pair has the same parity, the array is not special.

**Steps**
- Iterate from the first element to the second-to-last element.
- For each element at index `i`, check if `nums[i] % 2` equals `nums[i + 1] % 2`.
- If they are equal, return `false`.
- If the loop completes without finding any equal parity pairs, return `true`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isArraySpecial(self, nums: list[int]) -&gt; bool:\n        for i in range(len(nums) - 1):\n            if (nums[i] % 2) == (nums[i + 1] % 2):\n                return False\n        return True" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isArraySpecial(int[] nums) {\n        for (int i = 0; i &lt; nums.length - 1; i++) {\n            if ((nums[i] % 2) == (nums[i + 1] % 2)) {\n                return false;\n            }\n        }\n        return true;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isArraySpecial(vector&lt;int&gt;& nums) {\n        for (int i = 0; i &lt; nums.size() - 1; i++) {\n            if ((nums[i] % 2) == (nums[i + 1] % 2)) {\n                return false;\n            }\n        }\n        return true;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var isArraySpecial = function(nums) {\n    for (let i = 0; i &lt; nums.length - 1; i++) {\n        if ((nums[i] % 2) === (nums[i + 1] % 2)) {\n            return false;\n        }\n    }\n    return true;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isArraySpecial(nums: number[]): boolean {\n    for (let i = 0; i &lt; nums.length - 1; i++) {\n        if ((nums[i] % 2) === (nums[i + 1] % 2)) {\n            return false;\n        }\n    }\n    return true;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: This is the most efficient approach with a single pass.

---

## Functional Approach

**Intuition**
We can utilize built-in functional programming constructs to check if the condition holds for all adjacent pairs simultaneously.

**Steps**
- Create a sequence or stream of adjacent pairs.
- Check if all pairs satisfy the condition that their parities are different.
- Return the result of the check.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isArraySpecial(self, nums: list[int]) -&gt; bool:\n        return all((a % 2) != (b % 2) for a, b in zip(nums, nums[1:]))" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\n\nclass Solution {\n    public boolean isArraySpecial(int[] nums) {\n        return IntStream.range(0, nums.length - 1)\n            .allMatch(i -&gt; (nums[i] % 2) != (nums[i + 1] % 2));\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    bool isArraySpecial(vector&lt;int&gt;& nums) {\n        return std::adjacent_find(nums.begin(), nums.end() - 1, \n            [](int a, int b) { return (a % 2) == (b % 2); }) == nums.end() - 1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var isArraySpecial = function(nums) {\n    return nums.every((val, idx) =&gt; \n        idx === 0 || (val % 2) !== (nums[idx - 1] % 2)\n    );\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isArraySpecial(nums: number[]): boolean {\n    return nums.every((val, idx) =&gt; \n        idx === 0 || (val % 2) !== (nums[idx - 1] % 2)\n    );\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1) or O(n) depending on language implementation of intermediate streams/iterators.
- Notes: More concise but may have slight overhead compared to the iterative loop.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Special Array I Solutions","datePublished":"2024-02-14","description":"Determine if an array is special by checking if every pair of adjacent elements has different parity.","keywords":"LeetCode, Special Array I, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Check approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of the Iterative Check approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space."}},{"@type":"Question","name":"How does the Functional Approach differ from the Iterative Check?","acceptedAnswer":{"@type":"Answer","text":"The Functional Approach uses built-in methods like 'all' or 'every' to abstract the loop, often resulting in more concise code, though the underlying complexity remains similar."}}]}).replace(/</g,'\u003c')}
</script>