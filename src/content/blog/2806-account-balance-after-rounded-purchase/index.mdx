---
title: "Account Balance After Rounded Purchase"
summary: "Calculate the remaining account balance after a purchase is rounded to the nearest multiple of 10."
date: "Sep 09, 2025"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 56.40% | **Paid:** No
> **Topics:** Math

Initially, you have a bank account balance of 100 dollars.

You are given an integer purchaseAmount representing the amount you will spend on a single purchase in dollars.

At the store where you will make the purchase, the amount is rounded to the nearest multiple of 10. If the number ends in a 5 or greater, it is rounded up. If it ends in 4 or less, it is rounded down.

Return an integer representing the balance that remains in your bank account after your purchase.

- Examples
- Constraints
- [Mathematical Rounding](#mathematical-rounding)
- [Modulo Arithmetic](#modulo-arithmetic)
- [String Manipulation](#string-manipulation)

## Examples

### Example 1

**Input:**
```text
purchaseAmount = 9
```

**Output:**
```text
90
```

**Explanation:**
The nearest multiple of 10 to 9 is 10. So your account balance becomes 100 - 10 = 90.

### Example 2

**Input:**
```text
purchaseAmount = 15
```

**Output:**
```text
80
```

**Explanation:**
The nearest multiple of 10 to 15 is 20. So your account balance becomes 100 - 20 = 80.

### Example 3

**Input:**
```text
purchaseAmount = 10
```

**Output:**
```text
90
```

**Explanation:**
10 is a multiple of 10 itself. So your account balance becomes 100 - 10 = 90.

## Constraints

```text
1 <= purchaseAmount <= 100
```

---
## Mathematical Rounding

**Intuition**
Use integer arithmetic to round the purchase amount to the nearest multiple of 10 by adding 5 and then dividing by 10.

**Steps**
- Add 5 to the purchase amount
- Perform integer division by 10
- Multiply by 10 to get the rounded amount
- Subtract from 100 to get the remaining balance

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def accountBalanceAfterPurchase(self, purchaseAmount: int) -> int:
        rounded = ((purchaseAmount + 5) // 10) * 10
        return 100 - rounded
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int accountBalanceAfterPurchase(int purchaseAmount) {
        int rounded = ((purchaseAmount + 5) / 10) * 10;
        return 100 - rounded;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int accountBalanceAfterPurchase(int purchaseAmount) {
        int rounded = ((purchaseAmount + 5) / 10) * 10;
        return 100 - rounded;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} purchaseAmount
 * @return {number}
 */
var accountBalanceAfterPurchase = function(purchaseAmount) {
    const rounded = Math.floor((purchaseAmount + 5) / 10) * 10;
    return 100 - rounded;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function accountBalanceAfterPurchase(purchaseAmount: number): number {
    const rounded = Math.floor((purchaseAmount + 5) / 10) * 10;
    return 100 - rounded;
}
` },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Most efficient approach with constant time operations

---
## Modulo Arithmetic

**Intuition**
Extract the last digit using modulo operation and apply rounding rules based on whether it's 5 or greater.

**Steps**
- Get the last digit using purchaseAmount % 10
- If last digit is 5 or more, round up by adding (10 - last digit)
- If last digit is less than 5, round down by subtracting last digit
- Return 100 minus the rounded amount

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def accountBalanceAfterPurchase(self, purchaseAmount: int) -> int:
        last_digit = purchaseAmount % 10
        if last_digit &gt;= 5:
            rounded = purchaseAmount + (10 - last_digit)
        else:
            rounded = purchaseAmount - last_digit
        return 100 - rounded
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int accountBalanceAfterPurchase(int purchaseAmount) {
        int lastDigit = purchaseAmount % 10;
        int rounded;
        if (lastDigit &gt;= 5) {
            rounded = purchaseAmount + (10 - lastDigit);
        } else {
            rounded = purchaseAmount - lastDigit;
        }
        return 100 - rounded;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int accountBalanceAfterPurchase(int purchaseAmount) {
        int lastDigit = purchaseAmount % 10;
        int rounded;
        if (lastDigit &gt;= 5) {
            rounded = purchaseAmount + (10 - lastDigit);
        } else {
            rounded = purchaseAmount - lastDigit;
        }
        return 100 - rounded;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} purchaseAmount
 * @return {number}
 */
var accountBalanceAfterPurchase = function(purchaseAmount) {
    const lastDigit = purchaseAmount % 10;
    let rounded;
    if (lastDigit &gt;= 5) {
        rounded = purchaseAmount + (10 - lastDigit);
    } else {
        rounded = purchaseAmount - lastDigit;
    }
    return 100 - rounded;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function accountBalanceAfterPurchase(purchaseAmount: number): number {
    const lastDigit = purchaseAmount % 10;
    let rounded: number;
    if (lastDigit &gt;= 5) {
        rounded = purchaseAmount + (10 - lastDigit);
    } else {
        rounded = purchaseAmount - lastDigit;
    }
    return 100 - rounded;
}
` },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Slightly more verbose but equally efficient

---
## String Manipulation

**Intuition**
Convert the number to a string to extract the last digit and apply rounding logic.

**Steps**
- Convert purchaseAmount to string
- Extract the last character and convert back to integer
- Apply rounding rules based on the last digit
- Return 100 minus the rounded amount

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def accountBalanceAfterPurchase(self, purchaseAmount: int) -> int:
        s = str(purchaseAmount)
        last_digit = int(s[-1])
        if last_digit &gt;= 5:
            rounded = purchaseAmount + (10 - last_digit)
        else:
            rounded = purchaseAmount - last_digit
        return 100 - rounded
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int accountBalanceAfterPurchase(int purchaseAmount) {
        String s = String.valueOf(purchaseAmount);
        int lastDigit = Character.getNumericValue(s.charAt(s.length() - 1));
        int rounded;
        if (lastDigit &gt;= 5) {
            rounded = purchaseAmount + (10 - lastDigit);
        } else {
            rounded = purchaseAmount - lastDigit;
        }
        return 100 - rounded;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int accountBalanceAfterPurchase(int purchaseAmount) {
        string s = to_string(purchaseAmount);
        int lastDigit = s.back() - '0';
        int rounded;
        if (lastDigit &gt;= 5) {
            rounded = purchaseAmount + (10 - lastDigit);
        } else {
            rounded = purchaseAmount - lastDigit;
        }
        return 100 - rounded;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} purchaseAmount
 * @return {number}
 */
var accountBalanceAfterPurchase = function(purchaseAmount) {
    const s = purchaseAmount.toString();
    const lastDigit = parseInt(s[s.length - 1]);
    let rounded;
    if (lastDigit &gt;= 5) {
        rounded = purchaseAmount + (10 - lastDigit);
    } else {
        rounded = purchaseAmount - lastDigit;
    }
    return 100 - rounded;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function accountBalanceAfterPurchase(purchaseAmount: number): number {
    const s: string = purchaseAmount.toString();
    const lastDigit: number = parseInt(s[s.length - 1]);
    let rounded: number;
    if (lastDigit &gt;= 5) {
        rounded = purchaseAmount + (10 - lastDigit);
    } else {
        rounded = purchaseAmount - lastDigit;
    }
    return 100 - rounded;
}
` },]} />

**Complexity**
- Time: O(1) - String operations are constant time for fixed-size integers
- Space: O(1) - String representation is bounded by input size
- Notes: Less efficient due to string conversion overhead

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Account Balance After Rounded Purchase Solutions","datePublished":"2025-09-09","description":"Calculate the remaining account balance after a purchase is rounded to the nearest multiple of 10.","keywords":"LeetCode, Account Balance After Rounded Purchase, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Mathematical Rounding approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - All operations are constant time arithmetic operations."}},{"@type":"Question","name":"What is the space complexity of Modulo Arithmetic approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Only a few integer variables are used regardless of input size."}},{"@type":"Question","name":"Which approach is most efficient for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Mathematical Rounding approach is most efficient as it uses the fewest operations with ((purchaseAmount + 5) // 10) * 10 formula."}},{"@type":"Question","name":"What is the time complexity of String Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - String operations are constant time since the input is bounded by the constraint purchaseAmount &lt;= 100."}}]}).replace(/</g,'\u003c')}
</script>