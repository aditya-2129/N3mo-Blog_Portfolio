---
title: "Convert a Number to Hexadecimal"
summary: "Given an integer, return its hexadecimal string representation using two's complement for negatives."
date: "Feb 14, 2024"
tags:
  - Math
  - String
  - Bit Manipulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 54.00% | **Paid:** No
> **Topics:** Math, String, Bit Manipulation

Given an integer num, return a string representing its hexadecimal representation. For negative integers, use two’s complement method.

All the letters in the answer should be lowercase, and there should be no leading zeros in the answer unless the number is 0. If the number is zero, it is represented by a single zero character '0'; otherwise, the first character in the string is not the zero character.

The given number is guaranteed to fit within the range of a 32-bit signed integer.

- Examples
- Constraints
- [Bit Manipulation](#bit-manipulation)
- [Built-in Functions](#built-in-functions)

## Examples

```text
Input: num = 26
Output: "1a"
```

```text
Input: num = -1
Output: "ffffffff"
```

## Constraints

```text
- -2^31 <= num <= 2^31 - 1
```

---
## Bit Manipulation

**Intuition**
Hexadecimal is base 16, which corresponds to 4 bits in binary. We can extract the number 4 bits at a time and map it to the corresponding hex character.

**Steps**
- Handle the edge case where `num` is 0.
- Create a mapping array or string for hex characters "0123456789abcdef".
- Loop 8 times (since 32 bits / 4 bits = 8 hex digits).
- In each iteration, mask the last 4 bits using `num & 0xf` to get the value.
- Prepend the corresponding hex character to the result.
- Unsigned right shift `num` by 4 bits to process the next nibble.
- Remove leading zeros from the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def toHex(self, num: int) -&gt; str:\n        if num == 0:\n            return \"0\"\n        hex_map = \"0123456789abcdef\"\n        res = []\n        for _ in range(8):\n            res.append(hex_map[num & 0xf])\n            num &gt;&gt;= 4\n        return \"\".join(reversed(res))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String toHex(int num) {\n        if (num == 0) return \"0\";\n        char[] hex = \"0123456789abcdef\".toCharArray();\n        StringBuilder sb = new StringBuilder();\n        while (num != 0) {\n            sb.append(hex[num & 0xF]);\n            num &gt;&gt;&gt;= 4;\n        }\n        return sb.reverse().toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string toHex(int num) {\n        if (num == 0) return \"0\";\n        string hex = \"0123456789abcdef\";\n        string res = \"\";\n        unsigned int n = num;\n        while (n &gt; 0) {\n            res = hex[n & 0xF] + res;\n            n &gt;&gt;= 4;\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var toHex = function(num) {\n    if (num === 0) return \"0\";\n    const hex = \"0123456789abcdef\";\n    let res = \"\";\n    while (num !== 0) {\n        res = hex[num & 0xF] + res;\n        num &gt;&gt;&gt;= 4;\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function toHex(num: number): string {\n    if (num === 0) return \"0\";\n    const hex = \"0123456789abcdef\";\n    let res = \"\";\n    while (num !== 0) {\n        res = hex[num & 0xF] + res;\n        num &gt;&gt;&gt;= 4;\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(1) - We iterate a constant number of times (8).
- Space: O(1) - Excluding the space required for the output string.
- Notes: This is the most efficient algorithmic approach.

---
## Built-in Functions

**Intuition**
Most programming languages provide standard library functions to convert integers to hexadecimal strings, often handling two's complement for negative numbers automatically.

**Steps**
- Identify the built-in method for the target language (e.g., `hex()`, `toString(16)`, `Integer.toHexString`).
- Call the method with the input number.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def toHex(self, num: int) -&gt; str:\n        return hex(num & 0xffffffff)[2:]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String toHex(int num) {\n        return Integer.toHexString(num);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;sstream&gt;\n#include &lt;iomanip&gt;\n\nclass Solution {\npublic:\n    string toHex(int num) {\n        stringstream ss;\n        ss &lt;&lt; hex &lt;&lt; (unsigned int)num;\n        return ss.str();\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var toHex = function(num) {\n    return num.toString(16);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function toHex(num: number): string {\n    return num.toString(16);\n}" },]} />

**Complexity**
- Time: O(1) - Depends on implementation, but generally constant for fixed-width integers.
- Space: O(1) - Excluding the space required for the output string.
- Notes: This is the most concise approach but may not be allowed in interviews testing algorithmic knowledge.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Convert a Number to Hexadecimal Solutions","datePublished":"2024-02-14","description":"Given an integer, return its hexadecimal string representation using two's complement for negatives.","keywords":"LeetCode, Convert a Number to Hexadecimal, Easy, Math, String, Bit Manipulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Bit Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) because we iterate a constant 8 times (32 bits / 4 bits per hex digit)."}},{"@type":"Question","name":"How does the Bit Manipulation approach handle negative numbers?","acceptedAnswer":{"@type":"Answer","text":"It uses bitwise AND with a mask (0xf) and unsigned right shifting (or casting to unsigned) to treat the number as a sequence of bits, effectively simulating two's complement representation."}},{"@type":"Question","name":"What is the space complexity of these approaches?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) auxiliary space, not counting the space required for the output string."}}]}).replace(/</g,'\u003c')}
</script>