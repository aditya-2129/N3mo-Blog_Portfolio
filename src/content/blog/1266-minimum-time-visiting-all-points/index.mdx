---
title: "Minimum Time Visiting All Points"
summary: "Calculate the minimum time to visit all points in order on a 2D plane, moving one unit per second in any of 8 directions."
date: "Dec 15, 2025"
tags:
  - Array
  - Math
  - Geometry
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 84.80% | **Paid:** No
> **Topics:** Array, Math, Geometry

On a plane there are n points with integer coordinates points[i] = [xi, yi]. Your task is to find the minimum time in seconds to visit all points.

You can move according to the next rules:

* In one second you can move:
    * vertically by 1 unit,
    * horizontally by 1 unit,
    * diagonally by sqrt(2) units (in other words, move one unit vertically then one unit horizontally in 1 second).
* You have to visit the points in the same order as they appear in the array.
* You are allowed to pass through points that appear later in the order, but only visiting them counts towards the goal.

- Examples
- Constraints
- [Mathematical Approach (Chebyshev Distance)](#mathematical-approach-chebyshev-distance)
- [Simulation Approach](#simulation-approach)

## Examples

**Example 1:**

```text
Input: points = [[1,1],[3,4],[-1,0]]
Output: 7
Explanation:
One optimal path is [1,1] -> [2,2] -> [3,3] -> [3,4] -> [2,3] -> [1,2] -> [0,1] -> [-1,0]
Total time 7.
```

**Example 2:**

```text
Input: points = [[3,2],[-2,2]]
Output: 5
```

## Constraints

```text
2 <= points.length <= 1000
points[i].length == 2
-10⁴ <= xi, yi <= 10⁴
All points are distinct.
```

---

## Mathematical Approach (Chebyshev Distance)

**Intuition**
The minimum time to move from point A to point B is determined by the Chebyshev distance. Since diagonal movement covers both horizontal and vertical distance simultaneously, the time required is the maximum of the horizontal distance and the vertical distance.

**Steps**
- Initialize a variable `time` to 0.
- Iterate through the points array from the first point to the second-to-last point.
- For each pair of consecutive points `(x1, y1)` and `(x2, y2)`:
    - Calculate the absolute difference in x-coordinates: `dx = abs(x2 - x1)`.
    - Calculate the absolute difference in y-coordinates: `dy = abs(y2 - y1)`.
    - Add the maximum of `dx` and `dy` to `time`.
- Return `time`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def minTimeToVisitAllPoints(self, points: List[List[int]]) -&gt; int:\r\n        time = 0\r\n        for i in range(len(points) - 1):\r\n            x1, y1 = points[i]\r\n            x2, y2 = points[i + 1]\r\n            dx = abs(x2 - x1)\r\n            dy = abs(y2 - y1)\r\n            time += max(dx, dy)\r\n        return time" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int minTimeToVisitAllPoints(int[][] points) {\r\n        int time = 0;\r\n        for (int i = 0; i &lt; points.length - 1; i++) {\r\n            int x1 = points[i][0];\r\n            int y1 = points[i][1];\r\n            int x2 = points[i + 1][0];\r\n            int y2 = points[i + 1][1];\r\n            int dx = Math.abs(x2 - x1);\r\n            int dy = Math.abs(y2 - y1);\r\n            time += Math.max(dx, dy);\r\n        }\r\n        return time;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;cmath&gt;\r\n#include &lt;algorithm&gt;\r\n\r\nusing namespace std;\r\n\r\nclass Solution {\r\npublic:\r\n    int minTimeToVisitAllPoints(vector&lt;vector&lt;int&gt;&gt;& points) {\r\n        int time = 0;\r\n        for (int i = 0; i &lt; points.size() - 1; i++) {\r\n            int x1 = points[i][0];\r\n            int y1 = points[i][1];\r\n            int x2 = points[i + 1][0];\r\n            int y2 = points[i + 1][1];\r\n            int dx = abs(x2 - x1);\r\n            int dy = abs(y2 - y1);\r\n            time += max(dx, dy);\r\n        }\r\n        return time;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {number[][]} points\r\n * @return {number}\r\n */\r\nvar minTimeToVisitAllPoints = function(points) {\r\n    let time = 0;\r\n    for (let i = 0; i &lt; points.length - 1; i++) {\r\n        const x1 = points[i][0];\r\n        const y1 = points[i][1];\r\n        const x2 = points[i + 1][0];\r\n        const y2 = points[i + 1][1];\r\n        const dx = Math.abs(x2 - x1);\r\n        const dy = Math.abs(y2 - y1);\r\n        time += Math.max(dx, dy);\r\n    }\r\n    return time;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minTimeToVisitAllPoints(points: number[][]): number {\r\n    let time = 0;\r\n    for (let i = 0; i &lt; points.length - 1; i++) {\r\n        const x1 = points[i][0];\r\n        const y1 = points[i][1];\r\n        const x2 = points[i + 1][0];\r\n        const y2 = points[i + 1][1];\r\n        const dx = Math.abs(x2 - x1);\r\n        const dy = Math.abs(y2 - y1);\r\n        time += Math.max(dx, dy);\r\n    }\r\n    return time;\r\n}" },]} />

**Complexity**
- Time: O(N), where N is the number of points. We iterate through the list once.
- Space: O(1), we only use a few variables for calculation.
- Notes: This is the optimal solution for this problem.

---

## Simulation Approach

**Intuition**
Simulate the movement step-by-step. At each second, move diagonally towards the target if possible, otherwise move horizontally or vertically. This approach directly implements the movement rules.

**Steps**
- Initialize `time` to 0 and set current position to the first point.
- Iterate through the remaining points as targets.
- While the current position is not the target:
    - Determine the direction of movement for x and y axes (-1, 0, or 1).
    - Update the current position.
    - Increment `time`.
- Return `time`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def minTimeToVisitAllPoints(self, points: List[List[int]]) -&gt; int:\r\n        time = 0\r\n        cx, cy = points[0]\r\n        for tx, ty in points[1:]:\r\n            while cx != tx or cy != ty:\r\n                dx = tx - cx\r\n                dy = ty - cy\r\n                sx = 0 if dx == 0 else (1 if dx &gt; 0 else -1)\r\n                sy = 0 if dy == 0 else (1 if dy &gt; 0 else -1)\r\n                cx += sx\r\n                cy += sy\r\n                time += 1\r\n        return time" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int minTimeToVisitAllPoints(int[][] points) {\r\n        int time = 0;\r\n        int cx = points[0][0];\r\n        int cy = points[0][1];\r\n        for (int i = 1; i &lt; points.length; i++) {\r\n            int tx = points[i][0];\r\n            int ty = points[i][1];\r\n            while (cx != tx || cy != ty) {\r\n                int dx = Integer.compare(tx, cx);\r\n                int dy = Integer.compare(ty, cy);\r\n                cx += dx;\r\n                cy += dy;\r\n                time++;\r\n            }\r\n        }\r\n        return time;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;cmath&gt;\r\n\r\nusing namespace std;\r\n\r\nclass Solution {\r\npublic:\r\n    int minTimeToVisitAllPoints(vector&lt;vector&lt;int&gt;&gt;& points) {\r\n        int time = 0;\r\n        int cx = points[0][0];\r\n        int cy = points[0][1];\r\n        for (int i = 1; i &lt; points.size(); i++) {\r\n            int tx = points[i][0];\r\n            int ty = points[i][1];\r\n            while (cx != tx || cy != ty) {\r\n                int dx = (tx &gt; cx) ? 1 : ((tx &lt; cx) ? -1 : 0);\r\n                int dy = (ty &gt; cy) ? 1 : ((ty &lt; cy) ? -1 : 0);\r\n                cx += dx;\r\n                cy += dy;\r\n                time++;\r\n            }\r\n        }\r\n        return time;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {number[][]} points\r\n * @return {number}\r\n */\r\nvar minTimeToVisitAllPoints = function(points) {\r\n    let time = 0;\r\n    let cx = points[0][0];\r\n    let cy = points[0][1];\r\n    for (let i = 1; i &lt; points.length; i++) {\r\n        let tx = points[i][0];\r\n        let ty = points[i][1];\r\n        while (cx !== tx || cy !== ty) {\r\n            const dx = Math.sign(tx - cx);\r\n            const dy = Math.sign(ty - cy);\r\n            cx += dx;\r\n            cy += dy;\r\n            time++;\r\n        }\r\n    }\r\n    return time;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minTimeToVisitAllPoints(points: number[][]): number {\r\n    let time = 0;\r\n    let cx = points[0][0];\r\n    let cy = points[0][1];\r\n    for (let i = 1; i &lt; points.length; i++) {\r\n        let tx = points[i][0];\r\n        let ty = points[i][1];\r\n        while (cx !== tx || cy !== ty) {\r\n            const dx = Math.sign(tx - cx);\r\n            const dy = Math.sign(ty - cy);\r\n            cx += dx;\r\n            cy += dy;\r\n            time++;\r\n        }\r\n    }\r\n    return time;\r\n}" },]} />

**Complexity**
- Time: O(N * K), where N is the number of points and K is the maximum distance between any two points. In the worst case, K can be up to 2 * 10⁴.
- Space: O(1), only a few variables are used.
- Notes: While this approach is intuitive, it is less efficient than the mathematical approach for large distances.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Time Visiting All Points Solutions","datePublished":"2025-12-15","description":"Calculate the minimum time to visit all points in order on a 2D plane, moving one unit per second in any of 8 directions.","keywords":"LeetCode, Minimum Time Visiting All Points, Easy, Array, Math, Geometry"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Mathematical Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N), where N is the number of points."}},{"@type":"Question","name":"What is the space complexity of the Mathematical Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1), as we only use a constant amount of extra space."}},{"@type":"Question","name":"Why is the Chebyshev distance used for this problem?","acceptedAnswer":{"@type":"Answer","text":"Because diagonal movement allows covering one unit of horizontal and one unit of vertical distance simultaneously, the time taken is determined by the larger of the two distances."}}]}).replace(/</g,'\u003c')}
</script>