---
title: "Maximum Number of Pairs in Array"
summary: "Count pairs of equal numbers in an array and return the number of pairs formed and leftover elements."
date: "Jun 28, 2025"
tags:
  - Array
  - Hash Table
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 76.10% | **Paid:** No
> **Topics:** Array, Hash Table, Counting

You are given a 0-indexed integer array nums. In one operation, you may do the following:

Choose two integers from nums that are equal.
Remove both integers from nums, forming a pair.

The operation is done until no more pairs can be formed.

Return a 0-indexed integer array answer of size 2 where answer[0] is the number of pairs that are formed and answer[1] is the number of leftover integers in nums after doing the operation.

- Examples
- Constraints
- [Sorting Approach](#sorting-approach)
- [Hash Map Frequency Count](#hash-map-frequency-count)
- [Set Approach](#set-approach)

## Examples

```text
Input: nums = [1,3,2,1,3,2,2]
Output: [3,1]
Explanation:
- Remove the pair [1,1] from nums, resulting in nums = [3,2,3,2,2].
- Remove the pair [3,3] from nums, resulting in nums = [2,2,2].
- Remove the pair [2,2] from nums, resulting in nums = [2].
No more pairs can be formed. A total of 3 pairs have been formed, and there is 1 number leftover in nums.
```

```text
Input: nums = [1,1]
Output: [1,0]
Explanation: Remove the pair [1,1] from nums, resulting in nums = [].
No more pairs can be formed. A total of 1 pair has been formed, and there are 0 numbers leftover in nums.
```

```text
Input: nums = [0]
Output: [0,1]
Explanation: No pairs can be formed, and there is 1 number leftover in nums.
```

## Constraints

```text
1 <= nums.length <= 100
0 <= nums[i] <= 100
```

---

## Sorting Approach

**Intuition**
By sorting the array, equal numbers become adjacent, making it easy to count consecutive pairs.

**Steps**
- Sort the array in ascending order
- Iterate through the sorted array
- When two consecutive elements are equal, increment pair count and skip both
- Otherwise, move to the next element
- Calculate leftovers as total elements minus 2 times pairs

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def numberOfPairs(self, nums: List[int]) -&gt; List[int]:\n        nums.sort()\n        pairs = 0\n        i = 0\n        n = len(nums)\n        while i &lt; n:\n            if i + 1 &lt; n and nums[i] == nums[i + 1]:\n                pairs += 1\n                i += 2\n            else:\n                i += 1\n        leftovers = n - 2 * pairs\n        return [pairs, leftovers]" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int[] numberOfPairs(int[] nums) {\n        Arrays.sort(nums);\n        int pairs = 0;\n        int n = nums.length;\n        int i = 0;\n        while (i &lt; n) {\n            if (i + 1 &lt; n && nums[i] == nums[i + 1]) {\n                pairs++;\n                i += 2;\n            } else {\n                i++;\n            }\n        }\n        int leftovers = n - 2 * pairs;\n        return new int[]{pairs, leftovers};\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; numberOfPairs(vector&lt;int&gt;& nums) {\n        sort(nums.begin(), nums.end());\n        int pairs = 0;\n        int n = nums.size();\n        int i = 0;\n        while (i &lt; n) {\n            if (i + 1 &lt; n && nums[i] == nums[i + 1]) {\n                pairs++;\n                i += 2;\n            } else {\n                i++;\n            }\n        }\n        int leftovers = n - 2 * pairs;\n        return {pairs, leftovers};\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar numberOfPairs = function(nums) {\n    nums.sort((a, b) =&gt; a - b);\n    let pairs = 0;\n    const n = nums.length;\n    let i = 0;\n    while (i &lt; n) {\n        if (i + 1 &lt; n && nums[i] === nums[i + 1]) {\n            pairs++;\n            i += 2;\n        } else {\n            i++;\n        }\n    }\n    const leftovers = n - 2 * pairs;\n    return [pairs, leftovers];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfPairs(nums: number[]): number[] {\n    nums.sort((a, b) =&gt; a - b);\n    let pairs = 0;\n    const n = nums.length;\n    let i = 0;\n    while (i &lt; n) {\n        if (i + 1 &lt; n && nums[i] === nums[i + 1]) {\n            pairs++;\n            i += 2;\n        } else {\n            i++;\n        }\n    }\n    const leftovers = n - 2 * pairs;\n    return [pairs, leftovers];\n}" },]} />

**Complexity**
- Time: O(n log n) due to sorting
- Space: O(1) or O(n) depending on sorting implementation
- Notes: Simple but not optimal due to sorting overhead

## Hash Map Frequency Count

**Intuition**
Count the frequency of each number, then calculate pairs and leftovers directly from the frequencies.

**Steps**
- Build a frequency map of all numbers
- For each unique number, add frequency // 2 to pairs
- Add frequency % 2 to leftovers
- Return the result array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\nfrom collections import Counter\n\nclass Solution:\n    def numberOfPairs(self, nums: List[int]) -&gt; List[int]:\n        freq = Counter(nums)\n        pairs = 0\n        leftovers = 0\n        for count in freq.values():\n            pairs += count // 2\n            leftovers += count % 2\n        return [pairs, leftovers]" },
{ name: "Java", lang: "java", code: "import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public int[] numberOfPairs(int[] nums) {\n        Map&lt;Integer, Integer&gt; freq = new HashMap&lt;&gt;();\n        for (int num : nums) {\n            freq.put(num, freq.getOrDefault(num, 0) + 1);\n        }\n        int pairs = 0;\n        int leftovers = 0;\n        for (int count : freq.values()) {\n            pairs += count / 2;\n            leftovers += count % 2;\n        }\n        return new int[]{pairs, leftovers};\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_map&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; numberOfPairs(vector&lt;int&gt;& nums) {\n        unordered_map&lt;int, int&gt; freq;\n        for (int num : nums) {\n            freq[num]++;\n        }\n        int pairs = 0;\n        int leftovers = 0;\n        for (const auto& p : freq) {\n            pairs += p.second / 2;\n            leftovers += p.second % 2;\n        }\n        return {pairs, leftovers};\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar numberOfPairs = function(nums) {\n    const freq = new Map();\n    for (const num of nums) {\n        freq.set(num, (freq.get(num) || 0) + 1);\n    }\n    let pairs = 0;\n    let leftovers = 0;\n    for (const count of freq.values()) {\n        pairs += Math.floor(count / 2);\n        leftovers += count % 2;\n    }\n    return [pairs, leftovers];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfPairs(nums: number[]): number[] {\n    const freq = new Map&lt;number, number&gt;();\n    for (const num of nums) {\n        freq.set(num, (freq.get(num) || 0) + 1);\n    }\n    let pairs = 0;\n    let leftovers = 0;\n    for (const count of freq.values()) {\n        pairs += Math.floor(count / 2);\n        leftovers += count % 2;\n    }\n    return [pairs, leftovers];\n}" },]} />

**Complexity**
- Time: O(n) for single pass through array and map
- Space: O(n) for the frequency map
- Notes: Optimal time complexity with clear logic

## Set Approach

**Intuition**
Use a set to track unpaired numbers. When encountering a number already in the set, we found a pair.

**Steps**
- Initialize an empty set to track unpaired numbers
- For each number in the array:
  - If number exists in set, remove it and increment pair count
  - Otherwise, add the number to the set
- Leftovers equals the size of the set
- Return the result array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def numberOfPairs(self, nums: List[int]) -&gt; List[int]:\n        seen = set()\n        pairs = 0\n        for num in nums:\n            if num in seen:\n                seen.remove(num)\n                pairs += 1\n            else:\n                seen.add(num)\n        leftovers = len(seen)\n        return [pairs, leftovers]" },
{ name: "Java", lang: "java", code: "import java.util.HashSet;\nimport java.util.Set;\n\nclass Solution {\n    public int[] numberOfPairs(int[] nums) {\n        Set&lt;Integer&gt; seen = new HashSet&lt;&gt;();\n        int pairs = 0;\n        for (int num : nums) {\n            if (seen.contains(num)) {\n                seen.remove(num);\n                pairs++;\n            } else {\n                seen.add(num);\n            }\n        }\n        int leftovers = seen.size();\n        return new int[]{pairs, leftovers};\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; numberOfPairs(vector&lt;int&gt;& nums) {\n        unordered_set&lt;int&gt; seen;\n        int pairs = 0;\n        for (int num : nums) {\n            if (seen.count(num)) {\n                seen.erase(num);\n                pairs++;\n            } else {\n                seen.insert(num);\n            }\n        }\n        int leftovers = seen.size();\n        return {pairs, leftovers};\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar numberOfPairs = function(nums) {\n    const seen = new Set();\n    let pairs = 0;\n    for (const num of nums) {\n        if (seen.has(num)) {\n            seen.delete(num);\n            pairs++;\n        } else {\n            seen.add(num);\n        }\n    }\n    const leftovers = seen.size;\n    return [pairs, leftovers];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfPairs(nums: number[]): number[] {\n    const seen = new Set&lt;number&gt;();\n    let pairs = 0;\n    for (const num of nums) {\n        if (seen.has(num)) {\n            seen.delete(num);\n            pairs++;\n        } else {\n            seen.add(num);\n        }\n    }\n    const leftovers = seen.size;\n    return [pairs, leftovers];\n}" },]} />

**Complexity**
- Time: O(n) for single pass through array
- Space: O(n) for the set in worst case
- Notes: Elegant solution with constant-time set operations

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Maximum Number of Pairs in Array Solutions","datePublished":"2025-06-28","description":"Count pairs of equal numbers in an array and return the number of pairs formed and leftover elements.","keywords":"LeetCode, Maximum Number of Pairs in Array, Easy, Array, Hash Table, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Sorting Approach?","acceptedAnswer":{"@type":"Answer","text":"The Sorting Approach has a time complexity of O(n log n) due to the sorting step."}},{"@type":"Question","name":"What is the time complexity of the Hash Map Frequency Count approach?","acceptedAnswer":{"@type":"Answer","text":"The Hash Map Frequency Count approach has a time complexity of O(n) as it requires a single pass through the array and the frequency map."}},{"@type":"Question","name":"What is the time complexity of the Set Approach?","acceptedAnswer":{"@type":"Answer","text":"The Set Approach has a time complexity of O(n) with constant-time set operations for each element."}},{"@type":"Question","name":"Which approach has the best space complexity?","acceptedAnswer":{"@type":"Answer","text":"The Sorting Approach can have O(1) space complexity if using an in-place sorting algorithm, while Hash Map and Set approaches require O(n) space."}}]}).replace(/</g,'\u003c')}
</script>