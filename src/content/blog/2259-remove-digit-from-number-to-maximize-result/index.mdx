---
title: "Remove Digit From Number to Maximize Result"
summary: "Remove one occurrence of a digit from a number to maximize the resulting value."
date: "Apr 21, 2024"
tags:
  - String
  - Greedy
  - Enumeration
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 48.50% | **Paid:** No
> **Topics:** String, Greedy, Enumeration

You are given a string number representing a positive integer and a character digit.

Return the resulting string after removing exactly one occurrence of digit from number such that the value of the resulting string in decimal form is maximized. The test cases are generated such that digit occurs at least once in number.

- Examples
- Constraints
- [Brute Force Enumeration](#brute-force-enumeration)
- [Greedy Approach](#greedy-approach)

## Examples

**Example 1:**
```text
Input: number = "1231", digit = "1"
Output: "231"
Explanation: We can remove the first '1' to get "231" or remove the second '1' to get "123".
Since 231 > 123, we return "231".
```

**Example 2:**
```text
Input: number = "551", digit = "5"
Output: "51"
Explanation: We can remove the first '5' to get "51" or remove the second '5' to get "51".
Since both are equal, we return "51".
```

**Example 3:**
```text
Input: number = "123141", digit = "1"
Output: "23141"
Explanation: We can remove the first '1' to get "23141", remove the second '1' to get "12341", or remove the third '1' to get "12314".
Since 23141 > 12341 > 12314, we return "23141".
```

## Constraints

```text
3 <= number.length <= 100
number consists of digits from '1' to '9'.
digit is a digit from '1' to '9'.
digit occurs at least once in number.
```

---
## Brute Force Enumeration

**Intuition**
Try removing each occurrence of the digit and keep track of the maximum result.

**Steps**
- Iterate through all positions in the string
- When we find the target digit, create a new string by removing that character
- Compare the new string with the current maximum and update if larger
- Return the maximum result found

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def removeDigit(self, number: str, digit: str) -> str:
        max_result = ""
        for i, c in enumerate(number):
            if c == digit:
                new_num = number[:i] + number[i+1:]
                if new_num &gt; max_result:
                    max_result = new_num
        return max_result` },
{ name: "Java", lang: "java", code: `class Solution {
    public String removeDigit(String number, char digit) {
        String maxResult = "";
        for (int i = 0; i &lt; number.length(); i++) {
            if (number.charAt(i) == digit) {
                String newNum = number.substring(0, i) + number.substring(i + 1);
                if (newNum.compareTo(maxResult) &gt; 0) {
                    maxResult = newNum;
                }
            }
        }
        return maxResult;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    string removeDigit(string number, char digit) {
        string maxResult = "";
        for (int i = 0; i &lt; number.size(); i++) {
            if (number[i] == digit) {
                string newNum = number.substr(0, i) + number.substr(i + 1);
                if (newNum &gt; maxResult) {
                    maxResult = newNum;
                }
            }
        }
        return maxResult;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    removeDigit(number, digit) {
        let maxResult = "";
        for (let i = 0; i &lt; number.length; i++) {
            if (number[i] === digit) {
                const newNum = number.slice(0, i) + number.slice(i + 1);
                if (newNum &gt; maxResult) {
                    maxResult = newNum;
                }
            }
        }
        return maxResult;
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    removeDigit(number: string, digit: string): string {
        let maxResult = "";
        for (let i = 0; i &lt; number.length; i++) {
            if (number[i] === digit) {
                const newNum = number.slice(0, i) + number.slice(i + 1);
                if (newNum &gt; maxResult) {
                    maxResult = newNum;
                }
            }
        }
        return maxResult;
    }
}` },]} />

**Complexity**
- Time: O(n²) where n is the length of the string (string concatenation takes O(n))
- Space: O(n) for storing the result strings
- Notes: Simple to understand but not optimal due to string concatenation overhead

---
## Greedy Approach

**Intuition**
To maximize the result, we should remove the first occurrence of the digit that is followed by a larger digit. If no such position exists, remove the last occurrence.

**Steps**
- Iterate through the string (except the last character)
- If we find the target digit followed by a larger digit, remove this occurrence and return
- If no such position is found, remove the last occurrence of the digit

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def removeDigit(self, number: str, digit: str) -> str:
        for i in range(len(number) - 1):
            if number[i] == digit and number[i + 1] &gt; digit:
                return number[:i] + number[i+1:]
        # If no such position found, remove the last occurrence
        last_pos = number.rfind(digit)
        return number[:last_pos] + number[last_pos+1:]` },
{ name: "Java", lang: "java", code: `class Solution {
    public String removeDigit(String number, char digit) {
        for (int i = 0; i &lt; number.length() - 1; i++) {
            if (number.charAt(i) == digit && number.charAt(i + 1) &gt; digit) {
                return number.substring(0, i) + number.substring(i + 1);
            }
        }
        // If no such position found, remove the last occurrence
        int lastPos = number.lastIndexOf(digit);
        return number.substring(0, lastPos) + number.substring(lastPos + 1);
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    string removeDigit(string number, char digit) {
        for (int i = 0; i &lt; number.size() - 1; i++) {
            if (number[i] == digit && number[i + 1] &gt; digit) {
                return number.substr(0, i) + number.substr(i + 1);
            }
        }
        // If no such position found, remove the last occurrence
        int lastPos = number.rfind(digit);
        return number.substr(0, lastPos) + number.substr(lastPos + 1);
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    removeDigit(number, digit) {
        for (let i = 0; i &lt; number.length - 1; i++) {
            if (number[i] === digit && number[i + 1] &gt; digit) {
                return number.slice(0, i) + number.slice(i + 1);
            }
        }
        // If no such position found, remove the last occurrence
        const lastPos = number.lastIndexOf(digit);
        return number.slice(0, lastPos) + number.slice(lastPos + 1);
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    removeDigit(number: string, digit: string): string {
        for (let i = 0; i &lt; number.length - 1; i++) {
            if (number[i] === digit && number[i + 1] &gt; digit) {
                return number.slice(0, i) + number.slice(i + 1);
            }
        }
        // If no such position found, remove the last occurrence
        const lastPos = number.lastIndexOf(digit);
        return number.slice(0, lastPos) + number.slice(lastPos + 1);
    }
}` },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for the result string
- Notes: Optimal solution with single pass through the string

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Remove Digit From Number to Maximize Result Solutions","datePublished":"2024-04-21","description":"Remove one occurrence of a digit from a number to maximize the resulting value.","keywords":"LeetCode, Remove Digit From Number to Maximize Result, Easy, String, Greedy, Enumeration"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n²) where n is the length of the string, due to string concatenation operations."}},{"@type":"Question","name":"What is the time complexity of the Greedy approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we only need a single pass."}},{"@type":"Question","name":"Why does the greedy approach work?","acceptedAnswer":{"@type":"Answer","text":"Removing a digit that is followed by a larger digit maximizes the result because the larger digit moves to a more significant position. If no such position exists, removing the last occurrence is optimal."}}]}).replace(/</g,'\u003c')}
</script>