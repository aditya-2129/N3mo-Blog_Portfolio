---
title: "Average Salary Excluding the Minimum and Maximum Salary"
summary: "Calculate the average salary excluding the minimum and maximum salary from an array."
date: "Aug 19, 2024"
tags:
  - Array
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 63.60% | **Paid:** No
> **Topics:** Array, Sorting

You are given an array of unique integers salary where salary[i] is the salary of the ith employee.

Return the average salary of employees excluding the minimum and maximum salary. The answer within 10⁻⁵ of the actual answer will be accepted.

- Examples
- Constraints
- [Sorting Approach](#sorting-approach)
- [Linear Scan Approach](#linear-scan-approach)
- [Built-in Functions Approach](#built-in-functions-approach)

## Examples

### Example 1
```text
Input: salary = [4000,3000,1000,2000]
Output: 2500.00000
Explanation: Minimum salary is 1000, maximum salary is 4000. 
Average salary excluding min and max is (3000+2000)/2 = 2500
```

### Example 2
```text
Input: salary = [1000,2000,3000]
Output: 2000.00000
Explanation: Minimum salary is 1000, maximum salary is 3000. 
Average salary excluding min and max is 2000/1 = 2000
```

## Constraints

```text
- 3 <= salary.length <= 100
- 1000 <= salary[i] <= 10^6
- All the integers of salary are unique.
```

---
## Sorting Approach

**Intuition**
Sort the array to easily identify and exclude the minimum (first element) and maximum (last element) values.

**Steps**
- Sort the salary array in ascending order
- Sum all elements except the first (minimum) and last (maximum)
- Divide the sum by (n-2) to get the average

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def average(self, salary: list[int]) -> float:
        salary.sort()
        return sum(salary[1:-1]) / (len(salary) - 2)
` },
{ name: "Java", lang: "java", code: `import java.util.Arrays;

class Solution {
    public double average(int[] salary) {
        Arrays.sort(salary);
        double sum = 0;
        for (int i = 1; i &lt; salary.length - 1; i++) {
            sum += salary[i];
        }
        return sum / (salary.length - 2);
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <algorithm>
#include <numeric>

using namespace std;

class Solution {
public:
    double average(vector<int>& salary) {
        sort(salary.begin(), salary.end());
        double sum = 0;
        for (int i = 1; i &lt; salary.size() - 1; i++) {
            sum += salary[i];
        }
        return sum / (salary.size() - 2);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} salary
 * @return {number}
 */
var average = function(salary) {
    salary.sort((a, b) =&gt; a - b);
    let sum = 0;
    for (let i = 1; i &lt; salary.length - 1; i++) {
        sum += salary[i];
    }
    return sum / (salary.length - 2);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function average(salary: number[]): number {
    salary.sort((a, b) =&gt; a - b);
    let sum = 0;
    for (let i = 1; i &lt; salary.length - 1; i++) {
        sum += salary[i];
    }
    return sum / (salary.length - 2);
}
` },]} />

**Complexity**
- Time: O(n log n) for sorting
- Space: O(1) or O(n) depending on sorting implementation
- Notes: Simple but not optimal due to sorting overhead

---
## Linear Scan Approach

**Intuition**
Find minimum and maximum values in a single pass, then calculate the average excluding these two values.

**Steps**
- Initialize min and max variables
- Calculate total sum of all salaries
- Subtract min and max from the total sum
- Divide by (n-2) to get the average

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def average(self, salary: list[int]) -> float:
        min_sal = float('inf')
        max_sal = float('-inf')
        total = 0
        
        for s in salary:
            min_sal = min(min_sal, s)
            max_sal = max(max_sal, s)
            total += s
        
        return (total - min_sal - max_sal) / (len(salary) - 2)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public double average(int[] salary) {
        int minSal = Integer.MAX_VALUE;
        int maxSal = Integer.MIN_VALUE;
        double total = 0;
        
        for (int s : salary) {
            minSal = Math.min(minSal, s);
            maxSal = Math.max(maxSal, s);
            total += s;
        }
        
        return (total - minSal - maxSal) / (salary.length - 2);
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <climits>

using namespace std;

class Solution {
public:
    double average(vector<int>& salary) {
        int minSal = INT_MAX;
        int maxSal = INT_MIN;
        double total = 0;
        
        for (int s : salary) {
            minSal = min(minSal, s);
            maxSal = max(maxSal, s);
            total += s;
        }
        
        return (total - minSal - maxSal) / (salary.size() - 2);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} salary
 * @return {number}
 */
var average = function(salary) {
    let minSal = Infinity;
    let maxSal = -Infinity;
    let total = 0;
    
    for (const s of salary) {
        minSal = Math.min(minSal, s);
        maxSal = Math.max(maxSal, s);
        total += s;
    }
    
    return (total - minSal - maxSal) / (salary.length - 2);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function average(salary: number[]): number {
    let minSal = Infinity;
    let maxSal = -Infinity;
    let total = 0;
    
    for (const s of salary) {
        minSal = Math.min(minSal, s);
        maxSal = Math.max(maxSal, s);
        total += s;
    }
    
    return (total - minSal - maxSal) / (salary.length - 2);
}
` },]} />

**Complexity**
- Time: O(n) - single pass through the array
- Space: O(1) - only using constant extra space
- Notes: Optimal solution with linear time complexity

---
## Built-in Functions Approach

**Intuition**
Use language built-in functions to find min, max, and sum directly.

**Steps**
- Use built-in min function to find minimum salary
- Use built-in max function to find maximum salary
- Use built-in sum function to get total
- Calculate average excluding min and max

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def average(self, salary: list[int]) -> float:
        return (sum(salary) - min(salary) - max(salary)) / (len(salary) - 2)
` },
{ name: "Java", lang: "java", code: `import java.util.Arrays;
import java.util.IntSummaryStatistics;

class Solution {
    public double average(int[] salary) {
        IntSummaryStatistics stats = Arrays.stream(salary).summaryStatistics();
        return (stats.getSum() - stats.getMin() - stats.getMax()) / (double)(salary.length - 2);
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <algorithm>
#include <numeric>

using namespace std;

class Solution {
public:
    double average(vector<int>& salary) {
        int minSal = *min_element(salary.begin(), salary.end());
        int maxSal = *max_element(salary.begin(), salary.end());
        double total = accumulate(salary.begin(), salary.end(), 0);
        return (total - minSal - maxSal) / (salary.size() - 2);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} salary
 * @return {number}
 */
var average = function(salary) {
    const minSal = Math.min(...salary);
    const maxSal = Math.max(...salary);
    const total = salary.reduce((a, b) =&gt; a + b, 0);
    return (total - minSal - maxSal) / (salary.length - 2);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function average(salary: number[]): number {
    const minSal = Math.min(...salary);
    const maxSal = Math.max(...salary);
    const total = salary.reduce((a, b) =&gt; a + b, 0);
    return (total - minSal - maxSal) / (salary.length - 2);
}
` },]} />

**Complexity**
- Time: O(n) - built-in functions iterate through the array
- Space: O(1) - constant extra space
- Notes: Clean and readable code using language features

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Average Salary Excluding the Minimum and Maximum Salary Solutions","datePublished":"2024-08-19","description":"Calculate the average salary excluding the minimum and maximum salary from an array.","keywords":"LeetCode, Average Salary Excluding the Minimum and Maximum Salary, Easy, Array, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Sorting Approach?","acceptedAnswer":{"@type":"Answer","text":"The Sorting Approach has a time complexity of O(n log n) due to the sorting operation."}},{"@type":"Question","name":"What is the time complexity of the Linear Scan Approach?","acceptedAnswer":{"@type":"Answer","text":"The Linear Scan Approach has a time complexity of O(n) as it only requires a single pass through the array."}},{"@type":"Question","name":"What is the space complexity of the optimal solution?","acceptedAnswer":{"@type":"Answer","text":"The optimal solution (Linear Scan or Built-in Functions) has a space complexity of O(1) as it only uses constant extra space."}}]}).replace(/</g,'\u003c')}
</script>