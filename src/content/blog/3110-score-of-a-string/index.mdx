---
title: "Score of a String"
summary: "Calculate the sum of absolute differences between ASCII values of adjacent characters in a string."
date: "Apr 17, 2026"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 91.30% | **Paid:** No
> **Topics:** String

You are given a string s. The score of s is the sum of the absolute difference between the ASCII values of adjacent characters.

Return the score of s.

- Examples
- Constraints
- [Iterative Simulation](#iterative-simulation)
- [Functional Approach](#functional-approach)
- [Pre-calculation](#pre-calculation)

## Examples

**Example 1:**

```text
Input: s = "hello"
Output: 13
Explanation:
The ASCII values of the characters in s are: 'h' = 104, 'e' = 101, 'l' = 108, 'l' = 108, 'o' = 111.
The score of s is |104 - 101| + |101 - 108| + |108 - 108| + |108 - 111| = 3 + 7 + 0 + 3 = 13.
```

**Example 2:**

```text
Input: s = "zaz"
Output: 50
Explanation:
The ASCII values of the characters in s are: 'z' = 122, 'a' = 97, 'z' = 122.
The score of s is |122 - 97| + |97 - 122| = 25 + 25 = 50.
```

## Constraints

```text
2 <= s.length <= 100
s consists only of lowercase English letters.
```

---
## Iterative Simulation

**Intuition**
We can iterate through the string once, comparing each character with its neighbor to accumulate the total score.

**Steps**
- Initialize a variable `score` to 0.
- Loop through the string from the first character to the second-to-last character.
- In each iteration, calculate the absolute difference between the ASCII value of the current character `s[i]` and the next character `s[i+1]`.
- Add this difference to `score`.
- Return `score`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def scoreOfString(self, s: str) -&gt; int:\n        score = 0\n        for i in range(len(s) - 1):\n            score += abs(ord(s[i]) - ord(s[i+1]))\n        return score" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int scoreOfString(String s) {\n        int score = 0;\n        for (int i = 0; i &lt; s.length() - 1; i++) {\n            score += Math.abs(s.charAt(i) - s.charAt(i + 1));\n        }\n        return score;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int scoreOfString(string s) {\n        int score = 0;\n        for (int i = 0; i &lt; s.size() - 1; ++i) {\n            score += abs(s[i] - s[i + 1]);\n        }\n        return score;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var scoreOfString = function(s) {\n    let score = 0;\n    for (let i = 0; i &lt; s.length - 1; i++) {\n        score += Math.abs(s.charCodeAt(i) - s.charCodeAt(i + 1));\n    }\n    return score;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function scoreOfString(s: string): number {\n    let score = 0;\n    for (let i = 0; i &lt; s.length - 1; i++) {\n        score += Math.abs(s.charCodeAt(i) - s.charCodeAt(i + 1));\n    }\n    return score;\n}" },]} />

**Complexity**
- Time: O(n), where n is the length of the string. We traverse the string once.
- Space: O(1), we only use a constant amount of extra space for the score variable.
- Notes: This is the most efficient approach for this problem.

---
## Functional Approach

**Intuition**
We can utilize functional programming constructs like `zip`, `map`, and `reduce` (or streams) to calculate the differences and sum them up in a declarative way.

**Steps**
- Create a sequence of pairs of adjacent characters.
- Transform each pair into the absolute difference of their ASCII values.
- Sum all the differences.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def scoreOfString(self, s: str) -&gt; int:\n        return sum(abs(a - b) for a, b in zip(s, s[1:]))" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\n\nclass Solution {\n    public int scoreOfString(String s) {\n        return IntStream.range(0, s.length() - 1)\n            .map(i -&gt; Math.abs(s.charAt(i) - s.charAt(i + 1)))\n            .sum();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;numeric&gt;\n#include &lt;functional&gt;\n\nclass Solution {\npublic:\n    int scoreOfString(string s) {\n        return std::inner_product(\n            s.begin(), s.end() - 1, \n            s.begin() + 1, \n            0, \n            std::plus&lt;&gt;(), \n            [](char a, char b) { return std::abs(a - b); }\n        );\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var scoreOfString = function(s) {\n    let score = 0;\n    for (let i = 0; i &lt; s.length - 1; i++) {\n        score += Math.abs(s.charCodeAt(i) - s.charCodeAt(i + 1));\n    }\n    return score;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function scoreOfString(s: string): number {\n    let score = 0;\n    for (let i = 0; i &lt; s.length - 1; i++) {\n        score += Math.abs(s.charCodeAt(i) - s.charCodeAt(i + 1));\n    }\n    return score;\n}" },]} />

**Complexity**
- Time: O(n), where n is the length of the string.
- Space: O(1), assuming the stream operations do not create significant intermediate structures (or O(n) for the slice in Python).
- Notes: While concise, this approach may have slight overhead compared to the raw iterative loop in some languages.

---
## Pre-calculation

**Intuition**
Convert the string into an array of integers representing ASCII values first, then perform the summation on this integer array.

**Steps**
- Create an integer array `nums` where `nums[i]` is the ASCII value of `s[i]`.
- Iterate through `nums` to calculate the sum of absolute differences between adjacent elements.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def scoreOfString(self, s: str) -&gt; int:\n        nums = [ord(c) for c in s]\n        score = 0\n        for i in range(len(nums) - 1):\n            score += abs(nums[i] - nums[i+1])\n        return score" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int scoreOfString(String s) {\n        int[] nums = new int[s.length()];\n        for (int i = 0; i &lt; s.length(); i++) {\n            nums[i] = s.charAt(i);\n        }\n        int score = 0;\n        for (int i = 0; i &lt; nums.length - 1; i++) {\n            score += Math.abs(nums[i] - nums[i + 1]);\n        }\n        return score;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int scoreOfString(string s) {\n        vector&lt;int&gt; nums(s.begin(), s.end());\n        int score = 0;\n        for (int i = 0; i &lt; nums.size() - 1; ++i) {\n            score += abs(nums[i] - nums[i + 1]);\n        }\n        return score;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var scoreOfString = function(s) {\n    const nums = s.split('').map(c =&gt; c.charCodeAt(0));\n    let score = 0;\n    for (let i = 0; i &lt; nums.length - 1; i++) {\n        score += Math.abs(nums[i] - nums[i + 1]);\n    }\n    return score;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function scoreOfString(s: string): number {\n    const nums: number[] = s.split('').map(c =&gt; c.charCodeAt(0));\n    let score = 0;\n    for (let i = 0; i &lt; nums.length - 1; i++) {\n        score += Math.abs(nums[i] - nums[i + 1]);\n    }\n    return score;\n}" },]} />

**Complexity**
- Time: O(n), where n is the length of the string.
- Space: O(n), to store the array of ASCII values.
- Notes: This approach uses extra memory which is not strictly necessary for this problem.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Score of a String Solutions","datePublished":"2026-04-17","description":"Calculate the sum of absolute differences between ASCII values of adjacent characters in a string.","keywords":"LeetCode, Score of a String, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string."}},{"@type":"Question","name":"What is the space complexity of the Pre-calculation approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) because we store the ASCII values in an auxiliary array."}},{"@type":"Question","name":"Which approach is the most optimal for Score of a String?","acceptedAnswer":{"@type":"Answer","text":"The Iterative Simulation approach is the most optimal as it runs in O(n) time and uses O(1) auxiliary space."}}]}).replace(/</g,'\u003c')}
</script>