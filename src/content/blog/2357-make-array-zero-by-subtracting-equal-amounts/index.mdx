---
title: "Make Array Zero by Subtracting Equal Amounts"
summary: "Find the minimum number of operations to make all array elements zero by subtracting equal amounts from non-zero elements."
date: "Mar 22, 2026"
tags:
  - Array
  - Hash Table
  - Greedy
  - Sorting
  - Heap
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 73.80% | **Paid:** No
> **Topics:** Array, Hash Table, Greedy, Sorting, Heap (Priority Queue), Simulation

You are given a non-negative integer array nums. In one operation, you must:

Choose a positive integer x such that x is less than or equal to the smallest non-zero element in nums.
Subtract x from every positive element in nums.

Return the minimum number of operations to make every element in nums equal to 0.

- Examples
- Constraints
- [Simulation Approach](#simulation-approach)
- [Hash Set Approach](#hash-set-approach)
- [Sorting Approach](#sorting-approach)
- [Priority Queue Approach](#priority-queue-approach)

## Examples

### Example 1
```text
Input: nums = [1,5,0,3,5]
Output: 3
Explanation:
In the first operation, choose x = 1. Now, nums = [0,4,0,2,4].
In the second operation, choose x = 2. Now, nums = [0,2,0,0,2].
In the third operation, choose x = 2. Now, nums = [0,0,0,0,0].
```

### Example 2
```text
Input: nums = [0]
Output: 0
Explanation:
Each element is already 0, so no operations are needed.
```

## Constraints

```text
1 <= nums.length <= 100
0 <= nums[i] <= 100
```

## Simulation Approach

**Intuition**
Simulate the process by repeatedly finding the minimum non-zero element and subtracting it from all non-zero elements until all elements become zero.

**Steps**
- Initialize operations count to 0
- While there are non-zero elements in the array:
  - Find the minimum non-zero element
  - Subtract this minimum from all non-zero elements
  - Increment operations count
- Return the operations count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumOperations(self, nums: list[int]) -&gt; int:\n        operations = 0\n        while True:\n            # Find minimum non-zero element\n            min_non_zero = float('inf')\n            has_non_zero = False\n            for num in nums:\n                if num &gt; 0:\n                    has_non_zero = True\n                    min_non_zero = min(min_non_zero, num)\n            \n            if not has_non_zero:\n                break\n            \n            # Subtract min_non_zero from all non-zero elements\n            for i in range(len(nums)):\n                if nums[i] &gt; 0:\n                    nums[i] -= min_non_zero\n            \n            operations += 1\n        \n        return operations" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimumOperations(int[] nums) {\n        int operations = 0;\n        \n        while (true) {\n            // Find minimum non-zero element\n            int minNonZero = Integer.MAX_VALUE;\n            boolean hasNonZero = false;\n            \n            for (int num : nums) {\n                if (num &gt; 0) {\n                    hasNonZero = true;\n                    minNonZero = Math.min(minNonZero, num);\n                }\n            }\n            \n            if (!hasNonZero) {\n                break;\n            }\n            \n            // Subtract minNonZero from all non-zero elements\n            for (int i = 0; i &lt; nums.length; i++) {\n                if (nums[i] &gt; 0) {\n                    nums[i] -= minNonZero;\n                }\n            }\n            \n            operations++;\n        }\n        \n        return operations;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimumOperations(vector&lt;int&gt;& nums) {\n        int operations = 0;\n        \n        while (true) {\n            // Find minimum non-zero element\n            int minNonZero = INT_MAX;\n            bool hasNonZero = false;\n            \n            for (int num : nums) {\n                if (num &gt; 0) {\n                    hasNonZero = true;\n                    minNonZero = min(minNonZero, num);\n                }\n            }\n            \n            if (!hasNonZero) {\n                break;\n            }\n            \n            // Subtract minNonZero from all non-zero elements\n            for (int i = 0; i &lt; nums.size(); i++) {\n                if (nums[i] &gt; 0) {\n                    nums[i] -= minNonZero;\n                }\n            }\n            \n            operations++;\n        }\n        \n        return operations;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar minimumOperations = function(nums) {\n    let operations = 0;\n    \n    while (true) {\n        // Find minimum non-zero element\n        let minNonZero = Infinity;\n        let hasNonZero = false;\n        \n        for (const num of nums) {\n            if (num &gt; 0) {\n                hasNonZero = true;\n                minNonZero = Math.min(minNonZero, num);\n            }\n        }\n        \n        if (!hasNonZero) {\n            break;\n        }\n        \n        // Subtract minNonZero from all non-zero elements\n        for (let i = 0; i &lt; nums.length; i++) {\n            if (nums[i] &gt; 0) {\n                nums[i] -= minNonZero;\n            }\n        }\n        \n        operations++;\n    }\n    \n    return operations;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumOperations(nums: number[]): number {\n    let operations = 0;\n    \n    while (true) {\n        // Find minimum non-zero element\n        let minNonZero = Infinity;\n        let hasNonZero = false;\n        \n        for (const num of nums) {\n            if (num &gt; 0) {\n                hasNonZero = true;\n                minNonZero = Math.min(minNonZero, num);\n            }\n        }\n        \n        if (!hasNonZero) {\n            break;\n        }\n        \n        // Subtract minNonZero from all non-zero elements\n        for (let i = 0; i &lt; nums.length; i++) {\n            if (nums[i] &gt; 0) {\n                nums[i] -= minNonZero;\n            }\n        }\n        \n        operations++;\n    }\n    \n    return operations;\n}" },]} />

**Complexity**
- Time: O(n²) in the worst case where n is the length of the array
- Space: O(1)
- Notes: This approach directly simulates the process but is not the most efficient.

## Hash Set Approach

**Intuition**
Each unique positive value in the array requires exactly one operation. Therefore, the answer is simply the count of unique non-zero values.

**Steps**
- Create a hash set to store unique non-zero values
- Iterate through the array and add all non-zero values to the set
- Return the size of the set

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumOperations(self, nums: list[int]) -&gt; int:\n        # Count unique non-zero values\n        unique_non_zero = set()\n        for num in nums:\n            if num &gt; 0:\n                unique_non_zero.add(num)\n        return len(unique_non_zero)" },
{ name: "Java", lang: "java", code: "import java.util.HashSet;\nimport java.util.Set;\n\nclass Solution {\n    public int minimumOperations(int[] nums) {\n        // Count unique non-zero values\n        Set&lt;Integer&gt; uniqueNonZero = new HashSet&lt;&gt;();\n        for (int num : nums) {\n            if (num &gt; 0) {\n                uniqueNonZero.add(num);\n            }\n        }\n        return uniqueNonZero.size();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;unordered_set&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int minimumOperations(vector&lt;int&gt;& nums) {\n        // Count unique non-zero values\n        std::unordered_set&lt;int&gt; uniqueNonZero;\n        for (int num : nums) {\n            if (num &gt; 0) {\n                uniqueNonZero.insert(num);\n            }\n        }\n        return uniqueNonZero.size();\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar minimumOperations = function(nums) {\n    // Count unique non-zero values\n    const uniqueNonZero = new Set();\n    for (const num of nums) {\n        if (num &gt; 0) {\n            uniqueNonZero.add(num);\n        }\n    }\n    return uniqueNonZero.size;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumOperations(nums: number[]): number {\n    // Count unique non-zero values\n    const uniqueNonZero = new Set&lt;number&gt;();\n    for (const num of nums) {\n        if (num &gt; 0) {\n            uniqueNonZero.add(num);\n        }\n    }\n    return uniqueNonZero.size;\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(n) for the hash set
- Notes: This is the optimal solution with linear time complexity.

## Sorting Approach

**Intuition**
After sorting the array, we can count unique non-zero values by iterating through the sorted array and counting when we encounter a new value.

**Steps**
- Sort the array
- Iterate through the sorted array
- Count unique non-zero values
- Return the count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumOperations(self, nums: list[int]) -&gt; int:\n        nums.sort()\n        count = 0\n        prev = 0\n        \n        for num in nums:\n            if num &gt; 0 and num != prev:\n                count += 1\n                prev = num\n        \n        return count" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int minimumOperations(int[] nums) {\n        Arrays.sort(nums);\n        int count = 0;\n        int prev = 0;\n        \n        for (int num : nums) {\n            if (num &gt; 0 && num != prev) {\n                count++;\n                prev = num;\n            }\n        }\n        \n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int minimumOperations(vector&lt;int&gt;& nums) {\n        std::sort(nums.begin(), nums.end());\n        int count = 0;\n        int prev = 0;\n        \n        for (int num : nums) {\n            if (num &gt; 0 && num != prev) {\n                count++;\n                prev = num;\n            }\n        }\n        \n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar minimumOperations = function(nums) {\n    nums.sort((a, b) =&gt; a - b);\n    let count = 0;\n    let prev = 0;\n    \n    for (const num of nums) {\n        if (num &gt; 0 && num !== prev) {\n            count++;\n            prev = num;\n        }\n    }\n    \n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumOperations(nums: number[]): number {\n    nums.sort((a, b) =&gt; a - b);\n    let count = 0;\n    let prev = 0;\n    \n    for (const num of nums) {\n        if (num &gt; 0 && num !== prev) {\n            count++;\n            prev = num;\n        }\n    }\n    \n    return count;\n}" },]} />

**Complexity**
- Time: O(n log n) due to sorting
- Space: O(1) or O(n) depending on the sorting algorithm
- Notes: Sorting adds overhead but can be useful if we need the array sorted for other purposes.

## Priority Queue Approach

**Intuition**
Use a min-heap to efficiently get the smallest non-zero element in each operation, simulating the process more efficiently than the naive simulation.

**Steps**
- Add all non-zero elements to a min-heap
- While the heap is not empty:
  - Get the minimum element
  - If it's different from the previous minimum, increment operations
  - Update previous minimum
- Return the operations count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import heapq\n\nclass Solution:\n    def minimumOperations(self, nums: list[int]) -&gt; int:\n        # Create min-heap with non-zero elements\n        heap = [num for num in nums if num &gt; 0]\n        heapq.heapify(heap)\n        \n        operations = 0\n        prev = 0\n        \n        while heap:\n            curr = heapq.heappop(heap)\n            if curr != prev:\n                operations += 1\n                prev = curr\n        \n        return operations" },
{ name: "Java", lang: "java", code: "import java.util.PriorityQueue;\n\nclass Solution {\n    public int minimumOperations(int[] nums) {\n        // Create min-heap with non-zero elements\n        PriorityQueue&lt;Integer&gt; heap = new PriorityQueue&lt;&gt;();\n        for (int num : nums) {\n            if (num &gt; 0) {\n                heap.offer(num);\n            }\n        }\n        \n        int operations = 0;\n        int prev = 0;\n        \n        while (!heap.isEmpty()) {\n            int curr = heap.poll();\n            if (curr != prev) {\n                operations++;\n                prev = curr;\n            }\n        }\n        \n        return operations;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;queue&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int minimumOperations(vector&lt;int&gt;& nums) {\n        // Create min-heap with non-zero elements\n        std::priority_queue&lt;int, std::vector&lt;int&gt;, std::greater&lt;int&gt;&gt; heap;\n        for (int num : nums) {\n            if (num &gt; 0) {\n                heap.push(num);\n            }\n        }\n        \n        int operations = 0;\n        int prev = 0;\n        \n        while (!heap.empty()) {\n            int curr = heap.top();\n            heap.pop();\n            if (curr != prev) {\n                operations++;\n                prev = curr;\n            }\n        }\n        \n        return operations;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar minimumOperations = function(nums) {\n    // Create array with non-zero elements and sort\n    const heap = nums.filter(num =&gt; num &gt; 0).sort((a, b) =&gt; a - b);\n    \n    let operations = 0;\n    let prev = 0;\n    \n    while (heap.length &gt; 0) {\n        const curr = heap.shift();\n        if (curr !== prev) {\n            operations++;\n            prev = curr;\n        }\n    }\n    \n    return operations;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumOperations(nums: number[]): number {\n    // Create array with non-zero elements and sort\n    const heap = nums.filter(num =&gt; num &gt; 0).sort((a, b) =&gt; a - b);\n    \n    let operations = 0;\n    let prev = 0;\n    \n    while (heap.length &gt; 0) {\n        const curr = heap.shift()!;\n        if (curr !== prev) {\n            operations++;\n            prev = curr;\n        }\n    }\n    \n    return operations;\n}" },]} />

**Complexity**
- Time: O(n log n) for heap operations
- Space: O(n) for the heap
- Notes: Using a heap is more efficient than naive simulation but still not as optimal as the hash set approach.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Make Array Zero by Subtracting Equal Amounts Solutions","datePublished":"2026-03-22","description":"Find the minimum number of operations to make all array elements zero by subtracting equal amounts from non-zero elements.","keywords":"LeetCode, Make Array Zero by Subtracting Equal Amounts, Easy, Array, Hash Table, Greedy, Sorting, Heap (Priority Queue), Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The Hash Set approach has O(n) time complexity where n is the length of the array."}},{"@type":"Question","name":"What is the space complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The Hash Set approach has O(n) space complexity for storing unique non-zero values."}},{"@type":"Question","name":"Why is the Hash Set approach optimal?","acceptedAnswer":{"@type":"Answer","text":"Each unique positive value requires exactly one operation, so counting unique non-zero values gives the minimum number of operations directly."}},{"@type":"Question","name":"What is the time complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The Sorting approach has O(n log n) time complexity due to the sorting step."}},{"@type":"Question","name":"What is the time complexity of the Priority Queue approach?","acceptedAnswer":{"@type":"Answer","text":"The Priority Queue approach has O(n log n) time complexity for heap operations."}}]}).replace(/</g,'\u003c')}
</script>