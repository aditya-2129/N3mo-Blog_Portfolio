---
title: "Check if The Number is Fascinating"
summary: "Check if concatenating n, 2*n, and 3*n produces a number containing digits 1-9 exactly once."
date: "May 22, 2024"
tags:
  - Hash Table
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 53.10% | **Paid:** No
> **Topics:** Hash Table, Math

You are given an integer n. A number is fascinating if it concatenates n, 2*n, and 3*n, and the resulting number contains all digits from 1 to 9 exactly once and does not contain any 0.

Return true if n is fascinating, otherwise return false.

- Examples
- Constraints
- [String Concatenation with Set](#string-concatenation-with-set)
- [Array Counting](#array-counting)
- [Bit Manipulation](#bit-manipulation)

## Examples

### Example 1

**Input:**
```text
n = 192
```

**Output:**
```text
true
```

**Explanation:**
We concatenate the numbers n = 192 and 2 * n = 384 and 3 * n = 576. The resulting number is 192384576. This number contains all the digits from 1 to 9 exactly once.

### Example 2

**Input:**
```text
n = 100
```

**Output:**
```text
false
```

**Explanation:**
We concatenate the numbers n = 100 and 2 * n = 200 and 3 * n = 300. The resulting number is 100200300. This number does not satisfy any of the conditions.

## Constraints

```text
1 <= n <= 100
```

---
## String Concatenation with Set

**Intuition**
Concatenate n, 2*n, and 3*n as a string, then use a set to verify each digit from 1-9 appears exactly once with no zeros.

**Steps**
- Concatenate n, 2*n, and 3*n into a single string
- If length is not 9, return false
- Iterate through each character, rejecting zeros and duplicates
- Return true if set contains exactly 9 unique digits

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isFascinating(self, n: int) -&gt; bool:\n        s = str(n) + str(2*n) + str(3*n)\n        if len(s) != 9:\n            return False\n        seen = set()\n        for c in s:\n            if c == '0' or c in seen:\n                return False\n            seen.add(c)\n        return len(seen) == 9" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isFascinating(int n) {\n        String s = String.valueOf(n) + String.valueOf(2 * n) + String.valueOf(3 * n);\n        if (s.length() != 9) {\n            return false;\n        }\n        java.util.Set&lt;Character&gt; seen = new java.util.HashSet&lt;&gt;();\n        for (char c : s.toCharArray()) {\n            if (c == '0' || seen.contains(c)) {\n                return false;\n            }\n            seen.add(c);\n        }\n        return seen.size() == 9;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isFascinating(int n) {\n        string s = to_string(n) + to_string(2 * n) + to_string(3 * n);\n        if (s.length() != 9) {\n            return false;\n        }\n        unordered_set&lt;char&gt; seen;\n        for (char c : s) {\n            if (c == '0' || seen.count(c)) {\n                return false;\n            }\n            seen.insert(c);\n        }\n        return seen.size() == 9;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {boolean}\n */\nvar isFascinating = function(n) {\n    const s = String(n) + String(2 * n) + String(3 * n);\n    if (s.length !== 9) {\n        return false;\n    }\n    const seen = new Set();\n    for (const c of s) {\n        if (c === '0' || seen.has(c)) {\n            return false;\n        }\n        seen.add(c);\n    }\n    return seen.size === 9;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isFascinating(n: number): boolean {\n    const s = String(n) + String(2 * n) + String(3 * n);\n    if (s.length !== 9) {\n        return false;\n    }\n    const seen = new Set&lt;string&gt;();\n    for (const c of s) {\n        if (c === '0' || seen.has(c)) {\n            return false;\n        }\n        seen.add(c);\n    }\n    return seen.size === 9;\n}" },]} />

**Complexity**
- Time: O(1) - Fixed 9 characters to process
- Space: O(1) - Set holds at most 9 elements
- Notes: Clean and readable, uses hash set for O(1) lookups

---
## Array Counting

**Intuition**
Use a fixed-size array to count digit occurrences, checking that digits 1-9 each appear exactly once.

**Steps**
- Concatenate n, 2*n, and 3*n into a string
- Use an array of size 10 to count each digit
- Reject zeros and any digit appearing more than once
- Verify all digits 1-9 have count exactly 1

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isFascinating(self, n: int) -&gt; bool:\n        s = str(n) + str(2*n) + str(3*n)\n        if len(s) != 9:\n            return False\n        count = [0] * 10\n        for c in s:\n            digit = int(c)\n            if digit == 0:\n                return False\n            count[digit] += 1\n            if count[digit] &gt; 1:\n                return False\n        return all(count[i] == 1 for i in range(1, 10))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isFascinating(int n) {\n        String s = String.valueOf(n) + String.valueOf(2 * n) + String.valueOf(3 * n);\n        if (s.length() != 9) {\n            return false;\n        }\n        int[] count = new int[10];\n        for (char c : s.toCharArray()) {\n            int digit = c - '0';\n            if (digit == 0) {\n                return false;\n            }\n            count[digit]++;\n            if (count[digit] &gt; 1) {\n                return false;\n            }\n        }\n        for (int i = 1; i &lt;= 9; i++) {\n            if (count[i] != 1) {\n                return false;\n            }\n        }\n        return true;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isFascinating(int n) {\n        string s = to_string(n) + to_string(2 * n) + to_string(3 * n);\n        if (s.length() != 9) {\n            return false;\n        }\n        int count[10] = {0};\n        for (char c : s) {\n            int digit = c - '0';\n            if (digit == 0) {\n                return false;\n            }\n            count[digit]++;\n            if (count[digit] &gt; 1) {\n                return false;\n            }\n        }\n        for (int i = 1; i &lt;= 9; i++) {\n            if (count[i] != 1) {\n                return false;\n            }\n        }\n        return true;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {boolean}\n */\nvar isFascinating = function(n) {\n    const s = String(n) + String(2 * n) + String(3 * n);\n    if (s.length !== 9) {\n        return false;\n    }\n    const count = new Array(10).fill(0);\n    for (const c of s) {\n        const digit = parseInt(c);\n        if (digit === 0) {\n            return false;\n        }\n        count[digit]++;\n        if (count[digit] &gt; 1) {\n            return false;\n        }\n    }\n    for (let i = 1; i &lt;= 9; i++) {\n        if (count[i] !== 1) {\n            return false;\n        }\n    }\n    return true;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isFascinating(n: number): boolean {\n    const s = String(n) + String(2 * n) + String(3 * n);\n    if (s.length !== 9) {\n        return false;\n    }\n    const count: number[] = new Array(10).fill(0);\n    for (const c of s) {\n        const digit = parseInt(c);\n        if (digit === 0) {\n            return false;\n        }\n        count[digit]++;\n        if (count[digit] &gt; 1) {\n            return false;\n        }\n    }\n    for (let i = 1; i &lt;= 9; i++) {\n        if (count[i] !== 1) {\n            return false;\n        }\n    }\n    return true;\n}" },]} />

**Complexity**
- Time: O(1) - Fixed 9 characters to process
- Space: O(1) - Fixed array of size 10
- Notes: More memory efficient than hash set, direct index access

---
## Bit Manipulation

**Intuition**
Use a bitmask to track which digits have been seen, setting bit i when digit i is encountered.

**Steps**
- Concatenate n, 2*n, and 3*n into a string
- Use an integer as a bitmask, setting bit d for each digit d
- Reject zeros and any digit already seen (bit already set)
- Check if final mask equals 0b1111111110 (bits 1-9 all set)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isFascinating(self, n: int) -&gt; bool:\n        s = str(n) + str(2*n) + str(3*n)\n        if len(s) != 9:\n            return False\n        mask = 0\n        for c in s:\n            digit = int(c)\n            if digit == 0:\n                return False\n            bit = 1 &lt;&lt; digit\n            if mask & bit:\n                return False\n            mask |= bit\n        return mask == 0b1111111110" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isFascinating(int n) {\n        String s = String.valueOf(n) + String.valueOf(2 * n) + String.valueOf(3 * n);\n        if (s.length() != 9) {\n            return false;\n        }\n        int mask = 0;\n        for (char c : s.toCharArray()) {\n            int digit = c - '0';\n            if (digit == 0) {\n                return false;\n            }\n            int bit = 1 &lt;&lt; digit;\n            if ((mask & bit) != 0) {\n                return false;\n            }\n            mask |= bit;\n        }\n        return mask == 0b1111111110;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isFascinating(int n) {\n        string s = to_string(n) + to_string(2 * n) + to_string(3 * n);\n        if (s.length() != 9) {\n            return false;\n        }\n        int mask = 0;\n        for (char c : s) {\n            int digit = c - '0';\n            if (digit == 0) {\n                return false;\n            }\n            int bit = 1 &lt;&lt; digit;\n            if (mask & bit) {\n                return false;\n            }\n            mask |= bit;\n        }\n        return mask == 0b1111111110;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {boolean}\n */\nvar isFascinating = function(n) {\n    const s = String(n) + String(2 * n) + String(3 * n);\n    if (s.length !== 9) {\n        return false;\n    }\n    let mask = 0;\n    for (const c of s) {\n        const digit = parseInt(c);\n        if (digit === 0) {\n            return false;\n        }\n        const bit = 1 &lt;&lt; digit;\n        if ((mask & bit) !== 0) {\n            return false;\n        }\n        mask |= bit;\n    }\n    return mask === 0b1111111110;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isFascinating(n: number): boolean {\n    const s = String(n) + String(2 * n) + String(3 * n);\n    if (s.length !== 9) {\n        return false;\n    }\n    let mask = 0;\n    for (const c of s) {\n        const digit = parseInt(c);\n        if (digit === 0) {\n            return false;\n        }\n        const bit = 1 &lt;&lt; digit;\n        if ((mask & bit) !== 0) {\n            return false;\n        }\n        mask |= bit;\n    }\n    return mask === 0b1111111110;\n}" },]} />

**Complexity**
- Time: O(1) - Fixed 9 characters to process
- Space: O(1) - Single integer for bitmask
- Notes: Most space-efficient, bit operations are very fast

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check if The Number is Fascinating Solutions","datePublished":"2024-05-22","description":"Check if concatenating n, 2*n, and 3*n produces a number containing digits 1-9 exactly once.","keywords":"LeetCode, Check if The Number is Fascinating, Easy, Hash Table, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of String Concatenation with Set approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - We process a fixed 9 characters regardless of input size."}},{"@type":"Question","name":"What is the space complexity of Array Counting approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Uses a fixed array of size 10 to count digit occurrences."}},{"@type":"Question","name":"How does Bit Manipulation approach work?","acceptedAnswer":{"@type":"Answer","text":"Uses a bitmask where bit i represents if digit i has been seen, checking if final mask equals 0b1111111110."}},{"@type":"Question","name":"What makes a number fascinating?","acceptedAnswer":{"@type":"Answer","text":"A number is fascinating if concatenating n, 2*n, and 3*n produces a string containing digits 1-9 exactly once with no zeros."}}]}).replace(/</g,'\u003c')}
</script>