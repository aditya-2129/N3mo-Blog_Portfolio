---
title: "Semi-Ordered Permutation"
summary: "Find minimum adjacent swaps to move 1 to front and n to end in a permutation."
date: "Sep 29, 2025"
tags:
  - Array
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 64.20% | **Paid:** No
> **Topics:** Array, Simulation

You are given a permutation of the integers from 1 to n. A permutation is called semi-ordered if the first element is 1 and the last element is n.

You can perform the following operation any number of times:

Choose two adjacent elements and swap them.

Return the minimum number of operations needed to make the permutation semi-ordered.

## Examples

```text
Input: nums = [2,1,4,3]
Output: 2
Explanation: We can swap 2 and 1, then swap 4 and 3.
```

```text
Input: nums = [2,1,3]
Output: 2
Explanation: We can swap 2 and 1, then swap 3 and 1.
```

```text
Input: nums = [1,3,2]
Output: 1
Explanation: We can swap 3 and 2.
```

## Constraints

```text
2 <= nums.length <= 50
nums is a permutation of integers from 1 to nums.length.
```

- [Direct Position Calculation](#direct-position-calculation)
- [Simulation](#simulation)

---
## Direct Position Calculation

**Intuition**
The minimum swaps to move 1 to index 0 equals its current position, and moving n to index n-1 requires (n-1 - position of n) swaps. If 1 is to the right of n, moving 1 left shifts n right by one, requiring one less swap.

**Steps**
- Find the index of 1 (pos1) and index of n (posN)
- Calculate swaps as pos1 + (n-1 - posN)
- If pos1 &gt; posN, subtract 1 from total (since moving 1 past n shifts n right)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def semiOrderedPermutation(self, nums: List[int]) -&gt; int:\n        n = len(nums)\n        pos1 = nums.index(1)\n        posN = nums.index(n)\n        ans = pos1 + (n - 1 - posN)\n        if pos1 &gt; posN:\n            ans -= 1\n        return ans" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int semiOrderedPermutation(int[] nums) {\n        int n = nums.length;\n        int pos1 = -1, posN = -1;\n        for (int i = 0; i &lt; n; i++) {\n            if (nums[i] == 1) pos1 = i;\n            if (nums[i] == n) posN = i;\n        }\n        int ans = pos1 + (n - 1 - posN);\n        if (pos1 &gt; posN) ans--;\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int semiOrderedPermutation(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        int pos1 = -1, posN = -1;\n        for (int i = 0; i &lt; n; i++) {\n            if (nums[i] == 1) pos1 = i;\n            if (nums[i] == n) posN = i;\n        }\n        int ans = pos1 + (n - 1 - posN);\n        if (pos1 &gt; posN) ans--;\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    semiOrderedPermutation(nums) {\n        const n = nums.length;\n        let pos1 = -1, posN = -1;\n        for (let i = 0; i &lt; n; i++) {\n            if (nums[i] === 1) pos1 = i;\n            if (nums[i] === n) posN = i;\n        }\n        let ans = pos1 + (n - 1 - posN);\n        if (pos1 &gt; posN) ans--;\n        return ans;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    semiOrderedPermutation(nums: number[]): number {\n        const n = nums.length;\n        let pos1 = -1, posN = -1;\n        for (let i = 0; i &lt; n; i++) {\n            if (nums[i] === 1) pos1 = i;\n            if (nums[i] === n) posN = i;\n        }\n        let ans = pos1 + (n - 1 - posN);\n        if (pos1 &gt; posN) ans--;\n        return ans;\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal solution with single pass to find positions

---
## Simulation

**Intuition**
Simulate the actual swapping process: repeatedly swap 1 with its left neighbor until it reaches index 0, then swap n with its right neighbor until it reaches index n-1, counting each swap.

**Steps**
- Make a copy of the array to avoid modifying input
- While 1 is not at index 0, find its position and swap with left neighbor, increment count
- While n is not at last index, find its position and swap with right neighbor, increment count
- Return total swap count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def semiOrderedPermutation(self, nums: List[int]) -&gt; int:\n        n = len(nums)\n        arr = nums.copy()\n        swaps = 0\n        \n        while arr[0] != 1:\n            pos1 = arr.index(1)\n            arr[pos1], arr[pos1 - 1] = arr[pos1 - 1], arr[pos1]\n            swaps += 1\n        \n        while arr[-1] != n:\n            posN = arr.index(n)\n            arr[posN], arr[posN + 1] = arr[posN + 1], arr[posN]\n            swaps += 1\n        \n        return swaps" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int semiOrderedPermutation(int[] nums) {\n        int n = nums.length;\n        int[] arr = nums.clone();\n        int swaps = 0;\n        \n        while (arr[0] != 1) {\n            int pos1 = findIndex(arr, 1);\n            int temp = arr[pos1];\n            arr[pos1] = arr[pos1 - 1];\n            arr[pos1 - 1] = temp;\n            swaps++;\n        }\n        \n        while (arr[n - 1] != n) {\n            int posN = findIndex(arr, n);\n            int temp = arr[posN];\n            arr[posN] = arr[posN + 1];\n            arr[posN + 1] = temp;\n            swaps++;\n        }\n        \n        return swaps;\n    }\n    \n    private int findIndex(int[] arr, int target) {\n        for (int i = 0; i &lt; arr.length; i++) {\n            if (arr[i] == target) return i;\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int semiOrderedPermutation(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        vector&lt;int&gt; arr = nums;\n        int swaps = 0;\n        \n        while (arr[0] != 1) {\n            int pos1 = findIndex(arr, 1);\n            swap(arr[pos1], arr[pos1 - 1]);\n            swaps++;\n        }\n        \n        while (arr[n - 1] != n) {\n            int posN = findIndex(arr, n);\n            swap(arr[posN], arr[posN + 1]);\n            swaps++;\n        }\n        \n        return swaps;\n    }\n    \nprivate:\n    int findIndex(vector&lt;int&gt;& arr, int target) {\n        for (int i = 0; i &lt; arr.size(); i++) {\n            if (arr[i] == target) return i;\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    semiOrderedPermutation(nums) {\n        const n = nums.length;\n        const arr = [...nums];\n        let swaps = 0;\n        \n        while (arr[0] !== 1) {\n            const pos1 = arr.indexOf(1);\n            [arr[pos1], arr[pos1 - 1]] = [arr[pos1 - 1], arr[pos1]];\n            swaps++;\n        }\n        \n        while (arr[n - 1] !== n) {\n            const posN = arr.indexOf(n);\n            [arr[posN], arr[posN + 1]] = [arr[posN + 1], arr[posN]];\n            swaps++;\n        }\n        \n        return swaps;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    semiOrderedPermutation(nums: number[]): number {\n        const n = nums.length;\n        const arr = [...nums];\n        let swaps = 0;\n        \n        while (arr[0] !== 1) {\n            const pos1 = arr.indexOf(1);\n            [arr[pos1], arr[pos1 - 1]] = [arr[pos1 - 1], arr[pos1]];\n            swaps++;\n        }\n        \n        while (arr[n - 1] !== n) {\n            const posN = arr.indexOf(n);\n            [arr[posN], arr[posN + 1]] = [arr[posN + 1], arr[posN]];\n            swaps++;\n        }\n        \n        return swaps;\n    }\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(n)
- Notes: More intuitive but less efficient due to repeated index searches and array copies

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Semi-Ordered Permutation Solutions","datePublished":"2025-09-29","description":"Find minimum adjacent swaps to move 1 to front and n to end in a permutation.","keywords":"LeetCode, Semi-Ordered Permutation, Easy, Array, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Direct Position Calculation approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) - We traverse the array once to find positions of 1 and n."}},{"@type":"Question","name":"What is the space complexity of Direct Position Calculation approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - We only use a constant amount of extra space for variables."}},{"@type":"Question","name":"Why do we subtract 1 when position of 1 is greater than position of n?","acceptedAnswer":{"@type":"Answer","text":"When moving 1 to the front past n, n gets shifted one position to the right, reducing the needed swaps by 1."}},{"@type":"Question","name":"What is the time complexity of Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(n²) - Each swap requires finding the index, and we may need up to n swaps for each element."}}]}).replace(/</g,'\u003c')}
</script>