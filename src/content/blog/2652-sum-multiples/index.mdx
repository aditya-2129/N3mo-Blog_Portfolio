---
title: "Sum Multiples"
summary: "Given a positive integer n, return the sum of all numbers in the range [1, n] inclusive that are divisible by 3, 5, or 7."
date: "Mar 08, 2024"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 85.90% | **Paid:** No
> **Topics:** Math

Given a positive integer n, find the sum of all numbers in the range [1, n] inclusive that are divisible by 3, 5, or 7.

- Examples
- Constraints
- [Brute Force Iteration](#brute-force-iteration)
- [Mathematical Inclusion-Exclusion](#mathematical-inclusion-exclusion)

## Examples

```text
Input: n = 7
Output: 21
Explanation: Numbers in the range [1, 7] that are divisible by 3, 5, or 7 are 3, 5, 6, 7. The sum is 21.
```

```text
Input: n = 10
Output: 40
Explanation: Numbers in the range [1, 10] that are divisible by 3, 5, or 7 are 3, 5, 6, 7, 9, 10. The sum is 40.
```

```text
Input: n = 9
Output: 30
Explanation: Numbers in the range [1, 9] that are divisible by 3, 5, or 7 are 3, 5, 6, 7, 9. The sum is 30.
```

## Constraints

```text
1 <= n <= 10^3
```

---
## Brute Force Iteration

**Intuition**
Iterate through every number from 1 to n and check if it is divisible by 3, 5, or 7. If it is, add it to the running total.

**Steps**
- Initialize a variable `total` to 0.
- Loop from `i = 1` to `n` (inclusive).
- For each `i`, check if `i % 3 == 0` or `i % 5 == 0` or `i % 7 == 0`.
- If the condition is true, add `i` to `total`.
- Return `total`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfMultiples(self, n: int) -&gt; int:\n        total = 0\n        for i in range(1, n + 1):\n            if i % 3 == 0 or i % 5 == 0 or i % 7 == 0:\n                total += i\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfMultiples(int n) {\n        int total = 0;\n        for (int i = 1; i &lt;= n; i++) {\n            if (i % 3 == 0 || i % 5 == 0 || i % 7 == 0) {\n                total += i;\n            }\n        }\n        return total;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfMultiples(int n) {\n        int total = 0;\n        for (int i = 1; i &lt;= n; ++i) {\n            if (i % 3 == 0 || i % 5 == 0 || i % 7 == 0) {\n                total += i;\n            }\n        }\n        return total;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    sumOfMultiples(n) {\n        let total = 0;\n        for (let i = 1; i &lt;= n; i++) {\n            if (i % 3 === 0 || i % 5 === 0 || i % 7 === 0) {\n                total += i;\n            }\n        }\n        return total;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    sumOfMultiples(n: number): number {\n        let total = 0;\n        for (let i = 1; i &lt;= n; i++) {\n            if (i % 3 === 0 || i % 5 === 0 || i % 7 === 0) {\n                total += i;\n            }\n        }\n        return total;\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Simple to implement and efficient enough for the given constraints (n ≤ 10³).

---
## Mathematical Inclusion-Exclusion

**Intuition**
We can calculate the sum of multiples for each divisor individually using the arithmetic series sum formula, then apply the inclusion-exclusion principle to handle overlaps (numbers divisible by multiple divisors like 15, 21, 35, 105).

**Steps**
- Define a helper function `sumDiv(k)` that calculates the sum of all multiples of `k` up to `n`. The count of multiples is `m = n // k`. The sum is `k * m * (m + 1) / 2`.
- Calculate the sum of multiples for 3, 5, and 7 individually.
- Subtract the sum of multiples for the Least Common Multiples (LCM) of pairs: 15 (3,5), 21 (3,7), and 35 (5,7) because they were counted twice.
- Add back the sum of multiples for the LCM of all three: 105 (3,5,7) because it was subtracted three times and added three times, effectively removing it completely.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfMultiples(self, n: int) -&gt; int:\n        def sum_div(k):\n            m = n // k\n            return k * m * (m + 1) // 2\n        \n        return sum_div(3) + sum_div(5) + sum_div(7) - sum_div(15) - sum_div(21) - sum_div(35) + sum_div(105)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfMultiples(int n) {\n        return sumDiv(n, 3) + sumDiv(n, 5) + sumDiv(n, 7)\n             - sumDiv(n, 15) - sumDiv(n, 21) - sumDiv(n, 35)\n             + sumDiv(n, 105);\n    }\n    \n    private int sumDiv(int n, int k) {\n        int m = n / k;\n        return k * m * (m + 1) / 2;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfMultiples(int n) {\n        return sumDiv(n, 3) + sumDiv(n, 5) + sumDiv(n, 7)\n             - sumDiv(n, 15) - sumDiv(n, 21) - sumDiv(n, 35)\n             + sumDiv(n, 105);\n    }\nprivate:\n    int sumDiv(int n, int k) {\n        int m = n / k;\n        return k * m * (m + 1) / 2;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    sumOfMultiples(n) {\n        const sumDiv = (k) =&gt; {\n            const m = Math.floor(n / k);\n            return k * m * (m + 1) / 2;\n        };\n        return sumDiv(3) + sumDiv(5) + sumDiv(7)\n             - sumDiv(15) - sumDiv(21) - sumDiv(35)\n             + sumDiv(105);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    sumOfMultiples(n: number): number {\n        const sumDiv = (k: number): number =&gt; {\n            const m = Math.floor(n / k);\n            return k * m * (m + 1) / 2;\n        };\n        return sumDiv(3) + sumDiv(5) + sumDiv(7)\n             - sumDiv(15) - sumDiv(21) - sumDiv(35)\n             + sumDiv(105);\n    }\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Optimal mathematical solution that performs a constant number of arithmetic operations regardless of input size.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Sum Multiples Solutions","datePublished":"2024-03-08","description":"Given a positive integer n, return the sum of all numbers in the range [1, n] inclusive that are divisible by 3, 5, or 7.","keywords":"LeetCode, Sum Multiples, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the input number."}},{"@type":"Question","name":"What is the time complexity of the Mathematical Inclusion-Exclusion approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) as it uses a constant number of arithmetic operations."}},{"@type":"Question","name":"How does the Inclusion-Exclusion principle apply here?","acceptedAnswer":{"@type":"Answer","text":"We sum multiples of 3, 5, and 7, subtract overlaps (multiples of 15, 21, 35), and add back the triple overlap (multiples of 105)."}}]}).replace(/</g,'\u003c')}
</script>