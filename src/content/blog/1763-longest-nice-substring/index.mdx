---
title: "Longest Nice Substring"
summary: "Find the longest substring where every letter appears in both uppercase and lowercase forms."
date: "Dec 29, 2025"
tags:
  - Hash Table
  - String
  - Divide and Conquer
  - Bit Manipulation
  - Sliding Window
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 64.30% | **Paid:** No
> **Topics:** Hash Table, String, Divide and Conquer, Bit Manipulation, Sliding Window

A string s is nice if, for every letter of the alphabet that appears in s, it appears in both uppercase and lowercase. For example, "abABB" is nice because 'A' and 'a' appear, and 'B' and 'b' appear. However, "abA" is not nice because 'b' appears but 'B' does not.

Given a string s, return the longest substring of s that is nice.

If there are multiple, return the substring of the earliest occurrence. If there are none, return an empty string.

- Examples
- Constraints
- [Approach 1: Brute Force](#approach-1-brute-force)
- [Approach 2: Divide and Conquer](#approach-2-divide-and-conquer)

## Examples

Example 1:
```text
Input: s = "YazaAay"
Output: "aAa"
Explanation: "aAa" is a nice substring because 'A' and 'a' appear. "YazaAay" is not nice because 'Y' and 'y' do not both appear (only 'y' appears). Another nice substring is "zaA", but "aAa" is longer.
```

Example 2:
```text
Input: s = "Bb"
Output: "Bb"
Explanation: "Bb" is nice because both 'B' and 'b' appear. The whole string is a nice substring.
```

Example 3:
```text
Input: s = "c"
Output: ""
Explanation: There are no nice substrings. A single character cannot be nice because its counterpart is missing.
```

## Constraints

```text
1 <= s.length <= 100
s consists of uppercase and lowercase English letters.
```

---
## Approach 1: Brute Force

**Intuition**
Since the string length is limited to 100, we can afford to check every possible substring. We iterate through all start and end indices, extract the substring, and verify if it is "nice".

**Steps**
- Initialize a variable `longest` to store the result.
- Iterate through all possible starting indices `i` from 0 to `n-1`.
- Iterate through all possible ending indices `j` from `i+1` to `n`.
- Extract the substring `s[i...j]`.
- Check if the substring is nice by ensuring every character in it has its opposite case counterpart also present in the substring.
- If it is nice and longer than the current `longest`, update `longest`.
- Return `longest`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def longestNiceSubstring(self, s: str) -&gt; str:\n        def is_nice(sub):\n            chars = set(sub)\n            for c in chars:\n                if c.swapcase() not in chars:\n                    return False\n            return True\n\n        longest = \"\"\n        n = len(s)\n        for i in range(n):\n            for j in range(i + 1, n + 1):\n                sub = s[i:j]\n                if is_nice(sub) and len(sub) &gt; len(longest):\n                    longest = sub\n        return longest" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String longestNiceSubstring(String s) {\n        String longest = \"\";\n        int n = s.length();\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt;= n; j++) {\n                String sub = s.substring(i, j);\n                if (isNice(sub) && sub.length() &gt; longest.length()) {\n                    longest = sub;\n                }\n            }\n        }\n        return longest;\n    }\n\n    private boolean isNice(String s) {\n        int[] count = new int[128];\n        for (char c : s.toCharArray()) {\n            count[c]++;\n        }\n        for (char c : s.toCharArray()) {\n            char other = Character.isUpperCase(c) ? Character.toLowerCase(c) : Character.toUpperCase(c);\n            if (count[other] == 0) {\n                return false;\n            }\n        }\n        return true;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string longestNiceSubstring(string s) {\n        string longest = \"\";\n        int n = s.size();\n        for (int i = 0; i &lt; n; ++i) {\n            for (int j = i + 1; j &lt;= n; ++j) {\n                string sub = s.substr(i, j - i);\n                if (isNice(sub) && sub.size() &gt; longest.size()) {\n                    longest = sub;\n                }\n            }\n        }\n        return longest;\n    }\n\n    bool isNice(string s) {\n        int count[128] = {0};\n        for (char c : s) count[c]++;\n        for (char c : s) {\n            char other = isupper(c) ? tolower(c) : toupper(c);\n            if (count[other] == 0) return false;\n        }\n        return true;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar longestNiceSubstring = function(s) {\n    const isNice = (sub) =&gt; {\n        const set = new Set(sub.split(''));\n        for (let c of set) {\n            const other = c === c.toLowerCase() ? c.toUpperCase() : c.toLowerCase();\n            if (!set.has(other)) return false;\n        }\n        return true;\n    };\n\n    let longest = \"\";\n    const n = s.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt;= n; j++) {\n            const sub = s.substring(i, j);\n            if (isNice(sub) && sub.length &gt; longest.length) {\n                longest = sub;\n            }\n        }\n    }\n    return longest;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function longestNiceSubstring(s: string): string {\n    const isNice = (sub: string): boolean =&gt; {\n        const set = new Set(sub.split(''));\n        for (let c of set) {\n            const other = c === c.toLowerCase() ? c.toUpperCase() : c.toLowerCase();\n            if (!set.has(other)) return false;\n        }\n        return true;\n    };\n\n    let longest = \"\";\n    const n = s.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt;= n; j++) {\n            const sub = s.substring(i, j);\n            if (isNice(sub) && sub.length &gt; longest.length) {\n                longest = sub;\n            }\n        }\n    }\n    return longest;\n}" }]} />

**Complexity**
- Time: O(n³) — We iterate O(n²) substrings and checking niceness takes O(n).
- Space: O(1) — We use a fixed size array or set for character counts (max 52 chars).
- Notes: Simple to implement and sufficient given the constraint n ≤ 100.

---
## Approach 2: Divide and Conquer

**Intuition**
If a character `c` appears in the string `s` but its counterpart (uppercase/lowercase) does not, then `c` acts as a "divider". No nice substring can include this character `c`. Therefore, the longest nice substring must lie entirely in the segments to the left or right of `c`. We can split the string by `c` and recursively solve for the segments.

**Steps**
- Base case: If the string length is less than 2, return empty string (cannot be nice).
- Convert the string to a set of characters for O(1) lookups.
- Iterate through the string. For each character `c`, check if its counterpart exists in the set.
- If a character `c` is found without its counterpart:
    - Split the string into two parts: left of `c` and right of `c`.
    - Recursively call the function on both parts.
    - Return the longer result of the two recursive calls.
- If the loop completes without finding a "bad" character, the entire string is nice. Return it.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def longestNiceSubstring(self, s: str) -&gt; str:\n        if len(s) &lt; 2:\n            return \"\"\n        \n        chars = set(s)\n        for i, c in enumerate(s):\n            if c.swapcase() not in chars:\n                left = self.longestNiceSubstring(s[:i])\n                right = self.longestNiceSubstring(s[i+1:])\n                return left if len(left) &gt;= len(right) else right\n        \n        return s" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String longestNiceSubstring(String s) {\n        if (s.length() &lt; 2) return \"\";\n        \n        Set&lt;Character&gt; set = new HashSet&lt;&gt;();\n        for (char c : s.toCharArray()) set.add(c);\n        \n        for (int i = 0; i &lt; s.length(); i++) {\n            char c = s.charAt(i);\n            char other = Character.isUpperCase(c) ? Character.toLowerCase(c) : Character.toUpperCase(c);\n            if (!set.contains(other)) {\n                String left = longestNiceSubstring(s.substring(0, i));\n                String right = longestNiceSubstring(s.substring(i + 1));\n                return left.length() &gt;= right.length() ? left : right;\n            }\n        }\n        return s;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string longestNiceSubstring(string s) {\n        if (s.size() &lt; 2) return \"\";\n        \n        unordered_set&lt;char&gt; set(s.begin(), s.end());\n        \n        for (int i = 0; i &lt; s.size(); ++i) {\n            char c = s[i];\n            char other = isupper(c) ? tolower(c) : toupper(c);\n            if (set.find(other) == set.end()) {\n                string left = longestNiceSubstring(s.substr(0, i));\n                string right = longestNiceSubstring(s.substr(i + 1));\n                return left.size() &gt;= right.size() ? left : right;\n            }\n        }\n        return s;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar longestNiceSubstring = function(s) {\n    if (s.length &lt; 2) return \"\";\n    \n    const set = new Set(s.split(''));\n    \n    for (let i = 0; i &lt; s.length; i++) {\n        const c = s[i];\n        const other = c === c.toLowerCase() ? c.toUpperCase() : c.toLowerCase();\n        if (!set.has(other)) {\n            const left = longestNiceSubstring(s.substring(0, i));\n            const right = longestNiceSubstring(s.substring(i + 1));\n            return left.length &gt;= right.length ? left : right;\n        }\n    }\n    return s;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function longestNiceSubstring(s: string): string {\n    if (s.length &lt; 2) return \"\";\n    \n    const set = new Set(s.split(''));\n    \n    for (let i = 0; i &lt; s.length; i++) {\n        const c = s[i];\n        const other = c === c.toLowerCase() ? c.toUpperCase() : c.toLowerCase();\n        if (!set.has(other)) {\n            const left = longestNiceSubstring(s.substring(0, i));\n            const right = longestNiceSubstring(s.substring(i + 1));\n            return left.length &gt;= right.length ? left : right;\n        }\n    }\n    return s;\n}" }]} />

**Complexity**
- Time: O(n²) — In the worst case (e.g., "abcd..."), we might split and recurse n times, and each recursion scans the string.
- Space: O(n²) — Due to string slicing and recursion stack depth in worst case.
- Notes: More efficient than brute force for larger inputs, though both are acceptable here.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Longest Nice Substring Solutions","datePublished":"2025-12-29","description":"Find the longest substring where every letter appears in both uppercase and lowercase forms.","keywords":"LeetCode, Longest Nice Substring, Easy, Hash Table, String, Divide and Conquer, Bit Manipulation, Sliding Window"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n³), where n is the length of the string."}},{"@type":"Question","name":"What is the time complexity of the Divide and Conquer approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n²) in the worst case."}}]}).replace(/</g,'\u003c')}
</script>