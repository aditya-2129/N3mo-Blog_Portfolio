---
title: "GCD of Odd and Even Sums"
summary: "Calculate the GCD of sums of elements at odd and even indices in an array."
date: "Apr 30, 2024"
tags:
  - Math
  - Number Theory
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 85.00% | **Paid:** No
> **Topics:** Math, Number Theory

You are given an integer array nums. Return the greatest common divisor (GCD) of the sum of elements at even indices and the sum of elements at odd indices.

- Examples
- Constraints
- [Iterative with Euclidean Algorithm](#iterative-with-euclidean-algorithm)
- [Using Built-in GCD Function](#using-built-in-gcd-function)

## Examples

### Example 1

**Input:**
```text
n = 4
```

**Output:**
```text
4
```

**Explanation:**
Sum of the first 4 odd numbers sumOdd = 1 + 3 + 5 + 7 = 16

Sum of the first 4 even numbers sumEven = 2 + 4 + 6 + 8 = 20

Hence, GCD(sumOdd, sumEven) = GCD(16, 20) = 4.

### Example 2

**Input:**
```text
n = 5
```

**Output:**
```text
5
```

**Explanation:**
Sum of the first 5 odd numbers sumOdd = 1 + 3 + 5 + 7 + 9 = 25

Sum of the first 5 even numbers sumEven = 2 + 4 + 6 + 8 + 10 = 30

Hence, GCD(sumOdd, sumEven) = GCD(25, 30) = 5.

## Constraints

```text
1 <= nums.length <= 10⁵
-10⁹ <= nums[i] <= 10⁹
```

---
## Iterative with Euclidean Algorithm

**Intuition**
Calculate the sum of elements at even and odd indices separately, then find their GCD using the Euclidean algorithm.

**Steps**
- Initialize two variables for even and odd sums
- Iterate through the array, adding elements to their respective sums based on index parity
- Apply Euclidean algorithm to find GCD of the two sums
- Return the GCD

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def gcdOfSums(self, nums: list[int]) -> int:
        even_sum = 0
        odd_sum = 0
        
        for i, num in enumerate(nums):
            if i % 2 == 0:
                even_sum += num
            else:
                odd_sum += num
        
        def gcd(a, b):
            while b:
                a, b = b, a % b
            return abs(a)
        
        return gcd(even_sum, odd_sum)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int gcdOfSums(int[] nums) {
        long evenSum = 0;
        long oddSum = 0;
        
        for (int i = 0; i &lt; nums.length; i++) {
            if (i % 2 == 0) {
                evenSum += nums[i];
            } else {
                oddSum += nums[i];
            }
        }
        
        return (int) gcd(evenSum, oddSum);
    }
    
    private long gcd(long a, long b) {
        while (b != 0) {
            long temp = b;
            b = a % b;
            a = temp;
        }
        return Math.abs(a);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int gcdOfSums(vector<int>& nums) {
        long long evenSum = 0;
        long long oddSum = 0;
        
        for (int i = 0; i &lt; nums.size(); i++) {
            if (i % 2 == 0) {
                evenSum += nums[i];
            } else {
                oddSum += nums[i];
            }
        }
        
        return (int)gcd(evenSum, oddSum);
    }
    
private:
    long long gcd(long long a, long long b) {
        while (b != 0) {
            long long temp = b;
            b = a % b;
            a = temp;
        }
        return llabs(a);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @return {number}
 */
var gcdOfSums = function(nums) {
    let evenSum = 0;
    let oddSum = 0;
    
    for (let i = 0; i &lt; nums.length; i++) {
        if (i % 2 === 0) {
            evenSum += nums[i];
        } else {
            oddSum += nums[i];
        }
    }
    
    const gcd = (a, b) =&gt; {
        while (b !== 0) {
            let temp = b;
            b = a % b;
            a = temp;
        }
        return Math.abs(a);
    };
    
    return gcd(evenSum, oddSum);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function gcdOfSums(nums: number[]): number {
    let evenSum: number = 0;
    let oddSum: number = 0;
    
    for (let i = 0; i &lt; nums.length; i++) {
        if (i % 2 === 0) {
            evenSum += nums[i];
        } else {
            oddSum += nums[i];
        }
    }
    
    const gcd = (a: number, b: number): number =&gt; {
        while (b !== 0) {
            let temp = b;
            b = a % b;
            a = temp;
        }
        return Math.abs(a);
    };
    
    return gcd(evenSum, oddSum);
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(1) only using constant extra space
- Notes: Single pass through the array with efficient GCD computation

---
## Using Built-in GCD Function

**Intuition**
Leverage the built-in GCD functions provided by each language's standard library after computing the sums.

**Steps**
- Calculate even and odd indexed sums
- Use language's built-in GCD function
- Return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `import math

class Solution:
    def gcdOfSums(self, nums: list[int]) -> int:
        even_sum = sum(nums[i] for i in range(0, len(nums), 2))
        odd_sum = sum(nums[i] for i in range(1, len(nums), 2))
        
        return math.gcd(even_sum, odd_sum)
` },
{ name: "Java", lang: "java", code: `import java.math.BigInteger;

class Solution {
    public int gcdOfSums(int[] nums) {
        long evenSum = 0;
        long oddSum = 0;
        
        for (int i = 0; i &lt; nums.length; i++) {
            if (i % 2 == 0) {
                evenSum += nums[i];
            } else {
                oddSum += nums[i];
            }
        }
        
        return BigInteger.valueOf(evenSum)
            .gcd(BigInteger.valueOf(oddSum))
            .intValue();
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <numeric>

class Solution {
public:
    int gcdOfSums(vector<int>& nums) {
        long long evenSum = 0;
        long long oddSum = 0;
        
        for (int i = 0; i &lt; nums.size(); i++) {
            if (i % 2 == 0) {
                evenSum += nums[i];
            } else {
                oddSum += nums[i];
            }
        }
        
        return (int)std::gcd(evenSum, oddSum);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @return {number}
 */
var gcdOfSums = function(nums) {
    let evenSum = 0;
    let oddSum = 0;
    
    for (let i = 0; i &lt; nums.length; i++) {
        if (i % 2 === 0) {
            evenSum += nums[i];
        } else {
            oddSum += nums[i];
        }
    }
    
    const gcd = (a, b) =&gt; {
        a = Math.abs(a);
        b = Math.abs(b);
        while (b) {
            let t = b;
            b = a % b;
            a = t;
        }
        return a;
    };
    
    return gcd(evenSum, oddSum);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function gcdOfSums(nums: number[]): number {
    let evenSum: number = 0;
    let oddSum: number = 0;
    
    for (let i = 0; i &lt; nums.length; i++) {
        if (i % 2 === 0) {
            evenSum += nums[i];
        } else {
            oddSum += nums[i];
        }
    }
    
    const gcd = (a: number, b: number): number =&gt; {
        a = Math.abs(a);
        b = Math.abs(b);
        while (b !== 0) {
            let t = b;
            b = a % b;
            a = t;
        }
        return a;
    };
    
    return gcd(evenSum, oddSum);
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(1) only using constant extra space
- Notes: Cleaner code using built-in functions where available

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode GCD of Odd and Even Sums Solutions","datePublished":"2024-04-30","description":"Calculate the GCD of sums of elements at odd and even indices in an array.","keywords":"LeetCode, GCD of Odd and Even Sums, Easy, Math, Number Theory"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the iterative approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of the array, as we iterate through all elements once."}},{"@type":"Question","name":"What is the space complexity?","acceptedAnswer":{"@type":"Answer","text":"O(1) as we only use a constant amount of extra space for the sums and GCD computation."}},{"@type":"Question","name":"How do we handle negative numbers in the GCD calculation?","acceptedAnswer":{"@type":"Answer","text":"We take the absolute value of the sums before computing GCD, as GCD is always positive."}}]}).replace(/</g,'\u003c')}
</script>