---
title: "N-th Tribonacci Number"
summary: "Calculate the n-th Tribonacci number, defined by the recurrence relation Tn = Tn-1 + Tn-2 + Tn-3."
date: "Feb 12, 2025"
tags:
  - Math
  - Dynamic Programming
  - Memoization
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 63.20% | **Paid:** No
> **Topics:** Math, Dynamic Programming, Memoization

The Tribonacci sequence Tn is defined as follows:

T0 = 0, T1 = 1, T2 = 1, and Tn+3 = Tn + Tn+1 + Tn+2 for n &gt;= 0.

Given n, return Tn.

## Examples

### Example 1:
```text
Input: n = 4
Output: 4
Explanation:
T_3 = 0 + 1 + 1 = 2
T_4 = 1 + 1 + 2 = 4
```

### Example 2:
```text
Input: n = 25
Output: 1389537
```

## Constraints

```text
0 <= n <= 37
The answer is guaranteed to fit within a 32-bit integer, ie. answer <= 2^31 - 1.
```

- [Approach 1: Recursion (Brute Force)](#approach-1-recursion-brute-force)
- [Approach 2: Memoization (Top-Down DP)](#approach-2-memoization-top-down-dp)
- [Approach 3: Iterative (Bottom-Up DP)](#approach-3-iterative-bottom-up-dp)

## Examples

### Example 1:
```text
Input: n = 4
Output: 4
Explanation:
T_3 = 0 + 1 + 1 = 2
T_4 = 1 + 1 + 2 = 4
```

### Example 2:
```text
Input: n = 25
Output: 1389537
```

## Constraints

```text
0 <= n <= 37
The answer is guaranteed to fit within a 32-bit integer, ie. answer <= 2^31 - 1.
```

---
## Approach 1: Recursion (Brute Force)

**Intuition**
Directly translate the mathematical definition of the Tribonacci sequence into a recursive function. The function calls itself to calculate the previous three values.

**Steps**
- Handle base cases: if n is 0 return 0, if n is 1 or 2 return 1.
- For n &gt; 2, recursively call the function for n-1, n-2, and n-3.
- Sum the results of the three recursive calls and return the value.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def tribonacci(self, n: int) -&gt; int:\n        if n == 0:\n            return 0\n        if n &lt; 3:\n            return 1\n        return self.tribonacci(n - 1) + self.tribonacci(n - 2) + self.tribonacci(n - 3)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int tribonacci(int n) {\n        if (n == 0) return 0;\n        if (n &lt; 3) return 1;\n        return tribonacci(n - 1) + tribonacci(n - 2) + tribonacci(n - 3);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int tribonacci(int n) {\n        if (n == 0) return 0;\n        if (n &lt; 3) return 1;\n        return tribonacci(n - 1) + tribonacci(n - 2) + tribonacci(n - 3);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    tribonacci(n) {\n        if (n === 0) return 0;\n        if (n &lt; 3) return 1;\n        return this.tribonacci(n - 1) + this.tribonacci(n - 2) + this.tribonacci(n - 3);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    tribonacci(n: number): number {\n        if (n === 0) return 0;\n        if (n &lt; 3) return 1;\n        return this.tribonacci(n - 1) + this.tribonacci(n - 2) + this.tribonacci(n - 3);\n    }\n}" },]} />

**Complexity**
- Time: O(3ⁿ) - Each call branches into 3 calls, leading to exponential growth.
- Space: O(n) - Recursion stack depth.
- Notes: This approach is too slow for larger n due to repeated calculations.

---
## Approach 2: Memoization (Top-Down DP)

**Intuition**
Optimize the recursive approach by storing the results of previously computed values in a hash map or array. This avoids redundant calculations.

**Steps**
- Create a memoization storage (array or map).
- Define a helper function that checks if the value for n is already in storage.
- If it is, return the stored value.
- If not, calculate it recursively, store it, and return it.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def tribonacci(self, n: int) -&gt; int:\n        memo = {}\n        def helper(k):\n            if k == 0: return 0\n            if k &lt; 3: return 1\n            if k in memo: return memo[k]\n            memo[k] = helper(k - 1) + helper(k - 2) + helper(k - 3)\n            return memo[k]\n        return helper(n)" },
{ name: "Java", lang: "java", code: "class Solution {\n    private int[] memo;\n    public int tribonacci(int n) {\n        memo = new int[n + 1];\n        for(int i = 0; i &lt; memo.length; i++) memo[i] = -1;\n        return helper(n);\n    }\n    private int helper(int n) {\n        if (n == 0) return 0;\n        if (n &lt; 3) return 1;\n        if (memo[n] != -1) return memo[n];\n        memo[n] = helper(n - 1) + helper(n - 2) + helper(n - 3);\n        return memo[n];\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; memo;\n    int tribonacci(int n) {\n        memo.resize(n + 1, -1);\n        return helper(n);\n    }\n    int helper(int n) {\n        if (n == 0) return 0;\n        if (n &lt; 3) return 1;\n        if (memo[n] != -1) return memo[n];\n        memo[n] = helper(n - 1) + helper(n - 2) + helper(n - 3);\n        return memo[n];\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    tribonacci(n) {\n        const memo = {};\n        const helper = (k) =&gt; {\n            if (k === 0) return 0;\n            if (k &lt; 3) return 1;\n            if (memo[k] !== undefined) return memo[k];\n            memo[k] = helper(k - 1) + helper(k - 2) + helper(k - 3);\n            return memo[k];\n        };\n        return helper(n);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    tribonacci(n: number): number {\n        const memo: Record&lt;number, number&gt; = {};\n        const helper = (k: number): number =&gt; {\n            if (k === 0) return 0;\n            if (k &lt; 3) return 1;\n            if (memo[k] !== undefined) return memo[k];\n            memo[k] = helper(k - 1) + helper(k - 2) + helper(k - 3);\n            return memo[k];\n        };\n        return helper(n);\n    }\n}" },]} />

**Complexity**
- Time: O(n) - Each value from 0 to n is calculated exactly once.
- Space: O(n) - For the memoization array and recursion stack.
- Notes: Significantly faster than brute force recursion.

---
## Approach 3: Iterative (Bottom-Up DP)

**Intuition**
Build the sequence from the bottom up using three variables to store the last three Tribonacci numbers. This eliminates recursion overhead and extra space for memoization.

**Steps**
- Handle base cases for n = 0, 1, 2.
- Initialize three variables a, b, c to represent T0, T1, T2.
- Iterate from 3 up to n.
- In each iteration, calculate the next value as the sum of a, b, and c.
- Shift the values: a becomes b, b becomes c, c becomes the new sum.
- Return c after the loop finishes.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def tribonacci(self, n: int) -&gt; int:\n        if n == 0: return 0\n        if n &lt; 3: return 1\n        a, b, c = 0, 1, 1\n        for _ in range(3, n + 1):\n            next_val = a + b + c\n            a, b, c = b, c, next_val\n        return c" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int tribonacci(int n) {\n        if (n == 0) return 0;\n        if (n &lt; 3) return 1;\n        int a = 0, b = 1, c = 1;\n        for (int i = 3; i &lt;= n; i++) {\n            int next = a + b + c;\n            a = b;\n            b = c;\n            c = next;\n        }\n        return c;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int tribonacci(int n) {\n        if (n == 0) return 0;\n        if (n &lt; 3) return 1;\n        int a = 0, b = 1, c = 1;\n        for (int i = 3; i &lt;= n; i++) {\n            int next = a + b + c;\n            a = b;\n            b = c;\n            c = next;\n        }\n        return c;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    tribonacci(n) {\n        if (n === 0) return 0;\n        if (n &lt; 3) return 1;\n        let a = 0, b = 1, c = 1;\n        for (let i = 3; i &lt;= n; i++) {\n            let next = a + b + c;\n            a = b;\n            b = c;\n            c = next;\n        }\n        return c;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    tribonacci(n: number): number {\n        if (n === 0) return 0;\n        if (n &lt; 3) return 1;\n        let a = 0, b = 1, c = 1;\n        for (let i = 3; i &lt;= n; i++) {\n            let next = a + b + c;\n            a = b;\n            b = c;\n            c = next;\n        }\n        return c;\n    }\n}" },]} />

**Complexity**
- Time: O(n) - Single loop from 3 to n.
- Space: O(1) - Only three integer variables are used.
- Notes: This is the most optimal approach for this problem in terms of space complexity.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode N-th Tribonacci Number Solutions","datePublished":"2025-02-12","description":"Calculate the n-th Tribonacci number, defined by the recurrence relation Tn = Tn-1 + Tn-2 + Tn-3.","keywords":"LeetCode, N-th Tribonacci Number, Easy, Math, Dynamic Programming, Memoization"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative approach for N-th Tribonacci Number?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Iterative approach is O(n), where n is the input number."}},{"@type":"Question","name":"What is the space complexity of the Memoization approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity of the Memoization approach is O(n) to store the computed values and the recursion stack."}},{"@type":"Question","name":"Why is the Iterative approach preferred over Recursion?","acceptedAnswer":{"@type":"Answer","text":"The Iterative approach is preferred because it has O(1) space complexity compared to O(n) for Memoization and avoids the overhead of recursive function calls."}}]}).replace(/</g,'\u003c')}
</script>