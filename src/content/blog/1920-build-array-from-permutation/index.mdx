---
title: "Build Array from Permutation"
summary: "Given a zero-based permutation nums, build an array ans where ans[i] = nums[nums[i]] for every 0 &lt;= i &lt; nums.length."
date: "May 19, 2025"
tags:
  - Array
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 91.10% | **Paid:** No
> **Topics:** Array, Simulation

Given a zero-based permutation nums (0-indexed), build an array ans of the same length where ans[i] = nums[nums[i]] for each 0 &lt;= i &lt; nums.length. Return the constructed array ans.

A zero-based permutation nums is an array of distinct integers from 0 to nums.length - 1 (inclusive).

- Examples
- Constraints
- [Direct Simulation](#direct-simulation)
- [In-Place Encoding](#in-place-encoding)

## Examples

**Example 1:**

```text
Input: nums = [0,2,1,5,3,4]
Output: [0,1,2,4,5,3]
Explanation: ans = [nums[nums[0]], nums[nums[1]], nums[nums[2]], nums[nums[3]], nums[nums[4]], nums[nums[5]]]
    = [nums[0], nums[2], nums[1], nums[5], nums[3], nums[4]]
    = [0,1,2,4,5,3]
```

**Example 2:**

```text
Input: nums = [5,0,1,2,3,4]
Output: [4,5,0,1,2,3]
Explanation: ans = [nums[nums[0]], nums[nums[1]], nums[nums[2]], nums[nums[3]], nums[nums[4]], nums[nums[5]]]
    = [nums[5], nums[0], nums[1], nums[2], nums[3], nums[4]]
    = [4,5,0,1,2,3]
```

## Constraints

```text
1 <= nums.length <= 1000
0 <= nums[i] < nums.length
All integers in nums are unique.
```

---
## Direct Simulation

**Intuition**
The problem statement directly defines the relationship between the input array `nums` and the output array `ans`. We can simply iterate through the indices of `nums` and apply the formula `ans[i] = nums[nums[i]]` to fill a new array.

**Steps**
- Initialize a new array `ans` with the same length as `nums`.
- Iterate through each index `i` from `0` to `n-1`.
- For each index `i`, assign `ans[i]` the value of `nums[nums[i]]`.
- Return the array `ans`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def buildArray(self, nums: List[int]) -&gt; List[int]:\n        n = len(nums)\n        ans = [0] * n\n        for i in range(n):\n            ans[i] = nums[nums[i]]\n        return ans" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] buildArray(int[] nums) {\n        int n = nums.length;\n        int[] ans = new int[n];\n        for (int i = 0; i &lt; n; i++) {\n            ans[i] = nums[nums[i]];\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; buildArray(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        vector&lt;int&gt; ans(n);\n        for (int i = 0; i &lt; n; i++) {\n            ans[i] = nums[nums[i]];\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar buildArray = function(nums) {\n    const n = nums.length;\n    const ans = new Array(n);\n    for (let i = 0; i &lt; n; i++) {\n        ans[i] = nums[nums[i]];\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function buildArray(nums: number[]): number[] {\n    const n = nums.length;\n    const ans: number[] = new Array(n);\n    for (let i = 0; i &lt; n; i++) {\n        ans[i] = nums[nums[i]];\n    }\n    return ans;\n}" },]} />

**Complexity**
- Time: O(n) — We iterate through the array once.
- Space: O(n) — We allocate a new array of size `n` to store the result.
- Notes: This is the most straightforward and readable solution.

---
## In-Place Encoding

**Intuition**
We can optimize space complexity by modifying the input array in place to store both the original value and the new value. Since `0 &lt;= nums[i] &lt; n`, we can use the formula `nums[i] = nums[i] + (nums[nums[i]] % n) * n` to encode the new value. Later, we can retrieve the new value by dividing by `n`.

**Steps**
- Get the length `n` of the array.
- Iterate through the array. For each index `i`, update `nums[i]` to `nums[i] + (nums[nums[i]] % n) * n`. The modulo operation ensures we retrieve the original value at `nums[nums[i]]` even if it has been modified.
- Iterate through the array again. For each index `i`, update `nums[i]` to `nums[i] / n` (integer division). This extracts the new value.
- Return `nums`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def buildArray(self, nums: List[int]) -&gt; List[int]:\n        n = len(nums)\n        for i in range(n):\n            nums[i] = nums[i] + (nums[nums[i]] % n) * n\n        for i in range(n):\n            nums[i] = nums[i] // n\n        return nums" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] buildArray(int[] nums) {\n        int n = nums.length;\n        for (int i = 0; i &lt; n; i++) {\n            nums[i] = nums[i] + (nums[nums[i] % n] % n) * n;\n        }\n        for (int i = 0; i &lt; n; i++) {\n            nums[i] = nums[i] / n;\n        }\n        return nums;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; buildArray(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        for (int i = 0; i &lt; n; i++) {\n            nums[i] = nums[i] + (nums[nums[i] % n] % n) * n;\n        }\n        for (int i = 0; i &lt; n; i++) {\n            nums[i] = nums[i] / n;\n        }\n        return nums;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar buildArray = function(nums) {\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        nums[i] = nums[i] + (nums[nums[i] % n] % n) * n;\n    }\n    for (let i = 0; i &lt; n; i++) {\n        nums[i] = Math.floor(nums[i] / n);\n    }\n    return nums;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function buildArray(nums: number[]): number[] {\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        nums[i] = nums[i] + (nums[nums[i] % n] % n) * n;\n    }\n    for (let i = 0; i &lt; n; i++) {\n        nums[i] = Math.floor(nums[i] / n);\n    }\n    return nums;\n}" },]} />

**Complexity**
- Time: O(n) — We iterate through the array twice.
- Space: O(1) — We modify the array in place and use only a constant amount of extra space.
- Notes: This approach trades some readability for constant space complexity. It is useful when memory is extremely constrained.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Build Array from Permutation Solutions","datePublished":"2025-05-19","description":"Given a zero-based permutation nums, build an array ans where ans[i] = nums[nums[i]] for every 0 &lt;= i &lt; nums.length.","keywords":"LeetCode, Build Array from Permutation, Easy, Array, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Direct Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the array, as we iterate through the array once."}},{"@type":"Question","name":"What is the space complexity of the In-Place Encoding approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) extra space, as we modify the input array in place without allocating a new array for the result."}},{"@type":"Question","name":"Why can we use the In-Place Encoding technique?","acceptedAnswer":{"@type":"Answer","text":"Because the values in the permutation are bounded by 0 &lt;= nums[i] &lt; n, we can use multiplication and modulo operations to store two numbers in a single integer slot."}}]}).replace(/</g,'\u003c')}
</script>