---
title: "Maximum Difference Between Even and Odd Frequency I"
summary: "Find the maximum difference between the sum of even frequencies and sum of odd frequencies of characters in a string."
date: "Aug 29, 2025"
tags:
  - Hash Table
  - String
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 60.70% | **Paid:** No
> **Topics:** Hash Table, String, Counting

You are given a string `word`. You need to calculate the difference between the sum of frequencies of characters that appear an even number of times and the sum of frequencies of characters that appear an odd number of times.

Return the maximum possible difference.

- Examples
- Constraints
- [Hash Map Counting](#hash-map-counting)
- [Array Counting](#array-counting)
- [Sorting Approach](#sorting-approach)

## Examples

### Example 1

**Input:**
```text
s = "aaaaabbc"
```

**Output:**
```text
3
```

**Explanation:**
The character 'a' has an odd frequency of 5, and 'b' has an even frequency of 2.

The maximum difference is 5 - 2 = 3.

### Example 2

**Input:**
```text
s = "abcabcab"
```

**Output:**
```text
1
```

**Explanation:**
The character 'a' has an odd frequency of 3, and 'c' has an even frequency of 2.

The maximum difference is 3 - 2 = 1.

## Constraints

```text
- 3 <= s.length <= 100
- s consists only of lowercase English letters.
- s contains at least one character with an odd frequency and one with an even frequency.
```

---
## Hash Map Counting

**Intuition**
Use a hash map to count the frequency of each character, then iterate through the map to separate even and odd frequencies.

**Steps**
- Create a hash map to store character frequencies
- Iterate through the string and count each character
- Iterate through the hash map, adding frequencies to even or odd sum based on parity
- Return the absolute difference

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def maxDifference(self, word: str) -> int:
        freq = {}
        for c in word:
            freq[c] = freq.get(c, 0) + 1
        
        even_sum = 0
        odd_sum = 0
        for count in freq.values():
            if count % 2 == 0:
                even_sum += count
            else:
                odd_sum += count
        
        return abs(even_sum - odd_sum)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int maxDifference(String word) {
        Map<Character, Integer> freq = new HashMap&lt;&gt;();
        for (char c : word.toCharArray()) {
            freq.put(c, freq.getOrDefault(c, 0) + 1);
        }
        
        int evenSum = 0;
        int oddSum = 0;
        for (int count : freq.values()) {
            if (count % 2 == 0) {
                evenSum += count;
            } else {
                oddSum += count;
            }
        }
        
        return Math.abs(evenSum - oddSum);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int maxDifference(string word) {
        unordered_map<char, int> freq;
        for (char c : word) {
            freq[c]++;
        }
        
        int evenSum = 0;
        int oddSum = 0;
        for (auto& p : freq) {
            if (p.second % 2 == 0) {
                evenSum += p.second;
            } else {
                oddSum += p.second;
            }
        }
        
        return abs(evenSum - oddSum);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} word
 * @return {number}
 */
var maxDifference = function(word) {
    const freq = new Map();
    for (const c of word) {
        freq.set(c, (freq.get(c) || 0) + 1);
    }
    
    let evenSum = 0;
    let oddSum = 0;
    for (const count of freq.values()) {
        if (count % 2 === 0) {
            evenSum += count;
        } else {
            oddSum += count;
        }
    }
    
    return Math.abs(evenSum - oddSum);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function maxDifference(word: string): number {
    const freq = new Map<string, number>();
    for (const c of word) {
        freq.set(c, (freq.get(c) || 0) + 1);
    }
    
    let evenSum = 0;
    let oddSum = 0;
    for (const count of freq.values()) {
        if (count % 2 === 0) {
            evenSum += count;
        } else {
            oddSum += count;
        }
    }
    
    return Math.abs(evenSum - oddSum);
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(k) where k is the number of unique characters (at most 26 for lowercase letters)
- Notes: Simple and intuitive approach using hash map for counting

---
## Array Counting

**Intuition**
Since the input only contains lowercase English letters, we can use a fixed-size array of 26 elements for counting, which is more efficient than a hash map.

**Steps**
- Create an array of size 26 initialized to 0
- Iterate through the string, incrementing the count for each character
- Iterate through the array, adding frequencies to even or odd sum based on parity
- Return the absolute difference

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def maxDifference(self, word: str) -> int:
        freq = [0] * 26
        for c in word:
            freq[ord(c) - ord('a')] += 1
        
        even_sum = 0
        odd_sum = 0
        for count in freq:
            if count % 2 == 0:
                even_sum += count
            else:
                odd_sum += count
        
        return abs(even_sum - odd_sum)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int maxDifference(String word) {
        int[] freq = new int[26];
        for (char c : word.toCharArray()) {
            freq[c - 'a']++;
        }
        
        int evenSum = 0;
        int oddSum = 0;
        for (int count : freq) {
            if (count % 2 == 0) {
                evenSum += count;
            } else {
                oddSum += count;
            }
        }
        
        return Math.abs(evenSum - oddSum);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int maxDifference(string word) {
        int freq[26] = {0};
        for (char c : word) {
            freq[c - 'a']++;
        }
        
        int evenSum = 0;
        int oddSum = 0;
        for (int count : freq) {
            if (count % 2 == 0) {
                evenSum += count;
            } else {
                oddSum += count;
            }
        }
        
        return abs(evenSum - oddSum);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} word
 * @return {number}
 */
var maxDifference = function(word) {
    const freq = new Array(26).fill(0);
    for (const c of word) {
        freq[c.charCodeAt(0) - 'a'.charCodeAt(0)]++;
    }
    
    let evenSum = 0;
    let oddSum = 0;
    for (const count of freq) {
        if (count % 2 === 0) {
            evenSum += count;
        } else {
            oddSum += count;
        }
    }
    
    return Math.abs(evenSum - oddSum);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function maxDifference(word: string): number {
    const freq = new Array(26).fill(0);
    for (const c of word) {
        freq[c.charCodeAt(0) - 'a'.charCodeAt(0)]++;
    }
    
    let evenSum = 0;
    let oddSum = 0;
    for (const count of freq) {
        if (count % 2 === 0) {
            evenSum += count;
        } else {
            oddSum += count;
        }
    }
    
    return Math.abs(evenSum - oddSum);
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1) since the array size is fixed at 26
- Notes: More space-efficient than hash map for lowercase letters only

---
## Sorting Approach

**Intuition**
Sort the string so identical characters are adjacent, then count consecutive characters to find frequencies.

**Steps**
- Convert the string to a character array and sort it
- Iterate through the sorted array, counting consecutive identical characters
- For each group of identical characters, add the frequency to even or odd sum based on parity
- Return the absolute difference

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def maxDifference(self, word: str) -> int:
        chars = sorted(word)
        
        even_sum = 0
        odd_sum = 0
        i = 0
        n = len(chars)
        
        while i &lt; n:
            count = 1
            while i + count &lt; n and chars[i + count] == chars[i]:
                count += 1
            
            if count % 2 == 0:
                even_sum += count
            else:
                odd_sum += count
            
            i += count
        
        return abs(even_sum - odd_sum)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int maxDifference(String word) {
        char[] chars = word.toCharArray();
        Arrays.sort(chars);
        
        int evenSum = 0;
        int oddSum = 0;
        int i = 0;
        int n = chars.length;
        
        while (i &lt; n) {
            int count = 1;
            while (i + count &lt; n && chars[i + count] == chars[i]) {
                count++;
            }
            
            if (count % 2 == 0) {
                evenSum += count;
            } else {
                oddSum += count;
            }
            
            i += count;
        }
        
        return Math.abs(evenSum - oddSum);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int maxDifference(string word) {
        sort(word.begin(), word.end());
        
        int evenSum = 0;
        int oddSum = 0;
        int i = 0;
        int n = word.length();
        
        while (i &lt; n) {
            int count = 1;
            while (i + count &lt; n && word[i + count] == word[i]) {
                count++;
            }
            
            if (count % 2 == 0) {
                evenSum += count;
            } else {
                oddSum += count;
            }
            
            i += count;
        }
        
        return abs(evenSum - oddSum);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} word
 * @return {number}
 */
var maxDifference = function(word) {
    const chars = word.split('').sort();
    
    let evenSum = 0;
    let oddSum = 0;
    let i = 0;
    const n = chars.length;
    
    while (i &lt; n) {
        let count = 1;
        while (i + count &lt; n && chars[i + count] === chars[i]) {
            count++;
        }
        
        if (count % 2 === 0) {
            evenSum += count;
        } else {
            oddSum += count;
        }
        
        i += count;
    }
    
    return Math.abs(evenSum - oddSum);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function maxDifference(word: string): number {
    const chars = word.split('').sort();
    
    let evenSum = 0;
    let oddSum = 0;
    let i = 0;
    const n = chars.length;
    
    while (i &lt; n) {
        let count = 1;
        while (i + count &lt; n && chars[i + count] === chars[i]) {
            count++;
        }
        
        if (count % 2 === 0) {
            evenSum += count;
        } else {
            oddSum += count;
        }
        
        i += count;
    }
    
    return Math.abs(evenSum - oddSum);
}
` },]} />

**Complexity**
- Time: O(n log n) due to sorting
- Space: O(n) for the character array (or O(1) if sorting in place)
- Notes: Less efficient than counting approaches due to sorting overhead

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Maximum Difference Between Even and Odd Frequency I Solutions","datePublished":"2025-08-29","description":"Find the maximum difference between the sum of even frequencies and sum of odd frequencies of characters in a string.","keywords":"LeetCode, Maximum Difference Between Even and Odd Frequency I, Easy, Hash Table, String, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Map Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we iterate through the string once to count frequencies and once through the hash map to calculate the difference."}},{"@type":"Question","name":"What is the space complexity of the Array Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) since we use a fixed-size array of 26 elements regardless of the input size."}},{"@type":"Question","name":"Which approach is the most efficient for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Array Counting approach is the most efficient with O(n) time and O(1) space complexity, taking advantage of the constraint that the input only contains lowercase English letters."}}]}).replace(/</g,'\u003c')}
</script>