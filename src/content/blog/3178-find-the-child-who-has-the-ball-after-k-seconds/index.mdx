---
title: "Find the Child Who Has the Ball After K Seconds"
summary: "You are given n children standing in a line. The ball starts at child 0 and moves back and forth. Return the child holding the ball after k seconds."
date: "Feb 03, 2025"
tags:
  - Math
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 62.10% | **Paid:** No
> **Topics:** Math, Simulation

You are given n children standing in a line. The children are numbered from 0 to n - 1 from left to right. You are also given an integer k.

A ball is being passed between the children. The ball starts at child 0. The ball moves to the right (towards higher indices) one child per second. When the ball reaches the last child (child n - 1), it changes direction and moves to the left one child per second. When the ball reaches the first child (child 0), it changes direction and moves to the right again.

This process continues for k seconds. Return the number of the child who has the ball after k seconds.

- Examples
- Constraints
- [Approach 1: Simulation](#simulation)
- [Approach 2: Mathematical Modulo](#mathematical-modulo)

## Examples

### Example 1
```text
Input: n = 3, k = 2
Output: 2
Explanation:
1. At second 0, the ball is at child 0.
2. At second 1, the ball moves to child 1.
3. At second 2, the ball moves to child 2.
So, the ball is at child 2 after 2 seconds.
```

### Example 2
```text
Input: n = 4, k = 5
Output: 1
Explanation:
1. At second 0, the ball is at child 0.
2. At second 1, the ball moves to child 1.
3. At second 2, the ball moves to child 2.
4. At second 3, the ball moves to child 3.
5. At second 4, the ball changes direction to left and moves to child 2.
6. At second 5, the ball moves to child 1.
So, the ball is at child 1 after 5 seconds.
```

### Example 3
```text
Input: n = 2, k = 4
Output: 0
Explanation:
1. At second 0, the ball is at child 0.
2. At second 1, the ball moves to child 1.
3. At second 2, the ball changes direction to left and moves to child 0.
4. At second 3, the ball changes direction to right and moves to child 1.
5. At second 4, the ball changes direction to left and moves to child 0.
So, the ball is at child 0 after 4 seconds.
```

## Constraints

```text
2 <= n <= 50
1 <= k <= 50
```

---

## Simulation

**Intuition**
Since the constraints for n and k are very small (up to 50), we can simply simulate the process second by second. We keep track of the current position and the direction of movement.

**Steps**
- Initialize `pos` to 0 and `direction` to 1 (representing right).
- Loop `k` times:
    - Update `pos` by adding `direction`.
    - If `pos` reaches `n - 1`, change `direction` to -1 (left).
    - If `pos` reaches 0, change `direction` to 1 (right).
- Return `pos`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numberOfChild(self, n: int, k: int) -&gt; int:\n        pos = 0\n        direction = 1\n        for _ in range(k):\n            pos += direction\n            if pos == n - 1:\n                direction = -1\n            elif pos == 0:\n                direction = 1\n        return pos" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numberOfChild(int n, int k) {\n        int pos = 0;\n        int dir = 1;\n        for (int i = 0; i &lt; k; i++) {\n            pos += dir;\n            if (pos == n - 1) {\n                dir = -1;\n            } else if (pos == 0) {\n                dir = 1;\n            }\n        }\n        return pos;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numberOfChild(int n, int k) {\n        int pos = 0;\n        int dir = 1;\n        for (int i = 0; i &lt; k; ++i) {\n            pos += dir;\n            if (pos == n - 1) {\n                dir = -1;\n            } else if (pos == 0) {\n                dir = 1;\n            }\n        }\n        return pos;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var numberOfChild = function(n, k) {\n    let pos = 0;\n    let dir = 1;\n    for (let i = 0; i &lt; k; i++) {\n        pos += dir;\n        if (pos === n - 1) {\n            dir = -1;\n        } else if (pos === 0) {\n            dir = 1;\n        }\n    }\n    return pos;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfChild(n: number, k: number): number {\n    let pos = 0;\n    let dir = 1;\n    for (let i = 0; i &lt; k; i++) {\n        pos += dir;\n        if (pos === n - 1) {\n            dir = -1;\n        } else if (pos === 0) {\n            dir = 1;\n        }\n    }\n    return pos;\n}" },]} />

**Complexity**
- Time: O(k)
- Space: O(1)
- Notes: Simple and easy to implement, suitable for small constraints.

---

## Mathematical Modulo

**Intuition**
The ball moves in a repeating cycle. It goes from 0 to n-1 and back to 0. The length of this cycle is 2 * (n - 1). We can use the modulo operator to find the effective time within one cycle and map it to the child's position.

**Steps**
- Calculate the cycle length: `cycle = 2 * (n - 1)`.
- Find the remainder of `k` divided by `cycle`: `rem = k % cycle`.
- If `rem` is less than or equal to `n - 1`, the ball is moving right, so the position is `rem`.
- Otherwise, the ball is moving left. The position is `cycle - rem`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numberOfChild(self, n: int, k: int) -&gt; int:\n        cycle = 2 * (n - 1)\n        rem = k % cycle\n        if rem &lt;= n - 1:\n            return rem\n        return cycle - rem" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numberOfChild(int n, int k) {\n        int cycle = 2 * (n - 1);\n        int rem = k % cycle;\n        if (rem &lt;= n - 1) {\n            return rem;\n        }\n        return cycle - rem;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numberOfChild(int n, int k) {\n        int cycle = 2 * (n - 1);\n        int rem = k % cycle;\n        if (rem &lt;= n - 1) {\n            return rem;\n        }\n        return cycle - rem;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var numberOfChild = function(n, k) {\n    const cycle = 2 * (n - 1);\n    const rem = k % cycle;\n    if (rem &lt;= n - 1) {\n        return rem;\n    }\n    return cycle - rem;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfChild(n: number, k: number): number {\n    const cycle = 2 * (n - 1);\n    const rem = k % cycle;\n    if (rem &lt;= n - 1) {\n        return rem;\n    }\n    return cycle - rem;\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Optimal solution, handles very large values of k efficiently.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find the Child Who Has the Ball After K Seconds Solutions","datePublished":"2025-02-03","description":"You are given n children standing in a line. The ball starts at child 0 and moves back and forth. Return the child holding the ball after k seconds.","keywords":"LeetCode, Find the Child Who Has the Ball After K Seconds, Easy, Math, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Simulation approach is O(k), where k is the number of seconds."}},{"@type":"Question","name":"What is the time complexity of the Mathematical Modulo approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Mathematical Modulo approach is O(1), as it involves a constant number of arithmetic operations."}},{"@type":"Question","name":"How does the Mathematical Modulo approach work?","acceptedAnswer":{"@type":"Answer","text":"It calculates the position based on the cycle length 2 * (n - 1). The remainder of k divided by the cycle length determines the position within the cycle."}}]}).replace(/</g,'\u003c')}
</script>