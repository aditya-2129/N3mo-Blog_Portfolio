---
title: "Replace All Digits with Characters"
summary: "Replace each digit in a string with the character shifted by that digit from the previous character."
date: "Jun 17, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.70% | **Paid:** No
> **Topics:** String

You are given a 0-indexed string s that has lowercase English letters in its even indices and digits in its odd indices.

There is a function shift(c, x), where c is a character and x is a digit, that returns the xth character after c.

For example, shift('a', 5) = 'f' and shift('x', 0) = 'x'.

For every odd index i, you want to replace the digit at s[i] with shift(s[i-1], s[i]).

Return s after replacing all digits. It is guaranteed that all shift operations have valid results.

- Examples
- Constraints
- [Iterative String Building](#iterative-string-building)
- [In-place Character Array Modification](#in-place-character-array-modification)
- [Recursive Solution](#recursive-solution)

## Examples

```text
Input: s = "a1c1e"
Output: "abcde"
Explanation: The digits are replaced as follows:
- s[1] = shift('a',1) = 'b'
- s[3] = shift('c',1) = 'd'
```

```text
Input: s = "a1b2c3d4e"
Output: "abbdcfdhe"
Explanation: The digits are replaced as follows:
- s[1] = shift('a',1) = 'b'
- s[3] = shift('b',2) = 'd'
- s[5] = shift('c',3) = 'f'
- s[7] = shift('d',4) = 'h'
```

## Constraints

```text
2 <= s.length <= 100
s consists only of lowercase English letters and digits.
s is guaranteed to have an even number of digits.
All shift operations have valid results.
```

---
## Iterative String Building

**Intuition**
Iterate through the string character by character, keeping letters as-is and converting digits to shifted characters based on the previous letter.

**Steps**
- Initialize an empty result list/array
- Loop through each character with its index
- If index is even, append the character directly
- If index is odd, calculate shifted character using ASCII values and append
- Join the result list into a string and return

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def replaceDigits(self, s: str) -&gt; str:\n        result = []\n        for i, ch in enumerate(s):\n            if i % 2 == 0:\n                result.append(ch)\n            else:\n                prev = s[i - 1]\n                shift = int(ch)\n                result.append(chr(ord(prev) + shift))\n        return ''.join(result)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String replaceDigits(String s) {\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i &lt; s.length(); i++) {\n            if (i % 2 == 0) {\n                sb.append(s.charAt(i));\n            } else {\n                char prev = s.charAt(i - 1);\n                int shift = s.charAt(i) - '0';\n                sb.append((char)(prev + shift));\n            }\n        }\n        return sb.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string replaceDigits(string s) {\n        string result = \"\";\n        for (int i = 0; i &lt; s.size(); i++) {\n            if (i % 2 == 0) {\n                result += s[i];\n            } else {\n                char prev = s[i - 1];\n                int shift = s[i] - '0';\n                result += (char)(prev + shift);\n            }\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    replaceDigits(s) {\n        let result = [];\n        for (let i = 0; i &lt; s.length; i++) {\n            if (i % 2 === 0) {\n                result.push(s[i]);\n            } else {\n                let prev = s[i - 1];\n                let shift = parseInt(s[i]);\n                result.push(String.fromCharCode(prev.charCodeAt(0) + shift));\n            }\n        }\n        return result.join('');\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    replaceDigits(s: string): string {\n        let result: string[] = [];\n        for (let i = 0; i &lt; s.length; i++) {\n            if (i % 2 === 0) {\n                result.push(s[i]);\n            } else {\n                let prev = s[i - 1];\n                let shift = parseInt(s[i]);\n                result.push(String.fromCharCode(prev.charCodeAt(0) + shift));\n            }\n        }\n        return result.join('');\n    }\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for the result string
- Notes: Uses StringBuilder in Java for efficient concatenation

---
## In-place Character Array Modification

**Intuition**
Convert the string to a mutable character array and modify digits in-place by iterating through odd indices only.

**Steps**
- Convert string to character array
- Iterate through odd indices (1, 3, 5, ...)
- For each odd index, calculate shifted character from previous character
- Replace the digit with the shifted character
- Convert back to string and return

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def replaceDigits(self, s: str) -&gt; str:\n        chars = list(s)\n        for i in range(1, len(chars), 2):\n            prev = chars[i - 1]\n            shift = int(chars[i])\n            chars[i] = chr(ord(prev) + shift)\n        return ''.join(chars)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String replaceDigits(String s) {\n        char[] chars = s.toCharArray();\n        for (int i = 1; i &lt; chars.length; i += 2) {\n            char prev = chars[i - 1];\n            int shift = chars[i] - '0';\n            chars[i] = (char)(prev + shift);\n        }\n        return new String(chars);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string replaceDigits(string s) {\n        for (int i = 1; i &lt; s.size(); i += 2) {\n            char prev = s[i - 1];\n            int shift = s[i] - '0';\n            s[i] = (char)(prev + shift);\n        }\n        return s;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    replaceDigits(s) {\n        let chars = s.split('');\n        for (let i = 1; i &lt; chars.length; i += 2) {\n            let prev = chars[i - 1];\n            let shift = parseInt(chars[i]);\n            chars[i] = String.fromCharCode(prev.charCodeAt(0) + shift);\n        }\n        return chars.join('');\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    replaceDigits(s: string): string {\n        let chars: string[] = s.split('');\n        for (let i = 1; i &lt; chars.length; i += 2) {\n            let prev = chars[i - 1];\n            let shift = parseInt(chars[i]);\n            chars[i] = String.fromCharCode(prev.charCodeAt(0) + shift);\n        }\n        return chars.join('');\n    }\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for the character array (O(1) in C++ since strings are mutable)
- Notes: More memory efficient as we only iterate through odd indices

---
## Recursive Solution

**Intuition**
Use recursion to process the string, handling each character and building the result by combining the current character with the result of processing the rest.

**Steps**
- Define a recursive helper function with index parameter
- Base case: if index exceeds string length, return empty string
- If index is even, return current character plus recursive result
- If index is odd, calculate shifted character and return it plus recursive result
- Start recursion from index 0

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def replaceDigits(self, s: str) -&gt; str:\n        def helper(i):\n            if i &gt;= len(s):\n                return ''\n            if i % 2 == 0:\n                return s[i] + helper(i + 1)\n            else:\n                prev = s[i - 1]\n                shift = int(s[i])\n                return chr(ord(prev) + shift) + helper(i + 1)\n        return helper(0)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String replaceDigits(String s) {\n        return helper(s, 0);\n    }\n    \n    private String helper(String s, int i) {\n        if (i &gt;= s.length()) {\n            return \"\";\n        }\n        if (i % 2 == 0) {\n            return s.charAt(i) + helper(s, i + 1);\n        } else {\n            char prev = s.charAt(i - 1);\n            int shift = s.charAt(i) - '0';\n            return (char)(prev + shift) + helper(s, i + 1);\n        }\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string replaceDigits(string s) {\n        return helper(s, 0);\n    }\n    \nprivate:\n    string helper(string& s, int i) {\n        if (i &gt;= s.size()) {\n            return \"\";\n        }\n        if (i % 2 == 0) {\n            return string(1, s[i]) + helper(s, i + 1);\n        } else {\n            char prev = s[i - 1];\n            int shift = s[i] - '0';\n            return string(1, (char)(prev + shift)) + helper(s, i + 1);\n        }\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    replaceDigits(s) {\n        const helper = (i) =&gt; {\n            if (i &gt;= s.length) {\n                return '';\n            }\n            if (i % 2 === 0) {\n                return s[i] + helper(i + 1);\n            } else {\n                let prev = s[i - 1];\n                let shift = parseInt(s[i]);\n                return String.fromCharCode(prev.charCodeAt(0) + shift) + helper(i + 1);\n            }\n        };\n        return helper(0);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    replaceDigits(s: string): string {\n        const helper = (i: number): string =&gt; {\n            if (i &gt;= s.length) {\n                return '';\n            }\n            if (i % 2 === 0) {\n                return s[i] + helper(i + 1);\n            } else {\n                let prev = s[i - 1];\n                let shift = parseInt(s[i]);\n                return String.fromCharCode(prev.charCodeAt(0) + shift) + helper(i + 1);\n            }\n        };\n        return helper(0);\n    }\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for recursion stack and result string
- Notes: Not recommended for production due to stack overflow risk on large inputs

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Replace All Digits with Characters Solutions","datePublished":"2025-06-17","description":"Replace each digit in a string with the character shifted by that digit from the previous character.","keywords":"LeetCode, Replace All Digits with Characters, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Iterative String Building approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of the string."}},{"@type":"Question","name":"What is the space complexity of In-place Character Array Modification?","acceptedAnswer":{"@type":"Answer","text":"O(n) for the character array, except in C++ where strings are mutable, making it O(1)."}},{"@type":"Question","name":"Is the Recursive Solution recommended for production use?","acceptedAnswer":{"@type":"Answer","text":"No, due to potential stack overflow risk on large inputs."}},{"@type":"Question","name":"What is the most efficient approach for this problem?","acceptedAnswer":{"@type":"Answer","text":"In-place Character Array Modification is most efficient as it only iterates through odd indices and modifies in-place."}}]}).replace(/</g,'\u003c')}
</script>