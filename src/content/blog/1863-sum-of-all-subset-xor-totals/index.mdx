---
title: "Sum of All Subset XOR Totals"
summary: "Calculate the sum of XOR totals for every subset of an array."
date: "Feb 02, 2025"
tags:
  - Array
  - Math
  - Backtracking
  - Bit Manipulation
  - Combinatorics
  - Enumeration
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 90.10% | **Paid:** No
> **Topics:** Array, Math, Backtracking, Bit Manipulation, Combinatorics, Enumeration

The XOR total of an array is defined as the bitwise XOR of all its elements, or 0 if the array is empty.

For example, the XOR total of the array [2,5,6] is 2 XOR 5 XOR 6 = 1.

Given an array nums, return the sum of all XOR totals for every subset of nums.

Note: Subsets with the same elements but in different orders are considered the same.

- Examples
- Constraints
- [Backtracking](#backtracking)
- [Iterative Bitmask](#iterative-bitmask)
- [Bit Manipulation](#bit-manipulation)

## Examples

```text
Input: nums = [1,3]
Output: 6
Explanation: The 4 subsets of [1,3] are:
- The empty subset has an XOR total of 0.
- [1] has an XOR total of 1.
- [3] has an XOR total of 3.
- [1,3] has an XOR total of 1 XOR 3 = 2.
0 + 1 + 3 + 2 = 6
```

```text
Input: nums = [5,1,6]
Output: 28
Explanation: The 8 subsets of [5,1,6] are:
- The empty subset has an XOR total of 0.
- [5] has an XOR total of 5.
- [1] has an XOR total of 1.
- [6] has an XOR total of 6.
- [5,1] has an XOR total of 5 XOR 1 = 4.
- [5,6] has an XOR total of 5 XOR 6 = 3.
- [1,6] has an XOR total of 1 XOR 6 = 7.
- [5,1,6] has an XOR total of 5 XOR 1 XOR 6 = 2.
0 + 5 + 1 + 6 + 4 + 3 + 7 + 2 = 28
```

```text
Input: nums = [3,4,5,6,7,8]
Output: 480
Explanation: The sum of all XOR totals for every subset is 480.
```

## Constraints

```text
1 <= nums.length <= 12
1 <= nums[i] <= 20
```

---

## Backtracking

**Intuition**
Generate all possible subsets using recursion and calculate XOR for each subset, accumulating the sum.

**Steps**
- Use DFS to explore including or excluding each element
- Track current XOR value and add to total sum at each step
- Return the accumulated sum after exploring all possibilities

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def subsetXORSum(self, nums: List[int]) -&gt; int:\r\n        self.total = 0\r\n        \r\n        def dfs(index, current_xor):\r\n            if index == len(nums):\r\n                self.total += current_xor\r\n                return\r\n            dfs(index + 1, current_xor ^ nums[index])\r\n            dfs(index + 1, current_xor)\r\n        \r\n        dfs(0, 0)\r\n        return self.total" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    private int total = 0;\r\n    \r\n    public int subsetXORSum(int[] nums) {\r\n        dfs(nums, 0, 0);\r\n        return total;\r\n    }\r\n    \r\n    private void dfs(int[] nums, int index, int currentXor) {\r\n        if (index == nums.length) {\r\n            total += currentXor;\r\n            return;\r\n        }\r\n        dfs(nums, index + 1, currentXor ^ nums[index]);\r\n        dfs(nums, index + 1, currentXor);\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\r\npublic:\r\n    int total = 0;\r\n    \r\n    int subsetXORSum(vector&lt;int&gt;& nums) {\r\n        dfs(nums, 0, 0);\r\n        return total;\r\n    }\r\n    \r\n    void dfs(vector&lt;int&gt;& nums, int index, int currentXor) {\r\n        if (index == nums.size()) {\r\n            total += currentXor;\r\n            return;\r\n        }\r\n        dfs(nums, index + 1, currentXor ^ nums[index]);\r\n        dfs(nums, index + 1, currentXor);\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {number[]} nums\r\n * @return {number}\r\n */\r\nvar subsetXORSum = function(nums) {\r\n    let total = 0;\r\n    \r\n    function dfs(index, currentXor) {\r\n        if (index === nums.length) {\r\n            total += currentXor;\r\n            return;\r\n        }\r\n        dfs(index + 1, currentXor ^ nums[index]);\r\n        dfs(index + 1, currentXor);\r\n    }\r\n    \r\n    dfs(0, 0);\r\n    return total;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function subsetXORSum(nums: number[]): number {\r\n    let total = 0;\r\n    \r\n    function dfs(index: number, currentXor: number): void {\r\n        if (index === nums.length) {\r\n            total += currentXor;\r\n            return;\r\n        }\r\n        dfs(index + 1, currentXor ^ nums[index]);\r\n        dfs(index + 1, currentXor);\r\n    }\r\n    \r\n    dfs(0, 0);\r\n    return total;\r\n}" },]} />

**Complexity**
- Time: O(2ⁿ) where n is the length of nums
- Space: O(n) for recursion stack
- Notes: Simple but exponential time complexity

---

## Iterative Bitmask

**Intuition**
Use bitmask enumeration to represent each subset and calculate XOR for all 2ⁿ possible subsets.

**Steps**
- Iterate through all bitmasks from 0 to 2ⁿ - 1
- For each mask, calculate XOR of elements where corresponding bit is set
- Sum all XOR values

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def subsetXORSum(self, nums: List[int]) -&gt; int:\r\n        n = len(nums)\r\n        total = 0\r\n        \r\n        for mask in range(1 &lt;&lt; n):\r\n            current_xor = 0\r\n            for i in range(n):\r\n                if mask & (1 &lt;&lt; i):\r\n                    current_xor ^= nums[i]\r\n            total += current_xor\r\n        \r\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int subsetXORSum(int[] nums) {\r\n        int n = nums.length;\r\n        int total = 0;\r\n        \r\n        for (int mask = 0; mask &lt; (1 &lt;&lt; n); mask++) {\r\n            int currentXor = 0;\r\n            for (int i = 0; i &lt; n; i++) {\r\n                if ((mask & (1 &lt;&lt; i)) != 0) {\r\n                    currentXor ^= nums[i];\r\n                }\r\n            }\r\n            total += currentXor;\r\n        }\r\n        \r\n        return total;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\r\npublic:\r\n    int subsetXORSum(vector&lt;int&gt;& nums) {\r\n        int n = nums.size();\r\n        int total = 0;\r\n        \r\n        for (int mask = 0; mask &lt; (1 &lt;&lt; n); mask++) {\r\n            int currentXor = 0;\r\n            for (int i = 0; i &lt; n; i++) {\r\n                if (mask & (1 &lt;&lt; i)) {\r\n                    currentXor ^= nums[i];\r\n                }\r\n            }\r\n            total += currentXor;\r\n        }\r\n        \r\n        return total;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {number[]} nums\r\n * @return {number}\r\n */\r\nvar subsetXORSum = function(nums) {\r\n    const n = nums.length;\r\n    let total = 0;\r\n    \r\n    for (let mask = 0; mask &lt; (1 &lt;&lt; n); mask++) {\r\n        let currentXor = 0;\r\n        for (let i = 0; i &lt; n; i++) {\r\n            if (mask & (1 &lt;&lt; i)) {\r\n                currentXor ^= nums[i];\r\n            }\r\n        }\r\n        total += currentXor;\r\n    }\r\n    \r\n    return total;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function subsetXORSum(nums: number[]): number {\r\n    const n = nums.length;\r\n    let total = 0;\r\n    \r\n    for (let mask = 0; mask &lt; (1 &lt;&lt; n); mask++) {\r\n        let currentXor = 0;\r\n        for (let i = 0; i &lt; n; i++) {\r\n            if (mask & (1 &lt;&lt; i)) {\r\n                currentXor ^= nums[i];\r\n            }\r\n        }\r\n        total += currentXor;\r\n    }\r\n    \r\n    return total;\r\n}" },]} />

**Complexity**
- Time: O(n × 2ⁿ) where n is the length of nums
- Space: O(1)
- Notes: No recursion overhead, but still exponential

---

## Bit Manipulation

**Intuition**
For each bit position, if any number has that bit set, exactly 2^(n-1) subsets will have that bit in their XOR result.

**Steps**
- For each bit position from 0 to 20 (max value constraint)
- Check if any number has that bit set
- If yes, add 2^(n-1) × 2^bit to the total sum

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def subsetXORSum(self, nums: List[int]) -&gt; int:\r\n        n = len(nums)\r\n        total = 0\r\n        \r\n        for bit in range(21):\r\n            bit_set = False\r\n            for num in nums:\r\n                if num & (1 &lt;&lt; bit):\r\n                    bit_set = True\r\n                    break\r\n            if bit_set:\r\n                total += (1 &lt;&lt; (n - 1)) * (1 &lt;&lt; bit)\r\n        \r\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int subsetXORSum(int[] nums) {\r\n        int n = nums.length;\r\n        int total = 0;\r\n        \r\n        for (int bit = 0; bit &lt;= 20; bit++) {\r\n            boolean bitSet = false;\r\n            for (int num : nums) {\r\n                if ((num & (1 &lt;&lt; bit)) != 0) {\r\n                    bitSet = true;\r\n                    break;\r\n                }\r\n            }\r\n            if (bitSet) {\r\n                total += (1 &lt;&lt; (n - 1)) * (1 &lt;&lt; bit);\r\n            }\r\n        }\r\n        \r\n        return total;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\r\npublic:\r\n    int subsetXORSum(vector&lt;int&gt;& nums) {\r\n        int n = nums.size();\r\n        int total = 0;\r\n        \r\n        for (int bit = 0; bit &lt;= 20; bit++) {\r\n            bool bitSet = false;\r\n            for (int num : nums) {\r\n                if (num & (1 &lt;&lt; bit)) {\r\n                    bitSet = true;\r\n                    break;\r\n                }\r\n            }\r\n            if (bitSet) {\r\n                total += (1 &lt;&lt; (n - 1)) * (1 &lt;&lt; bit);\r\n            }\r\n        }\r\n        \r\n        return total;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {number[]} nums\r\n * @return {number}\r\n */\r\nvar subsetXORSum = function(nums) {\r\n    const n = nums.length;\r\n    let total = 0;\r\n    \r\n    for (let bit = 0; bit &lt;= 20; bit++) {\r\n        let bitSet = false;\r\n        for (const num of nums) {\r\n            if (num & (1 &lt;&lt; bit)) {\r\n                bitSet = true;\r\n                break;\r\n            }\r\n        }\r\n        if (bitSet) {\r\n            total += (1 &lt;&lt; (n - 1)) * (1 &lt;&lt; bit);\r\n        }\r\n    }\r\n    \r\n    return total;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function subsetXORSum(nums: number[]): number {\r\n    const n = nums.length;\r\n    let total = 0;\r\n    \r\n    for (let bit = 0; bit &lt;= 20; bit++) {\r\n        let bitSet = false;\r\n        for (const num of nums) {\r\n            if (num & (1 &lt;&lt; bit)) {\r\n                bitSet = true;\r\n                break;\r\n            }\r\n        }\r\n        if (bitSet) {\r\n            total += (1 &lt;&lt; (n - 1)) * (1 &lt;&lt; bit);\r\n        }\r\n    }\r\n    \r\n    return total;\r\n}" },]} />

**Complexity**
- Time: O(n × 21) = O(n) where n is the length of nums
- Space: O(1)
- Notes: Optimal solution with linear time complexity

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Sum of All Subset XOR Totals Solutions","datePublished":"2025-02-02","description":"Calculate the sum of XOR totals for every subset of an array.","keywords":"LeetCode, Sum of All Subset XOR Totals, Easy, Array, Math, Backtracking, Bit Manipulation, Combinatorics, Enumeration"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Backtracking approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(2^n) where n is the length of nums, as we explore all possible subsets."}},{"@type":"Question","name":"What is the time complexity of the Iterative Bitmask approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n * 2^n) where n is the length of nums, as we iterate through all 2^n subsets and for each subset we check n elements."}},{"@type":"Question","name":"What is the time complexity of the Bit Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n * 21) = O(n) where n is the length of nums, as we only check each bit position (up to 20) against all elements."}},{"@type":"Question","name":"Which approach is the most efficient?","acceptedAnswer":{"@type":"Answer","text":"The Bit Manipulation approach is the most efficient with O(n) time complexity and O(1) space complexity."}}]}).replace(/</g,'\u003c')}
</script>