---
title: "Ant on the Boundary"
summary: "An ant moves left or right based on an array of integers. Return the number of times it returns to the boundary (position 0) after the first move."
date: "Nov 12, 2025"
tags:
  - Array
  - Simulation
  - Prefix Sum
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 74.40% | **Paid:** No
> **Topics:** Array, Simulation, Prefix Sum

An ant is sitting on the boundary of a number line. The ant starts at position 0. It is given an array nums of length n where nums[i] represents the move the ant makes at the i-th step.

If nums[i] &gt; 0, the ant moves right nums[i] units.
If nums[i] &lt; 0, the ant moves left abs(nums[i]) units.

Return the number of times the ant returns to the boundary.

Note: The ant returns to the boundary when its position is 0 after a move. The ant does not count the starting position (0) as a return.

- Examples
- Constraints
- [Brute Force Simulation](#brute-force-simulation)
- [Prefix Sum (Optimal)](#prefix-sum-optimal)

## Examples

Example 1:
```text
Input: nums = [2,3,-5]
Output: 1
Explanation:
After the 1st move, the ant is at position 2.
After the 2nd move, the ant is at position 5.
After the 3rd move, the ant is at position 0.
So, the ant returns to the boundary 1 time.
```

Example 2:
```text
Input: nums = [3,2,-3,-4]
Output: 0
Explanation:
After the 1st move, the ant is at position 3.
After the 2nd move, the ant is at position 5.
After the 3rd move, the ant is at position 2.
After the 4th move, the ant is at position -2.
The ant never returns to the boundary.
```

## Constraints

```text
1 <= nums.length <= 100
-10⁹ <= nums[i] <= 10⁹
nums[i] != 0
```

---
## Brute Force Simulation

**Intuition**
Simulate the ant's movement step-by-step. For every step, calculate the total distance traveled from the start by summing all previous moves to check if the current position is 0.

**Steps**
- Initialize a counter for returns to 0.
- Iterate through the array with an outer loop representing the current step.
- Inside the outer loop, use an inner loop to sum all elements from the start up to the current index.
- If the calculated sum is 0, increment the counter.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def returnToBoundaryCount(self, nums: list[int]) -&gt; int:\n        count = 0\n        n = len(nums)\n        for i in range(n):\n            current_sum = 0\n            for j in range(i + 1):\n                current_sum += nums[j]\n            if current_sum == 0:\n                count += 1\n        return count\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int returnToBoundaryCount(int[] nums) {\n        int count = 0;\n        int n = nums.length;\n        for (int i = 0; i &lt; n; i++) {\n            long currentSum = 0;\n            for (int j = 0; j &lt;= i; j++) {\n                currentSum += nums[j];\n            }\n            if (currentSum == 0) {\n                count++;\n            }\n        }\n        return count;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int returnToBoundaryCount(vector&lt;int&gt;& nums) {\n        int count = 0;\n        int n = nums.size();\n        for (int i = 0; i &lt; n; i++) {\n            long long currentSum = 0;\n            for (int j = 0; j &lt;= i; j++) {\n                currentSum += nums[j];\n            }\n            if (currentSum == 0) {\n                count++;\n            }\n        }\n        return count;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var returnToBoundaryCount = function(nums) {\n    let count = 0;\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        let currentSum = 0;\n        for (let j = 0; j &lt;= i; j++) {\n            currentSum += nums[j];\n        }\n        if (currentSum === 0) {\n            count++;\n        }\n    }\n    return count;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function returnToBoundaryCount(nums: number[]): number {\n    let count = 0;\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        let currentSum = 0;\n        for (let j = 0; j &lt;= i; j++) {\n            currentSum += nums[j];\n        }\n        if (currentSum === 0) {\n            count++;\n        }\n    }\n    return count;\n}\n" },]} />

**Complexity**
- Time: O(n²) - We iterate through the array and for each element, we sum up to that element again.
- Space: O(1) - We only use a few variables for counting and summing.
- Notes: This approach is inefficient for larger arrays but demonstrates the simulation logic clearly.

---
## Prefix Sum (Optimal)

**Intuition**
Instead of recalculating the sum from scratch for every step, maintain a running total (prefix sum) of the ant's position. If the running total becomes 0, the ant is at the boundary.

**Steps**
- Initialize a variable `position` to 0 and a counter `result` to 0.
- Iterate through each number in the input array.
- Add the current number to `position`.
- If `position` is 0, increment `result`.
- Return `result`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def returnToBoundaryCount(self, nums: list[int]) -&gt; int:\n        position = 0\n        count = 0\n        for num in nums:\n            position += num\n            if position == 0:\n                count += 1\n        return count\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int returnToBoundaryCount(int[] nums) {\n        int position = 0;\n        int count = 0;\n        for (int num : nums) {\n            position += num;\n            if (position == 0) {\n                count++;\n            }\n        }\n        return count;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int returnToBoundaryCount(vector&lt;int&gt;& nums) {\n        int position = 0;\n        int count = 0;\n        for (int num : nums) {\n            position += num;\n            if (position == 0) {\n                count++;\n            }\n        }\n        return count;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var returnToBoundaryCount = function(nums) {\n    let position = 0;\n    let count = 0;\n    for (const num of nums) {\n        position += num;\n        if (position === 0) {\n            count++;\n        }\n    }\n    return count;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function returnToBoundaryCount(nums: number[]): number {\n    let position = 0;\n    let count = 0;\n    for (const num of nums) {\n        position += num;\n        if (position === 0) {\n            count++;\n        }\n    }\n    return count;\n}\n" },]} />

**Complexity**
- Time: O(n) - We pass through the array exactly once.
- Space: O(1) - We only use two integer variables regardless of input size.
- Notes: This is the most efficient approach, utilizing the concept of a running prefix sum.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Ant on the Boundary Solutions","datePublished":"2025-11-12","description":"An ant moves left or right based on an array of integers. Return the number of times it returns to the boundary (position 0) after the first move.","keywords":"LeetCode, Ant on the Boundary, Easy, Array, Simulation, Prefix Sum"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Prefix Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the input array."}},{"@type":"Question","name":"What is the space complexity of the Prefix Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1), as we only use a constant amount of extra space."}},{"@type":"Question","name":"Why is the Brute Force approach slower?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach has a time complexity of O(n²) because it recalculates the sum for every step from the beginning."}}]}).replace(/</g,'\u003c')}
</script>