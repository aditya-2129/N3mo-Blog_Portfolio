---
title: "Rearrange Spaces Between Words"
summary: "Rearrange spaces between words to distribute them equally, placing extra spaces at the end."
date: "Jan 15, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 44.20% | **Paid:** No
> **Topics:** String

You are given a string text of words that are placed among some number of spaces. Each word consists of one or more lowercase English letters and are separated by at least one space. It's guaranteed that text contains at least one word.

Rearrange the spaces so that there is an equal number of spaces between every pair of adjacent words and that number is maximized. If you cannot distribute all spaces equally, place the extra spaces at the end, meaning the returned string should be the same length as text.

Return the string after rearranging the spaces.

- Examples
- Constraints
- [Count and Distribute](#count-and-distribute)
- [Iterative Parsing](#iterative-parsing)

## Examples

### Example 1

**Input:**
```text
text = "  this   is  a sentence "
```

**Output:**
```text
"this   is   a   sentence"
```

**Explanation:**
There are a total of 9 spaces and 4 words. We can evenly divide the 9 spaces between the words: 9 / (4-1) = 3 spaces.

### Example 2

**Input:**
```text
text = " practice   makes   perfect"
```

**Output:**
```text
"practice   makes   perfect "
```

**Explanation:**
There are a total of 7 spaces and 3 words. 7 / (3-1) = 3 spaces plus 1 extra space. We place this extra space at the end of the string.

## Constraints

```text
1 <= text.length <= 100
text consists of lowercase English letters and spaces ' '.
text contains at least one word.
```

---

## Count and Distribute

**Intuition**
Count total spaces and extract words using built-in split, then calculate equal distribution between words with remainder at the end.

**Steps**
- Count total spaces in the string
- Extract all words using split
- Handle single word edge case (all spaces at end)
- Calculate spaces between words and extra spaces
- Join words with calculated gaps and append extra spaces

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def reorderSpaces(self, text: str) -&gt; str:\n        total_spaces = text.count(' ')\n        words = text.split()\n        num_words = len(words)\n        \n        if num_words == 1:\n            return words[0] + ' ' * total_spaces\n        \n        spaces_between = total_spaces // (num_words - 1)\n        extra_spaces = total_spaces % (num_words - 1)\n        \n        result = (' ' * spaces_between).join(words)\n        result += ' ' * extra_spaces\n        \n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String reorderSpaces(String text) {\n        int totalSpaces = 0;\n        for (char c : text.toCharArray()) {\n            if (c == ' ') totalSpaces++;\n        }\n        \n        String[] words = text.trim().split(\"\\\\s+\");\n        int numWords = words.length;\n        \n        if (numWords == 1) {\n            return words[0] + \" \".repeat(totalSpaces);\n        }\n        \n        int spacesBetween = totalSpaces / (numWords - 1);\n        int extraSpaces = totalSpaces % (numWords - 1);\n        \n        StringBuilder sb = new StringBuilder();\n        String gap = \" \".repeat(spacesBetween);\n        for (int i = 0; i &lt; numWords; i++) {\n            sb.append(words[i]);\n            if (i &lt; numWords - 1) {\n                sb.append(gap);\n            }\n        }\n        sb.append(\" \".repeat(extraSpaces));\n        \n        return sb.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string reorderSpaces(string text) {\n        int totalSpaces = 0;\n        vector&lt;string&gt; words;\n        string word;\n        \n        for (char c : text) {\n            if (c == ' ') {\n                totalSpaces++;\n                if (!word.empty()) {\n                    words.push_back(word);\n                    word.clear();\n                }\n            } else {\n                word += c;\n            }\n        }\n        if (!word.empty()) {\n            words.push_back(word);\n        }\n        \n        int numWords = words.size();\n        \n        if (numWords == 1) {\n            return words[0] + string(totalSpaces, ' ');\n        }\n        \n        int spacesBetween = totalSpaces / (numWords - 1);\n        int extraSpaces = totalSpaces % (numWords - 1);\n        \n        string result;\n        string gap(spacesBetween, ' ');\n        for (int i = 0; i &lt; numWords; i++) {\n            result += words[i];\n            if (i &lt; numWords - 1) {\n                result += gap;\n            }\n        }\n        result += string(extraSpaces, ' ');\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} text\n * @return {string}\n */\nvar reorderSpaces = function(text) {\n    let totalSpaces = 0;\n    for (let c of text) {\n        if (c === ' ') totalSpaces++;\n    }\n    \n    const words = text.trim().split(/\\s+/);\n    const numWords = words.length;\n    \n    if (numWords === 1) {\n        return words[0] + ' '.repeat(totalSpaces);\n    }\n    \n    const spacesBetween = Math.floor(totalSpaces / (numWords - 1));\n    const extraSpaces = totalSpaces % (numWords - 1);\n    \n    const gap = ' '.repeat(spacesBetween);\n    let result = words.join(gap);\n    result += ' '.repeat(extraSpaces);\n    \n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function reorderSpaces(text: string): string {\n    let totalSpaces = 0;\n    for (const c of text) {\n        if (c === ' ') totalSpaces++;\n    }\n    \n    const words = text.trim().split(/\\s+/);\n    const numWords = words.length;\n    \n    if (numWords === 1) {\n        return words[0] + ' '.repeat(totalSpaces);\n    }\n    \n    const spacesBetween = Math.floor(totalSpaces / (numWords - 1));\n    const extraSpaces = totalSpaces % (numWords - 1);\n    \n    const gap = ' '.repeat(spacesBetween);\n    let result = words.join(gap);\n    result += ' '.repeat(extraSpaces);\n    \n    return result;\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of text
- Space: O(n) for storing words and result
- Notes: Clean and readable using built-in string methods

---

## Iterative Parsing

**Intuition**
Manually parse the string character by character to count spaces and collect words, then build the result with calculated spacing.

**Steps**
- Iterate through each character counting spaces and building words
- Store completed words in a list
- Calculate spaces between words and remainder
- Build result by appending words with gaps between them

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def reorderSpaces(self, text: str) -&gt; str:\n        total_spaces = 0\n        words = []\n        current_word = []\n        \n        for c in text:\n            if c == ' ':\n                total_spaces += 1\n                if current_word:\n                    words.append(''.join(current_word))\n                    current_word = []\n            else:\n                current_word.append(c)\n        \n        if current_word:\n            words.append(''.join(current_word))\n        \n        num_words = len(words)\n        \n        if num_words == 1:\n            return words[0] + ' ' * total_spaces\n        \n        spaces_between = total_spaces // (num_words - 1)\n        extra_spaces = total_spaces % (num_words - 1)\n        \n        result = []\n        for i, word in enumerate(words):\n            result.append(word)\n            if i &lt; num_words - 1:\n                result.append(' ' * spaces_between)\n        \n        result.append(' ' * extra_spaces)\n        \n        return ''.join(result)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String reorderSpaces(String text) {\n        int totalSpaces = 0;\n        List&lt;String&gt; words = new ArrayList&lt;&gt;();\n        StringBuilder currentWord = new StringBuilder();\n        \n        for (char c : text.toCharArray()) {\n            if (c == ' ') {\n                totalSpaces++;\n                if (currentWord.length() &gt; 0) {\n                    words.add(currentWord.toString());\n                    currentWord.setLength(0);\n                }\n            } else {\n                currentWord.append(c);\n            }\n        }\n        \n        if (currentWord.length() &gt; 0) {\n            words.add(currentWord.toString());\n        }\n        \n        int numWords = words.size();\n        \n        if (numWords == 1) {\n            return words.get(0) + \" \".repeat(totalSpaces);\n        }\n        \n        int spacesBetween = totalSpaces / (numWords - 1);\n        int extraSpaces = totalSpaces % (numWords - 1);\n        \n        StringBuilder result = new StringBuilder();\n        String gap = \" \".repeat(spacesBetween);\n        \n        for (int i = 0; i &lt; numWords; i++) {\n            result.append(words.get(i));\n            if (i &lt; numWords - 1) {\n                result.append(gap);\n            }\n        }\n        result.append(\" \".repeat(extraSpaces));\n        \n        return result.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string reorderSpaces(string text) {\n        int totalSpaces = 0;\n        vector&lt;string&gt; words;\n        string word;\n        \n        for (char c : text) {\n            if (c == ' ') {\n                totalSpaces++;\n                if (!word.empty()) {\n                    words.push_back(word);\n                    word.clear();\n                }\n            } else {\n                word.push_back(c);\n            }\n        }\n        \n        if (!word.empty()) {\n            words.push_back(word);\n        }\n        \n        int numWords = words.size();\n        \n        if (numWords == 1) {\n            return words[0] + string(totalSpaces, ' ');\n        }\n        \n        int spacesBetween = totalSpaces / (numWords - 1);\n        int extraSpaces = totalSpaces % (numWords - 1);\n        \n        string result;\n        string gap(spacesBetween, ' ');\n        \n        for (int i = 0; i &lt; numWords; i++) {\n            result += words[i];\n            if (i &lt; numWords - 1) {\n                result += gap;\n            }\n        }\n        result += string(extraSpaces, ' ');\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} text\n * @return {string}\n */\nvar reorderSpaces = function(text) {\n    let totalSpaces = 0;\n    const words = [];\n    let currentWord = '';\n    \n    for (const c of text) {\n        if (c === ' ') {\n            totalSpaces++;\n            if (currentWord) {\n                words.push(currentWord);\n                currentWord = '';\n            }\n        } else {\n            currentWord += c;\n        }\n    }\n    \n    if (currentWord) {\n        words.push(currentWord);\n    }\n    \n    const numWords = words.length;\n    \n    if (numWords === 1) {\n        return words[0] + ' '.repeat(totalSpaces);\n    }\n    \n    const spacesBetween = Math.floor(totalSpaces / (numWords - 1));\n    const extraSpaces = totalSpaces % (numWords - 1);\n    \n    const gap = ' '.repeat(spacesBetween);\n    let result = '';\n    \n    for (let i = 0; i &lt; numWords; i++) {\n        result += words[i];\n        if (i &lt; numWords - 1) {\n            result += gap;\n        }\n    }\n    result += ' '.repeat(extraSpaces);\n    \n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function reorderSpaces(text: string): string {\n    let totalSpaces = 0;\n    const words: string[] = [];\n    let currentWord = '';\n    \n    for (const c of text) {\n        if (c === ' ') {\n            totalSpaces++;\n            if (currentWord) {\n                words.push(currentWord);\n                currentWord = '';\n            }\n        } else {\n            currentWord += c;\n        }\n    }\n    \n    if (currentWord) {\n        words.push(currentWord);\n    }\n    \n    const numWords = words.length;\n    \n    if (numWords === 1) {\n        return words[0] + ' '.repeat(totalSpaces);\n    }\n    \n    const spacesBetween = Math.floor(totalSpaces / (numWords - 1));\n    const extraSpaces = totalSpaces % (numWords - 1);\n    \n    const gap = ' '.repeat(spacesBetween);\n    let result = '';\n    \n    for (let i = 0; i &lt; numWords; i++) {\n        result += words[i];\n        if (i &lt; numWords - 1) {\n            result += gap;\n        }\n    }\n    result += ' '.repeat(extraSpaces);\n    \n    return result;\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of text
- Space: O(n) for storing words and result
- Notes: More explicit parsing logic, useful when built-in split is unavailable

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Rearrange Spaces Between Words Solutions","datePublished":"2025-01-15","description":"Rearrange spaces between words to distribute them equally, placing extra spaces at the end.","keywords":"LeetCode, Rearrange Spaces Between Words, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Count and Distribute approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of text, as we count spaces and split words in a single pass."}},{"@type":"Question","name":"What is the space complexity of Iterative Parsing approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for storing words and the result string."}},{"@type":"Question","name":"How do we handle the single word edge case?","acceptedAnswer":{"@type":"Answer","text":"When there is only one word, all spaces are placed at the end of that word."}}]}).replace(/</g,'\u003c')}
</script>