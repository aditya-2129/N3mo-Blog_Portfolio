---
title: "Excel Sheet Column Number"
summary: "Given a string columnTitle representing the column title as it appears in an Excel sheet, return its corresponding column number."
date: "Mar 11, 2026"
tags:
  - Math
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 67.70% | **Paid:** No
> **Topics:** Math, String

Given a string columnTitle that represents the column title as appears in an Excel sheet, return its corresponding column number.

For example:

A -> 1
B -> 2
C -> 3
...
Z -> 26
AA -> 27
AB -> 28
...

- Examples
- Constraints
- [Approach 1: Iterative Left-to-Right](#approach-1-iterative-left-to-right)
- [Approach 2: Iterative Right-to-Left](#approach-2-iterative-right-to-left)
- [Approach 3: Recursive](#approach-3-recursive)

## Examples

```text
Input: columnTitle = "A"
Output: 1
```

```text
Input: columnTitle = "AB"
Output: 28
```

```text
Input: columnTitle = "ZY"
Output: 701
```

## Constraints

```text
1 <= columnTitle.length <= 7
columnTitle consists only of uppercase English letters.
columnTitle is in the range ["A", "FXSHRXW"].
```

---
## Approach 1: Iterative Left-to-Right

**Intuition**
This problem is essentially a base conversion task, similar to converting a binary string to a decimal number, but here the base is 26. We can process the string from left to right, accumulating the result. For each new character, we multiply the current result by 26 (shifting the current digits to the left) and add the value of the new character.

**Steps**
- Initialize a variable `result` to 0.
- Iterate through each character in the string `columnTitle`.
- For each character, calculate its value: `val = char - 'A' + 1`.
- Update the result: `result = result * 26 + val`.
- Return the final `result`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def titleToNumber(self, columnTitle: str) -&gt; int:\n        result = 0\n        for char in columnTitle:\n            value = ord(char) - ord('A') + 1\n            result = result * 26 + value\n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int titleToNumber(String columnTitle) {\n        int result = 0;\n        for (int i = 0; i &lt; columnTitle.length(); i++) {\n            char c = columnTitle.charAt(i);\n            int value = c - 'A' + 1;\n            result = result * 26 + value;\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int titleToNumber(string columnTitle) {\n        int result = 0;\n        for (char c : columnTitle) {\n            int value = c - 'A' + 1;\n            result = result * 26 + value;\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} columnTitle\n * @return {number}\n */\nvar titleToNumber = function(columnTitle) {\n    let result = 0;\n    for (let i = 0; i &lt; columnTitle.length; i++) {\n        const value = columnTitle.charCodeAt(i) - 'A'.charCodeAt(0) + 1;\n        result = result * 26 + value;\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function titleToNumber(columnTitle: string): number {\n    let result = 0;\n    for (let i = 0; i &lt; columnTitle.length; i++) {\n        const value = columnTitle.charCodeAt(i) - 'A'.charCodeAt(0) + 1;\n        result = result * 26 + value;\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(n), where n is the length of the string. We iterate through the string once.
- Space: O(1), we only use a constant amount of extra space for variables.
- Notes: This is the most efficient and standard approach for this problem.

---
## Approach 2: Iterative Right-to-Left

**Intuition**
We can also view this as a polynomial sum where the rightmost character is multiplied by 26⁰, the next by 26¹, and so on. We iterate from the end of the string to the beginning, calculating the contribution of each character based on its position.

**Steps**
- Initialize `result` to 0 and `power` to 1.
- Iterate through the string from the last character to the first.
- Calculate the value of the current character.
- Add `value * power` to `result`.
- Multiply `power` by 26 for the next iteration.
- Return `result`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def titleToNumber(self, columnTitle: str) -&gt; int:\n        result = 0\n        power = 1\n        for char in reversed(columnTitle):\n            value = ord(char) - ord('A') + 1\n            result += value * power\n            power *= 26\n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int titleToNumber(String columnTitle) {\n        int result = 0;\n        int power = 1;\n        for (int i = columnTitle.length() - 1; i &gt;= 0; i--) {\n            char c = columnTitle.charAt(i);\n            int value = c - 'A' + 1;\n            result += value * power;\n            power *= 26;\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int titleToNumber(string columnTitle) {\n        int result = 0;\n        int power = 1;\n        for (int i = columnTitle.size() - 1; i &gt;= 0; i--) {\n            int value = columnTitle[i] - 'A' + 1;\n            result += value * power;\n            power *= 26;\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} columnTitle\n * @return {number}\n */\nvar titleToNumber = function(columnTitle) {\n    let result = 0;\n    let power = 1;\n    for (let i = columnTitle.length - 1; i &gt;= 0; i--) {\n        const value = columnTitle.charCodeAt(i) - 'A'.charCodeAt(0) + 1;\n        result += value * power;\n        power *= 26;\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function titleToNumber(columnTitle: string): number {\n    let result = 0;\n    let power = 1;\n    for (let i = columnTitle.length - 1; i &gt;= 0; i--) {\n        const value = columnTitle.charCodeAt(i) - 'A'.charCodeAt(0) + 1;\n        result += value * power;\n        power *= 26;\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(n), where n is the length of the string.
- Space: O(1), constant space usage.
- Notes: Slightly less intuitive than the left-to-right approach but mathematically equivalent.

---
## Approach 3: Recursive

**Intuition**
The problem can be defined recursively. The value of a string is 26 times the value of the string excluding the last character, plus the value of the last character. `f(s) = 26 * f(s[0:n-1]) + val(s[n-1])`.

**Steps**
- Base case: If the string is empty, return 0.
- Recursive step: Calculate the value of the last character.
- Return `26 * recursive_call(prefix) + last_char_value`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def titleToNumber(self, columnTitle: str) -&gt; int:\n        if not columnTitle:\n            return 0\n        last_val = ord(columnTitle[-1]) - ord('A') + 1\n        return 26 * self.titleToNumber(columnTitle[:-1]) + last_val" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int titleToNumber(String columnTitle) {\n        if (columnTitle == null || columnTitle.isEmpty()) {\n            return 0;\n        }\n        int lastVal = columnTitle.charAt(columnTitle.length() - 1) - 'A' + 1;\n        String prefix = columnTitle.substring(0, columnTitle.length() - 1);\n        return 26 * titleToNumber(prefix) + lastVal;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int titleToNumber(string columnTitle) {\n        if (columnTitle.empty()) {\n            return 0;\n        }\n        int lastVal = columnTitle.back() - 'A' + 1;\n        columnTitle.pop_back();\n        return 26 * titleToNumber(columnTitle) + lastVal;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} columnTitle\n * @return {number}\n */\nvar titleToNumber = function(columnTitle) {\n    if (columnTitle.length === 0) {\n        return 0;\n    }\n    const lastVal = columnTitle.charCodeAt(columnTitle.length - 1) - 'A'.charCodeAt(0) + 1;\n    const prefix = columnTitle.slice(0, -1);\n    return 26 * titleToNumber(prefix) + lastVal;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function titleToNumber(columnTitle: string): number {\n    if (columnTitle.length === 0) {\n        return 0;\n    }\n    const lastVal = columnTitle.charCodeAt(columnTitle.length - 1) - 'A'.charCodeAt(0) + 1;\n    const prefix = columnTitle.slice(0, -1);\n    return 26 * titleToNumber(prefix) + lastVal;\n}" },]} />

**Complexity**
- Time: O(n), where n is the length of the string.
- Space: O(n), due to the recursion stack depth.
- Notes: While elegant, this approach uses more stack memory than the iterative solutions.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Excel Sheet Column Number Solutions","datePublished":"2026-03-11","description":"Given a string columnTitle representing the column title as it appears in an Excel sheet, return its corresponding column number.","keywords":"LeetCode, Excel Sheet Column Number, Easy, Math, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Left-to-Right approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string."}},{"@type":"Question","name":"What is the space complexity of the Recursive approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) due to the recursion stack depth."}},{"@type":"Question","name":"Why is the Iterative Left-to-Right approach preferred?","acceptedAnswer":{"@type":"Answer","text":"It is preferred because it has O(1) space complexity and is straightforward to implement."}}]}).replace(/</g,'\u003c')}
</script>