---
title: "Consecutive Characters"
summary: "Given a string s, find the maximum length of a substring containing only one unique character."
date: "Dec 10, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 60.40% | **Paid:** No
> **Topics:** String

The power of the string is the maximum length of a non-empty substring that contains only one unique character.

Given a string s, return the power of s.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Single Pass](#single-pass)

## Examples

### Example 1

**Input:**
```text
s = "leetcode"
```

**Output:**
```text
2
```

**Explanation:**
The substring "ee" is of length 2 with the character 'e' only.

### Example 2

**Input:**
```text
s = "abbcccddddeeeeedcba"
```

**Output:**
```text
5
```

**Explanation:**
The substring "eeeee" is of length 5 with the character 'e' only.

## Constraints

```text
1 <= s.length <= 500
s consists of only lowercase English letters.
```

---
## Brute Force

**Intuition**
Check every possible substring to see if it consists of only one unique character and keep track of the maximum length found.

**Steps**
- Iterate through the string with a pointer `i` representing the start of the substring.
- Iterate with a pointer `j` from `i` to the end of the string.
- For each substring `s[i...j]`, check if all characters are the same.
- If they are, update the maximum power.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maxPower(self, s: str) -&gt; int:\n        n = len(s)\n        max_len = 0\n        for i in range(n):\n            for j in range(i, n):\n                if len(set(s[i:j+1])) == 1:\n                    max_len = max(max_len, j - i + 1)\n        return max_len" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxPower(String s) {\n        int n = s.length();\n        int maxLen = 0;\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i; j &lt; n; j++) {\n                boolean allSame = true;\n                char c = s.charAt(i);\n                for (int k = i + 1; k &lt;= j; k++) {\n                    if (s.charAt(k) != c) {\n                        allSame = false;\n                        break;\n                    }\n                }\n                if (allSame) {\n                    maxLen = Math.max(maxLen, j - i + 1);\n                }\n            }\n        }\n        return maxLen;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int maxPower(string s) {\n        int n = s.length();\n        int maxLen = 0;\n        for (int i = 0; i &lt; n; ++i) {\n            for (int j = i; j &lt; n; ++j) {\n                bool allSame = true;\n                char c = s[i];\n                for (int k = i + 1; k &lt;= j; ++k) {\n                    if (s[k] != c) {\n                        allSame = false;\n                        break;\n                    }\n                }\n                if (allSame) {\n                    maxLen = max(maxLen, j - i + 1);\n                }\n            }\n        }\n        return maxLen;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var maxPower = function(s) {\n    let n = s.length;\n    let maxLen = 0;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i; j &lt; n; j++) {\n            let sub = s.substring(i, j + 1);\n            let allSame = true;\n            for (let k = 1; k &lt; sub.length; k++) {\n                if (sub[k] !== sub[0]) {\n                    allSame = false;\n                    break;\n                }\n            }\n            if (allSame) {\n                maxLen = Math.max(maxLen, j - i + 1);\n            }\n        }\n    }\n    return maxLen;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function maxPower(s: string): number {\n    let n = s.length;\n    let maxLen = 0;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i; j &lt; n; j++) {\n            let sub = s.substring(i, j + 1);\n            let allSame = true;\n            for (let k = 1; k &lt; sub.length; k++) {\n                if (sub[k] !== sub[0]) {\n                    allSame = false;\n                    break;\n                }\n            }\n            if (allSame) {\n                maxLen = Math.max(maxLen, j - i + 1);\n            }\n        }\n    }\n    return maxLen;\n}" },]} />

**Complexity**
- Time: O(n³) or O(n²) depending on implementation details (checking substring validity takes O(k) where k is length). In the worst case, this is O(n²) if we check efficiently, but the provided solution is roughly O(n³) due to substring creation/iteration inside loops.
- Space: O(1) or O(k) for substring extraction depending on language.
- Notes: Simple to implement but inefficient for large strings.

---
## Single Pass

**Intuition**
Traverse the string once, maintaining a running count of the current consecutive character sequence. Update the maximum whenever the sequence breaks or ends.

**Steps**
- Initialize `max_power` and `current_count` to 1.
- Iterate from the second character to the end.
- If the current character is the same as the previous one, increment `current_count`.
- Otherwise, reset `current_count` to 1.
- Update `max_power` with the maximum of itself and `current_count`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maxPower(self, s: str) -&gt; int:\n        if not s:\n            return 0\n        max_len = 1\n        curr_len = 1\n        for i in range(1, len(s)):\n            if s[i] == s[i-1]:\n                curr_len += 1\n            else:\n                max_len = max(max_len, curr_len)\n                curr_len = 1\n        return max(max_len, curr_len)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxPower(String s) {\n        if (s == null || s.length() == 0) return 0;\n        int maxLen = 1;\n        int currLen = 1;\n        for (int i = 1; i &lt; s.length(); i++) {\n            if (s.charAt(i) == s.charAt(i - 1)) {\n                currLen++;\n            } else {\n                maxLen = Math.max(maxLen, currLen);\n                currLen = 1;\n            }\n        }\n        return Math.max(maxLen, currLen);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int maxPower(string s) {\n        if (s.empty()) return 0;\n        int maxLen = 1;\n        int currLen = 1;\n        for (int i = 1; i &lt; s.length(); ++i) {\n            if (s[i] == s[i - 1]) {\n                currLen++;\n            } else {\n                maxLen = max(maxLen, currLen);\n                currLen = 1;\n            }\n        }\n        return max(maxLen, currLen);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var maxPower = function(s) {\n    if (!s) return 0;\n    let maxLen = 1;\n    let currLen = 1;\n    for (let i = 1; i &lt; s.length; i++) {\n        if (s[i] === s[i - 1]) {\n            currLen++;\n        } else {\n            maxLen = Math.max(maxLen, currLen);\n            currLen = 1;\n        }\n    }\n    return Math.max(maxLen, currLen);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function maxPower(s: string): number {\n    if (!s) return 0;\n    let maxLen = 1;\n    let currLen = 1;\n    for (let i = 1; i &lt; s.length; i++) {\n        if (s[i] === s[i - 1]) {\n            currLen++;\n        } else {\n            maxLen = Math.max(maxLen, currLen);\n            currLen = 1;\n        }\n    }\n    return Math.max(maxLen, currLen);\n}" },]} />

**Complexity**
- Time: O(n) - We traverse the string exactly once.
- Space: O(1) - We only use a few variables for counting.
- Notes: This is the optimal solution for this problem.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Consecutive Characters Solutions","datePublished":"2024-12-10","description":"Given a string s, find the maximum length of a substring containing only one unique character.","keywords":"LeetCode, Consecutive Characters, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n^2) or O(n^3) depending on the implementation, as it checks all possible substrings."}},{"@type":"Question","name":"What is the time complexity of the Single Pass approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), as we iterate through the string only once."}},{"@type":"Question","name":"What is the space complexity of the optimal solution?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1), since we only use a constant amount of extra space for counters."}}]}).replace(/</g,'\u003c')}
</script>