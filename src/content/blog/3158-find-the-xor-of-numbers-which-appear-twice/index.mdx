---
title: "Find the XOR of Numbers Which Appear Twice"
summary: "You are given an integer array nums. Return the XOR of all the numbers that appear exactly twice in the array."
date: "May 04, 2025"
tags:
  - Array
  - Hash Table
  - Bit Manipulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 78.90% | **Paid:** No
> **Topics:** Array, Hash Table, Bit Manipulation

You are given a positive integer array nums.

Return the XOR of all the numbers that appear twice in the array. If no number appears twice, return 0.

- Examples
- Constraints
- [Hash Map Frequency Count](#hash-map-frequency-count)
- [Sorting](#sorting)
- [Fixed Array Counting](#fixed-array-counting)

## Examples

```text
Input: nums = [1,2,1,3]
Output: 1
Explanation: The number 1 appears twice, the XOR of 1 is 1.
```

```text
Input: nums = [1,2,3]
Output: 0
Explanation: No number appears twice, so the result is 0.
```

```text
Input: nums = [1,2,2,1]
Output: 3
Explanation: The numbers 1 and 2 appear twice. 1 XOR 2 = 3.
```

## Constraints

```text
1 <= nums.length <= 50
1 <= nums[i] <= 50
```

---
## Hash Map Frequency Count

**Intuition**
We can iterate through the array and count the frequency of each number using a hash map. Then, we iterate through the map entries and XOR the keys where the value is exactly 2.

**Steps**
- Initialize an empty hash map and a result variable set to 0.
- Iterate through the input array, incrementing the count for each number in the hash map.
- Iterate through the hash map entries. If a number's count is 2, XOR it with the result.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def duplicateNumbersXOR(self, nums: List[int]) -&gt; int:\n        counts = {}\n        res = 0\n        for n in nums:\n            counts[n] = counts.get(n, 0) + 1\n        for k, v in counts.items():\n            if v == 2:\n                res ^= k\n        return res" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int duplicateNumbersXOR(int[] nums) {\n        Map&lt;Integer, Integer&gt; map = new HashMap&lt;&gt;();\n        int res = 0;\n        for (int n : nums) {\n            map.put(n, map.getOrDefault(n, 0) + 1);\n        }\n        for (Map.Entry&lt;Integer, Integer&gt; entry : map.entrySet()) {\n            if (entry.getValue() == 2) {\n                res ^= entry.getKey();\n            }\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int duplicateNumbersXOR(vector&lt;int&gt;& nums) {\n        unordered_map&lt;int, int&gt; counts;\n        int res = 0;\n        for (int n : nums) {\n            counts[n]++;\n        }\n        for (auto const& [key, val] : counts) {\n            if (val == 2) {\n                res ^= key;\n            }\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar duplicateNumbersXOR = function(nums) {\n    const map = new Map();\n    let res = 0;\n    for (const n of nums) {\n        map.set(n, (map.get(n) || 0) + 1);\n    }\n    for (const [key, val] of map) {\n        if (val === 2) {\n            res ^= key;\n        }\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function duplicateNumbersXOR(nums: number[]): number {\n    const map = new Map&lt;number, number&gt;();\n    let res = 0;\n    for (const n of nums) {\n        map.set(n, (map.get(n) || 0) + 1);\n    }\n    for (const [key, val] of map) {\n        if (val === 2) {\n            res ^= key;\n        }\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Uses extra space proportional to the number of unique elements.

---
## Sorting

**Intuition**
If we sort the array, duplicate numbers will be adjacent to each other. We can then iterate through the sorted array and check if `nums[i]` equals `nums[i+1]`.

**Steps**
- Sort the input array in ascending order.
- Initialize a result variable to 0 and an index `i` to 0.
- Iterate while `i &lt; nums.length - 1`.
- If `nums[i] == nums[i+1]`, XOR `nums[i]` with the result and increment `i` by 2 to skip the duplicate pair.
- Otherwise, increment `i` by 1.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def duplicateNumbersXOR(self, nums: List[int]) -&gt; int:\n        nums.sort()\n        res = 0\n        i = 0\n        n = len(nums)\n        while i &lt; n - 1:\n            if nums[i] == nums[i+1]:\n                res ^= nums[i]\n                i += 2\n            else:\n                i += 1\n        return res" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int duplicateNumbersXOR(int[] nums) {\n        Arrays.sort(nums);\n        int res = 0;\n        for (int i = 0; i &lt; nums.length - 1; ) {\n            if (nums[i] == nums[i + 1]) {\n                res ^= nums[i];\n                i += 2;\n            } else {\n                i++;\n            }\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int duplicateNumbersXOR(vector&lt;int&gt;& nums) {\n        sort(nums.begin(), nums.end());\n        int res = 0;\n        for (int i = 0; i &lt; nums.size() - 1; ) {\n            if (nums[i] == nums[i + 1]) {\n                res ^= nums[i];\n                i += 2;\n            } else {\n                i++;\n            }\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar duplicateNumbersXOR = function(nums) {\n    nums.sort((a, b) =&gt; a - b);\n    let res = 0;\n    for (let i = 0; i &lt; nums.length - 1; ) {\n        if (nums[i] === nums[i + 1]) {\n            res ^= nums[i];\n            i += 2;\n        } else {\n            i++;\n        }\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function duplicateNumbersXOR(nums: number[]): number {\n    nums.sort((a, b) =&gt; a - b);\n    let res = 0;\n    for (let i = 0; i &lt; nums.length - 1; ) {\n        if (nums[i] === nums[i + 1]) {\n            res ^= nums[i];\n            i += 2;\n        } else {\n            i++;\n        }\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(1) or O(n) depending on the sorting algorithm's space complexity.
- Notes: Modifies the input array.

---
## Fixed Array Counting

**Intuition**
The problem constraints state that `1 &lt;= nums[i] &lt;= 50`. Since the range of possible values is very small and fixed, we can use an array of size 51 to count frequencies instead of a hash map. This is often faster and more memory-efficient for small ranges.

**Steps**
- Initialize an integer array `count` of size 51 with all zeros.
- Iterate through `nums`, incrementing `count[num]` for each element.
- Initialize a result variable to 0.
- Iterate from 1 to 50. If `count[i] == 2`, XOR `i` with the result.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def duplicateNumbersXOR(self, nums: List[int]) -&gt; int:\n        count = [0] * 51\n        for n in nums:\n            count[n] += 1\n        res = 0\n        for i in range(1, 51):\n            if count[i] == 2:\n                res ^= i\n        return res" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int duplicateNumbersXOR(int[] nums) {\n        int[] count = new int[51];\n        for (int n : nums) {\n            count[n]++;\n        }\n        int res = 0;\n        for (int i = 1; i &lt;= 50; i++) {\n            if (count[i] == 2) {\n                res ^= i;\n            }\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int duplicateNumbersXOR(vector&lt;int&gt;& nums) {\n        int count[51] = {0};\n        for (int n : nums) {\n            count[n]++;\n        }\n        int res = 0;\n        for (int i = 1; i &lt;= 50; i++) {\n            if (count[i] == 2) {\n                res ^= i;\n            }\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar duplicateNumbersXOR = function(nums) {\n    const count = new Array(51).fill(0);\n    for (const n of nums) {\n        count[n]++;\n    }\n    let res = 0;\n    for (let i = 1; i &lt;= 50; i++) {\n        if (count[i] === 2) {\n            res ^= i;\n        }\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function duplicateNumbersXOR(nums: number[]): number {\n    const count: number[] = new Array(51).fill(0);\n    for (const n of nums) {\n        count[n]++;\n    }\n    let res = 0;\n    for (let i = 1; i &lt;= 50; i++) {\n        if (count[i] === 2) {\n            res ^= i;\n        }\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1) (The array size is fixed at 51 regardless of input size).
- Notes: Optimal approach given the specific constraints.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find the XOR of Numbers Which Appear Twice Solutions","datePublished":"2025-05-04","description":"You are given an integer array nums. Return the XOR of all the numbers that appear exactly twice in the array.","keywords":"LeetCode, Find the XOR of Numbers Which Appear Twice, Easy, Array, Hash Table, Bit Manipulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Map approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of the Fixed Array approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because the counting array size is fixed at 51."}},{"@type":"Question","name":"Why is the Fixed Array approach efficient here?","acceptedAnswer":{"@type":"Answer","text":"It is efficient because the constraints limit the values to 1-50, allowing direct indexing without hash collisions."}}]}).replace(/</g,'\u003c')}
</script>