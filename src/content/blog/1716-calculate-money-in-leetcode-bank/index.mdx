---
title: "Calculate Money in Leetcode Bank"
summary: "Calculate total money saved after n days with increasing daily deposits that reset weekly."
date: "Jan 10, 2024"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.40% | **Paid:** No
> **Topics:** Math

Hercy wants to save money for his first car. He puts money in the Leetcode bank every day.

He starts by putting in $1 on Monday, the first day. Every day from Tuesday to Sunday, he will put in $1 more than the day before. On every subsequent Monday, he will put in $1 more than the previous Monday.

Given n, return the total amount of money he will have in the Leetcode bank at the end of the n-th day.

- Examples
- Constraints
- [Simulation Approach](#simulation-approach)
- [Mathematical Formula](#mathematical-formula)

## Examples

**Example 1:**

```text
Input: n = 4
Output: 10
Explanation: After the 4th day, the total is 1 + 2 + 3 + 4 = 10.
```

**Example 2:**

```text
Input: n = 10
Output: 37
Explanation: After the 10th day, the total is (1 + 2 + 3 + 4 + 5 + 6 + 7) + (2 + 3 + 4) = 37. Notice that on the 2nd Monday, Hercy puts in only $2.
```

**Example 3:**

```text
Input: n = 20
Output: 96
Explanation: After the 20th day, the total is (1 + 2 + 3 + 4 + 5 + 6 + 7) + (2 + 3 + 4 + 5 + 6 + 7 + 8) + (3 + 4 + 5 + 6 + 7 + 8) = 96.
```

## Constraints

```text
1 <= n <= 1000
```

---

## Simulation Approach

**Intuition**
Iterate through each day, calculate the amount saved on that day based on the week and day of week, and accumulate the total.

**Steps**
- Initialize total to 0
- For each day from 1 to n:
  - Calculate the week number (0-indexed) as (day - 1) // 7
  - Calculate the day of week (0-indexed) as (day - 1) % 7
  - Amount saved = week + day_of_week + 1
  - Add to total
- Return total

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def totalMoney(self, n: int) -> int:
        total = 0
        for day in range(1, n + 1):
            week = (day - 1) // 7
            day_of_week = (day - 1) % 7
            total += week + day_of_week + 1
        return total
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int totalMoney(int n) {
        int total = 0;
        for (int day = 1; day &lt;= n; day++) {
            int week = (day - 1) / 7;
            int dayOfWeek = (day - 1) % 7;
            total += week + dayOfWeek + 1;
        }
        return total;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int totalMoney(int n) {
        int total = 0;
        for (int day = 1; day &lt;= n; day++) {
            int week = (day - 1) / 7;
            int dayOfWeek = (day - 1) % 7;
            total += week + dayOfWeek + 1;
        }
        return total;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var totalMoney = function(n) {
    let total = 0;
    for (let day = 1; day &lt;= n; day++) {
        const week = Math.floor((day - 1) / 7);
        const dayOfWeek = (day - 1) % 7;
        total += week + dayOfWeek + 1;
    }
    return total;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function totalMoney(n: number): number {
    let total = 0;
    for (let day = 1; day &lt;= n; day++) {
        const week = Math.floor((day - 1) / 7);
        const dayOfWeek = (day - 1) % 7;
        total += week + dayOfWeek + 1;
    }
    return total;
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Simple and intuitive, but not optimal for very large n values.

---

## Mathematical Formula

**Intuition**
Use arithmetic series formulas to calculate the sum of complete weeks and remaining days separately, avoiding iteration.

**Steps**
- Calculate complete weeks w = n // 7 and remaining days r = n % 7
- Sum of complete weeks = 28w + 7 × w × (w - 1) / 2
- Sum of remaining days = r × w + r × (r + 1) / 2
- Return the sum of both parts

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def totalMoney(self, n: int) -> int:
        w = n // 7
        r = n % 7
        complete_weeks = 28 * w + 7 * w * (w - 1) // 2
        remaining_days = r * w + r * (r + 1) // 2
        return complete_weeks + remaining_days
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int totalMoney(int n) {
        int w = n / 7;
        int r = n % 7;
        int completeWeeks = 28 * w + 7 * w * (w - 1) / 2;
        int remainingDays = r * w + r * (r + 1) / 2;
        return completeWeeks + remainingDays;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int totalMoney(int n) {
        int w = n / 7;
        int r = n % 7;
        int completeWeeks = 28 * w + 7 * w * (w - 1) / 2;
        int remainingDays = r * w + r * (r + 1) / 2;
        return completeWeeks + remainingDays;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var totalMoney = function(n) {
    const w = Math.floor(n / 7);
    const r = n % 7;
    const completeWeeks = 28 * w + 7 * w * (w - 1) / 2;
    const remainingDays = r * w + r * (r + 1) / 2;
    return completeWeeks + remainingDays;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function totalMoney(n: number): number {
    const w = Math.floor(n / 7);
    const r = n % 7;
    const completeWeeks = 28 * w + 7 * w * (w - 1) / 2;
    const remainingDays = r * w + r * (r + 1) / 2;
    return completeWeeks + remainingDays;
}
` },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Optimal solution with constant time complexity using mathematical derivation.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Calculate Money in Leetcode Bank Solutions","datePublished":"2024-01-10","description":"Calculate total money saved after n days with increasing daily deposits that reset weekly.","keywords":"LeetCode, Calculate Money in Leetcode Bank, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The simulation approach has O(n) time complexity as it iterates through each day."}},{"@type":"Question","name":"What is the time complexity of the mathematical formula approach?","acceptedAnswer":{"@type":"Answer","text":"The mathematical formula approach has O(1) time complexity as it uses direct calculation."}},{"@type":"Question","name":"What is the space complexity of both approaches?","acceptedAnswer":{"@type":"Answer","text":"Both approaches have O(1) space complexity as they only use a constant amount of extra space."}}]}).replace(/</g,'\u003c')}
</script>