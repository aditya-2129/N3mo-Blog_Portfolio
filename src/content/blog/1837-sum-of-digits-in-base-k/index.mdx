---
title: "Sum of Digits in Base K"
summary: "Given an integer n in base 10 and a base k, return the sum of digits of n after converting to base k."
date: "Jun 04, 2025"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 78.70% | **Paid:** No
> **Topics:** Math

Given an integer n (in base 10) and a base k, return the sum of the digits of n after converting n from base 10 to base k.

## Examples

### Example 1:
```text
Input: n = 34, k = 6
Output: 9
Explanation: 34 (base 10) expressed in base 6 is 54. 5 + 4 = 9.
```

### Example 2:
```text
Input: n = 10, k = 10
Output: 1
Explanation: n is already in base 10. 1 + 0 = 1.
```

## Constraints

```text
1 <= n <= 100
2 <= k <= 10
```

## Table of Contents

- Examples
- Constraints
- [Repeated Division](#repeated-division)
- [Recursive Approach](#recursive-approach)
- [String Conversion](#string-conversion)

---
## Repeated Division

**Intuition**
The standard method for base conversion involves repeatedly dividing the number by the target base and collecting remainders, which represent the digits in the new base.

**Steps**
- Initialize sum to 0
- While n is greater than 0, get the remainder when divided by k
- Add the remainder to the sum
- Divide n by k (integer division)
- Return the sum

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def sumBase(self, n: int, k: int) -> int:
        total = 0
        while n &gt; 0:
            total += n % k
            n //= k
        return total` },
{ name: "Java", lang: "java", code: `class Solution {
    public int sumBase(int n, int k) {
        int total = 0;
        while (n &gt; 0) {
            total += n % k;
            n /= k;
        }
        return total;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int sumBase(int n, int k) {
        int total = 0;
        while (n &gt; 0) {
            total += n % k;
            n /= k;
        }
        return total;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    sumBase(n, k) {
        let total = 0;
        while (n &gt; 0) {
            total += n % k;
            n = Math.floor(n / k);
        }
        return total;
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    sumBase(n: number, k: number): number {
        let total = 0;
        while (n &gt; 0) {
            total += n % k;
            n = Math.floor(n / k);
        }
        return total;
    }
}` },]} />

**Complexity**
- Time: O(logₖ(n)) - number of digits in base k
- Space: O(1) - only using a constant amount of extra space
- Notes: This is the most efficient approach with minimal space usage

---
## Recursive Approach

**Intuition**
We can use recursion to process each digit by taking the remainder and recursively processing the quotient.

**Steps**
- Base case: if n is 0, return 0
- Return the remainder (n % k) plus the recursive call on n // k

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def sumBase(self, n: int, k: int) -> int:
        if n == 0:
            return 0
        return (n % k) + self.sumBase(n // k, k)` },
{ name: "Java", lang: "java", code: `class Solution {
    public int sumBase(int n, int k) {
        if (n == 0) {
            return 0;
        }
        return (n % k) + sumBase(n / k, k);
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int sumBase(int n, int k) {
        if (n == 0) {
            return 0;
        }
        return (n % k) + sumBase(n / k, k);
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    sumBase(n, k) {
        if (n === 0) {
            return 0;
        }
        return (n % k) + this.sumBase(Math.floor(n / k), k);
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    sumBase(n: number, k: number): number {
        if (n === 0) {
            return 0;
        }
        return (n % k) + this.sumBase(Math.floor(n / k), k);
    }
}` },]} />

**Complexity**
- Time: O(logₖ(n)) - number of recursive calls equals number of digits
- Space: O(logₖ(n)) - recursion stack depth equals number of digits
- Notes: Elegant but uses more stack space than iterative approach

---
## String Conversion

**Intuition**
Convert the number to its string representation in base k, then iterate through characters to sum their numeric values.

**Steps**
- Build the base k representation string by repeatedly getting remainders
- Convert each character back to its numeric value
- Sum all values

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def sumBase(self, n: int, k: int) -> int:
        result = ""
        while n &gt; 0:
            result = str(n % k) + result
            n //= k
        return sum(int(c) for c in result)` },
{ name: "Java", lang: "java", code: `class Solution {
    public int sumBase(int n, int k) {
        StringBuilder sb = new StringBuilder();
        while (n &gt; 0) {
            sb.insert(0, n % k);
            n /= k;
        }
        int total = 0;
        for (int i = 0; i &lt; sb.length(); i++) {
            total += sb.charAt(i) - '0';
        }
        return total;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int sumBase(int n, int k) {
        string result = "";
        while (n &gt; 0) {
            result = char('0' + n % k) + result;
            n /= k;
        }
        int total = 0;
        for (char c : result) {
            total += c - '0';
        }
        return total;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    sumBase(n, k) {
        let result = "";
        while (n &gt; 0) {
            result = (n % k) + result;
            n = Math.floor(n / k);
        }
        let total = 0;
        for (let c of result) {
            total += parseInt(c);
        }
        return total;
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    sumBase(n: number, k: number): number {
        let result = "";
        while (n &gt; 0) {
            result = (n % k) + result;
            n = Math.floor(n / k);
        }
        let total = 0;
        for (let c of result) {
            total += parseInt(c);
        }
        return total;
    }
}` },]} />

**Complexity**
- Time: O(logₖ(n)) - building string and summing digits
- Space: O(logₖ(n)) - storing the string representation
- Notes: Less efficient due to string operations but demonstrates the concept clearly

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Sum of Digits in Base K Solutions","datePublished":"2025-06-04","description":"Given an integer n in base 10 and a base k, return the sum of digits of n after converting to base k.","keywords":"LeetCode, Sum of Digits in Base K, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Repeated Division approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(logₖ(n)) where n is the input number and k is the base, as we process each digit in the base k representation."}},{"@type":"Question","name":"What is the space complexity of the Repeated Division approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for the sum variable."}},{"@type":"Question","name":"What is the time complexity of the Recursive approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(logₖ(n)) as we make one recursive call per digit in the base k representation."}},{"@type":"Question","name":"What is the space complexity of the Recursive approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(logₖ(n)) due to the recursion stack depth equal to the number of digits."}},{"@type":"Question","name":"What is the time complexity of the String Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(logₖ(n)) for building the string and summing the digits, with higher constant factors due to string operations."}},{"@type":"Question","name":"What is the space complexity of the String Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(logₖ(n)) as we store the string representation of the number in base k."}}]}).replace(/</g,'\u003c')}
</script>