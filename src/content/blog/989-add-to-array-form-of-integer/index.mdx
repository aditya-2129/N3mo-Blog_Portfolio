---
title: "Add to Array-Form of Integer"
summary: "The array-form of an integer is an array representing its digits. Add an integer k to the array-form of an integer num."
date: "Mar 25, 2024"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 45.50% | **Paid:** No
> **Topics:** Array, Math

The array-form of an integer num is an array representing its digits in left-to-right order.

*   For example, for num = 1321, the array form is [1,3,2,1].

Given num, the array-form of an integer, and an integer k, return the array-form of the integer num + k.

---

- Examples
- Constraints
- [Elementary Addition (Iterative with Carry)](#elementary-addition-iterative-with-carry)
- [Reverse and Add](#reverse-and-add)

## Examples

**Example 1:**

```text
Input: num = [1,2,0,0], k = 34
Output: [1,2,3,4]
Explanation: 1200 + 34 = 1234
```

**Example 2:**

```text
Input: num = [2,1,5], k = 806
Output: [1,0,2,1]
Explanation: 215 + 806 = 1021
```

**Example 3:**

```text
Input: num = [0], k = 10000
Output: [1,0,0,0,1]
Explanation: 0 + 10000 = 10000
```

## Constraints

```text
1 <= num.length <= 10^4
0 <= num[i] <= 9
num does not contain any leading zeros, except for the number 0 itself.
0 <= k <= 10^4
```

---

## Elementary Addition (Iterative with Carry)

**Intuition**
We can simulate the process of adding numbers digit by digit, starting from the least significant digit (the end of the array) to the most significant digit (the start). We maintain a carry value to handle sums greater than 9.

**Steps**
- Initialize an empty list `res` to store the result, `i` to the last index of `num`, and `carry` to 0.
- Loop while `i &gt;= 0` or `k &gt; 0` or `carry &gt; 0`.
- In each iteration, take the current digit from `num` (if available) and the last digit of `k` (`k % 10`).
- Calculate the sum of the two digits and the carry.
- Update `carry` to `sum / 10` and append `sum % 10` to `res`.
- Decrement `i` and update `k` to `k / 10`.
- Finally, reverse `res` to get the correct order.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def addToArrayForm(self, num: list[int], k: int) -&gt; list[int]:\n        res = []\n        i = len(num) - 1\n        carry = 0\n        \n        while i &gt;= 0 or k &gt; 0:\n            x = num[i] if i &gt;= 0 else 0\n            y = k % 10\n            \n            total = x + y + carry\n            carry = total // 10\n            res.append(total % 10)\n            \n            i -= 1\n            k //= 10\n            \n        if carry:\n            res.append(carry)\n            \n        return res[::-1]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public List&lt;Integer&gt; addToArrayForm(int[] num, int k) {\n        List&lt;Integer&gt; res = new ArrayList&lt;&gt;();\n        int i = num.length - 1;\n        int carry = 0;\n        \n        while (i &gt;= 0 || k &gt; 0) {\n            int x = (i &gt;= 0) ? num[i] : 0;\n            int y = k % 10;\n            \n            int sum = x + y + carry;\n            carry = sum / 10;\n            res.add(sum % 10);\n            \n            i--;\n            k /= 10;\n        }\n        \n        if (carry &gt; 0) {\n            res.add(carry);\n        }\n        \n        Collections.reverse(res);\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; addToArrayForm(vector&lt;int&gt;& num, int k) {\n        vector&lt;int&gt; res;\n        int i = num.size() - 1;\n        int carry = 0;\n        \n        while (i &gt;= 0 || k &gt; 0) {\n            int x = (i &gt;= 0) ? num[i] : 0;\n            int y = k % 10;\n            \n            int sum = x + y + carry;\n            carry = sum / 10;\n            res.push_back(sum % 10);\n            \n            i--;\n            k /= 10;\n        }\n        \n        if (carry &gt; 0) {\n            res.push_back(carry);\n        }\n        \n        reverse(res.begin(), res.end());\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var addToArrayForm = function(num, k) {\n    let res = [];\n    let i = num.length - 1;\n    let carry = 0;\n    \n    while (i &gt;= 0 || k &gt; 0) {\n        let x = (i &gt;= 0) ? num[i] : 0;\n        let y = k % 10;\n        \n        let sum = x + y + carry;\n        carry = Math.floor(sum / 10);\n        res.push(sum % 10);\n        \n        i--;\n        k = Math.floor(k / 10);\n    }\n    \n    if (carry &gt; 0) {\n        res.push(carry);\n    }\n    \n    return res.reverse();\n};" },
{ name: "TypeScript", lang: "typescript", code: "function addToArrayForm(num: number[], k: number): number[] {\n    let res: number[] = [];\n    let i = num.length - 1;\n    let carry = 0;\n    \n    while (i &gt;= 0 || k &gt; 0) {\n        let x = (i &gt;= 0) ? num[i] : 0;\n        let y = k % 10;\n        \n        let sum = x + y + carry;\n        carry = Math.floor(sum / 10);\n        res.push(sum % 10);\n        \n        i--;\n        k = Math.floor(k / 10);\n    }\n    \n    if (carry &gt; 0) {\n        res.push(carry);\n    }\n    \n    return res.reverse();\n}" }]} />

**Complexity**
- Time: O(max(N, log K)) where N is the length of `num`. We iterate through the digits of `num` and `k`.
- Space: O(max(N, log K)) to store the result.
- Notes: This is the most efficient approach as it avoids string conversions and handles large numbers naturally.

## Reverse and Add

**Intuition**
By reversing the array, we can process the least significant digits at index 0, simplifying the indexing logic. We add the digits of `k` to the reversed array, handle carries, and then reverse the result back.

**Steps**
- Reverse the input array `num`.
- Iterate through the array while `k &gt; 0`, adding `k % 10` to the current element.
- Handle carry by propagating it to the next element or pushing a new element if the end of the array is reached.
- If `k` still has digits after processing the array, push the remaining digits of `k` to the array.
- Reverse the array back to its original form.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def addToArrayForm(self, num: list[int], k: int) -&gt; list[int]:\n        num.reverse()\n        i = 0\n        \n        while k &gt; 0:\n            digit = k % 10\n            if i &lt; len(num):\n                num[i] += digit\n                carry = num[i] // 10\n                num[i] %= 10\n                k = k // 10 + carry\n            else:\n                num.append(digit)\n                k //= 10\n            i += 1\n            \n        num.reverse()\n        return num" },
{ name: "Java", lang: "java", code: "class Solution {\n    public List&lt;Integer&gt; addToArrayForm(int[] num, int k) {\n        List&lt;Integer&gt; list = new ArrayList&lt;&gt;();\n        for (int n : num) list.add(n);\n        Collections.reverse(list);\n        \n        int i = 0;\n        while (k &gt; 0) {\n            int digit = k % 10;\n            if (i &lt; list.size()) {\n                int sum = list.get(i) + digit;\n                list.set(i, sum % 10);\n                k = k / 10 + sum / 10;\n            } else {\n                list.add(digit);\n                k /= 10;\n            }\n            i++;\n        }\n        \n        Collections.reverse(list);\n        return list;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; addToArrayForm(vector&lt;int&gt;& num, int k) {\n        reverse(num.begin(), num.end());\n        \n        for (int i = 0; i &lt; num.size(); ++i) {\n            if (k == 0) break;\n            int sum = num[i] + k % 10;\n            num[i] = sum % 10;\n            k = k / 10 + sum / 10;\n        }\n        \n        while (k &gt; 0) {\n            num.push_back(k % 10);\n            k /= 10;\n        }\n        \n        reverse(num.begin(), num.end());\n        return num;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var addToArrayForm = function(num, k) {\n    num.reverse();\n    \n    for (let i = 0; i &lt; num.length; i++) {\n        if (k === 0) break;\n        let sum = num[i] + (k % 10);\n        num[i] = sum % 10;\n        k = Math.floor(k / 10) + Math.floor(sum / 10);\n    }\n    \n    while (k &gt; 0) {\n        num.push(k % 10);\n        k = Math.floor(k / 10);\n    }\n    \n    return num.reverse();\n};" },
{ name: "TypeScript", lang: "typescript", code: "function addToArrayForm(num: number[], k: number): number[] {\n    num.reverse();\n    \n    for (let i = 0; i &lt; num.length; i++) {\n        if (k === 0) break;\n        let sum = num[i] + (k % 10);\n        num[i] = sum % 10;\n        k = Math.floor(k / 10) + Math.floor(sum / 10);\n    }\n    \n    while (k &gt; 0) {\n        num.push(k % 10);\n        k = Math.floor(k / 10);\n    }\n    \n    return num.reverse();\n}" }]} />

**Complexity**
- Time: O(N + log K) for the reversals and the addition loop.
- Space: O(1) extra space if modifying in-place (excluding the space for the output list itself).
- Notes: Modifying the input in-place can be slightly more efficient in space but requires reversing the array twice.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Add to Array-Form of Integer Solutions","datePublished":"2024-03-25","description":"The array-form of an integer is an array representing its digits. Add an integer k to the array-form of an integer num.","keywords":"LeetCode, Add to Array-Form of Integer, Easy, Array, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Elementary Addition approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(max(N, log K)), where N is the length of the array and K is the integer."}},{"@type":"Question","name":"What is the space complexity of the Elementary Addition approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(max(N, log K)) to store the result list."}},{"@type":"Question","name":"Why do we need to handle carry in these solutions?","acceptedAnswer":{"@type":"Answer","text":"Carry is required because the sum of two digits can be greater than 9, which needs to be added to the next significant digit."}}]}).replace(/</g,'\u003c')}
</script>