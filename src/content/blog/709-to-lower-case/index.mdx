---
title: "To Lower Case"
summary: "Given a string s, return the string after replacing every uppercase letter with the same lowercase letter."
date: "Jun 30, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 84.80% | **Paid:** No
> **Topics:** String

Given a string s, return the string after replacing every uppercase letter with the same lowercase letter.

- Examples
- Constraints
- [Approach 1: Built-in Functions](#approach-1-built-in-functions)
- [Approach 2: ASCII Manipulation](#approach-2-ascii-manipulation)
- [Approach 3: Lookup Table](#approach-3-lookup-table)

## Examples

### Example 1

**Input:**
```text
s = "Hello"
```

**Output:**
```text
"hello"
```

### Example 2

**Input:**
```text
s = "here"
```

**Output:**
```text
"here"
```

### Example 3

**Input:**
```text
s = "LOVELY"
```

**Output:**
```text
"lovely"
```

## Constraints

```text
1 <= s.length <= 100
s consists of printable ASCII characters.
```

---
## Approach 1: Built-in Functions

**Intuition**
Most programming languages provide standard library methods to convert strings to lowercase, which handles the logic internally.

**Steps**
- Call the language-specific lowercase method on the input string.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def toLowerCase(self, s: str) -&gt; str:\n        return s.lower()" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String toLowerCase(String s) {\n        return s.toLowerCase();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;cctype&gt;\n#include &lt;string&gt;\n\nclass Solution {\npublic:\n    string toLowerCase(string s) {\n        std::transform(s.begin(), s.end(), s.begin(), \n                   [](unsigned char c){ return std::tolower(c); });\n        return s;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar toLowerCase = function(s) {\n    return s.toLowerCase();\n};" },
{ name: "TypeScript", lang: "typescript", code: "function toLowerCase(s: string): string {\n    return s.toLowerCase();\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Strings are immutable in many languages, so a new string is allocated.

---
## Approach 2: ASCII Manipulation

**Intuition**
Uppercase letters 'A' through 'Z' have ASCII values from 65 to 90, while lowercase 'a' through 'z' range from 97 to 122. The difference is 32. We can iterate through the string and add 32 to any character that falls within the uppercase range.

**Steps**
- Initialize an empty result string or builder.
- Iterate through each character in the input string.
- If the character's ASCII value is between 65 ('A') and 90 ('Z'), add 32 to it to get the lowercase version.
- Append the character (converted or original) to the result.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def toLowerCase(self, s: str) -&gt; str:\n        res = []\n        for ch in s:\n            if 'A' &lt;= ch &lt;= 'Z':\n                res.append(chr(ord(ch) + 32))\n            else:\n                res.append(ch)\n        return ''.join(res)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String toLowerCase(String s) {\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i &lt; s.length(); i++) {\n            char c = s.charAt(i);\n            if (c &gt;= 'A' && c &lt;= 'Z') {\n                sb.append((char)(c + 32));\n            } else {\n                sb.append(c);\n            }\n        }\n        return sb.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\n\nclass Solution {\npublic:\n    string toLowerCase(string s) {\n        for (char &c : s) {\n            if (c &gt;= 'A' && c &lt;= 'Z') {\n                c += 32;\n            }\n        }\n        return s;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar toLowerCase = function(s) {\n    let res = '';\n    for (let i = 0; i &lt; s.length; i++) {\n        let code = s.charCodeAt(i);\n        if (code &gt;= 65 && code &lt;= 90) {\n            res += String.fromCharCode(code + 32);\n        } else {\n            res += s[i];\n        }\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function toLowerCase(s: string): string {\n    let res = '';\n    for (let i = 0; i &lt; s.length; i++) {\n        let code = s.charCodeAt(i);\n        if (code &gt;= 65 && code &lt;= 90) {\n            res += String.fromCharCode(code + 32);\n        } else {\n            res += s[i];\n        }\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: This approach avoids the overhead of function calls found in built-in methods.

---
## Approach 3: Lookup Table

**Intuition**
We can precompute a mapping of all 128 ASCII characters to their lowercase equivalents. This allows for O(1) lookup per character during iteration.

**Steps**
- Create an array or map of size 128 (for standard ASCII).
- Populate the array such that index 'A' maps to 'a', 'B' to 'b', etc., and all other characters map to themselves.
- Iterate through the input string, looking up each character in the table to build the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def toLowerCase(self, s: str) -&gt; str:\n        mapping = {chr(i): chr(i + 32) for i in range(65, 91)}\n        return ''.join(mapping.get(ch, ch) for ch in s)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String toLowerCase(String s) {\n        char[] map = new char[128];\n        for (int i = 0; i &lt; 128; i++) {\n            map[i] = (char) i;\n        }\n        for (int i = 65; i &lt;= 90; i++) {\n            map[i] = (char) (i + 32);\n        }\n        \n        char[] res = new char[s.length()];\n        for (int i = 0; i &lt; s.length(); i++) {\n            res[i] = map[s.charAt(i)];\n        }\n        return new String(res);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\n\nclass Solution {\npublic:\n    string toLowerCase(string s) {\n        char map[128];\n        for (int i = 0; i &lt; 128; i++) map[i] = i;\n        for (int i = 'A'; i &lt;= 'Z'; i++) map[i] = i + 32;\n        \n        for (char &c : s) {\n            c = map[c];\n        }\n        return s;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar toLowerCase = function(s) {\n    const map = new Array(128);\n    for (let i = 0; i &lt; 128; i++) {\n        map[i] = String.fromCharCode(i);\n    }\n    for (let i = 65; i &lt;= 90; i++) {\n        map[i] = String.fromCharCode(i + 32);\n    }\n    \n    let res = '';\n    for (let i = 0; i &lt; s.length; i++) {\n        res += map[s.charCodeAt(i)];\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function toLowerCase(s: string): string {\n    const map: string[] = new Array(128);\n    for (let i = 0; i &lt; 128; i++) {\n        map[i] = String.fromCharCode(i);\n    }\n    for (let i = 65; i &lt;= 90; i++) {\n        map[i] = String.fromCharCode(i + 32);\n    }\n    \n    let res = '';\n    for (let i = 0; i &lt; s.length; i++) {\n        res += map[s.charCodeAt(i)];\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1) (The lookup table is fixed size 128, regardless of input n)
- Notes: Useful if the conversion logic is more complex than a simple offset.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode To Lower Case Solutions","datePublished":"2025-06-30","description":"Given a string s, return the string after replacing every uppercase letter with the same lowercase letter.","keywords":"LeetCode, To Lower Case, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Built-in Functions approach?","acceptedAnswer":{"@type":"Answer","text":"O(n), where n is the length of the string."}},{"@type":"Question","name":"What is the space complexity of the ASCII Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) to store the resulting string, as strings are immutable in most languages."}},{"@type":"Question","name":"How does the ASCII Manipulation approach work?","acceptedAnswer":{"@type":"Answer","text":"It iterates through the string and adds 32 to the ASCII value of any character between 'A' and 'Z' to convert it to lowercase."}}]}).replace(/</g,'\u003c')}
</script>