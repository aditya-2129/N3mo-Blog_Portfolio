---
title: "Defanging an IP Address"
summary: "Given a valid IP address, replace every '.' with '[.]' to defang it."
date: "Sep 07, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 90.00% | **Paid:** No
> **Topics:** String

Given a valid IPv4 address address, return a defanged version of that IP address.

A defanged IP address replaces every period "." with "[.]".

- Examples
- Constraints
- [Approach 1: Built-in String Replacement](#approach-1-built-in-string-replacement)
- [Approach 2: Iterative Construction](#approach-2-iterative-construction)
- [Approach 3: Regular Expressions](#approach-3-regular-expressions)

## Examples

```text
Input: address = "1.1.1.1"
Output: "1[.]1[.]1[.]1"
```

```text
Input: address = "255.100.50.0"
Output: "255[.]100[.]50[.]0"
```

## Constraints

```text
address contains only digits and '.'.
address is a valid IPv4 address.
```

---
## Approach 1: Built-in String Replacement

**Intuition**
Most modern programming languages provide built-in methods to replace all occurrences of a substring within a string. We can directly use these methods to replace every "." with "[.]".

**Steps**
- Call the built-in replace function on the input string.
- Pass "." as the target substring and "[.]" as the replacement substring.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def defangIPaddr(self, address: str) -&gt; str:\n        return address.replace('.', '[.]')" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String defangIPaddr(String address) {\n        return address.replace(\".\", \"[.]\");\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;regex&gt;\n\nclass Solution {\npublic:\n    string defangIPaddr(string address) {\n        return std::regex_replace(address, std::regex(\"\\\\.\"), \"[.]\");\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} address\n * @return {string}\n */\nvar defangIPaddr = function(address) {\n    return address.replaceAll('.', '[.]');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function defangIPaddr(address: string): string {\n    return address.replaceAll('.', '[.]');\n}" },]} />

**Complexity**
- Time: O(n) — The replace operation scans the string once.
- Space: O(n) — A new string is created to store the result.
- Notes: This is the most concise and readable solution in most languages.

---
## Approach 2: Iterative Construction

**Intuition**
We can iterate through the input string character by character. If we encounter a '.', we append '[.]' to our result; otherwise, we append the character itself.

**Steps**
- Initialize an empty result string (or a mutable builder).
- Loop through each character in the input string.
- If the character is '.', append '[.]' to the result.
- Else, append the character to the result.
- Return the constructed string.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def defangIPaddr(self, address: str) -&gt; str:\n        res = []\n        for c in address:\n            if c == '.':\n                res.append('[.]')\n            else:\n                res.append(c)\n        return ''.join(res)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String defangIPaddr(String address) {\n        StringBuilder sb = new StringBuilder();\n        for (char c : address.toCharArray()) {\n            if (c == '.') {\n                sb.append(\"[.]\");\n            } else {\n                sb.append(c);\n            }\n        }\n        return sb.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string defangIPaddr(string address) {\n        string res = \"\";\n        for (char c : address) {\n            if (c == '.') {\n                res += \"[.]\";\n            } else {\n                res += c;\n            }\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} address\n * @return {string}\n */\nvar defangIPaddr = function(address) {\n    let res = '';\n    for (let i = 0; i &lt; address.length; i++) {\n        if (address[i] === '.') {\n            res += '[.]';\n        } else {\n            res += address[i];\n        }\n    }\n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function defangIPaddr(address: string): string {\n    let res = '';\n    for (let i = 0; i &lt; address.length; i++) {\n        if (address[i] === '.') {\n            res += '[.]';\n        } else {\n            res += address[i];\n        }\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: O(n) — We iterate through the string once.
- Space: O(n) — We store the result in a new string/buffer.
- Notes: In languages like Java, using `StringBuilder` is more efficient than string concatenation in a loop.

---
## Approach 3: Regular Expressions

**Intuition**
We can use a regular expression to match all occurrences of the '.' character and replace them with the defanged string '[.]'. This is a powerful pattern-matching approach.

**Steps**
- Define a regex pattern that matches the literal dot character.
- Use the regex replace function to substitute all matches with '[.]'.
- Return the modified string.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import re\n\nclass Solution:\n    def defangIPaddr(self, address: str) -&gt; str:\n        return re.sub(r'\\.', '[.]', address)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String defangIPaddr(String address) {\n        return address.replaceAll(\"\\\\.\", \"[.]\");\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;regex&gt;\n\nclass Solution {\npublic:\n    string defangIPaddr(string address) {\n        return std::regex_replace(address, std::regex(\"\\\\.\"), \"[.]\");\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} address\n * @return {string}\n */\nvar defangIPaddr = function(address) {\n    return address.replace(/\\./g, '[.]');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function defangIPaddr(address: string): string {\n    return address.replace(/\\./g, '[.]');\n}" },]} />

**Complexity**
- Time: O(n) — The regex engine processes the string linearly.
- Space: O(n) — Space for the new string and regex overhead.
- Notes: While powerful, regex can be overkill for simple fixed-string replacements compared to built-in string methods.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Defanging an IP Address Solutions","datePublished":"2024-09-07","description":"Given a valid IP address, replace every '.' with '[.]' to defang it.","keywords":"LeetCode, Defanging an IP Address, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Built-in String Replacement?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string, as the replace operation scans the string once."}},{"@type":"Question","name":"What is the space complexity of Iterative Construction?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) because we create a new string to store the defanged IP address."}},{"@type":"Question","name":"When should you use Regular Expressions for this problem?","acceptedAnswer":{"@type":"Answer","text":"Regular expressions are generally overkill for simple fixed-string replacement but are useful if the pattern to replace is complex or dynamic."}}]}).replace(/</g,'\u003c')}
</script>