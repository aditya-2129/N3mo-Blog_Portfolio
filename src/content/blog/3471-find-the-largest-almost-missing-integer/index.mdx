---
title: "Find the Largest Almost Missing Integer"
summary: "Find the largest integer x not in nums such that x+1 is in nums."
date: "Dec 12, 2024"
tags:
  - Array
  - Hash Table
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 37.30% | **Paid:** No
> **Topics:** Array, Hash Table

You are given an integer array nums. An integer x is called almost missing if x is not present in nums, but x + 1 is present in nums. Return the largest almost missing integer. If no such integer exists, return -1.

- Examples
- Constraints
- [Hash Set](#hash-set)
- [Sorting](#sorting)

## Examples

Example 1:
```text
Input: nums = [1,2,3]
Output: 0
Explanation: 0 is not in nums, but 0 + 1 = 1 is in nums. No other integer larger than 0 satisfies the condition.
```

Example 2:
```text
Input: nums = [1,3,4]
Output: 2
Explanation: 2 is not in nums, but 2 + 1 = 3 is in nums. 0 is also almost missing, but 2 is larger.
```

Example 3:
```text
Input: nums = [5]
Output: 4
Explanation: 4 is not in nums, but 4 + 1 = 5 is in nums.
```

## Constraints

```text
1 <= nums.length <= 10⁵
-10⁹ <= nums[i] <= 10⁹
```

---
## Hash Set

**Intuition**
We can iterate through the unique numbers in the array. For every number `y` present in the array, we check if `y - 1` is missing. If `y - 1` is missing, then `x = y - 1` is an "almost missing" integer. We simply track the maximum such `x` found.

**Steps**
- Insert all elements of `nums` into a Hash Set for O(1) lookups.
- Initialize `ans` to -1.
- Iterate through each number `y` in the set:
  - If `y - 1` is not in the set, update `ans` with `max(ans, y - 1)`.
- Return `ans`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findLargestAlmostMissing(self, nums: List[int]) -&gt; int:\n        num_set = set(nums)\n        ans = -1\n        for y in num_set:\n            if y - 1 not in num_set:\n                ans = max(ans, y - 1)\n        return ans" },
{ name: "Java", lang: "java", code: "import java.util.HashSet;\nimport java.util.Set;\n\nclass Solution {\n    public int findLargestAlmostMissing(int[] nums) {\n        Set&lt;Integer&gt; set = new HashSet&lt;&gt;();\n        for (int num : nums) {\n            set.add(num);\n        }\n        int ans = -1;\n        for (int y : set) {\n            if (!set.contains(y - 1)) {\n                ans = Math.max(ans, y - 1);\n            }\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int findLargestAlmostMissing(vector&lt;int&gt;& nums) {\n        unordered_set&lt;int&gt; numSet(nums.begin(), nums.end());\n        int ans = -1;\n        for (int y : numSet) {\n            if (numSet.find(y - 1) == numSet.end()) {\n                ans = max(ans, y - 1);\n            }\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar findLargestAlmostMissing = function(nums) {\n    const numSet = new Set(nums);\n    let ans = -1;\n    for (const y of numSet) {\n        if (!numSet.has(y - 1)) {\n            ans = Math.max(ans, y - 1);\n        }\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findLargestAlmostMissing(nums: number[]): number {\n    const numSet = new Set(nums);\n    let ans = -1;\n    for (const y of numSet) {\n        if (!numSet.has(y - 1)) {\n            ans = Math.max(ans, y - 1);\n        }\n    }\n    return ans;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Uses O(n) extra space for the set.

---
## Sorting

**Intuition**
If we sort the array, we can identify gaps between consecutive numbers. If `nums[i] &gt; nums[i-1] + 1`, it means `nums[i] - 1` is missing from the array. Since `nums[i]` is present, `nums[i] - 1` is an almost missing integer. We also consider `nums[0] - 1` as a candidate.

**Steps**
- Sort the array `nums`.
- Initialize `ans` to `nums[0] - 1`.
- Iterate through the array starting from index 1:
  - If `nums[i] &gt; nums[i - 1] + 1`, update `ans` with `max(ans, nums[i] - 1)`.
- Return `ans`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findLargestAlmostMissing(self, nums: List[int]) -&gt; int:\n        nums.sort()\n        ans = nums[0] - 1\n        for i in range(1, len(nums)):\n            if nums[i] &gt; nums[i - 1] + 1:\n                ans = max(ans, nums[i] - 1)\n        return ans" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int findLargestAlmostMissing(int[] nums) {\n        Arrays.sort(nums);\n        int ans = nums[0] - 1;\n        for (int i = 1; i &lt; nums.length; i++) {\n            if (nums[i] &gt; nums[i - 1] + 1) {\n                ans = Math.max(ans, nums[i] - 1);\n            }\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int findLargestAlmostMissing(vector&lt;int&gt;& nums) {\n        sort(nums.begin(), nums.end());\n        int ans = nums[0] - 1;\n        for (int i = 1; i &lt; nums.size(); i++) {\n            if (nums[i] &gt; nums[i - 1] + 1) {\n                ans = max(ans, nums[i] - 1);\n            }\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar findLargestAlmostMissing = function(nums) {\n    nums.sort((a, b) =&gt; a - b);\n    let ans = nums[0] - 1;\n    for (let i = 1; i &lt; nums.length; i++) {\n        if (nums[i] &gt; nums[i - 1] + 1) {\n            ans = Math.max(ans, nums[i] - 1);\n        }\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findLargestAlmostMissing(nums: number[]): number {\n    nums.sort((a, b) =&gt; a - b);\n    let ans = nums[0] - 1;\n    for (let i = 1; i &lt; nums.length; i++) {\n        if (nums[i] &gt; nums[i - 1] + 1) {\n            ans = Math.max(ans, nums[i] - 1);\n        }\n    }\n    return ans;\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(1) or O(n) depending on the sorting algorithm.
- Notes: Slower than the Hash Set approach due to sorting, but uses less space if sorting is in-place.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find the Largest Almost Missing Integer Solutions","datePublished":"2024-12-12","description":"Find the largest integer x not in nums such that x+1 is in nums.","keywords":"LeetCode, Find the Largest Almost Missing Integer, Easy, Array, Hash Table"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) or O(n) depending on the sorting algorithm implementation."}}]}).replace(/</g,'\u003c')}
</script>