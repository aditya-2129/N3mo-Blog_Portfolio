---
title: "Traffic Signal Color"
summary: "Determine the color of a traffic signal after a given time, knowing it cycles Green -> Yellow -> Red -> Green every second."
date: "Nov 16, 2024"
tags:
  - Simulation
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 83.10% | **Paid:** No
> **Topics:** N/A

You are given a string currentRoad representing the current color of the traffic signal and an integer time representing the number of seconds that have passed. The traffic signal changes color every second in the following order: Green -> Yellow -> Red -> Green. Return the color of the traffic signal after time seconds.

- Examples
- Constraints
- [Simulation (Brute Force)](#simulation-brute-force)
- [Modulo Arithmetic (Optimal)](#modulo-arithmetic-optimal)

## Examples

### Example 1

**Input:**
```text
timer = 60
```

**Output:**
```text
"Red"
```

**Explanation:**
Since timer = 60, and 30 &lt; timer &lt;= 90, the answer is "Red".

### Example 2

**Input:**
```text
timer = 5
```

**Output:**
```text
"Invalid"
```

**Explanation:**
Since timer = 5, it does not satisfy any of the given conditions, the answer is "Invalid".

## Constraints

```text
- 0 <= timer <= 1000
```

---
## Simulation (Brute Force)

**Intuition**
We can simulate the passage of time second by second, updating the color of the traffic signal according to the fixed cycle Green -> Yellow -> Red -> Green.

**Steps**
- Initialize a variable to hold the current color.
- Loop from 0 to time - 1.
- In each iteration, update the current color to the next color in the sequence.
- Return the final color.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def trafficSignalColor(self, currentRoad: str, time: int) -&gt; str:\n        colors = [\"Green\", \"Yellow\", \"Red\"]\n        idx = colors.index(currentRoad)\n        for _ in range(time):\n            idx = (idx + 1) % 3\n        return colors[idx]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String trafficSignalColor(String currentRoad, int time) {\n        String[] colors = {\"Green\", \"Yellow\", \"Red\"};\n        int idx = 0;\n        if (currentRoad.equals(\"Green\")) idx = 0;\n        else if (currentRoad.equals(\"Yellow\")) idx = 1;\n        else idx = 2;\n        \n        for (int i = 0; i &lt; time; i++) {\n            idx = (idx + 1) % 3;\n        }\n        return colors[idx];\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string trafficSignalColor(string currentRoad, int time) {\n        vector&lt;string&gt; colors = {\"Green\", \"Yellow\", \"Red\"};\n        int idx = 0;\n        if (currentRoad == \"Green\") idx = 0;\n        else if (currentRoad == \"Yellow\") idx = 1;\n        else idx = 2;\n        \n        for (int i = 0; i &lt; time; i++) {\n            idx = (idx + 1) % 3;\n        }\n        return colors[idx];\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} currentRoad\n * @param {number} time\n * @return {string}\n */\nvar trafficSignalColor = function(currentRoad, time) {\n    const colors = [\"Green\", \"Yellow\", \"Red\"];\n    let idx = colors.indexOf(currentRoad);\n    for (let i = 0; i &lt; time; i++) {\n        idx = (idx + 1) % 3;\n    }\n    return colors[idx];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function trafficSignalColor(currentRoad: string, time: number): string {\n    const colors: string[] = [\"Green\", \"Yellow\", \"Red\"];\n    let idx: number = colors.indexOf(currentRoad);\n    for (let i = 0; i &lt; time; i++) {\n        idx = (idx + 1) % 3;\n    }\n    return colors[idx];\n}" },]} />

**Complexity**
- Time: O(time) - We iterate once for every unit of time.
- Space: O(1) - We only store a few variables.
- Notes: This approach will Time Limit Exceed (TLE) for large values of time (up to 10⁹).

---
## Modulo Arithmetic (Optimal)

**Intuition**
Since the traffic signal cycles every 3 seconds, we can use the modulo operator to find the final state without iterating through every second. We map the colors to indices 0, 1, and 2, add the time, and take modulo 3 to find the new index.

**Steps**
- Map the input string currentRoad to an integer index (0 for Green, 1 for Yellow, 2 for Red).
- Calculate the new index by adding time to the current index and taking the result modulo 3.
- Map the resulting index back to the corresponding color string and return it.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def trafficSignalColor(self, currentRoad: str, time: int) -&gt; str:\n        colors = [\"Green\", \"Yellow\", \"Red\"]\n        idx = colors.index(currentRoad)\n        new_idx = (idx + time) % 3\n        return colors[new_idx]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String trafficSignalColor(String currentRoad, int time) {\n        String[] colors = {\"Green\", \"Yellow\", \"Red\"};\n        int idx = 0;\n        if (currentRoad.equals(\"Green\")) idx = 0;\n        else if (currentRoad.equals(\"Yellow\")) idx = 1;\n        else idx = 2;\n        \n        int newIdx = (idx + time) % 3;\n        return colors[newIdx];\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string trafficSignalColor(string currentRoad, int time) {\n        vector&lt;string&gt; colors = {\"Green\", \"Yellow\", \"Red\"};\n        int idx = 0;\n        if (currentRoad == \"Green\") idx = 0;\n        else if (currentRoad == \"Yellow\") idx = 1;\n        else idx = 2;\n        \n        int newIdx = (idx + time) % 3;\n        return colors[newIdx];\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} currentRoad\n * @param {number} time\n * @return {string}\n */\nvar trafficSignalColor = function(currentRoad, time) {\n    const colors = [\"Green\", \"Yellow\", \"Red\"];\n    let idx = colors.indexOf(currentRoad);\n    let newIdx = (idx + time) % 3;\n    return colors[newIdx];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function trafficSignalColor(currentRoad: string, time: number): string {\n    const colors: string[] = [\"Green\", \"Yellow\", \"Red\"];\n    let idx: number = colors.indexOf(currentRoad);\n    let newIdx: number = (idx + time) % 3;\n    return colors[newIdx];\n}" },]} />

**Complexity**
- Time: O(1) - Mathematical calculation is performed in constant time.
- Space: O(1) - Only a fixed-size array and variables are used.
- Notes: This is the optimal solution as it handles the maximum constraint of time efficiently.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Traffic Signal Color Solutions","datePublished":"2024-11-16","description":"Determine the color of a traffic signal after a given time, knowing it cycles Green -> Yellow -> Red -> Green every second.","keywords":"LeetCode, Traffic Signal Color, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Modulo Arithmetic approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) because we perform a constant number of arithmetic operations regardless of the input size."}},{"@type":"Question","name":"Why does the Simulation approach fail for large inputs?","acceptedAnswer":{"@type":"Answer","text":"The Simulation approach has a time complexity of O(time). Since time can be up to 10^9, iterating that many times exceeds the time limit for execution."}},{"@type":"Question","name":"How is the cycle of the traffic signal represented?","acceptedAnswer":{"@type":"Answer","text":"The cycle is represented as an array or list of strings: [\"Green\", \"Yellow\", \"Red\"]. We use modulo 3 arithmetic to wrap around the array indices."}}]}).replace(/</g,'\u003c')}
</script>