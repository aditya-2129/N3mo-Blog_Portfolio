---
title: "Number of Beautiful Pairs"
summary: "Count pairs (i,j) where first digit of nums[i] and last digit of nums[j] are coprime (gcd=1)."
date: "Apr 08, 2026"
tags:
  - Array
  - Hash Table
  - Math
  - Counting
  - Number Theory
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 52.30% | **Paid:** No
> **Topics:** Array, Hash Table, Math, Counting, Number Theory

You are given a 0-indexed integer array nums. A pair of indices i, j is called beautiful if 0 &lt;= i &lt; j &lt; nums.length and the first digit of nums[i] and the last digit of nums[j] are coprime.

Return the total number of beautiful pairs in nums.

Two numbers are coprime if their greatest common divisor (gcd) is equal to 1.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Optimized Counting](#optimized-counting)

## Examples

### Example 1
```text
Input: nums = [2,5,1,4]
Output: 5
Explanation: There are 5 beautiful pairs:
- (0,1): first digit of 2 is 2, last digit of 5 is 5, gcd(2,5) = 1
- (0,2): first digit of 2 is 2, last digit of 1 is 1, gcd(2,1) = 1
- (1,2): first digit of 5 is 5, last digit of 1 is 1, gcd(5,1) = 1
- (1,3): first digit of 5 is 5, last digit of 4 is 4, gcd(5,4) = 1
- (2,3): first digit of 1 is 1, last digit of 4 is 4, gcd(1,4) = 1
```

### Example 2
```text
Input: nums = [11,21,12]
Output: 2
Explanation: There are 2 beautiful pairs:
- (0,1): first digit of 11 is 1, last digit of 21 is 1, gcd(1,1) = 1
- (0,2): first digit of 11 is 1, last digit of 12 is 2, gcd(1,2) = 1
```

## Constraints

```text
2 <= nums.length <= 100
1 <= nums[i] <= 9999
```

---
## Brute Force

**Intuition**
Check every possible pair (i, j) where i &lt; j, extract the first digit of nums[i] and last digit of nums[j], then verify if they are coprime using gcd.

**Steps**
- Iterate through all pairs (i, j) with i &lt; j
- For each pair, extract first digit of nums[i] by repeatedly dividing by 10
- Extract last digit of nums[j] using modulo 10
- Check if gcd of these two digits equals 1
- Count all such beautiful pairs

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countBeautifulPairs(self, nums: list[int]) -&gt; int:\n        def first_digit(n: int) -&gt; int:\n            while n &gt;= 10:\n                n //= 10\n            return n\n        \n        def last_digit(n: int) -&gt; int:\n            return n % 10\n        \n        def gcd(a: int, b: int) -&gt; int:\n            while b:\n                a, b = b, a % b\n            return a\n        \n        count = 0\n        n = len(nums)\n        for i in range(n):\n            for j in range(i + 1, n):\n                if gcd(first_digit(nums[i]), last_digit(nums[j])) == 1:\n                    count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int countBeautifulPairs(int[] nums) {\n        int count = 0;\n        int n = nums.length;\n        \n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int firstDigit = getFirstDigit(nums[i]);\n                int lastDigit = getLastDigit(nums[j]);\n                if (gcd(firstDigit, lastDigit) == 1) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n    \n    private int getFirstDigit(int n) {\n        while (n &gt;= 10) {\n            n /= 10;\n        }\n        return n;\n    }\n    \n    private int getLastDigit(int n) {\n        return n % 10;\n    }\n    \n    private int gcd(int a, int b) {\n        while (b != 0) {\n            int temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countBeautifulPairs(vector&lt;int&gt;& nums) {\n        int count = 0;\n        int n = nums.size();\n        \n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int firstDigit = getFirstDigit(nums[i]);\n                int lastDigit = getLastDigit(nums[j]);\n                if (gcd(firstDigit, lastDigit) == 1) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n    \nprivate:\n    int getFirstDigit(int n) {\n        while (n &gt;= 10) {\n            n /= 10;\n        }\n        return n;\n    }\n    \n    int getLastDigit(int n) {\n        return n % 10;\n    }\n    \n    int gcd(int a, int b) {\n        while (b != 0) {\n            int temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar countBeautifulPairs = function(nums) {\n    const getFirstDigit = (n) =&gt; {\n        while (n &gt;= 10) {\n            n = Math.floor(n / 10);\n        }\n        return n;\n    };\n    \n    const getLastDigit = (n) =&gt; n % 10;\n    \n    const gcd = (a, b) =&gt; {\n        while (b !== 0) {\n            const temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    };\n    \n    let count = 0;\n    const n = nums.length;\n    \n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            if (gcd(getFirstDigit(nums[i]), getLastDigit(nums[j])) === 1) {\n                count++;\n            }\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countBeautifulPairs(nums: number[]): number {\n    const getFirstDigit = (n: number): number =&gt; {\n        while (n &gt;= 10) {\n            n = Math.floor(n / 10);\n        }\n        return n;\n    };\n    \n    const getLastDigit = (n: number): number =&gt; n % 10;\n    \n    const gcd = (a: number, b: number): number =&gt; {\n        while (b !== 0) {\n            const temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    };\n    \n    let count = 0;\n    const n = nums.length;\n    \n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            if (gcd(getFirstDigit(nums[i]), getLastDigit(nums[j])) === 1) {\n                count++;\n            }\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n² × log(max(nums))) for extracting digits and computing gcd
- Space: O(1)
- Notes: Simple but inefficient for large arrays. Works within constraints since n ≤ 100.

---
## Optimized Counting

**Intuition**
Since there are only 10 possible digits (0-9), we can precompute coprime relationships and use a frequency array to count first digits seen so far, avoiding the nested loop.

**Steps**
- Precompute a 10×10 matrix marking which digit pairs are coprime
- Maintain a frequency array counting occurrences of each first digit
- For each number, get its last digit and sum up counts of first digits that are coprime with it
- Add the current number's first digit to the frequency array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countBeautifulPairs(self, nums: list[int]) -&gt; int:\n        def first_digit(n: int) -&gt; int:\n            while n &gt;= 10:\n                n //= 10\n            return n\n        \n        def last_digit(n: int) -&gt; int:\n            return n % 10\n        \n        def gcd(a: int, b: int) -&gt; int:\n            while b:\n                a, b = b, a % b\n            return a\n        \n        # Precompute coprime matrix for digits 0-9\n        coprime = [[False] * 10 for _ in range(10)]\n        for i in range(10):\n            for j in range(10):\n                if gcd(i, j) == 1:\n                    coprime[i][j] = True\n        \n        count = 0\n        first_digit_count = [0] * 10\n        \n        for num in nums:\n            ld = last_digit(num)\n            # Count first digits seen so far that are coprime with ld\n            for fd in range(10):\n                if coprime[fd][ld]:\n                    count += first_digit_count[fd]\n            # Add first digit of current number\n            fd = first_digit(num)\n            first_digit_count[fd] += 1\n        \n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int countBeautifulPairs(int[] nums) {\n        // Precompute coprime matrix for digits 0-9\n        boolean[][] coprime = new boolean[10][10];\n        for (int i = 0; i &lt; 10; i++) {\n            for (int j = 0; j &lt; 10; j++) {\n                coprime[i][j] = gcd(i, j) == 1;\n            }\n        }\n        \n        int count = 0;\n        int[] firstDigitCount = new int[10];\n        \n        for (int num : nums) {\n            int lastDigit = num % 10;\n            // Count first digits seen so far that are coprime with lastDigit\n            for (int fd = 0; fd &lt; 10; fd++) {\n                if (coprime[fd][lastDigit]) {\n                    count += firstDigitCount[fd];\n                }\n            }\n            // Add first digit of current number\n            int firstDigit = getFirstDigit(num);\n            firstDigitCount[firstDigit]++;\n        }\n        return count;\n    }\n    \n    private int getFirstDigit(int n) {\n        while (n &gt;= 10) {\n            n /= 10;\n        }\n        return n;\n    }\n    \n    private int gcd(int a, int b) {\n        while (b != 0) {\n            int temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countBeautifulPairs(vector&lt;int&gt;& nums) {\n        // Precompute coprime matrix for digits 0-9\n        bool coprime[10][10] = {false};\n        for (int i = 0; i &lt; 10; i++) {\n            for (int j = 0; j &lt; 10; j++) {\n                coprime[i][j] = (gcd(i, j) == 1);\n            }\n        }\n        \n        int count = 0;\n        int firstDigitCount[10] = {0};\n        \n        for (int num : nums) {\n            int lastDigit = num % 10;\n            // Count first digits seen so far that are coprime with lastDigit\n            for (int fd = 0; fd &lt; 10; fd++) {\n                if (coprime[fd][lastDigit]) {\n                    count += firstDigitCount[fd];\n                }\n            }\n            // Add first digit of current number\n            int firstDigit = getFirstDigit(num);\n            firstDigitCount[firstDigit]++;\n        }\n        return count;\n    }\n    \nprivate:\n    int getFirstDigit(int n) {\n        while (n &gt;= 10) {\n            n /= 10;\n        }\n        return n;\n    }\n    \n    int gcd(int a, int b) {\n        while (b != 0) {\n            int temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar countBeautifulPairs = function(nums) {\n    const getFirstDigit = (n) =&gt; {\n        while (n &gt;= 10) {\n            n = Math.floor(n / 10);\n        }\n        return n;\n    };\n    \n    const gcd = (a, b) =&gt; {\n        while (b !== 0) {\n            const temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    };\n    \n    // Precompute coprime matrix for digits 0-9\n    const coprime = Array(10).fill(null).map(() =&gt; Array(10).fill(false));\n    for (let i = 0; i &lt; 10; i++) {\n        for (let j = 0; j &lt; 10; j++) {\n            coprime[i][j] = gcd(i, j) === 1;\n        }\n    }\n    \n    let count = 0;\n    const firstDigitCount = Array(10).fill(0);\n    \n    for (const num of nums) {\n        const lastDigit = num % 10;\n        // Count first digits seen so far that are coprime with lastDigit\n        for (let fd = 0; fd &lt; 10; fd++) {\n            if (coprime[fd][lastDigit]) {\n                count += firstDigitCount[fd];\n            }\n        }\n        // Add first digit of current number\n        const firstDigit = getFirstDigit(num);\n        firstDigitCount[firstDigit]++;\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countBeautifulPairs(nums: number[]): number {\n    const getFirstDigit = (n: number): number =&gt; {\n        while (n &gt;= 10) {\n            n = Math.floor(n / 10);\n        }\n        return n;\n    };\n    \n    const gcd = (a: number, b: number): number =&gt; {\n        while (b !== 0) {\n            const temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    };\n    \n    // Precompute coprime matrix for digits 0-9\n    const coprime: boolean[][] = Array(10).fill(null).map(() =&gt; Array(10).fill(false));\n    for (let i = 0; i &lt; 10; i++) {\n        for (let j = 0; j &lt; 10; j++) {\n            coprime[i][j] = gcd(i, j) === 1;\n        }\n    }\n    \n    let count = 0;\n    const firstDigitCount: number[] = Array(10).fill(0);\n    \n    for (const num of nums) {\n        const lastDigit = num % 10;\n        // Count first digits seen so far that are coprime with lastDigit\n        for (let fd = 0; fd &lt; 10; fd++) {\n            if (coprime[fd][lastDigit]) {\n                count += firstDigitCount[fd];\n            }\n        }\n        // Add first digit of current number\n        const firstDigit = getFirstDigit(num);\n        firstDigitCount[firstDigit]++;\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n × 10) = O(n) for single pass with constant digit lookup
- Space: O(1) for the 10×10 coprime matrix and frequency array
- Notes: Optimal solution leveraging the small digit space (0-9). Much faster than brute force for larger arrays.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Number of Beautiful Pairs Solutions","datePublished":"2026-04-08","description":"Count pairs (i,j) where first digit of nums[i] and last digit of nums[j] are coprime (gcd=1).","keywords":"LeetCode, Number of Beautiful Pairs, Easy, Array, Hash Table, Math, Counting, Number Theory"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach has a time complexity of O(n² × log(max(nums))) due to checking all pairs and extracting digits."}},{"@type":"Question","name":"What is the time complexity of the Optimized Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The Optimized Counting approach has a time complexity of O(n) since we only iterate through the array once and perform constant-time operations for each element."}},{"@type":"Question","name":"Why is the Optimized Counting approach more efficient?","acceptedAnswer":{"@type":"Answer","text":"The Optimized Counting approach leverages the fact that there are only 10 possible digits (0-9), allowing us to use a frequency array and precomputed coprime matrix instead of checking all pairs."}},{"@type":"Question","name":"What is the space complexity of both approaches?","acceptedAnswer":{"@type":"Answer","text":"Both approaches have O(1) space complexity. The Brute Force uses only a few variables, while the Optimized Counting uses a fixed 10×10 matrix and a 10-element frequency array."}}]}).replace(/</g,'\u003c')}
</script>