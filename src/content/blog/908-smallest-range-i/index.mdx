---
title: "Smallest Range I"
summary: "Find the minimum possible difference between max and min elements after adding/subtracting at most k from each element."
date: "Oct 24, 2025"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 73.50% | **Paid:** No
> **Topics:** Array, Math

You are given an integer array nums and an integer k.

In one operation, you can choose any index i where 0 &lt;= i &lt; nums.length and change nums[i] to nums[i] + x where x is an integer from the range [-k, k]. You can apply this operation at most once for each index i.

The score of nums is the difference between the maximum and minimum elements in nums after applying the operation.

Return the minimum score of nums after changing the values at each index.

- Examples
- Constraints
- [Simple Iteration](#simple-iteration)
- [Sorting](#sorting)
- [Built-in Functions](#built-in-functions)

## Examples

### Example 1

**Input:**
```text
nums = [1], k = 0
```

**Output:**
```text
0
```

**Explanation:**
The score is max(nums) - min(nums) = 1 - 1 = 0.

### Example 2

**Input:**
```text
nums = [0,10], k = 2
```

**Output:**
```text
6
```

**Explanation:**
Change nums to be [2, 8]. The score is max(nums) - min(nums) = 8 - 2 = 6.

### Example 3

**Input:**
```text
nums = [1,3,6], k = 3
```

**Output:**
```text
0
```

**Explanation:**
Change nums to be [4, 4, 4]. The score is max(nums) - min(nums) = 4 - 4 = 0.

## Constraints

```text
1 <= nums.length <= 10⁴
0 <= nums[i] <= 10⁴
0 <= k <= 10⁴
```

---
## Simple Iteration

**Intuition**
To minimize the range, decrease the maximum element by k and increase the minimum element by k. The new range is max - min - 2k, or 0 if negative.

**Steps**
- Find the minimum and maximum values by iterating through the array
- Calculate the result as max - min - 2k
- Return 0 if the result is negative, otherwise return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestRangeI(self, nums: List[int], k: int) -&gt; int:\n        if len(nums) == 1:\n            return 0\n        \n        min_val = nums[0]\n        max_val = nums[0]\n        \n        for num in nums:\n            if num &lt; min_val:\n                min_val = num\n            if num &gt; max_val:\n                max_val = num\n        \n        result = max_val - min_val - 2 * k\n        return max(0, result)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int smallestRangeI(int[] nums, int k) {\n        if (nums.length == 1) {\n            return 0;\n        }\n        \n        int minVal = nums[0];\n        int maxVal = nums[0];\n        \n        for (int num : nums) {\n            minVal = Math.min(minVal, num);\n            maxVal = Math.max(maxVal, num);\n        }\n        \n        int result = maxVal - minVal - 2 * k;\n        return Math.max(0, result);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int smallestRangeI(vector&lt;int&gt;& nums, int k) {\n        if (nums.size() == 1) {\n            return 0;\n        }\n        \n        int minVal = nums[0];\n        int maxVal = nums[0];\n        \n        for (int num : nums) {\n            minVal = min(minVal, num);\n            maxVal = max(maxVal, num);\n        }\n        \n        int result = maxVal - minVal - 2 * k;\n        return max(0, result);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @param {number} k\n * @return {number}\n */\nvar smallestRangeI = function(nums, k) {\n    if (nums.length === 1) {\n        return 0;\n    }\n    \n    let minVal = nums[0];\n    let maxVal = nums[0];\n    \n    for (const num of nums) {\n        minVal = Math.min(minVal, num);\n        maxVal = Math.max(maxVal, num);\n    }\n    \n    const result = maxVal - minVal - 2 * k;\n    return Math.max(0, result);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function smallestRangeI(nums: number[], k: number): number {\n    if (nums.length === 1) {\n        return 0;\n    }\n    \n    let minVal = nums[0];\n    let maxVal = nums[0];\n    \n    for (const num of nums) {\n        minVal = Math.min(minVal, num);\n        maxVal = Math.max(maxVal, num);\n    }\n    \n    const result = maxVal - minVal - 2 * k;\n    return Math.max(0, result);\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal solution with single pass through the array

---
## Sorting

**Intuition**
After sorting, the minimum is at index 0 and maximum is at the last index. Calculate the range directly from these positions.

**Steps**
- Sort the array in ascending order
- The minimum is at index 0, maximum at the last index
- Calculate result as nums[last] - nums[0] - 2k
- Return 0 if negative, otherwise return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestRangeI(self, nums: List[int], k: int) -&gt; int:\n        if len(nums) == 1:\n            return 0\n        \n        nums.sort()\n        result = nums[-1] - nums[0] - 2 * k\n        return max(0, result)" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int smallestRangeI(int[] nums, int k) {\n        if (nums.length == 1) {\n            return 0;\n        }\n        \n        Arrays.sort(nums);\n        int result = nums[nums.length - 1] - nums[0] - 2 * k;\n        return Math.max(0, result);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int smallestRangeI(vector&lt;int&gt;& nums, int k) {\n        if (nums.size() == 1) {\n            return 0;\n        }\n        \n        sort(nums.begin(), nums.end());\n        int result = nums.back() - nums.front() - 2 * k;\n        return max(0, result);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @param {number} k\n * @return {number}\n */\nvar smallestRangeI = function(nums, k) {\n    if (nums.length === 1) {\n        return 0;\n    }\n    \n    nums.sort((a, b) =&gt; a - b);\n    const result = nums[nums.length - 1] - nums[0] - 2 * k;\n    return Math.max(0, result);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function smallestRangeI(nums: number[], k: number): number {\n    if (nums.length === 1) {\n        return 0;\n    }\n    \n    nums.sort((a, b) =&gt; a - b);\n    const result = nums[nums.length - 1] - nums[0] - 2 * k;\n    return Math.max(0, result);\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(1) or O(n) depending on sorting implementation
- Notes: Simpler code but slower due to sorting overhead

---
## Built-in Functions

**Intuition**
Use language-specific built-in functions to find min and max values directly, then compute the result.

**Steps**
- Use built-in min/max functions to find the range
- Calculate result as max - min - 2k
- Return 0 if negative, otherwise return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestRangeI(self, nums: List[int], k: int) -&gt; int:\n        if len(nums) == 1:\n            return 0\n        \n        result = max(nums) - min(nums) - 2 * k\n        return max(0, result)" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int smallestRangeI(int[] nums, int k) {\n        if (nums.length == 1) {\n            return 0;\n        }\n        \n        int minVal = Arrays.stream(nums).min().getAsInt();\n        int maxVal = Arrays.stream(nums).max().getAsInt();\n        int result = maxVal - minVal - 2 * k;\n        return Math.max(0, result);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int smallestRangeI(vector&lt;int&gt;& nums, int k) {\n        if (nums.size() == 1) {\n            return 0;\n        }\n        \n        auto [minIt, maxIt] = minmax_element(nums.begin(), nums.end());\n        int result = *maxIt - *minIt - 2 * k;\n        return max(0, result);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @param {number} k\n * @return {number}\n */\nvar smallestRangeI = function(nums, k) {\n    if (nums.length === 1) {\n        return 0;\n    }\n    \n    const minVal = Math.min(...nums);\n    const maxVal = Math.max(...nums);\n    const result = maxVal - minVal - 2 * k;\n    return Math.max(0, result);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function smallestRangeI(nums: number[], k: number): number {\n    if (nums.length === 1) {\n        return 0;\n    }\n    \n    const minVal = Math.min(...nums);\n    const maxVal = Math.max(...nums);\n    const result = maxVal - minVal - 2 * k;\n    return Math.max(0, result);\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Clean and concise code using built-in utilities

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Smallest Range I Solutions","datePublished":"2025-10-24","description":"Find the minimum possible difference between max and min elements after adding/subtracting at most k from each element.","keywords":"LeetCode, Smallest Range I, Easy, Array, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Simple Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the array, as we iterate through the array once to find min and max values."}},{"@type":"Question","name":"What is the time complexity of Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n log n) due to the sorting operation, which is the dominant factor."}},{"@type":"Question","name":"What is the space complexity of all approaches?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) for all approaches as we only use a constant amount of extra space regardless of input size."}},{"@type":"Question","name":"Why do we return 0 when max - min - 2k is negative?","acceptedAnswer":{"@type":"Answer","text":"When max - min - 2k is negative, it means we can make all elements equal (or overlap) by adjusting each element by at most k, so the minimum possible range is 0."}}]}).replace(/</g,'\u003c')}
</script>