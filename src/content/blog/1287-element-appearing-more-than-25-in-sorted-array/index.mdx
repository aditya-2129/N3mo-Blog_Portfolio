---
title: "Element Appearing More Than 25% In Sorted Array"
summary: "Given a sorted integer array, find the element that appears more than 25% of the time."
date: "Apr 01, 2025"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 61.20% | **Paid:** No
> **Topics:** Array

Given an integer array sorted in non-decreasing order, return the element that appears more than 25% of the time in this array.

- Examples
- Constraints
- [Hash Map Frequency Count](#hash-map-frequency-count)
- [Linear Scan](#linear-scan)
- [Fixed Window Check](#fixed-window-check)
- [Binary Search on Candidates](#binary-search-on-candidates)

## Examples

**Example 1:**

```text
Input: arr = [1,2,2,6,6,6,6,7,10]
Output: 6
```

**Example 2:**

```text
Input: arr = [1,1]
Output: 1
```

## Constraints

```text
- 1 <= arr.length <= 10^4
- 0 <= arr[i] <= 10^5
```

---

## Hash Map Frequency Count

**Intuition**
We can iterate through the array and count the frequency of every element using a hash map. Since we only need to find the element that appears more than 25% of the time, we can check the counts against the threshold `n / 4`.

**Steps**
- Calculate the threshold `quarter` as `n / 4`.
- Initialize a hash map to store frequencies.
- Iterate through the array, incrementing the count for each element.
- Iterate through the map entries and return the key where the value exceeds `quarter`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\nfrom collections import Counter\n\nclass Solution:\n    def findSpecialInteger(self, arr: List[int]) -> int:\n        n = len(arr)\n        quarter = n // 4\n        count = Counter(arr)\n        for k, v in count.items():\n            if v > quarter:\n                return k\n        return -1"
},
{ name: "Java", lang: "java", code: "import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public int findSpecialInteger(int[] arr) {\n        int n = arr.length;\n        int quarter = n / 4;\n        Map<Integer, Integer> count = new HashMap<>();\n        for (int num : arr) {\n            count.put(num, count.getOrDefault(num, 0) + 1);\n        }\n        for (Map.Entry<Integer, Integer> entry : count.entrySet()) {\n            if (entry.getValue() > quarter) {\n                return entry.getKey();\n            }\n        }\n        return -1;\n    }\n}"
},
{ name: "C++", lang: "cpp", code: "#include <vector>\n#include <unordered_map>\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int findSpecialInteger(vector<int>& arr) {\n        int n = arr.size();\n        int quarter = n / 4;\n        unordered_map<int, int> count;\n        for (int num : arr) {\n            count[num]++;\n        }\n        for (auto& p : count) {\n            if (p.second > quarter) {\n                return p.first;\n            }\n        }\n        return -1;\n    }\n};"
},
{ name: "JavaScript", lang: "javascript", code: "var findSpecialInteger = function(arr) {\n    const n = arr.length;\n    const quarter = Math.floor(n / 4);\n    const count = new Map();\n    for (const num of arr) {\n        count.set(num, (count.get(num) || 0) + 1);\n    }\n    for (const [key, val] of count) {\n        if (val > quarter) {\n            return key;\n        }\n    }\n    return -1;\n};"
},
{ name: "TypeScript", lang: "typescript", code: "function findSpecialInteger(arr: number[]): number {\n    const n = arr.length;\n    const quarter = Math.floor(n / 4);\n    const count = new Map<number, number>();\n    for (const num of arr) {\n        count.set(num, (count.get(num) || 0) + 1);\n    }\n    for (const [key, val] of count) {\n        if (val > quarter) {\n            return key;\n        }\n    }\n    return -1;\n}"
},]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Uses extra space proportional to the number of unique elements.

---

## Linear Scan

**Intuition**
Since the array is sorted, identical elements are grouped together. We can iterate through the array and count the length of the current consecutive sequence. If the count exceeds `n / 4`, we return that element immediately.

**Steps**
- Calculate `n` and `quarter`.
- Initialize `count` to 1.
- Iterate from index 1 to the end of the array.
- If `arr[i]` equals `arr[i-1]`, increment `count`. Otherwise, reset `count` to 1.
- If `count` exceeds `quarter`, return `arr[i]`.
- Return the last element as a fallback (handles cases where the target is at the very end).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def findSpecialInteger(self, arr: List[int]) -> int:\n        n = len(arr)\n        quarter = n // 4\n        count = 1\n        for i in range(1, n):\n            if arr[i] == arr[i - 1]:\n                count += 1\n            else:\n                count = 1\n            if count > quarter:\n                return arr[i]\n        return arr[-1]"
},
{ name: "Java", lang: "java", code: "class Solution {\n    public int findSpecialInteger(int[] arr) {\n        int n = arr.length;\n        int quarter = n / 4;\n        int count = 1;\n        for (int i = 1; i < n; i++) {\n            if (arr[i] == arr[i - 1]) {\n                count++;\n            } else {\n                count = 1;\n            }\n            if (count > quarter) {\n                return arr[i];\n            }\n        }\n        return arr[n - 1];\n    }\n}"
},
{ name: "C++", lang: "cpp", code: "#include <vector>\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int findSpecialInteger(vector<int>& arr) {\n        int n = arr.size();\n        int quarter = n / 4;\n        int count = 1;\n        for (int i = 1; i < n; i++) {\n            if (arr[i] == arr[i - 1]) {\n                count++;\n            } else {\n                count = 1;\n            }\n            if (count > quarter) {\n                return arr[i];\n            }\n        }\n        return arr[n - 1];\n    }\n};"
},
{ name: "JavaScript", lang: "javascript", code: "var findSpecialInteger = function(arr) {\n    const n = arr.length;\n    const quarter = Math.floor(n / 4);\n    let count = 1;\n    for (let i = 1; i < n; i++) {\n        if (arr[i] === arr[i - 1]) {\n            count++;\n        } else {\n            count = 1;\n        }\n        if (count > quarter) {\n            return arr[i];\n        }\n    }\n    return arr[n - 1];\n};"
},
{ name: "TypeScript", lang: "typescript", code: "function findSpecialInteger(arr: number[]): number {\n    const n = arr.length;\n    const quarter = Math.floor(n / 4);\n    let count = 1;\n    for (let i = 1; i < n; i++) {\n        if (arr[i] === arr[i - 1]) {\n            count++;\n        } else {\n            count = 1;\n        }\n        if (count > quarter) {\n            return arr[i];\n        }\n    }\n    return arr[n - 1];\n}"
},]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal space usage, leveraging the sorted property.

---

## Fixed Window Check

**Intuition**
If an element appears more than 25% of the time in a sorted array of length `n`, then that element must occupy at least one position in the first 75% of the array and also appear at the position `n / 4` steps ahead. Specifically, for any index `i`, if `arr[i] == arr[i + n/4]`, then `arr[i]` is the answer because the distance between these two indices is `n/4`, meaning there are at least `n/4 + 1` occurrences of that element.

**Steps**
- Calculate `n` and `span` (which is `n / 4`).
- Iterate `i` from 0 to `n - span - 1`.
- Check if `arr[i]` is equal to `arr[i + span]`.
- If they are equal, return `arr[i]`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def findSpecialInteger(self, arr: List[int]) -> int:\n        n = len(arr)\n        span = n // 4\n        for i in range(n - span):\n            if arr[i] == arr[i + span]:\n                return arr[i]\n        return -1"
},
{ name: "Java", lang: "java", code: "class Solution {\n    public int findSpecialInteger(int[] arr) {\n        int n = arr.length;\n        int span = n / 4;\n        for (int i = 0; i < n - span; i++) {\n            if (arr[i] == arr[i + span]) {\n                return arr[i];\n            }\n        }\n        return -1;\n    }\n}"
},
{ name: "C++", lang: "cpp", code: "#include <vector>\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int findSpecialInteger(vector<int>& arr) {\n        int n = arr.size();\n        int span = n / 4;\n        for (int i = 0; i < n - span; i++) {\n            if (arr[i] == arr[i + span]) {\n                return arr[i];\n            }\n        }\n        return -1;\n    }\n};"
},
{ name: "JavaScript", lang: "javascript", code: "var findSpecialInteger = function(arr) {\n    const n = arr.length;\n    const span = Math.floor(n / 4);\n    for (let i = 0; i < n - span; i++) {\n        if (arr[i] === arr[i + span]) {\n            return arr[i];\n        }\n    }\n    return -1;\n};"
},
{ name: "TypeScript", lang: "typescript", code: "function findSpecialInteger(arr: number[]): number {\n    const n = arr.length;\n    const span = Math.floor(n / 4);\n    for (let i = 0; i < n - span; i++) {\n        if (arr[i] === arr[i + span]) {\n            return arr[i];\n        }\n    }\n    return -1;\n}"
},]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Very concise and efficient, relying on the mathematical property of the sorted array.

---

## Binary Search on Candidates

**Intuition**
Since the array is sorted, the element appearing more than 25% must occupy one of the "quarter" positions: `0`, `n/4`, `n/2`, or `3n/4`. We can pick these four candidates and use binary search to find the first and last occurrence of each candidate. If the range length (`last - first + 1`) is greater than `n / 4`, we return that candidate.

**Steps**
- Calculate `n` and `quarter`.
- Define a helper function `findLeft` to find the first index of a target using binary search.
- Define a helper function `findRight` to find the last index of a target using binary search.
- Iterate through the candidate indices: `0`, `n/4`, `n/2`, `3n/4`.
- For each candidate value, find its left and right bounds.
- If `right - left + 1 &gt; quarter`, return the candidate.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\nimport bisect\n\nclass Solution:\n    def findSpecialInteger(self, arr: List[int]) -> int:\n        n = len(arr)\n        quarter = n // 4\n        candidates = [arr[n // 4], arr[n // 2], arr[3 * n // 4]]\n        \n        for cand in candidates:\n            left = bisect.bisect_left(arr, cand)\n            right = bisect.bisect_right(arr, cand) - 1\n            if right - left + 1 > quarter:\n                return cand\n        return arr[0]"
},
{ name: "Java", lang: "java", code: "class Solution {\n    public int findSpecialInteger(int[] arr) {\n        int n = arr.length;\n        int quarter = n / 4;\n        int[] candidates = {arr[n / 4], arr[n / 2], arr[3 * n / 4]};\n        \n        for (int cand : candidates) {\n            int left = findLeft(arr, cand);\n            int right = findRight(arr, cand);\n            if (right - left + 1 > quarter) {\n                return cand;\n            }\n        }\n        return arr[0];\n    }\n    \n    private int findLeft(int[] arr, int target) {\n        int lo = 0, hi = arr.length - 1;\n        while (lo < hi) {\n            int mid = lo + (hi - lo) / 2;\n            if (arr[mid] < target) lo = mid + 1;\n            else hi = mid;\n        }\n        return lo;\n    }\n    \n    private int findRight(int[] arr, int target) {\n        int lo = 0, hi = arr.length - 1;\n        while (lo < hi) {\n            int mid = lo + (hi - lo + 1) / 2;\n            if (arr[mid] > target) hi = mid - 1;\n            else lo = mid;\n        }\n        return lo;\n    }\n}"
},
{ name: "C++", lang: "cpp", code: "#include <vector>\n#include <algorithm>\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int findSpecialInteger(vector<int>& arr) {\n        int n = arr.size();\n        int quarter = n / 4;\n        vector<int> candidates = {arr[n / 4], arr[n / 2], arr[3 * n / 4]};\n        \n        for (int cand : candidates) {\n            int left = lower_bound(arr.begin(), arr.end(), cand) - arr.begin();\n            int right = upper_bound(arr.begin(), arr.end(), cand) - arr.begin() - 1;\n            if (right - left + 1 > quarter) {\n                return cand;\n            }\n        }\n        return arr[0];\n    }\n};"
},
{ name: "JavaScript", lang: "javascript", code: "var findSpecialInteger = function(arr) {\n    const n = arr.length;\n    const quarter = Math.floor(n / 4);\n    const candidates = [arr[Math.floor(n / 4)], arr[Math.floor(n / 2)], arr[Math.floor(3 * n / 4)]];\n    \n    const findLeft = (target) => {\n        let lo = 0, hi = n - 1;\n        while (lo < hi) {\n            const mid = Math.floor((lo + hi) / 2);\n            if (arr[mid] < target) lo = mid + 1;\n            else hi = mid;\n        }\n        return lo;\n    };\n    \n    const findRight = (target) => {\n        let lo = 0, hi = n - 1;\n        while (lo < hi) {\n            const mid = Math.floor((lo + hi + 1) / 2);\n            if (arr[mid] > target) hi = mid - 1;\n            else lo = mid;\n        }\n        return lo;\n    };\n\n    for (const cand of candidates) {\n        const left = findLeft(cand);\n        const right = findRight(cand);\n        if (right - left + 1 > quarter) {\n            return cand;\n        }\n    }\n    return arr[0];\n};"
},
{ name: "TypeScript", lang: "typescript", code: "function findSpecialInteger(arr: number[]): number {\n    const n = arr.length;\n    const quarter = Math.floor(n / 4);\n    const candidates = [arr[Math.floor(n / 4)], arr[Math.floor(n / 2)], arr[Math.floor(3 * n / 4)]];\n    \n    const findLeft = (target: number): number => {\n        let lo = 0, hi = n - 1;\n        while (lo < hi) {\n            const mid = Math.floor((lo + hi) / 2);\n            if (arr[mid] < target) lo = mid + 1;\n            else hi = mid;\n        }\n        return lo;\n    };\n    \n    const findRight = (target: number): number => {\n        let lo = 0, hi = n - 1;\n        while (lo < hi) {\n            const mid = Math.floor((lo + hi + 1) / 2);\n            if (arr[mid] > target) hi = mid - 1;\n            else lo = mid;\n        }\n        return lo;\n    };\n\n    for (const cand of candidates) {\n        const left = findLeft(cand);\n        const right = findRight(cand);\n        if (right - left + 1 > quarter) {\n            return cand;\n        }\n    }\n    return arr[0];\n}"
},]} />

**Complexity**
- Time: O(log n)
- Space: O(1)
- Notes: Theoretically the fastest time complexity, though with a small constant factor due to checking multiple candidates.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Element Appearing More Than 25% In Sorted Array Solutions","datePublished":"2025-04-01","description":"Given a sorted integer array, find the element that appears more than 25% of the time.","keywords":"LeetCode, Element Appearing More Than 25% In Sorted Array, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Map approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as it only uses a few variables for counting."}},{"@type":"Question","name":"How does the Fixed Window Check approach work?","acceptedAnswer":{"@type":"Answer","text":"It checks if arr[i] equals arr[i + n/4]. If true, that element appears more than 25% of the time."}},{"@type":"Question","name":"What is the fastest possible time complexity for this problem?","acceptedAnswer":{"@type":"Answer","text":"The fastest time complexity is O(log n) using Binary Search on specific candidate indices."}}]}).replace(/</g,'\u003c')}
</script>