---
title: "Modify Columns"
summary: "Modify the data type of a specific column in a 2D array from string to integer."
date: "May 21, 2024"
tags:
  - Array
  - Matrix
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 92.30% | **Paid:** No
> **Topics:** Array, Matrix

You are given a 2D array `employees` where each row represents an employee with columns `employee_id`, `name`, and `salary`. The `salary` column (index 2) contains string values. You need to modify the `salary` column by converting all string values to integers.

- Examples
- Constraints
- [Approach 1: Direct Iteration](#approach-1-direct-iteration)
- [Approach 2: Using Map](#approach-2-using-map)

## Examples

### Example 1

**Input:**
```text
DataFrame employees
+---------+--------+
| name    | salary |
+---------+--------+
| Jack    | 19666  |
| Piper   | 74754  |
| Mia     | 62509  |
| Ulysses | 54866  |
+---------+--------+
```

**Output:**
```text
+---------+--------+
| name    | salary |
+---------+--------+
| Jack    | 39332  |
| Piper   | 149508 |
| Mia     | 125018 |
| Ulysses | 109732 |
+---------+--------+
```

**Explanation:**
Every salary has been doubled.

## Constraints

```text
- The array will have at least 1 row.
- Each row will have exactly 3 elements.
- The salary column contains valid integer values as strings.
- employee_id and name columns remain unchanged.
```

---
## Approach 1: Direct Iteration

**Intuition**
Iterate through each row and convert the salary column value from string to integer.

**Steps**
- Loop through each row in the 2D array.
- For each row, access the salary column (index 2).
- Convert the string value to integer using parseInt or equivalent.
- Update the row with the converted value.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\ndef modifySalaryColumn(employees: List[List[str]]) -&gt; List[List[object]]:\n    for row in employees:\n        row[2] = int(row[2])\n    return employees" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\npublic class Solution {\n    public List&lt;List&lt;Object&gt;&gt; modifySalaryColumn(List&lt;List&lt;String&gt;&gt; employees) {\n        List&lt;List&lt;Object&gt;&gt; result = new ArrayList&lt;&gt;();\n        for (List&lt;String&gt; row : employees) {\n            List&lt;Object&gt; newRow = new ArrayList&lt;&gt;();\n            newRow.add(row.get(0));\n            newRow.add(row.get(1));\n            newRow.add(Integer.parseInt(row.get(2)));\n            result.add(newRow);\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;vector&lt;string&gt;&gt; modifySalaryColumn(vector&lt;vector&lt;string&gt;&gt;& employees) {\n        for (auto& row : employees) {\n            row[2] = to_string(stoi(row[2]));\n        }\n        return employees;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string[][]} employees\n * @return {Array&lt;Array&lt;string|number&gt;&gt;}\n */\nfunction modifySalaryColumn(employees) {\n    for (let i = 0; i &lt; employees.length; i++) {\n        employees[i][2] = parseInt(employees[i][2], 10);\n    }\n    return employees;\n}" },
{ name: "TypeScript", lang: "typescript", code: "type EmployeeRow = [string, string, string | number];\n\nfunction modifySalaryColumn(employees: EmployeeRow[][]): EmployeeRow[][] {\n    for (let i = 0; i &lt; employees.length; i++) {\n        employees[i][2] = parseInt(employees[i][2] as string, 10);\n    }\n    return employees;\n}" },]} />

**Complexity**
- Time: O(n) where n is the number of rows in the array
- Space: O(1) additional space (in-place modification)
- Notes: Simple and direct approach for type conversion.

---
## Approach 2: Using Map

**Intuition**
Use the map function to transform each row, converting the salary column value.

**Steps**
- Use map to iterate over each row.
- For each row, create a new row with the salary value converted to integer.
- Return the mapped array.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\ndef modifySalaryColumn(employees: List[List[str]]) -&gt; List[List[object]]:\n    return [[row[0], row[1], int(row[2])] for row in employees]" },
{ name: "Java", lang: "java", code: "import java.util.*;\nimport java.util.stream.*;\n\npublic class Solution {\n    public List&lt;List&lt;Object&gt;&gt; modifySalaryColumn(List&lt;List&lt;String&gt;&gt; employees) {\n        return employees.stream()\n            .map(row -&gt; Arrays.asList(row.get(0), row.get(1), Integer.parseInt(row.get(2))))\n            .collect(Collectors.toList());\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;vector&lt;string&gt;&gt; modifySalaryColumn(vector&lt;vector&lt;string&gt;&gt;& employees) {\n        vector&lt;vector&lt;string&gt;&gt; result;\n        for (const auto& row : employees) {\n            vector&lt;string&gt; newRow = row;\n            newRow[2] = to_string(stoi(row[2]));\n            result.push_back(newRow);\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string[][]} employees\n * @return {Array&lt;Array&lt;string|number&gt;&gt;}\n */\nfunction modifySalaryColumn(employees) {\n    return employees.map(row =&gt; [row[0], row[1], parseInt(row[2], 10)]);\n}" },
{ name: "TypeScript", lang: "typescript", code: "type EmployeeRow = [string, string, string | number];\n\nfunction modifySalaryColumn(employees: EmployeeRow[][]): EmployeeRow[][] {\n    return employees.map(row =&gt; [row[0], row[1], parseInt(row[2] as string, 10)]);\n}" },]} />

**Complexity**
- Time: O(n) where n is the number of rows in the array
- Space: O(n) for creating a new array
- Notes: Functional approach that creates a new array instead of modifying in-place.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Modify Columns Solutions","datePublished":"2024-05-21","description":"Modify the data type of a specific column in a 2D array from string to integer.","keywords":"LeetCode, Modify Columns, Easy, Array, Matrix"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of modifying columns in a 2D array?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the number of rows in the array, as each row needs to be processed for type conversion."}},{"@type":"Question","name":"What is the space complexity of the direct iteration approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) additional space for in-place modification, as we only modify existing elements without creating new data structures."}},{"@type":"Question","name":"What is the difference between direct iteration and using map?","acceptedAnswer":{"@type":"Answer","text":"Direct iteration modifies the array in-place with O(1) additional space, while using map creates a new array with O(n) space but provides a more functional programming style."}}]}).replace(/</g,'\u003c')}
</script>