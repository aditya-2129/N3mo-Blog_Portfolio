---
title: "Minimum Average of Smallest and Largest Elements"
summary: "Find the minimum average of pairs formed by repeatedly removing the smallest and largest elements from an array."
date: "Apr 28, 2024"
tags:
  - Array
  - Two Pointers
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 85.30% | **Paid:** No
> **Topics:** Array, Two Pointers, Sorting

You are given an array nums consisting of positive integers.

Return the minimum possible average after removing the smallest and largest elements from the array repeatedly.

The average of k elements is the sum of the k elements divided by k.

Note that when you remove the largest and smallest elements, there may be multiple elements with the same value. You may remove any one instance.

## Table of Contents
- Examples
- Constraints
- [Sorting + Two Pointers](#sorting--two-pointers)
- [Simulation (Brute Force)](#simulation-brute-force)

## Examples

### Example 1:
**Input:** nums = [1,2,3,7,8,9]
**Output:** 5.0
**Explanation:**
- Remove 1 and 9, average is (1 + 9) / 2 = 5.0.
- Remove 2 and 8, average is (2 + 8) / 2 = 5.0.
- Remove 3 and 7, average is (3 + 7) / 2 = 5.0.
The minimum average is 5.0.

### Example 2:
**Input:** nums = [1,2,3,4,5,6,7,8]
**Output:** 4.5
**Explanation:**
- Remove 1 and 8, average is (1 + 8) / 2 = 4.5.
- Remove 2 and 7, average is (2 + 7) / 2 = 4.5.
- Remove 3 and 6, average is (3 + 6) / 2 = 4.5.
- Remove 4 and 5, average is (4 + 5) / 2 = 4.5.
The minimum average is 4.5.

### Example 3:
**Input:** nums = [1,2,3,4,5,6]
**Output:** 3.5
**Explanation:**
- Remove 1 and 6, average is (1 + 6) / 2 = 3.5.
- Remove 2 and 5, average is (2 + 5) / 2 = 3.5.
- Remove 3 and 4, average is (3 + 4) / 2 = 3.5.
The minimum average is 3.5.

## Constraints

```text
2 <= nums.length <= 50
nums.length is even.
1 <= nums[i] <= 50
```

---

## Sorting + Two Pointers

**Intuition**
If we sort the array, the smallest element will be at the beginning and the largest at the end. The pairs we form will always be the current smallest and current largest remaining elements. By sorting once, we can easily access these pairs using two pointers moving towards the center.

**Steps**
- Sort the array `nums` in ascending order.
- Initialize two pointers, `left` at 0 and `right` at `nums.length - 1`.
- Initialize `min_avg` to a large value (e.g., infinity).
- Iterate while `left &lt; right`:
    - Calculate the average of `nums[left]` and `nums[right]`.
    - Update `min_avg` if the current average is smaller.
    - Increment `left` and decrement `right`.
- Return `min_avg`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumAverage(self, nums: list[int]) -&gt; float:\n        nums.sort()\n        n = len(nums)\n        min_avg = float('inf')\n        for i in range(n // 2):\n            avg = (nums[i] + nums[n - 1 - i]) / 2\n            if avg &lt; min_avg:\n                min_avg = avg\n        return min_avg" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public double minimumAverage(int[] nums) {\n        Arrays.sort(nums);\n        int n = nums.length;\n        double minAvg = Double.MAX_VALUE;\n        for (int i = 0; i &lt; n / 2; i++) {\n            double avg = (nums[i] + nums[n - 1 - i]) / 2.0;\n            if (avg &lt; minAvg) {\n                minAvg = avg;\n            }\n        }\n        return minAvg;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n#include &lt;climits&gt;\n\nclass Solution {\npublic:\n    double minimumAverage(std::vector&lt;int&gt;& nums) {\n        std::sort(nums.begin(), nums.end());\n        int n = nums.size();\n        double minAvg = INT_MAX;\n        for (int i = 0; i &lt; n / 2; i++) {\n            double avg = (nums[i] + nums[n - 1 - i]) / 2.0;\n            if (avg &lt; minAvg) {\n                minAvg = avg;\n            }\n        }\n        return minAvg;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar minimumAverage = function(nums) {\n    nums.sort((a, b) =&gt; a - b);\n    const n = nums.length;\n    let minAvg = Infinity;\n    for (let i = 0; i &lt; n / 2; i++) {\n        const avg = (nums[i] + nums[n - 1 - i]) / 2;\n        if (avg &lt; minAvg) {\n            minAvg = avg;\n        }\n    }\n    return minAvg;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumAverage(nums: number[]): number {\n    nums.sort((a, b) =&gt; a - b);\n    const n = nums.length;\n    let minAvg = Infinity;\n    for (let i = 0; i &lt; n / 2; i++) {\n        const avg = (nums[i] + nums[n - 1 - i]) / 2;\n        if (avg &lt; minAvg) {\n            minAvg = avg;\n        }\n    }\n    return minAvg;\n}" },]} />

**Complexity**
- Time: O(N log N) due to sorting.
- Space: O(1) or O(N) depending on the sorting algorithm's space complexity.
- Notes: This is the most efficient approach for this problem.

---

## Simulation (Brute Force)

**Intuition**
We can simulate the process exactly as described. In each step, find the minimum and maximum elements in the current array, calculate their average, and remove them from the array. Repeat until the array is empty.

**Steps**
- Initialize `min_avg` to infinity.
- Loop while `nums` is not empty:
    - Find the minimum value `min_val` and its index.
    - Find the maximum value `max_val` and its index.
    - Calculate the average of `min_val` and `max_val`.
    - Update `min_avg` if the current average is smaller.
    - Remove `min_val` and `max_val` from the array.
- Return `min_avg`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumAverage(self, nums: list[int]) -&gt; float:\n        min_avg = float('inf')\n        while nums:\n            min_val = min(nums)\n            max_val = max(nums)\n            avg = (min_val + max_val) / 2\n            if avg &lt; min_avg:\n                min_avg = avg\n            nums.remove(min_val)\n            nums.remove(max_val)\n        return min_avg" },
{ name: "Java", lang: "java", code: "import java.util.ArrayList;\nimport java.util.Collections;\n\nclass Solution {\n    public double minimumAverage(int[] nums) {\n        ArrayList&lt;Integer&gt; list = new ArrayList&lt;&gt;();\n        for (int num : nums) {\n            list.add(num);\n        }\n        double minAvg = Double.MAX_VALUE;\n        while (!list.isEmpty()) {\n            int minVal = Collections.min(list);\n            int maxVal = Collections.max(list);\n            double avg = (minVal + maxVal) / 2.0;\n            if (avg &lt; minAvg) {\n                minAvg = avg;\n            }\n            list.remove(Integer.valueOf(minVal));\n            list.remove(Integer.valueOf(maxVal));\n        }\n        return minAvg;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n#include &lt;climits&gt;\n\nclass Solution {\npublic:\n    double minimumAverage(std::vector&lt;int&gt;& nums) {\n        double minAvg = INT_MAX;\n        while (!nums.empty()) {\n            auto minIt = std::min_element(nums.begin(), nums.end());\n            auto maxIt = std::max_element(nums.begin(), nums.end());\n            int minVal = *minIt;\n            int maxVal = *maxIt;\n            double avg = (minVal + maxVal) / 2.0;\n            if (avg &lt; minAvg) {\n                minAvg = avg;\n            }\n            // Erase max first to handle potential index shift if min is before max\n            // But iterators are stable relative to the vector state before erase.\n            // We need to be careful about order if they are adjacent or same.\n            // Since min != max (unless size 1, but size is even and &gt;= 2), they are distinct.\n            // Erase the one with the larger index first to keep the other iterator valid.\n            if (minIt &gt; maxIt) {\n                nums.erase(maxIt);\n                nums.erase(minIt);\n            } else {\n                nums.erase(minIt);\n                nums.erase(maxIt);\n            }\n        }\n        return minAvg;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar minimumAverage = function(nums) {\n    let minAvg = Infinity;\n    while (nums.length &gt; 0) {\n        const minVal = Math.min(...nums);\n        const maxVal = Math.max(...nums);\n        const avg = (minVal + maxVal) / 2;\n        if (avg &lt; minAvg) {\n            minAvg = avg;\n        }\n        const minIndex = nums.indexOf(minVal);\n        nums.splice(minIndex, 1);\n        const maxIndex = nums.indexOf(maxVal);\n        nums.splice(maxIndex, 1);\n    }\n    return minAvg;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumAverage(nums: number[]): number {\n    let minAvg = Infinity;\n    while (nums.length &gt; 0) {\n        const minVal = Math.min(...nums);\n        const maxVal = Math.max(...nums);\n        const avg = (minVal + maxVal) / 2;\n        if (avg &lt; minAvg) {\n            minAvg = avg;\n        }\n        const minIndex = nums.indexOf(minVal);\n        nums.splice(minIndex, 1);\n        const maxIndex = nums.indexOf(maxVal);\n        nums.splice(maxIndex, 1);\n    }\n    return minAvg;\n}" },]} />

**Complexity**
- Time: O(N²) because finding min/max and removing elements are O(N) operations, repeated N/2 times.
- Space: O(N) to store the list (if not in-place) or O(1) if modifying in-place (though removals are costly).
- Notes: This approach is less efficient than sorting but directly follows the problem description.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Average of Smallest and Largest Elements Solutions","datePublished":"2024-04-28","description":"Find the minimum average of pairs formed by repeatedly removing the smallest and largest elements from an array.","keywords":"LeetCode, Minimum Average of Smallest and Largest Elements, Easy, Array, Two Pointers, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N log N) due to the sorting step, where N is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) if we ignore the space required for the output, or O(N) if the sorting algorithm uses additional space."}},{"@type":"Question","name":"What is the time complexity of the Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N²) because for each of the N/2 iterations, we scan the array to find min and max, and remove elements, which are O(N) operations."}}]}).replace(/</g,'\u003c')}
</script>