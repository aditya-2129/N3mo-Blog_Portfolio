---
title: "Restore Finishing Order"
summary: "Given an array where each element represents the number of runners before that runner, restore the finishing order."
date: "Oct 22, 2025"
tags:
  - Array
  - Hash Table
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 91.20% | **Paid:** No
> **Topics:** Array, Hash Table

There is a race with n runners numbered from 0 to n - 1. You are given an array finishPosition of size n, where finishPosition[i] represents the number of runners who finished before the i-th runner. The finishing positions are 0-indexed (0 means no one finished before, i.e., first place).

Return an array result of size n where result[j] represents the runner who finished in position j (i.e., the runner who has exactly j runners before them).

- Examples
- Constraints
- [Direct Array Construction](#direct-array-construction)
- [Sorting Approach](#sorting-approach)
- [Hash Map Approach](#hash-map-approach)

## Examples

### Example 1

**Input:**
```text
order = [3,1,2,5,4], friends = [1,3,4]
```

**Output:**
```text
[3,1,4]
```

**Explanation:**
The finishing order is [3, 1, 2, 5, 4]. Therefore, the finishing order of your friends is [3, 1, 4].

### Example 2

**Input:**
```text
order = [1,4,5,3,2], friends = [2,5]
```

**Output:**
```text
[5,2]
```

**Explanation:**
The finishing order is [1, 4, 5, 3, 2]. Therefore, the finishing order of your friends is [5, 2].

## Constraints

```text
- 1 <= n == order.length <= 100
- order contains every integer from 1 to n exactly once
- 1 <= friends.length <= min(8, n)
- 1 <= friends[i] <= n
- friends is strictly increasing
```

---

## Direct Array Construction

**Intuition**
Since finishPosition[i] tells us the position where runner i should be placed, we can directly place each runner at their corresponding position in a new result array.

**Steps**
- Create a result array of size n
- For each runner, place them at index finishPosition[runner] in the result array
- Return the result array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def restoreFinishingOrder(self, finishPosition: List[int]) -&gt; List[int]:\n        n = len(finishPosition)\n        result = [0] * n\n        for runner in range(n):\n            result[finishPosition[runner]] = runner\n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] restoreFinishingOrder(int[] finishPosition) {\n        int n = finishPosition.length;\n        int[] result = new int[n];\n        for (int runner = 0; runner &lt; n; runner++) {\n            result[finishPosition[runner]] = runner;\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; restoreFinishingOrder(vector&lt;int&gt;& finishPosition) {\n        int n = finishPosition.size();\n        vector&lt;int&gt; result(n);\n        for (int runner = 0; runner &lt; n; runner++) {\n            result[finishPosition[runner]] = runner;\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    restoreFinishingOrder(finishPosition) {\n        const n = finishPosition.length;\n        const result = new Array(n);\n        for (let runner = 0; runner &lt; n; runner++) {\n            result[finishPosition[runner]] = runner;\n        }\n        return result;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    restoreFinishingOrder(finishPosition: number[]): number[] {\n        const n = finishPosition.length;\n        const result: number[] = new Array(n);\n        for (let runner = 0; runner &lt; n; runner++) {\n            result[finishPosition[runner]] = runner;\n        }\n        return result;\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Optimal solution with single pass through the array

---

## Sorting Approach

**Intuition**
Create pairs of (position, runner) and sort by position to get the finishing order.

**Steps**
- Create pairs of (finishPosition[i], i) for each runner i
- Sort the pairs by the first element (position)
- Extract the runner values from the sorted pairs

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def restoreFinishingOrder(self, finishPosition: List[int]) -&gt; List[int]:\n        pairs = [(finishPosition[i], i) for i in range(len(finishPosition))]\n        pairs.sort()\n        return [runner for _, runner in pairs]" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int[] restoreFinishingOrder(int[] finishPosition) {\n        int n = finishPosition.length;\n        int[][] pairs = new int[n][2];\n        for (int i = 0; i &lt; n; i++) {\n            pairs[i][0] = finishPosition[i];\n            pairs[i][1] = i;\n        }\n        Arrays.sort(pairs, (a, b) -&gt; a[0] - b[0]);\n        int[] result = new int[n];\n        for (int i = 0; i &lt; n; i++) {\n            result[i] = pairs[i][1];\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; restoreFinishingOrder(vector&lt;int&gt;& finishPosition) {\n        int n = finishPosition.size();\n        vector&lt;pair&lt;int, int&gt;&gt; pairs;\n        for (int i = 0; i &lt; n; i++) {\n            pairs.push_back({finishPosition[i], i});\n        }\n        sort(pairs.begin(), pairs.end());\n        vector&lt;int&gt; result(n);\n        for (int i = 0; i &lt; n; i++) {\n            result[i] = pairs[i].second;\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    restoreFinishingOrder(finishPosition) {\n        const n = finishPosition.length;\n        const pairs = finishPosition.map((pos, runner) =&gt; [pos, runner]);\n        pairs.sort((a, b) =&gt; a[0] - b[0]);\n        return pairs.map(pair =&gt; pair[1]);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    restoreFinishingOrder(finishPosition: number[]): number[] {\n        const n = finishPosition.length;\n        const pairs: [number, number][] = finishPosition.map((pos, runner) =&gt; [pos, runner]);\n        pairs.sort((a, b) =&gt; a[0] - b[0]);\n        return pairs.map(pair =&gt; pair[1]);\n    }\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(n)
- Notes: Sorting adds overhead compared to direct array construction

---

## Hash Map Approach

**Intuition**
Use a hash map to store the mapping from position to runner, then build the result array by looking up each position.

**Steps**
- Create a hash map mapping position to runner
- For each position from 0 to n-1, look up the corresponding runner
- Build and return the result array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def restoreFinishingOrder(self, finishPosition: List[int]) -&gt; List[int]:\n        n = len(finishPosition)\n        position_to_runner = {}\n        for runner in range(n):\n            position_to_runner[finishPosition[runner]] = runner\n        result = [position_to_runner[pos] for pos in range(n)]\n        return result" },
{ name: "Java", lang: "java", code: "import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public int[] restoreFinishingOrder(int[] finishPosition) {\n        int n = finishPosition.length;\n        Map&lt;Integer, Integer&gt; positionToRunner = new HashMap&lt;&gt;();\n        for (int runner = 0; runner &lt; n; runner++) {\n            positionToRunner.put(finishPosition[runner], runner);\n        }\n        int[] result = new int[n];\n        for (int pos = 0; pos &lt; n; pos++) {\n            result[pos] = positionToRunner.get(pos);\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_map&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;int&gt; restoreFinishingOrder(vector&lt;int&gt;& finishPosition) {\n        int n = finishPosition.size();\n        unordered_map&lt;int, int&gt; positionToRunner;\n        for (int runner = 0; runner &lt; n; runner++) {\n            positionToRunner[finishPosition[runner]] = runner;\n        }\n        vector&lt;int&gt; result(n);\n        for (int pos = 0; pos &lt; n; pos++) {\n            result[pos] = positionToRunner[pos];\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    restoreFinishingOrder(finishPosition) {\n        const n = finishPosition.length;\n        const positionToRunner = new Map();\n        for (let runner = 0; runner &lt; n; runner++) {\n            positionToRunner.set(finishPosition[runner], runner);\n        }\n        const result = new Array(n);\n        for (let pos = 0; pos &lt; n; pos++) {\n            result[pos] = positionToRunner.get(pos);\n        }\n        return result;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    restoreFinishingOrder(finishPosition: number[]): number[] {\n        const n = finishPosition.length;\n        const positionToRunner = new Map&lt;number, number&gt;();\n        for (let runner = 0; runner &lt; n; runner++) {\n            positionToRunner.set(finishPosition[runner], runner);\n        }\n        const result: number[] = new Array(n);\n        for (let pos = 0; pos &lt; n; pos++) {\n            result[pos] = positionToRunner.get(pos)!;\n        }\n        return result;\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Hash map overhead makes it slightly less efficient than direct array construction

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Restore Finishing Order Solutions","datePublished":"2025-10-22","description":"Given an array where each element represents the number of runners before that runner, restore the finishing order.","keywords":"LeetCode, Restore Finishing Order, Easy, Array, Hash Table"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Direct Array Construction approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the number of runners."}},{"@type":"Question","name":"What is the space complexity of the Direct Array Construction approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) for the result array."}},{"@type":"Question","name":"What is the time complexity of the Sorting Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n log n) due to the sorting step."}},{"@type":"Question","name":"What is the space complexity of the Sorting Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) for storing the pairs and the result."}},{"@type":"Question","name":"What is the time complexity of the Hash Map Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) for iterating through the array and building the hash map."}},{"@type":"Question","name":"What is the space complexity of the Hash Map Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) for the hash map and the result array."}}]}).replace(/</g,'\u003c')}
</script>