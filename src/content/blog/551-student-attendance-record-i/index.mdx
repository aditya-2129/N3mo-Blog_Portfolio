---
title: "Student Attendance Record I"
summary: "Check if a student's attendance record is rewardable based on absence count and consecutive late days."
date: "Jun 08, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 50.20% | **Paid:** No
> **Topics:** String

You are given a string s representing an attendance record for a student where each character signifies whether the student was absent, late, or present on that day. The record only contains the following three characters:

'A': Absent.
'L': Late.
'P': Present.

The student is eligible for an attendance award if they meet both of the following criteria:

The student was absent ('A') for strictly fewer than 2 days total.
The student was never late ('L') for 3 or more consecutive days.
Return true if the student is eligible for an attendance award, or false otherwise.

- Examples
- Constraints
- [Single Pass with Counters](#single-pass-with-counters)
- [String Methods Approach](#string-methods-approach)
- [Regular Expression Approach](#regular-expression-approach)

## Examples

```text
Input: s = "PPALLP"
Output: true
Explanation: The student has fewer than 2 absences and was never late 3 or more consecutive days.
```

```text
Input: s = "PPALLL"
Output: false
Explanation: The student was late 3 consecutive days in the last 3 days.
```

## Constraints

```text
1 <= s.length <= 1000
s[i] is either 'A', 'L', or 'P'.
```

---

## Single Pass with Counters

**Intuition**
Iterate through the string once, tracking the total count of absences and the current streak of consecutive late days.

**Steps**
- Initialize counters for absences and consecutive late days
- For each character, update the appropriate counter
- Reset consecutive late counter when we see 'A' or 'P'
- Return false early if either condition is violated
- Return true if we complete the iteration without violations

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkRecord(self, s: str) -&gt; bool:\n        count_A = 0\n        consecutive_L = 0\n        \n        for char in s:\n            if char == 'A':\n                count_A += 1\n                consecutive_L = 0\n                if count_A &gt;= 2:\n                    return False\n            elif char == 'L':\n                consecutive_L += 1\n                if consecutive_L &gt;= 3:\n                    return False\n            else:  # 'P'\n                consecutive_L = 0\n        \n        return True" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkRecord(String s) {\n        int countA = 0;\n        int consecutiveL = 0;\n        \n        for (int i = 0; i &lt; s.length(); i++) {\n            char c = s.charAt(i);\n            if (c == 'A') {\n                countA++;\n                consecutiveL = 0;\n                if (countA &gt;= 2) {\n                    return false;\n                }\n            } else if (c == 'L') {\n                consecutiveL++;\n                if (consecutiveL &gt;= 3) {\n                    return false;\n                }\n            } else { // 'P'\n                consecutiveL = 0;\n            }\n        }\n        \n        return true;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkRecord(string s) {\n        int countA = 0;\n        int consecutiveL = 0;\n        \n        for (char c : s) {\n            if (c == 'A') {\n                countA++;\n                consecutiveL = 0;\n                if (countA &gt;= 2) {\n                    return false;\n                }\n            } else if (c == 'L') {\n                consecutiveL++;\n                if (consecutiveL &gt;= 3) {\n                    return false;\n                }\n            } else { // 'P'\n                consecutiveL = 0;\n            }\n        }\n        \n        return true;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {boolean}\n */\nvar checkRecord = function(s) {\n    let countA = 0;\n    let consecutiveL = 0;\n    \n    for (let i = 0; i &lt; s.length; i++) {\n        const c = s[i];\n        if (c === 'A') {\n            countA++;\n            consecutiveL = 0;\n            if (countA &gt;= 2) {\n                return false;\n            }\n        } else if (c === 'L') {\n            consecutiveL++;\n            if (consecutiveL &gt;= 3) {\n                return false;\n            }\n        } else { // 'P'\n            consecutiveL = 0;\n        }\n    }\n    \n    return true;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkRecord(s: string): boolean {\n    let countA = 0;\n    let consecutiveL = 0;\n    \n    for (let i = 0; i &lt; s.length; i++) {\n        const c = s[i];\n        if (c === 'A') {\n            countA++;\n            consecutiveL = 0;\n            if (countA &gt;= 2) {\n                return false;\n            }\n        } else if (c === 'L') {\n            consecutiveL++;\n            if (consecutiveL &gt;= 3) {\n                return false;\n            }\n        } else { // 'P'\n            consecutiveL = 0;\n        }\n    }\n    \n    return true;\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1) only using constant extra space
- Notes: Early termination possible when conditions are violated

---

## String Methods Approach

**Intuition**
Use built-in string methods to directly check the two conditions: count of 'A' and presence of "LLL" substring.

**Steps**
- Count occurrences of 'A' using count method
- Check if "LLL" exists in the string using contains or includes
- Return true only if count of 'A' is less than 2 AND "LLL" is not present

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def checkRecord(self, s: str) -&gt; bool:\n        return s.count('A') &lt; 2 and 'LLL' not in s" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean checkRecord(String s) {\n        int countA = 0;\n        for (int i = 0; i &lt; s.length(); i++) {\n            if (s.charAt(i) == 'A') {\n                countA++;\n            }\n        }\n        return countA &lt; 2 && !s.contains(\"LLL\");\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool checkRecord(string s) {\n        int countA = 0;\n        for (char c : s) {\n            if (c == 'A') {\n                countA++;\n            }\n        }\n        return countA &lt; 2 && s.find(\"LLL\") == string::npos;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {boolean}\n */\nvar checkRecord = function(s) {\n    let countA = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        if (s[i] === 'A') {\n            countA++;\n        }\n    }\n    return countA &lt; 2 && !s.includes('LLL');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkRecord(s: string): boolean {\n    let countA = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        if (s[i] === 'A') {\n            countA++;\n        }\n    }\n    return countA &lt; 2 && !s.includes('LLL');\n}" },]} />

**Complexity**
- Time: O(n) for counting and substring search
- Space: O(1) constant extra space
- Notes: More readable but may traverse string multiple times internally

---

## Regular Expression Approach

**Intuition**
Use regex pattern matching to check for invalid patterns: two or more 'A's or three consecutive 'L's.

**Steps**
- Create regex pattern to match two 'A's with any characters in between
- Create regex pattern to match three consecutive 'L's
- Return true if neither pattern is found in the string

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import re\n\nclass Solution:\n    def checkRecord(self, s: str) -&gt; bool:\n        return not (re.search(r'A.*A', s) or re.search(r'LLL', s))" },
{ name: "Java", lang: "java", code: "import java.util.regex.*;\n\nclass Solution {\n    public boolean checkRecord(String s) {\n        Pattern patternA = Pattern.compile(\"A.*A\");\n        Pattern patternL = Pattern.compile(\"LLL\");\n        return !patternA.matcher(s).find() && !patternL.matcher(s).find();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;regex&gt;\n\nclass Solution {\npublic:\n    bool checkRecord(string s) {\n        regex patternA(\"A.*A\");\n        regex patternL(\"LLL\");\n        return !regex_search(s, patternA) && !regex_search(s, patternL);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {boolean}\n */\nvar checkRecord = function(s) {\n    return !(/A.*A/.test(s) || /LLL/.test(s));\n};" },
{ name: "TypeScript", lang: "typescript", code: "function checkRecord(s: string): boolean {\n    return !(/A.*A/.test(s) || /LLL/.test(s));\n}" },]} />

**Complexity**
- Time: O(n) regex matching complexity
- Space: O(1) constant extra space for patterns
- Notes: Elegant but regex overhead may be slower for simple patterns

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Student Attendance Record I Solutions","datePublished":"2025-06-08","description":"Check if a student's attendance record is rewardable based on absence count and consecutive late days.","keywords":"LeetCode, Student Attendance Record I, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Single Pass with Counters approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of the string, as we iterate through each character exactly once."}},{"@type":"Question","name":"What is the space complexity of all approaches?","acceptedAnswer":{"@type":"Answer","text":"O(1) constant extra space for all approaches, as we only use a few variables regardless of input size."}},{"@type":"Question","name":"What are the two conditions for a rewardable attendance record?","acceptedAnswer":{"@type":"Answer","text":"The student must have fewer than 2 total absences ('A') and never be late ('L') for 3 or more consecutive days."}},{"@type":"Question","name":"Which approach is most efficient for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Single Pass with Counters approach is most efficient as it can terminate early when conditions are violated, though all approaches have the same asymptotic complexity."}}]}).replace(/</g,'\u003c')}
</script>