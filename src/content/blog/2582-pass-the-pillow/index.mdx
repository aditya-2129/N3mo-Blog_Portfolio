---
title: "Pass the Pillow"
summary: "Find which person holds the pillow after passing it back and forth in a line for a given time."
date: "Apr 26, 2026"
tags:
  - Math
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 56.50% | **Paid:** No
> **Topics:** Math, Simulation

There are n people standing in a line labeled from 1 to n. The first person in line is holding a pillow initially.

Every second, the person holding the pillow passes it to the next person standing in line. Once the pillow reaches the end of the line (i.e., the nth person), the passing direction reverses, and the pillow continues to be passed in the opposite direction.

Given the number of people in the line n and the time time in seconds, return the index of the person holding the pillow after time seconds.

- Examples
- Constraints
- [Simulation Approach](#simulation-approach)
- [Mathematical Approach](#mathematical-approach)

## Examples

**Example 1:**

```text
Input: n = 4, time = 5
Output: 2
Explanation: People are labeled as 1, 2, 3, 4. The pillow passing sequence is: 1->2->3->4->3->2. After 5 seconds, the pillow is at person 2.
```

**Example 2:**

```text
Input: n = 3, time = 2
Output: 3
Explanation: People are labeled as 1, 2, 3. The pillow passing sequence is: 1->2->3. After 2 seconds, the pillow is at person 3.
```

## Constraints

```text
2 <= n <= 1000
1 <= time <= 1000
```

---

## Simulation Approach

**Intuition**
Simulate the pillow passing process second by second, tracking the current position and direction of movement.

**Steps**
- Start at position 1 with forward direction
- For each second, move the pillow in the current direction
- Reverse direction when reaching person 1 or person n
- Return the final position after time seconds

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def passThePillow(self, n: int, time: int) -&gt; int:\n        pos = 1\n        direction = 1  # 1 for forward, -1 for backward\n        \n        for _ in range(time):\n            pos += direction\n            if pos == n:\n                direction = -1\n            elif pos == 1:\n                direction = 1\n        \n        return pos" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int passThePillow(int n, int time) {\n        int pos = 1;\n        int direction = 1; // 1 for forward, -1 for backward\n        \n        for (int i = 0; i &lt; time; i++) {\n            pos += direction;\n            if (pos == n) {\n                direction = -1;\n            } else if (pos == 1) {\n                direction = 1;\n            }\n        }\n        \n        return pos;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int passThePillow(int n, int time) {\n        int pos = 1;\n        int direction = 1; // 1 for forward, -1 for backward\n        \n        for (int i = 0; i &lt; time; i++) {\n            pos += direction;\n            if (pos == n) {\n                direction = -1;\n            } else if (pos == 1) {\n                direction = 1;\n            }\n        }\n        \n        return pos;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @param {number} time\n * @return {number}\n */\nvar passThePillow = function(n, time) {\n    let pos = 1;\n    let direction = 1; // 1 for forward, -1 for backward\n    \n    for (let i = 0; i &lt; time; i++) {\n        pos += direction;\n        if (pos === n) {\n            direction = -1;\n        } else if (pos === 1) {\n            direction = 1;\n        }\n    }\n    \n    return pos;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function passThePillow(n: number, time: number): number {\n    let pos = 1;\n    let direction = 1; // 1 for forward, -1 for backward\n    \n    for (let i = 0; i &lt; time; i++) {\n        pos += direction;\n        if (pos === n) {\n            direction = -1;\n        } else if (pos === 1) {\n            direction = 1;\n        }\n    }\n    \n    return pos;\n}" },]} />

**Complexity**
- Time: O(time)
- Space: O(1)
- Notes: Simple and intuitive, but not optimal for large time values

---

## Mathematical Approach

**Intuition**
The pillow moves in a repeating cycle of length 2(n-1). Use modulo arithmetic to find the position directly without simulation.

**Steps**
- Calculate the cycle length as 2(n-1)
- Find the remainder when time is divided by cycle length
- If remainder is in the first half (≤ n-1), position is 1 + remainder
- Otherwise, position is n - (remainder - (n-1))

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def passThePillow(self, n: int, time: int) -&gt; int:\n        cycle = 2 * (n - 1)\n        remainder = time % cycle\n        \n        if remainder &lt;= n - 1:\n            return 1 + remainder\n        else:\n            return n - (remainder - (n - 1))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int passThePillow(int n, int time) {\n        int cycle = 2 * (n - 1);\n        int remainder = time % cycle;\n        \n        if (remainder &lt;= n - 1) {\n            return 1 + remainder;\n        } else {\n            return n - (remainder - (n - 1));\n        }\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int passThePillow(int n, int time) {\n        int cycle = 2 * (n - 1);\n        int remainder = time % cycle;\n        \n        if (remainder &lt;= n - 1) {\n            return 1 + remainder;\n        } else {\n            return n - (remainder - (n - 1));\n        }\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @param {number} time\n * @return {number}\n */\nvar passThePillow = function(n, time) {\n    const cycle = 2 * (n - 1);\n    const remainder = time % cycle;\n    \n    if (remainder &lt;= n - 1) {\n        return 1 + remainder;\n    } else {\n        return n - (remainder - (n - 1));\n    }\n};" },
{ name: "TypeScript", lang: "typescript", code: "function passThePillow(n: number, time: number): number {\n    const cycle = 2 * (n - 1);\n    const remainder = time % cycle;\n    \n    if (remainder &lt;= n - 1) {\n        return 1 + remainder;\n    } else {\n        return n - (remainder - (n - 1));\n    }\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Optimal solution using mathematical pattern recognition

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Pass the Pillow Solutions","datePublished":"2026-04-26","description":"Find which person holds the pillow after passing it back and forth in a line for a given time.","keywords":"LeetCode, Pass the Pillow, Easy, Math, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(time) - we iterate through each second."}},{"@type":"Question","name":"What is the time complexity of the Mathematical approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - constant time using modulo arithmetic."}},{"@type":"Question","name":"What is the space complexity for both approaches?","acceptedAnswer":{"@type":"Answer","text":"O(1) - both use constant extra space."}},{"@type":"Question","name":"Why is the cycle length 2(n-1)?","acceptedAnswer":{"@type":"Answer","text":"The pillow travels from 1 to n (n-1 steps) and back from n to 1 (n-1 steps), totaling 2(n-1) steps per complete cycle."}}]}).replace(/</g,'\u003c')}
</script>