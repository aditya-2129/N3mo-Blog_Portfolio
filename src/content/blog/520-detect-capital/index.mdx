---
title: "Detect Capital"
summary: "Determine if a word uses capital letters correctly: all uppercase, all lowercase, or only the first letter uppercase."
date: "Jan 14, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 56.60% | **Paid:** No
> **Topics:** String

We define the usage of capitals in a word to be right when one of the following cases holds:

This word uses all capitals, like "USA".
All letters in this word are not capitals, like "leetcode".
Only the first letter in this word is capital, like "Google".

Given a string word, return true if the usage of capitals is right.

- Examples
- Constraints
- [Approach 1: Built-in String Methods](#approach-1-built-in-string-methods)
- [Approach 2: Iterative Counting](#approach-2-iterative-counting)
- [Approach 3: Regular Expression](#approach-3-regular-expression)

## Examples

### Example 1

**Input:**
```text
word = "USA"
```

**Output:**
```text
true
```

### Example 2

**Input:**
```text
word = "FlaG"
```

**Output:**
```text
false
```

## Constraints

```text
1 <= word.length <= 100
word consists of lowercase and uppercase English letters.
```

---
## Approach 1: Built-in String Methods

**Intuition**
Most programming languages provide standard library methods to check the case of strings or characters. We can simply check if the word matches one of the three valid patterns: all uppercase, all lowercase, or title case (first letter uppercase, rest lowercase).

**Steps**
- Check if the word is entirely uppercase.
- Check if the word is entirely lowercase.
- Check if the word follows title case rules (first char uppercase, rest lowercase).
- Return true if any of the above checks pass.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def detectCapitalUse(self, word: str) -&gt; bool:\n        return word.isupper() or word.islower() or word.istitle()" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean detectCapitalUse(String word) {\n        return word.equals(word.toUpperCase()) ||\n               word.equals(word.toLowerCase()) ||\n               Character.isUpperCase(word.charAt(0)) && \n               word.substring(1).equals(word.substring(1).toLowerCase());\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;cctype&gt;\n#include &lt;string&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    bool detectCapitalUse(string word) {\n        bool allUpper = all_of(word.begin(), word.end(), [](unsigned char c){ return isupper(c); });\n        bool allLower = all_of(word.begin(), word.end(), [](unsigned char c){ return islower(c); });\n        bool titleCase = isupper(word[0]) && all_of(word.begin() + 1, word.end(), [](unsigned char c){ return islower(c); });\n        return allUpper || allLower || titleCase;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} word\n * @return {boolean}\n */\nvar detectCapitalUse = function(word) {\n    return word === word.toUpperCase() || \n           word === word.toLowerCase() || \n           (word[0] === word[0].toUpperCase() && word.slice(1) === word.slice(1).toLowerCase());\n};" },
{ name: "TypeScript", lang: "typescript", code: "function detectCapitalUse(word: string): boolean {\n    return word === word.toUpperCase() || \n           word === word.toLowerCase() || \n           (word[0] === word[0].toUpperCase() && word.slice(1) === word.slice(1).toLowerCase());\n}" },]} />

**Complexity**
- Time: O(n) — Standard string methods iterate through the string.
- Space: O(1) — No extra space proportional to input size is used.
- Notes: Very readable and concise, though it may create temporary string copies depending on language implementation.

---
## Approach 2: Iterative Counting

**Intuition**
We can iterate through the string and count how many capital letters are present. The word is valid if the count of capitals is 0 (all lowercase), equal to the length of the word (all uppercase), or exactly 1 and the first character is uppercase.

**Steps**
- Initialize a counter for capital letters.
- Iterate through each character in the string.
- If the character is uppercase, increment the counter.
- After the loop, check the three conditions: count is 0, count equals length, or (count is 1 and the first character is uppercase).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def detectCapitalUse(self, word: str) -&gt; bool:\n        n = len(word)\n        cap_count = 0\n        for c in word:\n            if c.isupper():\n                cap_count += 1\n        return cap_count == 0 or cap_count == n or (cap_count == 1 and word[0].isupper())" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean detectCapitalUse(String word) {\n        int n = word.length();\n        int capCount = 0;\n        for (int i = 0; i &lt; n; i++) {\n            if (Character.isUpperCase(word.charAt(i))) {\n                capCount++;\n            }\n        }\n        return capCount == 0 || capCount == n || (capCount == 1 && Character.isUpperCase(word.charAt(0)));\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    bool detectCapitalUse(string word) {\n        int n = word.size();\n        int capCount = 0;\n        for (char c : word) {\n            if (isupper(c)) {\n                capCount++;\n            }\n        }\n        return capCount == 0 || capCount == n || (capCount == 1 && isupper(word[0]));\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} word\n * @return {boolean}\n */\nvar detectCapitalUse = function(word) {\n    let n = word.length;\n    let capCount = 0;\n    for (let i = 0; i &lt; n; i++) {\n        if (word[i] &gt;= 'A' && word[i] &lt;= 'Z') {\n            capCount++;\n        }\n    }\n    return capCount === 0 || capCount === n || (capCount === 1 && word[0] &gt;= 'A' && word[0] &lt;= 'Z');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function detectCapitalUse(word: string): boolean {\n    let n: number = word.length;\n    let capCount: number = 0;\n    for (let i = 0; i &lt; n; i++) {\n        if (word[i] &gt;= 'A' && word[i] &lt;= 'Z') {\n            capCount++;\n        }\n    }\n    return capCount === 0 || capCount === n || (capCount === 1 && word[0] &gt;= 'A' && word[0] &lt;= 'Z');\n}" },]} />

**Complexity**
- Time: O(n) — We traverse the string once.
- Space: O(1) — We only use a few integer variables.
- Notes: Efficient and avoids creating new string objects.

---
## Approach 3: Regular Expression

**Intuition**
The problem can be modeled as a pattern matching exercise. We can define a regular expression that matches the three valid patterns: all uppercase, all lowercase, or title case.

**Steps**
- Construct a regex pattern that matches the string start to end.
- The pattern should match: `[A-Z]*` (all caps), `[a-z]*` (all lower), or `[A-Z][a-z]*` (title case).
- Test the input word against this pattern.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import re\n\nclass Solution:\n    def detectCapitalUse(self, word: str) -&gt; bool:\n        return bool(re.fullmatch(r'[A-Z]*|[a-z]*|[A-Z][a-z]*', word))" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean detectCapitalUse(String word) {\n        return word.matches(\"[A-Z]*|[a-z]*|[A-Z][a-z]*\");\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;regex&gt;\n#include &lt;string&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    bool detectCapitalUse(string word) {\n        regex pattern(\"[A-Z]*|[a-z]*|[A-Z][a-z]*\");\n        return regex_match(word, pattern);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} word\n * @return {boolean}\n */\nvar detectCapitalUse = function(word) {\n    return /^[A-Z]*$|^[a-z]*$|^[A-Z][a-z]*$/.test(word);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function detectCapitalUse(word: string): boolean {\n    return /^[A-Z]*$|^[a-z]*$|^[A-Z][a-z]*$/.test(word);\n}" },]} />

**Complexity**
- Time: O(n) — Regex engines generally scan the string.
- Space: O(1) — Constant space for the regex pattern structure.
- Notes: Concise one-liner, but regex can be slower than simple iteration for very small strings and harder to debug for beginners.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Detect Capital Solutions","datePublished":"2024-01-14","description":"Determine if a word uses capital letters correctly: all uppercase, all lowercase, or only the first letter uppercase.","keywords":"LeetCode, Detect Capital, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Built-in String Methods approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string, as standard methods iterate through the characters."}},{"@type":"Question","name":"What is the space complexity of the Iterative Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use a counter variable and do not allocate extra space proportional to the input size."}},{"@type":"Question","name":"How does the Regular Expression approach solve the problem?","acceptedAnswer":{"@type":"Answer","text":"It uses a regex pattern to match the string against three valid formats: all uppercase, all lowercase, or title case."}}]}).replace(/</g,'\u003c')}
</script>