---
title: "Add Two Integers"
summary: "Given two integers num1 and num2, return the sum of the two integers."
date: "Sep 22, 2024"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 88.00% | **Paid:** No
> **Topics:** Math

Given two integers num1 and num2, return the sum of the two integers.

- Examples
- Constraints
- [Direct Addition](#direct-addition)
- [Bitwise Manipulation](#bitwise-manipulation)

## Examples

```text
Input: num1 = 12, num2 = 5
Output: 17
Explanation: num1 + num2 = 12 + 5 = 17
```

```text
Input: num1 = -10, num2 = 4
Output: -6
Explanation: num1 + num2 = -10 + 4 = -6
```

## Constraints

```text
-100 <= num1, num2 <= 100
```

---
## Direct Addition

**Intuition**
The most straightforward way to add two integers is to use the built-in addition operator provided by the programming language.

**Steps**
- Return the result of `num1 + num2`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sum(self, num1: int, num2: int) -&gt; int:\n        return num1 + num2" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sum(int num1, int num2) {\n        return num1 + num2;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sum(int num1, int num2) {\n        return num1 + num2;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var sum = function(num1, num2) {\n    return num1 + num2;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sum(num1: number, num2: number): number {\n    return num1 + num2;\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This is the optimal solution for readability and performance.

---
## Bitwise Manipulation

**Intuition**
We can simulate addition using bitwise operators. The XOR (`^`) operation handles the sum without carry, while the AND (`&`) operation followed by a left shift (`&lt;&lt;`) calculates the carry. We repeat this until there is no carry left.

**Steps**
- While `num2` is not 0:
    - Calculate the carry where both bits are 1: `carry = num1 & num2`.
    - Calculate the sum without carry: `num1 = num1 ^ num2`.
    - Shift the carry to the left by 1 bit: `num2 = carry &lt;&lt; 1`.
- Return `num1`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sum(self, num1: int, num2: int) -&gt; int:\n        while num2 != 0:\n            carry = num1 & num2\n            num1 = num1 ^ num2\n            num2 = carry &lt;&lt; 1\n        return num1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sum(int num1, int num2) {\n        while (num2 != 0) {\n            int carry = num1 & num2;\n            num1 = num1 ^ num2;\n            num2 = carry &lt;&lt; 1;\n        }\n        return num1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sum(int num1, int num2) {\n        while (num2 != 0) {\n            int carry = num1 & num2;\n            num1 = num1 ^ num2;\n            num2 = carry &lt;&lt; 1;\n        }\n        return num1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var sum = function(num1, num2) {\n    while (num2 !== 0) {\n        let carry = num1 & num2;\n        num1 = num1 ^ num2;\n        num2 = carry &lt;&lt; 1;\n    }\n    return num1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sum(num1: number, num2: number): number {\n    while (num2 !== 0) {\n        let carry = num1 & num2;\n        num1 = num1 ^ num2;\n        num2 = carry &lt;&lt; 1;\n    }\n    return num1;\n}" },]} />

**Complexity**
- Time: O(1) - The loop runs at most 32 times (for 32-bit integers).
- Space: O(1)
- Notes: This approach is useful for understanding low-level arithmetic but is generally slower than direct addition.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Add Two Integers Solutions","datePublished":"2024-09-22","description":"Given two integers num1 and num2, return the sum of the two integers.","keywords":"LeetCode, Add Two Integers, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Direct Addition approach?","acceptedAnswer":{"@type":"Answer","text":"O(1)"}},{"@type":"Question","name":"What is the space complexity of the Bitwise Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(1)"}}]}).replace(/</g,'\u003c')}
</script>