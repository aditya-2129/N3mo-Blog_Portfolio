---
title: "Select Data"
summary: "Select specific columns (name and age) from a DataFrame containing student information."
date: "May 16, 2025"
tags:
  - Pandas
  - Data-Analysis
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 78.50% | **Paid:** No
> **Topics:** N/A

DataFrame students
+-------------+--------+
| Column Name | Type   |
+-------------+--------+
| student_id  | int    |
| name        | object |
| age         | int    |
+-------------+--------+
Write a solution to select the name and age of the student.

The result format is in the following example.

- Examples
- Constraints
- [Direct Column Selection](#direct-column-selection)
- [Using loc Accessor](#using-loc-accessor)
- [Using filter Method](#using-filter-method)

## Examples

### Example 1:
```text
Input:
+------------+---------+-----+
| student_id | name    | age |
+------------+---------+-----+
| 1          | Alice   | 20  |
| 2          | Bob     | 21  |
| 3          | Charlie | 22  |
+------------+---------+-----+
Output:
+---------+-----+
| name    | age |
+---------+-----+
| Alice   | 20  |
| Bob     | 21  |
| Charlie | 22  |
+---------+-----+
```

## Constraints

```text
The DataFrame students contains columns student_id, name, and age.
```

---

## Direct Column Selection

**Intuition**
The most straightforward way to select specific columns in Pandas is by passing a list of column names to the indexing operator `[]`. This returns a new DataFrame containing only the specified columns in the order they appear in the list.

**Steps**
- Access the DataFrame using the list of desired column names `['name', 'age']`.
- Return the resulting DataFrame.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import pandas as pd\n\ndef selectData(students: pd.DataFrame) -&gt; pd.DataFrame:\n    return students[['name', 'age']]" },
{ name: "Java", lang: "java", code: "import java.util.*;\nimport java.util.stream.*;\n\nclass Solution {\n    public List&lt;List&lt;String&gt;&gt; selectData(List&lt;List&lt;String&gt;&gt; students) {\n        // Assuming input is a list of rows, where index 1 is name and index 2 is age\n        return students.stream()\n            .map(row -&gt; Arrays.asList(row.get(1), row.get(2)))\n            .collect(Collectors.toList());\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;utility&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;vector&lt;string&gt;&gt; selectData(vector&lt;vector&lt;string&gt;&gt;& students) {\n        vector&lt;vector&lt;string&gt;&gt; result;\n        // Assuming index 1 is name and index 2 is age\n        for (const auto& row : students) {\n            result.push_back({row[1], row[2]});\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {Array&lt;Array&lt;string&gt;&gt;} students\n * @return {Array&lt;Array&lt;string&gt;&gt;}\n */\nvar selectData = function(students) {\n    // Assuming index 1 is name and index 2 is age\n    return students.map(row =&gt; [row[1], row[2]]);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function selectData(students: string[][]): string[][] {\n    // Assuming index 1 is name and index 2 is age\n    return students.map(row =&gt; [row[1], row[2]]);\n}" },]} />

**Complexity**
- Time: O(n) where n is the number of rows.
- Space: O(n) to store the resulting DataFrame.
- Notes: This is the most idiomatic and concise way to select columns in Pandas.

## Using loc Accessor

**Intuition**
The `.loc` accessor is primarily label-based, but can also be used with boolean arrays. It allows selecting rows and columns by label. Here we select all rows (`:`) and specific columns by their labels.

**Steps**
- Use `.loc` with a slice `:` to select all rows.
- Pass the list of column names `['name', 'age']` to select specific columns.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import pandas as pd\n\ndef selectData(students: pd.DataFrame) -&gt; pd.DataFrame:\n    return students.loc[:, ['name', 'age']]" },
{ name: "Java", lang: "java", code: "import java.util.*;\nimport java.util.stream.*;\n\nclass Solution {\n    public List&lt;List&lt;String&gt;&gt; selectData(List&lt;List&lt;String&gt;&gt; students) {\n        List&lt;List&lt;String&gt;&gt; result = new ArrayList&lt;&gt;();\n        for (List&lt;String&gt; row : students) {\n            // Explicitly selecting columns by index/label logic\n            List&lt;String&gt; selected = new ArrayList&lt;&gt;();\n            selected.add(row.get(1)); // name\n            selected.add(row.get(2)); // age\n            result.add(selected);\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;vector&lt;string&gt;&gt; selectData(vector&lt;vector&lt;string&gt;&gt;& students) {\n        vector&lt;vector&lt;string&gt;&gt; result;\n        result.reserve(students.size());\n        for (auto& row : students) {\n            // Selecting specific indices corresponding to labels\n            result.push_back({row[1], row[2]});\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {Array&lt;Array&lt;string&gt;&gt;} students\n * @return {Array&lt;Array&lt;string&gt;&gt;}\n */\nvar selectData = function(students) {\n    const result = [];\n    for (let i = 0; i &lt; students.length; i++) {\n        const row = students[i];\n        // Selecting specific indices\n        result.push([row[1], row[2]]);\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function selectData(students: string[][]): string[][] {\n    const result: string[][] = [];\n    for (const row of students) {\n        // Selecting specific indices\n        result.push([row[1], row[2]]);\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Useful when mixing row and column selection logic, though slightly more verbose than direct indexing for simple column selection.

## Using filter Method

**Intuition**
The `filter` method in Pandas allows selecting subsets of the DataFrame based on labels (either row index or column names). It is particularly useful if you want to select columns based on a pattern or regex, but works with exact names too.

**Steps**
- Call the `filter` method on the DataFrame.
- Use the `items` parameter (or `like`/`regex`) with the list `['name', 'age']`.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import pandas as pd\n\ndef selectData(students: pd.DataFrame) -&gt; pd.DataFrame:\n    return students.filter(items=['name', 'age'])" },
{ name: "Java", lang: "java", code: "import java.util.*;\nimport java.util.stream.*;\n\nclass Solution {\n    public List&lt;List&lt;String&gt;&gt; selectData(List&lt;List&lt;String&gt;&gt; students) {\n        // Simulating filter by checking indices\n        return students.stream()\n            .map(row -&gt; {\n                List&lt;String&gt; filteredRow = new ArrayList&lt;&gt;();\n                if (row.size() &gt; 1) filteredRow.add(row.get(1)); // name\n                if (row.size() &gt; 2) filteredRow.add(row.get(2)); // age\n                return filteredRow;\n            })\n            .collect(Collectors.toList());\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    vector&lt;vector&lt;string&gt;&gt; selectData(vector&lt;vector&lt;string&gt;&gt;& students) {\n        vector&lt;vector&lt;string&gt;&gt; result;\n        for (const auto& row : students) {\n            vector&lt;string&gt; filteredRow;\n            // Filter specific columns\n            if (row.size() &gt; 1) filteredRow.push_back(row[1]);\n            if (row.size() &gt; 2) filteredRow.push_back(row[2]);\n            result.push_back(filteredRow);\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {Array&lt;Array&lt;string&gt;&gt;} students\n * @return {Array&lt;Array&lt;string&gt;&gt;}\n */\nvar selectData = function(students) {\n    // Simulating filter logic\n    return students.map(row =&gt; {\n        const newRow = [];\n        if (row[1] !== undefined) newRow.push(row[1]);\n        if (row[2] !== undefined) newRow.push(row[2]);\n        return newRow;\n    });\n};" },
{ name: "TypeScript", lang: "typescript", code: "function selectData(students: string[][]): string[][] {\n    // Simulating filter logic\n    return students.map(row =&gt; {\n        const newRow: string[] = [];\n        if (row[1] !== undefined) newRow.push(row[1]);\n        if (row[2] !== undefined) newRow.push(row[2]);\n        return newRow;\n    });\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Overkill for simple selection but very powerful for selecting columns matching patterns (e.g., `like='name'`).

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Select Data Solutions","datePublished":"2025-05-16","description":"Select specific columns (name and age) from a DataFrame containing student information.","keywords":"LeetCode, Select Data, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Direct Column Selection?","acceptedAnswer":{"@type":"Answer","text":"O(n), where n is the number of rows in the DataFrame."}},{"@type":"Question","name":"What is the space complexity of using the loc accessor?","acceptedAnswer":{"@type":"Answer","text":"O(n), as a new DataFrame is created containing the selected columns."}},{"@type":"Question","name":"When should you use the filter method?","acceptedAnswer":{"@type":"Answer","text":"The filter method is best used when selecting columns based on patterns or regex, though it works for exact names too."}}]}).replace(/</g,'\u003c')}
</script>