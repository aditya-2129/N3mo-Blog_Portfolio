---
title: "Find Subarrays With Equal Sum"
summary: "Check if there exist two distinct subarrays of length 2 with equal sum in an array."
date: "Dec 26, 2025"
tags:
  - Array
  - Hash Table
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 67.00% | **Paid:** No
> **Topics:** Array, Hash Table

Given a 0-indexed integer array nums, determine whether there exist two subarrays of length 2 with equal sum. Note that the two subarrays must begin at different indices.

Return true if these subarrays exist, and false otherwise.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Hash Set](#hash-set)

## Examples

### Example 1
```text
Input: nums = [4,2,4]
Output: true
Explanation: The subarrays with elements [4,2] and [2,4] have the same sum of 6.
```

### Example 2
```text
Input: nums = [1,2,3,4,5]
Output: false
Explanation: No two subarrays of size 2 have the same sum.
```

### Example 3
```text
Input: nums = [0,0,0]
Output: true
Explanation: The subarrays [nums[0],nums[1]] and [nums[1],nums[2]] have the same sum of 0.
```

## Constraints

```text
2 <= nums.length <= 1000
-10⁹ <= nums[i] <= 10⁹
```

---
## Brute Force

**Intuition**
Compare every pair of subarrays of length 2 to check if any two have the same sum.

**Steps**
- Iterate through all possible starting indices for the first subarray.
- For each first subarray, iterate through all possible starting indices for the second subarray.
- Compare the sums and return true if they match.
- Return false if no matching pairs found.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def findSubarrays(self, nums: List[int]) -> bool:
        n = len(nums)
        for i in range(n - 1):
            sum1 = nums[i] + nums[i + 1]
            for j in range(i + 1, n - 1):
                sum2 = nums[j] + nums[j + 1]
                if sum1 == sum2:
                    return True
        return False` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean findSubarrays(int[] nums) {
        int n = nums.length;
        for (int i = 0; i &lt; n - 1; i++) {
            int sum1 = nums[i] + nums[i + 1];
            for (int j = i + 1; j &lt; n - 1; j++) {
                int sum2 = nums[j] + nums[j + 1];
                if (sum1 == sum2) {
                    return true;
                }
            }
        }
        return false;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool findSubarrays(vector<int>& nums) {
        int n = nums.size();
        for (int i = 0; i &lt; n - 1; i++) {
            int sum1 = nums[i] + nums[i + 1];
            for (int j = i + 1; j &lt; n - 1; j++) {
                int sum2 = nums[j] + nums[j + 1];
                if (sum1 == sum2) {
                    return true;
                }
            }
        }
        return false;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @return {boolean}
 */
var findSubarrays = function(nums) {
    const n = nums.length;
    for (let i = 0; i &lt; n - 1; i++) {
        const sum1 = nums[i] + nums[i + 1];
        for (let j = i + 1; j &lt; n - 1; j++) {
            const sum2 = nums[j] + nums[j + 1];
            if (sum1 === sum2) {
                return true;
            }
        }
    }
    return false;
};` },
{ name: "TypeScript", lang: "typescript", code: `function findSubarrays(nums: number[]): boolean {
    const n = nums.length;
    for (let i = 0; i &lt; n - 1; i++) {
        const sum1 = nums[i] + nums[i + 1];
        for (let j = i + 1; j &lt; n - 1; j++) {
            const sum2 = nums[j] + nums[j + 1];
            if (sum1 === sum2) {
                return true;
            }
        }
    }
    return false;
}` },]} />

**Complexity**
- Time: O(n²)
- Space: O(1)
- Notes: Simple but inefficient for large arrays.

---
## Hash Set

**Intuition**
Store all subarray sums in a hash set. If we encounter a sum that already exists, we found two subarrays with equal sum.

**Steps**
- Create an empty hash set to store subarray sums.
- Iterate through the array, calculating the sum of each subarray of length 2.
- If a sum is already in the set, return true.
- Otherwise, add the sum to the set.
- Return false if no duplicate sums found.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def findSubarrays(self, nums: List[int]) -> bool:
        seen = set()
        for i in range(len(nums) - 1):
            subarray_sum = nums[i] + nums[i + 1]
            if subarray_sum in seen:
                return True
            seen.add(subarray_sum)
        return False` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean findSubarrays(int[] nums) {
        Set<Long> seen = new HashSet&lt;&gt;();
        for (int i = 0; i &lt; nums.length - 1; i++) {
            long sum = (long) nums[i] + nums[i + 1];
            if (seen.contains(sum)) {
                return true;
            }
            seen.add(sum);
        }
        return false;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool findSubarrays(vector<int>& nums) {
        unordered_set<long long> seen;
        for (int i = 0; i &lt; nums.size() - 1; i++) {
            long long sum = (long long) nums[i] + nums[i + 1];
            if (seen.count(sum)) {
                return true;
            }
            seen.insert(sum);
        }
        return false;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @return {boolean}
 */
var findSubarrays = function(nums) {
    const seen = new Set();
    for (let i = 0; i &lt; nums.length - 1; i++) {
        const sum = nums[i] + nums[i + 1];
        if (seen.has(sum)) {
            return true;
        }
        seen.add(sum);
    }
    return false;
};` },
{ name: "TypeScript", lang: "typescript", code: `function findSubarrays(nums: number[]): boolean {
    const seen = new Set<number>();
    for (let i = 0; i &lt; nums.length - 1; i++) {
        const sum = nums[i] + nums[i + 1];
        if (seen.has(sum)) {
            return true;
        }
        seen.add(sum);
    }
    return false;
}` },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Optimal solution with linear time complexity.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find Subarrays With Equal Sum Solutions","datePublished":"2025-12-26","description":"Check if there exist two distinct subarrays of length 2 with equal sum in an array.","keywords":"LeetCode, Find Subarrays With Equal Sum, Easy, Array, Hash Table"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n²) where n is the length of the array."}},{"@type":"Question","name":"What is the time complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Hash Set approach is O(n) where n is the length of the array."}},{"@type":"Question","name":"What is the space complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity of the Hash Set approach is O(n) in the worst case to store all subarray sums."}}]}).replace(/</g,'\u003c')}
</script>