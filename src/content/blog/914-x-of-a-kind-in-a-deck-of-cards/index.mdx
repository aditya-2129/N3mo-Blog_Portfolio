---
title: "X of a Kind in a Deck of Cards"
summary: "Check if a deck of cards can be partitioned into groups of equal size where each group consists of identical cards."
date: "Feb 29, 2024"
tags:
  - Array
  - Hash Table
  - Math
  - Counting
  - Number Theory
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 30.30% | **Paid:** No
> **Topics:** Array, Hash Table, Math, Counting, Number Theory

In a deck of cards, each card has an integer written on it.

Return true if and only if you can choose X &gt;= 2 such that it is possible to split the entire deck into 1 or more groups of cards, where:

Each group has exactly X cards.
All the cards in each group have the same integer.

## Examples

### Example 1

**Input:**
```text
deck = [1,2,3,4,4,3,2,1]
```

**Output:**
```text
true
```

**Explanation:**
Possible partition [1,1],[2,2],[3,3],[4,4].

### Example 2

**Input:**
```text
deck = [1,1,1,2,2,2,3,3]
```

**Output:**
```text
false
```

**Explanation:**
No possible partition.

## Constraints

```text
1 <= deck.length <= 10⁴
0 <= deck[i] < 10⁴
```

- Examples
- Constraints
- [Approach 1: Counting and GCD](#approach-1-counting-and-gcd)
- [Approach 2: Brute Force Group Size](#approach-2-brute-force-group-size)

## Examples

```text
Input: deck = [1,2,3,4,4,3,2,1]
Output: true
Explanation: Possible partition [1,1],[2,2],[3,3],[4,4].
```

```text
Input: deck = [1,1,1,2,2,2,3,3]
Output: false
Explanation: No possible partition.
```

```text
Input: deck = [1]
Output: false
Explanation: No possible partition.
```

## Constraints

```text
1 <= deck.length <= 10⁴
0 <= deck[i] < 10⁴
```

---
## Approach 1: Counting and GCD

**Intuition**
If we count the frequency of each card, the group size X must divide the count of every card type. The largest possible X is the Greatest Common Divisor (GCD) of all frequencies. If the GCD is at least 2, a valid partition exists.

**Steps**
- Count the frequency of each number in the deck using a hash map.
- Compute the GCD of all the frequency values.
- Return true if the GCD is greater than or equal to 2, otherwise return false.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from collections import Counter\nfrom math import gcd\nfrom functools import reduce\nfrom typing import List\n\nclass Solution:\n    def hasGroupsSizeX(self, deck: List[int]) -&gt; bool:\n        counts = Counter(deck).values()\n        return reduce(gcd, counts) &gt;= 2\n" },
{ name: "Java", lang: "java", code: "import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public boolean hasGroupsSizeX(int[] deck) {\n        Map&lt;Integer, Integer&gt; counts = new HashMap&lt;&gt;();\n        for (int card : deck) {\n            counts.put(card, counts.getOrDefault(card, 0) + 1);\n        }\n        int currentGcd = -1;\n        for (int count : counts.values()) {\n            if (currentGcd == -1) {\n                currentGcd = count;\n            } else {\n                currentGcd = gcd(currentGcd, count);\n            }\n            if (currentGcd == 1) {\n                return false;\n            }\n        }\n        return currentGcd &gt;= 2;\n    }\n\n    private int gcd(int a, int b) {\n        while (b != 0) {\n            int temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_map&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    bool hasGroupsSizeX(vector&lt;int&gt;& deck) {\n        unordered_map&lt;int, int&gt; counts;\n        for (int card : deck) {\n            counts[card]++;\n        }\n        int currentGcd = 0;\n        for (auto const& [key, val] : counts) {\n            currentGcd = __gcd(currentGcd, val);\n        }\n        return currentGcd &gt;= 2;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var hasGroupsSizeX = function(deck) {\n    const counts = new Map();\n    for (const card of deck) {\n        counts.set(card, (counts.get(card) || 0) + 1);\n    }\n    let currentGcd = 0;\n    const gcd = (a, b) =&gt; {\n        while (b !== 0) {\n            let temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    };\n    for (const count of counts.values()) {\n        currentGcd = gcd(currentGcd, count);\n        if (currentGcd === 1) {\n            return false;\n        }\n    }\n    return currentGcd &gt;= 2;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function hasGroupsSizeX(deck: number[]): boolean {\n    const counts = new Map&lt;number, number&gt;();\n    for (const card of deck) {\n        counts.set(card, (counts.get(card) || 0) + 1);\n    }\n    let currentGcd = 0;\n    const gcd = (a: number, b: number): number =&gt; {\n        while (b !== 0) {\n            let temp = b;\n            b = a % b;\n            a = temp;\n        }\n        return a;\n    };\n    for (const count of counts.values()) {\n        currentGcd = gcd(currentGcd, count);\n        if (currentGcd === 1) {\n            return false;\n        }\n    }\n    return currentGcd &gt;= 2;\n}\n" },]} />

**Complexity**
- Time: O(N * log(M)), where N is the size of the deck and M is the maximum frequency.
- Space: O(N) to store the frequency counts.
- Notes: This is the most efficient approach, leveraging number theory to avoid unnecessary iterations.

---
## Approach 2: Brute Force Group Size

**Intuition**
Since the group size X must be at least 2 and cannot exceed the minimum frequency of any card, we can iterate through all possible values of X from 2 up to the minimum frequency. For each X, we check if it divides all card frequencies.

**Steps**
- Count the frequency of each number.
- Find the minimum frequency among all cards.
- Iterate X from 2 to the minimum frequency (inclusive).
- For each X, check if every frequency is divisible by X.
- If such an X is found, return true. If the loop finishes without success, return false.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from collections import Counter\nfrom typing import List\n\nclass Solution:\n    def hasGroupsSizeX(self, deck: List[int]) -&gt; bool:\n        if len(deck) &lt; 2:\n            return False\n        counts = Counter(deck)\n        min_count = min(counts.values())\n        if min_count &lt; 2:\n            return False\n        for x in range(2, min_count + 1):\n            if all(c % x == 0 for c in counts.values()):\n                return True\n        return False\n" },
{ name: "Java", lang: "java", code: "import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public boolean hasGroupsSizeX(int[] deck) {\n        if (deck.length &lt; 2) return false;\n        Map&lt;Integer, Integer&gt; counts = new HashMap&lt;&gt;();\n        for (int card : deck) {\n            counts.put(card, counts.getOrDefault(card, 0) + 1);\n        }\n        int minCount = Integer.MAX_VALUE;\n        for (int count : counts.values()) {\n            minCount = Math.min(minCount, count);\n        }\n        if (minCount &lt; 2) return false;\n\n        for (int x = 2; x &lt;= minCount; x++) {\n            boolean valid = true;\n            for (int count : counts.values()) {\n                if (count % x != 0) {\n                    valid = false;\n                    break;\n                }\n            }\n            if (valid) return true;\n        }\n        return false;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_map&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    bool hasGroupsSizeX(vector&lt;int&gt;& deck) {\n        if (deck.size() &lt; 2) return false;\n        unordered_map&lt;int, int&gt; counts;\n        for (int card : deck) {\n            counts[card]++;\n        }\n        int minCount = INT_MAX;\n        for (auto const& [key, val] : counts) {\n            minCount = min(minCount, val);\n        }\n        if (minCount &lt; 2) return false;\n\n        for (int x = 2; x &lt;= minCount; ++x) {\n            bool valid = true;\n            for (auto const& [key, val] : counts) {\n                if (val % x != 0) {\n                    valid = false;\n                    break;\n                }\n            }\n            if (valid) return true;\n        }\n        return false;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var hasGroupsSizeX = function(deck) {\n    if (deck.length &lt; 2) return false;\n    const counts = new Map();\n    for (const card of deck) {\n        counts.set(card, (counts.get(card) || 0) + 1);\n    }\n    let minCount = Infinity;\n    for (const count of counts.values()) {\n        minCount = Math.min(minCount, count);\n    }\n    if (minCount &lt; 2) return false;\n\n    for (let x = 2; x &lt;= minCount; x++) {\n        let valid = true;\n        for (const count of counts.values()) {\n            if (count % x !== 0) {\n                valid = false;\n                break;\n            }\n        }\n        if (valid) return true;\n    }\n    return false;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function hasGroupsSizeX(deck: number[]): boolean {\n    if (deck.length &lt; 2) return false;\n    const counts = new Map&lt;number, number&gt;();\n    for (const card of deck) {\n        counts.set(card, (counts.get(card) || 0) + 1);\n    }\n    let minCount = Infinity;\n    for (const count of counts.values()) {\n        minCount = Math.min(minCount, count);\n    }\n    if (minCount &lt; 2) return false;\n\n    for (let x = 2; x &lt;= minCount; x++) {\n        let valid = true;\n        for (const count of counts.values()) {\n            if (count % x !== 0) {\n                valid = false;\n                break;\n            }\n        }\n        if (valid) return true;\n    }\n    return false;\n}\n" },]} />

**Complexity**
- Time: O(N * M), where N is the size of the deck and M is the minimum frequency.
- Space: O(N) to store the frequency counts.
- Notes: This approach is straightforward but can be slower than the GCD method if the minimum frequency is large.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode X of a Kind in a Deck of Cards Solutions","datePublished":"2024-02-29","description":"Check if a deck of cards can be partitioned into groups of equal size where each group consists of identical cards.","keywords":"LeetCode, X of a Kind in a Deck of Cards, Easy, Array, Hash Table, Math, Counting, Number Theory"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Counting and GCD approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * log(M)), where N is the size of the deck and M is the maximum frequency of any card."}},{"@type":"Question","name":"What is the space complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(N) to store the frequency counts of the cards in the deck."}}]}).replace(/</g,'\u003c')}
</script>