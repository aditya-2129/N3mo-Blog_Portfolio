---
title: "Find Nearest Point That Has the Same X or Y Coordinate"
summary: "Find the point in an array that shares an x or y coordinate with a target point and has the smallest Manhattan distance."
date: "May 28, 2025"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 70.00% | **Paid:** No
> **Topics:** Array

You are given two integers, x and y, which represent your current location on a Cartesian grid: (x, y). You are also given an array points where points[i] = [ai, bi] represents that a point exists at (ai, bi). A point is valid if it shares the same x-coordinate or the same y-coordinate as your location.

Return the index (0-indexed) of the valid point with the smallest Manhattan distance from your current location. If there are multiple, return the valid point with the smallest index. If there are no valid points, return -1.

The Manhattan distance between two points (x1, y1) and (x2, y2) is abs(x1 - x2) + abs(y1 - y2).

- Examples
- Constraints
- [Linear Scan](#linear-scan)
- [Functional Approach](#functional-approach)

## Examples

**Example 1:**

```text
Input: x = 3, y = 4, points = [[1,2],[3,1],[2,4],[2,3],[4,4]]
Output: 2
Explanation: The valid points are [3,1], [2,4], and [4,4].
The point with the smallest Manhattan distance is [2,4] with a distance of 1.
```

**Example 2:**

```text
Input: x = 3, y = 4, points = [[3,4]]
Output: 0
Explanation: The point [3,4] is valid and has the smallest distance of 0.
```

**Example 3:**

```text
Input: x = 3, y = 4, points = [[2,3]]
Output: -1
Explanation: There are no valid points.
```

## Constraints

```text
1 <= points.length <= 10⁴
points[i].length == 2
1 <= x, y, ai, bi <= 10⁴
```

---
## Linear Scan

**Intuition**
We iterate through the list of points once. For each point, we check if it is valid (shares x or y). If it is, we calculate its Manhattan distance to the target. We keep track of the smallest distance found so far and the corresponding index.

**Steps**
- Initialize `min_dist` to infinity and `ans` to -1.
- Iterate through the `points` array using the index `i`.
- For each point `(a, b)`, check if `a == x` or `b == y`.
- If valid, calculate `dist = abs(a - x) + abs(b - y)`.
- If `dist &lt; min_dist`, update `min_dist` to `dist` and `ans` to `i`.
- Return `ans` after the loop finishes.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def nearestValidPoint(self, x: int, y: int, points: list[list[int]]) -&gt; int:\n        min_dist = float('inf')\n        ans = -1\n        for i, (a, b) in enumerate(points):\n            if a == x or b == y:\n                dist = abs(a - x) + abs(b - y)\n                if dist &lt; min_dist:\n                    min_dist = dist\n                    ans = i\n        return ans" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int nearestValidPoint(int x, int y, int[][] points) {\n        int minDist = Integer.MAX_VALUE;\n        int ans = -1;\n        for (int i = 0; i &lt; points.length; i++) {\n            int a = points[i][0];\n            int b = points[i][1];\n            if (a == x || b == y) {\n                int dist = Math.abs(a - x) + Math.abs(b - y);\n                if (dist &lt; minDist) {\n                    minDist = dist;\n                    ans = i;\n                }\n            }\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int nearestValidPoint(int x, int y, vector&lt;vector&lt;int&gt;&gt;& points) {\n        int minDist = INT_MAX;\n        int ans = -1;\n        for (int i = 0; i &lt; points.size(); i++) {\n            int a = points[i][0];\n            int b = points[i][1];\n            if (a == x || b == y) {\n                int dist = abs(a - x) + abs(b - y);\n                if (dist &lt; minDist) {\n                    minDist = dist;\n                    ans = i;\n                }\n            }\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var nearestValidPoint = function(x, y, points) {\n    let minDist = Infinity;\n    let ans = -1;\n    for (let i = 0; i &lt; points.length; i++) {\n        const [a, b] = points[i];\n        if (a === x || b === y) {\n            const dist = Math.abs(a - x) + Math.abs(b - y);\n            if (dist &lt; minDist) {\n                minDist = dist;\n                ans = i;\n            }\n        }\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function nearestValidPoint(x: number, y: number, points: number[][]): number {\n    let minDist = Infinity;\n    let ans = -1;\n    for (let i = 0; i &lt; points.length; i++) {\n        const [a, b] = points[i];\n        if (a === x || b === y) {\n            const dist = Math.abs(a - x) + Math.abs(b - y);\n            if (dist &lt; minDist) {\n                minDist = dist;\n                ans = i;\n            }\n        }\n    }\n    return ans;\n}" },]} />

**Complexity**
- Time: O(n), where n is the number of points. We visit each point exactly once.
- Space: O(1). We only use a few variables for storage.
- Notes: This is the most optimal approach for this problem.

---
## Functional Approach

**Intuition**
We utilize functional programming constructs like `filter`, `map`, and `reduce` (or their language-specific equivalents) to process the array. This approach filters for valid points first, then finds the one with the minimum distance.

**Steps**
- Filter the array to keep only points where `x` or `y` matches.
- Transform the remaining points to include their original index and calculated distance.
- Reduce the collection to find the entry with the smallest distance (and smallest index in case of ties).
- Return the index or -1 if the filtered list is empty.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def nearestValidPoint(self, x: int, y: int, points: list[list[int]]) -&gt; int:\n        def get_dist(i):\n            a, b = points[i]\n            return abs(a - x) + abs(b - y)\n        \n        valid_indices = [i for i, (a, b) in enumerate(points) if a == x or b == y]\n        if not valid_indices:\n            return -1\n        return min(valid_indices, key=get_dist)" },
{ name: "Java", lang: "java", code: "import java.util.stream.*;\n\nclass Solution {\n    public int nearestValidPoint(int x, int y, int[][] points) {\n        return IntStream.range(0, points.length)\n            .filter(i -&gt; points[i][0] == x || points[i][1] == y)\n            .boxed()\n            .min(Comparator.comparingInt((Integer i) -&gt; Math.abs(points[i][0] - x) + Math.abs(points[i][1] - y))\n                  .thenComparingInt(i -&gt; i))\n            .orElse(-1);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;vector&gt;\n#include &lt;climits&gt;\n\nclass Solution {\npublic:\n    int nearestValidPoint(int x, int y, vector&lt;vector&lt;int&gt;&gt;& points) {\n        int ans = -1;\n        int min_dist = INT_MAX;\n        \n        for_each(points.begin(), points.end(), [&x, &y, &ans, &min_dist, &points](const vector&lt;int&gt;& p) {\n            int a = p[0];\n            int b = p[1];\n            if (a == x || b == y) {\n                int dist = abs(a - x) + abs(b - y);\n                if (dist &lt; min_dist) {\n                    min_dist = dist;\n                    // Calculate index based on pointer arithmetic or iterator\n                    ans = &p - &points[0]; \n                }\n            }\n        });\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var nearestValidPoint = function(x, y, points) {\n    let result = -1;\n    let minDist = Infinity;\n    \n    points.forEach((point, index) =&gt; {\n        const [a, b] = point;\n        if (a === x || b === y) {\n            const dist = Math.abs(a - x) + Math.abs(b - y);\n            if (dist &lt; minDist) {\n                minDist = dist;\n                result = index;\n            }\n        }\n    });\n    \n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function nearestValidPoint(x: number, y: number, points: number[][]): number {\n    let result = -1;\n    let minDist = Infinity;\n    \n    points.forEach((point, index) =&gt; {\n        const [a, b] = point;\n        if (a === x || b === y) {\n            const dist = Math.abs(a - x) + Math.abs(b - y);\n            if (dist &lt; minDist) {\n                minDist = dist;\n                result = index;\n            }\n        }\n    });\n    \n    return result;\n}" },]} />

**Complexity**
- Time: O(n). We still need to examine every element to filter or find the minimum.
- Space: O(n) in the worst case for languages that create intermediate arrays (like Python list comprehensions or Java Streams), though O(1) is possible with specific reductions.
- Notes: While often more concise, functional approaches can sometimes have higher constant factors or memory overhead compared to a simple loop.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find Nearest Point That Has the Same X or Y Coordinate Solutions","datePublished":"2025-05-28","description":"Find the point in an array that shares an x or y coordinate with a target point and has the smallest Manhattan distance.","keywords":"LeetCode, Find Nearest Point That Has the Same X or Y Coordinate, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of points, as we iterate through the list once."}},{"@type":"Question","name":"What is the space complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) since we only use a few variables to track the minimum distance and index."}},{"@type":"Question","name":"How is the Manhattan distance calculated?","acceptedAnswer":{"@type":"Answer","text":"The Manhattan distance between two points (x1, y1) and (x2, y2) is calculated as abs(x1 - x2) + abs(y1 - y2)."}}]}).replace(/</g,'\u003c')}
</script>