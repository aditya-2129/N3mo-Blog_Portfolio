---
title: "Concatenate Non-Zero Digits and Multiply by Sum I"
summary: "Given an integer n, return the product of the sum of its non-zero digits and the integer formed by concatenating its non-zero digits."
date: "May 08, 2025"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 55.90% | **Paid:** No
> **Topics:** Math

You are given a positive integer n.

Let sum be the sum of the non-zero digits of n.

Let concat be the integer formed by concatenating the non-zero digits of n in the same order they appear in n.

Return the product of sum and concat.

- Examples
- Constraints
- [String Manipulation](#string-manipulation)
- [Mathematical Extraction](#mathematical-extraction)

## Examples

### Example 1

**Input:**
```text
n = 10203004
```

**Output:**
```text
12340
```

**Explanation:**
The non-zero digits are 1, 2, 3, and 4. Thus, x = 1234.

The sum of digits is sum = 1 + 2 + 3 + 4 = 10.

Therefore, the answer is x * sum = 1234 * 10 = 12340.

### Example 2

**Input:**
```text
n = 1000
```

**Output:**
```text
1
```

**Explanation:**
The non-zero digit is 1, so x = 1 and sum = 1.

Therefore, the answer is x * sum = 1 * 1 = 1.

## Constraints

```text
1 <= n <= 10^9
```

---
## String Manipulation

**Intuition**
Convert the integer to a string to easily iterate over each digit, filter out zeros, and reconstruct the concatenated number.

**Steps**
- Convert the integer `n` to a string.
- Iterate through each character in the string.
- If the character is not '0', add its integer value to the sum and append it to a new string representing the concatenated number.
- Convert the concatenated string back to an integer.
- Return the product of the sum and the concatenated integer.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfDigitsAndConcat(self, n: int) -&gt; int:\n        s = str(n)\n        concat_str = \"\"\n        total = 0\n        \n        for ch in s:\n            if ch != '0':\n                total += int(ch)\n                concat_str += ch\n                \n        return total * int(concat_str)\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfDigitsAndConcat(int n) {\n        String s = String.valueOf(n);\n        StringBuilder sb = new StringBuilder();\n        int sum = 0;\n        \n        for (char c : s.toCharArray()) {\n            if (c != '0') {\n                sum += c - '0';\n                sb.append(c);\n            }\n        }\n        \n        return sum * Integer.parseInt(sb.toString());\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfDigitsAndConcat(int n) {\n        string s = to_string(n);\n        string concatStr = \"\";\n        int sum = 0;\n        \n        for (char c : s) {\n            if (c != '0') {\n                sum += c - '0';\n                concatStr += c;\n            }\n        }\n        \n        return sum * stoi(concatStr);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar sumOfDigitsAndConcat = function(n) {\n    let s = n.toString();\n    let concatStr = \"\";\n    let sum = 0;\n    \n    for (let c of s) {\n        if (c !== '0') {\n            sum += parseInt(c);\n            concatStr += c;\n        }\n    }\n    \n    return sum * parseInt(concatStr);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfDigitsAndConcat(n: number): number {\n    let s = n.toString();\n    let concatStr = \"\";\n    let sum = 0;\n    \n    for (let c of s) {\n        if (c !== '0') {\n            sum += parseInt(c);\n            concatStr += c;\n        }\n    }\n    \n    return sum * parseInt(concatStr);\n}\n" },]} />

**Complexity**
- Time: O(d) where d is the number of digits in n.
- Space: O(d) to store the string representation.
- Notes: Simple to implement but uses extra space for strings.

---
## Mathematical Extraction

**Intuition**
Extract digits using modulo and division operations to avoid string conversion overhead. Since modulo extracts digits from right to left, we build the number in reverse and then reverse it back.

**Steps**
- Initialize `sum`, `rev_concat`, and `temp` (copy of n) to 0.
- Loop while `temp` is greater than 0.
- Extract the last digit using `temp % 10`.
- If the digit is not 0, add it to `sum` and append it to `rev_concat` (`rev_concat = rev_concat * 10 + digit`).
- Remove the last digit by dividing `temp` by 10.
- Reverse `rev_concat` to get the correct `concat` order.
- Return the product of `sum` and `concat`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfDigitsAndConcat(self, n: int) -&gt; int:\n        rev_concat = 0\n        total = 0\n        temp = n\n        \n        while temp &gt; 0:\n            digit = temp % 10\n            if digit != 0:\n                total += digit\n                rev_concat = rev_concat * 10 + digit\n            temp //= 10\n            \n        # Reverse rev_concat to get correct order\n        concat = 0\n        while rev_concat &gt; 0:\n            concat = concat * 10 + rev_concat % 10\n            rev_concat //= 10\n            \n        return total * concat\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfDigitsAndConcat(int n) {\n        int revConcat = 0, sum = 0;\n        int temp = n;\n        \n        while (temp &gt; 0) {\n            int d = temp % 10;\n            if (d != 0) {\n                sum += d;\n                revConcat = revConcat * 10 + d;\n            }\n            temp /= 10;\n        }\n        \n        // Reverse revConcat to get correct order\n        int concat = 0;\n        while (revConcat &gt; 0) {\n            concat = concat * 10 + revConcat % 10;\n            revConcat /= 10;\n        }\n        \n        return sum * concat;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfDigitsAndConcat(int n) {\n        int revConcat = 0, sum = 0;\n        int temp = n;\n        \n        while (temp &gt; 0) {\n            int d = temp % 10;\n            if (d != 0) {\n                sum += d;\n                revConcat = revConcat * 10 + d;\n            }\n            temp /= 10;\n        }\n        \n        // Reverse revConcat to get correct order\n        int concat = 0;\n        while (revConcat &gt; 0) {\n            concat = concat * 10 + revConcat % 10;\n            revConcat /= 10;\n        }\n        \n        return sum * concat;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar sumOfDigitsAndConcat = function(n) {\n    let revConcat = 0, sum = 0;\n    let temp = n;\n    \n    while (temp &gt; 0) {\n        let d = temp % 10;\n        if (d !== 0) {\n            sum += d;\n            revConcat = revConcat * 10 + d;\n        }\n        temp = Math.floor(temp / 10);\n    }\n    \n    // Reverse revConcat to get correct order\n    let concat = 0;\n    while (revConcat &gt; 0) {\n        concat = concat * 10 + revConcat % 10;\n        revConcat = Math.floor(revConcat / 10);\n    }\n    \n    return sum * concat;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfDigitsAndConcat(n: number): number {\n    let revConcat = 0, sum = 0;\n    let temp = n;\n    \n    while (temp &gt; 0) {\n        let d = temp % 10;\n        if (d !== 0) {\n            sum += d;\n            revConcat = revConcat * 10 + d;\n        }\n        temp = Math.floor(temp / 10);\n    }\n    \n    // Reverse revConcat to get correct order\n    let concat = 0;\n    while (revConcat &gt; 0) {\n        concat = concat * 10 + revConcat % 10;\n        revConcat = Math.floor(revConcat / 10);\n    }\n    \n    return sum * concat;\n}\n" },]} />

**Complexity**
- Time: O(d) where d is the number of digits in n.
- Space: O(1) auxiliary space.
- Notes: More efficient space-wise as it avoids string allocations.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Concatenate Non-Zero Digits and Multiply by Sum I Solutions","datePublished":"2025-05-08","description":"Given an integer n, return the product of the sum of its non-zero digits and the integer formed by concatenating its non-zero digits.","keywords":"LeetCode, Concatenate Non-Zero Digits and Multiply by Sum I, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the String Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(d) where d is the number of digits in n."}},{"@type":"Question","name":"What is the space complexity of the Mathematical Extraction approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) auxiliary space."}}]}).replace(/</g,'\u003c')}
</script>