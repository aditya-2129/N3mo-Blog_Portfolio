---
title: "Alternating Digit Sum"
summary: "Calculate the alternating sum of digits in a positive integer, starting with addition for the leftmost digit."
date: "Jul 17, 2025"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 69.10% | **Paid:** No
> **Topics:** Math

You are given a positive integer n. Each digit of n is alternatingly summed as follows:
- The leftmost digit is added.
- The next digit is subtracted.
- The next digit is added.
- And so on.

Return the alternating digit sum of n.

## Examples

```text
Input: n = 521
Output: 4
Explanation: +5 - 2 + 1 = 4
```

```text
Input: n = 111
Output: 1
Explanation: +1 - 1 + 1 = 1
```

```text
Input: n = 886996
Output: 0
Explanation: +8 - 8 + 6 - 9 + 9 - 6 = 0
```

## Constraints

```text
1 <= n <= 10⁹
```

- [String Conversion](#string-conversion)
- [Mathematical - Reverse and Count](#mathematical-reverse-and-count)
- [Mathematical - Count Digits First](#mathematical-count-digits-first)
- [Array/Stack Approach](#arraystack-approach)

---
## String Conversion

**Intuition**
Convert the number to a string and iterate through each character, adding or subtracting based on the position index.

**Steps**
- Convert the integer to a string
- Iterate through each character with its index
- Add the digit value if index is even, subtract if index is odd
- Return the final result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def alternateDigitSum(self, n: int) -> int:
        s = str(n)
        result = 0
        for i, ch in enumerate(s):
            if i % 2 == 0:
                result += int(ch)
            else:
                result -= int(ch)
        return result` },
{ name: "Java", lang: "java", code: `class Solution {
    public int alternateDigitSum(int n) {
        String s = String.valueOf(n);
        int result = 0;
        for (int i = 0; i &lt; s.length(); i++) {
            int digit = s.charAt(i) - '0';
            if (i % 2 == 0) {
                result += digit;
            } else {
                result -= digit;
            }
        }
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int alternateDigitSum(int n) {
        string s = to_string(n);
        int result = 0;
        for (int i = 0; i &lt; s.length(); i++) {
            int digit = s[i] - '0';
            if (i % 2 == 0) {
                result += digit;
            } else {
                result -= digit;
            }
        }
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var alternateDigitSum = function(n) {
    const s = n.toString();
    let result = 0;
    for (let i = 0; i &lt; s.length; i++) {
        const digit = parseInt(s[i]);
        if (i % 2 === 0) {
            result += digit;
        } else {
            result -= digit;
        }
    }
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function alternateDigitSum(n: number): number {
    const s = n.toString();
    let result = 0;
    for (let i = 0; i &lt; s.length; i++) {
        const digit = parseInt(s[i]);
        if (i % 2 === 0) {
            result += digit;
        } else {
            result -= digit;
        }
    }
    return result;
}` },]} />

**Complexity**
- Time: O(log n) - number of digits in n
- Space: O(log n) - for storing the string representation
- Notes: Simple and readable, but uses extra space for string conversion

---
## Mathematical - Reverse and Count

**Intuition**
Reverse the number first, then process digits from right to left (which becomes left to right after reversal) with alternating signs.

**Steps**
- Reverse the number by extracting digits and building a new number
- Process the reversed number digit by digit
- Alternate between adding and subtracting each digit
- Return the final result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def alternateDigitSum(self, n: int) -> int:
        # Reverse the number
        rev = 0
        temp = n
        while temp &gt; 0:
            rev = rev * 10 + temp % 10
            temp //= 10
        
        # Calculate alternating sum
        result = 0
        sign = 1
        while rev &gt; 0:
            result += sign * (rev % 10)
            rev //= 10
            sign = -sign
        return result` },
{ name: "Java", lang: "java", code: `class Solution {
    public int alternateDigitSum(int n) {
        // Reverse the number
        int rev = 0;
        int temp = n;
        while (temp &gt; 0) {
            rev = rev * 10 + temp % 10;
            temp /= 10;
        }
        
        // Calculate alternating sum
        int result = 0;
        int sign = 1;
        while (rev &gt; 0) {
            result += sign * (rev % 10);
            rev /= 10;
            sign = -sign;
        }
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int alternateDigitSum(int n) {
        // Reverse the number
        int rev = 0;
        int temp = n;
        while (temp &gt; 0) {
            rev = rev * 10 + temp % 10;
            temp /= 10;
        }
        
        // Calculate alternating sum
        int result = 0;
        int sign = 1;
        while (rev &gt; 0) {
            result += sign * (rev % 10);
            rev /= 10;
            sign = -sign;
        }
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var alternateDigitSum = function(n) {
    // Reverse the number
    let rev = 0;
    let temp = n;
    while (temp &gt; 0) {
        rev = rev * 10 + temp % 10;
        temp = Math.floor(temp / 10);
    }
    
    // Calculate alternating sum
    let result = 0;
    let sign = 1;
    while (rev &gt; 0) {
        result += sign * (rev % 10);
        rev = Math.floor(rev / 10);
        sign = -sign;
    }
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function alternateDigitSum(n: number): number {
    // Reverse the number
    let rev = 0;
    let temp = n;
    while (temp &gt; 0) {
        rev = rev * 10 + temp % 10;
        temp = Math.floor(temp / 10);
    }
    
    // Calculate alternating sum
    let result = 0;
    let sign = 1;
    while (rev &gt; 0) {
        result += sign * (rev % 10);
        rev = Math.floor(rev / 10);
        sign = -sign;
    }
    return result;
}` },]} />

**Complexity**
- Time: O(log n) - two passes through the digits
- Space: O(1) - only uses a few integer variables
- Notes: Pure mathematical approach with constant space, but requires two passes

---
## Mathematical - Count Digits First

**Intuition**
Count the total number of digits first to determine the starting sign, then process from right to left with alternating signs.

**Steps**
- Count the number of digits in the number
- Determine starting sign: positive if odd number of digits, negative if even
- Process digits from right to left, alternating signs
- Return the final result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def alternateDigitSum(self, n: int) -> int:
        # Count digits
        temp = n
        num_digits = 0
        while temp &gt; 0:
            num_digits += 1
            temp //= 10
        
        # Calculate alternating sum from right to left
        result = 0
        sign = 1 if num_digits % 2 == 1 else -1
        while n &gt; 0:
            result += sign * (n % 10)
            n //= 10
            sign = -sign
        return result` },
{ name: "Java", lang: "java", code: `class Solution {
    public int alternateDigitSum(int n) {
        // Count digits
        int temp = n;
        int numDigits = 0;
        while (temp &gt; 0) {
            numDigits++;
            temp /= 10;
        }
        
        // Calculate alternating sum from right to left
        int result = 0;
        int sign = (numDigits % 2 == 1) ? 1 : -1;
        while (n &gt; 0) {
            result += sign * (n % 10);
            n /= 10;
            sign = -sign;
        }
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int alternateDigitSum(int n) {
        // Count digits
        int temp = n;
        int numDigits = 0;
        while (temp &gt; 0) {
            numDigits++;
            temp /= 10;
        }
        
        // Calculate alternating sum from right to left
        int result = 0;
        int sign = (numDigits % 2 == 1) ? 1 : -1;
        while (n &gt; 0) {
            result += sign * (n % 10);
            n /= 10;
            sign = -sign;
        }
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var alternateDigitSum = function(n) {
    // Count digits
    let temp = n;
    let numDigits = 0;
    while (temp &gt; 0) {
        numDigits++;
        temp = Math.floor(temp / 10);
    }
    
    // Calculate alternating sum from right to left
    let result = 0;
    let sign = (numDigits % 2 === 1) ? 1 : -1;
    while (n &gt; 0) {
        result += sign * (n % 10);
        n = Math.floor(n / 10);
        sign = -sign;
    }
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function alternateDigitSum(n: number): number {
    // Count digits
    let temp = n;
    let numDigits = 0;
    while (temp &gt; 0) {
        numDigits++;
        temp = Math.floor(temp / 10);
    }
    
    // Calculate alternating sum from right to left
    let result = 0;
    let sign = (numDigits % 2 === 1) ? 1 : -1;
    while (n &gt; 0) {
        result += sign * (n % 10);
        n = Math.floor(n / 10);
        sign = -sign;
    }
    return result;
}` },]} />

**Complexity**
- Time: O(log n) - two passes through the digits
- Space: O(1) - only uses a few integer variables
- Notes: Similar to reverse approach but avoids number reversal, still requires two passes

---
## Array/Stack Approach

**Intuition**
Extract all digits into an array, then process them in reverse order with alternating signs starting from positive.

**Steps**
- Extract all digits from right to left into an array
- Iterate through the array in reverse order (left to right)
- Alternate between adding and subtracting each digit
- Return the final result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def alternateDigitSum(self, n: int) -> int:
        digits = []
        while n &gt; 0:
            digits.append(n % 10)
            n //= 10
        
        result = 0
        sign = 1
        for i in range(len(digits) - 1, -1, -1):
            result += sign * digits[i]
            sign = -sign
        return result` },
{ name: "Java", lang: "java", code: `import java.util.*;

class Solution {
    public int alternateDigitSum(int n) {
        List<Integer> digits = new ArrayList&lt;&gt;();
        while (n &gt; 0) {
            digits.add(n % 10);
            n /= 10;
        }
        
        int result = 0;
        int sign = 1;
        for (int i = digits.size() - 1; i &gt;= 0; i--) {
            result += sign * digits.get(i);
            sign = -sign;
        }
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
using namespace std;

class Solution {
public:
    int alternateDigitSum(int n) {
        vector<int> digits;
        while (n &gt; 0) {
            digits.push_back(n % 10);
            n /= 10;
        }
        
        int result = 0;
        int sign = 1;
        for (int i = digits.size() - 1; i &gt;= 0; i--) {
            result += sign * digits[i];
            sign = -sign;
        }
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @return {number}
 */
var alternateDigitSum = function(n) {
    const digits = [];
    while (n &gt; 0) {
        digits.push(n % 10);
        n = Math.floor(n / 10);
    }
    
    let result = 0;
    let sign = 1;
    for (let i = digits.length - 1; i &gt;= 0; i--) {
        result += sign * digits[i];
        sign = -sign;
    }
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function alternateDigitSum(n: number): number {
    const digits: number[] = [];
    while (n &gt; 0) {
        digits.push(n % 10);
        n = Math.floor(n / 10);
    }
    
    let result = 0;
    let sign = 1;
    for (let i = digits.length - 1; i &gt;= 0; i--) {
        result += sign * digits[i];
        sign = -sign;
    }
    return result;
}` },]} />

**Complexity**
- Time: O(log n) - extracting and processing digits
- Space: O(log n) - storing all digits in an array
- Notes: More intuitive but uses extra space for the array

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Alternating Digit Sum Solutions","datePublished":"2025-07-17","description":"Calculate the alternating sum of digits in a positive integer, starting with addition for the leftmost digit.","keywords":"LeetCode, Alternating Digit Sum, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of String Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"O(log n) where n is the input number, as we iterate through each digit once."}},{"@type":"Question","name":"What is the space complexity of Mathematical approaches?","acceptedAnswer":{"@type":"Answer","text":"O(1) as they only use a constant amount of extra space for variables."}},{"@type":"Question","name":"Which approach is most space efficient?","acceptedAnswer":{"@type":"Answer","text":"The Mathematical approaches (Reverse and Count, Count Digits First) are most space efficient with O(1) space complexity."}},{"@type":"Question","name":"What is the maximum input size for this problem?","acceptedAnswer":{"@type":"Answer","text":"The input n can be up to 10⁹, which means at most 10 digits to process."}}]}).replace(/</g,'\u003c')}
</script>