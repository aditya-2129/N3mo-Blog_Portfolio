---
title: "Furthest Point From Origin"
summary: "Calculate the maximum possible distance from the origin after moving left, right, or either direction."
date: "Feb 17, 2026"
tags:
  - String
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 77.00% | **Paid:** No
> **Topics:** String, Counting

You are given a string `moves` of length `n` consisting only of characters `'L'`, `'R'`, and `'_'`. The characters `'L'` and `'R'` represent moves where the object moves one unit to the left or right, respectively. The character `'_'` represents a move where the object can move either one unit to the left or one unit to the right.

Return the distance from the origin (0) after performing all the moves such that the distance is maximized.

- Examples
- Constraints
- [Approach 1: Counting](#approach-1-counting)
- [Approach 2: Simulation](#approach-2-simulation)

## Examples

```text
Input: moves = "L_RL__R"
Output: 3
Explanation: The moves are "L_RL__R". We can replace the '_'s with 'R' to get "LRRLLRR". The distance is 3.
```

```text
Input: moves = "_R__LL_"
Output: 5
Explanation: The moves are "_R__LL_". We can replace the '_'s with 'R' to get "RRRLLLR". The distance is 5.
```

```text
Input: moves = "_______"
Output: 7
```

## Constraints

```text
1 <= moves.length <= 100
moves consists only of 'L', 'R', and '_'.
```

---
## Approach 1: Counting

**Intuition**
Count the net displacement from 'L' and 'R'. The '_' characters can always be used to increase the absolute distance from the origin.

**Steps**
- Count the number of 'L', 'R', and '_' characters.
- Calculate the base distance as the absolute difference between 'L' and 'R' counts.
- Add the count of '_' to the base distance to get the maximum distance.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def furthestDistanceFromOrigin(self, moves: str) -&gt; int:\n        l = moves.count('L')\n        r = moves.count('R')\n        u = moves.count('_')\n        return abs(l - r) + u" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int furthestDistanceFromOrigin(String moves) {\n        int l = 0, r = 0, u = 0;\n        for (char c : moves.toCharArray()) {\n            if (c == 'L') l++;\n            else if (c == 'R') r++;\n            else u++;\n        }\n        return Math.abs(l - r) + u;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int furthestDistanceFromOrigin(string moves) {\n        int l = 0, r = 0, u = 0;\n        for (char c : moves) {\n            if (c == 'L') l++;\n            else if (c == 'R') r++;\n            else u++;\n        }\n        return abs(l - r) + u;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var furthestDistanceFromOrigin = function(moves) {\n    let l = 0, r = 0, u = 0;\n    for (let c of moves) {\n        if (c === 'L') l++;\n        else if (c === 'R') r++;\n        else u++;\n    }\n    return Math.abs(l - r) + u;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function furthestDistanceFromOrigin(moves: string): number {\n    let l = 0, r = 0, u = 0;\n    for (let c of moves) {\n        if (c === 'L') l++;\n        else if (c === 'R') r++;\n        else u++;\n    }\n    return Math.abs(l - r) + u;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Efficient single pass solution.

---
## Approach 2: Simulation

**Intuition**
Simulate the movement step-by-step. For fixed moves, move accordingly. For wildcards, move in the direction that increases the distance from 0.

**Steps**
- Initialize a position variable to 0.
- Iterate through each character in the string.
- If 'L', decrement position. If 'R', increment position.
- If '_', check the current position. If positive, move right; otherwise, move left.
- Return the absolute value of the final position.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def furthestDistanceFromOrigin(self, moves: str) -&gt; int:\n        pos = 0\n        for c in moves:\n            if c == 'L':\n                pos -= 1\n            elif c == 'R':\n                pos += 1\n            else:\n                if pos &gt; 0:\n                    pos += 1\n                else:\n                    pos -= 1\n        return abs(pos)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int furthestDistanceFromOrigin(String moves) {\n        int pos = 0;\n        for (char c : moves.toCharArray()) {\n            if (c == 'L') pos--;\n            else if (c == 'R') pos++;\n            else {\n                if (pos &gt; 0) pos++;\n                else pos--;\n            }\n        }\n        return Math.abs(pos);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int furthestDistanceFromOrigin(string moves) {\n        int pos = 0;\n        for (char c : moves) {\n            if (c == 'L') pos--;\n            else if (c == 'R') pos++;\n            else {\n                if (pos &gt; 0) pos++;\n                else pos--;\n            }\n        }\n        return abs(pos);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var furthestDistanceFromOrigin = function(moves) {\n    let pos = 0;\n    for (let c of moves) {\n        if (c === 'L') pos--;\n        else if (c === 'R') pos++;\n        else {\n            if (pos &gt; 0) pos++;\n            else pos--;\n        }\n    }\n    return Math.abs(pos);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function furthestDistanceFromOrigin(moves: string): number {\n    let pos = 0;\n    for (let c of moves) {\n        if (c === 'L') pos--;\n        else if (c === 'R') pos++;\n        else {\n            if (pos &gt; 0) pos++;\n            else pos--;\n        }\n    }\n    return Math.abs(pos);\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Intuitive simulation approach.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Furthest Point From Origin Solutions","datePublished":"2026-02-17","description":"Calculate the maximum possible distance from the origin after moving left, right, or either direction.","keywords":"LeetCode, Furthest Point From Origin, Easy, String, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string, as we iterate through the string once."}},{"@type":"Question","name":"What is the space complexity of the Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) since we only use a few integer variables for counting."}},{"@type":"Question","name":"What is the time complexity of the Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), as we process each character in the string exactly once."}},{"@type":"Question","name":"What is the space complexity of the Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only maintain a single integer for the position."}}]}).replace(/</g,'\u003c')}
</script>