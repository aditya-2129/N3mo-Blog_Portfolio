---
title: "Mirror Distance of an Integer"
summary: "Calculate the absolute difference between an integer and its mirror (digit-reversed) value."
date: "Jun 26, 2025"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 91.50% | **Paid:** No
> **Topics:** Math

## Problem Description

Given an integer n, find its mirror (the number formed by reversing its digits) and return the absolute difference between n and its mirror.

For example, the mirror of 123 is 321, and the mirror distance is |123 - 321| = 198.

## Table of Contents

- Examples
- Constraints
- [String Manipulation](#string-manipulation)
- [Mathematical Reversal](#mathematical-reversal)
- [Recursive Reversal](#recursive-reversal)

## Examples

### Example 1

**Input:**
```text
n = 25
```

**Output:**
```text
27
```

**Explanation:**
reverse(25) = 52.

Thus, the answer is abs(25 - 52) = 27.

### Example 2

**Input:**
```text
n = 10
```

**Output:**
```text
9
```

**Explanation:**
reverse(10) = 01 which is 1.

Thus, the answer is abs(10 - 1) = 9.

### Example 3

**Input:**
```text
n = 7
```

**Output:**
```text
0
```

**Explanation:**
reverse(7) = 7.

Thus, the answer is abs(7 - 7) = 0.

## Constraints

```text
-10⁹ <= n <= 10⁹
```

## String Manipulation

**Intuition**
Convert the number to a string, reverse it, and convert back to an integer to find the mirror, then calculate the absolute difference.

**Steps**
- Extract the sign of the number
- Convert the absolute value to a string
- Reverse the string and convert back to integer
- Apply the original sign to get the mirror
- Return the absolute difference between n and its mirror

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def mirrorDistance(self, n: int) -> int:
        sign = -1 if n &lt; 0 else 1
        mirror = int(str(abs(n))[::-1]) * sign
        return abs(n - mirror)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int mirrorDistance(int n) {
        int sign = n &lt; 0 ? -1 : 1;
        String reversed = new StringBuilder(String.valueOf(Math.abs(n))).reverse().toString();
        int mirror = Integer.parseInt(reversed) * sign;
        return Math.abs(n - mirror);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int mirrorDistance(int n) {
        int sign = n &lt; 0 ? -1 : 1;
        string s = to_string(abs(n));
        reverse(s.begin(), s.end());
        int mirror = stoi(s) * sign;
        return abs(n - mirror);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    mirrorDistance(n) {
        const sign = n &lt; 0 ? -1 : 1;
        const mirror = parseInt(Math.abs(n).toString().split('').reverse().join('')) * sign;
        return Math.abs(n - mirror);
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    mirrorDistance(n: number): number {
        const sign = n &lt; 0 ? -1 : 1;
        const mirror = parseInt(Math.abs(n).toString().split('').reverse().join('')) * sign;
        return Math.abs(n - mirror);
    }
}
` },]} />

**Complexity**
- Time: O(log n) - Converting to string and reversing takes time proportional to the number of digits
- Space: O(log n) - String representation of the number
- Notes: Simple and readable, but uses extra space for string conversion

## Mathematical Reversal

**Intuition**
Extract digits from the number using modulo and division operations, then build the reversed number mathematically.

**Steps**
- Extract the sign of the number
- Use modulo 10 to get the last digit
- Build the mirror by multiplying by 10 and adding each digit
- Apply the original sign to get the mirror
- Return the absolute difference between n and its mirror

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def mirrorDistance(self, n: int) -> int:
        sign = -1 if n &lt; 0 else 1
        abs_n = abs(n)
        mirror = 0
        while abs_n &gt; 0:
            mirror = mirror * 10 + abs_n % 10
            abs_n //= 10
        mirror *= sign
        return abs(n - mirror)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int mirrorDistance(int n) {
        int sign = n &lt; 0 ? -1 : 1;
        int absN = Math.abs(n);
        int mirror = 0;
        while (absN &gt; 0) {
            mirror = mirror * 10 + absN % 10;
            absN /= 10;
        }
        mirror *= sign;
        return Math.abs(n - mirror);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int mirrorDistance(int n) {
        int sign = n &lt; 0 ? -1 : 1;
        int absN = abs(n);
        int mirror = 0;
        while (absN &gt; 0) {
            mirror = mirror * 10 + absN % 10;
            absN /= 10;
        }
        mirror *= sign;
        return abs(n - mirror);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    mirrorDistance(n) {
        const sign = n &lt; 0 ? -1 : 1;
        let absN = Math.abs(n);
        let mirror = 0;
        while (absN &gt; 0) {
            mirror = mirror * 10 + absN % 10;
            absN = Math.floor(absN / 10);
        }
        mirror *= sign;
        return Math.abs(n - mirror);
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    mirrorDistance(n: number): number {
        const sign = n &lt; 0 ? -1 : 1;
        let absN = Math.abs(n);
        let mirror = 0;
        while (absN &gt; 0) {
            mirror = mirror * 10 + absN % 10;
            absN = Math.floor(absN / 10);
        }
        mirror *= sign;
        return Math.abs(n - mirror);
    }
}
` },]} />

**Complexity**
- Time: O(log n) - We process each digit exactly once
- Space: O(1) - Only using a constant amount of extra space
- Notes: Most efficient approach with constant space complexity

## Recursive Reversal

**Intuition**
Use recursion to reverse the digits by processing the last digit first and building the reversed number.

**Steps**
- Extract the sign of the number
- Define a recursive function that takes the remaining number and the current reversed value
- Base case: when the number is 0, return the reversed value
- Recursive case: extract the last digit and add it to the reversed value, then recurse with the remaining digits
- Apply the original sign to get the mirror
- Return the absolute difference between n and its mirror

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def mirrorDistance(self, n: int) -> int:
        sign = -1 if n &lt; 0 else 1
        abs_n = abs(n)
        
        def reverse(num, rev):
            if num == 0:
                return rev
            return reverse(num // 10, rev * 10 + num % 10)
        
        mirror = reverse(abs_n, 0) * sign
        return abs(n - mirror)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int mirrorDistance(int n) {
        int sign = n &lt; 0 ? -1 : 1;
        int absN = Math.abs(n);
        int mirror = reverse(absN, 0) * sign;
        return Math.abs(n - mirror);
    }
    
    private int reverse(int num, int rev) {
        if (num == 0) {
            return rev;
        }
        return reverse(num / 10, rev * 10 + num % 10);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int mirrorDistance(int n) {
        int sign = n &lt; 0 ? -1 : 1;
        int absN = abs(n);
        int mirror = reverse(absN, 0) * sign;
        return abs(n - mirror);
    }
    
private:
    int reverse(int num, int rev) {
        if (num == 0) {
            return rev;
        }
        return reverse(num / 10, rev * 10 + num % 10);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    mirrorDistance(n) {
        const sign = n &lt; 0 ? -1 : 1;
        const absN = Math.abs(n);
        const mirror = this.reverse(absN, 0) * sign;
        return Math.abs(n - mirror);
    }
    
    reverse(num, rev) {
        if (num === 0) {
            return rev;
        }
        return this.reverse(Math.floor(num / 10), rev * 10 + num % 10);
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    mirrorDistance(n: number): number {
        const sign = n &lt; 0 ? -1 : 1;
        const absN = Math.abs(n);
        const mirror = this.reverse(absN, 0) * sign;
        return Math.abs(n - mirror);
    }
    
    private reverse(num: number, rev: number): number {
        if (num === 0) {
            return rev;
        }
        return this.reverse(Math.floor(num / 10), rev * 10 + num % 10);
    }
}
` },]} />

**Complexity**
- Time: O(log n) - We process each digit exactly once
- Space: O(log n) - Recursion stack depth equals the number of digits
- Notes: Elegant solution but uses stack space due to recursion

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Mirror Distance of an Integer Solutions","datePublished":"2025-06-26","description":"Calculate the absolute difference between an integer and its mirror (digit-reversed) value.","keywords":"LeetCode, Mirror Distance of an Integer, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the String Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(log n) - Converting to string and reversing takes time proportional to the number of digits."}},{"@type":"Question","name":"What is the space complexity of the Mathematical Reversal approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Only using a constant amount of extra space."}},{"@type":"Question","name":"Which approach is most efficient for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Mathematical Reversal approach is most efficient with O(log n) time and O(1) space complexity."}},{"@type":"Question","name":"How does the Recursive Reversal approach compare to the iterative approach?","acceptedAnswer":{"@type":"Answer","text":"Both have O(log n) time complexity, but the recursive approach uses O(log n) stack space while the iterative approach uses O(1) space."}}]}).replace(/</g,'\u003c')}
</script>