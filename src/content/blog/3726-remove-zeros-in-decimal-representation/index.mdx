---
title: "Remove Zeros in Decimal Representation"
summary: "Given an integer n, return the integer obtained by removing all trailing zeros from its decimal representation."
date: "Jan 15, 2026"
tags:
  - Math
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 76.40% | **Paid:** No
> **Topics:** Math, Simulation

Given an integer n, return the integer obtained by removing all trailing zeros from the decimal representation of n.

- Examples
- Constraints
- [String Manipulation](#string-manipulation)
- [Mathematical Iteration](#mathematical-iteration)

## Examples

### Example 1

**Input:**
```text
n = 1020030
```

**Output:**
```text
123
```

**Explanation:**
After removing all zeros from 1020030, we get 123.

### Example 2

**Input:**
```text
n = 1
```

**Output:**
```text
1
```

**Explanation:**
1 has no zero in its decimal representation. Therefore, the answer is 1.

## Constraints

```text
0 <= n <= 10^9
```

---
## String Manipulation

**Intuition**
Convert the integer to a string to easily access and remove characters from the end of the sequence.

**Steps**
- Convert the integer `n` to its string representation.
- Iterate from the end of the string and remove characters while they are '0'.
- If the resulting string is empty (which happens if input was 0), return 0.
- Otherwise, convert the modified string back to an integer and return it.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def removeZeros(self, n: int) -&gt; int:\n        s = str(n)\n        s = s.rstrip('0')\n        return int(s) if s else 0" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int removeZeros(int n) {\n        String s = Integer.toString(n);\n        int i = s.length() - 1;\n        while (i &gt;= 0 && s.charAt(i) == '0') {\n            i--;\n        }\n        s = s.substring(0, i + 1);\n        return s.isEmpty() ? 0 : Integer.parseInt(s);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int removeZeros(int n) {\n        string s = to_string(n);\n        while (!s.empty() && s.back() == '0') {\n            s.pop_back();\n        }\n        return s.empty() ? 0 : stoi(s);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    removeZeros(n) {\n        let s = n.toString();\n        while (s.length &gt; 0 && s.endsWith('0')) {\n            s = s.slice(0, -1);\n        }\n        return s.length === 0 ? 0 : parseInt(s);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    removeZeros(n: number): number {\n        let s = n.toString();\n        while (s.length &gt; 0 && s.endsWith('0')) {\n            s = s.slice(0, -1);\n        }\n        return s.length === 0 ? 0 : parseInt(s);\n    }\n}" },]} />

**Complexity**
- Time: O(log n) - The number of digits is proportional to log₁₀(n).
- Space: O(log n) - To store the string representation.
- Notes: Simple to implement but uses extra space for the string.

---
## Mathematical Iteration

**Intuition**
Use modulo and division operations to mathematically remove trailing zeros without converting to a string.

**Steps**
- While `n` is greater than 0 and the last digit (`n % 10`) is 0, divide `n` by 10.
- Return the resulting `n`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def removeZeros(self, n: int) -&gt; int:\n        while n &gt; 0 and n % 10 == 0:\n            n //= 10\n        return n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int removeZeros(int n) {\n        while (n &gt; 0 && n % 10 == 0) {\n            n /= 10;\n        }\n        return n;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int removeZeros(int n) {\n        while (n &gt; 0 && n % 10 == 0) {\n            n /= 10;\n        }\n        return n;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    removeZeros(n) {\n        while (n &gt; 0 && n % 10 === 0) {\n            n = Math.floor(n / 10);\n        }\n        return n;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    removeZeros(n: number): number {\n        while (n &gt; 0 && n % 10 === 0) {\n            n = Math.floor(n / 10);\n        }\n        return n;\n    }\n}" },]} />

**Complexity**
- Time: O(log n) - We iterate once per digit.
- Space: O(1) - Constant extra space used.
- Notes: Optimal space complexity and avoids string conversion overhead.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Remove Zeros in Decimal Representation Solutions","datePublished":"2026-01-15","description":"Given an integer n, return the integer obtained by removing all trailing zeros from its decimal representation.","keywords":"LeetCode, Remove Zeros in Decimal Representation, Easy, Math, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the String Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(log n), where n is the value of the input integer."}},{"@type":"Question","name":"What is the space complexity of the Mathematical Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"O(1), as it modifies the integer in place without using extra data structures."}},{"@type":"Question","name":"How does the Mathematical Iteration approach handle the input 0?","acceptedAnswer":{"@type":"Answer","text":"The loop condition requires n &gt; 0, so if n is 0, the loop is skipped and 0 is returned immediately."}}]}).replace(/</g,'\u003c')}
</script>