---
title: "Reformat Date"
summary: "Convert a date from 'Day Month Year' format to 'YYYY-MM-DD' format."
date: "Feb 11, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 68.50% | **Paid:** No
> **Topics:** String

Given a date string in the format Day Month Year, where:

Day is in the set \{"1st", "2nd", "3rd", "4th", ..., "30th", "31st"\}.
Month is in the set \{"Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"\}.
Year is in the range [1900, 2100].

Convert the date string to the format YYYY-MM-DD.

- Examples
- Constraints
- [String Manipulation](#string-manipulation)
- [HashMap Lookup](#hashmap-lookup)
- [Date Parsing](#date-parsing)

## Examples

```text
Input: date = "20th Oct 2052"
Output: "2052-10-20"
```

```text
Input: date = "6th Jun 1933"
Output: "1933-06-06"
```

```text
Input: date = "26th May 1960"
Output: "1960-05-26"
```

## Constraints

```text
The given dates are valid dates in the Gregorian calendar.
```

---
## String Manipulation

**Intuition**
Split the input string by spaces, extract day by removing the suffix, find month index from an array, and format all components with leading zeros.

**Steps**
- Split the date string by spaces to get day, month, and year parts
- Remove the last two characters from day to get the numeric day
- Find the month index (1-12) by searching in a months array
- Format day and month with leading zeros if needed
- Combine in YYYY-MM-DD format

<CodeTabs client:load tabs={[
{ name: "Python3", lang: "python", code: "class Solution:\\n    def reformatDate(self, date: str) -> str:\\n        months = [\\\"Jan\\\", \\\"Feb\\\", \\\"Mar\\\", \\\"Apr\\\", \\\"May\\\", \\\"Jun\\\", \\\"Jul\\\", \\\"Aug\\\", \\\"Sep\\\", \\\"Oct\\\", \\\"Nov\\\", \\\"Dec\\\"]\\n        parts = date.split()\\n        day = parts[0][:-2]\\n        month = str(months.index(parts[1]) + 1).zfill(2)\\n        year = parts[2]\\n        return f\\\"{year}-{month}-{day.zfill(2)}\\\"" },
{ name: "Java", lang: "java", code: "class Solution {\\n    public String reformatDate(String date) {\\n        String[] months = {\\\"Jan\\\", \\\"Feb\\\", \\\"Mar\\\", \\\"Apr\\\", \\\"May\\\", \\\"Jun\\\", \\\"Jul\\\", \\\"Aug\\\", \\\"Sep\\\", \\\"Oct\\\", \\\"Nov\\\", \\\"Dec\\\"};\\n        String[] parts = date.split(\\\" \\\");\\n        String day = parts[0].substring(0, parts[0].length() - 2);\\n        int monthIndex = 0;\\n        for (int i = 0; i < months.length; i++) {\\n            if (months[i].equals(parts[1])) {\\n                monthIndex = i + 1;\\n                break;\\n            }\\n        }\\n        String month = String.format(\\\"%02d\\\", monthIndex);\\n        String year = parts[2];\\n        return year + \\\"-\\\" + month + \\\"-\\\" + String.format(\\\"%02d\\\", Integer.parseInt(day));\\n    }\\n}" },
{ name: "C++", lang: "cpp", code: "#include <vector>\\n#include <string>\\n#include <sstream>\\nusing namespace std;\\n\\nclass Solution {\\npublic:\\n    string reformatDate(string date) {\\n        vector<string> months = {\\\"Jan\\\", \\\"Feb\\\", \\\"Mar\\\", \\\"Apr\\\", \\\"May\\\", \\\"Jun\\\", \\\"Jul\\\", \\\"Aug\\\", \\\"Sep\\\", \\\"Oct\\\", \\\"Nov\\\", \\\"Dec\\\"};\\n        vector<string> parts;\\n        stringstream ss(date);\\n        string part;\\n        while (ss >> part) {\\n            parts.push_back(part);\\n        }\\n        string day = parts[0].substr(0, parts[0].length() - 2);\\n        int monthIndex = 0;\\n        for (int i = 0; i < months.size(); i++) {\\n            if (months[i] == parts[1]) {\\n                monthIndex = i + 1;\\n                break;\\n            }\\n        }\\n        string month = to_string(monthIndex);\\n        if (month.length() == 1) month = \\\"0\\\" + month;\\n        string year = parts[2];\\n        if (day.length() == 1) day = \\\"0\\\" + day;\\n        return year + \\\"-\\\" + month + \\\"-\\\" + day;\\n    }\\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\\n    reformatDate(date) {\\n        const months = [\\\"Jan\\\", \\\"Feb\\\", \\\"Mar\\\", \\\"Apr\\\", \\\"May\\\", \\\"Jun\\\", \\\"Jul\\\", \\\"Aug\\\", \\\"Sep\\\", \\\"Oct\\\", \\\"Nov\\\", \\\"Dec\\\"];\\n        const parts = date.split(\\\" \\\");\\n        let day = parts[0].slice(0, -2);\\n        const monthIndex = months.indexOf(parts[1]) + 1;\\n        const month = monthIndex.toString().padStart(2, '0');\\n        const year = parts[2];\\n        day = day.padStart(2, '0');\\n        return year + \\\"-\\\" + month + \\\"-\\\" + day;\\n    }\\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\\n    reformatDate(date: string): string {\\n        const months = [\\\"Jan\\\", \\\"Feb\\\", \\\"Mar\\\", \\\"Apr\\\", \\\"May\\\", \\\"Jun\\\", \\\"Jul\\\", \\\"Aug\\\", \\\"Sep\\\", \\\"Oct\\\", \\\"Nov\\\", \\\"Dec\\\"];\\n        const parts = date.split(\\\" \\\");\\n        let day = parts[0].slice(0, -2);\\n        const monthIndex = months.indexOf(parts[1]) + 1;\\n        const month = monthIndex.toString().padStart(2, '0');\\n        const year = parts[2];\\n        day = day.padStart(2, '0');\\n        return year + \\\"-\\\" + month + \\\"-\\\" + day;\\n    }\\n}" }
]} />

**Complexity**
- Time: O(1) - Fixed number of operations regardless of input size
- Space: O(1) - Using fixed-size arrays and variables
- Notes: Simple and readable, but linear search for month is slightly inefficient

---
## HashMap Lookup

**Intuition**
Use a hash map to store month abbreviations mapped to their numeric values for O(1) lookup time.

**Steps**
- Create a hash map mapping month abbreviations to their two-digit string representations
- Split the input string by spaces
- Extract day by removing the suffix and pad with leading zero
- Look up month value from the hash map
- Combine year, month, and day in YYYY-MM-DD format

<CodeTabs client:load tabs={[
{ name: "Python3", lang: "python", code: "class Solution:\\n    def reformatDate(self, date: str) -> str:\\n        month_map = {\\\"Jan\\\": \\\"01\\\", \\\"Feb\\\": \\\"02\\\", \\\"Mar\\\": \\\"03\\\", \\\"Apr\\\": \\\"04\\\", \\\"May\\\": \\\"05\\\", \\\"Jun\\\": \\\"06\\\",\\n                     \\\"Jul\\\": \\\"07\\\", \\\"Aug\\\": \\\"08\\\", \\\"Sep\\\": \\\"09\\\", \\\"Oct\\\": \\\"10\\\", \\\"Nov\\\": \\\"11\\\", \\\"Dec\\\": \\\"12\\\"}\\n        parts = date.split()\\n        day = parts[0][:-2].zfill(2)\\n        month = month_map[parts[1]]\\n        year = parts[2]\\n        return f\\\"{year}-{month}-{day}\\\"" },
{ name: "Java", lang: "java", code: "import java.util.*;\\n\\nclass Solution {\\n    public String reformatDate(String date) {\\n        Map<String, String> monthMap = new HashMap<>();\\n        monthMap.put(\\\"Jan\\\", \\\"01\\\");\\n        monthMap.put(\\\"Feb\\\", \\\"02\\\");\\n        monthMap.put(\\\"Mar\\\", \\\"03\\\");\\n        monthMap.put(\\\"Apr\\\", \\\"04\\\");\\n        monthMap.put(\\\"May\\\", \\\"05\\\");\\n        monthMap.put(\\\"Jun\\\", \\\"06\\\");\\n        monthMap.put(\\\"Jul\\\", \\\"07\\\");\\n        monthMap.put(\\\"Aug\\\", \\\"08\\\");\\n        monthMap.put(\\\"Sep\\\", \\\"09\\\");\\n        monthMap.put(\\\"Oct\\\", \\\"10\\\");\\n        monthMap.put(\\\"Nov\\\", \\\"11\\\");\\n        monthMap.put(\\\"Dec\\\", \\\"12\\\");\\n        \\n        String[] parts = date.split(\\\" \\\");\\n        String day = parts[0].substring(0, parts[0].length() - 2);\\n        day = day.length() == 1 ? \\\"0\\\" + day : day;\\n        String month = monthMap.get(parts[1]);\\n        String year = parts[2];\\n        return year + \\\"-\\\" + month + \\\"-\\\" + day;\\n    }\\n}" },
{ name: "C++", lang: "cpp", code: "#include <unordered_map>\\n#include <string>\\n#include <vector>\\n#include <sstream>\\nusing namespace std;\\n\\nclass Solution {\\npublic:\\n    string reformatDate(string date) {\\n        unordered_map<string, string> monthMap = {\\n            {\\\"Jan\\\", \\\"01\\\"}, {\\\"Feb\\\", \\\"02\\\"}, {\\\"Mar\\\", \\\"03\\\"}, {\\\"Apr\\\", \\\"04\\\"}, {\\\"May\\\", \\\"05\\\"}, {\\\"Jun\\\", \\\"06\\\"},\\n            {\\\"Jul\\\", \\\"07\\\"}, {\\\"Aug\\\", \\\"08\\\"}, {\\\"Sep\\\", \\\"09\\\"}, {\\\"Oct\\\", \\\"10\\\"}, {\\\"Nov\\\", \\\"11\\\"}, {\\\"Dec\\\", \\\"12\\\"}\\n        };\\n        \\n        vector<string> parts;\\n        stringstream ss(date);\\n        string part;\\n        while (ss >> part) {\\n            parts.push_back(part);\\n        }\\n        \\n        string day = parts[0].substr(0, parts[0].length() - 2);\\n        if (day.length() == 1) day = \\\"0\\\" + day;\\n        string month = monthMap[parts[1]];\\n        string year = parts[2];\\n        return year + \\\"-\\\" + month + \\\"-\\\" + day;\\n    }\\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\\n    reformatDate(date) {\\n        const monthMap = {\\n            \\\"Jan\\\": \\\"01\\\", \\\"Feb\\\": \\\"02\\\", \\\"Mar\\\": \\\"03\\\", \\\"Apr\\\": \\\"04\\\", \\\"May\\\": \\\"05\\\", \\\"Jun\\\": \\\"06\\\",\\n            \\\"Jul\\\": \\\"07\\\", \\\"Aug\\\": \\\"08\\\", \\\"Sep\\\": \\\"09\\\", \\\"Oct\\\": \\\"10\\\", \\\"Nov\\\": \\\"11\\\", \\\"Dec\\\": \\\"12\\\"\\n        };\\n        \\n        const parts = date.split(\\\" \\\");\\n        let day = parts[0].slice(0, -2).padStart(2, '0');\\n        const month = monthMap[parts[1]];\\n        const year = parts[2];\\n        return year + \\\"-\\\" + month + \\\"-\\\" + day;\\n    }\\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\\n    reformatDate(date: string): string {\\n        const monthMap: { [key: string]: string } = {\\n            \\\"Jan\\\": \\\"01\\\", \\\"Feb\\\": \\\"02\\\", \\\"Mar\\\": \\\"03\\\", \\\"Apr\\\": \\\"04\\\", \\\"May\\\": \\\"05\\\", \\\"Jun\\\": \\\"06\\\",\\n            \\\"Jul\\\": \\\"07\\\", \\\"Aug\\\": \\\"08\\\", \\\"Sep\\\": \\\"09\\\", \\\"Oct\\\": \\\"10\\\", \\\"Nov\\\": \\\"11\\\", \\\"Dec\\\": \\\"12\\\"\\n        };\\n        \\n        const parts = date.split(\\\" \\\");\\n        let day = parts[0].slice(0, -2).padStart(2, '0');\\n        const month = monthMap[parts[1]];\\n        const year = parts[2];\\n        return year + \\\"-\\\" + month + \\\"-\\\" + day;\\n    }\\n}" }
]} />

**Complexity**
- Time: O(1) - Constant time operations with hash map lookup
- Space: O(1) - Fixed-size hash map with 12 entries
- Notes: More efficient month lookup than array search, at the cost of slightly more initialization code

---
## Date Parsing

**Intuition**
Leverage built-in date parsing libraries to handle the conversion, parsing the custom format and outputting in standard format.

**Steps**
- Parse the input string to extract day, month, and year components
- Convert month abbreviation to numeric value
- Create a Date object with the parsed components
- Format the Date object to YYYY-MM-DD string

<CodeTabs client:load tabs={[
{ name: "Python3", lang: "python", code: "from datetime import datetime\\n\\nclass Solution:\\n    def reformatDate(self, date: str) -> str:\\n        months = {\\\"Jan\\\": 1, \\\"Feb\\\": 2, \\\"Mar\\\": 3, \\\"Apr\\\": 4, \\\"May\\\": 5, \\\"Jun\\\": 6,\\n                  \\\"Jul\\\": 7, \\\"Aug\\\": 8, \\\"Sep\\\": 9, \\\"Oct\\\": 10, \\\"Nov\\\": 11, \\\"Dec\\\": 12}\\n        parts = date.split()\\n        day = int(parts[0][:-2])\\n        month = months[parts[1]]\\n        year = int(parts[2])\\n        dt = datetime(year, month, day)\\n        return dt.strftime(\\\"%Y-%m-%d\\\")" },
{ name: "Java", lang: "java", code: "import java.text.*;\\nimport java.util.*;\\n\\nclass Solution {\\n    public String reformatDate(String date) throws ParseException {\\n        String[] months = {\\\"Jan\\\", \\\"Feb\\\", \\\"Mar\\\", \\\"Apr\\\", \\\"May\\\", \\\"Jun\\\", \\\"Jul\\\", \\\"Aug\\\", \\\"Sep\\\", \\\"Oct\\\", \\\"Nov\\\", \\\"Dec\\\"};\\n        String[] parts = date.split(\\\" \\\");\\n        String day = parts[0].substring(0, parts[0].length() - 2);\\n        int monthIndex = 0;\\n        for (int i = 0; i < months.length; i++) {\\n            if (months[i].equals(parts[1])) {\\n                monthIndex = i + 1;\\n                break;\\n            }\\n        }\\n        String year = parts[2];\\n        \\n        SimpleDateFormat inputFormat = new SimpleDateFormat(\\\"yyyy M d\\\");\\n        Date d = inputFormat.parse(year + \\\" \\\" + monthIndex + \\\" \\\" + day);\\n        SimpleDateFormat outputFormat = new SimpleDateFormat(\\\"yyyy-MM-dd\\\");\\n        return outputFormat.format(d);\\n    }\\n}" },
{ name: "C++", lang: "cpp", code: "#include <ctime>\\n#include <string>\\n#include <vector>\\n#include <sstream>\\n#include <iomanip>\\nusing namespace std;\\n\\nclass Solution {\\npublic:\\n    string reformatDate(string date) {\\n        vector<string> months = {\\\"Jan\\\", \\\"Feb\\\", \\\"Mar\\\", \\\"Apr\\\", \\\"May\\\", \\\"Jun\\\", \\\"Jul\\\", \\\"Aug\\\", \\\"Sep\\\", \\\"Oct\\\", \\\"Nov\\\", \\\"Dec\\\"};\\n        vector<string> parts;\\n        stringstream ss(date);\\n        string part;\\n        while (ss >> part) {\\n            parts.push_back(part);\\n        }\\n        \\n        int day = stoi(parts[0].substr(0, parts[0].length() - 2));\\n        int monthIndex = 0;\\n        for (int i = 0; i < months.size(); i++) {\\n            if (months[i] == parts[1]) {\\n                monthIndex = i + 1;\\n                break;\\n            }\\n        }\\n        int year = stoi(parts[2]);\\n        \\n        struct tm tm = {0};\\n        tm.tm_year = year - 1900;\\n        tm.tm_mon = monthIndex - 1;\\n        tm.tm_mday = day;\\n        \\n        char buffer[11];\\n        strftime(buffer, sizeof(buffer), \\\"%Y-%m-%d\\\", &tm);\\n        return string(buffer);\\n    }\\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\\n    reformatDate(date) {\\n        const months = [\\\"Jan\\\", \\\"Feb\\\", \\\"Mar\\\", \\\"Apr\\\", \\\"May\\\", \\\"Jun\\\", \\\"Jul\\\", \\\"Aug\\\", \\\"Sep\\\", \\\"Oct\\\", \\\"Nov\\\", \\\"Dec\\\"];\\n        const parts = date.split(\\\" \\\");\\n        const day = parseInt(parts[0].slice(0, -2));\\n        const monthIndex = months.indexOf(parts[1]);\\n        const year = parseInt(parts[2]);\\n        \\n        const d = new Date(year, monthIndex, day);\\n        const yearStr = d.getFullYear().toString();\\n        const monthStr = (d.getMonth() + 1).toString().padStart(2, '0');\\n        const dayStr = d.getDate().toString().padStart(2, '0');\\n        \\n        return yearStr + \\\"-\\\" + monthStr + \\\"-\\\" + dayStr;\\n    }\\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\\n    reformatDate(date: string): string {\\n        const months = [\\\"Jan\\\", \\\"Feb\\\", \\\"Mar\\\", \\\"Apr\\\", \\\"May\\\", \\\"Jun\\\", \\\"Jul\\\", \\\"Aug\\\", \\\"Sep\\\", \\\"Oct\\\", \\\"Nov\\\", \\\"Dec\\\"];\\n        const parts = date.split(\\\" \\\");\\n        const day = parseInt(parts[0].slice(0, -2));\\n        const monthIndex = months.indexOf(parts[1]);\\n        const year = parseInt(parts[2]);\\n        \\n        const d = new Date(year, monthIndex, day);\\n        const yearStr = d.getFullYear().toString();\\n        const monthStr = (d.getMonth() + 1).toString().padStart(2, '0');\\n        const dayStr = d.getDate().toString().padStart(2, '0');\\n        \\n        return yearStr + \\\"-\\\" + monthStr + \\\"-\\\" + dayStr;\\n    }\\n}" }
]} />

**Complexity**
- Time: O(1) - Constant time operations
- Space: O(1) - Fixed space for date object and formatting
- Notes: Leverages built-in date libraries which can handle edge cases automatically, but may be overkill for this simple transformation

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Reformat Date Solutions","datePublished":"2024-02-11","description":"Convert a date from 'Day Month Year' format to 'YYYY-MM-DD' format.","keywords":"LeetCode, Reformat Date, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of String Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Fixed number of operations regardless of input size"}},{"@type":"Question","name":"What is the time complexity of HashMap Lookup approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Constant time operations with hash map lookup"}},{"@type":"Question","name":"What is the time complexity of Date Parsing approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Constant time operations"}},{"@type":"Question","name":"Which approach is most efficient for month lookup?","acceptedAnswer":{"@type":"Answer","text":"HashMap Lookup provides O(1) lookup time compared to linear search in array-based approach"}}]}).replace(/</g,'\u003c')}
</script>