---
title: "Count the Number of Special Characters I"
summary: "You are given a string word. A character is special if it appears in both lowercase and uppercase forms in word. Return the number of special characters."
date: "Jun 12, 2025"
tags:
  - Hash Table
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 67.00% | **Paid:** No
> **Topics:** Hash Table, String

You are given a string word.

A character is special if it appears in both lowercase and uppercase forms in word.

Return the number of special characters in word.

## Table of Contents
- Examples
- Constraints
- [Hash Set Approach](#hash-set-approach)
- [Boolean Array Approach](#boolean-array-approach)
- [Bitmasking Approach](#bitmasking-approach)

## Examples

### Example 1
```text
Input: word = "aaAbcBC"
Output: 2
Explanation:
The special characters are 'a' and 'b'.
```

### Example 2
```text
Input: word = "abc"
Output: 0
Explanation:
No character appears in both lowercase and uppercase form.
```

### Example 3
```text
Input: word = "abBCab"
Output: 1
Explanation:
The special character is 'b'.
```

## Constraints

```text
1 <= word.length <= 50
word consists of lowercase and uppercase English letters.
```

---
## Hash Set Approach

**Intuition**
We can use two sets to store the unique lowercase and uppercase characters found in the string. By iterating through the alphabet, we can check if a character exists in both sets.

**Steps**
- Initialize two empty sets, one for lowercase and one for uppercase.
- Iterate through each character in the input string.
- If the character is lowercase, add it to the lowercase set. If it is uppercase, add it to the uppercase set.
- Initialize a counter to 0.
- Iterate from 'a' to 'z' (or 0 to 25).
- For each letter, check if it exists in the lowercase set and if its uppercase version exists in the uppercase set.
- If both exist, increment the counter.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numberOfSpecialChars(self, word: str) -&gt; int:\n        lower_set = set()\n        upper_set = set()\n        \n        for char in word:\n            if 'a' &lt;= char &lt;= 'z':\n                lower_set.add(char)\n            else:\n                upper_set.add(char)\n        \n        count = 0\n        for i in range(26):\n            char = chr(ord('a') + i)\n            if char in lower_set and char.upper() in upper_set:\n                count += 1\n                \n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numberOfSpecialChars(String word) {\n        boolean[] lower = new boolean[26];\n        boolean[] upper = new boolean[26];\n        \n        for (char c : word.toCharArray()) {\n            if (c &gt;= 'a' && c &lt;= 'z') {\n                lower[c - 'a'] = true;\n            } else {\n                upper[c - 'A'] = true;\n            }\n        }\n        \n        int count = 0;\n        for (int i = 0; i &lt; 26; i++) {\n            if (lower[i] && upper[i]) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numberOfSpecialChars(string word) {\n        unordered_set&lt;char&gt; lower;\n        unordered_set&lt;char&gt; upper;\n        \n        for (char c : word) {\n            if (c &gt;= 'a' && c &lt;= 'z') {\n                lower.insert(c);\n            } else {\n                upper.insert(c);\n            }\n        }\n        \n        int count = 0;\n        for (int i = 0; i &lt; 26; ++i) {\n            char c = 'a' + i;\n            if (lower.find(c) != lower.end() && upper.find(toupper(c)) != upper.end()) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var numberOfSpecialChars = function(word) {\n    const lowerSet = new Set();\n    const upperSet = new Set();\n    \n    for (const c of word) {\n        if (c &gt;= 'a' && c &lt;= 'z') {\n            lowerSet.add(c);\n        } else {\n            upperSet.add(c);\n        }\n    }\n    \n    let count = 0;\n    for (let i = 0; i &lt; 26; i++) {\n        const char = String.fromCharCode(97 + i);\n        if (lowerSet.has(char) && upperSet.has(char.toUpperCase())) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfSpecialChars(word: string): number {\n    const lowerSet = new Set&lt;string&gt;();\n    const upperSet = new Set&lt;string&gt;();\n    \n    for (const c of word) {\n        if (c &gt;= 'a' && c &lt;= 'z') {\n            lowerSet.add(c);\n        } else {\n            upperSet.add(c);\n        }\n    }\n    \n    let count = 0;\n    for (let i = 0; i &lt; 26; i++) {\n        const char = String.fromCharCode(97 + i);\n        if (lowerSet.has(char) && upperSet.has(char.toUpperCase())) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(N)
- Space: O(1)
- Notes: The space is O(1) because the sets can hold at most 26 characters each, regardless of the input size N.

---
## Boolean Array Approach

**Intuition**
Since we only deal with English letters, we can use two fixed-size boolean arrays of length 26 to track the presence of lowercase and uppercase characters. This is often faster than using hash sets due to direct indexing.

**Steps**
- Create two boolean arrays of size 26, initialized to false.
- Iterate through the string. For each character, determine its index (0 for 'a' or 'A', 25 for 'z' or 'Z').
- Mark the corresponding position in the lowercase or uppercase array as true.
- Iterate from 0 to 25. If both the lowercase and uppercase arrays have true at the current index, increment the result.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numberOfSpecialChars(self, word: str) -&gt; int:\n        lower = [False] * 26\n        upper = [False] * 26\n        \n        for char in word:\n            if 'a' &lt;= char &lt;= 'z':\n                lower[ord(char) - ord('a')] = True\n            else:\n                upper[ord(char) - ord('A')] = True\n                \n        count = 0\n        for i in range(26):\n            if lower[i] and upper[i]:\n                count += 1\n                \n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numberOfSpecialChars(String word) {\n        boolean[] lower = new boolean[26];\n        boolean[] upper = new boolean[26];\n        \n        for (int i = 0; i &lt; word.length(); i++) {\n            char c = word.charAt(i);\n            if (c &gt;= 'a' && c &lt;= 'z') {\n                lower[c - 'a'] = true;\n            } else {\n                upper[c - 'A'] = true;\n            }\n        }\n        \n        int count = 0;\n        for (int i = 0; i &lt; 26; i++) {\n            if (lower[i] && upper[i]) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numberOfSpecialChars(string word) {\n        bool lower[26] = {false};\n        bool upper[26] = {false};\n        \n        for (char c : word) {\n            if (c &gt;= 'a' && c &lt;= 'z') {\n                lower[c - 'a'] = true;\n            } else {\n                upper[c - 'A'] = true;\n            }\n        }\n        \n        int count = 0;\n        for (int i = 0; i &lt; 26; ++i) {\n            if (lower[i] && upper[i]) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var numberOfSpecialChars = function(word) {\n    const lower = new Array(26).fill(false);\n    const upper = new Array(26).fill(false);\n    \n    for (const c of word) {\n        if (c &gt;= 'a' && c &lt;= 'z') {\n            lower[c.charCodeAt(0) - 97] = true;\n        } else {\n            upper[c.charCodeAt(0) - 65] = true;\n        }\n    }\n    \n    let count = 0;\n    for (let i = 0; i &lt; 26; i++) {\n        if (lower[i] && upper[i]) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfSpecialChars(word: string): number {\n    const lower: boolean[] = new Array(26).fill(false);\n    const upper: boolean[] = new Array(26).fill(false);\n    \n    for (const c of word) {\n        if (c &gt;= 'a' && c &lt;= 'z') {\n            lower[c.charCodeAt(0) - 97] = true;\n        } else {\n            upper[c.charCodeAt(0) - 65] = true;\n        }\n    }\n    \n    let count = 0;\n    for (let i = 0; i &lt; 26; i++) {\n        if (lower[i] && upper[i]) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(N)
- Space: O(1)
- Notes: Uses fixed-size arrays (52 booleans total), which is very memory efficient.

---
## Bitmasking Approach

**Intuition**
We can use two integers as bitmasks to represent the presence of characters. Each bit in an integer corresponds to a letter in the alphabet (e.g., bit 0 for 'a'/'A', bit 1 for 'b'/'B'). This allows us to store the state of all 26 letters in a single 32-bit integer.

**Steps**
- Initialize two integers, `lowerMask` and `upperMask`, to 0.
- Iterate through the string.
- If the character is lowercase, calculate its bit position and set the corresponding bit in `lowerMask` using bitwise OR.
- If the character is uppercase, calculate its bit position and set the corresponding bit in `upperMask`.
- After processing the string, iterate from 0 to 25.
- For each index `i`, check if the `i`-th bit is set in both `lowerMask` and `upperMask` using bitwise AND.
- If both bits are set, increment the counter.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numberOfSpecialChars(self, word: str) -&gt; int:\n        lower_mask = 0\n        upper_mask = 0\n        \n        for char in word:\n            if 'a' &lt;= char &lt;= 'z':\n                lower_mask |= 1 &lt;&lt; (ord(char) - ord('a'))\n            else:\n                upper_mask |= 1 &lt;&lt; (ord(char) - ord('A'))\n                \n        count = 0\n        for i in range(26):\n            if (lower_mask &gt;&gt; i) & 1 and (upper_mask &gt;&gt; i) & 1:\n                count += 1\n                \n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numberOfSpecialChars(String word) {\n        int lowerMask = 0;\n        int upperMask = 0;\n        \n        for (char c : word.toCharArray()) {\n            if (c &gt;= 'a' && c &lt;= 'z') {\n                lowerMask |= (1 &lt;&lt; (c - 'a'));\n            } else {\n                upperMask |= (1 &lt;&lt; (c - 'A'));\n            }\n        }\n        \n        int count = 0;\n        for (int i = 0; i &lt; 26; i++) {\n            if (((lowerMask &gt;&gt; i) & 1) == 1 && ((upperMask &gt;&gt; i) & 1) == 1) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numberOfSpecialChars(string word) {\n        int lowerMask = 0;\n        int upperMask = 0;\n        \n        for (char c : word) {\n            if (c &gt;= 'a' && c &lt;= 'z') {\n                lowerMask |= (1 &lt;&lt; (c - 'a'));\n            } else {\n                upperMask |= (1 &lt;&lt; (c - 'A'));\n            }\n        }\n        \n        int count = 0;\n        for (int i = 0; i &lt; 26; ++i) {\n            if ((lowerMask & (1 &lt;&lt; i)) && (upperMask & (1 &lt;&lt; i))) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var numberOfSpecialChars = function(word) {\n    let lowerMask = 0;\n    let upperMask = 0;\n    \n    for (const c of word) {\n        if (c &gt;= 'a' && c &lt;= 'z') {\n            lowerMask |= (1 &lt;&lt; (c.charCodeAt(0) - 97));\n        } else {\n            upperMask |= (1 &lt;&lt; (c.charCodeAt(0) - 65));\n        }\n    }\n    \n    let count = 0;\n    for (let i = 0; i &lt; 26; i++) {\n        if ((lowerMask & (1 &lt;&lt; i)) && (upperMask & (1 &lt;&lt; i))) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfSpecialChars(word: string): number {\n    let lowerMask = 0;\n    let upperMask = 0;\n    \n    for (const c of word) {\n        if (c &gt;= 'a' && c &lt;= 'z') {\n            lowerMask |= (1 &lt;&lt; (c.charCodeAt(0) - 97));\n        } else {\n            upperMask |= (1 &lt;&lt; (c.charCodeAt(0) - 65));\n        }\n    }\n    \n    let count = 0;\n    for (let i = 0; i &lt; 26; i++) {\n        if ((lowerMask & (1 &lt;&lt; i)) && (upperMask & (1 &lt;&lt; i))) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(N)
- Space: O(1)
- Notes: Extremely space-efficient, using only two integers to store the entire state.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Count the Number of Special Characters I Solutions","datePublished":"2025-06-12","description":"You are given a string word. A character is special if it appears in both lowercase and uppercase forms in word. Return the number of special characters.","keywords":"LeetCode, Count the Number of Special Characters I, Easy, Hash Table, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N), where N is the length of the string, as we iterate through the string once and then through the alphabet of constant size 26."}},{"@type":"Question","name":"What is the space complexity of the Bitmasking approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use two integer variables to store the bitmask, regardless of the input size."}},{"@type":"Question","name":"Why is the Boolean Array approach often preferred over Hash Sets?","acceptedAnswer":{"@type":"Answer","text":"The Boolean Array approach is often preferred because it uses direct indexing which is faster than the hashing mechanism of Sets, and it avoids the overhead of object creation."}}]}).replace(/</g,'\u003c')}
</script>