---
title: "Sort Array By Parity"
summary: "Move all even integers to the beginning of the array followed by all odd integers."
date: "Dec 01, 2025"
tags:
  - Array
  - Two Pointers
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 76.50% | **Paid:** No
> **Topics:** Array, Two Pointers, Sorting

Given an integer array nums, move all the even integers at the beginning of the array followed by all the odd integers.

Return any array that satisfies this condition.

- Examples
- Constraints
- [Two Pointers](#two-pointers)
- [Extra Space](#extra-space)
- [In-place Partition](#in-place-partition)

## Examples

**Example 1:**

```text
Input: nums = [3,1,2,4]
Output: [2,4,3,1]
Explanation: The outputs [4,2,3,1], [2,4,1,3], and [4,2,1,3] would also be accepted.
```

**Example 2:**

```text
Input: nums = [0]
Output: [0]
```

## Constraints

```text
1 <= nums.length <= 5000
0 <= nums[i] <= 5000
```

---
## Two Pointers

**Intuition**
Use two pointers starting from opposite ends of the array. The left pointer finds odd numbers while the right pointer finds even numbers, then swap them.

**Steps**
- Initialize left pointer at index 0 and right pointer at the last index
- Move left pointer forward while it points to an even number
- Move right pointer backward while it points to an odd number
- Swap elements at left and right pointers when left points to odd and right points to even
- Continue until pointers meet or cross

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sortArrayByParity(self, nums: List[int]) -&gt; List[int]:\n        left, right = 0, len(nums) - 1\n        while left &lt; right:\n            while left &lt; right and nums[left] % 2 == 0:\n                left += 1\n            while left &lt; right and nums[right] % 2 == 1:\n                right -= 1\n            if left &lt; right:\n                nums[left], nums[right] = nums[right], nums[left]\n                left += 1\n                right -= 1\n        return nums" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] sortArrayByParity(int[] nums) {\n        int left = 0, right = nums.length - 1;\n        while (left &lt; right) {\n            while (left &lt; right && nums[left] % 2 == 0) {\n                left++;\n            }\n            while (left &lt; right && nums[right] % 2 == 1) {\n                right--;\n            }\n            if (left &lt; right) {\n                int temp = nums[left];\n                nums[left] = nums[right];\n                nums[right] = temp;\n                left++;\n                right--;\n            }\n        }\n        return nums;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; sortArrayByParity(vector&lt;int&gt;& nums) {\n        int left = 0, right = nums.size() - 1;\n        while (left &lt; right) {\n            while (left &lt; right && nums[left] % 2 == 0) {\n                left++;\n            }\n            while (left &lt; right && nums[right] % 2 == 1) {\n                right--;\n            }\n            if (left &lt; right) {\n                swap(nums[left], nums[right]);\n                left++;\n                right--;\n            }\n        }\n        return nums;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar sortArrayByParity = function(nums) {\n    let left = 0, right = nums.length - 1;\n    while (left &lt; right) {\n        while (left &lt; right && nums[left] % 2 === 0) {\n            left++;\n        }\n        while (left &lt; right && nums[right] % 2 === 1) {\n            right--;\n        }\n        if (left &lt; right) {\n            [nums[left], nums[right]] = [nums[right], nums[left]];\n            left++;\n            right--;\n        }\n    }\n    return nums;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sortArrayByParity(nums: number[]): number[] {\n    let left = 0, right = nums.length - 1;\n    while (left &lt; right) {\n        while (left &lt; right && nums[left] % 2 === 0) {\n            left++;\n        }\n        while (left &lt; right && nums[right] % 2 === 1) {\n            right--;\n        }\n        if (left &lt; right) {\n            [nums[left], nums[right]] = [nums[right], nums[left]];\n            left++;\n            right--;\n        }\n    }\n    return nums;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal in-place solution with minimal swaps

---
## Extra Space

**Intuition**
Create a new array and first add all even numbers, then add all odd numbers from the original array.

**Steps**
- Initialize an empty result array
- Iterate through the input array and add all even numbers to result
- Iterate through the input array again and add all odd numbers to result
- Return the result array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sortArrayByParity(self, nums: List[int]) -&gt; List[int]:\n        result = []\n        for num in nums:\n            if num % 2 == 0:\n                result.append(num)\n        for num in nums:\n            if num % 2 == 1:\n                result.append(num)\n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] sortArrayByParity(int[] nums) {\n        int[] result = new int[nums.length];\n        int index = 0;\n        for (int num : nums) {\n            if (num % 2 == 0) {\n                result[index++] = num;\n            }\n        }\n        for (int num : nums) {\n            if (num % 2 == 1) {\n                result[index++] = num;\n            }\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; sortArrayByParity(vector&lt;int&gt;& nums) {\n        vector&lt;int&gt; result;\n        for (int num : nums) {\n            if (num % 2 == 0) {\n                result.push_back(num);\n            }\n        }\n        for (int num : nums) {\n            if (num % 2 == 1) {\n                result.push_back(num);\n            }\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar sortArrayByParity = function(nums) {\n    const result = [];\n    for (const num of nums) {\n        if (num % 2 === 0) {\n            result.push(num);\n        }\n    }\n    for (const num of nums) {\n        if (num % 2 === 1) {\n            result.push(num);\n        }\n    }\n    return result;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sortArrayByParity(nums: number[]): number[] {\n    const result: number[] = [];\n    for (const num of nums) {\n        if (num % 2 === 0) {\n            result.push(num);\n        }\n    }\n    for (const num of nums) {\n        if (num % 2 === 1) {\n            result.push(num);\n        }\n    }\n    return result;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Simple and readable but uses extra memory

---
## In-place Partition

**Intuition**
Use a single pointer to track where the next even number should be placed, similar to the partition step in quicksort.

**Steps**
- Initialize a pointer j at index 0
- Iterate through the array with pointer i
- When nums[i] is even, swap it with nums[j] and increment j
- Continue until the end of the array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sortArrayByParity(self, nums: List[int]) -&gt; List[int]:\n        j = 0\n        for i in range(len(nums)):\n            if nums[i] % 2 == 0:\n                nums[i], nums[j] = nums[j], nums[i]\n                j += 1\n        return nums" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] sortArrayByParity(int[] nums) {\n        int j = 0;\n        for (int i = 0; i &lt; nums.length; i++) {\n            if (nums[i] % 2 == 0) {\n                int temp = nums[i];\n                nums[i] = nums[j];\n                nums[j] = temp;\n                j++;\n            }\n        }\n        return nums;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; sortArrayByParity(vector&lt;int&gt;& nums) {\n        int j = 0;\n        for (int i = 0; i &lt; nums.size(); i++) {\n            if (nums[i] % 2 == 0) {\n                swap(nums[i], nums[j]);\n                j++;\n            }\n        }\n        return nums;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number[]}\n */\nvar sortArrayByParity = function(nums) {\n    let j = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (nums[i] % 2 === 0) {\n            [nums[i], nums[j]] = [nums[j], nums[i]];\n            j++;\n        }\n    }\n    return nums;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sortArrayByParity(nums: number[]): number[] {\n    let j = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (nums[i] % 2 === 0) {\n            [nums[i], nums[j]] = [nums[j], nums[i]];\n            j++;\n        }\n    }\n    return nums;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Clean in-place solution with single pass

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Sort Array By Parity Solutions","datePublished":"2025-12-01","description":"Move all even integers to the beginning of the array followed by all odd integers.","keywords":"LeetCode, Sort Array By Parity, Easy, Array, Two Pointers, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The Two Pointers approach has O(n) time complexity as each element is visited at most once."}},{"@type":"Question","name":"What is the space complexity of the Extra Space approach?","acceptedAnswer":{"@type":"Answer","text":"The Extra Space approach has O(n) space complexity as it creates a new array to store the result."}},{"@type":"Question","name":"Which approach is the most space-efficient?","acceptedAnswer":{"@type":"Answer","text":"Both the Two Pointers and In-place Partition approaches are the most space-efficient with O(1) space complexity."}}]}).replace(/</g,'\u003c')}
</script>