---
title: "Maximum Product of Two Elements in an Array"
summary: "Find the maximum value of (nums[i]-1)*(nums[j]-1) by choosing two different indices i and j from the array."
date: "Nov 23, 2024"
tags:
  - Array
  - Sorting
  - Heap
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 83.60% | **Paid:** No
> **Topics:** Array, Sorting, Heap (Priority Queue)

Given the array of integers nums, you will choose two different indices i and j of that array. Return the maximum value of (nums[i]-1)*(nums[j]-1).

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Sorting](#sorting)
- [Heap (Priority Queue)](#heap-priority-queue)
- [Single Pass](#single-pass)

## Examples

```text
Input: nums = [3,4,5,2]
Output: 12
Explanation: If you choose the indices i=1 and j=2 (indexed from 0), you will get the maximum value, that is, (nums[1]-1)*(nums[2]-1) = (4-1)*(5-1) = 3 * 4 = 12.
```

```text
Input: nums = [1,5,4,5]
Output: 16
Explanation: Choosing the indices i=1 and j=3 (indexed from 0), you will get the maximum value of (5-1)*(5-1) = 16.
```

```text
Input: nums = [3,7]
Output: 12
```

## Constraints

```text
2 <= nums.length <= 500
1 <= nums[i] <= 10^3
```

---
## Brute Force

**Intuition**
Check every possible pair of elements in the array to find the maximum product.

**Steps**
- Initialize a variable `maxProd` to store the maximum product found.
- Iterate through the array with two nested loops.
- For each pair `(i, j)` where `i &lt; j`, calculate `(nums[i] - 1) * (nums[j] - 1)`.
- Update `maxProd` if the current product is greater.
- Return `maxProd`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def maxProduct(self, nums: List[int]) -&gt; int:\n        max_prod = 0\n        n = len(nums)\n        for i in range(n):\n            for j in range(i + 1, n):\n                prod = (nums[i] - 1) * (nums[j] - 1)\n                if prod &gt; max_prod:\n                    max_prod = prod\n        return max_prod\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxProduct(int[] nums) {\n        int maxProd = 0;\n        int n = nums.length;\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int prod = (nums[i] - 1) * (nums[j] - 1);\n                if (prod &gt; maxProd) {\n                    maxProd = prod;\n                }\n            }\n        }\n        return maxProd;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int maxProduct(vector&lt;int&gt;& nums) {\n        int maxProd = 0;\n        int n = nums.size();\n        for (int i = 0; i &lt; n; i++) {\n            for (int j = i + 1; j &lt; n; j++) {\n                int prod = (nums[i] - 1) * (nums[j] - 1);\n                if (prod &gt; maxProd) {\n                    maxProd = prod;\n                }\n            }\n        }\n        return maxProd;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var maxProduct = function(nums) {\n    let maxProd = 0;\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            const prod = (nums[i] - 1) * (nums[j] - 1);\n            if (prod &gt; maxProd) {\n                maxProd = prod;\n            }\n        }\n    }\n    return maxProd;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function maxProduct(nums: number[]): number {\n    let maxProd = 0;\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        for (let j = i + 1; j &lt; n; j++) {\n            const prod = (nums[i] - 1) * (nums[j] - 1);\n            if (prod &gt; maxProd) {\n                maxProd = prod;\n            }\n        }\n    }\n    return maxProd;\n}\n" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1)
- Notes: Simple but inefficient for large arrays.

---
## Sorting

**Intuition**
The maximum product will be formed by the two largest numbers in the array. Sorting allows us to easily access these two numbers.

**Steps**
- Sort the array in ascending order.
- The two largest numbers will be at the end of the array.
- Calculate the product using the last two elements.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def maxProduct(self, nums: List[int]) -&gt; int:\n        nums.sort()\n        return (nums[-1] - 1) * (nums[-2] - 1)\n" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int maxProduct(int[] nums) {\n        Arrays.sort(nums);\n        int n = nums.length;\n        return (nums[n - 1] - 1) * (nums[n - 2] - 1);\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int maxProduct(vector&lt;int&gt;& nums) {\n        sort(nums.begin(), nums.end());\n        int n = nums.size();\n        return (nums[n - 1] - 1) * (nums[n - 2] - 1);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var maxProduct = function(nums) {\n    nums.sort((a, b) =&gt; a - b);\n    const n = nums.length;\n    return (nums[n - 1] - 1) * (nums[n - 2] - 1);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function maxProduct(nums: number[]): number {\n    nums.sort((a, b) =&gt; a - b);\n    const n = nums.length;\n    return (nums[n - 1] - 1) * (nums[n - 2] - 1);\n}\n" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(1) or O(n) depending on the sorting algorithm.
- Notes: More efficient than brute force, but sorting is not strictly necessary.

---
## Heap (Priority Queue)

**Intuition**
We can use a Max-Heap to efficiently retrieve the two largest elements in the array.

**Steps**
- Insert all elements into a Max-Heap.
- Extract the largest element (root).
- Extract the second largest element (new root).
- Calculate the product.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import heapq\nfrom typing import List\n\nclass Solution:\n    def maxProduct(self, nums: List[int]) -&gt; int:\n        # Python's heapq is a min-heap, so we invert values to simulate a max-heap\n        max_heap = [-x for x in nums]\n        heapq.heapify(max_heap)\n        \n        first = -heapq.heappop(max_heap)\n        second = -heapq.heappop(max_heap)\n        \n        return (first - 1) * (second - 1)\n" },
{ name: "Java", lang: "java", code: "import java.util.Collections;\nimport java.util.PriorityQueue;\n\nclass Solution {\n    public int maxProduct(int[] nums) {\n        PriorityQueue&lt;Integer&gt; pq = new PriorityQueue&lt;&gt;(Collections.reverseOrder());\n        for (int num : nums) {\n            pq.offer(num);\n        }\n        int first = pq.poll();\n        int second = pq.poll();\n        return (first - 1) * (second - 1);\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;queue&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int maxProduct(vector&lt;int&gt;& nums) {\n        priority_queue&lt;int&gt; pq(nums.begin(), nums.end());\n        int first = pq.top(); pq.pop();\n        int second = pq.top(); pq.pop();\n        return (first - 1) * (second - 1);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class MaxHeap {\n    constructor() {\n        this.heap = [];\n    }\n\n    push(val) {\n        this.heap.push(val);\n        this.bubbleUp(this.heap.length - 1);\n    }\n\n    bubbleUp(index) {\n        while (index &gt; 0) {\n            let parent = Math.floor((index - 1) / 2);\n            if (this.heap[parent] &gt;= this.heap[index]) break;\n            [this.heap[parent], this.heap[index]] = [this.heap[index], this.heap[parent]];\n            index = parent;\n        }\n    }\n\n    pop() {\n        const max = this.heap[0];\n        const end = this.heap.pop();\n        if (this.heap.length &gt; 0) {\n            this.heap[0] = end;\n            this.sinkDown(0);\n        }\n        return max;\n    }\n\n    sinkDown(index) {\n        const length = this.heap.length;\n        while (true) {\n            let leftChild = 2 * index + 1;\n            let rightChild = 2 * index + 2;\n            let swap = null;\n\n            if (leftChild &lt; length && this.heap[leftChild] &gt; this.heap[index]) {\n                swap = leftChild;\n            }\n            if (rightChild &lt; length) {\n                if ((swap === null && this.heap[rightChild] &gt; this.heap[index]) ||\n                    (swap !== null && this.heap[rightChild] &gt; this.heap[leftChild])) {\n                    swap = rightChild;\n                }\n            }\n            if (swap === null) break;\n            [this.heap[index], this.heap[swap]] = [this.heap[swap], this.heap[index]];\n            index = swap;\n        }\n    }\n}\n\nvar maxProduct = function(nums) {\n    const pq = new MaxHeap();\n    for (const num of nums) {\n        pq.push(num);\n    }\n    const first = pq.pop();\n    const second = pq.pop();\n    return (first - 1) * (second - 1);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "class MaxHeap {\n    heap: number[] = [];\n\n    push(val: number): void {\n        this.heap.push(val);\n        this.bubbleUp(this.heap.length - 1);\n    }\n\n    bubbleUp(index: number): void {\n        while (index &gt; 0) {\n            let parent = Math.floor((index - 1) / 2);\n            if (this.heap[parent] &gt;= this.heap[index]) break;\n            [this.heap[parent], this.heap[index]] = [this.heap[index], this.heap[parent]];\n            index = parent;\n        }\n    }\n\n    pop(): number {\n        const max = this.heap[0];\n        const end = this.heap.pop();\n        if (this.heap.length &gt; 0) {\n            this.heap[0] = end!;\n            this.sinkDown(0);\n        }\n        return max;\n    }\n\n    sinkDown(index: number): void {\n        const length = this.heap.length;\n        while (true) {\n            let leftChild = 2 * index + 1;\n            let rightChild = 2 * index + 2;\n            let swap: number | null = null;\n\n            if (leftChild &lt; length && this.heap[leftChild] &gt; this.heap[index]) {\n                swap = leftChild;\n            }\n            if (rightChild &lt; length) {\n                if ((swap === null && this.heap[rightChild] &gt; this.heap[index]) ||\n                    (swap !== null && this.heap[rightChild] &gt; this.heap[leftChild])) {\n                    swap = rightChild;\n                }\n            }\n            if (swap === null) break;\n            [this.heap[index], this.heap[swap]] = [this.heap[swap], this.heap[index]];\n            index = swap;\n        }\n    }\n}\n\nfunction maxProduct(nums: number[]): number {\n    const pq = new MaxHeap();\n    for (const num of nums) {\n        pq.push(num);\n    }\n    const first = pq.pop();\n    const second = pq.pop();\n    return (first - 1) * (second - 1);\n}\n" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(n)
- Notes: Useful if we need to dynamically access top K elements, but overkill for just two.

---
## Single Pass

**Intuition**
We only need the two largest numbers. We can find them in a single traversal without sorting or a heap.

**Steps**
- Initialize two variables, `max1` and `max2`, to keep track of the largest and second largest numbers found so far.
- Iterate through the array.
- If the current number is greater than `max1`, update `max2` to be `max1`, and `max1` to be the current number.
- Else if the current number is greater than `max2`, update `max2` to be the current number.
- Return the product `(max1 - 1) * (max2 - 1)`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def maxProduct(self, nums: List[int]) -&gt; int:\n        max1 = max2 = 0\n        for num in nums:\n            if num &gt; max1:\n                max2 = max1\n                max1 = num\n            elif num &gt; max2:\n                max2 = num\n        return (max1 - 1) * (max2 - 1)\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxProduct(int[] nums) {\n        int max1 = 0, max2 = 0;\n        for (int num : nums) {\n            if (num &gt; max1) {\n                max2 = max1;\n                max1 = num;\n            } else if (num &gt; max2) {\n                max2 = num;\n            }\n        }\n        return (max1 - 1) * (max2 - 1);\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int maxProduct(vector&lt;int&gt;& nums) {\n        int max1 = 0, max2 = 0;\n        for (int num : nums) {\n            if (num &gt; max1) {\n                max2 = max1;\n                max1 = num;\n            } else if (num &gt; max2) {\n                max2 = num;\n            }\n        }\n        return (max1 - 1) * (max2 - 1);\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var maxProduct = function(nums) {\n    let max1 = 0, max2 = 0;\n    for (const num of nums) {\n        if (num &gt; max1) {\n            max2 = max1;\n            max1 = num;\n        } else if (num &gt; max2) {\n            max2 = num;\n        }\n    }\n    return (max1 - 1) * (max2 - 1);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function maxProduct(nums: number[]): number {\n    let max1 = 0, max2 = 0;\n    for (const num of nums) {\n        if (num &gt; max1) {\n            max2 = max1;\n            max1 = num;\n        } else if (num &gt; max2) {\n            max2 = num;\n        }\n    }\n    return (max1 - 1) * (max2 - 1);\n}\n" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: The most optimal approach for this problem.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Maximum Product of Two Elements in an Array Solutions","datePublished":"2024-11-23","description":"Find the maximum value of (nums[i]-1)*(nums[j]-1) by choosing two different indices i and j from the array.","keywords":"LeetCode, Maximum Product of Two Elements in an Array, Easy, Array, Sorting, Heap (Priority Queue)"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n²), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the most optimal time complexity for this problem?","acceptedAnswer":{"@type":"Answer","text":"The most optimal time complexity is O(n), achieved by the Single Pass approach."}},{"@type":"Question","name":"How does the Sorting approach work?","acceptedAnswer":{"@type":"Answer","text":"The Sorting approach sorts the array in O(n log n) time and then multiplies the two largest elements found at the end of the sorted array."}},{"@type":"Question","name":"Why is the Single Pass approach optimal?","acceptedAnswer":{"@type":"Answer","text":"The Single Pass approach is optimal because it finds the two largest numbers in a single iteration, resulting in O(n) time complexity and O(1) space complexity."}}]}).replace(/</g,'\u003c')}
</script>