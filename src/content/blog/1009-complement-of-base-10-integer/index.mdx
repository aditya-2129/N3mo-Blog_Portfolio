---
title: "Complement of Base 10 Integer"
summary: "The complement of an integer is the integer you get when you flip all the 0's to 1's and all the 1's to 0's in its binary representation."
date: "Jul 03, 2024"
tags:
  - Bit Manipulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 63.40% | **Paid:** No
> **Topics:** Bit Manipulation

The complement of an integer is the integer you get when you flip all the 0's to 1's and all the 1's to 0's in its binary representation.

For example, The integer 5 is "101" in binary, its complement is "010" which is the integer 2.

Given an integer n, return its complement.

- Examples
- Constraints
- [Bit Manipulation with Mask](#bit-manipulation-with-mask)
- [Bit Shifting](#bit-shifting)
- [String Conversion](#string-conversion)

## Examples

```text
Input: n = 5
Output: 2
Explanation: 5 is "101" in binary, with complement "010" in binary, which is 2 in base-10.
```

```text
Input: n = 7
Output: 0
Explanation: 7 is "111" in binary, with complement "000" in binary, which is 0 in base-10.
```

```text
Input: n = 10
Output: 5
Explanation: 10 is "1010" in binary, with complement "0101" in binary, which is 5 in base-10.
```

## Constraints

```text
0 <= num <= 10^9
```

---

## Bit Manipulation with Mask

**Intuition**
To find the complement, we can XOR the number `n` with a mask that has all bits set to 1 up to the most significant bit (MSB) of `n`. XORing a bit with 1 flips it.

**Steps**
- Handle the edge case where `n` is 0 (return 1).
- Calculate the number of bits required to represent `n`.
- Create a mask by shifting 1 left by the bit count and subtracting 1 (e.g., for 3 bits, `1 &lt;&lt; 3` is 1000, minus 1 is 111).
- Return `n ^ mask`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def bitwiseComplement(self, n: int) -&gt; int:\n        if n == 0:\n            return 1\n        mask = (1 &lt;&lt; n.bit_length()) - 1\n        return n ^ mask" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int bitwiseComplement(int n) {\n        if (n == 0) return 1;\n        int mask = (Integer.highestOneBit(n) &lt;&lt; 1) - 1;\n        return n ^ mask;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int bitwiseComplement(int n) {\n        if (n == 0) return 1;\n        int mask = (1 &lt;&lt; (int)(log2(n) + 1)) - 1;\n        return n ^ mask;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var bitwiseComplement = function(n) {\n    if (n === 0) return 1;\n    let mask = (1 &lt;&lt; n.toString(2).length) - 1;\n    return n ^ mask;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function bitwiseComplement(n: number): number {\n    if (n === 0) return 1;\n    let mask = (1 &lt;&lt; n.toString(2).length) - 1;\n    return n ^ mask;\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This is the most efficient approach using built-in bit functions.

---

## Bit Shifting

**Intuition**
We can build the mask bit by bit. We iterate through the bits of `n`, shifting the mask left and adding 1 until we have processed all bits of `n`.

**Steps**
- If `n` is 0, return 1.
- Initialize `mask` to 0 and `temp` to `n`.
- While `temp` is greater than 0, shift `mask` left by 1, OR it with 1, and shift `temp` right by 1.
- Finally, return `n ^ mask`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def bitwiseComplement(self, n: int) -&gt; int:\n        if n == 0:\n            return 1\n        mask = 0\n        temp = n\n        while temp &gt; 0:\n            mask = (mask &lt;&lt; 1) | 1\n            temp &gt;&gt;= 1\n        return n ^ mask" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int bitwiseComplement(int n) {\n        if (n == 0) return 1;\n        int mask = 0;\n        int temp = n;\n        while (temp &gt; 0) {\n            mask = (mask &lt;&lt; 1) | 1;\n            temp &gt;&gt;= 1;\n        }\n        return n ^ mask;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int bitwiseComplement(int n) {\n        if (n == 0) return 1;\n        int mask = 0;\n        int temp = n;\n        while (temp &gt; 0) {\n            mask = (mask &lt;&lt; 1) | 1;\n            temp &gt;&gt;= 1;\n        }\n        return n ^ mask;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var bitwiseComplement = function(n) {\n    if (n === 0) return 1;\n    let mask = 0;\n    let temp = n;\n    while (temp &gt; 0) {\n        mask = (mask &lt;&lt; 1) | 1;\n        temp &gt;&gt;= 1;\n    }\n    return n ^ mask;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function bitwiseComplement(n: number): number {\n    if (n === 0) return 1;\n    let mask = 0;\n    let temp = n;\n    while (temp &gt; 0) {\n        mask = (mask &lt;&lt; 1) | 1;\n        temp &gt;&gt;= 1;\n    }\n    return n ^ mask;\n}" },]} />

**Complexity**
- Time: O(log n)
- Space: O(1)
- Notes: Iterates once per bit of the number.

---

## String Conversion

**Intuition**
Convert the integer to its binary string representation, iterate through the string to flip the bits, and then convert the result back to an integer.

**Steps**
- If `n` is 0, return 1.
- Convert `n` to a binary string.
- Create a new string by flipping '0' to '1' and '1' to '0'.
- Parse the new binary string back to an integer.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def bitwiseComplement(self, n: int) -&gt; int:\n        if n == 0:\n            return 1\n        binary = bin(n)[2:]\n        complement = \"\"\n        for bit in binary:\n            if bit == \"0\":\n                complement += \"1\"\n            else:\n                complement += \"0\"\n        return int(complement, 2)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int bitwiseComplement(int n) {\n        if (n == 0) return 1;\n        String binary = Integer.toBinaryString(n);\n        StringBuilder complement = new StringBuilder();\n        for (char bit : binary.toCharArray()) {\n            if (bit == '0') {\n                complement.append('1');\n            } else {\n                complement.append('0');\n            }\n        }\n        return Integer.parseInt(complement.toString(), 2);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int bitwiseComplement(int n) {\n        if (n == 0) return 1;\n        string binary;\n        int temp = n;\n        while (temp &gt; 0) {\n            binary = (char)('0' + (temp % 2)) + binary;\n            temp /= 2;\n        }\n        for (char &bit : binary) {\n            bit = (bit == '0') ? '1' : '0';\n        }\n        return stoi(binary, nullptr, 2);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var bitwiseComplement = function(n) {\n    if (n === 0) return 1;\n    let binary = n.toString(2);\n    let complement = \"\";\n    for (let i = 0; i &lt; binary.length; i++) {\n        complement += (binary[i] === \"0\") ? \"1\" : \"0\";\n    }\n    return parseInt(complement, 2);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function bitwiseComplement(n: number): number {\n    if (n === 0) return 1;\n    let binary = n.toString(2);\n    let complement = \"\";\n    for (let i = 0; i &lt; binary.length; i++) {\n        complement += (binary[i] === \"0\") ? \"1\" : \"0\";\n    }\n    return parseInt(complement, 2);\n}" },]} />

**Complexity**
- Time: O(log n)
- Space: O(log n)
- Notes: Less efficient due to string manipulation overhead.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Complement of Base 10 Integer Solutions","datePublished":"2024-07-03","description":"The complement of an integer is the integer you get when you flip all the 0's to 1's and all the 1's to 0's in its binary representation.","keywords":"LeetCode, Complement of Base 10 Integer, Easy, Bit Manipulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Bit Manipulation with Mask approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) because we perform a constant number of bitwise operations regardless of the input size."}},{"@type":"Question","name":"What is the space complexity of the Bit Manipulation with Mask approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a few extra variables."}},{"@type":"Question","name":"What is the time complexity of the String Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(log n) because we iterate through the binary representation of the number, which has log n bits."}},{"@type":"Question","name":"What is the space complexity of the String Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(log n) to store the binary string representation."}}]}).replace(/</g,'\u003c')}
</script>