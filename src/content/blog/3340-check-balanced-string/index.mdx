---
title: "Check Balanced String"
summary: "Determine if a numeric string is balanced by comparing the sum of digits in the first half with the sum of digits in the second half."
date: "May 26, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 82.60% | **Paid:** No
> **Topics:** String

You are given a string `s` consisting only of digits. A string is balanced if the sum of the digits in the first half of the string is equal to the sum of the digits in the second half of the string.

Return `true` if `s` is balanced, otherwise return `false`.

- Examples
- Constraints
- [Two-Pass Summation](#two-pass-summation)
- [Single Pass Two Pointers](#single-pass-two-pointers)
- [Single Pass Net Balance](#single-pass-net-balance)

## Examples

### Example 1

**Input:**
```text
num = "1234"
```

**Output:**
```text
false
```

**Explanation:**
The sum of digits at even indices is 1 + 3 == 4, and the sum of digits at odd indices is 2 + 4 == 6.

Since 4 is not equal to 6, num is not balanced.

### Example 2

**Input:**
```text
num = "24123"
```

**Output:**
```text
true
```

**Explanation:**
The sum of digits at even indices is 2 + 1 + 3 == 6, and the sum of digits at odd indices is 4 + 2 == 6.

Since both are equal the num is balanced.

## Constraints

```text
2 <= s.length <= 100
s.length is even.
s consists of only digits.
```

---
## Two-Pass Summation

**Intuition**
We can simply iterate through the first half of the string to calculate the sum of those digits, then iterate through the second half to calculate the sum of the remaining digits. Finally, we compare the two sums.

**Steps**
- Calculate the midpoint of the string length.
- Iterate from the start to the midpoint, converting characters to integers and accumulating the sum in `sum1`.
- Iterate from the midpoint to the end, converting characters to integers and accumulating the sum in `sum2`.
- Return `true` if `sum1` equals `sum2`, otherwise `false`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def isBalanced(self, s: str) -> bool:
        n = len(s)
        half = n // 2
        sum1 = 0
        for i in range(half):
            sum1 += int(s[i])
        
        sum2 = 0
        for i in range(half, n):
            sum2 += int(s[i])
            
        return sum1 == sum2
` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean isBalanced(String s) {
        int n = s.length();
        int half = n / 2;
        int sum1 = 0;
        for (int i = 0; i &lt; half; i++) {
            sum1 += s.charAt(i) - '0';
        }
        
        int sum2 = 0;
        for (int i = half; i &lt; n; i++) {
            sum2 += s.charAt(i) - '0';
        }
        
        return sum1 == sum2;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool isBalanced(string s) {
        int n = s.length();
        int half = n / 2;
        int sum1 = 0;
        for (int i = 0; i &lt; half; i++) {
            sum1 += s[i] - '0';
        }
        
        int sum2 = 0;
        for (int i = half; i &lt; n; i++) {
            sum2 += s[i] - '0';
        }
        
        return sum1 == sum2;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    isBalanced(s) {
        const n = s.length;
        const half = Math.floor(n / 2);
        let sum1 = 0;
        for (let i = 0; i &lt; half; i++) {
            sum1 += parseInt(s[i]);
        }
        
        let sum2 = 0;
        for (let i = half; i &lt; n; i++) {
            sum2 += parseInt(s[i]);
        }
        
        return sum1 === sum2;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    isBalanced(s: string): boolean {
        const n = s.length;
        const half = Math.floor(n / 2);
        let sum1 = 0;
        for (let i = 0; i &lt; half; i++) {
            sum1 += parseInt(s[i]);
        }
        
        let sum2 = 0;
        for (let i = half; i &lt; n; i++) {
            sum2 += parseInt(s[i]);
        }
        
        return sum1 === sum2;
    }
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the string. We iterate through the string exactly once (split into two loops).
- Space: O(1) as we only use a few integer variables for storage.
- Notes: This is the most straightforward approach but requires two separate loop structures.

---
## Single Pass Two Pointers

**Intuition**
Instead of calculating sums separately, we can iterate through both halves simultaneously using two pointers. One pointer starts at the beginning, and the other starts at the midpoint. We accumulate sums for both halves in a single loop.

**Steps**
- Initialize `left` pointer to 0 and `right` pointer to `n / 2`.
- Initialize `sum1` and `sum2` to 0.
- Loop while `right` is less than `n`.
- Add the digit at `s[left]` to `sum1` and the digit at `s[right]` to `sum2`.
- Increment both `left` and `right`.
- Return `true` if `sum1` equals `sum2`, otherwise `false`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def isBalanced(self, s: str) -> bool:
        n = len(s)
        half = n // 2
        sum1 = 0
        sum2 = 0
        
        for i in range(half):
            sum1 += int(s[i])
            sum2 += int(s[half + i])
            
        return sum1 == sum2
` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean isBalanced(String s) {
        int n = s.length();
        int half = n / 2;
        int sum1 = 0;
        int sum2 = 0;
        
        for (int i = 0; i &lt; half; i++) {
            sum1 += s.charAt(i) - '0';
            sum2 += s.charAt(half + i) - '0';
        }
        
        return sum1 == sum2;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool isBalanced(string s) {
        int n = s.length();
        int half = n / 2;
        int sum1 = 0;
        int sum2 = 0;
        
        for (int i = 0; i &lt; half; i++) {
            sum1 += s[i] - '0';
            sum2 += s[half + i] - '0';
        }
        
        return sum1 == sum2;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    isBalanced(s) {
        const n = s.length;
        const half = Math.floor(n / 2);
        let sum1 = 0;
        let sum2 = 0;
        
        for (let i = 0; i &lt; half; i++) {
            sum1 += parseInt(s[i]);
            sum2 += parseInt(s[half + i]);
        }
        
        return sum1 === sum2;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    isBalanced(s: string): boolean {
        const n = s.length;
        const half = Math.floor(n / 2);
        let sum1 = 0;
        let sum2 = 0;
        
        for (let i = 0; i &lt; half; i++) {
            sum1 += parseInt(s[i]);
            sum2 += parseInt(s[half + i]);
        }
        
        return sum1 === sum2;
    }
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the string. We iterate `n/2` times, performing constant work each time.
- Space: O(1) as we only use a few integer variables.
- Notes: Slightly cleaner loop structure than the two-pass approach, but effectively the same performance.

---
## Single Pass Net Balance

**Intuition**
We can optimize space slightly (conceptually) by maintaining a single "balance" variable. We add digits from the first half to this balance and subtract digits from the second half. If the string is balanced, the final result will be zero.

**Steps**
- Initialize `balance` to 0.
- Iterate through the string with index `i` from 0 to `n - 1`.
- If `i` is in the first half (`i &lt; n / 2`), add the digit value to `balance`.
- If `i` is in the second half (`i &gt;= n / 2`), subtract the digit value from `balance`.
- Return `true` if `balance` is 0, otherwise `false`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def isBalanced(self, s: str) -> bool:
        n = len(s)
        half = n // 2
        balance = 0
        
        for i in range(n):
            digit = int(s[i])
            if i &lt; half:
                balance += digit
            else:
                balance -= digit
                
        return balance == 0
` },
{ name: "Java", lang: "java", code: `class Solution {
    public boolean isBalanced(String s) {
        int n = s.length();
        int half = n / 2;
        int balance = 0;
        
        for (int i = 0; i &lt; n; i++) {
            int digit = s.charAt(i) - '0';
            if (i &lt; half) {
                balance += digit;
            } else {
                balance -= digit;
            }
        }
        
        return balance == 0;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    bool isBalanced(string s) {
        int n = s.length();
        int half = n / 2;
        int balance = 0;
        
        for (int i = 0; i &lt; n; i++) {
            int digit = s[i] - '0';
            if (i &lt; half) {
                balance += digit;
            } else {
                balance -= digit;
            }
        }
        
        return balance == 0;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    isBalanced(s) {
        const n = s.length;
        const half = Math.floor(n / 2);
        let balance = 0;
        
        for (let i = 0; i &lt; n; i++) {
            const digit = parseInt(s[i]);
            if (i &lt; half) {
                balance += digit;
            } else {
                balance -= digit;
            }
        }
        
        return balance === 0;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    isBalanced(s: string): boolean {
        const n = s.length;
        const half = Math.floor(n / 2);
        let balance = 0;
        
        for (let i = 0; i &lt; n; i++) {
            const digit = parseInt(s[i]);
            if (i &lt; half) {
                balance += digit;
            } else {
                balance -= digit;
            }
        }
        
        return balance === 0;
    }
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the string. We iterate through the string exactly once.
- Space: O(1) as we only use a single integer variable for the balance.
- Notes: This is the most elegant solution as it reduces the problem to a single accumulation check.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check Balanced String Solutions","datePublished":"2025-05-26","description":"Determine if a numeric string is balanced by comparing the sum of digits in the first half with the sum of digits in the second half.","keywords":"LeetCode, Check Balanced String, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Two-Pass Summation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string, as we iterate through all characters."}},{"@type":"Question","name":"What is the space complexity of the Single Pass Net Balance approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use a single integer variable to track the balance."}},{"@type":"Question","name":"How does the Single Pass Net Balance approach work?","acceptedAnswer":{"@type":"Answer","text":"It adds digits from the first half to a running balance and subtracts digits from the second half. If the string is balanced, the final balance will be zero."}}]}).replace(/</g,'\u003c')}
</script>