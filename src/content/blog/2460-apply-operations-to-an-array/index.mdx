---
title: "Apply Operations to an Array"
summary: "Apply operations to adjacent equal elements, then shift all zeros to the end of the array."
date: "Mar 18, 2024"
tags:
  - Array
  - Two Pointers
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 74.70% | **Paid:** No
> **Topics:** Array, Two Pointers, Simulation

You are given a 0-indexed array nums of size n consisting of non-negative integers.

You need to apply n - 1 operations to this array where, in the ith operation (0-indexed), you will apply the following on the element with index i:

If nums[i] == nums[i + 1], then nums[i] becomes 2 * nums[i], otherwise nums[i] becomes 0.
After performing all operations, shift all the 0's of the array to the right end of the array.

For example, the array [1,0,2,0,0,1] after shifting all 0's to the right becomes [1,2,1,0,0,0].

Return the resulting array.

- Examples
- Constraints
- [In-place Simulation](#in-place-simulation)
- [Auxiliary Array](#auxiliary-array)

## Examples

Example 1:
```text
Input: nums = [1,2,2,1,1,0]
Output: [1,4,2,0,0,0]
Explanation: We do the following operations:
- i = 0: nums[0] and nums[1] are not equal, so we skip this operation.
- i = 1: nums[1] and nums[2] are equal, we double nums[1] and replace nums[2] with 0. The array becomes [1,4,0,1,1,0].
- i = 2: nums[2] and nums[3] are not equal, so we skip this operation.
- i = 3: nums[3] and nums[4] are equal, we double nums[3] and replace nums[4] with 0. The array becomes [1,4,0,2,0,0].
- i = 4: nums[4] and nums[5] are not equal, so we skip this operation.
After performing the operations, the array becomes [1,4,0,2,0,0].
After shifting the zeros, the array becomes [1,4,2,0,0,0].
```

Example 2:
```text
Input: nums = [0,1]
Output: [1,0]
Explanation: No operation is needed, just shift the 0 to the right.
```

## Constraints

```text
1 <= nums.length <= 2000
0 <= nums[i] <= 1000
```

---
## In-place Simulation

**Intuition**
We first iterate through the array to apply the doubling and zeroing logic. Then, we use a two-pointer technique to move all non-zero elements to the front, effectively shifting zeros to the end without using extra space.

**Steps**
- Iterate from the first element to the second-to-last element.
- If the current element equals the next element, double the current element and set the next element to 0.
- Use a pointer to track the position for the next non-zero element.
- Iterate through the array again, copying non-zero elements to the front.
- Fill the remaining positions in the array with zeros.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def applyOperations(self, nums: List[int]) -&gt; List[int]:\n        n = len(nums)\n        \n        # Step 1: Apply operations\n        for i in range(n - 1):\n            if nums[i] == nums[i + 1]:\n                nums[i] *= 2\n                nums[i + 1] = 0\n        \n        # Step 2: Shift zeros to the end\n        insert_pos = 0\n        for i in range(n):\n            if nums[i] != 0:\n                nums[insert_pos] = nums[i]\n                insert_pos += 1\n        \n        # Fill remaining with zeros\n        while insert_pos &lt; n:\n            nums[insert_pos] = 0\n            insert_pos += 1\n            \n        return nums" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] applyOperations(int[] nums) {\n        int n = nums.length;\n        \n        // Step 1: Apply operations\n        for (int i = 0; i &lt; n - 1; i++) {\n            if (nums[i] == nums[i + 1]) {\n                nums[i] *= 2;\n                nums[i + 1] = 0;\n            }\n        }\n        \n        // Step 2: Shift zeros to the end\n        int insertPos = 0;\n        for (int i = 0; i &lt; n; i++) {\n            if (nums[i] != 0) {\n                nums[insertPos++] = nums[i];\n            }\n        }\n        \n        // Fill remaining with zeros\n        while (insertPos &lt; n) {\n            nums[insertPos++] = 0;\n        }\n        \n        return nums;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; applyOperations(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        \n        // Step 1: Apply operations\n        for (int i = 0; i &lt; n - 1; ++i) {\n            if (nums[i] == nums[i + 1]) {\n                nums[i] *= 2;\n                nums[i + 1] = 0;\n            }\n        }\n        \n        // Step 2: Shift zeros to the end\n        int insertPos = 0;\n        for (int i = 0; i &lt; n; ++i) {\n            if (nums[i] != 0) {\n                nums[insertPos++] = nums[i];\n            }\n        }\n        \n        // Fill remaining with zeros\n        while (insertPos &lt; n) {\n            nums[insertPos++] = 0;\n        }\n        \n        return nums;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var applyOperations = function(nums) {\n    const n = nums.length;\n    \n    // Step 1: Apply operations\n    for (let i = 0; i &lt; n - 1; i++) {\n        if (nums[i] === nums[i + 1]) {\n            nums[i] *= 2;\n            nums[i + 1] = 0;\n        }\n    }\n    \n    // Step 2: Shift zeros to the end\n    let insertPos = 0;\n    for (let i = 0; i &lt; n; i++) {\n        if (nums[i] !== 0) {\n            nums[insertPos++] = nums[i];\n        }\n    }\n    \n    // Fill remaining with zeros\n    while (insertPos &lt; n) {\n        nums[insertPos++] = 0;\n    }\n    \n    return nums;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function applyOperations(nums: number[]): number[] {\n    const n = nums.length;\n    \n    // Step 1: Apply operations\n    for (let i = 0; i &lt; n - 1; i++) {\n        if (nums[i] === nums[i + 1]) {\n            nums[i] *= 2;\n            nums[i + 1] = 0;\n        }\n    }\n    \n    // Step 2: Shift zeros to the end\n    let insertPos = 0;\n    for (let i = 0; i &lt; n; i++) {\n        if (nums[i] !== 0) {\n            nums[insertPos++] = nums[i];\n        }\n    }\n    \n    // Fill remaining with zeros\n    while (insertPos &lt; n) {\n        nums[insertPos++] = 0;\n    }\n    \n    return nums;\n}" }]} />

**Complexity**
- Time: O(n) — We traverse the array a constant number of times.
- Space: O(1) — We modify the array in place without using extra data structures proportional to the input size.
- Notes: This is the most space-efficient approach.

---
## Auxiliary Array

**Intuition**
We create a new array to store the result. We iterate through the original array, apply the operations, and immediately place non-zero elements into the new array. Finally, we fill the rest of the new array with zeros.

**Steps**
- Create a result array of the same size as the input.
- Iterate through the input array up to the second-to-last element.
- Apply the doubling and zeroing logic to the input array.
- Iterate through the modified input array and copy non-zero elements to the result array.
- Fill the remaining slots in the result array with zeros.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def applyOperations(self, nums: List[int]) -&gt; List[int]:\n        n = len(nums)\n        res = []\n        \n        # Apply operations\n        for i in range(n - 1):\n            if nums[i] == nums[i + 1]:\n                nums[i] *= 2\n                nums[i + 1] = 0\n        \n        # Build result array\n        for num in nums:\n            if num != 0:\n                res.append(num)\n        \n        # Append remaining zeros\n        res.extend([0] * (n - len(res)))\n        return res" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] applyOperations(int[] nums) {\n        int n = nums.length;\n        int[] res = new int[n];\n        \n        // Apply operations\n        for (int i = 0; i &lt; n - 1; i++) {\n            if (nums[i] == nums[i + 1]) {\n                nums[i] *= 2;\n                nums[i + 1] = 0;\n            }\n        }\n        \n        // Build result array\n        int idx = 0;\n        for (int num : nums) {\n            if (num != 0) {\n                res[idx++] = num;\n            }\n        }\n        // Remaining elements are already 0 by default in Java\n        \n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; applyOperations(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        vector&lt;int&gt; res;\n        \n        // Apply operations\n        for (int i = 0; i &lt; n - 1; ++i) {\n            if (nums[i] == nums[i + 1]) {\n                nums[i] *= 2;\n                nums[i + 1] = 0;\n            }\n        }\n        \n        // Build result array\n        for (int num : nums) {\n            if (num != 0) {\n                res.push_back(num);\n            }\n        }\n        \n        // Fill remaining zeros\n        while (res.size() &lt; n) {\n            res.push_back(0);\n        }\n        \n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var applyOperations = function(nums) {\n    const n = nums.length;\n    const res = [];\n    \n    // Apply operations\n    for (let i = 0; i &lt; n - 1; i++) {\n        if (nums[i] === nums[i + 1]) {\n            nums[i] *= 2;\n            nums[i + 1] = 0;\n        }\n    }\n    \n    // Build result array\n    for (let i = 0; i &lt; n; i++) {\n        if (nums[i] !== 0) {\n            res.push(nums[i]);\n        }\n    }\n    \n    // Fill remaining zeros\n    while (res.length &lt; n) {\n        res.push(0);\n    }\n    \n    return res;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function applyOperations(nums: number[]): number[] {\n    const n = nums.length;\n    const res: number[] = [];\n    \n    // Apply operations\n    for (let i = 0; i &lt; n - 1; i++) {\n        if (nums[i] === nums[i + 1]) {\n            nums[i] *= 2;\n            nums[i + 1] = 0;\n        }\n    }\n    \n    // Build result array\n    for (let i = 0; i &lt; n; i++) {\n        if (nums[i] !== 0) {\n            res.push(nums[i]);\n        }\n    }\n    \n    // Fill remaining zeros\n    while (res.length &lt; n) {\n        res.push(0);\n    }\n    \n    return res;\n}" }]} />

**Complexity**
- Time: O(n) — We iterate through the array a constant number of times.
- Space: O(n) — We allocate a new array to store the result.
- Notes: This approach is often easier to implement and reason about but uses more memory.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Apply Operations to an Array Solutions","datePublished":"2024-03-18","description":"Apply operations to adjacent equal elements, then shift all zeros to the end of the array.","keywords":"LeetCode, Apply Operations to an Array, Easy, Array, Two Pointers, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the In-place Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the array. We iterate through the array once to apply operations and once more to shift zeros."}},{"@type":"Question","name":"What is the space complexity of the Auxiliary Array approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) because we create a new array to store the result."}}]}).replace(/</g,'\u003c')}
</script>