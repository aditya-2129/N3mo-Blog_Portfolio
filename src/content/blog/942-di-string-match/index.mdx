---
title: "DI String Match"
summary: "Given a string of I and D characters, reconstruct a permutation where I means increase and D means decrease."
date: "Feb 08, 2025"
tags:
  - Array
  - Two Pointers
  - String
  - Greedy
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 81.00% | **Paid:** No
> **Topics:** Array, Two Pointers, String, Greedy

A permutation perm of n + 1 integers of all the integers in the range [0, n] can be represented as a string s of length n where:

- s[i] == 'I' if perm[i] &lt; perm[i + 1], and
- s[i] == 'D' if perm[i] &gt; perm[i + 1].

Given a string s, reconstruct the permutation perm and return it. If there are multiple valid permutations perm, return any of them.

- Examples
- Constraints
- [Two Pointers (Greedy)](#two-pointers-greedy)
- [Stack-based Approach](#stack-based-approach)
- [Reverse Segments](#reverse-segments)

## Examples

### Example 1
```text
Input: s = "IDID"
Output: [0,4,1,3,2]
Explanation: 
- perm[0] < perm[1] (0 < 4) matches 'I'
- perm[1] > perm[2] (4 > 1) matches 'D'
- perm[2] < perm[3] (1 < 3) matches 'I'
- perm[3] > perm[4] (3 > 2) matches 'D'
```

### Example 2
```text
Input: s = "III"
Output: [0,1,2,3]
Explanation: All positions require increase, so we use the smallest available numbers in order.
```

### Example 3
```text
Input: s = "DDDD"
Output: [4,3,2,1,0]
Explanation: All positions require decrease, so we use the largest available numbers in order.
```

## Constraints
```text
1 <= s.length <= 10⁵
s[i] is either 'I' or 'D'.
```

## Two Pointers (Greedy)

**Intuition**
Use two pointers to track the smallest and largest available numbers. For 'I', assign the smallest remaining number to ensure we can always find a larger number later. For 'D', assign the largest remaining number to ensure we can always find a smaller number later.

**Steps**
- Initialize low = 0 and high = n (where n = s.length())
- Iterate through each character in s:
  - If s[i] == 'I', assign low to result[i] and increment low
  - If s[i] == 'D', assign high to result[i] and decrement high
- Assign the remaining value (low == high) to the last position

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def diStringMatch(self, s: str) -> list[int]:
        n = len(s)
        result = [0] * (n + 1)
        low, high = 0, n
        
        for i, char in enumerate(s):
            if char == 'I':
                result[i] = low
                low += 1
            else:
                result[i] = high
                high -= 1
        
        result[n] = low
        return result` },
{ name: "Java", lang: "java", code: `class Solution {
    public int[] diStringMatch(String s) {
        int n = s.length();
        int[] result = new int[n + 1];
        int low = 0, high = n;
        
        for (int i = 0; i &lt; n; i++) {
            if (s.charAt(i) == 'I') {
                result[i] = low++;
            } else {
                result[i] = high--;
            }
        }
        
        result[n] = low;
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    vector<int> diStringMatch(string s) {
        int n = s.length();
        vector<int> result(n + 1);
        int low = 0, high = n;
        
        for (int i = 0; i &lt; n; i++) {
            if (s[i] == 'I') {
                result[i] = low++;
            } else {
                result[i] = high--;
            }
        }
        
        result[n] = low;
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number[]}
 */
var diStringMatch = function(s) {
    const n = s.length;
    const result = new Array(n + 1);
    let low = 0, high = n;
    
    for (let i = 0; i &lt; n; i++) {
        if (s[i] === 'I') {
            result[i] = low++;
        } else {
            result[i] = high--;
        }
    }
    
    result[n] = low;
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function diStringMatch(s: string): number[] {
    const n = s.length;
    const result: number[] = new Array(n + 1);
    let low = 0, high = n;
    
    for (let i = 0; i &lt; n; i++) {
        if (s[i] === 'I') {
            result[i] = low++;
        } else {
            result[i] = high--;
        }
    }
    
    result[n] = low;
    return result;
}` },]} />

**Complexity**
- Time: O(n) - Single pass through the string
- Space: O(n) - For the result array (O(1) extra space)
- Notes: Optimal solution with minimal space usage

## Stack-based Approach

**Intuition**
Initialize the result with [0, 1, 2, ..., n]. Use a stack to track indices where 'D' starts. When we encounter 'I' or reach the end, reverse all segments marked by 'D' to satisfy the decreasing requirement.

**Steps**
- Initialize result with [0, 1, 2, ..., n]
- Use a stack to track start indices of 'D' segments
- Iterate through the string:
  - If s[i] == 'D', push i onto the stack
  - If s[i] == 'I' or i == n-1, pop all indices from stack and reverse the segment from that index to i+1
- Return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def diStringMatch(self, s: str) -> list[int]:
        n = len(s)
        result = list(range(n + 1))
        stack = []
        
        for i, char in enumerate(s):
            if char == 'D':
                stack.append(i)
            else:
                while stack:
                    start = stack.pop()
                    result[start], result[i + 1] = result[i + 1], result[start]
        
        while stack:
            start = stack.pop()
            result[start], result[n] = result[n], result[start]
        
        return result` },
{ name: "Java", lang: "java", code: `class Solution {
    public int[] diStringMatch(String s) {
        int n = s.length();
        int[] result = new int[n + 1];
        for (int i = 0; i &lt;= n; i++) {
            result[i] = i;
        }
        
        java.util.Stack<Integer> stack = new java.util.Stack&lt;&gt;();
        
        for (int i = 0; i &lt; n; i++) {
            if (s.charAt(i) == 'D') {
                stack.push(i);
            } else {
                while (!stack.isEmpty()) {
                    int start = stack.pop();
                    int temp = result[start];
                    result[start] = result[i + 1];
                    result[i + 1] = temp;
                }
            }
        }
        
        while (!stack.isEmpty()) {
            int start = stack.pop();
            int temp = result[start];
            result[start] = result[n];
            result[n] = temp;
        }
        
        return result;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    vector<int> diStringMatch(string s) {
        int n = s.length();
        vector<int> result(n + 1);
        for (int i = 0; i &lt;= n; i++) {
            result[i] = i;
        }
        
        stack<int> st;
        
        for (int i = 0; i &lt; n; i++) {
            if (s[i] == 'D') {
                st.push(i);
            } else {
                while (!st.empty()) {
                    int start = st.top();
                    st.pop();
                    swap(result[start], result[i + 1]);
                }
            }
        }
        
        while (!st.empty()) {
            int start = st.top();
            st.pop();
            swap(result[start], result[n]);
        }
        
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number[]}
 */
var diStringMatch = function(s) {
    const n = s.length;
    const result = Array.from({length: n + 1}, (_, i) =&gt; i);
    const stack = [];
    
    for (let i = 0; i &lt; n; i++) {
        if (s[i] === 'D') {
            stack.push(i);
        } else {
            while (stack.length &gt; 0) {
                const start = stack.pop();
                [result[start], result[i + 1]] = [result[i + 1], result[start]];
            }
        }
    }
    
    while (stack.length &gt; 0) {
        const start = stack.pop();
        [result[start], result[n]] = [result[n], result[start]];
    }
    
    return result;
};` },
{ name: "TypeScript", lang: "typescript", code: `function diStringMatch(s: string): number[] {
    const n = s.length;
    const result: number[] = Array.from({length: n + 1}, (_, i) =&gt; i);
    const stack: number[] = [];
    
    for (let i = 0; i &lt; n; i++) {
        if (s[i] === 'D') {
            stack.push(i);
        } else {
            while (stack.length &gt; 0) {
                const start = stack.pop()!;
                [result[start], result[i + 1]] = [result[i + 1], result[start]];
            }
        }
    }
    
    while (stack.length &gt; 0) {
        const start = stack.pop()!;
        [result[start], result[n]] = [result[n], result[start]];
    }
    
    return result;
}` },]} />

**Complexity**
- Time: O(n) - Each element is swapped at most once
- Space: O(n) - For the stack in worst case (all 'D's)
- Notes: More complex than two pointers but demonstrates a different pattern

## Reverse Segments

**Intuition**
Initialize result with [0, 1, 2, ..., n]. Identify consecutive 'D' segments and reverse each segment to satisfy the decreasing requirement.

**Steps**
- Initialize result with [0, 1, 2, ..., n]
- Find all consecutive segments of 'D' in the string
- For each segment from index i to j (inclusive), reverse result[i:j+2]
- Return the result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def diStringMatch(self, s: str) -> list[int]:
        n = len(s)
        result = list(range(n + 1))
        i = 0
        
        while i &lt; n:
            if s[i] == 'D':
                start = i
                while i &lt; n and s[i] == 'D':
                    i += 1
                result[start:i + 1] = result[start:i + 1][::-1]
            else:
                i += 1
        
        return result` },
{ name: "Java", lang: "java", code: `class Solution {
    public int[] diStringMatch(String s) {
        int n = s.length();
        int[] result = new int[n + 1];
        for (int i = 0; i &lt;= n; i++) {
            result[i] = i;
        }
        
        int i = 0;
        while (i &lt; n) {
            if (s.charAt(i) == 'D') {
                int start = i;
                while (i &lt; n && s.charAt(i) == 'D') {
                    i++;
                }
                reverse(result, start, i);
            } else {
                i++;
            }
        }
        
        return result;
    }
    
    private void reverse(int[] arr, int start, int end) {
        while (start &lt; end) {
            int temp = arr[start];
            arr[start] = arr[end];
            arr[end] = temp;
            start++;
            end--;
        }
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    vector<int> diStringMatch(string s) {
        int n = s.length();
        vector<int> result(n + 1);
        for (int i = 0; i &lt;= n; i++) {
            result[i] = i;
        }
        
        int i = 0;
        while (i &lt; n) {
            if (s[i] == 'D') {
                int start = i;
                while (i &lt; n && s[i] == 'D') {
                    i++;
                }
                reverse(result.begin() + start, result.begin() + i + 1);
            } else {
                i++;
            }
        }
        
        return result;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {string} s
 * @return {number[]}
 */
var diStringMatch = function(s) {
    const n = s.length;
    const result = Array.from({length: n + 1}, (_, i) =&gt; i);
    let i = 0;
    
    while (i &lt; n) {
        if (s[i] === 'D') {
            const start = i;
            while (i &lt; n && s[i] === 'D') {
                i++;
            }
            reverse(result, start, i);
        } else {
            i++;
        }
    }
    
    return result;
};

function reverse(arr, start, end) {
    while (start &lt; end) {
        [arr[start], arr[end]] = [arr[end], arr[start]];
        start++;
        end--;
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `function diStringMatch(s: string): number[] {
    const n = s.length;
    const result: number[] = Array.from({length: n + 1}, (_, i) =&gt; i);
    let i = 0;
    
    while (i &lt; n) {
        if (s[i] === 'D') {
            const start = i;
            while (i &lt; n && s[i] === 'D') {
                i++;
            }
            reverse(result, start, i);
        } else {
            i++;
        }
    }
    
    return result;
}

function reverse(arr: number[], start: number, end: number): void {
    while (start &lt; end) {
        [arr[start], arr[end]] = [arr[end], arr[start]];
        start++;
        end--;
    }
}` },]} />

**Complexity**
- Time: O(n) - Each element is visited at most twice
- Space: O(1) - In-place reversal
- Notes: Intuitive approach that directly addresses the problem requirements

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode DI String Match Solutions","datePublished":"2025-02-08","description":"Given a string of I and D characters, reconstruct a permutation where I means increase and D means decrease.","keywords":"LeetCode, DI String Match, Easy, Array, Two Pointers, String, Greedy"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The Two Pointers approach has O(n) time complexity as we make a single pass through the string."}},{"@type":"Question","name":"What is the space complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The Two Pointers approach uses O(n) space for the result array, with only O(1) extra space for the two pointers."}},{"@type":"Question","name":"What is the time complexity of the Stack-based approach?","acceptedAnswer":{"@type":"Answer","text":"The Stack-based approach has O(n) time complexity as each element is swapped at most once."}},{"@type":"Question","name":"What is the space complexity of the Stack-based approach?","acceptedAnswer":{"@type":"Answer","text":"The Stack-based approach uses O(n) space in the worst case for the stack when all characters are 'D'."}},{"@type":"Question","name":"What is the time complexity of the Reverse Segments approach?","acceptedAnswer":{"@type":"Answer","text":"The Reverse Segments approach has O(n) time complexity as each element is visited at most twice."}},{"@type":"Question","name":"What is the space complexity of the Reverse Segments approach?","acceptedAnswer":{"@type":"Answer","text":"The Reverse Segments approach uses O(1) extra space as reversals are done in-place."}}]}).replace(/</g,'\u003c')}
</script>