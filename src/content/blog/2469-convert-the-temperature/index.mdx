---
title: "Convert the Temperature"
summary: "Convert Celsius to Kelvin and Fahrenheit using simple mathematical formulas."
date: "Sep 18, 2024"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 90.30% | **Paid:** No
> **Topics:** Math

You are given a non-negative floating point number rounded to two decimal places celsius, which denotes the temperature in Celsius.

You should convert Celsius into Kelvin and Fahrenheit and return it as an array ans = [kelvin, fahrenheit].

Return the array ans.

Note that:
- Kelvin = Celsius + 273.15
- Fahrenheit = Celsius * 1.80 + 32.00

- Examples
- Constraints
- [Direct Formula Approach](#direct-formula-approach)

## Examples

Example 1:
```text
Input: celsius = 36.50
Output: [309.65000,97.70000]
Explanation: Temperature at 36.50 Celsius converted in Kelvin is 309.65 and converted in Fahrenheit is 97.70.
```

Example 2:
```text
Input: celsius = 122.11
Output: [395.26000,251.79800]
Explanation: Temperature at 122.11 Celsius converted in Kelvin is 395.26 and converted in Fahrenheit is 251.798.
```

## Constraints
```text
0 <= celsius <= 1000
```

---
## Direct Formula Approach

**Intuition**
Apply the given conversion formulas directly to compute Kelvin and Fahrenheit from Celsius.

**Steps**
- Calculate Kelvin by adding 273.15 to Celsius
- Calculate Fahrenheit by multiplying Celsius by 1.80 and adding 32.00
- Return both values as an array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def convertTemperature(self, celsius: float) -> list[float]:
        kelvin = celsius + 273.15
        fahrenheit = celsius * 1.80 + 32.00
        return [kelvin, fahrenheit]
` },
{ name: "Java", lang: "java", code: `class Solution {
    public double[] convertTemperature(double celsius) {
        double kelvin = celsius + 273.15;
        double fahrenheit = celsius * 1.80 + 32.00;
        return new double[]{kelvin, fahrenheit};
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    vector<double> convertTemperature(double celsius) {
        double kelvin = celsius + 273.15;
        double fahrenheit = celsius * 1.80 + 32.00;
        return {kelvin, fahrenheit};
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} celsius
 * @return {number[]}
 */
var convertTemperature = function(celsius) {
    const kelvin = celsius + 273.15;
    const fahrenheit = celsius * 1.80 + 32.00;
    return [kelvin, fahrenheit];
};
` },
{ name: "TypeScript", lang: "typescript", code: `function convertTemperature(celsius: number): number[] {
    const kelvin = celsius + 273.15;
    const fahrenheit = celsius * 1.80 + 32.00;
    return [kelvin, fahrenheit];
}
` },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Constant time and space as we only perform two arithmetic operations.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Convert the Temperature Solutions","datePublished":"2024-09-18","description":"Convert Celsius to Kelvin and Fahrenheit using simple mathematical formulas.","keywords":"LeetCode, Convert the Temperature, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Direct Formula Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) as we only perform two arithmetic operations regardless of input size."}},{"@type":"Question","name":"What is the space complexity of the Direct Formula Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only store two result values."}}]}).replace(/</g,'\u003c')}
</script>