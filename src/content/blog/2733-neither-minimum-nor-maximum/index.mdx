---
title: "Neither Minimum nor Maximum"
summary: "Find an integer in the array that is neither the minimum nor the maximum value."
date: "Jan 06, 2026"
tags:
  - Array
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 76.40% | **Paid:** No
> **Topics:** Array, Sorting

Given an integer array nums containing distinct positive integers, find a number that is neither the minimum nor the maximum value in the array.

Return the number if it exists, otherwise return -1.

- Examples
- Constraints
- [Approach 1: Sorting](#sorting)
- [Approach 2: Linear Scan](#linear-scan)

## Examples

### Example 1
```text
Input: nums = [3,2,1,4]
Output: 2
Explanation: 2 is neither the minimum (1) nor the maximum (4) in the array.
```

### Example 2
```text
Input: nums = [1,2]
Output: -1
Explanation: Since the array length is less than 3, there is no number that is neither minimum nor maximum.
```

### Example 3
```text
Input: nums = [2,1,3]
Output: 2
Explanation: 2 is neither the minimum (1) nor the maximum (3) in the array.
```

## Constraints

```text
3 <= nums.length <= 100
1 <= nums[i] <= 100
All the integers of nums are distinct.
```

---
## Sorting

**Intuition**
Since the array contains distinct integers, sorting the array places the minimum at the beginning and the maximum at the end. Any element in between (specifically the second element) is guaranteed to be neither the minimum nor the maximum.

**Steps**
- Sort the array in non-decreasing order.
- If the length of the array is less than 3, return -1.
- Return the element at index 1 (the second element).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findNonMinOrMax(self, nums: List[int]) -&gt; int:\n        if len(nums) &lt; 3:\n            return -1\n        nums.sort()\n        return nums[1]" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int findNonMinOrMax(int[] nums) {\n        if (nums.length &lt; 3) {\n            return -1;\n        }\n        Arrays.sort(nums);\n        return nums[1];\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int findNonMinOrMax(vector&lt;int&gt;& nums) {\n        if (nums.size() &lt; 3) {\n            return -1;\n        }\n        sort(nums.begin(), nums.end());\n        return nums[1];\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar findNonMinOrMax = function(nums) {\n    if (nums.length &lt; 3) {\n        return -1;\n    }\n    nums.sort((a, b) =&gt; a - b);\n    return nums[1];\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findNonMinOrMax(nums: number[]): number {\n    if (nums.length &lt; 3) {\n        return -1;\n    }\n    nums.sort((a, b) =&gt; a - b);\n    return nums[1];\n}" },]} />

**Complexity**
- Time: O(n log n)
- Space: O(1) or O(n) depending on sorting implementation.
- Notes: Sorting is simple but not the most time-efficient.

---
## Linear Scan

**Intuition**
We can find the minimum and maximum values in a single pass. Then, we iterate through the array again to find the first element that is not equal to the minimum or maximum. Since the array has distinct elements and length &gt;= 3, such an element must exist.

**Steps**
- Initialize min and max variables.
- Iterate through the array to find the global minimum and maximum.
- Iterate through the array again. If an element is not equal to min and not equal to max, return it.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findNonMinOrMax(self, nums: List[int]) -&gt; int:\n        if len(nums) &lt; 3:\n            return -1\n        min_val = min(nums)\n        max_val = max(nums)\n        for num in nums:\n            if num != min_val and num != max_val:\n                return num\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int findNonMinOrMax(int[] nums) {\n        if (nums.length &lt; 3) {\n            return -1;\n        }\n        int min = Integer.MAX_VALUE;\n        int max = Integer.MIN_VALUE;\n        for (int num : nums) {\n            if (num &lt; min) min = num;\n            if (num &gt; max) max = num;\n        }\n        for (int num : nums) {\n            if (num != min && num != max) {\n                return num;\n            }\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;climits&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int findNonMinOrMax(vector&lt;int&gt;& nums) {\n        if (nums.size() &lt; 3) {\n            return -1;\n        }\n        int min_val = INT_MAX;\n        int max_val = INT_MIN;\n        for (int num : nums) {\n            if (num &lt; min_val) min_val = num;\n            if (num &gt; max_val) max_val = num;\n        }\n        for (int num : nums) {\n            if (num != min_val && num != max_val) {\n                return num;\n            }\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar findNonMinOrMax = function(nums) {\n    if (nums.length &lt; 3) {\n        return -1;\n    }\n    let min = Infinity;\n    let max = -Infinity;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (nums[i] &lt; min) min = nums[i];\n        if (nums[i] &gt; max) max = nums[i];\n    }\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (nums[i] !== min && nums[i] !== max) {\n            return nums[i];\n        }\n    }\n    return -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findNonMinOrMax(nums: number[]): number {\n    if (nums.length &lt; 3) {\n        return -1;\n    }\n    let min = Infinity;\n    let max = -Infinity;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (nums[i] &lt; min) min = nums[i];\n        if (nums[i] &gt; max) max = nums[i];\n    }\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (nums[i] !== min && nums[i] !== max) {\n            return nums[i];\n        }\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: This is the optimal time complexity solution.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Neither Minimum nor Maximum Solutions","datePublished":"2026-01-06","description":"Find an integer in the array that is neither the minimum nor the maximum value.","keywords":"LeetCode, Neither Minimum nor Maximum, Easy, Array, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n log n) due to the sorting step."}},{"@type":"Question","name":"What is the time complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) as we iterate through the array a constant number of times."}}]}).replace(/</g,'\u003c')}
</script>