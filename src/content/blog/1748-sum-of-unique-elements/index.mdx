---
title: "Sum of Unique Elements"
summary: "Return the sum of all unique elements in an array where unique elements appear exactly once."
date: "Aug 15, 2025"
tags:
  - Array
  - Hash Table
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 79.90% | **Paid:** No
> **Topics:** Array, Hash Table, Counting

You are given an integer array nums. The unique elements of an array are the elements that appear exactly once in the array.

Return the sum of all the unique elements of nums.

- Examples
- Constraints
- [Hash Map Counting](#hash-map-counting)
- [Array Counting](#array-counting)
- [Sorting Approach](#sorting-approach)

## Examples

### Example 1
```text
Input: nums = [1,2,3,2]
Output: 4
Explanation:
The unique elements are [1,3], and the sum is 4.
```

### Example 2
```text
Input: nums = [1,1,1,1,1]
Output: 0
Explanation:
There are no unique elements, and the sum is 0.
```

### Example 3
```text
Input: nums = [1,2,3,4,5]
Output: 15
Explanation:
The unique elements are [1,2,3,4,5], and the sum is 15.
```

## Constraints

```text
1 <= nums.length <= 100
1 <= nums[i] <= 100
```

---
## Hash Map Counting

**Intuition**
Use a hash map to count the frequency of each element, then sum elements that appear exactly once.

**Steps**
- Create a hash map to store the count of each element
- Iterate through the array and increment the count for each element
- Iterate through the hash map and sum up elements with count equal to 1

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def sumOfUnique(self, nums: List[int]) -> int:
        count = {}
        for num in nums:
            count[num] = count.get(num, 0) + 1
        return sum(num for num, freq in count.items() if freq == 1)` },
{ name: "Java", lang: "java", code: `import java.util.*;

class Solution {
    public int sumOfUnique(int[] nums) {
        Map<Integer, Integer> count = new HashMap&lt;&gt;();
        for (int num : nums) {
            count.put(num, count.getOrDefault(num, 0) + 1);
        }
        int sum = 0;
        for (Map.Entry<Integer, Integer> entry : count.entrySet()) {
            if (entry.getValue() == 1) {
                sum += entry.getKey();
            }
        }
        return sum;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <unordered_map>
using namespace std;

class Solution {
public:
    int sumOfUnique(vector<int>& nums) {
        unordered_map<int, int> count;
        for (int num : nums) {
            count[num]++;
        }
        int sum = 0;
        for (auto& [num, freq] : count) {
            if (freq == 1) {
                sum += num;
            }
        }
        return sum;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @return {number}
 */
var sumOfUnique = function(nums) {
    const count = new Map();
    for (const num of nums) {
        count.set(num, (count.get(num) || 0) + 1);
    }
    let sum = 0;
    for (const [num, freq] of count) {
        if (freq === 1) {
            sum += num;
        }
    }
    return sum;
};` },
{ name: "TypeScript", lang: "typescript", code: `function sumOfUnique(nums: number[]): number {
    const count = new Map<number, number>();
    for (const num of nums) {
        count.set(num, (count.get(num) || 0) + 1);
    }
    let sum = 0;
    for (const [num, freq] of count) {
        if (freq === 1) {
            sum += num;
        }
    }
    return sum;
}` },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(n) for the hash map in the worst case
- Notes: Simple and intuitive approach, works for any range of values

---
## Array Counting

**Intuition**
Since all elements are between 1 and 100, use a fixed-size array of 101 elements to count frequencies efficiently.

**Steps**
- Create an array of size 101 initialized to 0
- Iterate through nums and increment the count at index equal to the element value
- Sum up all indices where the count is exactly 1

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def sumOfUnique(self, nums: List[int]) -> int:
        count = [0] * 101
        for num in nums:
            count[num] += 1
        return sum(i for i in range(101) if count[i] == 1)` },
{ name: "Java", lang: "java", code: `class Solution {
    public int sumOfUnique(int[] nums) {
        int[] count = new int[101];
        for (int num : nums) {
            count[num]++;
        }
        int sum = 0;
        for (int i = 1; i &lt;= 100; i++) {
            if (count[i] == 1) {
                sum += i;
            }
        }
        return sum;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
using namespace std;

class Solution {
public:
    int sumOfUnique(vector<int>& nums) {
        int count[101] = {0};
        for (int num : nums) {
            count[num]++;
        }
        int sum = 0;
        for (int i = 1; i &lt;= 100; i++) {
            if (count[i] == 1) {
                sum += i;
            }
        }
        return sum;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @return {number}
 */
var sumOfUnique = function(nums) {
    const count = new Array(101).fill(0);
    for (const num of nums) {
        count[num]++;
    }
    let sum = 0;
    for (let i = 1; i &lt;= 100; i++) {
        if (count[i] === 1) {
            sum += i;
        }
    }
    return sum;
};` },
{ name: "TypeScript", lang: "typescript", code: `function sumOfUnique(nums: number[]): number {
    const count = new Array(101).fill(0);
    for (const num of nums) {
        count[num]++;
    }
    let sum = 0;
    for (let i = 1; i &lt;= 100; i++) {
        if (count[i] === 1) {
            sum += i;
        }
    }
    return sum;
}` },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(1) since the counting array has fixed size 101
- Notes: Most space-efficient approach due to the constraint on element values

---
## Sorting Approach

**Intuition**
Sort the array and check each element against its neighbors to determine if it appears exactly once.

**Steps**
- Sort the array in non-decreasing order
- Iterate through the array and check if each element is different from both its neighbors
- Sum up elements that are unique (different from both neighbors)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def sumOfUnique(self, nums: List[int]) -> int:
        nums.sort()
        n = len(nums)
        total = 0
        for i in range(n):
            if (i == 0 or nums[i] != nums[i - 1]) and (i == n - 1 or nums[i] != nums[i + 1]):
                total += nums[i]
        return total` },
{ name: "Java", lang: "java", code: `import java.util.*;

class Solution {
    public int sumOfUnique(int[] nums) {
        Arrays.sort(nums);
        int n = nums.length;
        int sum = 0;
        for (int i = 0; i &lt; n; i++) {
            boolean leftUnique = (i == 0) || (nums[i] != nums[i - 1]);
            boolean rightUnique = (i == n - 1) || (nums[i] != nums[i + 1]);
            if (leftUnique && rightUnique) {
                sum += nums[i];
            }
        }
        return sum;
    }
}` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <algorithm>
using namespace std;

class Solution {
public:
    int sumOfUnique(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        int n = nums.size();
        int sum = 0;
        for (int i = 0; i &lt; n; i++) {
            bool leftUnique = (i == 0) || (nums[i] != nums[i - 1]);
            bool rightUnique = (i == n - 1) || (nums[i] != nums[i + 1]);
            if (leftUnique && rightUnique) {
                sum += nums[i];
            }
        }
        return sum;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @return {number}
 */
var sumOfUnique = function(nums) {
    nums.sort((a, b) =&gt; a - b);
    const n = nums.length;
    let sum = 0;
    for (let i = 0; i &lt; n; i++) {
        const leftUnique = (i === 0) || (nums[i] !== nums[i - 1]);
        const rightUnique = (i === n - 1) || (nums[i] !== nums[i + 1]);
        if (leftUnique && rightUnique) {
            sum += nums[i];
        }
    }
    return sum;
};` },
{ name: "TypeScript", lang: "typescript", code: `function sumOfUnique(nums: number[]): number {
    nums.sort((a, b) =&gt; a - b);
    const n = nums.length;
    let sum = 0;
    for (let i = 0; i &lt; n; i++) {
        const leftUnique = (i === 0) || (nums[i] !== nums[i - 1]);
        const rightUnique = (i === n - 1) || (nums[i] !== nums[i + 1]);
        if (leftUnique && rightUnique) {
            sum += nums[i];
        }
    }
    return sum;
}` },]} />

**Complexity**
- Time: O(n log n) due to sorting
- Space: O(1) or O(n) depending on the sorting algorithm
- Notes: Less efficient than counting approaches but demonstrates an alternative method

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Sum of Unique Elements Solutions","datePublished":"2025-08-15","description":"Return the sum of all unique elements in an array where unique elements appear exactly once.","keywords":"LeetCode, Sum of Unique Elements, Easy, Array, Hash Table, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Map Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the array, as we iterate through the array twice - once to count frequencies and once to sum unique elements."}},{"@type":"Question","name":"What is the space complexity of the Array Counting approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) since we use a fixed-size array of 101 elements regardless of the input size, thanks to the constraint that all elements are between 1 and 100."}},{"@type":"Question","name":"Why is the Sorting approach less efficient?","acceptedAnswer":{"@type":"Answer","text":"The Sorting approach has O(n log n) time complexity due to the sorting step, which is slower than the O(n) time complexity of the counting approaches."}},{"@type":"Question","name":"Which approach is recommended for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Array Counting approach is recommended as it has O(n) time complexity and O(1) space complexity, making it the most efficient solution given the problem constraints."}}]}).replace(/</g,'\u003c')}
</script>