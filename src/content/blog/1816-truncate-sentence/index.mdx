---
title: "Truncate Sentence"
summary: "Return the first k words of a sentence separated by spaces."
date: "Oct 26, 2024"
tags:
  - Array
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 86.80% | **Paid:** No
> **Topics:** Array, String

A sentence is a list of words that are separated by a single space with no leading or trailing spaces. Each of the words consists of only uppercase and lowercase English letters.

Given a string s representing a sentence and an integer k, return the first k words of the sentence.

- Examples
- Constraints
- [Split and Join](#split-and-join)
- [Iterative Character Processing](#iterative-character-processing)
- [Two Pointers](#two-pointers)

## Examples

### Example 1

**Input:**
```text
s = "Hello how are you Contestant", k = 4
```

**Output:**
```text
"Hello how are you"
```

**Explanation:**
The words in s are ["Hello", "how" "are", "you", "Contestant"].
The first 4 words are ["Hello", "how", "are", "you"].
Hence, you should return "Hello how are you".

### Example 2

**Input:**
```text
s = "What is the solution to this problem", k = 4
```

**Output:**
```text
"What is the solution"
```

**Explanation:**
The words in s are ["What", "is" "the", "solution", "to", "this", "problem"].
The first 4 words are ["What", "is", "the", "solution"].
Hence, you should return "What is the solution".

### Example 3

**Input:**
```text
s = "chopper is not a tanuki", k = 5
```

**Output:**
```text
"chopper is not a tanuki"
```

## Constraints
```text
1 <= s.length <= 500
s consists of only English letters and spaces ' '.
s contains no leading or trailing spaces.
All the words in s are separated by a single space.
1 <= k <= s.length
```

---
## Split and Join

**Intuition**
Split the sentence into words by spaces, take the first k words, and join them back with spaces.

**Steps**
- Split the string by space character
- Slice the array to get first k elements
- Join the sliced array with spaces

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def truncateSentence(self, s: str, k: int) -&gt; str:\n        words = s.split(' ')\n        return ' '.join(words[:k])" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String truncateSentence(String s, int k) {\n        String[] words = s.split(\" \");\n        StringBuilder result = new StringBuilder();\n        for (int i = 0; i &lt; k; i++) {\n            if (i &gt; 0) {\n                result.append(\" \");\n            }\n            result.append(words[i]);\n        }\n        return result.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string truncateSentence(string s, int k) {\n        vector&lt;string&gt; words;\n        string word;\n        stringstream ss(s);\n        while (ss &gt;&gt; word) {\n            words.push_back(word);\n        }\n        string result;\n        for (int i = 0; i &lt; k; i++) {\n            if (i &gt; 0) {\n                result += \" \";\n            }\n            result += words[i];\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    truncateSentence(s, k) {\n        const words = s.split(' ');\n        return words.slice(0, k).join(' ');\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    truncateSentence(s: string, k: number): string {\n        const words = s.split(' ');\n        return words.slice(0, k).join(' ');\n    }\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for storing the split words array
- Notes: Clean and readable, uses built-in string methods

---
## Iterative Character Processing

**Intuition**
Iterate through each character, build words one by one, and stop after collecting k words.

**Steps**
- Track word count and current word being built
- When a space is encountered, increment word count and add word to result
- Stop when k words are collected
- Handle the last word if it's the k-th word

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def truncateSentence(self, s: str, k: int) -&gt; str:\n        word_count = 0\n        result = []\n        current_word = []\n        \n        for char in s:\n            if char == ' ':\n                word_count += 1\n                result.append(''.join(current_word))\n                current_word = []\n                if word_count == k:\n                    break\n            else:\n                current_word.append(char)\n        \n        if word_count &lt; k and current_word:\n            result.append(''.join(current_word))\n        \n        return ' '.join(result)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String truncateSentence(String s, int k) {\n        int wordCount = 0;\n        StringBuilder result = new StringBuilder();\n        StringBuilder currentWord = new StringBuilder();\n        \n        for (int i = 0; i &lt; s.length(); i++) {\n            char c = s.charAt(i);\n            if (c == ' ') {\n                wordCount++;\n                if (wordCount &gt; 1) {\n                    result.append(\" \");\n                }\n                result.append(currentWord);\n                currentWord = new StringBuilder();\n                if (wordCount == k) {\n                    break;\n                }\n            } else {\n                currentWord.append(c);\n            }\n        }\n        \n        if (wordCount &lt; k && currentWord.length() &gt; 0) {\n            if (wordCount &gt; 0) {\n                result.append(\" \");\n            }\n            result.append(currentWord);\n        }\n        \n        return result.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string truncateSentence(string s, int k) {\n        int wordCount = 0;\n        string result;\n        string currentWord;\n        \n        for (char c : s) {\n            if (c == ' ') {\n                wordCount++;\n                if (!result.empty()) {\n                    result += \" \";\n                }\n                result += currentWord;\n                currentWord.clear();\n                if (wordCount == k) {\n                    break;\n                }\n            } else {\n                currentWord += c;\n            }\n        }\n        \n        if (wordCount &lt; k && !currentWord.empty()) {\n            if (wordCount &gt; 0) {\n                result += \" \";\n            }\n            result += currentWord;\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    truncateSentence(s, k) {\n        let wordCount = 0;\n        let result = '';\n        let currentWord = '';\n        \n        for (let i = 0; i &lt; s.length; i++) {\n            const c = s[i];\n            if (c === ' ') {\n                wordCount++;\n                if (result.length &gt; 0) {\n                    result += ' ';\n                }\n                result += currentWord;\n                currentWord = '';\n                if (wordCount === k) {\n                    break;\n                }\n            } else {\n                currentWord += c;\n            }\n        }\n        \n        if (wordCount &lt; k && currentWord.length &gt; 0) {\n            if (wordCount &gt; 0) {\n                result += ' ';\n            }\n            result += currentWord;\n        }\n        \n        return result;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    truncateSentence(s: string, k: number): string {\n        let wordCount = 0;\n        let result = '';\n        let currentWord = '';\n        \n        for (let i = 0; i &lt; s.length; i++) {\n            const c = s[i];\n            if (c === ' ') {\n                wordCount++;\n                if (result.length &gt; 0) {\n                    result += ' ';\n                }\n                result += currentWord;\n                currentWord = '';\n                if (wordCount === k) {\n                    break;\n                }\n            } else {\n                currentWord += c;\n            }\n        }\n        \n        if (wordCount &lt; k && currentWord.length &gt; 0) {\n            if (wordCount &gt; 0) {\n                result += ' ';\n            }\n            result += currentWord;\n        }\n        \n        return result;\n    }\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for storing the result string
- Notes: More manual control, avoids creating intermediate arrays

---
## Two Pointers

**Intuition**
Find the position of the k-th space and return the substring up to that position.

**Steps**
- Iterate through the string counting spaces
- When the k-th space is found, return substring from start to that position
- If k spaces are never found, return the entire string

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def truncateSentence(self, s: str, k: int) -&gt; str:\n        word_count = 0\n        \n        for i in range(len(s)):\n            if s[i] == ' ':\n                word_count += 1\n                if word_count == k:\n                    return s[:i]\n        \n        return s" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String truncateSentence(String s, int k) {\n        int wordCount = 0;\n        \n        for (int i = 0; i &lt; s.length(); i++) {\n            if (s.charAt(i) == ' ') {\n                wordCount++;\n                if (wordCount == k) {\n                    return s.substring(0, i);\n                }\n            }\n        }\n        \n        return s;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string truncateSentence(string s, int k) {\n        int wordCount = 0;\n        \n        for (int i = 0; i &lt; s.length(); i++) {\n            if (s[i] == ' ') {\n                wordCount++;\n                if (wordCount == k) {\n                    return s.substr(0, i);\n                }\n            }\n        }\n        \n        return s;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    truncateSentence(s, k) {\n        let wordCount = 0;\n        \n        for (let i = 0; i &lt; s.length; i++) {\n            if (s[i] === ' ') {\n                wordCount++;\n                if (wordCount === k) {\n                    return s.substring(0, i);\n                }\n            }\n        }\n        \n        return s;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    truncateSentence(s: string, k: number): string {\n        let wordCount = 0;\n        \n        for (let i = 0; i &lt; s.length; i++) {\n            if (s[i] === ' ') {\n                wordCount++;\n                if (wordCount === k) {\n                    return s.substring(0, i);\n                }\n            }\n        }\n        \n        return s;\n    }\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for the result substring
- Notes: Most space-efficient as it doesn't store intermediate arrays

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Truncate Sentence Solutions","datePublished":"2024-10-26","description":"Return the first k words of a sentence separated by spaces.","keywords":"LeetCode, Truncate Sentence, Easy, Array, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Split and Join approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of the string"}},{"@type":"Question","name":"What is the space complexity of Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for the result substring"}},{"@type":"Question","name":"Which approach is most space-efficient?","acceptedAnswer":{"@type":"Answer","text":"Two Pointers approach as it doesn't store intermediate arrays"}}]}).replace(/</g,'\u003c')}
</script>