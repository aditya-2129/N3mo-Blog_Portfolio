---
title: "Smallest Divisible Digit Product I"
summary: "Find the smallest positive integer with n digits whose digit product is divisible by k."
date: "Jul 21, 2025"
tags:
  - Math
  - Enumeration
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 64.40% | **Paid:** No
> **Topics:** Math, Enumeration

You are given two integers n and k.

Return the smallest positive integer x such that:

x has exactly n digits.
The product of the digits of x is divisible by k.
If no such integer exists, return -1.

- Examples
- Constraints
- [Mathematical Logic](#mathematical-logic)
- [Brute Force Enumeration](#brute-force-enumeration)

## Examples

### Example 1

**Input:**
```text
n = 10, t = 2
```

**Output:**
```text
10
```

**Explanation:**
The digit product of 10 is 0, which is divisible by 2, making it the smallest number greater than or equal to 10 that satisfies the condition.

### Example 2

**Input:**
```text
n = 15, t = 3
```

**Output:**
```text
16
```

**Explanation:**
The digit product of 16 is 6, which is divisible by 3, making it the smallest number greater than or equal to 15 that satisfies the condition.

## Constraints

```text
1 <= n <= 9
1 <= k <= 100
```

---
## Mathematical Logic

**Intuition**
The key observation is that for any number with more than 1 digit, the smallest possible number is `10^(n-1)` (e.g., 10, 100, 1000). This number always contains at least one zero. The product of digits including a zero is 0. Since 0 is divisible by any non-zero integer $k$, the answer for any $n &gt; 1$ is simply `10^(n-1)`. For $n=1$, we must check digits 1 through 9 manually.

**Steps**
- If $n &gt; 1$, return `10^(n-1)`.
- If $n == 1$, iterate through digits $d$ from 1 to 9.
- If $d \% k == 0$, return $d$.
- If the loop finishes without finding a match, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestNumber(self, n: int, k: int) -&gt; int:\n        if n &gt; 1:\n            return 10 ** (n - 1)\n        for i in range(1, 10):\n            if i % k == 0:\n                return i\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int smallestNumber(int n, int k) {\n        if (n &gt; 1) {\n            return (int) Math.pow(10, n - 1);\n        }\n        for (int i = 1; i &lt;= 9; i++) {\n            if (i % k == 0) return i;\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int smallestNumber(int n, int k) {\n        if (n &gt; 1) {\n            return (int)pow(10, n - 1);\n        }\n        for (int i = 1; i &lt;= 9; ++i) {\n            if (i % k == 0) return i;\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    smallestNumber(n, k) {\n        if (n &gt; 1) {\n            return 10 ** (n - 1);\n        }\n        for (let i = 1; i &lt;= 9; i++) {\n            if (i % k === 0) return i;\n        }\n        return -1;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    smallestNumber(n: number, k: number): number {\n        if (n &gt; 1) {\n            return 10 ** (n - 1);\n        }\n        for (let i = 1; i &lt;= 9; i++) {\n            if (i % k === 0) return i;\n        }\n        return -1;\n    }\n}" },]} />

**Complexity**
- Time: O(1) - We perform at most 9 iterations for $n=1$, or a constant time calculation for $n>1$.
- Space: O(1) - No extra space is used.
- Notes: This is the optimal solution.

---
## Brute Force Enumeration

**Intuition**
We can iterate through all `n`-digit numbers starting from the smallest (`10^(n-1)`) and check if the product of their digits is divisible by `k`. The first number we find is the answer.

**Steps**
- Calculate the start range as `10^(n-1)` and end range as `10^n - 1`.
- Iterate $x$ from start to end.
- For each $x$, calculate the product of its digits.
- If product $\% k == 0$, return $x$.
- If the loop finishes, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestNumber(self, n: int, k: int) -&gt; int:\n        start = 10 ** (n - 1)\n        end = 10 ** n\n        for x in range(start, end):\n            prod = 1\n            temp = x\n            while temp &gt; 0:\n                prod *= temp % 10\n                temp //= 10\n            if prod % k == 0:\n                return x\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int smallestNumber(int n, int k) {\n        int start = (int) Math.pow(10, n - 1);\n        int end = (int) Math.pow(10, n);\n        for (int x = start; x &lt; end; x++) {\n            int prod = 1;\n            int temp = x;\n            while (temp &gt; 0) {\n                prod *= temp % 10;\n                temp /= 10;\n            }\n            if (prod % k == 0) return x;\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int smallestNumber(int n, int k) {\n        int start = (int)pow(10, n - 1);\n        int end = (int)pow(10, n);\n        for (int x = start; x &lt; end; x++) {\n            int prod = 1;\n            int temp = x;\n            while (temp &gt; 0) {\n                prod *= temp % 10;\n                temp /= 10;\n            }\n            if (prod % k == 0) return x;\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    smallestNumber(n, k) {\n        const start = 10 ** (n - 1);\n        const end = 10 ** n;\n        for (let x = start; x &lt; end; x++) {\n            let prod = 1;\n            let temp = x;\n            while (temp &gt; 0) {\n                prod *= temp % 10;\n                temp = Math.floor(temp / 10);\n            }\n            if (prod % k === 0) return x;\n        }\n        return -1;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    smallestNumber(n: number, k: number): number {\n        const start = 10 ** (n - 1);\n        const end = 10 ** n;\n        for (let x = start; x &lt; end; x++) {\n            let prod = 1;\n            let temp = x;\n            while (temp &gt; 0) {\n                prod *= temp % 10;\n                temp = Math.floor(temp / 10);\n            }\n            if (prod % k === 0) return x;\n        }\n        return -1;\n    }\n}" },]} />

**Complexity**
- Time: O(9 * 10ⁿ) - In the worst case, we iterate through all $n$-digit numbers. For $n=9$, this is very slow.
- Space: O(1) - Constant space.
- Notes: This approach is conceptually simple but inefficient for larger values of $n$ compared to the mathematical approach.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Smallest Divisible Digit Product I Solutions","datePublished":"2025-07-21","description":"Find the smallest positive integer with n digits whose digit product is divisible by k.","keywords":"LeetCode, Smallest Divisible Digit Product I, Easy, Math, Enumeration"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Mathematical approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) because we perform at most 9 iterations for n=1, or a constant time calculation for n>1."}},{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(9 * 10^n) as we may iterate through all n-digit numbers in the worst case."}}]}).replace(/</g,'\u003c')}
</script>