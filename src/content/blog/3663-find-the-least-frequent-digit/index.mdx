---
title: "Find The Least Frequent Digit"
summary: "Given an integer n, return the least frequent digit. If multiple digits have the same least frequency, return the smallest one."
date: "Mar 19, 2026"
tags:
  - Array
  - Hash Table
  - Math
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 69.70% | **Paid:** No
> **Topics:** Array, Hash Table, Math, Counting

You are given an integer n. Return the least frequent digit in n. If there are multiple digits with the same least frequency, return the smallest one.

- Examples
- Constraints
- [Counting Array (Math)](#counting-array-math)
- [String Conversion](#string-conversion)
- [Hash Map](#hash-map)

## Examples

### Example 1

**Input:**
```text
n = 1553322
```

**Output:**
```text
1
```

**Explanation:**
The least frequent digit in n is 1, which appears only once. All other digits appear twice.

### Example 2

**Input:**
```text
n = 723344511
```

**Output:**
```text
2
```

**Explanation:**
The least frequent digits in n are 7, 2, and 5; each appears only once.

## Constraints

```text
- 1 <= n <= 2^31​​​​​​​ - 1
```

---
## Counting Array (Math)

**Intuition**
Since there are only 10 possible digits (0-9), we can use a fixed-size array of length 10 to count the occurrences of each digit. We then iterate through this array to find the digit with the smallest count. If counts are equal, the smaller digit wins.

**Steps**
- Initialize an array `count` of size 10 with all zeros.
- Handle the case where `n` is 0 by incrementing `count[0]`.
- For `n &gt; 0`, extract the last digit using `n % 10`, increment its count, and remove the digit using `n /= 10`.
- Iterate from 0 to 9. Track the minimum frequency found so far and the corresponding digit. Update the answer whenever a strictly smaller frequency is found.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findLeastFrequentDigit(self, n: int) -&gt; int:\n        count = [0] * 10\n        if n == 0:\n            count[0] = 1\n        else:\n            while n &gt; 0:\n                digit = n % 10\n                count[digit] += 1\n                n //= 10\n        \n        min_freq = float('inf')\n        ans = 0\n        for i in range(10):\n            if count[i] &lt; min_freq:\n                min_freq = count[i]\n                ans = i\n        return ans" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int findLeastFrequentDigit(int n) {\n        int[] count = new int[10];\n        if (n == 0) {\n            count[0] = 1;\n        } else {\n            while (n &gt; 0) {\n                int digit = n % 10;\n                count[digit]++;\n                n /= 10;\n            }\n        }\n        \n        int minFreq = Integer.MAX_VALUE;\n        int ans = 0;\n        for (int i = 0; i &lt; 10; i++) {\n            if (count[i] &lt; minFreq) {\n                minFreq = count[i];\n                ans = i;\n            }\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int findLeastFrequentDigit(int n) {\n        int count[10] = {0};\n        if (n == 0) {\n            count[0] = 1;\n        } else {\n            while (n &gt; 0) {\n                int digit = n % 10;\n                count[digit]++;\n                n /= 10;\n            }\n        }\n        \n        int minFreq = INT_MAX;\n        int ans = 0;\n        for (int i = 0; i &lt; 10; ++i) {\n            if (count[i] &lt; minFreq) {\n                minFreq = count[i];\n                ans = i;\n            }\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar findLeastFrequentDigit = function(n) {\n    let count = new Array(10).fill(0);\n    if (n === 0) {\n        count[0] = 1;\n    } else {\n        while (n &gt; 0) {\n            let digit = n % 10;\n            count[digit]++;\n            n = Math.floor(n / 10);\n        }\n    }\n    \n    let minFreq = Infinity;\n    let ans = 0;\n    for (let i = 0; i &lt; 10; i++) {\n        if (count[i] &lt; minFreq) {\n            minFreq = count[i];\n            ans = i;\n        }\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findLeastFrequentDigit(n: number): number {\n    let count: number[] = new Array(10).fill(0);\n    if (n === 0) {\n        count[0] = 1;\n    } else {\n        while (n &gt; 0) {\n            let digit: number = n % 10;\n            count[digit]++;\n            n = Math.floor(n / 10);\n        }\n    }\n    \n    let minFreq: number = Infinity;\n    let ans: number = 0;\n    for (let i = 0; i &lt; 10; i++) {\n        if (count[i] &lt; minFreq) {\n            minFreq = count[i];\n            ans = i;\n        }\n    }\n    return ans;\n}" },]} />

**Complexity**
- Time: O(log n) — We iterate over the number of digits in n, which is log₁₀(n).
- Space: O(1) — The count array is always of fixed size 10.
- Notes: This is the most efficient approach as it avoids string conversion overhead and uses minimal memory.

---
## String Conversion

**Intuition**
Convert the integer to a string to iterate over each digit easily. This approach leverages built-in language features for string iteration, which can be more readable than modulo arithmetic.

**Steps**
- Convert `n` to a string `s`.
- Initialize a `count` array of size 10 with zeros.
- Iterate through each character `c` in `s`. Convert `c` to an integer and increment the corresponding index in `count`.
- Iterate from 0 to 9 to find the smallest index with the minimum frequency value.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findLeastFrequentDigit(self, n: int) -&gt; int:\n        s = str(n)\n        count = [0] * 10\n        for char in s:\n            count[int(char)] += 1\n        \n        min_freq = float('inf')\n        ans = 0\n        for i in range(10):\n            if count[i] &lt; min_freq:\n                min_freq = count[i]\n                ans = i\n        return ans" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int findLeastFrequentDigit(int n) {\n        String s = String.valueOf(n);\n        int[] count = new int[10];\n        for (int i = 0; i &lt; s.length(); i++) {\n            char c = s.charAt(i);\n            count[c - '0']++;\n        }\n        \n        int minFreq = Integer.MAX_VALUE;\n        int ans = 0;\n        for (int i = 0; i &lt; 10; i++) {\n            if (count[i] &lt; minFreq) {\n                minFreq = count[i];\n                ans = i;\n            }\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int findLeastFrequentDigit(int n) {\n        string s = to_string(n);\n        int count[10] = {0};\n        for (char c : s) {\n            count[c - '0']++;\n        }\n        \n        int minFreq = INT_MAX;\n        int ans = 0;\n        for (int i = 0; i &lt; 10; ++i) {\n            if (count[i] &lt; minFreq) {\n                minFreq = count[i];\n                ans = i;\n            }\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar findLeastFrequentDigit = function(n) {\n    let s = n.toString();\n    let count = new Array(10).fill(0);\n    for (let i = 0; i &lt; s.length; i++) {\n        count[parseInt(s[i])]++;\n    }\n    \n    let minFreq = Infinity;\n    let ans = 0;\n    for (let i = 0; i &lt; 10; i++) {\n        if (count[i] &lt; minFreq) {\n            minFreq = count[i];\n            ans = i;\n        }\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findLeastFrequentDigit(n: number): number {\n    let s: string = n.toString();\n    let count: number[] = new Array(10).fill(0);\n    for (let i = 0; i &lt; s.length; i++) {\n        count[parseInt(s[i])]++;\n    }\n    \n    let minFreq: number = Infinity;\n    let ans: number = 0;\n    for (let i = 0; i &lt; 10; i++) {\n        if (count[i] &lt; minFreq) {\n            minFreq = count[i];\n            ans = i;\n        }\n    }\n    return ans;\n}" },]} />

**Complexity**
- Time: O(log n) — Converting to string and iterating takes time proportional to the number of digits.
- Space: O(log n) — The string representation of `n` requires extra space proportional to the number of digits.
- Notes: Slightly higher space complexity than the math approach due to string allocation, but offers cleaner syntax.

---
## Hash Map

**Intuition**
Use a hash map (or dictionary) to store the frequency of each digit found in `n`. This is a generalized approach for counting problems, though slightly overkill for the fixed range 0-9.

**Steps**
- Initialize an empty map.
- Convert `n` to a string or use math to extract digits.
- Update the count for each digit in the map.
- Iterate from 0 to 9. For each digit, check the map for its frequency (defaulting to 0 if not present). Track the digit with the minimum frequency.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findLeastFrequentDigit(self, n: int) -&gt; int:\n        from collections import defaultdict\n        freq = defaultdict(int)\n        \n        if n == 0:\n            freq[0] = 1\n        else:\n            while n &gt; 0:\n                freq[n % 10] += 1\n                n //= 10\n        \n        min_freq = float('inf')\n        ans = 0\n        for i in range(10):\n            current = freq.get(i, 0)\n            if current &lt; min_freq:\n                min_freq = current\n                ans = i\n        return ans" },
{ name: "Java", lang: "java", code: "import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public int findLeastFrequentDigit(int n) {\n        Map&lt;Integer, Integer&gt; freq = new HashMap&lt;&gt;();\n        \n        if (n == 0) {\n            freq.put(0, 1);\n        } else {\n            while (n &gt; 0) {\n                int digit = n % 10;\n                freq.put(digit, freq.getOrDefault(digit, 0) + 1);\n                n /= 10;\n            }\n        }\n        \n        int minFreq = Integer.MAX_VALUE;\n        int ans = 0;\n        for (int i = 0; i &lt; 10; i++) {\n            int current = freq.getOrDefault(i, 0);\n            if (current &lt; minFreq) {\n                minFreq = current;\n                ans = i;\n            }\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;unordered_map&gt;\n\nclass Solution {\npublic:\n    int findLeastFrequentDigit(int n) {\n        std::unordered_map&lt;int, int&gt; freq;\n        \n        if (n == 0) {\n            freq[0] = 1;\n        } else {\n            while (n &gt; 0) {\n                int digit = n % 10;\n                freq[digit]++;\n                n /= 10;\n            }\n        }\n        \n        int minFreq = INT_MAX;\n        int ans = 0;\n        for (int i = 0; i &lt; 10; ++i) {\n            int current = 0;\n            if (freq.find(i) != freq.end()) {\n                current = freq[i];\n            }\n            if (current &lt; minFreq) {\n                minFreq = current;\n                ans = i;\n            }\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar findLeastFrequentDigit = function(n) {\n    let freq = new Map();\n    \n    if (n === 0) {\n        freq.set(0, 1);\n    } else {\n        while (n &gt; 0) {\n            let digit = n % 10;\n            freq.set(digit, (freq.get(digit) || 0) + 1);\n            n = Math.floor(n / 10);\n        }\n    }\n    \n    let minFreq = Infinity;\n    let ans = 0;\n    for (let i = 0; i &lt; 10; i++) {\n        let current = freq.get(i) || 0;\n        if (current &lt; minFreq) {\n            minFreq = current;\n            ans = i;\n        }\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findLeastFrequentDigit(n: number): number {\n    let freq: Map&lt;number, number&gt; = new Map();\n    \n    if (n === 0) {\n        freq.set(0, 1);\n    } else {\n        while (n &gt; 0) {\n            let digit: number = n % 10;\n            freq.set(digit, (freq.get(digit) || 0) + 1);\n            n = Math.floor(n / 10);\n        }\n    }\n    \n    let minFreq: number = Infinity;\n    let ans: number = 0;\n    for (let i = 0; i &lt; 10; i++) {\n        let current: number = freq.get(i) || 0;\n        if (current &lt; minFreq) {\n            minFreq = current;\n            ans = i;\n        }\n    }\n    return ans;\n}" },]} />

**Complexity**
- Time: O(log n) — Iterating over digits and the fixed range 0-9.
- Space: O(1) — The map stores at most 10 entries.
- Notes: While functionally correct, using a hash map for a fixed range of 10 digits is less efficient than a simple array due to hashing overhead.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find The Least Frequent Digit Solutions","datePublished":"2026-03-19","description":"Given an integer n, return the least frequent digit. If multiple digits have the same least frequency, return the smallest one.","keywords":"LeetCode, Find The Least Frequent Digit, Easy, Array, Hash Table, Math, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Counting Array approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(log n), where n is the input value, as we process each digit once."}},{"@type":"Question","name":"Why do we iterate from 0 to 9 to find the answer?","acceptedAnswer":{"@type":"Answer","text":"We iterate 0 to 9 to ensure we check all possible digits, including those that might not appear in the number (frequency 0). This also naturally handles the tie-breaker by selecting the smallest digit first."}},{"@type":"Question","name":"Which approach is most efficient for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Counting Array (Math) approach is most efficient as it uses O(1) space and avoids the overhead of string conversion or hash map operations."}}]}).replace(/</g,'\u003c')}
</script>