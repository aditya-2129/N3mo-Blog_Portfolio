---
title: "Determine Color of a Chessboard Square"
summary: "Determine if a given chessboard square coordinate is white or black based on alternating color patterns."
date: "Jul 24, 2025"
tags:
  - Math
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 79.80% | **Paid:** No
> **Topics:** Math, String

You are given coordinates, a string that represents the position of a square on the chessboard.

The chessboard is an 8x8 grid, where the squares are colored alternately white and black. The top-left corner (a1) is black.

Return true if the square is white, and false otherwise.

The coordinate string always represents a valid chessboard square.

- Examples
- Constraints
- [Mathematical Parity Check](#mathematical-parity-check)
- [ASCII Value Calculation](#ascii-value-calculation)
- [Set Lookup](#set-lookup)

## Examples

**Example 1:**
```text
Input: coordinates = "a1"
Output: false
Explanation: "a1" is a black square.
```

**Example 2:**
```text
Input: coordinates = "h3"
Output: true
Explanation: "h3" is a white square.
```

**Example 3:**
```text
Input: coordinates = "c7"
Output: false
```

## Constraints

```text
coordinates.length == 2
'a' <= coordinates[0] <= 'h'
'1' <= coordinates[1] <= '8'
```

---

## Mathematical Parity Check

**Intuition**
The color of a square depends on the parity (even or odd nature) of the sum of its row and column indices. If the sum is even, the square is white; if odd, it is black.

**Steps**
- Extract the column letter (a-h) and convert it to a 0-based index (a=0, b=1, etc.).
- Extract the row number (1-8) and convert it to an integer.
- Calculate the sum of the column index and the row number.
- Return true if the sum is even, false otherwise.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def squareIsWhite(self, coordinates: str) -> bool:\n        col = ord(coordinates[0]) - ord(\'a\')\n        row = int(coordinates[1])\n        return (col + row) % 2 == 0' },
{ name: "Java", lang: "java", code: 'class Solution {\n    public boolean squareIsWhite(String coordinates) {\n        int col = coordinates.charAt(0) - \'a\';\n        int row = coordinates.charAt(1) - \'0\';\n        return (col + row) % 2 == 0;\n    }\n}' },
{ name: "C++", lang: "cpp", code: 'class Solution {\npublic:\n    bool squareIsWhite(string coordinates) {\n        int col = coordinates[0] - \'a\';\n        int row = coordinates[1] - \'0\';\n        return (col + row) % 2 == 0;\n    }\n};' },
{ name: "JavaScript", lang: "javascript", code: 'var squareIsWhite = function(coordinates) {\n    const col = coordinates.charCodeAt(0) - "a".charCodeAt(0);\n    const row = parseInt(coordinates[1]);\n    return (col + row) % 2 === 0;\n};' },
{ name: "TypeScript", lang: "typescript", code: 'function squareIsWhite(coordinates: string): boolean {\n    const col = coordinates.charCodeAt(0) - "a".charCodeAt(0);\n    const row = parseInt(coordinates[1]);\n    return (col + row) % 2 === 0;\n}' },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This is the most optimal approach with constant time operations.

---

## ASCII Value Calculation

**Intuition**
We can leverage the ASCII values of the characters directly to perform the parity check without explicitly mapping the letter to an index variable, though the logic remains mathematically identical to the parity check.

**Steps**
- Calculate the difference between the ASCII value of the first character and 'a'.
- Calculate the difference between the ASCII value of the second character and '0'.
- Sum these differences and check if the result is even.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def squareIsWhite(self, coordinates: str) -> bool:\n        return (ord(coordinates[0]) + int(coordinates[1])) % 2 == 1' },
{ name: "Java", lang: "java", code: 'class Solution {\n    public boolean squareIsWhite(String coordinates) {\n        return (coordinates.charAt(0) + coordinates.charAt(1)) % 2 == 1;\n    }\n}' },
{ name: "C++", lang: "cpp", code: 'class Solution {\npublic:\n    bool squareIsWhite(string coordinates) {\n        return (coordinates[0] + coordinates[1]) % 2 == 1;\n    }\n};' },
{ name: "JavaScript", lang: "javascript", code: 'var squareIsWhite = function(coordinates) {\n    return (coordinates.charCodeAt(0) + coordinates.charCodeAt(1)) % 2 === 1;\n};' },
{ name: "TypeScript", lang: "typescript", code: 'function squareIsWhite(coordinates: string): boolean {\n    return (coordinates.charCodeAt(0) + coordinates.charCodeAt(1)) % 2 === 1;\n}' },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This is a concise implementation relying on ASCII properties.

---

## Set Lookup

**Intuition**
Since the chessboard size is fixed and small (8x8), we can pre-compute all white squares and store them in a set. We then simply check if the input coordinate exists in that set.

**Steps**
- Initialize a set containing all coordinates of white squares.
- Check if the input `coordinates` string is present in the set.
- Return the result of the check.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def squareIsWhite(self, coordinates: str) -> bool:\n        white_squares = {\n            "a2", "a4", "a6", "a8",\n            "b1", "b3", "b5", "b7",\n            "c2", "c4", "c6", "c8",\n            "d1", "d3", "d5", "d7",\n            "e2", "e4", "e6", "e8",\n            "f1", "f3", "f5", "f7",\n            "g2", "g4", "g6", "g8",\n            "h1", "h3", "h5", "h7"\n        }\n        return coordinates in white_squares' },
{ name: "Java", lang: "java", code: 'import java.util.HashSet;\nimport java.util.Set;\n\nclass Solution {\n    public boolean squareIsWhite(String coordinates) {\n        Set<String> whiteSquares = new HashSet&lt;&gt;();\n        whiteSquares.add("a2"); whiteSquares.add("a4"); whiteSquares.add("a6"); whiteSquares.add("a8");\n        whiteSquares.add("b1"); whiteSquares.add("b3"); whiteSquares.add("b5"); whiteSquares.add("b7");\n        whiteSquares.add("c2"); whiteSquares.add("c4"); whiteSquares.add("c6"); whiteSquares.add("c8");\n        whiteSquares.add("d1"); whiteSquares.add("d3"); whiteSquares.add("d5"); whiteSquares.add("d7");\n        whiteSquares.add("e2"); whiteSquares.add("e4"); whiteSquares.add("e6"); whiteSquares.add("e8");\n        whiteSquares.add("f1"); whiteSquares.add("f3"); whiteSquares.add("f5"); whiteSquares.add("f7");\n        whiteSquares.add("g2"); whiteSquares.add("g4"); whiteSquares.add("g6"); whiteSquares.add("g8");\n        whiteSquares.add("h1"); whiteSquares.add("h3"); whiteSquares.add("h5"); whiteSquares.add("h7");\n        return whiteSquares.contains(coordinates);\n    }\n}' },
{ name: "C++", lang: "cpp", code: '#include <string>\n#include <unordered_set>\n\nclass Solution {\npublic:\n    bool squareIsWhite(string coordinates) {\n        unordered_set<string> whiteSquares = {\n            "a2", "a4", "a6", "a8",\n            "b1", "b3", "b5", "b7",\n            "c2", "c4", "c6", "c8",\n            "d1", "d3", "d5", "d7",\n            "e2", "e4", "e6", "e8",\n            "f1", "f3", "f5", "f7",\n            "g2", "g4", "g6", "g8",\n            "h1", "h3", "h5", "h7"\n        };\n        return whiteSquares.find(coordinates) != whiteSquares.end();\n    }\n};' },
{ name: "JavaScript", lang: "javascript", code: 'var squareIsWhite = function(coordinates) {\n    const whiteSquares = new Set([\n        "a2", "a4", "a6", "a8",\n        "b1", "b3", "b5", "b7",\n        "c2", "c4", "c6", "c8",\n        "d1", "d3", "d5", "d7",\n        "e2", "e4", "e6", "e8",\n        "f1", "f3", "f5", "f7",\n        "g2", "g4", "g6", "g8",\n        "h1", "h3", "h5", "h7"\n    ]);\n    return whiteSquares.has(coordinates);\n};' },
{ name: "TypeScript", lang: "typescript", code: 'function squareIsWhite(coordinates: string): boolean {\n    const whiteSquares = new Set([\n        "a2", "a4", "a6", "a8",\n        "b1", "b3", "b5", "b7",\n        "c2", "c4", "c6", "c8",\n        "d1", "d3", "d5", "d7",\n        "e2", "e4", "e6", "e8",\n        "f1", "f3", "f5", "f7",\n        "g2", "g4", "g6", "g8",\n        "h1", "h3", "h5", "h7"\n    ]);\n    return whiteSquares.has(coordinates);\n}' },]} />

**Complexity**
- Time: O(1) (Lookup in a set is O(1) on average)
- Space: O(1) (The set size is fixed at 32 elements)
- Notes: Less efficient in terms of code size and memory compared to the mathematical approach, but conceptually simple.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Determine Color of a Chessboard Square Solutions","datePublished":"2025-07-24","description":"Determine if a given chessboard square coordinate is white or black based on alternating color patterns.","keywords":"LeetCode, Determine Color of a Chessboard Square, Easy, Math, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Mathematical Parity Check approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) because it involves a constant number of arithmetic operations."}},{"@type":"Question","name":"What is the space complexity of the Set Lookup approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because the set stores a fixed number of elements (32 white squares) regardless of input."}},{"@type":"Question","name":"Why does the sum of indices determine the color?","acceptedAnswer":{"@type":"Answer","text":"On a chessboard, colors alternate. Moving one step in any direction changes the color. Therefore, the parity of the Manhattan distance (sum of row and column indices) from the origin determines the color."}}]}).replace(/</g,'\u003c')}
</script>