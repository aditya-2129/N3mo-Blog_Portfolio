---
title: "Find Minimum Operations to Make All Elements Divisible by Three"
summary: "Calculate the minimum operations needed to make all array elements divisible by 3 by adding or subtracting 1."
date: "Jun 28, 2024"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 90.80% | **Paid:** No
> **Topics:** Array, Math

You are given an integer array nums. In one operation, you can add 1 or subtract 1 from any element of nums.

Return the minimum number of operations to make all elements of nums divisible by 3.

- Examples
- Constraints
- [Direct Counting](#direct-counting)
- [Mathematical Formula](#mathematical-formula)
- [Functional Programming](#functional-programming)

## Examples

### Example 1
```text
Input: nums = [1,2,3,4]
Output: 3
Explanation:
- All array elements can be made divisible by 3 using 3 operations:
Subtract 1 from 1
Add 1 to 2
Subtract 1 from 4
```

### Example 2
```text
Input: nums = [3,6,9]
Output: 0
```

## Constraints
```text
1 <= nums.length <= 50
1 <= nums[i] <= 50
```

## Direct Counting

**Intuition**
For each element, if it's not divisible by 3, we need exactly 1 operation (either add or subtract 1 to reach the nearest multiple of 3).

**Steps**
- Iterate through each element in the array
- Check if the element is divisible by 3
- If not, increment the operation count
- Return the total count

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def minimumOperations(self, nums: List[int]) -> int:
        return sum(1 for num in nums if num % 3 != 0)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minimumOperations(int[] nums) {
        int count = 0;
        for (int num : nums) {
            if (num % 3 != 0) {
                count++;
            }
        }
        return count;
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <vector>
using namespace std;

class Solution {
public:
    int minimumOperations(vector<int>& nums) {
        int count = 0;
        for (int num : nums) {
            if (num % 3 != 0) {
                count++;
            }
        }
        return count;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    minimumOperations(nums) {
        let count = 0;
        for (const num of nums) {
            if (num % 3 !== 0) {
                count++;
            }
        }
        return count;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    minimumOperations(nums: number[]): number {
        let count = 0;
        for (const num of nums) {
            if (num % 3 !== 0) {
                count++;
            }
        }
        return count;
    }
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(1) constant extra space
- Notes: Simple and efficient approach

## Mathematical Formula

**Intuition**
The minimum operations for each element is min(remainder, 3 - remainder) where remainder is num % 3.

**Steps**
- Iterate through each element in the array
- Calculate the remainder when divided by 3
- Add min(remainder, 3 - remainder) to the total
- Return the total

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def minimumOperations(self, nums: List[int]) -> int:
        return sum(min(num % 3, 3 - num % 3) for num in nums)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minimumOperations(int[] nums) {
        int count = 0;
        for (int num : nums) {
            int remainder = num % 3;
            count += Math.min(remainder, 3 - remainder);
        }
        return count;
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <algorithm>
using namespace std;

class Solution {
public:
    int minimumOperations(vector<int>& nums) {
        int count = 0;
        for (int num : nums) {
            int remainder = num % 3;
            count += min(remainder, 3 - remainder);
        }
        return count;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    minimumOperations(nums) {
        let count = 0;
        for (const num of nums) {
            const remainder = num % 3;
            count += Math.min(remainder, 3 - remainder);
        }
        return count;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    minimumOperations(nums: number[]): number {
        let count = 0;
        for (const num of nums) {
            const remainder = num % 3;
            count += Math.min(remainder, 3 - remainder);
        }
        return count;
    }
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(1) constant extra space
- Notes: More mathematical formulation, same complexity as direct counting

## Functional Programming

**Intuition**
Use functional programming constructs like map, reduce, or accumulate to process the array in a declarative style.

**Steps**
- Use reduce/accumulate to sum up the operations needed
- For each element, add 1 if not divisible by 3, else add 0
- Return the accumulated result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List
from functools import reduce

class Solution:
    def minimumOperations(self, nums: List[int]) -> int:
        return reduce(lambda acc, num: acc + (1 if num % 3 != 0 else 0), nums, 0)
` },
{ name: "Java", lang: "java", code: `import java.util.Arrays;

class Solution {
    public int minimumOperations(int[] nums) {
        return Arrays.stream(nums)
                     .map(num -> num % 3 != 0 ? 1 : 0)
                     .sum();
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <numeric>
using namespace std;

class Solution {
public:
    int minimumOperations(vector<int>& nums) {
        return accumulate(nums.begin(), nums.end(), 0, [](int acc, int num) {
            return acc + (num % 3 != 0 ? 1 : 0);
        });
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    minimumOperations(nums) {
        return nums.reduce((acc, num) =&gt; acc + (num % 3 !== 0 ? 1 : 0), 0);
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    minimumOperations(nums: number[]): number {
        return nums.reduce((acc, num) =&gt; acc + (num % 3 !== 0 ? 1 : 0), 0);
    }
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(1) constant extra space
- Notes: Declarative style, may have slight overhead in some languages

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find Minimum Operations to Make All Elements Divisible by Three Solutions","datePublished":"2024-06-28","description":"Calculate the minimum operations needed to make all array elements divisible by 3 by adding or subtracting 1.","keywords":"LeetCode, Find Minimum Operations to Make All Elements Divisible by Three, Easy, Array, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Direct Counting approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of the array."}},{"@type":"Question","name":"What is the space complexity of all approaches?","acceptedAnswer":{"@type":"Answer","text":"O(1) constant extra space for all approaches."}},{"@type":"Question","name":"Why does each non-divisible element need exactly 1 operation?","acceptedAnswer":{"@type":"Answer","text":"If num % 3 == 1, subtract 1. If num % 3 == 2, add 1. Both cases require exactly 1 operation."}}]}).replace(/</g,'\u003c')}
</script>