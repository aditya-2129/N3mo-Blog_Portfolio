---
title: "Toeplitz Matrix"
summary: "Given a matrix, return true if it is a Toeplitz matrix where every diagonal has the same elements."
date: "Oct 12, 2024"
tags:
  - Array
  - Matrix
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 69.70% | **Paid:** No
> **Topics:** Array, Matrix

Given an m x n matrix, return true if the matrix is Toeplitz. Otherwise, return false.

A matrix is Toeplitz if every diagonal from top-left to bottom-right has the same elements.

- Examples
- Constraints
- [Approach 1: Diagonal Traversal](#approach-1-diagonal-traversal)
- [Approach 2: Compare with Top-Left Neighbor](#approach-2-compare-with-top-left-neighbor)
- [Approach 3: Hash Map by Diagonal Index](#approach-3-hash-map-by-diagonal-index)

## Examples

### Example 1:
```text
Input: matrix = [[1,2,3,4],[5,1,2,3],[9,5,1,2]]
Output: true
Explanation:
In the above grid, the diagonals are:
"[9]", "[5, 5]", "[1, 1, 1]", "[2, 2, 2]", "[3, 3]", "[4]".
In each diagonal all elements are the same, so the answer is True.
```

### Example 2:
```text
Input: matrix = [[1,2],[2,2]]
Output: false
Explanation:
The diagonal "[1, 2]" has different elements.
```

## Constraints

```text
m == matrix.length
n == matrix[i].length
1 <= m, n <= 20
0 <= matrix[i][j] <= 99
```

---

## Approach 1: Diagonal Traversal

**Intuition**
Iterate through each diagonal starting from the first row and first column, checking if all elements on each diagonal are equal.

**Steps**
- Start from each element in the first row and first column
- For each starting position, traverse diagonally down-right
- Compare all elements on the diagonal with the first element
- Return false if any mismatch is found

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def isToeplitzMatrix(self, matrix):\n        m, n = len(matrix), len(matrix[0])\n        \n        # Check diagonals starting from first row\n        for col in range(n):\n            val = matrix[0][col]\n            row, c = 1, col + 1\n            while row < m and c < n:\n                if matrix[row][c] != val:\n                    return False\n                row += 1\n                c += 1\n        \n        # Check diagonals starting from first column (excluding first element)\n        for row in range(1, m):\n            val = matrix[row][0]\n            r, col = row + 1, 1\n            while r < m and col < n:\n                if matrix[r][col] != val:\n                    return False\n                r += 1\n                col += 1\n        \n        return True\n' },
{ name: "Java", lang: "java", code: 'class Solution {\n    public boolean isToeplitzMatrix(int[][] matrix) {\n        int m = matrix.length;\n        int n = matrix[0].length;\n        \n        // Check diagonals starting from first row\n        for (int col = 0; col &lt; n; col++) {\n            int val = matrix[0][col];\n            int row = 1, c = col + 1;\n            while (row &lt; m && c &lt; n) {\n                if (matrix[row][c] != val) {\n                    return false;\n                }\n                row++;\n                c++;\n            }\n        }\n        \n        // Check diagonals starting from first column (excluding first element)\n        for (int row = 1; row &lt; m; row++) {\n            int val = matrix[row][0];\n            int r = row + 1, col = 1;\n            while (r &lt; m && col &lt; n) {\n                if (matrix[r][col] != val) {\n                    return false;\n                }\n                r++;\n                col++;\n            }\n        }\n        \n        return true;\n    }\n}\n' },
{ name: "C++", lang: "cpp", code: 'class Solution {\npublic:\n    bool isToeplitzMatrix(vector<vector<int>&gt;& matrix) {\n        int m = matrix.size();\n        int n = matrix[0].size();\n        \n        // Check diagonals starting from first row\n        for (int col = 0; col &lt; n; col++) {\n            int val = matrix[0][col];\n            int row = 1, c = col + 1;\n            while (row &lt; m && c &lt; n) {\n                if (matrix[row][c] != val) {\n                    return false;\n                }\n                row++;\n                c++;\n            }\n        }\n        \n        // Check diagonals starting from first column (excluding first element)\n        for (int row = 1; row &lt; m; row++) {\n            int val = matrix[row][0];\n            int r = row + 1, col = 1;\n            while (r &lt; m && col &lt; n) {\n                if (matrix[r][col] != val) {\n                    return false;\n                }\n                r++;\n                col++;\n            }\n        }\n        \n        return true;\n    }\n};\n' },
{ name: "JavaScript", lang: "javascript", code: '/**\n * @param {number[][]} matrix\n * @return {boolean}\n */\nvar isToeplitzMatrix = function(matrix) {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    \n    // Check diagonals starting from first row\n    for (let col = 0; col &lt; n; col++) {\n        const val = matrix[0][col];\n        let row = 1, c = col + 1;\n        while (row &lt; m && c &lt; n) {\n            if (matrix[row][c] !== val) {\n                return false;\n            }\n            row++;\n            c++;\n        }\n    }\n    \n    // Check diagonals starting from first column (excluding first element)\n    for (let row = 1; row &lt; m; row++) {\n        const val = matrix[row][0];\n        let r = row + 1, col = 1;\n        while (r &lt; m && col &lt; n) {\n            if (matrix[r][col] !== val) {\n                return false;\n            }\n            r++;\n            col++;\n        }\n    }\n    \n    return true;\n};\n' },
{ name: "TypeScript", lang: "typescript", code: 'function isToeplitzMatrix(matrix: number[][]): boolean {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    \n    // Check diagonals starting from first row\n    for (let col = 0; col &lt; n; col++) {\n        const val = matrix[0][col];\n        let row = 1, c = col + 1;\n        while (row &lt; m && c &lt; n) {\n            if (matrix[row][c] !== val) {\n                return false;\n            }\n            row++;\n            c++;\n        }\n    }\n    \n    // Check diagonals starting from first column (excluding first element)\n    for (let row = 1; row &lt; m; row++) {\n        const val = matrix[row][0];\n        let r = row + 1, col = 1;\n        while (r &lt; m && col &lt; n) {\n            if (matrix[r][col] !== val) {\n                return false;\n            }\n            r++;\n            col++;\n        }\n    }\n    \n    return true;\n}\n' },]} />

**Complexity**
- Time: O(m × n)
- Space: O(1)
- Notes: Visits each element at most once, but requires two separate loops for row and column starts

---

## Approach 2: Compare with Top-Left Neighbor

**Intuition**
For a Toeplitz matrix, each element must equal its top-left neighbor. Simply compare each element (except first row and column) with its diagonal predecessor.

**Steps**
- Iterate through the matrix starting from row 1 and column 1
- For each element, compare it with the element at (row-1, col-1)
- Return false if any mismatch is found
- Return true if all comparisons pass

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def isToeplitzMatrix(self, matrix):\n        m, n = len(matrix), len(matrix[0])\n        \n        for row in range(1, m):\n            for col in range(1, n):\n                if matrix[row][col] != matrix[row - 1][col - 1]:\n                    return False\n        \n        return True\n' },
{ name: "Java", lang: "java", code: 'class Solution {\n    public boolean isToeplitzMatrix(int[][] matrix) {\n        int m = matrix.length;\n        int n = matrix[0].length;\n        \n        for (int row = 1; row &lt; m; row++) {\n            for (int col = 1; col &lt; n; col++) {\n                if (matrix[row][col] != matrix[row - 1][col - 1]) {\n                    return false;\n                }\n            }\n        }\n        \n        return true;\n    }\n}\n' },
{ name: "C++", lang: "cpp", code: 'class Solution {\npublic:\n    bool isToeplitzMatrix(vector<vector<int>&gt;& matrix) {\n        int m = matrix.size();\n        int n = matrix[0].size();\n        \n        for (int row = 1; row &lt; m; row++) {\n            for (int col = 1; col &lt; n; col++) {\n                if (matrix[row][col] != matrix[row - 1][col - 1]) {\n                    return false;\n                }\n            }\n        }\n        \n        return true;\n    }\n};\n' },
{ name: "JavaScript", lang: "javascript", code: '/**\n * @param {number[][]} matrix\n * @return {boolean}\n */\nvar isToeplitzMatrix = function(matrix) {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    \n    for (let row = 1; row &lt; m; row++) {\n        for (let col = 1; col &lt; n; col++) {\n            if (matrix[row][col] !== matrix[row - 1][col - 1]) {\n                return false;\n            }\n        }\n    }\n    \n    return true;\n};\n' },
{ name: "TypeScript", lang: "typescript", code: 'function isToeplitzMatrix(matrix: number[][]): boolean {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    \n    for (let row = 1; row &lt; m; row++) {\n        for (let col = 1; col &lt; n; col++) {\n            if (matrix[row][col] !== matrix[row - 1][col - 1]) {\n                return false;\n            }\n        }\n    }\n    \n    return true;\n}\n' },]} />

**Complexity**
- Time: O(m × n)
- Space: O(1)
- Notes: Most elegant solution with single pass through the matrix

---

## Approach 3: Hash Map by Diagonal Index

**Intuition**
Elements on the same diagonal have the same difference (row - col). Use a hash map to store the expected value for each diagonal and verify consistency.

**Steps**
- Create a hash map to store values for each diagonal (keyed by row - col)
- Iterate through all elements in the matrix
- For each element, check if its diagonal already has a value stored
- If stored value differs from current element, return false
- Otherwise, store the current element as the expected value for that diagonal
- Return true if all elements are consistent

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def isToeplitzMatrix(self, matrix):\n        m, n = len(matrix), len(matrix[0])\n        diagonal = {}\n        \n        for row in range(m):\n            for col in range(n):\n                key = row - col\n                if key in diagonal:\n                    if diagonal[key] != matrix[row][col]:\n                        return False\n                else:\n                    diagonal[key] = matrix[row][col]\n        \n        return True\n' },
{ name: "Java", lang: "java", code: 'import java.util.HashMap;\nimport java.util.Map;\n\nclass Solution {\n    public boolean isToeplitzMatrix(int[][] matrix) {\n        int m = matrix.length;\n        int n = matrix[0].length;\n        Map<Integer, Integer> diagonal = new HashMap&lt;&gt;();\n        \n        for (int row = 0; row &lt; m; row++) {\n            for (int col = 0; col &lt; n; col++) {\n                int key = row - col;\n                if (diagonal.containsKey(key)) {\n                    if (diagonal.get(key) != matrix[row][col]) {\n                        return false;\n                    }\n                } else {\n                    diagonal.put(key, matrix[row][col]);\n                }\n            }\n        }\n        \n        return true;\n    }\n}\n' },
{ name: "C++", lang: "cpp", code: '#include <unordered_map>\n\nclass Solution {\npublic:\n    bool isToeplitzMatrix(vector<vector<int>&gt;& matrix) {\n        int m = matrix.size();\n        int n = matrix[0].size();\n        unordered_map<int, int> diagonal;\n        \n        for (int row = 0; row &lt; m; row++) {\n            for (int col = 0; col &lt; n; col++) {\n                int key = row - col;\n                if (diagonal.find(key) != diagonal.end()) {\n                    if (diagonal[key] != matrix[row][col]) {\n                        return false;\n                    }\n                } else {\n                    diagonal[key] = matrix[row][col];\n                }\n            }\n        }\n        \n        return true;\n    }\n};\n' },
{ name: "JavaScript", lang: "javascript", code: '/**\n * @param {number[][]} matrix\n * @return {boolean}\n */\nvar isToeplitzMatrix = function(matrix) {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    const diagonal = new Map();\n    \n    for (let row = 0; row &lt; m; row++) {\n        for (let col = 0; col &lt; n; col++) {\n            const key = row - col;\n            if (diagonal.has(key)) {\n                if (diagonal.get(key) !== matrix[row][col]) {\n                    return false;\n                }\n            } else {\n                diagonal.set(key, matrix[row][col]);\n            }\n        }\n    }\n    \n    return true;\n};\n' },
{ name: "TypeScript", lang: "typescript", code: 'function isToeplitzMatrix(matrix: number[][]): boolean {\n    const m = matrix.length;\n    const n = matrix[0].length;\n    const diagonal = new Map<number, number>();\n    \n    for (let row = 0; row &lt; m; row++) {\n        for (let col = 0; col &lt; n; col++) {\n            const key = row - col;\n            if (diagonal.has(key)) {\n                if (diagonal.get(key) !== matrix[row][col]) {\n                    return false;\n                }\n            } else {\n                diagonal.set(key, matrix[row][col]);\n            }\n        }\n    }\n    \n    return true;\n}\n' },]} />

**Complexity**
- Time: O(m × n)
- Space: O(m + n)
- Notes: Uses extra space for hash map but provides clear conceptual mapping of diagonals

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Toeplitz Matrix Solutions","datePublished":"2024-10-12","description":"Given a matrix, return true if it is a Toeplitz matrix where every diagonal has the same elements.","keywords":"LeetCode, Toeplitz Matrix, Easy, Array, Matrix"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the optimal solution for Toeplitz Matrix?","acceptedAnswer":{"@type":"Answer","text":"The optimal solution has O(m × n) time complexity where m is the number of rows and n is the number of columns in the matrix."}},{"@type":"Question","name":"What is the space complexity of the compare with top-left neighbor approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use constant extra space for comparison without any additional data structures."}},{"@type":"Question","name":"How does the hash map approach identify elements on the same diagonal?","acceptedAnswer":{"@type":"Answer","text":"Elements on the same diagonal have the same difference (row - col). We use this difference as a key in the hash map to group and verify diagonal elements."}},{"@type":"Question","name":"What is the most efficient approach for checking Toeplitz Matrix?","acceptedAnswer":{"@type":"Answer","text":"The compare with top-left neighbor approach is most efficient with O(m × n) time and O(1) space, requiring only a single pass through the matrix."}}]}).replace(/</g,'\u003c')}
</script>