---
title: "Maximum Unique Subarray Sum After Deletion"
summary: "Find the maximum sum of a subarray with all unique elements after deleting at most one element from the array."
date: "Oct 08, 2024"
tags:
  - Array
  - Hash Table
  - Greedy
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 40.50% | **Paid:** No
> **Topics:** Array, Hash Table, Greedy

You are given an array of integers. You can delete at most one element from the array. After the deletion (if any), find the maximum sum of a subarray where all elements are unique.

Return the maximum possible sum.

## Examples

### Example 1
```text
Input: nums = [4,2,4,5,6]
Output: 17
Explanation: Delete the first 4, then the subarray [2,4,5,6] has all unique elements with sum 17.
```

### Example 2
```text
Input: nums = [5,2,1,2,5,2,1,2,5]
Output: 8
Explanation: The subarray [5,2,1] has all unique elements with sum 8. No deletion needed.
```

### Example 3
```text
Input: nums = [1,2,3,2,1]
Output: 6
Explanation: Delete the first 2, then the subarray [1,3,2,1] has all unique elements with sum 7.
```

## Constraints

```text
1 <= nums.length <= 10⁵
-10⁴ <= nums[i] <= 10⁴
```

- [Brute Force](#brute-force)
- [Sliding Window with Hash Set](#sliding-window-with-hash-set)
- [Sliding Window with Deletion Tracking](#sliding-window-with-deletion-tracking)

## Examples

### Example 1
```text
Input: nums = [4,2,4,5,6]
Output: 17
Explanation: Delete the first 4, then the subarray [2,4,5,6] has all unique elements with sum 17.
```

### Example 2
```text
Input: nums = [5,2,1,2,5,2,1,2,5]
Output: 8
Explanation: The subarray [5,2,1] has all unique elements with sum 8. No deletion needed.
```

### Example 3
```text
Input: nums = [1,2,3,2,1]
Output: 7
Explanation: Delete the first 2, then the subarray [1,3,2,1] has all unique elements with sum 7.
```

## Constraints

```text
1 <= nums.length <= 10⁵
-10⁴ <= nums[i] <= 10⁴
```

---
## Brute Force

**Intuition**
Try deleting each element (or none) and find the maximum sum of a subarray with unique elements for each case.

**Steps**
- Iterate through each index, considering deleting that element (or no deletion)
- For each modified array, use a sliding window to find the maximum sum of a subarray with unique elements
- Track and return the maximum sum across all cases

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maxUniqueSubarraySum(self, nums):\n        n = len(nums)\n        max_sum = float('-inf')\n        \n        # Try deleting each element (or none)\n        for delete_idx in range(-1, n):\n            seen = set()\n            current_sum = 0\n            window_sum = 0\n            \n            for i in range(n):\n                if i == delete_idx:\n                    continue\n                \n                if nums[i] in seen:\n                    # Reset window\n                    seen.clear()\n                    window_sum = 0\n                \n                seen.add(nums[i])\n                window_sum += nums[i]\n                current_sum = max(current_sum, window_sum)\n            \n            max_sum = max(max_sum, current_sum)\n        \n        return max_sum" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxUniqueSubarraySum(int[] nums) {\n        int n = nums.length;\n        int maxSum = Integer.MIN_VALUE;\n        \n        for (int deleteIdx = -1; deleteIdx &lt; n; deleteIdx++) {\n            Set&lt;Integer&gt; seen = new HashSet&lt;&gt;();\n            int currentSum = 0;\n            int windowSum = 0;\n            \n            for (int i = 0; i &lt; n; i++) {\n                if (i == deleteIdx) continue;\n                \n                if (seen.contains(nums[i])) {\n                    seen.clear();\n                    windowSum = 0;\n                }\n                \n                seen.add(nums[i]);\n                windowSum += nums[i];\n                currentSum = Math.max(currentSum, windowSum);\n            }\n            \n            maxSum = Math.max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int maxUniqueSubarraySum(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        int maxSum = INT_MIN;\n        \n        for (int deleteIdx = -1; deleteIdx &lt; n; deleteIdx++) {\n            unordered_set&lt;int&gt; seen;\n            int currentSum = 0;\n            int windowSum = 0;\n            \n            for (int i = 0; i &lt; n; i++) {\n                if (i == deleteIdx) continue;\n                \n                if (seen.count(nums[i])) {\n                    seen.clear();\n                    windowSum = 0;\n                }\n                \n                seen.insert(nums[i]);\n                windowSum += nums[i];\n                currentSum = max(currentSum, windowSum);\n            }\n            \n            maxSum = max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    maxUniqueSubarraySum(nums) {\n        const n = nums.length;\n        let maxSum = -Infinity;\n        \n        for (let deleteIdx = -1; deleteIdx &lt; n; deleteIdx++) {\n            const seen = new Set();\n            let currentSum = 0;\n            let windowSum = 0;\n            \n            for (let i = 0; i &lt; n; i++) {\n                if (i === deleteIdx) continue;\n                \n                if (seen.has(nums[i])) {\n                    seen.clear();\n                    windowSum = 0;\n                }\n                \n                seen.add(nums[i]);\n                windowSum += nums[i];\n                currentSum = Math.max(currentSum, windowSum);\n            }\n            \n            maxSum = Math.max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    maxUniqueSubarraySum(nums: number[]): number {\n        const n = nums.length;\n        let maxSum = -Infinity;\n        \n        for (let deleteIdx = -1; deleteIdx &lt; n; deleteIdx++) {\n            const seen = new Set&lt;number&gt;();\n            let currentSum = 0;\n            let windowSum = 0;\n            \n            for (let i = 0; i &lt; n; i++) {\n                if (i === deleteIdx) continue;\n                \n                if (seen.has(nums[i])) {\n                    seen.clear();\n                    windowSum = 0;\n                }\n                \n                seen.add(nums[i]);\n                windowSum += nums[i];\n                currentSum = Math.max(currentSum, windowSum);\n            }\n            \n            maxSum = Math.max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(n)
- Notes: Simple but inefficient for large inputs

---
## Sliding Window with Hash Set

**Intuition**
Use a sliding window with a hash set to track unique elements. When a duplicate is found, shrink the window from the left.

**Steps**
- Maintain a sliding window with a hash set of unique elements
- Track the current window sum
- When encountering a duplicate, shrink the window until the duplicate is removed
- Update the maximum sum at each step

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maxUniqueSubarraySum(self, nums):\n        seen = set()\n        left = 0\n        max_sum = float('-inf')\n        current_sum = 0\n        \n        for right in range(len(nums)):\n            while nums[right] in seen:\n                seen.remove(nums[left])\n                current_sum -= nums[left]\n                left += 1\n            \n            seen.add(nums[right])\n            current_sum += nums[right]\n            max_sum = max(max_sum, current_sum)\n        \n        return max_sum" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxUniqueSubarraySum(int[] nums) {\n        Set&lt;Integer&gt; seen = new HashSet&lt;&gt;();\n        int left = 0;\n        int maxSum = Integer.MIN_VALUE;\n        int currentSum = 0;\n        \n        for (int right = 0; right &lt; nums.length; right++) {\n            while (seen.contains(nums[right])) {\n                seen.remove(nums[left]);\n                currentSum -= nums[left];\n                left++;\n            }\n            \n            seen.add(nums[right]);\n            currentSum += nums[right];\n            maxSum = Math.max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int maxUniqueSubarraySum(vector&lt;int&gt;& nums) {\n        unordered_set&lt;int&gt; seen;\n        int left = 0;\n        int maxSum = INT_MIN;\n        int currentSum = 0;\n        \n        for (int right = 0; right &lt; nums.size(); right++) {\n            while (seen.count(nums[right])) {\n                seen.erase(nums[left]);\n                currentSum -= nums[left];\n                left++;\n            }\n            \n            seen.insert(nums[right]);\n            currentSum += nums[right];\n            maxSum = max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    maxUniqueSubarraySum(nums) {\n        const seen = new Set();\n        let left = 0;\n        let maxSum = -Infinity;\n        let currentSum = 0;\n        \n        for (let right = 0; right &lt; nums.length; right++) {\n            while (seen.has(nums[right])) {\n                seen.delete(nums[left]);\n                currentSum -= nums[left];\n                left++;\n            }\n            \n            seen.add(nums[right]);\n            currentSum += nums[right];\n            maxSum = Math.max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    maxUniqueSubarraySum(nums: number[]): number {\n        const seen = new Set&lt;number&gt;();\n        let left = 0;\n        let maxSum = -Infinity;\n        let currentSum = 0;\n        \n        for (let right = 0; right &lt; nums.length; right++) {\n            while (seen.has(nums[right])) {\n                seen.delete(nums[left]);\n                currentSum -= nums[left];\n                left++;\n            }\n            \n            seen.add(nums[right]);\n            currentSum += nums[right];\n            maxSum = Math.max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Efficient but doesn't account for the deletion option

---
## Sliding Window with Deletion Tracking

**Intuition**
Extend the sliding window approach to handle one deletion. Track if we've used our deletion and handle duplicates accordingly.

**Steps**
- Use a hash map to track the count of each element in the window
- Maintain a flag for whether we've used our deletion
- When encountering a duplicate:
  - If deletion not used, skip the duplicate element
  - If deletion used, shrink window from the left
- Track and update the maximum sum

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maxUniqueSubarraySum(self, nums):\n        from collections import defaultdict\n        \n        count = defaultdict(int)\n        left = 0\n        max_sum = float('-inf')\n        current_sum = 0\n        deleted = False\n        \n        for right in range(len(nums)):\n            if count[nums[right]] &gt; 0:\n                if not deleted:\n                    deleted = True\n                else:\n                    while count[nums[right]] &gt; 0:\n                        count[nums[left]] -= 1\n                        current_sum -= nums[left]\n                        left += 1\n            \n            count[nums[right]] += 1\n            current_sum += nums[right]\n            max_sum = max(max_sum, current_sum)\n        \n        return max_sum" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxUniqueSubarraySum(int[] nums) {\n        Map&lt;Integer, Integer&gt; count = new HashMap&lt;&gt;();\n        int left = 0;\n        int maxSum = Integer.MIN_VALUE;\n        int currentSum = 0;\n        boolean deleted = false;\n        \n        for (int right = 0; right &lt; nums.length; right++) {\n            if (count.getOrDefault(nums[right], 0) &gt; 0) {\n                if (!deleted) {\n                    deleted = true;\n                } else {\n                    while (count.getOrDefault(nums[right], 0) &gt; 0) {\n                        count.put(nums[left], count.get(nums[left]) - 1);\n                        currentSum -= nums[left];\n                        left++;\n                    }\n                }\n            }\n            \n            count.put(nums[right], count.getOrDefault(nums[right], 0) + 1);\n            currentSum += nums[right];\n            maxSum = Math.max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int maxUniqueSubarraySum(vector&lt;int&gt;& nums) {\n        unordered_map&lt;int, int&gt; count;\n        int left = 0;\n        int maxSum = INT_MIN;\n        int currentSum = 0;\n        bool deleted = false;\n        \n        for (int right = 0; right &lt; nums.size(); right++) {\n            if (count[nums[right]] &gt; 0) {\n                if (!deleted) {\n                    deleted = true;\n                } else {\n                    while (count[nums[right]] &gt; 0) {\n                        count[nums[left]]--;\n                        currentSum -= nums[left];\n                        left++;\n                    }\n                }\n            }\n            \n            count[nums[right]]++;\n            currentSum += nums[right];\n            maxSum = max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    maxUniqueSubarraySum(nums) {\n        const count = new Map();\n        let left = 0;\n        let maxSum = -Infinity;\n        let currentSum = 0;\n        let deleted = false;\n        \n        for (let right = 0; right &lt; nums.length; right++) {\n            if ((count.get(nums[right]) || 0) &gt; 0) {\n                if (!deleted) {\n                    deleted = true;\n                } else {\n                    while ((count.get(nums[right]) || 0) &gt; 0) {\n                        count.set(nums[left], count.get(nums[left]) - 1);\n                        currentSum -= nums[left];\n                        left++;\n                    }\n                }\n            }\n            \n            count.set(nums[right], (count.get(nums[right]) || 0) + 1);\n            currentSum += nums[right];\n            maxSum = Math.max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    maxUniqueSubarraySum(nums: number[]): number {\n        const count = new Map&lt;number, number&gt;();\n        let left = 0;\n        let maxSum = -Infinity;\n        let currentSum = 0;\n        let deleted = false;\n        \n        for (let right = 0; right &lt; nums.length; right++) {\n            if ((count.get(nums[right]) || 0) &gt; 0) {\n                if (!deleted) {\n                    deleted = true;\n                } else {\n                    while ((count.get(nums[right]) || 0) &gt; 0) {\n                        count.set(nums[left], count.get(nums[left])! - 1);\n                        currentSum -= nums[left];\n                        left++;\n                    }\n                }\n            }\n            \n            count.set(nums[right], (count.get(nums[right]) || 0) + 1);\n            currentSum += nums[right];\n            maxSum = Math.max(maxSum, currentSum);\n        }\n        \n        return maxSum;\n    }\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Optimal solution that handles the deletion option efficiently

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Maximum Unique Subarray Sum After Deletion Solutions","datePublished":"2024-10-08","description":"Find the maximum sum of a subarray with all unique elements after deleting at most one element from the array.","keywords":"LeetCode, Maximum Unique Subarray Sum After Deletion, Easy, Array, Hash Table, Greedy"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach has a time complexity of O(n²) as we try deleting each element and run a sliding window for each case."}},{"@type":"Question","name":"What is the time complexity of the Sliding Window with Deletion Tracking approach?","acceptedAnswer":{"@type":"Answer","text":"The Sliding Window with Deletion Tracking approach has a time complexity of O(n) as each element is processed at most twice."}},{"@type":"Question","name":"What data structures are used in the optimal solution?","acceptedAnswer":{"@type":"Answer","text":"The optimal solution uses a hash map to track element counts and a sliding window with two pointers."}}]}).replace(/</g,'\u003c')}
</script>