---
title: "Count Subarrays of Length Three With a Condition"
summary: "Count subarrays of length 3 where the middle element is strictly greater than its neighbors."
date: "Feb 19, 2024"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 61.40% | **Paid:** No
> **Topics:** Array

Given a 0-indexed integer array nums, return the number of subarrays of length 3 where the middle element is strictly greater than the other two elements.

Formally, count the number of indices i such that:
- 0 &lt; i &lt; nums.length - 1
- nums[i] &gt; nums[i - 1]
- nums[i] &gt; nums[i + 1]

## Table of Contents
- Examples
- Constraints
- [Linear Scan](#linear-scan)
- [Sliding Window](#sliding-window)
- [Functional Approach](#functional-approach)

## Examples

### Example 1

**Input:**
```text
nums = [1,2,1,4,1]
```

**Output:**
```text
1
```

**Explanation:**
Only the subarray [1,4,1] contains exactly 3 elements where the sum of the first and third numbers equals half the middle number.

### Example 2

**Input:**
```text
nums = [1,1,1]
```

**Output:**
```text
0
```

**Explanation:**
[1,1,1] is the only subarray of length 3. However, its first and third numbers do not add to half the middle number.

## Constraints

```text
3 <= nums.length <= 100
1 <= nums[i] <= 100
```

---

## Linear Scan

**Intuition**
We iterate through the array and check every element that could be the middle of a subarray of length 3. This corresponds to indices 1 through n-2.

**Steps**
- Initialize a counter to 0.
- Iterate through the array starting from index 1 to the second to last index (length - 2).
- For each index i, check if nums[i] is strictly greater than nums[i-1] and nums[i+1].
- If the condition is met, increment the counter.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countSubarrays(self, nums: List[int]) -&gt; int:\n        count = 0\n        n = len(nums)\n        for i in range(1, n - 1):\n            if nums[i] &gt; nums[i - 1] and nums[i] &gt; nums[i + 1]:\n                count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int countSubarrays(int[] nums) {\n        int count = 0;\n        int n = nums.length;\n        for (int i = 1; i &lt; n - 1; i++) {\n            if (nums[i] &gt; nums[i - 1] && nums[i] &gt; nums[i + 1]) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countSubarrays(vector&lt;int&gt;& nums) {\n        int count = 0;\n        int n = nums.size();\n        for (int i = 1; i &lt; n - 1; ++i) {\n            if (nums[i] &gt; nums[i - 1] && nums[i] &gt; nums[i + 1]) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var countSubarrays = function(nums) {\n    let count = 0;\n    const n = nums.length;\n    for (let i = 1; i &lt; n - 1; i++) {\n        if (nums[i] &gt; nums[i - 1] && nums[i] &gt; nums[i + 1]) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countSubarrays(nums: number[]): number {\n    let count = 0;\n    const n = nums.length;\n    for (let i = 1; i &lt; n - 1; i++) {\n        if (nums[i] &gt; nums[i - 1] && nums[i] &gt; nums[i + 1]) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Most efficient approach for this problem.

---

## Sliding Window

**Intuition**
We can visualize a window of size 3 sliding across the array. At each step, we check the middle element of the current window.

**Steps**
- Initialize a counter to 0.
- Iterate from index 0 to n - 3 (inclusive).
- For each starting index i, the window covers indices i, i+1, i+2.
- Check if nums[i+1] (the middle) is greater than nums[i] and nums[i+2].
- Increment counter if true.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countSubarrays(self, nums: List[int]) -&gt; int:\n        count = 0\n        n = len(nums)\n        for i in range(n - 2):\n            if nums[i + 1] &gt; nums[i] and nums[i + 1] &gt; nums[i + 2]:\n                count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int countSubarrays(int[] nums) {\n        int count = 0;\n        int n = nums.length;\n        for (int i = 0; i &lt; n - 2; i++) {\n            if (nums[i + 1] &gt; nums[i] && nums[i + 1] &gt; nums[i + 2]) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countSubarrays(vector&lt;int&gt;& nums) {\n        int count = 0;\n        int n = nums.size();\n        for (int i = 0; i &lt; n - 2; ++i) {\n            if (nums[i + 1] &gt; nums[i] && nums[i + 1] &gt; nums[i + 2]) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var countSubarrays = function(nums) {\n    let count = 0;\n    const n = nums.length;\n    for (let i = 0; i &lt; n - 2; i++) {\n        if (nums[i + 1] &gt; nums[i] && nums[i + 1] &gt; nums[i + 2]) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countSubarrays(nums: number[]): number {\n    let count = 0;\n    const n = nums.length;\n    for (let i = 0; i &lt; n - 2; i++) {\n        if (nums[i + 1] &gt; nums[i] && nums[i + 1] &gt; nums[i + 2]) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Conceptually similar to Linear Scan, but focuses on the window boundaries.

---

## Functional Approach

**Intuition**
We use functional programming constructs like filter, reduce, or stream operations to count the valid indices without explicit loop management.

**Steps**
- Generate a range of valid middle indices (1 to n-2).
- Filter this range based on the peak condition.
- Count the number of remaining indices.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countSubarrays(self, nums: List[int]) -&gt; int:\n        return sum(1 for i in range(1, len(nums) - 1) if nums[i] &gt; nums[i - 1] and nums[i] &gt; nums[i + 1])" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\n\nclass Solution {\n    public int countSubarrays(int[] nums) {\n        return (int) IntStream.range(1, nums.length - 1)\n            .filter(i -&gt; nums[i] &gt; nums[i - 1] && nums[i] &gt; nums[i + 1])\n            .count();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countSubarrays(vector&lt;int&gt;& nums) {\n        int count = 0;\n        int n = nums.size();\n        for (int i = 1; i &lt; n - 1; ++i) {\n            if (nums[i] &gt; nums[i - 1] && nums[i] &gt; nums[i + 1]) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var countSubarrays = function(nums) {\n    return nums.reduce((acc, _, i) =&gt; {\n        if (i &gt; 0 && i &lt; nums.length - 1 && nums[i] &gt; nums[i - 1] && nums[i] &gt; nums[i + 1]) {\n            return acc + 1;\n        }\n        return acc;\n    }, 0);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countSubarrays(nums: number[]): number {\n    return nums.reduce((acc, _, i) =&gt; {\n        if (i &gt; 0 && i &lt; nums.length - 1 && nums[i] &gt; nums[i - 1] && nums[i] &gt; nums[i + 1]) {\n            return acc + 1;\n        }\n        return acc;\n    }, 0);\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Concise syntax, often preferred in modern codebases for simple logic.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Count Subarrays of Length Three With a Condition Solutions","datePublished":"2024-02-19","description":"Count subarrays of length 3 where the middle element is strictly greater than its neighbors.","keywords":"LeetCode, Count Subarrays of Length Three With a Condition, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the array. We iterate through the array once."}},{"@type":"Question","name":"What is the space complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for the counter."}},{"@type":"Question","name":"Why do we iterate from index 1 to n-2?","acceptedAnswer":{"@type":"Answer","text":"We are looking for subarrays of length 3. The middle element of such a subarray cannot be the first or last element of the array, so valid middle indices range from 1 to n-2."}}]}).replace(/</g,'\u003c')}
</script>