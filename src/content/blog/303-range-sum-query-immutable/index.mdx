---
title: "Range Sum Query - Immutable"
summary: "Design a class to efficiently calculate the sum of elements between two indices in an immutable array."
date: "Feb 02, 2024"
tags:
  - Array
  - Design
  - Prefix Sum
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 72.10% | **Paid:** No
> **Topics:** Array, Design, Prefix Sum

Given an integer array nums, handle multiple queries of the following type:

Calculate the sum of the elements of nums between indices left and right inclusive where left &lt;= right.

Implement the NumArray class:

NumArray(int[] nums) Initializes the object with the integer array nums.
int sumRange(int left, int right) Returns the sum of the elements of nums between indices left and right inclusive (i.e. nums[left] + nums[left + 1] + ... + nums[right]).

- Examples
- Constraints
- [Approach 1: Brute Force](#brute-force)
- [Approach 2: Prefix Sum](#prefix-sum)

## Examples

### Example 1

**Input:**
```text
["NumArray", "sumRange", "sumRange", "sumRange"]
[[[-2, 0, 3, -5, 2, -1]], [0, 2], [2, 5], [0, 5]]
```

**Output:**
```text
[null, 1, -1, -3]
```

**Explanation:**
```text
NumArray numArray = new NumArray([-2, 0, 3, -5, 2, -1]);
numArray.sumRange(0, 2); // return (-2) + 0 + 3 = 1
numArray.sumRange(2, 5); // return 3 + (-5) + 2 + (-1) = -1
numArray.sumRange(0, 5); // return (-2) + 0 + 3 + (-5) + 2 + (-1) = -3
```

## Constraints

```text
- 1 <= nums.length <= 10^4
- -10^5 <= nums[i] <= 10^5
- 0 <= left <= right < nums.length
- At most 10^4 calls will be made to sumRange.
```

---
## Brute Force

**Intuition**
For each query, iterate through the array from index left to right and sum all elements.

**Steps**
- Store the original array in the constructor.
- For sumRange, iterate from left to right and accumulate the sum.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class NumArray:
    def __init__(self, nums):
        self.nums = nums

    def sumRange(self, left, right):
        total = 0
        for i in range(left, right + 1):
            total += self.nums[i]
        return total
` },
{ name: "Java", lang: "java", code: `class NumArray {
    private int[] nums;

    public NumArray(int[] nums) {
        this.nums = nums;
    }

    public int sumRange(int left, int right) {
        int sum = 0;
        for (int i = left; i &lt;= right; i++) {
            sum += nums[i];
        }
        return sum;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class NumArray {
private:
    vector<int> nums;

public:
    NumArray(vector<int>& nums) {
        this->nums = nums;
    }

    int sumRange(int left, int right) {
        int sum = 0;
        for (int i = left; i &lt;= right; i++) {
            sum += nums[i];
        }
        return sum;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class NumArray {
    constructor(nums) {
        this.nums = nums;
    }

    sumRange(left, right) {
        let sum = 0;
        for (let i = left; i &lt;= right; i++) {
            sum += this.nums[i];
        }
        return sum;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class NumArray {
    private nums: number[];

    constructor(nums: number[]) {
        this.nums = nums;
    }

    sumRange(left: number, right: number): number {
        let sum = 0;
        for (let i = left; i &lt;= right; i++) {
            sum += this.nums[i];
        }
        return sum;
    }
}
` },]} />

**Complexity**
- Time: O(n) per query, where n is the length of the range
- Space: O(1) additional space
- Notes: Simple but inefficient for multiple queries

---
## Prefix Sum

**Intuition**
Precompute cumulative sums where prefix[i] stores the sum of elements from index 0 to i-1. Then sumRange(left, right) = prefix[right+1] - prefix[left].

**Steps**
- Build a prefix sum array where prefix[0] = 0 and prefix[i] = prefix[i-1] + nums[i-1].
- For sumRange, return prefix[right+1] - prefix[left].

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class NumArray:
    def __init__(self, nums):
        self.prefix = [0] * (len(nums) + 1)
        for i in range(len(nums)):
            self.prefix[i + 1] = self.prefix[i] + nums[i]

    def sumRange(self, left, right):
        return self.prefix[right + 1] - self.prefix[left]
` },
{ name: "Java", lang: "java", code: `class NumArray {
    private int[] prefix;

    public NumArray(int[] nums) {
        prefix = new int[nums.length + 1];
        for (int i = 0; i &lt; nums.length; i++) {
            prefix[i + 1] = prefix[i] + nums[i];
        }
    }

    public int sumRange(int left, int right) {
        return prefix[right + 1] - prefix[left];
    }
}
` },
{ name: "C++", lang: "cpp", code: `class NumArray {
private:
    vector<int> prefix;

public:
    NumArray(vector<int>& nums) {
        prefix.resize(nums.size() + 1);
        for (int i = 0; i &lt; nums.size(); i++) {
            prefix[i + 1] = prefix[i] + nums[i];
        }
    }

    int sumRange(int left, int right) {
        return prefix[right + 1] - prefix[left];
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class NumArray {
    constructor(nums) {
        this.prefix = new Array(nums.length + 1).fill(0);
        for (let i = 0; i &lt; nums.length; i++) {
            this.prefix[i + 1] = this.prefix[i] + nums[i];
        }
    }

    sumRange(left, right) {
        return this.prefix[right + 1] - this.prefix[left];
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class NumArray {
    private prefix: number[];

    constructor(nums: number[]) {
        this.prefix = new Array(nums.length + 1).fill(0);
        for (let i = 0; i &lt; nums.length; i++) {
            this.prefix[i + 1] = this.prefix[i] + nums[i];
        }
    }

    sumRange(left: number, right: number): number {
        return this.prefix[right + 1] - this.prefix[left];
    }
}
` },]} />

**Complexity**
- Time: O(1) per query, O(n) preprocessing
- Space: O(n) for the prefix array
- Notes: Optimal solution for multiple queries on immutable array

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Range Sum Query - Immutable Solutions","datePublished":"2024-02-02","description":"Design a class to efficiently calculate the sum of elements between two indices in an immutable array.","keywords":"LeetCode, Range Sum Query - Immutable, Easy, Array, Design, Prefix Sum"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) per query where n is the length of the range."}},{"@type":"Question","name":"What is the time complexity of the Prefix Sum approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) per query with O(n) preprocessing time."}},{"@type":"Question","name":"What is the space complexity of the Prefix Sum approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for storing the prefix sum array."}}]}).replace(/</g,'\u003c')}
</script>