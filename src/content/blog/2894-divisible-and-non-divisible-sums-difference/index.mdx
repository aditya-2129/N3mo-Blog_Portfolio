---
title: "Divisible and Non-divisible Sums Difference"
summary: "Calculate the difference between the sum of numbers divisible by m and the sum of numbers not divisible by m in the range [1, n]."
date: "Jul 31, 2025"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 91.00% | **Paid:** No
> **Topics:** Math

You are given two positive integers n and m.

Create two integer arrays nums1 and nums2, where:

- nums1[i] is divisible by m if i is divisible by m, and nums1[i] is not divisible by m if i is not divisible by m.
- nums2[i] is divisible by m if i is not divisible by m, and nums2[i] is not divisible by m if i is divisible by m.

Return the absolute difference between the sum of nums1 and the sum of nums2.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Mathematical Formula](#mathematical-formula)

## Examples

**Example 1:**

```text
Input: n = 10, m = 3
Output: 19
Explanation:
nums1 = [0, 0, 0, 3, 0, 0, 6, 0, 0, 9, 0]
nums2 = [0, 1, 2, 0, 4, 5, 0, 7, 8, 0, 10]
The sum of nums1 is 18 and the sum of nums2 is 37.
The absolute difference is |18 - 37| = 19.
```

**Example 2:**

```text
Input: n = 5, m = 6
Output: 15
Explanation:
nums1 = [0, 0, 0, 0, 0, 0]
nums2 = [0, 1, 2, 3, 4, 5]
The sum of nums1 is 0 and the sum of nums2 is 15.
The absolute difference is |0 - 15| = 15.
```

**Example 3:**

```text
Input: n = 5, m = 1
Output: 15
Explanation:
nums1 = [0, 1, 2, 3, 4, 5]
nums2 = [0, 0, 0, 0, 0, 0]
The sum of nums1 is 15 and the sum of nums2 is 0.
The absolute difference is |15 - 0| = 15.
```

## Constraints

```text
1 <= n <= 10⁹
1 <= m <= 10⁹
```

---
## Brute Force

**Intuition**
Iterate through all numbers from 1 to n, check divisibility by m, and accumulate sums separately for divisible and non-divisible numbers.

**Steps**
- Initialize sum1 and sum2 to 0
- Loop from 1 to n
- If current number is divisible by m, add it to sum1, otherwise add to sum2
- Return the absolute difference between sum1 and sum2

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def differenceOfSums(self, n: int, m: int) -> int:
        sum1 = 0
        sum2 = 0
        for i in range(1, n + 1):
            if i % m == 0:
                sum1 += i
            else:
                sum2 += i
        return abs(sum1 - sum2)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int differenceOfSums(int n, int m) {
        int sum1 = 0;
        int sum2 = 0;
        for (int i = 1; i &lt;= n; i++) {
            if (i % m == 0) {
                sum1 += i;
            } else {
                sum2 += i;
            }
        }
        return Math.abs(sum1 - sum2);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int differenceOfSums(int n, int m) {
        long long sum1 = 0;
        long long sum2 = 0;
        for (int i = 1; i &lt;= n; i++) {
            if (i % m == 0) {
                sum1 += i;
            } else {
                sum2 += i;
            }
        }
        return abs(sum1 - sum2);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @param {number} m
 * @return {number}
 */
var differenceOfSums = function(n, m) {
    let sum1 = 0;
    let sum2 = 0;
    for (let i = 1; i &lt;= n; i++) {
        if (i % m === 0) {
            sum1 += i;
        } else {
            sum2 += i;
        }
    }
    return Math.abs(sum1 - sum2);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function differenceOfSums(n: number, m: number): number {
    let sum1 = 0;
    let sum2 = 0;
    for (let i = 1; i &lt;= n; i++) {
        if (i % m === 0) {
            sum1 += i;
        } else {
            sum2 += i;
        }
    }
    return Math.abs(sum1 - sum2);
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Simple but inefficient for large n values up to 10⁹

---
## Mathematical Formula

**Intuition**
Use arithmetic series formulas to calculate sums directly without iteration. The sum of numbers divisible by m forms an arithmetic progression, and the total sum from 1 to n is also a known formula.

**Steps**
- Calculate k = n / m (number of multiples of m in [1, n])
- Sum of divisible numbers = m * k * (k + 1) / 2
- Total sum from 1 to n = n * (n + 1) / 2
- Sum of non-divisible numbers = Total sum - Sum of divisible numbers
- Return absolute difference

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def differenceOfSums(self, n: int, m: int) -> int:
        k = n // m
        sum_divisible = m * k * (k + 1) // 2
        total_sum = n * (n + 1) // 2
        sum_non_divisible = total_sum - sum_divisible
        return abs(sum_divisible - sum_non_divisible)
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int differenceOfSums(int n, int m) {
        long k = n / m;
        long sumDivisible = m * k * (k + 1) / 2;
        long totalSum = (long) n * (n + 1) / 2;
        long sumNonDivisible = totalSum - sumDivisible;
        return (int) Math.abs(sumDivisible - sumNonDivisible);
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int differenceOfSums(int n, int m) {
        long long k = n / m;
        long long sumDivisible = (long long)m * k * (k + 1) / 2;
        long long totalSum = (long long)n * (n + 1) / 2;
        long long sumNonDivisible = totalSum - sumDivisible;
        return abs(sumDivisible - sumNonDivisible);
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number} n
 * @param {number} m
 * @return {number}
 */
var differenceOfSums = function(n, m) {
    const k = Math.floor(n / m);
    const sumDivisible = m * k * (k + 1) / 2;
    const totalSum = n * (n + 1) / 2;
    const sumNonDivisible = totalSum - sumDivisible;
    return Math.abs(sumDivisible - sumNonDivisible);
};
` },
{ name: "TypeScript", lang: "typescript", code: `function differenceOfSums(n: number, m: number): number {
    const k = Math.floor(n / m);
    const sumDivisible = m * k * (k + 1) / 2;
    const totalSum = n * (n + 1) / 2;
    const sumNonDivisible = totalSum - sumDivisible;
    return Math.abs(sumDivisible - sumNonDivisible);
}
` },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Optimal solution using mathematical formulas, handles large values efficiently

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Divisible and Non-divisible Sums Difference Solutions","datePublished":"2025-07-31","description":"Calculate the difference between the sum of numbers divisible by m and the sum of numbers not divisible by m in the range [1, n].","keywords":"LeetCode, Divisible and Non-divisible Sums Difference, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the brute force approach?","acceptedAnswer":{"@type":"Answer","text":"The brute force approach has a time complexity of O(n) as it iterates through all numbers from 1 to n."}},{"@type":"Question","name":"What is the time complexity of the mathematical formula approach?","acceptedAnswer":{"@type":"Answer","text":"The mathematical formula approach has a time complexity of O(1) as it uses arithmetic series formulas to calculate the result directly."}},{"@type":"Question","name":"Why do we need to use long long in C++ and long in Java?","acceptedAnswer":{"@type":"Answer","text":"Since n and m can be up to 10^9, the intermediate calculations like n * (n + 1) can exceed the range of 32-bit integers, requiring 64-bit integer types."}}]}).replace(/</g,'\u003c')}
</script>