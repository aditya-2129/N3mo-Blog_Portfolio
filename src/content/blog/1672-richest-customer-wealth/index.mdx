---
title: "Richest Customer Wealth"
summary: "You are given an array accounts where accounts[i][j] is the money the i-th customer has in the j-th bank. Return the wealth that the richest customer has."
date: "Jul 04, 2025"
tags:
  - Array
  - Matrix
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 88.70% | **Paid:** No
> **Topics:** Array, Matrix

You are given an integer array accounts where accounts[i][j] is the amount of money the i​​​​​​​​​​​th​​​​ customer has in the j​​​​​​​​​​​th​​​​ bank. Return the wealth that the richest customer has.

A customer's wealth is the amount of money they have in all their bank accounts. The richest customer is the customer that has the maximum wealth.

- Examples
- Constraints
- [Iterative Row Sum](#iterative-row-sum)
- [Functional Programming](#functional-programming)

## Examples

**Example 1:**

```text
Input: accounts = [[1,2,3],[3,2,1]]
Output: 6
Explanation:
1st customer has wealth = 1 + 2 + 3 = 6
2nd customer has wealth = 3 + 2 + 1 = 6
Both customers are considered the richest with a wealth of 6 each, so return 6.
```

**Example 2:**

```text
Input: accounts = [[1,5],[7,3],[3,5]]
Output: 10
Explanation: 
1st customer has wealth = 6
2nd customer has wealth = 10 
3rd customer has wealth = 8
The 2nd customer is the richest with a wealth of 10.
```

**Example 3:**

```text
Input: accounts = [[2,8,7],[7,1,3],[1,9,5]]
Output: 17
```

## Constraints

```text
m == accounts.length
n == accounts[i].length
1 <= m, n <= 50
1 <= accounts[i][j] <= 100
```

---

## Iterative Row Sum

**Intuition**
We iterate through each customer (row) and sum up their money across all banks (columns). We keep track of the maximum sum found so far.

**Steps**
- Initialize `max_wealth` to 0.
- Loop through each customer in the `accounts` array.
- For each customer, calculate the sum of their bank accounts.
- If the current customer's wealth is greater than `max_wealth`, update `max_wealth`.
- Return `max_wealth`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maximumWealth(self, accounts: List[List[int]]) -&gt; int:\n        max_wealth = 0\n        for customer in accounts:\n            current_wealth = sum(customer)\n            if current_wealth &gt; max_wealth:\n                max_wealth = current_wealth\n        return max_wealth" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maximumWealth(int[][] accounts) {\n        int maxWealth = 0;\n        for (int[] customer : accounts) {\n            int currentWealth = 0;\n            for (int bank : customer) {\n                currentWealth += bank;\n            }\n            if (currentWealth &gt; maxWealth) {\n                maxWealth = currentWealth;\n            }\n        }\n        return maxWealth;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int maximumWealth(vector&lt;vector&lt;int&gt;&gt;& accounts) {\n        int maxWealth = 0;\n        for (const auto& customer : accounts) {\n            int currentWealth = 0;\n            for (int bank : customer) {\n                currentWealth += bank;\n            }\n            maxWealth = max(maxWealth, currentWealth);\n        }\n        return maxWealth;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var maximumWealth = function(accounts) {\n    let maxWealth = 0;\n    for (let i = 0; i &lt; accounts.length; i++) {\n        let currentWealth = 0;\n        for (let j = 0; j &lt; accounts[i].length; j++) {\n            currentWealth += accounts[i][j];\n        }\n        if (currentWealth &gt; maxWealth) {\n            maxWealth = currentWealth;\n        }\n    }\n    return maxWealth;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function maximumWealth(accounts: number[][]): number {\n    let maxWealth = 0;\n    for (let i = 0; i &lt; accounts.length; i++) {\n        let currentWealth = 0;\n        for (let j = 0; j &lt; accounts[i].length; j++) {\n            currentWealth += accounts[i][j];\n        }\n        if (currentWealth &gt; maxWealth) {\n            maxWealth = currentWealth;\n        }\n    }\n    return maxWealth;\n};" },]} />

**Complexity**
- Time: O(m·n)
- Space: O(1)
- Notes: We only use a few variables to store the current and maximum wealth.

---

## Functional Programming

**Intuition**
Utilize built-in array methods to map each customer to their total wealth and then reduce the array to find the maximum value.

**Steps**
- Map each row (customer) to the sum of its elements.
- Find the maximum value in the resulting array of sums.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maximumWealth(self, accounts: List[List[int]]) -&gt; int:\n        return max(sum(customer) for customer in accounts)" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int maximumWealth(int[][] accounts) {\n        return Arrays.stream(accounts)\n                     .mapToInt(customer -&gt; Arrays.stream(customer).sum())\n                     .max()\n                     .getAsInt();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;numeric&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int maximumWealth(vector&lt;vector&lt;int&gt;&gt;& accounts) {\n        int maxWealth = 0;\n        for (const auto& customer : accounts) {\n            int currentWealth = std::accumulate(customer.begin(), customer.end(), 0);\n            maxWealth = std::max(maxWealth, currentWealth);\n        }\n        return maxWealth;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var maximumWealth = function(accounts) {\n    return Math.max(...accounts.map(customer =&gt; customer.reduce((a, b) =&gt; a + b, 0)));\n};" },
{ name: "TypeScript", lang: "typescript", code: "function maximumWealth(accounts: number[][]): number {\n    return Math.max(...accounts.map(customer =&gt; customer.reduce((a, b) =&gt; a + b, 0)));\n};" },]} />

**Complexity**
- Time: O(m·n)
- Space: O(m) or O(1)
- Notes: In languages like JavaScript, `map` creates a new array, resulting in O(m) space. In Python, using a generator expression keeps it at O(1).

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Richest Customer Wealth Solutions","datePublished":"2025-07-04","description":"You are given an array accounts where accounts[i][j] is the money the i-th customer has in the j-th bank. Return the wealth that the richest customer has.","keywords":"LeetCode, Richest Customer Wealth, Easy, Array, Matrix"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Row Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(m·n), where m is the number of customers and n is the number of banks, as we must visit every element once."}},{"@type":"Question","name":"What is the space complexity of the Iterative Row Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use a few variables to store the current sum and the maximum wealth found."}},{"@type":"Question","name":"Can we solve this using built-in functions?","acceptedAnswer":{"@type":"Answer","text":"Yes, we can use built-in functions like map, reduce, or sum to calculate the wealth of each customer and then find the maximum value."}}]}).replace(/</g,'\u003c')}
</script>