---
title: "Special Positions in a Binary Matrix"
summary: "Count positions in a binary matrix where the element is 1 and all other elements in its row and column are 0."
date: "Jul 29, 2024"
tags:
  - Array
  - Matrix
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 72.70% | **Paid:** No
> **Topics:** Array, Matrix

Given an m x n binary matrix mat, return the number of special positions in mat.

A position (i, j) is called special if mat[i][j] == 1 and all other elements in row i and column j are 0 (in other words, mat[i][j] is the only element that is 1 in its row and column).

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Precompute Sums](#precompute-sums)

## Examples

```text
Input: mat = [[1,0,0],[0,0,1],[1,0,0]]
Output: 1
Explanation: (1, 2) is a special position because mat[1][2] == 1 and all other elements in row 1 and column 2 are 0.
```

```text
Input: mat = [[1,0,0],[0,1,0],[0,0,1]]
Output: 3
Explanation: (0, 0), (1, 1), (2, 2) are special positions.
```

## Constraints

```text
m == mat.length
n == mat[i].length
1 <= m, n <= 100
mat[i][j] is either 0 or 1.
```

---
## Brute Force

**Intuition**
For each cell containing 1, verify that all other elements in its row and column are 0 by scanning through them.

**Steps**
- Iterate through each cell in the matrix
- When finding a 1, scan the entire row to check if it's the only 1
- If valid, scan the entire column to check if it's the only 1
- Increment count if both checks pass

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def numSpecial(self, mat: List[List[int]]) -&gt; int:\n        m, n = len(mat), len(mat[0])\n        count = 0\n        \n        for i in range(m):\n            for j in range(n):\n                if mat[i][j] == 1:\n                    valid = True\n                    for k in range(n):\n                        if k != j and mat[i][k] != 0:\n                            valid = False\n                            break\n                    if not valid:\n                        continue\n                    for k in range(m):\n                        if k != i and mat[k][j] != 0:\n                            valid = False\n                            break\n                    if valid:\n                        count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numSpecial(int[][] mat) {\n        int m = mat.length;\n        int n = mat[0].length;\n        int count = 0;\n        \n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (mat[i][j] == 1) {\n                    boolean valid = true;\n                    for (int k = 0; k &lt; n; k++) {\n                        if (k != j && mat[i][k] != 0) {\n                            valid = false;\n                            break;\n                        }\n                    }\n                    if (!valid) continue;\n                    for (int k = 0; k &lt; m; k++) {\n                        if (k != i && mat[k][j] != 0) {\n                            valid = false;\n                            break;\n                        }\n                    }\n                    if (valid) count++;\n                }\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numSpecial(vector&lt;vector&lt;int&gt;&gt;& mat) {\n        int m = mat.size();\n        int n = mat[0].size();\n        int count = 0;\n        \n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (mat[i][j] == 1) {\n                    bool valid = true;\n                    for (int k = 0; k &lt; n; k++) {\n                        if (k != j && mat[i][k] != 0) {\n                            valid = false;\n                            break;\n                        }\n                    }\n                    if (!valid) continue;\n                    for (int k = 0; k &lt; m; k++) {\n                        if (k != i && mat[k][j] != 0) {\n                            valid = false;\n                            break;\n                        }\n                    }\n                    if (valid) count++;\n                }\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[][]} mat\n * @return {number}\n */\nvar numSpecial = function(mat) {\n    const m = mat.length;\n    const n = mat[0].length;\n    let count = 0;\n    \n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (mat[i][j] === 1) {\n                let valid = true;\n                for (let k = 0; k &lt; n; k++) {\n                    if (k !== j && mat[i][k] !== 0) {\n                        valid = false;\n                        break;\n                    }\n                }\n                if (!valid) continue;\n                for (let k = 0; k &lt; m; k++) {\n                    if (k !== i && mat[k][j] !== 0) {\n                        valid = false;\n                        break;\n                    }\n                }\n                if (valid) count++;\n            }\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numSpecial(mat: number[][]): number {\n    const m = mat.length;\n    const n = mat[0].length;\n    let count = 0;\n    \n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (mat[i][j] === 1) {\n                let valid = true;\n                for (let k = 0; k &lt; n; k++) {\n                    if (k !== j && mat[i][k] !== 0) {\n                        valid = false;\n                        break;\n                    }\n                }\n                if (!valid) continue;\n                for (let k = 0; k &lt; m; k++) {\n                    if (k !== i && mat[k][j] !== 0) {\n                        valid = false;\n                        break;\n                    }\n                }\n                if (valid) count++;\n            }\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(m × n × (m + n))
- Space: O(1)
- Notes: Simple but inefficient for larger matrices due to repeated scanning

---
## Precompute Sums

**Intuition**
Precompute the sum of each row and column once, then a position is special if its value is 1 and both its row sum and column sum equal 1.

**Steps**
- Compute sum of all elements in each row
- Compute sum of all elements in each column
- Iterate through matrix and count positions where value is 1, row sum is 1, and column sum is 1

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def numSpecial(self, mat: List[List[int]]) -&gt; int:\n        m, n = len(mat), len(mat[0])\n        row_sum = [sum(row) for row in mat]\n        col_sum = [sum(mat[i][j] for i in range(m)) for j in range(n)]\n        \n        count = 0\n        for i in range(m):\n            for j in range(n):\n                if mat[i][j] == 1 and row_sum[i] == 1 and col_sum[j] == 1:\n                    count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numSpecial(int[][] mat) {\n        int m = mat.length;\n        int n = mat[0].length;\n        int[] rowSum = new int[m];\n        int[] colSum = new int[n];\n        \n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                rowSum[i] += mat[i][j];\n                colSum[j] += mat[i][j];\n            }\n        }\n        \n        int count = 0;\n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (mat[i][j] == 1 && rowSum[i] == 1 && colSum[j] == 1) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numSpecial(vector&lt;vector&lt;int&gt;&gt;& mat) {\n        int m = mat.size();\n        int n = mat[0].size();\n        vector&lt;int&gt; rowSum(m, 0);\n        vector&lt;int&gt; colSum(n, 0);\n        \n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                rowSum[i] += mat[i][j];\n                colSum[j] += mat[i][j];\n            }\n        }\n        \n        int count = 0;\n        for (int i = 0; i &lt; m; i++) {\n            for (int j = 0; j &lt; n; j++) {\n                if (mat[i][j] == 1 && rowSum[i] == 1 && colSum[j] == 1) {\n                    count++;\n                }\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[][]} mat\n * @return {number}\n */\nvar numSpecial = function(mat) {\n    const m = mat.length;\n    const n = mat[0].length;\n    const rowSum = new Array(m).fill(0);\n    const colSum = new Array(n).fill(0);\n    \n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            rowSum[i] += mat[i][j];\n            colSum[j] += mat[i][j];\n        }\n    }\n    \n    let count = 0;\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (mat[i][j] === 1 && rowSum[i] === 1 && colSum[j] === 1) {\n                count++;\n            }\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numSpecial(mat: number[][]): number {\n    const m = mat.length;\n    const n = mat[0].length;\n    const rowSum: number[] = new Array(m).fill(0);\n    const colSum: number[] = new Array(n).fill(0);\n    \n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            rowSum[i] += mat[i][j];\n            colSum[j] += mat[i][j];\n        }\n    }\n    \n    let count = 0;\n    for (let i = 0; i &lt; m; i++) {\n        for (let j = 0; j &lt; n; j++) {\n            if (mat[i][j] === 1 && rowSum[i] === 1 && colSum[j] === 1) {\n                count++;\n            }\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(m × n)
- Space: O(m + n)
- Notes: Optimal solution with linear time complexity, trading space for efficiency

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Special Positions in a Binary Matrix Solutions","datePublished":"2024-07-29","description":"Count positions in a binary matrix where the element is 1 and all other elements in its row and column are 0.","keywords":"LeetCode, Special Positions in a Binary Matrix, Easy, Array, Matrix"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach has a time complexity of O(m × n × (m + n)) where m and n are the dimensions of the matrix."}},{"@type":"Question","name":"What is the time complexity of the Precompute Sums approach?","acceptedAnswer":{"@type":"Answer","text":"The Precompute Sums approach has a time complexity of O(m × n) which is optimal for this problem."}},{"@type":"Question","name":"What is the space complexity of the optimal solution?","acceptedAnswer":{"@type":"Answer","text":"The optimal solution uses O(m + n) space to store the row and column sums."}}]}).replace(/</g,'\u003c')}
</script>