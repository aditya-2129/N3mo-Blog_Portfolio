---
title: "Kth Missing Positive Number"
summary: "Given an array arr of positive integers sorted in strictly increasing order, and an integer k, return the kth positive integer that is missing from this array."
date: "Jan 18, 2024"
tags:
  - Array
  - Binary Search
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 63.40% | **Paid:** No
> **Topics:** Array, Binary Search

Given an array arr of positive integers sorted in a strictly increasing order, and an integer k.

Return the kth positive integer that is missing from this array.

- Examples
- Constraints
- [Approach 1: Linear Scan](#linear-scan)
- [Approach 2: Binary Search](#binary-search)

## Examples

```text
Input: arr = [2,3,4,7,11], k = 5
Output: 9
Explanation: The missing positive integers are [1,5,6,8,9,10,12,13,...]. The 5th missing positive integer is 9.
```

```text
Input: arr = [1,2,3,4], k = 2
Output: 6
Explanation: The missing positive integers are [5,6,7,...]. The 2nd missing positive integer is 6.
```

## Constraints

```text
1 <= arr.length <= 1000
1 <= arr[i] <= 1000
1 <= k <= 1000
arr is sorted in a strictly increasing order.
```

---

## Linear Scan

**Intuition**
We can simulate the process of counting positive integers starting from 1. If the current number exists in the array, we skip it; otherwise, we count it as a missing number until we reach the kth missing one.

**Steps**
- Initialize a pointer `i` for the array and a counter `missing` for missing numbers.
- Iterate through positive integers starting from 1.
- If the current number matches `arr[i]`, increment `i` to move to the next element in the array.
- If the current number does not match `arr[i]`, increment `missing`.
- When `missing` equals `k`, return the current number.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findKthPositive(self, arr: List[int], k: int) -&gt; int:\n        missing_count = 0\n        current = 1\n        i = 0\n        n = len(arr)\n        while missing_count &lt; k:\n            if i &lt; n and arr[i] == current:\n                i += 1\n            else:\n                missing_count += 1\n                if missing_count == k:\n                    return current\n            current += 1\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int findKthPositive(int[] arr, int k) {\n        int missingCount = 0;\n        int current = 1;\n        int i = 0;\n        int n = arr.length;\n        while (missingCount &lt; k) {\n            if (i &lt; n && arr[i] == current) {\n                i++;\n            } else {\n                missingCount++;\n                if (missingCount == k) {\n                    return current;\n                }\n            }\n            current++;\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int findKthPositive(vector&lt;int&gt;& arr, int k) {\n        int missingCount = 0;\n        int current = 1;\n        int i = 0;\n        int n = arr.size();\n        while (missingCount &lt; k) {\n            if (i &lt; n && arr[i] == current) {\n                i++;\n            } else {\n                missingCount++;\n                if (missingCount == k) {\n                    return current;\n                }\n            }\n            current++;\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var findKthPositive = function(arr, k) {\n    let missingCount = 0;\n    let current = 1;\n    let i = 0;\n    const n = arr.length;\n    while (missingCount &lt; k) {\n        if (i &lt; n && arr[i] === current) {\n            i++;\n        } else {\n            missingCount++;\n            if (missingCount === k) {\n                return current;\n            }\n        }\n        current++;\n    }\n    return -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findKthPositive(arr: number[], k: number): number {\n    let missingCount = 0;\n    let current = 1;\n    let i = 0;\n    const n = arr.length;\n    while (missingCount &lt; k) {\n        if (i &lt; n && arr[i] === current) {\n            i++;\n        } else {\n            missingCount++;\n            if (missingCount === k) {\n                return current;\n            }\n        }\n        current++;\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(n + k)
- Space: O(1)
- Notes: Simple to implement but can be slow if k is very large compared to n.

---

## Binary Search

**Intuition**
For any index `i`, the number of missing positive integers before `arr[i]` is given by `arr[i] - i - 1`. We can use binary search to find the smallest index `left` such that the number of missing integers before `arr[left]` is at least `k`. The answer is then `left + k`.

**Steps**
- Initialize `left = 0` and `right = arr.length - 1`.
- While `left &lt;= right`:
    - Calculate `mid = left + (right - left) / 2`.
    - Calculate `missing = arr[mid] - mid - 1`.
    - If `missing &lt; k`, the kth missing number is to the right, so set `left = mid + 1`.
    - Otherwise, the kth missing number is to the left or at `mid`, so set `right = mid - 1`.
- After the loop, `left` is the insertion point. The kth missing number is `left + k`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def findKthPositive(self, arr: List[int], k: int) -&gt; int:\n        left, right = 0, len(arr) - 1\n        while left &lt;= right:\n            mid = left + (right - left) // 2\n            missing = arr[mid] - mid - 1\n            if missing &lt; k:\n                left = mid + 1\n            else:\n                right = mid - 1\n        return left + k" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int findKthPositive(int[] arr, int k) {\n        int left = 0, right = arr.length - 1;\n        while (left &lt;= right) {\n            int mid = left + (right - left) / 2;\n            int missing = arr[mid] - mid - 1;\n            if (missing &lt; k) {\n                left = mid + 1;\n            } else {\n                right = mid - 1;\n            }\n        }\n        return left + k;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int findKthPositive(vector&lt;int&gt;& arr, int k) {\n        int left = 0, right = arr.size() - 1;\n        while (left &lt;= right) {\n            int mid = left + (right - left) / 2;\n            int missing = arr[mid] - mid - 1;\n            if (missing &lt; k) {\n                left = mid + 1;\n            } else {\n                right = mid - 1;\n            }\n        }\n        return left + k;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var findKthPositive = function(arr, k) {\n    let left = 0, right = arr.length - 1;\n    while (left &lt;= right) {\n        const mid = left + Math.floor((right - left) / 2);\n        const missing = arr[mid] - mid - 1;\n        if (missing &lt; k) {\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n    return left + k;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function findKthPositive(arr: number[], k: number): number {\n    let left = 0, right = arr.length - 1;\n    while (left &lt;= right) {\n        const mid = left + Math.floor((right - left) / 2);\n        const missing = arr[mid] - mid - 1;\n        if (missing &lt; k) {\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n    return left + k;\n}" },]} />

**Complexity**
- Time: O(log n)
- Space: O(1)
- Notes: Optimal solution for large input sizes.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Kth Missing Positive Number Solutions","datePublished":"2024-01-18","description":"Given an array arr of positive integers sorted in strictly increasing order, and an integer k, return the kth positive integer that is missing from this array.","keywords":"LeetCode, Kth Missing Positive Number, Easy, Array, Binary Search"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Linear Scan approach is O(n + k), where n is the length of the array and k is the kth missing number."}},{"@type":"Question","name":"What is the time complexity of the Binary Search approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Binary Search approach is O(log n), as it halves the search space in each iteration."}},{"@type":"Question","name":"What is the space complexity for these approaches?","acceptedAnswer":{"@type":"Answer","text":"Both the Linear Scan and Binary Search approaches have a space complexity of O(1) since they do not use extra space proportional to the input size."}}]}).replace(/</g,'\u003c')}
</script>