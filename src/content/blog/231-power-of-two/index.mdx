---
title: "Power of Two"
summary: "Given an integer n, return true if it is a power of two. Otherwise return false."
date: "Jan 23, 2024"
tags:
  - Math
  - Bit Manipulation
  - Recursion
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 50.10% | **Paid:** No
> **Topics:** Math, Bit Manipulation, Recursion

Given an integer n, return true if it is a power of two. Otherwise, return false.

An integer n is a power of two, if there exists an integer x such that n == 2x.

- Examples
- Constraints
- [Approach 1: Iterative Division](#approach-1-iterative-division)
- [Approach 2: Bit Manipulation (n & (n-1))](#approach-2-bit-manipulation-n--n-1)
- [Approach 3: Bit Manipulation (n & -n)](#approach-3-bit-manipulation-n---n)
- [Approach 4: Math & Logarithms](#approach-4-math--logarithms)
- [Approach 5: Recursion](#approach-5-recursion)

## Examples

```text
Input: n = 1
Output: true
Explanation: 2⁰ = 1
```

```text
Input: n = 16
Output: true
Explanation: 2⁴ = 16
```

```text
Input: n = 3
Output: false
```

## Constraints

```text
-2³¹ <= n <= 2³¹ - 1
```

---

## Approach 1: Iterative Division

**Intuition**
If a number is a power of two, we can continuously divide it by 2. If we eventually reach 1, it is a power of two. If we encounter an odd number greater than 1, it is not.

**Steps**
- Check if n is less than or equal to 0. If so, return false.
- Use a while loop to iterate while n is even (n % 2 == 0).
- Inside the loop, divide n by 2.
- After the loop, check if n is equal to 1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isPowerOfTwo(self, n: int) -&gt; bool:\n        if n &lt;= 0:\n            return False\n        while n % 2 == 0:\n            n //= 2\n        return n == 1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isPowerOfTwo(int n) {\n        if (n &lt;= 0) return false;\n        while (n % 2 == 0) {\n            n /= 2;\n        }\n        return n == 1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isPowerOfTwo(int n) {\n        if (n &lt;= 0) return false;\n        while (n % 2 == 0) {\n            n /= 2;\n        }\n        return n == 1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var isPowerOfTwo = function(n) {\n    if (n &lt;= 0) return false;\n    while (n % 2 === 0) {\n        n /= 2;\n    }\n    return n === 1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isPowerOfTwo(n: number): boolean {\n    if (n &lt;= 0) return false;\n    while (n % 2 === 0) {\n        n /= 2;\n    }\n    return n === 1;\n}" },]} />

**Complexity**
- Time: O(log n)
- Space: O(1)
- Notes: Simple logic, but slower than bit manipulation.

## Approach 2: Bit Manipulation (n & (n-1))

**Intuition**
A power of two in binary representation has exactly one bit set to 1 (e.g., 8 is 1000). If we subtract 1 from a power of two, all bits to the right of that set bit flip to 1, and the set bit becomes 0 (e.g., 7 is 0111). The bitwise AND of these two numbers is always 0.

**Steps**
- Check if n is greater than 0.
- Return the result of (n & (n - 1)) == 0.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isPowerOfTwo(self, n: int) -&gt; bool:\n        return n &gt; 0 and (n & (n - 1)) == 0" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isPowerOfTwo(int n) {\n        return n &gt; 0 && (n & (n - 1)) == 0;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isPowerOfTwo(int n) {\n        return n &gt; 0 && (n & (n - 1)) == 0;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var isPowerOfTwo = function(n) {\n    return n &gt; 0 && (n & (n - 1)) === 0;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isPowerOfTwo(n: number): boolean {\n    return n &gt; 0 && (n & (n - 1)) === 0;\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Highly efficient, commonly used bit trick.

## Approach 3: Bit Manipulation (n & -n)

**Intuition**
In two's complement representation, -n is equivalent to ~n + 1. The operation n & -n isolates the lowest set bit of n. For a power of two, there is only one set bit, so n & -n equals n.

**Steps**
- Check if n is greater than 0.
- Return the result of n == (n & -n).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isPowerOfTwo(self, n: int) -&gt; bool:\n        return n &gt; 0 and (n & -n) == n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isPowerOfTwo(int n) {\n        return n &gt; 0 && (n & -n) == n;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isPowerOfTwo(int n) {\n        return n &gt; 0 && (n & -n) == n;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var isPowerOfTwo = function(n) {\n    return n &gt; 0 && (n & -n) === n;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isPowerOfTwo(n: number): boolean {\n    return n &gt; 0 && (n & -n) === n;\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Efficient, relies on two's complement arithmetic.

## Approach 4: Math & Logarithms

**Intuition**
If n is a power of two, then log₂(n) is an integer. We can calculate the logarithm and check if it is a whole number.

**Steps**
- Check if n is greater than 0.
- Calculate the base-2 logarithm of n.
- Check if the result is an integer (modulo 1 equals 0).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import math\n\nclass Solution:\n    def isPowerOfTwo(self, n: int) -&gt; bool:\n        if n &lt;= 0:\n            return False\n        return math.log2(n).is_integer()" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isPowerOfTwo(int n) {\n        if (n &lt;= 0) return false;\n        // Using log(n) / log(2) to get log base 2\n        double log = Math.log(n) / Math.log(2);\n        // Check if log is close to an integer to handle floating point errors\n        return Math.abs(log - Math.round(log)) &lt; 1e-10;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;cmath&gt;\n\nclass Solution {\npublic:\n    bool isPowerOfTwo(int n) {\n        if (n &lt;= 0) return false;\n        // log2 returns floating point\n        double val = log2(n);\n        // Check if val is an integer by comparing floor and ceil\n        return floor(val) == val;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var isPowerOfTwo = function(n) {\n    if (n &lt;= 0) return false;\n    const log = Math.log2(n);\n    return Number.isInteger(log);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isPowerOfTwo(n: number): boolean {\n    if (n &lt;= 0) return false;\n    const log = Math.log2(n);\n    return Number.isInteger(log);\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Susceptible to floating-point precision errors for very large integers.

## Approach 5: Recursion

**Intuition**
We can recursively divide n by 2. The base cases are n == 1 (true) and n &lt;= 0 or n is odd (false).

**Steps**
- If n &lt;= 0, return false.
- If n == 1, return true.
- If n is odd (n % 2 != 0), return false.
- Otherwise, return the result of isPowerOfTwo(n / 2).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isPowerOfTwo(self, n: int) -&gt; bool:\n        if n &lt;= 0:\n            return False\n        if n == 1:\n            return True\n        if n % 2 != 0:\n            return False\n        return self.isPowerOfTwo(n // 2)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isPowerOfTwo(int n) {\n        if (n &lt;= 0) return false;\n        if (n == 1) return true;\n        if (n % 2 != 0) return false;\n        return isPowerOfTwo(n / 2);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isPowerOfTwo(int n) {\n        if (n &lt;= 0) return false;\n        if (n == 1) return true;\n        if (n % 2 != 0) return false;\n        return isPowerOfTwo(n / 2);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var isPowerOfTwo = function(n) {\n    if (n &lt;= 0) return false;\n    if (n == 1) return true;\n    if (n % 2 !== 0) return false;\n    return isPowerOfTwo(n / 2);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isPowerOfTwo(n: number): boolean {\n    if (n &lt;= 0) return false;\n    if (n == 1) return true;\n    if (n % 2 !== 0) return false;\n    return isPowerOfTwo(n / 2);\n}" },]} />

**Complexity**
- Time: O(log n)
- Space: O(log n)
- Notes: Uses stack space proportional to the number of divisions.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Power of Two Solutions","datePublished":"2024-01-23","description":"Given an integer n, return true if it is a power of two. Otherwise return false.","keywords":"LeetCode, Power of Two, Easy, Math, Bit Manipulation, Recursion"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Division approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(log n) because we divide the number by 2 in each step."}},{"@type":"Question","name":"What is the time complexity of the Bit Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) as it involves a constant number of bitwise operations."}},{"@type":"Question","name":"What is the space complexity of the Recursion approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(log n) due to the recursion stack depth."}}]}).replace(/</g,'\u003c')}
</script>