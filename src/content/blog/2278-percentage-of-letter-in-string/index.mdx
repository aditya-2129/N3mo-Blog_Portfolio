---
title: "Percentage of Letter in String"
summary: "Given a string and a letter, calculate the percentage of the string composed of that letter, rounded down."
date: "May 24, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 75.10% | **Paid:** No
> **Topics:** String

Given a string s and a character letter, return the percentage of characters in s that equal letter rounded down to the nearest whole percent.

## Examples

Example 1:
```text
Input: s = "foobar", letter = "o"
Output: 33
Explanation:
The percentage of characters in s that equal the letter 'o' is 2 / 6 * 100 = 33% when rounded down, so we return 33.
```

Example 2:
```text
Input: s = "jjjj", letter = "k"
Output: 0
Explanation:
The percentage of characters in s that equal the letter 'k' is 0%, so we return 0.
```

## Constraints

```text
1 <= s.length <= 100
s consists of lowercase English letters.
letter is a lowercase English letter.
```

- Examples
- Constraints
- [Linear Scan](#linear-scan)
- [Built-in Functions](#built-in-functions)

---
## Linear Scan

**Intuition**
We iterate through the string once to count how many times the target letter appears, then calculate the percentage based on the string's length.

**Steps**
- Initialize a counter to 0.
- Loop through each character in the string.
- If the character matches the target letter, increment the counter.
- Calculate the percentage using integer arithmetic: (count * 100) / length.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def percentageLetter(self, s: str, letter: str) -&gt; int:\n        count = 0\n        for char in s:\n            if char == letter:\n                count += 1\n        return (count * 100) // len(s)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int percentageLetter(String s, char letter) {\n        int count = 0;\n        for (int i = 0; i &lt; s.length(); i++) {\n            if (s.charAt(i) == letter) {\n                count++;\n            }\n        }\n        return (count * 100) / s.length();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int percentageLetter(string s, char letter) {\n        int count = 0;\n        for (char c : s) {\n            if (c == letter) {\n                count++;\n            }\n        }\n        return (count * 100) / s.length();\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    percentageLetter(s, letter) {\n        let count = 0;\n        for (let i = 0; i &lt; s.length; i++) {\n            if (s[i] === letter) {\n                count++;\n            }\n        }\n        return Math.floor((count * 100) / s.length);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    percentageLetter(s: string, letter: string): number {\n        let count = 0;\n        for (let i = 0; i &lt; s.length; i++) {\n            if (s[i] === letter) {\n                count++;\n            }\n        }\n        return Math.floor((count * 100) / s.length);\n    }\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string.
- Space: O(1) extra space.
- Notes: This is the most fundamental approach and works in any language.

---
## Built-in Functions

**Intuition**
Most modern languages provide optimized library functions to count occurrences of a character or substring within a string. We can leverage these to make the code concise.

**Steps**
- Use the language's specific count method (e.g., count() in Python, std::count in C++) to find the frequency of the letter.
- Calculate the percentage using the formula: (frequency * 100) / length.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def percentageLetter(self, s: str, letter: str) -&gt; int:\n        return (s.count(letter) * 100) // len(s)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int percentageLetter(String s, char letter) {\n        long count = s.chars().filter(ch -&gt; ch == letter).count();\n        return (int) ((count * 100) / s.length());\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n\nclass Solution {\npublic:\n    int percentageLetter(string s, char letter) {\n        int count = std::count(s.begin(), s.end(), letter);\n        return (count * 100) / s.length();\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    percentageLetter(s, letter) {\n        const count = [...s].filter(c =&gt; c === letter).length;\n        return Math.floor((count * 100) / s.length);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    percentageLetter(s: string, letter: string): number {\n        const count = [...s].filter(c =&gt; c === letter).length;\n        return Math.floor((count * 100) / s.length);\n    }\n}" },]} />

**Complexity**
- Time: O(n) as the built-in functions must still scan the string.
- Space: O(1) or O(n) depending on implementation (e.g., spread operator in JS creates an array).
- Notes: While concise, be mindful of hidden space complexity in languages like JavaScript where strings are iterable but often converted to arrays for functional methods.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Percentage of Letter in String Solutions","datePublished":"2024-05-24","description":"Given a string and a letter, calculate the percentage of the string composed of that letter, rounded down.","keywords":"LeetCode, Percentage of Letter in String, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string, because we iterate through the string once."}},{"@type":"Question","name":"What is the space complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a single integer variable for counting."}},{"@type":"Question","name":"How does the Built-in Functions approach compare to Linear Scan?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is generally the same O(n), but the Built-in approach is often more concise and readable, though it may have slightly higher constant overhead or auxiliary space usage depending on the language."}}]}).replace(/</g,'\u003c')}
</script>