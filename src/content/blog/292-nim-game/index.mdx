---
title: "Nim Game"
summary: "Determine if you can win the Nim Game where you and a friend take turns removing 1-3 stones from a heap."
date: "Feb 25, 2026"
tags:
  - Math
  - Brainteaser
  - Game Theory
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 59.80% | **Paid:** No
> **Topics:** Math, Brainteaser, Game Theory

You are playing the following Nim Game with your friend:

Initially, there is a heap of stones on the table.
You and your friend will alternate taking turns, and you go first.
On each turn, the person whose turn it is will remove 1 to 3 stones from the heap.
The one who removes the last stone is the winner.
Given the number of stones in the heap, return true if you can win the game assuming both you and your friend play optimally, otherwise return false.

- Examples
- Constraints
- [Mathematical Approach](#mathematical-approach)
- [Dynamic Programming Approach](#dynamic-programming-approach)
- [Recursive with Memoization Approach](#recursive-with-memoization-approach)

## Examples

**Example 1:**
```text
Input: n = 4
Output: false
Explanation: These are the possible outcomes:
1. You remove 1 stone. Your friend removes 3 stones, winning the game.
2. You remove 2 stones. Your friend removes 2 stones, winning the game.
3. You remove 3 stones. Your friend removes the last stone, winning the game.
In all outcomes, your friend wins.
```

**Example 2:**
```text
Input: n = 1
Output: true
```

**Example 3:**
```text
Input: n = 2
Output: true
```

## Constraints

```text
1 <= n <= 2³¹ - 1
```

---
## Mathematical Approach

**Intuition**
If the number of stones is a multiple of 4, you will always lose with optimal play from both sides. Otherwise, you can always take enough stones to leave a multiple of 4 for your opponent.

**Steps**
- Check if n is divisible by 4
- If yes, return false (you lose)
- Otherwise, return true (you win)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def canWinNim(self, n: int) -&gt; bool:\n        return n % 4 != 0\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean canWinNim(int n) {\n        return n % 4 != 0;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool canWinNim(int n) {\n        return n % 4 != 0;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {boolean}\n */\nvar canWinNim = function(n) {\n    return n % 4 !== 0;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function canWinNim(n: number): boolean {\n    return n % 4 !== 0;\n}\n" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This is the optimal solution with constant time and space complexity.

---
## Dynamic Programming Approach

**Intuition**
Build up the solution from base cases. For each number of stones, you win if at least one of your possible moves (taking 1, 2, or 3 stones) leaves your opponent in a losing position.

**Steps**
- Initialize dp array where dp[i] represents if you can win with i stones
- Set base cases: dp[1] = dp[2] = dp[3] = true
- For each i from 4 to n, dp[i] = !dp[i-1] || !dp[i-2] || !dp[i-3]
- Return dp[n]

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def canWinNim(self, n: int) -&gt; bool:\n        if n &lt;= 3:\n            return True\n        dp = [False] * (n + 1)\n        dp[1] = dp[2] = dp[3] = True\n        for i in range(4, n + 1):\n            dp[i] = not dp[i-1] or not dp[i-2] or not dp[i-3]\n        return dp[n]\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean canWinNim(int n) {\n        if (n &lt;= 3) return true;\n        boolean[] dp = new boolean[n + 1];\n        dp[1] = dp[2] = dp[3] = true;\n        for (int i = 4; i &lt;= n; i++) {\n            dp[i] = !dp[i-1] || !dp[i-2] || !dp[i-3];\n        }\n        return dp[n];\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool canWinNim(int n) {\n        if (n &lt;= 3) return true;\n        vector&lt;bool&gt; dp(n + 1, false);\n        dp[1] = dp[2] = dp[3] = true;\n        for (int i = 4; i &lt;= n; i++) {\n            dp[i] = !dp[i-1] || !dp[i-2] || !dp[i-3];\n        }\n        return dp[n];\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {boolean}\n */\nvar canWinNim = function(n) {\n    if (n &lt;= 3) return true;\n    const dp = new Array(n + 1).fill(false);\n    dp[1] = dp[2] = dp[3] = true;\n    for (let i = 4; i &lt;= n; i++) {\n        dp[i] = !dp[i-1] || !dp[i-2] || !dp[i-3];\n    }\n    return dp[n];\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function canWinNim(n: number): boolean {\n    if (n &lt;= 3) return true;\n    const dp: boolean[] = new Array(n + 1).fill(false);\n    dp[1] = dp[2] = dp[3] = true;\n    for (let i = 4; i &lt;= n; i++) {\n        dp[i] = !dp[i-1] || !dp[i-2] || !dp[i-3];\n    }\n    return dp[n];\n}\n" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: This approach reveals the pattern but is not practical for large n due to memory constraints.

---
## Recursive with Memoization Approach

**Intuition**
Use recursion to explore all possible moves. If any move leads to the opponent losing, you win. Memoization prevents redundant calculations.

**Steps**
- Create a memoization map/dictionary
- Define recursive function that checks if you can win with given stones
- Base case: if stones &lt;= 3, return true
- Check memo for cached result
- Recursively check all three possible moves
- Cache and return result

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def canWinNim(self, n: int) -&gt; bool:\n        memo = {}\n        \n        def canWin(stones):\n            if stones &lt;= 3:\n                return True\n            if stones in memo:\n                return memo[stones]\n            result = not canWin(stones - 1) or not canWin(stones - 2) or not canWin(stones - 3)\n            memo[stones] = result\n            return result\n        \n        return canWin(n)\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    private Map&lt;Integer, Boolean&gt; memo = new HashMap&lt;&gt;();\n    \n    public boolean canWinNim(int n) {\n        if (n &lt;= 3) return true;\n        if (memo.containsKey(n)) return memo.get(n);\n        \n        boolean result = !canWinNim(n - 1) || !canWinNim(n - 2) || !canWinNim(n - 3);\n        memo.put(n, result);\n        return result;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\nprivate:\n    unordered_map&lt;int, bool&gt; memo;\npublic:\n    bool canWinNim(int n) {\n        if (n &lt;= 3) return true;\n        if (memo.find(n) != memo.end()) return memo[n];\n        \n        bool result = !canWinNim(n - 1) || !canWinNim(n - 2) || !canWinNim(n - 3);\n        memo[n] = result;\n        return result;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {boolean}\n */\nvar canWinNim = function(n) {\n    const memo = new Map();\n    \n    function canWin(stones) {\n        if (stones &lt;= 3) return true;\n        if (memo.has(stones)) return memo.get(stones);\n        \n        const result = !canWin(stones - 1) || !canWin(stones - 2) || !canWin(stones - 3);\n        memo.set(stones, result);\n        return result;\n    }\n    \n    return canWin(n);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function canWinNim(n: number): boolean {\n    const memo = new Map&lt;number, boolean&gt;();\n    \n    function canWin(stones: number): boolean {\n        if (stones &lt;= 3) return true;\n        if (memo.has(stones)) return memo.get(stones)!;\n        \n        const result = !canWin(stones - 1) || !canWin(stones - 2) || !canWin(stones - 3);\n        memo.set(stones, result);\n        return result;\n    }\n    \n    return canWin(n);\n}\n" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: May cause stack overflow for very large n due to recursion depth limits.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Nim Game Solutions","datePublished":"2026-02-25","description":"Determine if you can win the Nim Game where you and a friend take turns removing 1-3 stones from a heap.","keywords":"LeetCode, Nim Game, Easy, Math, Brainteaser, Game Theory"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Mathematical Approach?","acceptedAnswer":{"@type":"Answer","text":"The Mathematical Approach has O(1) time complexity as it only requires a single modulo operation."}},{"@type":"Question","name":"What is the space complexity of the Dynamic Programming Approach?","acceptedAnswer":{"@type":"Answer","text":"The Dynamic Programming Approach has O(n) space complexity due to the dp array storing results for all values up to n."}},{"@type":"Question","name":"Why does the Mathematical Approach work?","acceptedAnswer":{"@type":"Answer","text":"If n is a multiple of 4, any move you make (1, 2, or 3 stones) leaves your opponent with a non-multiple of 4, allowing them to always return you to a multiple of 4. Eventually, you're left with exactly 4 stones and lose."}}]}).replace(/</g,'\u003c')}
</script>