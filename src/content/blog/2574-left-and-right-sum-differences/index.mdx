---
title: "Left and Right Sum Differences"
summary: "Given an integer array nums, return the difference array where each element is the absolute difference between the sum of elements to its left and the sum of elements to its right."
date: "Feb 09, 2024"
tags:
  - Array
  - Prefix Sum
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 88.00% | **Paid:** No
> **Topics:** Array, Prefix Sum

Given a 0-indexed integer array nums, find the difference array answer.

For each index i, answer[i] = |leftSum[i] - rightSum[i]|.

Where:
- leftSum[i] is the sum of elements to the left of index i in the array nums. If there is no such element, leftSum[i] = 0.
- rightSum[i] is the sum of elements to the right of index i in the array nums. If there is no such element, rightSum[i] = 0.

Return the array answer.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Prefix Sum (Optimal)](#prefix-sum-optimal)
- [Two Pass Prefix Sum](#two-pass-prefix-sum)

## Examples

```text
Input: nums = [10,4,8,3]
Output: [15,1,11,22]
Explanation: 
The array leftSum is [0,10,14,22] and the array rightSum is [15,11,3,0].
The array answer is [|0 - 15|,|10 - 11|,|14 - 3|,|22 - 0|] = [15,1,11,22].
```

```text
Input: nums = [1]
Output: [0]
Explanation:
The array leftSum is [0] and the array rightSum is [0].
The array answer is [|0 - 0|] = [0].
```

## Constraints

```text
1 <= nums.length <= 1000
1 <= nums[i] <= 10⁵
```

---

## Brute Force

**Intuition**
For each element in the array, we can explicitly calculate the sum of elements to its left and the sum of elements to its right by iterating through the array.

**Steps**
- Initialize an empty result array.
- Iterate through the array with index `i`.
- For each `i`, calculate `leftSum` by iterating from `0` to `i - 1`.
- For each `i`, calculate `rightSum` by iterating from `i + 1` to `n - 1`.
- Calculate the absolute difference and append it to the result.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def leftRightDifference(self, nums: List[int]) -&gt; List[int]:\r\n        n = len(nums)\r\n        answer = []\r\n        for i in range(n):\r\n            left_sum = 0\r\n            right_sum = 0\r\n            \r\n            # Calculate left sum\r\n            for j in range(i):\r\n                left_sum += nums[j]\r\n                \r\n            # Calculate right sum\r\n            for j in range(i + 1, n):\r\n                right_sum += nums[j]\r\n                \r\n            answer.append(abs(left_sum - right_sum))\r\n            \r\n        return answer" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int[] leftRightDifference(int[] nums) {\r\n        int n = nums.length;\r\n        int[] answer = new int[n];\r\n        \r\n        for (int i = 0; i &lt; n; i++) {\r\n            int leftSum = 0;\r\n            int rightSum = 0;\r\n            \r\n            // Calculate left sum\r\n            for (int j = 0; j &lt; i; j++) {\r\n                leftSum += nums[j];\r\n            }\r\n            \r\n            // Calculate right sum\r\n            for (int j = i + 1; j &lt; n; j++) {\r\n                rightSum += nums[j];\r\n            }\r\n            \r\n            answer[i] = Math.abs(leftSum - rightSum);\r\n        }\r\n        \r\n        return answer;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;cmath&gt;\r\n\r\nclass Solution {\r\npublic:\r\n    std::vector&lt;int&gt; leftRightDifference(std::vector&lt;int&gt;& nums) {\r\n        int n = nums.size();\r\n        std::vector&lt;int&gt; answer(n);\r\n        \r\n        for (int i = 0; i &lt; n; i++) {\r\n            int leftSum = 0;\r\n            int rightSum = 0;\r\n            \r\n            // Calculate left sum\r\n            for (int j = 0; j &lt; i; j++) {\r\n                leftSum += nums[j];\r\n            }\r\n            \r\n            // Calculate right sum\r\n            for (int j = i + 1; j &lt; n; j++) {\r\n                rightSum += nums[j];\r\n            }\r\n            \r\n            answer[i] = abs(leftSum - rightSum);\r\n        }\r\n        \r\n        return answer;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {number[]} nums\r\n * @return {number[]}\r\n */\r\nvar leftRightDifference = function(nums) {\r\n    const n = nums.length;\r\n    const answer = [];\r\n    \r\n    for (let i = 0; i &lt; n; i++) {\r\n        let leftSum = 0;\r\n        let rightSum = 0;\r\n        \r\n        // Calculate left sum\r\n        for (let j = 0; j &lt; i; j++) {\r\n            leftSum += nums[j];\r\n        }\r\n        \r\n        // Calculate right sum\r\n        for (let j = i + 1; j &lt; n; j++) {\r\n            rightSum += nums[j];\r\n        }\r\n        \r\n        answer.push(Math.abs(leftSum - rightSum));\r\n    }\r\n    \r\n    return answer;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function leftRightDifference(nums: number[]): number[] {\r\n    const n: number = nums.length;\r\n    const answer: number[] = [];\r\n    \r\n    for (let i = 0; i &lt; n; i++) {\r\n        let leftSum: number = 0;\r\n        let rightSum: number = 0;\r\n        \r\n        // Calculate left sum\r\n        for (let j = 0; j &lt; i; j++) {\r\n            leftSum += nums[j];\r\n        }\r\n        \r\n        // Calculate right sum\r\n        for (let j = i + 1; j &lt; n; j++) {\r\n            rightSum += nums[j];\r\n        }\r\n        \r\n        answer.push(Math.abs(leftSum - rightSum));\r\n    }\r\n    \r\n    return answer;\r\n}" },]} />

**Complexity**
- Time: O(n²) - For each element, we iterate through the array to calculate sums.
- Space: O(1) - Excluding the space required for the output array.
- Notes: Simple to implement but inefficient for large arrays.

---

## Prefix Sum (Optimal)

**Intuition**
We can calculate the total sum of the array once. As we iterate through the array, we can maintain a running `leftSum`. The `rightSum` for the current index can be derived by subtracting the `leftSum` and the current element from the total sum.

**Steps**
- Calculate the total sum of the array.
- Initialize `leftSum` to 0.
- Iterate through the array.
- For each element `nums[i]`, calculate `rightSum = totalSum - leftSum - nums[i]`.
- Calculate the absolute difference between `leftSum` and `rightSum` and store it in the result.
- Add `nums[i]` to `leftSum`.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def leftRightDifference(self, nums: List[int]) -&gt; List[int]:\r\n        total_sum = sum(nums)\r\n        left_sum = 0\r\n        answer = []\r\n        \r\n        for n in nums:\r\n            right_sum = total_sum - left_sum - n\r\n            answer.append(abs(left_sum - right_sum))\r\n            left_sum += n\r\n            \r\n        return answer" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int[] leftRightDifference(int[] nums) {\r\n        int totalSum = 0;\r\n        for (int n : nums) {\r\n            totalSum += n;\r\n        }\r\n        \r\n        int leftSum = 0;\r\n        int[] answer = new int[nums.length];\r\n        \r\n        for (int i = 0; i &lt; nums.length; i++) {\r\n            int rightSum = totalSum - leftSum - nums[i];\r\n            answer[i] = Math.abs(leftSum - rightSum);\r\n            leftSum += nums[i];\r\n        }\r\n        \r\n        return answer;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;cmath&gt;\r\n\r\nclass Solution {\r\npublic:\r\n    std::vector&lt;int&gt; leftRightDifference(std::vector&lt;int&gt;& nums) {\r\n        int totalSum = 0;\r\n        for (int n : nums) {\r\n            totalSum += n;\r\n        }\r\n        \r\n        int leftSum = 0;\r\n        std::vector&lt;int&gt; answer;\r\n        \r\n        for (int n : nums) {\r\n            int rightSum = totalSum - leftSum - n;\r\n            answer.push_back(abs(leftSum - rightSum));\r\n            leftSum += n;\r\n        }\r\n        \r\n        return answer;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {number[]} nums\r\n * @return {number[]}\r\n */\r\nvar leftRightDifference = function(nums) {\r\n    let totalSum = 0;\r\n    for (let n of nums) {\r\n        totalSum += n;\r\n    }\r\n    \r\n    let leftSum = 0;\r\n    const answer = [];\r\n    \r\n    for (let i = 0; i &lt; nums.length; i++) {\r\n        let rightSum = totalSum - leftSum - nums[i];\r\n        answer.push(Math.abs(leftSum - rightSum));\r\n        leftSum += nums[i];\r\n    }\r\n    \r\n    return answer;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function leftRightDifference(nums: number[]): number[] {\r\n    let totalSum: number = 0;\r\n    for (let n of nums) {\r\n        totalSum += n;\r\n    }\r\n    \r\n    let leftSum: number = 0;\r\n    const answer: number[] = [];\r\n    \r\n    for (let i = 0; i &lt; nums.length; i++) {\r\n        let rightSum: number = totalSum - leftSum - nums[i];\r\n        answer.push(Math.abs(leftSum - rightSum));\r\n        leftSum += nums[i];\r\n    }\r\n    \r\n    return answer;\r\n}" },]} />

**Complexity**
- Time: O(n) - We pass through the array twice (once for total sum, once for the answer).
- Space: O(1) - Excluding the space required for the output array.
- Notes: This is the most efficient approach for this problem.

---

## Two Pass Prefix Sum

**Intuition**
We can pre-compute the prefix sum (sum of elements up to index i) and suffix sum (sum of elements from index i to end) for every index. Then, we can construct the answer array by looking up these pre-computed values.

**Steps**
- Initialize `leftSum` array of size n with 0s.
- Initialize `rightSum` array of size n with 0s.
- Fill `leftSum` array: `leftSum[i] = leftSum[i-1] + nums[i-1]`.
- Fill `rightSum` array: `rightSum[i] = rightSum[i+1] + nums[i+1]`.
- Iterate and calculate `answer[i] = |leftSum[i] - rightSum[i]|`.
- Return answer.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def leftRightDifference(self, nums: List[int]) -&gt; List[int]:\r\n        n = len(nums)\r\n        left_sum = [0] * n\r\n        right_sum = [0] * n\r\n        \r\n        # Calculate prefix sums\r\n        for i in range(1, n):\r\n            left_sum[i] = left_sum[i-1] + nums[i-1]\r\n            \r\n        # Calculate suffix sums\r\n        for i in range(n - 2, -1, -1):\r\n            right_sum[i] = right_sum[i+1] + nums[i+1]\r\n            \r\n        # Calculate answer\r\n        return [abs(left_sum[i] - right_sum[i]) for i in range(n)]" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int[] leftRightDifference(int[] nums) {\r\n        int n = nums.length;\r\n        int[] leftSum = new int[n];\r\n        int[] rightSum = new int[n];\r\n        \r\n        // Calculate prefix sums\r\n        for (int i = 1; i &lt; n; i++) {\r\n            leftSum[i] = leftSum[i - 1] + nums[i - 1];\r\n        }\r\n        \r\n        // Calculate suffix sums\r\n        for (int i = n - 2; i &gt;= 0; i--) {\r\n            rightSum[i] = rightSum[i + 1] + nums[i + 1];\r\n        }\r\n        \r\n        // Calculate answer\r\n        int[] answer = new int[n];\r\n        for (int i = 0; i &lt; n; i++) {\r\n            answer[i] = Math.abs(leftSum[i] - rightSum[i]);\r\n        }\r\n        \r\n        return answer;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;cmath&gt;\r\n\r\nclass Solution {\r\npublic:\r\n    std::vector&lt;int&gt; leftRightDifference(std::vector&lt;int&gt;& nums) {\r\n        int n = nums.size();\r\n        std::vector&lt;int&gt; leftSum(n, 0);\r\n        std::vector&lt;int&gt; rightSum(n, 0);\r\n        \r\n        // Calculate prefix sums\r\n        for (int i = 1; i &lt; n; i++) {\r\n            leftSum[i] = leftSum[i - 1] + nums[i - 1];\r\n        }\r\n        \r\n        // Calculate suffix sums\r\n        for (int i = n - 2; i &gt;= 0; i--) {\r\n            rightSum[i] = rightSum[i + 1] + nums[i + 1];\r\n        }\r\n        \r\n        // Calculate answer\r\n        std::vector&lt;int&gt; answer(n);\r\n        for (int i = 0; i &lt; n; i++) {\r\n            answer[i] = abs(leftSum[i] - rightSum[i]);\r\n        }\r\n        \r\n        return answer;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\r\n * @param {number[]} nums\r\n * @return {number[]}\r\n */\r\nvar leftRightDifference = function(nums) {\r\n    const n = nums.length;\r\n    const leftSum = new Array(n).fill(0);\r\n    const rightSum = new Array(n).fill(0);\r\n    \r\n    // Calculate prefix sums\r\n    for (let i = 1; i &lt; n; i++) {\r\n        leftSum[i] = leftSum[i - 1] + nums[i - 1];\r\n    }\r\n    \r\n    // Calculate suffix sums\r\n    for (let i = n - 2; i &gt;= 0; i--) {\r\n        rightSum[i] = rightSum[i + 1] + nums[i + 1];\r\n    }\r\n    \r\n    // Calculate answer\r\n    const answer = [];\r\n    for (let i = 0; i &lt; n; i++) {\r\n        answer.push(Math.abs(leftSum[i] - rightSum[i]));\r\n    }\r\n    \r\n    return answer;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function leftRightDifference(nums: number[]): number[] {\r\n    const n: number = nums.length;\r\n    const leftSum: number[] = new Array(n).fill(0);\r\n    const rightSum: number[] = new Array(n).fill(0);\r\n    \r\n    // Calculate prefix sums\r\n    for (let i = 1; i &lt; n; i++) {\r\n        leftSum[i] = leftSum[i - 1] + nums[i - 1];\r\n    }\r\n    \r\n    // Calculate suffix sums\r\n    for (let i = n - 2; i &gt;= 0; i--) {\r\n        rightSum[i] = rightSum[i + 1] + nums[i + 1];\r\n    }\r\n    \r\n    // Calculate answer\r\n    const answer: number[] = [];\r\n    for (let i = 0; i &lt; n; i++) {\r\n        answer.push(Math.abs(leftSum[i] - rightSum[i]));\r\n    }\r\n    \r\n    return answer;\r\n}" },]} />

**Complexity**
- Time: O(n) - We pass through the array three times (left, right, answer).
- Space: O(n) - We use two extra arrays of size n to store prefix and suffix sums.
- Notes: Uses more memory than the optimal approach but clearly demonstrates the concept of prefix sums.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Left and Right Sum Differences Solutions","datePublished":"2024-02-09","description":"Given an integer array nums, return the difference array where each element is the absolute difference between the sum of elements to its left and the sum of elements to its right.","keywords":"LeetCode, Left and Right Sum Differences, Easy, Array, Prefix Sum"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n²), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the most efficient approach for Left and Right Sum Differences?","acceptedAnswer":{"@type":"Answer","text":"The Prefix Sum (Optimal) approach is the most efficient, with a time complexity of O(n) and space complexity of O(1) excluding the output array."}},{"@type":"Question","name":"How does the Prefix Sum approach work?","acceptedAnswer":{"@type":"Answer","text":"It calculates the total sum of the array once. Then, while iterating, it maintains a running left sum. The right sum is derived by subtracting the left sum and the current element from the total sum."}}]}).replace(/</g,'\u003c')}
</script>