---
title: "Best Poker Hand"
summary: "Determine the best poker hand (Flush, Three of a Kind, Pair, or High Card) from 5 cards given their ranks and suits."
date: "Jul 28, 2024"
tags:
  - Array
  - Hash Table
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 61.80% | **Paid:** No
> **Topics:** Array, Hash Table, Counting

You are given an integer array ranks and a character array suits. You have 5 cards where the ith card has a rank of ranks[i] and a suit of suits[i].

The following are the types of poker hands you can make from best to worst:

1. "Flush": Five cards of the same suit.
2. "Three of a Kind": Three cards of the same rank.
3. "Pair": Two cards of the same rank.
4. "High Card": Any single card.

Return a string representing the best type of poker hand you can make with the given cards.

Note that the return values are case-sensitive.

- Examples
- Constraints
- [Hash Map Counting](#hash-map-counting)
- [Array-based Counting](#array-based-counting)

## Examples

```text
Input: ranks = [13,2,3,1,9], suits = ["a","a","a","a","a"]
Output: "Flush"
Explanation: The hand with all cards of the same suit is a flush.
```

```text
Input: ranks = [4,4,2,4,4], suits = ["d","a","a","b","c"]
Output: "Three of a Kind"
Explanation: The hand with three cards of the same rank is a three of a kind.
```

```text
Input: ranks = [10,10,2,12,9], suits = ["a","b","c","a","d"]
Output: "Pair"
Explanation: The hand with two cards of the same rank is a pair.
```

## Constraints

```text
ranks.length == suits.length == 5
1 <= ranks[i] <= 13
'a' <= suits[i] <= 'd'
```

---
## Hash Map Counting

**Intuition**
Use a hash map to count the frequency of each rank and check if all suits are identical. Evaluate hands in order of priority: Flush first, then Three of a Kind, Pair, and finally High Card.

**Steps**
- Check if all suits are the same (Flush)
- Count the frequency of each rank using a hash map
- Check if any rank appears 3 or more times (Three of a Kind)
- Check if any rank appears 2 or more times (Pair)
- Return "High Card" if none of the above conditions are met

<CodeTabs client:load tabs={[
{ name: "Python3", lang: "python", code: "from collections import Counter\nfrom typing import List\n\nclass Solution:\n    def bestHand(self, ranks: List[int], suits: List[str]) -> str:\n        # Check for Flush\n        if len(set(suits)) == 1:\n            return \\\"Flush\\\"\n        \n        # Count ranks\n        rank_count = Counter(ranks)\n        \n        # Check for Three of a Kind\n        if any(count >= 3 for count in rank_count.values()):\n            return \\\"Three of a Kind\\\"\n        \n        # Check for Pair\n        if any(count >= 2 for count in rank_count.values()):\n            return \\\"Pair\\\"\n        \n        return \\\"High Card\\\"" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public String bestHand(int[] ranks, char[] suits) {\n        // Check for Flush\n        boolean flush = true;\n        for (int i = 1; i < suits.length; i++) {\n            if (suits[i] != suits[0]) {\n                flush = false;\n                break;\n            }\n        }\n        if (flush) return \\\"Flush\\\";\n        \n        // Count ranks\n        Map<Integer, Integer> rankCount = new HashMap<>();\n        for (int rank : ranks) {\n            rankCount.put(rank, rankCount.getOrDefault(rank, 0) + 1);\n        }\n        \n        // Check for Three of a Kind\n        for (int count : rankCount.values()) {\n            if (count >= 3) return \\\"Three of a Kind\\\";\n        }\n        \n        // Check for Pair\n        for (int count : rankCount.values()) {\n            if (count >= 2) return \\\"Pair\\\";\n        }\n        \n        return \\\"High Card\\\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include <vector>\n#include <string>\n#include <unordered_map>\nusing namespace std;\n\nclass Solution {\npublic:\n    string bestHand(vector<int>& ranks, vector<char>& suits) {\n        // Check for Flush\n        bool flush = true;\n        for (int i = 1; i < suits.size(); i++) {\n            if (suits[i] != suits[0]) {\n                flush = false;\n                break;\n            }\n        }\n        if (flush) return \\\"Flush\\\";\n        \n        // Count ranks\n        unordered_map<int, int> rankCount;\n        for (int rank : ranks) {\n            rankCount[rank]++;\n        }\n        \n        // Check for Three of a Kind\n        for (auto& p : rankCount) {\n            if (p.second >= 3) return \\\"Three of a Kind\\\";\n        }\n        \n        // Check for Pair\n        for (auto& p : rankCount) {\n            if (p.second >= 2) return \\\"Pair\\\";\n        }\n        \n        return \\\"High Card\\\";\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\\n * @param {number[]} ranks\\n * @param {character[]} suits\\n * @return {string}\\n */\\nvar bestHand = function(ranks, suits) {\\n    // Check for Flush\\n    let flush = true;\\n    for (let i = 1; i < suits.length; i++) {\\n        if (suits[i] !== suits[0]) {\\n            flush = false;\\n            break;\\n        }\\n    }\\n    if (flush) return \\\"Flush\\\";\\n    \\n    // Count ranks\\n    const rankCount = new Map();\\n    for (const rank of ranks) {\\n        rankCount.set(rank, (rankCount.get(rank) || 0) + 1);\\n    }\\n    \\n    // Check for Three of a Kind\\n    for (const count of rankCount.values()) {\\n        if (count >= 3) return \\\"Three of a Kind\\\";\\n    }\\n    \\n    // Check for Pair\\n    for (const count of rankCount.values()) {\\n        if (count >= 2) return \\\"Pair\\\";\\n    }\\n    \\n    return \\\"High Card\\\";\\n};" },
{ name: "TypeScript", lang: "typescript", code: "function bestHand(ranks: number[], suits: string[]): string {\\n    // Check for Flush\\n    let flush = true;\\n    for (let i = 1; i < suits.length; i++) {\\n        if (suits[i] !== suits[0]) {\\n            flush = false;\\n            break;\\n        }\\n    }\\n    if (flush) return \\\"Flush\\\";\\n    \\n    // Count ranks\\n    const rankCount = new Map<number, number>();\\n    for (const rank of ranks) {\\n        rankCount.set(rank, (rankCount.get(rank) || 0) + 1);\\n    }\\n    \\n    // Check for Three of a Kind\\n    for (const count of rankCount.values()) {\\n        if (count >= 3) return \\\"Three of a Kind\\\";\\n    }\\n    \\n    // Check for Pair\\n    for (const count of rankCount.values()) {\\n        if (count >= 2) return \\\"Pair\\\";\\n    }\\n    \\n    return \\\"High Card\\\";\\n}" }
]} />

**Complexity**
- Time: O(n) where n is the number of cards (always 5)
- Space: O(n) for storing the rank counts
- Notes: Hash map provides O(1) average time for insert and lookup operations

---
## Array-based Counting

**Intuition**
Since ranks are limited to values 1-13, we can use a fixed-size array instead of a hash map for counting. This eliminates hash function overhead and provides direct index access.

**Steps**
- Check if all suits are the same (Flush)
- Create an array of size 14 to count rank frequencies
- Find the maximum frequency among all ranks
- Return the appropriate hand type based on the maximum frequency

<CodeTabs client:load tabs={[
{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def bestHand(self, ranks: List[int], suits: List[str]) -> str:\n        # Check for Flush\n        if len(set(suits)) == 1:\n            return \\\"Flush\\\"\n        \n        # Count ranks using array\n        rank_count = [0] * 14\n        for rank in ranks:\n            rank_count[rank] += 1\n        \n        max_count = max(rank_count)\n        \n        if max_count >= 3:\n            return \\\"Three of a Kind\\\"\n        elif max_count >= 2:\n            return \\\"Pair\\\"\n        else:\n            return \\\"High Card\\\"" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String bestHand(int[] ranks, char[] suits) {\n        // Check for Flush\n        boolean flush = true;\n        for (int i = 1; i < suits.length; i++) {\n            if (suits[i] != suits[0]) {\n                flush = false;\n                break;\n            }\n        }\n        if (flush) return \\\"Flush\\\";\n        \n        // Count ranks using array\n        int[] rankCount = new int[14];\n        for (int rank : ranks) {\n            rankCount[rank]++;\n        }\n        \n        int maxCount = 0;\n        for (int count : rankCount) {\n            maxCount = Math.max(maxCount, count);\n        }\n        \n        if (maxCount >= 3) return \\\"Three of a Kind\\\";\n        if (maxCount >= 2) return \\\"Pair\\\";\n        return \\\"High Card\\\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include <vector>\n#include <string>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    string bestHand(vector<int>& ranks, vector<char>& suits) {\n        // Check for Flush\n        bool flush = true;\n        for (int i = 1; i < suits.size(); i++) {\n            if (suits[i] != suits[0]) {\n                flush = false;\n                break;\n            }\n        }\n        if (flush) return \\\"Flush\\\";\n        \n        // Count ranks using array\n        int rankCount[14] = {0};\n        for (int rank : ranks) {\n            rankCount[rank]++;\n        }\n        \n        int maxCount = 0;\n        for (int count : rankCount) {\n            maxCount = max(maxCount, count);\n        }\n        \n        if (maxCount >= 3) return \\\"Three of a Kind\\\";\n        if (maxCount >= 2) return \\\"Pair\\\";\n        return \\\"High Card\\\";\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\\n * @param {number[]} ranks\\n * @param {character[]} suits\\n * @return {string}\\n */\\nvar bestHand = function(ranks, suits) {\\n    // Check for Flush\\n    let flush = true;\\n    for (let i = 1; i < suits.length; i++) {\\n        if (suits[i] !== suits[0]) {\\n            flush = false;\\n            break;\\n        }\\n    }\\n    if (flush) return \\\"Flush\\\";\\n    \\n    // Count ranks using array\\n    const rankCount = new Array(14).fill(0);\\n    for (const rank of ranks) {\\n        rankCount[rank]++;\\n    }\\n    \\n    let maxCount = 0;\\n    for (const count of rankCount) {\\n        maxCount = Math.max(maxCount, count);\\n    }\\n    \\n    if (maxCount >= 3) return \\\"Three of a Kind\\\";\\n    if (maxCount >= 2) return \\\"Pair\\\";\\n    return \\\"High Card\\\";\\n};" },
{ name: "TypeScript", lang: "typescript", code: "function bestHand(ranks: number[], suits: string[]): string {\\n    // Check for Flush\\n    let flush = true;\\n    for (let i = 1; i < suits.length; i++) {\\n        if (suits[i] !== suits[0]) {\\n            flush = false;\\n            break;\\n        }\\n    }\\n    if (flush) return \\\"Flush\\\";\\n    \\n    // Count ranks using array\\n    const rankCount = new Array(14).fill(0);\\n    for (const rank of ranks) {\\n        rankCount[rank]++;\\n    }\\n    \\n    let maxCount = 0;\\n    for (const count of rankCount) {\\n        maxCount = Math.max(maxCount, count);\\n    }\\n    \\n    if (maxCount >= 3) return \\\"Three of a Kind\\\";\\n    if (maxCount >= 2) return \\\"Pair\\\";\\n    return \\\"High Card\\\";\\n}" }
]} />

**Complexity**
- Time: O(n) where n is the number of cards (always 5)
- Space: O(1) since the array size is fixed at 14 regardless of input
- Notes: More memory efficient than hash map approach with better cache locality

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Best Poker Hand Solutions","datePublished":"2024-07-28","description":"Determine the best poker hand (Flush, Three of a Kind, Pair, or High Card) from 5 cards given their ranks and suits.","keywords":"LeetCode, Best Poker Hand, Easy, Array, Hash Table, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Hash Map Counting approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of cards (always 5), as we iterate through the arrays once and perform O(1) hash map operations."}},{"@type":"Question","name":"What is the space complexity of Array-based Counting approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) since we use a fixed-size array of 14 elements regardless of the input size."}},{"@type":"Question","name":"Why check for Flush before checking rank-based hands?","acceptedAnswer":{"@type":"Answer","text":"Flush is the highest priority hand type in this problem, so we must check it first to return the correct best hand."}}]}).replace(/</g,'\u003c')}
</script>