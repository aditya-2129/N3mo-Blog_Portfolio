---
title: "Sign of the Product of an Array"
summary: "Determine the sign of the product of an array of integers without calculating the actual product to avoid overflow."
date: "Mar 15, 2026"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 64.80% | **Paid:** No
> **Topics:** Array, Math

There is a function `signFunc(x)` that returns:

* `1` if `x` is positive.
* `-1` if `x` is negative.
* `0` if `x` is equal to `0`.

You are given an integer array `nums`. Let `product` be the product of all values in the array `nums`.

Return `signFunc(product)`.

- Examples
- Constraints
- [Approach 1: Direct Multiplication](#approach-1-direct-multiplication)
- [Approach 2: Counting Negatives](#approach-2-counting-negatives)

## Examples

**Example 1:**

```text
Input: nums = [-1,-2,-3,-4,3,2,1]
Output: 1
Explanation: The product of all values in the array is 144, and signFunc(144) = 1.
```

**Example 2:**

```text
Input: nums = [1,5,0,2,-3]
Output: 0
Explanation: The product of all values in the array is 0, and signFunc(0) = 0.
```

**Example 3:**

```text
Input: nums = [-1,1,-1,1,-1]
Output: -1
Explanation: The product of all values in the array is -1, and signFunc(-1) = -1.
```

## Constraints

```text
1 <= nums.length <= 1000
-100 <= nums[i] <= 100
```

---
## Approach 1: Direct Multiplication

**Intuition**
The most straightforward way to solve this problem is to calculate the product of all numbers in the array and then check its sign. This directly follows the problem statement.

**Steps**
- Initialize a variable `product` to 1.
- Iterate through the array `nums`, multiplying each element into `product`.
- After the loop, check if `product` is greater than 0 (return 1), less than 0 (return -1), or equal to 0 (return 0).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def arraySign(self, nums: list[int]) -&gt; int:\n        product = 1\n        for num in nums:\n            product *= num\n        if product &gt; 0:\n            return 1\n        elif product &lt; 0:\n            return -1\n        else:\n            return 0" },
{ name: "Java", lang: "java", code: "import java.math.BigInteger;\n\nclass Solution {\n    public int arraySign(int[] nums) {\n        BigInteger product = BigInteger.ONE;\n        for (int num : nums) {\n            product = product.multiply(BigInteger.valueOf(num));\n        }\n        return product.signum();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int arraySign(vector&lt;int&gt;& nums) {\n        long long product = 1;\n        for (int num : nums) {\n            product *= num;\n        }\n        if (product &gt; 0) return 1;\n        if (product &lt; 0) return -1;\n        return 0;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var arraySign = function(nums) {\n    let product = 1n;\n    for (let num of nums) {\n        product *= BigInt(num);\n    }\n    if (product &gt; 0n) return 1;\n    if (product &lt; 0n) return -1;\n    return 0;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function arraySign(nums: number[]): number {\n    let product: bigint = 1n;\n    for (let num of nums) {\n        product *= BigInt(num);\n    }\n    if (product &gt; 0n) return 1;\n    if (product &lt; 0n) return -1;\n    return 0;\n}" },]} />

**Complexity**
- Time: O(n) - We iterate through the array once.
- Space: O(1) - We only use a single variable to store the product (ignoring the space for the BigInt object itself in languages like Java/JS).
- Notes: In languages with fixed-size integers (C++, Java with `int`), this approach risks overflow because the product can grow up to 100¹⁰⁰⁰, which exceeds the limit of a 64-bit integer (2⁶³-1). To make it runnable in Java and JavaScript, we use arbitrary-precision integers (`BigInteger` and `BigInt`). In C++, `long long` will overflow for large inputs, making this approach technically incorrect for the upper constraint limits without a big-integer library.

---
## Approach 2: Counting Negatives

**Intuition**
We can determine the sign of a product without calculating the product itself. The sign is positive if the number of negative integers is even, and negative if the count is odd. If any number is zero, the product is immediately zero.

**Steps**
- Initialize a counter `negCount` to 0.
- Iterate through the array `nums`:
  - If the current number is 0, return 0 immediately.
  - If the current number is negative, increment `negCount`.
- After the loop, if `negCount` is even, return 1. Otherwise, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def arraySign(self, nums: list[int]) -&gt; int:\n        neg_count = 0\n        for num in nums:\n            if num == 0:\n                return 0\n            if num &lt; 0:\n                neg_count += 1\n        return -1 if neg_count % 2 else 1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int arraySign(int[] nums) {\n        int negCount = 0;\n        for (int num : nums) {\n            if (num == 0) return 0;\n            if (num &lt; 0) negCount++;\n        }\n        return negCount % 2 == 0 ? 1 : -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int arraySign(vector&lt;int&gt;& nums) {\n        int negCount = 0;\n        for (int num : nums) {\n            if (num == 0) return 0;\n            if (num &lt; 0) negCount++;\n        }\n        return negCount % 2 == 0 ? 1 : -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var arraySign = function(nums) {\n    let negCount = 0;\n    for (let num of nums) {\n        if (num === 0) return 0;\n        if (num &lt; 0) negCount++;\n    }\n    return negCount % 2 === 0 ? 1 : -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function arraySign(nums: number[]): number {\n    let negCount: number = 0;\n    for (let num of nums) {\n        if (num === 0) return 0;\n        if (num &lt; 0) negCount++;\n    }\n    return negCount % 2 === 0 ? 1 : -1;\n}" },]} />

**Complexity**
- Time: O(n) - We traverse the array once.
- Space: O(1) - We only use a single integer counter.
- Notes: This is the optimal approach as it avoids any potential overflow issues and uses minimal memory.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Sign of the Product of an Array Solutions","datePublished":"2026-03-15","description":"Determine the sign of the product of an array of integers without calculating the actual product to avoid overflow.","keywords":"LeetCode, Sign of the Product of an Array, Easy, Array, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Approach 1: Direct Multiplication?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of Approach 2: Counting Negatives?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a single variable to count negative numbers."}},{"@type":"Question","name":"Why is Approach 2 preferred over Approach 1?","acceptedAnswer":{"@type":"Answer","text":"Approach 2 is preferred because it avoids integer overflow issues that can occur with Approach 1 when the product of array elements exceeds the maximum value of standard integer types."}}]}).replace(/</g,'\u003c')}
</script>