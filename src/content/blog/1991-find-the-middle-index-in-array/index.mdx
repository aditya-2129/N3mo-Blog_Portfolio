---
title: "Find the Middle Index in Array"
summary: "Find the leftmost index where the sum of elements to the left equals the sum of elements to the right."
date: "Aug 07, 2025"
tags:
  - Array
  - Prefix Sum
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 69.60% | **Paid:** No
> **Topics:** Array, Prefix Sum

Given a 0-indexed integer array nums, find the leftmost middleIndex (i.e., the smallest amongst all the possible ones).

A middleIndex is an index where the sum of all the numbers strictly to its left is equal to the sum of all the numbers strictly to its right.

If the sum to the left is 0, it is valid. If the sum to the right is 0, it is valid.

If no such index exists, return -1.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Prefix Sum](#prefix-sum)

## Examples

**Example 1:**

```text
Input: nums = [2,3,-1,8,4]
Output: 3
Explanation:
The middle index is 3.
Left sum = nums[0] + nums[1] + nums[2] = 2 + 3 + -1 = 4
Right sum = nums[4] = 4
```

**Example 2:**

```text
Input: nums = [1,-1,4]
Output: 2
Explanation:
The middle index is 2.
Left sum = nums[0] + nums[1] = 1 + -1 = 0
Right sum = 0
```

**Example 3:**

```text
Input: nums = [2,5]
Output: -1
Explanation:
There is no valid middle index.
```

## Constraints

```text
1 <= nums.length <= 100
-1000 <= nums[i] <= 1000
```

---
## Brute Force

**Intuition**
Iterate through each index and calculate the sum of elements to the left and right separately to check for equality.

**Steps**
- Iterate through the array with index `i` from 0 to `n-1`.
- Calculate the sum of elements from index 0 to `i-1` (left sum).
- Calculate the sum of elements from index `i+1` to `n-1` (right sum).
- If left sum equals right sum, return `i`.
- If the loop finishes without finding a valid index, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def findMiddleIndex(self, nums: List[int]) -&gt; int:\n        n = len(nums)\n        for i in range(n):\n            left_sum = sum(nums[:i])\n            right_sum = sum(nums[i+1:])\n            if left_sum == right_sum:\n                return i\n        return -1\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int findMiddleIndex(int[] nums) {\n        int n = nums.length;\n        for (int i = 0; i &lt; n; i++) {\n            int leftSum = 0;\n            for (int j = 0; j &lt; i; j++) {\n                leftSum += nums[j];\n            }\n            int rightSum = 0;\n            for (int j = i + 1; j &lt; n; j++) {\n                rightSum += nums[j];\n            }\n            if (leftSum == rightSum) {\n                return i;\n            }\n        }\n        return -1;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    int findMiddleIndex(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        for (int i = 0; i &lt; n; ++i) {\n            int leftSum = 0;\n            for (int j = 0; j &lt; i; ++j) {\n                leftSum += nums[j];\n            }\n            int rightSum = 0;\n            for (int j = i + 1; j &lt; n; ++j) {\n                rightSum += nums[j];\n            }\n            if (leftSum == rightSum) {\n                return i;\n            }\n        }\n        return -1;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var findMiddleIndex = function(nums) {\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        let leftSum = 0;\n        for (let j = 0; j &lt; i; j++) {\n            leftSum += nums[j];\n        }\n        let rightSum = 0;\n        for (let j = i + 1; j &lt; n; j++) {\n            rightSum += nums[j];\n        }\n        if (leftSum === rightSum) {\n            return i;\n        }\n    }\n    return -1;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function findMiddleIndex(nums: number[]): number {\n    const n = nums.length;\n    for (let i = 0; i &lt; n; i++) {\n        let leftSum = 0;\n        for (let j = 0; j &lt; i; j++) {\n            leftSum += nums[j];\n        }\n        let rightSum = 0;\n        for (let j = i + 1; j &lt; n; j++) {\n            rightSum += nums[j];\n        }\n        if (leftSum === rightSum) {\n            return i;\n        }\n    }\n    return -1;\n}\n" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1)
- Notes: Simple to implement but inefficient for large arrays due to repeated summation.

---
## Prefix Sum

**Intuition**
Calculate the total sum of the array once. As we iterate, maintain a running left sum. The right sum can be derived by subtracting the left sum and the current element from the total sum.

**Steps**
- Calculate the total sum of the array.
- Initialize `leftSum` to 0.
- Iterate through the array:
    - Calculate `rightSum` as `totalSum - leftSum - nums[i]`.
    - If `leftSum` equals `rightSum`, return `i`.
    - Add `nums[i]` to `leftSum`.
- If the loop completes, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\n\nclass Solution:\n    def findMiddleIndex(self, nums: List[int]) -&gt; int:\n        total_sum = sum(nums)\n        left_sum = 0\n        for i, num in enumerate(nums):\n            if left_sum == total_sum - left_sum - num:\n                return i\n            left_sum += num\n        return -1\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int findMiddleIndex(int[] nums) {\n        int totalSum = 0;\n        for (int num : nums) {\n            totalSum += num;\n        }\n        int leftSum = 0;\n        for (int i = 0; i &lt; nums.length; i++) {\n            if (leftSum == totalSum - leftSum - nums[i]) {\n                return i;\n            }\n            leftSum += nums[i];\n        }\n        return -1;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;numeric&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    int findMiddleIndex(vector&lt;int&gt;& nums) {\n        int totalSum = accumulate(nums.begin(), nums.end(), 0);\n        int leftSum = 0;\n        for (int i = 0; i &lt; nums.size(); ++i) {\n            if (leftSum == totalSum - leftSum - nums[i]) {\n                return i;\n            }\n            leftSum += nums[i];\n        }\n        return -1;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var findMiddleIndex = function(nums) {\n    const totalSum = nums.reduce((a, b) =&gt; a + b, 0);\n    let leftSum = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (leftSum === totalSum - leftSum - nums[i]) {\n            return i;\n        }\n        leftSum += nums[i];\n    }\n    return -1;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function findMiddleIndex(nums: number[]): number {\n    const totalSum = nums.reduce((a, b) =&gt; a + b, 0);\n    let leftSum = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (leftSum === totalSum - leftSum - nums[i]) {\n            return i;\n        }\n        leftSum += nums[i];\n    }\n    return -1;\n}\n" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal solution with a single pass through the array.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find the Middle Index in Array Solutions","datePublished":"2025-08-07","description":"Find the leftmost index where the sum of elements to the left equals the sum of elements to the right.","keywords":"LeetCode, Find the Middle Index in Array, Easy, Array, Prefix Sum"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n²), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the time complexity of the Prefix Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Prefix Sum approach is O(n), as we traverse the array only once."}}]}).replace(/</g,'\u003c')}
</script>