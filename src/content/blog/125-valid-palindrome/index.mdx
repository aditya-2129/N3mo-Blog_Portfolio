---
title: "Valid Palindrome"
summary: "Check if a string is a palindrome considering only alphanumeric characters and ignoring case."
date: "Sep 05, 2024"
tags:
  - Two Pointers
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 53.20% | **Paid:** No
> **Topics:** Two Pointers, String

A phrase is a palindrome if, after converting all uppercase letters into lowercase letters and removing all non-alphanumeric characters, it reads the same forward and backward. Alphanumeric characters include letters and numbers.

Given a string s, return true if it is a palindrome, or false otherwise.

- Examples
- Constraints
- [Two Pointers](#two-pointers)
- [Filter and Reverse](#filter-and-reverse)

## Examples

```text
Input: s = "A man, a plan, a canal: Panama"
Output: true
Explanation: "amanaplanacanalpanama" is a palindrome.
```

```text
Input: s = "race a car"
Output: false
Explanation: "raceacar" is not a palindrome.
```

```text
Input: s = " "
Output: true
Explanation: s is an empty string "" after removing non-alphanumeric characters.
Since an empty string reads the same forward and backward, it is a palindrome.
```

## Constraints

```text
1 <= s.length <= 2 * 10⁵
s consists only of printable ASCII characters.
```

---
## Two Pointers

**Intuition**
Use two pointers starting from the beginning and end of the string. Move them towards each other, skipping non-alphanumeric characters, and compare the lowercased characters.

**Steps**
- Initialize `left` pointer at 0 and `right` pointer at `s.length - 1`.
- Loop while `left &lt; right`.
- Increment `left` while it points to a non-alphanumeric character.
- Decrement `right` while it points to a non-alphanumeric character.
- Compare `s[left]` and `s[right]` (case-insensitive). If they differ, return false.
- Move both pointers inward.
- If the loop finishes, return true.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def isPalindrome(self, s: str) -> bool:\n        left, right = 0, len(s) - 1\n        while left < right:\n            while left < right and not s[left].isalnum():\n                left += 1\n            while left < right and not s[right].isalnum():\n                right -= 1\n            if s[left].lower() != s[right].lower():\n                return False\n            left += 1\n            right -= 1\n        return True' },
{ name: "Java", lang: "java", code: 'class Solution {\n    public boolean isPalindrome(String s) {\n        int left = 0, right = s.length() - 1;\n        while (left &lt; right) {\n            while (left &lt; right && !Character.isLetterOrDigit(s.charAt(left))) {\n                left++;\n            }\n            while (left &lt; right && !Character.isLetterOrDigit(s.charAt(right))) {\n                right--;\n            }\n            if (Character.toLowerCase(s.charAt(left)) != Character.toLowerCase(s.charAt(right))) {\n                return false;\n            }\n            left++;\n            right--;\n        }\n        return true;\n    }\n}' },
{ name: "C++", lang: "cpp", code: 'class Solution {\npublic:\n    bool isPalindrome(string s) {\n        int left = 0, right = s.size() - 1;\n        while (left &lt; right) {\n            while (left &lt; right && !isalnum(s[left])) {\n                left++;\n            }\n            while (left &lt; right && !isalnum(s[right])) {\n                right--;\n            }\n            if (tolower(s[left]) != tolower(s[right])) {\n                return false;\n            }\n            left++;\n            right--;\n        }\n        return true;\n    }\n};' },
{ name: "JavaScript", lang: "javascript", code: '/**\n * @param {string} s\n * @return {boolean}\n */\nvar isPalindrome = function(s) {\n    let left = 0, right = s.length - 1;\n    const isAlphaNum = (c) =&gt; {\n        return (c &gt;= "a" && c &lt;= "z") || (c &gt;= "A" && c &lt;= "Z") || (c &gt;= "0" && c &lt;= "9");\n    };\n    while (left &lt; right) {\n        while (left &lt; right && !isAlphaNum(s[left])) {\n            left++;\n        }\n        while (left &lt; right && !isAlphaNum(s[right])) {\n            right--;\n        }\n        if (s[left].toLowerCase() !== s[right].toLowerCase()) {\n            return false;\n        }\n        left++;\n        right--;\n    }\n    return true;\n};' },
{ name: "TypeScript", lang: "typescript", code: 'function isPalindrome(s: string): boolean {\n    let left = 0, right = s.length - 1;\n    const isAlphaNum = (c: string): boolean =&gt; {\n        return (c &gt;= "a" && c &lt;= "z") || (c &gt;= "A" && c &lt;= "Z") || (c &gt;= "0" && c &lt;= "9");\n    };\n    while (left &lt; right) {\n        while (left &lt; right && !isAlphaNum(s[left])) {\n            left++;\n        }\n        while (left &lt; right && !isAlphaNum(s[right])) {\n            right--;\n        }\n        if (s[left].toLowerCase() !== s[right].toLowerCase()) {\n            return false;\n        }\n        left++;\n        right--;\n    }\n    return true;\n}' },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal solution with constant extra space.

---
## Filter and Reverse

**Intuition**
Create a new string containing only alphanumeric characters converted to lowercase. Check if this new string is equal to its reverse.

**Steps**
- Iterate through the original string `s`.
- If a character is alphanumeric, convert it to lowercase and append it to a new string `cleaned`.
- Reverse the `cleaned` string.
- Compare `cleaned` with its reverse. Return true if equal, false otherwise.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def isPalindrome(self, s: str) -> bool:\n        cleaned = "".join(ch.lower() for ch in s if ch.isalnum())\n        return cleaned == cleaned[::-1]' },
{ name: "Java", lang: "java", code: 'class Solution {\n    public boolean isPalindrome(String s) {\n        StringBuilder sb = new StringBuilder();\n        for (char c : s.toCharArray()) {\n            if (Character.isLetterOrDigit(c)) {\n                sb.append(Character.toLowerCase(c));\n            }\n        }\n        String cleaned = sb.toString();\n        String reversed = sb.reverse().toString();\n        return cleaned.equals(reversed);\n    }\n}' },
{ name: "C++", lang: "cpp", code: 'class Solution {\npublic:\n    bool isPalindrome(string s) {\n        string cleaned;\n        for (char c : s) {\n            if (isalnum(c)) {\n                cleaned += tolower(c);\n            }\n        }\n        string rev = cleaned;\n        reverse(rev.begin(), rev.end());\n        return cleaned == rev;\n    }\n};' },
{ name: "JavaScript", lang: "javascript", code: '/**\n * @param {string} s\n * @return {boolean}\n */\nvar isPalindrome = function(s) {\n    const cleaned = s.replace(/[^a-zA-Z0-9]/g, "").toLowerCase();\n    const reversed = cleaned.split("").reverse().join("");\n    return cleaned === reversed;\n};' },
{ name: "TypeScript", lang: "typescript", code: 'function isPalindrome(s: string): boolean {\n    const cleaned = s.replace(/[^a-zA-Z0-9]/g, "").toLowerCase();\n    const reversed = cleaned.split("").reverse().join("");\n    return cleaned === reversed;\n}' },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Uses extra space to store the cleaned string, but logic is very simple.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Valid Palindrome Solutions","datePublished":"2024-09-05","description":"Check if a string is a palindrome considering only alphanumeric characters and ignoring case.","keywords":"LeetCode, Valid Palindrome, Easy, Two Pointers, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"O(n), where n is the length of the string. We traverse the string once."}},{"@type":"Question","name":"What is the space complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"O(1). We only use two pointers for extra space."}},{"@type":"Question","name":"What is the time complexity of the Filter and Reverse approach?","acceptedAnswer":{"@type":"Answer","text":"O(n). We iterate through the string to filter and then reverse it."}},{"@type":"Question","name":"What is the space complexity of the Filter and Reverse approach?","acceptedAnswer":{"@type":"Answer","text":"O(n). We create a new string to store the filtered characters."}}]}).replace(/</g,'\u003c')}
</script>