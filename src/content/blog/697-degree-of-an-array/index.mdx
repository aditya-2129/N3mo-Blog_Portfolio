---
title: "Degree of an Array"
summary: "Find the smallest length of a contiguous subarray that has the same degree as the original array."
date: "May 20, 2024"
tags:
  - Array
  - Hash Table
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 58.30% | **Paid:** No
> **Topics:** Array, Hash Table

Given a non-empty array of non-negative integers nums, the degree of this array is defined as the maximum frequency of any one of its elements.

Your task is to find the smallest possible length of a (contiguous) subarray of nums, that has the same degree as nums.

- Examples
- Constraints
- [Hash Map with First and Last Index](#hash-map-with-first-and-last-index)
- [Single Pass Tracking](#single-pass-tracking)

## Examples

### Example 1

**Input:**
```text
nums = [1,2,2,3,1]
```

**Output:**
```text
2
```

**Explanation:**
The input array has a degree of 2 because both elements 1 and 2 appear twice.
Of the subarrays that have the same degree:
[1, 2, 2, 3, 1], [1, 2, 2, 3], [2, 2, 3, 1], [1, 2, 2], [2, 2, 3], [2, 2]
The shortest length is 2. So return 2.

### Example 2

**Input:**
```text
nums = [1,2,2,3,1,4,2]
```

**Output:**
```text
6
```

**Explanation:**
The degree is 3 because the element 2 is repeated 3 times.
So the subarray [2,2,3,1,4,2] starting from index 1 and ending at index 6 has the same degree as nums.
The length of this subarray is 6.

## Constraints

```text
- nums.length will be between 1 and 50,000.
- nums[i] will be an integer between 0 and 49,999.
```

---

## Hash Map with First and Last Index

**Intuition**
Track the frequency, first occurrence, and last occurrence of each element using hash maps, then find the minimum length among elements with maximum frequency.

**Steps**
- Create three hash maps: count, firstIndex, and lastIndex
- Iterate through the array, updating count and recording first/last indices
- Find the maximum frequency (degree)
- For all elements with this frequency, calculate the span (lastIndex - firstIndex + 1)
- Return the minimum span

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def findShortestSubArray(self, nums):
        count = {}
        first = {}
        last = {}
        
        for i, num in enumerate(nums):
            if num not in first:
                first[num] = i
            last[num] = i
            count[num] = count.get(num, 0) + 1
        
        degree = max(count.values())
        min_length = float('inf')
        
        for num in count:
            if count[num] == degree:
                min_length = min(min_length, last[num] - first[num] + 1)
        
        return min_length
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int findShortestSubArray(int[] nums) {
        Map<Integer, Integer> count = new HashMap&lt;&gt;();
        Map<Integer, Integer> first = new HashMap&lt;&gt;();
        Map<Integer, Integer> last = new HashMap&lt;&gt;();
        
        for (int i = 0; i &lt; nums.length; i++) {
            int num = nums[i];
            first.putIfAbsent(num, i);
            last.put(num, i);
            count.put(num, count.getOrDefault(num, 0) + 1);
        }
        
        int degree = 0;
        for (int c : count.values()) {
            degree = Math.max(degree, c);
        }
        
        int minLength = Integer.MAX_VALUE;
        for (int num : count.keySet()) {
            if (count.get(num) == degree) {
                minLength = Math.min(minLength, last.get(num) - first.get(num) + 1);
            }
        }
        
        return minLength;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int findShortestSubArray(vector<int>& nums) {
        unordered_map<int, int> count, first, last;
        
        for (int i = 0; i &lt; nums.size(); i++) {
            if (first.find(nums[i]) == first.end()) {
                first[nums[i]] = i;
            }
            last[nums[i]] = i;
            count[nums[i]]++;
        }
        
        int degree = 0;
        for (auto& p : count) {
            degree = max(degree, p.second);
        }
        
        int minLength = INT_MAX;
        for (auto& p : count) {
            if (p.second == degree) {
                minLength = min(minLength, last[p.first] - first[p.first] + 1);
            }
        }
        
        return minLength;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @return {number}
 */
var findShortestSubArray = function(nums) {
    const count = {};
    const first = {};
    const last = {};
    
    for (let i = 0; i &lt; nums.length; i++) {
        const num = nums[i];
        if (first[num] === undefined) {
            first[num] = i;
        }
        last[num] = i;
        count[num] = (count[num] || 0) + 1;
    }
    
    const degree = Math.max(...Object.values(count));
    let minLength = Infinity;
    
    for (const num in count) {
        if (count[num] === degree) {
            minLength = Math.min(minLength, last[num] - first[num] + 1);
        }
    }
    
    return minLength;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function findShortestSubArray(nums: number[]): number {
    const count: Record<number, number> = {};
    const first: Record<number, number> = {};
    const last: Record<number, number> = {};
    
    for (let i = 0; i &lt; nums.length; i++) {
        const num = nums[i];
        if (first[num] === undefined) {
            first[num] = i;
        }
        last[num] = i;
        count[num] = (count[num] || 0) + 1;
    }
    
    const degree = Math.max(...Object.values(count));
    let minLength = Infinity;
    
    for (const num in count) {
        if (count[num] === degree) {
            minLength = Math.min(minLength, last[num] - first[num] + 1);
        }
    }
    
    return minLength;
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(n) for the hash maps
- Notes: Clean and readable solution with three hash maps

---

## Single Pass Tracking

**Intuition**
Track count, first index, and last index in a single pass while maintaining the current degree and minimum length.

**Steps**
- Use a single hash map to store count, first index, and last index for each element
- During iteration, update all three values
- Keep track of the current degree and minimum length
- Return the minimum length found

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def findShortestSubArray(self, nums):
        info = {}
        degree = 0
        min_length = float('inf')
        
        for i, num in enumerate(nums):
            if num not in info:
                info[num] = [1, i, i]  # [count, first, last]
            else:
                info[num][0] += 1
                info[num][2] = i
            
            count, first, last = info[num]
            
            if count &gt; degree:
                degree = count
                min_length = last - first + 1
            elif count == degree:
                min_length = min(min_length, last - first + 1)
        
        return min_length
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int findShortestSubArray(int[] nums) {
        Map<Integer, int[]&gt; info = new HashMap&lt;&gt;();
        int degree = 0;
        int minLength = Integer.MAX_VALUE;
        
        for (int i = 0; i &lt; nums.length; i++) {
            int num = nums[i];
            if (!info.containsKey(num)) {
                info.put(num, new int[]{1, i, i}); // [count, first, last]
            } else {
                info.get(num)[0]++;
                info.get(num)[2] = i;
            }
            
            int[] data = info.get(num);
            int count = data[0];
            int first = data[1];
            int last = data[2];
            
            if (count &gt; degree) {
                degree = count;
                minLength = last - first + 1;
            } else if (count == degree) {
                minLength = Math.min(minLength, last - first + 1);
            }
        }
        
        return minLength;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int findShortestSubArray(vector<int>& nums) {
        unordered_map<int, vector<int>&gt; info;
        int degree = 0;
        int minLength = INT_MAX;
        
        for (int i = 0; i &lt; nums.size(); i++) {
            int num = nums[i];
            if (info.find(num) == info.end()) {
                info[num] = {1, i, i}; // [count, first, last]
            } else {
                info[num][0]++;
                info[num][2] = i;
            }
            
            int count = info[num][0];
            int first = info[num][1];
            int last = info[num][2];
            
            if (count &gt; degree) {
                degree = count;
                minLength = last - first + 1;
            } else if (count == degree) {
                minLength = min(minLength, last - first + 1);
            }
        }
        
        return minLength;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[]} nums
 * @return {number}
 */
var findShortestSubArray = function(nums) {
    const info = {};
    let degree = 0;
    let minLength = Infinity;
    
    for (let i = 0; i &lt; nums.length; i++) {
        const num = nums[i];
        if (info[num] === undefined) {
            info[num] = [1, i, i]; // [count, first, last]
        } else {
            info[num][0]++;
            info[num][2] = i;
        }
        
        const [count, first, last] = info[num];
        
        if (count &gt; degree) {
            degree = count;
            minLength = last - first + 1;
        } else if (count === degree) {
            minLength = Math.min(minLength, last - first + 1);
        }
    }
    
    return minLength;
};
` },
{ name: "TypeScript", lang: "typescript", code: `function findShortestSubArray(nums: number[]): number {
    const info: Record<number, [number, number, number]&gt; = {};
    let degree = 0;
    let minLength = Infinity;
    
    for (let i = 0; i &lt; nums.length; i++) {
        const num = nums[i];
        if (info[num] === undefined) {
            info[num] = [1, i, i]; // [count, first, last]
        } else {
            info[num][0]++;
            info[num][2] = i;
        }
        
        const [count, first, last] = info[num];
        
        if (count &gt; degree) {
            degree = count;
            minLength = last - first + 1;
        } else if (count === degree) {
            minLength = Math.min(minLength, last - first + 1);
        }
    }
    
    return minLength;
}
` },]} />

**Complexity**
- Time: O(n) where n is the length of the array
- Space: O(n) for the hash map
- Notes: Single pass solution that updates the answer dynamically

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Degree of an Array Solutions","datePublished":"2024-05-20","description":"Find the smallest length of a contiguous subarray that has the same degree as the original array.","keywords":"LeetCode, Degree of an Array, Easy, Array, Hash Table"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Hash Map approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the array, as we iterate through the array once and then through the unique elements."}},{"@type":"Question","name":"What is the space complexity of the Single Pass Tracking approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) for storing the hash map with count, first index, and last index for each unique element."}}]}).replace(/</g,'\u003c')}
</script>