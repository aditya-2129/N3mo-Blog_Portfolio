---
title: "To Be Or Not To Be"
summary: "Implement an expect function that returns an object with toBe and notToBe methods for value comparison."
date: "Aug 21, 2025"
tags:
  - JavaScript
  - TypeScript
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 63.30% | **Paid:** No
> **Topics:** N/A

Write a function expect that helps developers test their code. It should take in any value val and return an object with the following two functions.

toBe(val) accepts another value and returns true if the two values === each other. If they are not equal, it should throw an error "Not Equal".

notToBe(val) accepts another value and returns true if the two values !== each other. If they are equal, it should throw an error "Equal".

- Examples
- Constraints
- [Object Return Approach](#object-return-approach)
- [Class-based Approach](#class-based-approach)

## Examples

### Example 1

**Input:**
```text
func = () => expect(5).toBe(5)
```

**Output:**
```text
{"value": true}
```

**Explanation:**
5 === 5 so this expression returns true.

### Example 2

**Input:**
```text
func = () => expect(5).toBe(null)
```

**Output:**
```text
{"error": "Not Equal"}
```

**Explanation:**
5 !== null so this expression throw the error "Not Equal".

### Example 3

**Input:**
```text
func = () => expect(5).notToBe(null)
```

**Output:**
```text
{"value": true}
```

**Explanation:**
5 !== null so this expression returns true.

## Constraints

```text
val is a primitive value (string, number, boolean, null, undefined, symbol)
```

---

## Object Return Approach

**Intuition**
Return an object containing two methods that compare the original value with the passed value using strict equality operators.

**Steps**
- Create an expect function that takes a value
- Return an object with toBe and notToBe methods
- toBe checks strict equality (===) and throws "Not Equal" if false
- notToBe checks strict inequality (!==) and throws "Equal" if false

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Expect:\n    def __init__(self, val):\n        self.val = val\n    \n    def toBe(self, other):\n        if self.val == other:\n            return True\n        raise Exception(\"Not Equal\")\n    \n    def notToBe(self, other):\n        if self.val != other:\n            return True\n        raise Exception(\"Equal\")\n\ndef expect(val):\n    return Expect(val)" },
{ name: "Java", lang: "java", code: "class Expect {\n    private Object val;\n    \n    public Expect(Object val) {\n        this.val = val;\n    }\n    \n    public boolean toBe(Object other) {\n        if (val == other || (val != null && val.equals(other))) {\n            return true;\n        }\n        throw new Error(\"Not Equal\");\n    }\n    \n    public boolean notToBe(Object other) {\n        if (val != other && (val == null || !val.equals(other))) {\n            return true;\n        }\n        throw new Error(\"Equal\");\n    }\n}\n\nclass Solution {\n    public Expect expect(Object val) {\n        return new Expect(val);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include <string>\n#include <stdexcept>\n\nusing namespace std;\n\nclass Expect {\nprivate:\n    string val;\n    \npublic:\n    Expect(string v) : val(v) {}\n    \n    bool toBe(string other) {\n        if (val == other) {\n            return true;\n        }\n        throw runtime_error(\"Not Equal\");\n    }\n    \n    bool notToBe(string other) {\n        if (val != other) {\n            return true;\n        }\n        throw runtime_error(\"Equal\");\n    }\n};\n\nclass Solution {\npublic:\n    Expect expect(string val) {\n        return Expect(val);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "function expect(val) {\n    return {\n        toBe: function(other) {\n            if (val === other) {\n                return true;\n            }\n            throw new Error(\"Not Equal\");\n        },\n        notToBe: function(other) {\n            if (val !== other) {\n                return true;\n            }\n            throw new Error(\"Equal\");\n        }\n    };\n}" },
{ name: "TypeScript", lang: "typescript", code: "type ExpectMethods = {\n    toBe: (other: any) => boolean;\n    notToBe: (other: any) => boolean;\n};\n\nfunction expect(val: any): ExpectMethods {\n    return {\n        toBe: function(other: any): boolean {\n            if (val === other) {\n                return true;\n            }\n            throw new Error(\"Not Equal\");\n        },\n        notToBe: function(other: any): boolean {\n            if (val !== other) {\n                return true;\n            }\n            throw new Error(\"Equal\");\n        }\n    };\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Simple and direct approach with minimal overhead

---

## Class-based Approach

**Intuition**
Use a class to encapsulate the expected value and provide comparison methods as class methods.

**Steps**
- Define a class that stores the expected value
- Implement toBe and notToBe as instance methods
- Create and return an instance of this class from the expect function

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Expect:\n    def __init__(self, val):\n        self.val = val\n    \n    def toBe(self, other):\n        if self.val is other or self.val == other:\n            return True\n        raise Exception(\"Not Equal\")\n    \n    def notToBe(self, other):\n        if self.val is not other and self.val != other:\n            return True\n        raise Exception(\"Equal\")\n\ndef expect(val):\n    return Expect(val)" },
{ name: "Java", lang: "java", code: "class Expect {\n    private Object val;\n    \n    public Expect(Object val) {\n        this.val = val;\n    }\n    \n    public boolean toBe(Object other) {\n        if (val == other || (val != null && val.equals(other))) {\n            return true;\n        }\n        throw new Error(\"Not Equal\");\n    }\n    \n    public boolean notToBe(Object other) {\n        if (val != other && (val == null || !val.equals(other))) {\n            return true;\n        }\n        throw new Error(\"Equal\");\n    }\n}\n\nclass Solution {\n    public Expect expect(Object val) {\n        return new Expect(val);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include <string>\n#include <stdexcept>\n\nusing namespace std;\n\nclass Expect {\nprivate:\n    string val;\n    \npublic:\n    Expect(string v) : val(v) {}\n    \n    bool toBe(string other) {\n        if (val == other) {\n            return true;\n        }\n        throw runtime_error(\"Not Equal\");\n    }\n    \n    bool notToBe(string other) {\n        if (val != other) {\n            return true;\n        }\n        throw runtime_error(\"Equal\");\n    }\n};\n\nclass Solution {\npublic:\n    Expect expect(string val) {\n        return Expect(val);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Expect {\n    constructor(val) {\n        this.val = val;\n    }\n    \n    toBe(other) {\n        if (this.val === other) {\n            return true;\n        }\n        throw new Error(\"Not Equal\");\n    }\n    \n    notToBe(other) {\n        if (this.val !== other) {\n            return true;\n        }\n        throw new Error(\"Equal\");\n    }\n}\n\nfunction expect(val) {\n    return new Expect(val);\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Expect {\n    private val: any;\n    \n    constructor(val: any) {\n        this.val = val;\n    }\n    \n    toBe(other: any): boolean {\n        if (this.val === other) {\n            return true;\n        }\n        throw new Error(\"Not Equal\");\n    }\n    \n    notToBe(other: any): boolean {\n        if (this.val !== other) {\n            return true;\n        }\n        throw new Error(\"Equal\");\n    }\n}\n\nfunction expect(val: any): Expect {\n    return new Expect(val);\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: More structured approach, useful for extending with additional methods

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode To Be Or Not To Be Solutions","datePublished":"2025-08-21","description":"Implement an expect function that returns an object with toBe and notToBe methods for value comparison.","keywords":"LeetCode, To Be Or Not To Be, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Object Return Approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Simple comparison operations"}},{"@type":"Question","name":"What is the space complexity of the Class-based Approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Only stores the reference value"}},{"@type":"Question","name":"What operators are used for comparison?","acceptedAnswer":{"@type":"Answer","text":"Strict equality (===) and strict inequality (!==) operators"}}]}).replace(/</g,'\u003c')}
</script>