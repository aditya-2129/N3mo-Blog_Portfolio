---
title: "Minimum Number of Flips to Reverse Binary String"
summary: "Find the minimum number of bit flips needed to make a binary string equal to its reverse."
date: "Jul 19, 2025"
tags:
  - Math
  - Two Pointers
  - String
  - Bit Manipulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 76.40% | **Paid:** No
> **Topics:** Math, Two Pointers, String, Bit Manipulation

You are given a binary string s. Return the minimum number of flips needed to make s equal to its reverse. A flip changes a '0' to '1' or a '1' to '0'.

- Examples
- Constraints
- [Two Pointers](#two-pointers)
- [Direct Comparison](#direct-comparison)
- [Iterative](#iterative)

## Examples

### Example 1

**Input:**
```text
n = 7
```

**Output:**
```text
0
```

**Explanation:**
The binary representation of 7 is "111". Its reverse is also "111", which is the same. Hence, no flips are needed.

### Example 2

**Input:**
```text
n = 10
```

**Output:**
```text
4
```

**Explanation:**
The binary representation of 10 is "1010". Its reverse is "0101". All four bits must be flipped to make them equal. Thus, the minimum number of flips required is 4.

## Constraints

```text
1 <= s.length <= 10⁵
s[i] is either '0' or '1'.
```

## Two Pointers

**Intuition**
Use two pointers starting from both ends of the string, comparing characters and counting mismatches until they meet in the middle.

**Steps**
- Initialize left = 0, right = n - 1, and flips = 0
- While left &lt; right:
  - If s[left] != s[right], increment flips
  - Move left right and right left
- Return flips

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def minFlips(self, s: str) -> int:
        n = len(s)
        left, right = 0, n - 1
        flips = 0
        
        while left &lt; right:
            if s[left] != s[right]:
                flips += 1
            left += 1
            right -= 1
        
        return flips` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minFlips(String s) {
        int n = s.length();
        int left = 0, right = n - 1;
        int flips = 0;
        
        while (left &lt; right) {
            if (s.charAt(left) != s.charAt(right)) {
                flips++;
            }
            left++;
            right--;
        }
        
        return flips;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minFlips(string s) {
        int n = s.length();
        int left = 0, right = n - 1;
        int flips = 0;
        
        while (left &lt; right) {
            if (s[left] != s[right]) {
                flips++;
            }
            left++;
            right--;
        }
        
        return flips;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    minFlips(s) {
        const n = s.length;
        let left = 0, right = n - 1;
        let flips = 0;
        
        while (left &lt; right) {
            if (s[left] !== s[right]) {
                flips++;
            }
            left++;
            right--;
        }
        
        return flips;
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    minFlips(s: string): number {
        const n = s.length;
        let left = 0, right = n - 1;
        let flips = 0;
        
        while (left &lt; right) {
            if (s[left] !== s[right]) {
                flips++;
            }
            left++;
            right--;
        }
        
        return flips;
    }
}` },]} />

**Complexity**
- Time: O(n) - We traverse the string once
- Space: O(1) - Only using constant extra space
- Notes: Optimal solution with minimal space usage

## Direct Comparison

**Intuition**
Compare the string with its reverse and count differences, then divide by 2 since each mismatched pair is counted twice.

**Steps**
- Create the reverse of the string
- Count positions where original and reverse differ
- Return count / 2

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def minFlips(self, s: str) -> int:
        reversed_s = s[::-1]
        diff = sum(1 for a, b in zip(s, reversed_s) if a != b)
        return diff // 2` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minFlips(String s) {
        String reversed = new StringBuilder(s).reverse().toString();
        int diff = 0;
        for (int i = 0; i &lt; s.length(); i++) {
            if (s.charAt(i) != reversed.charAt(i)) {
                diff++;
            }
        }
        return diff / 2;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minFlips(string s) {
        string reversed = s;
        reverse(reversed.begin(), reversed.end());
        int diff = 0;
        for (int i = 0; i &lt; s.length(); i++) {
            if (s[i] != reversed[i]) {
                diff++;
            }
        }
        return diff / 2;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    minFlips(s) {
        const reversed = s.split('').reverse().join('');
        let diff = 0;
        for (let i = 0; i &lt; s.length; i++) {
            if (s[i] !== reversed[i]) {
                diff++;
            }
        }
        return Math.floor(diff / 2);
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    minFlips(s: string): number {
        const reversed = s.split('').reverse().join('');
        let diff = 0;
        for (let i = 0; i &lt; s.length; i++) {
            if (s[i] !== reversed[i]) {
                diff++;
            }
        }
        return Math.floor(diff / 2);
    }
}` },]} />

**Complexity**
- Time: O(n) - Creating reverse and comparing
- Space: O(n) - Storing the reversed string
- Notes: Uses extra space for the reversed string

## Iterative

**Intuition**
Iterate through the first half of the string and compare each character with its mirror position from the end.

**Steps**
- Initialize flips = 0
- For i from 0 to n/2 - 1:
  - If s[i] != s[n-1-i], increment flips
- Return flips

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def minFlips(self, s: str) -> int:
        n = len(s)
        flips = 0
        
        for i in range(n // 2):
            if s[i] != s[n - 1 - i]:
                flips += 1
        
        return flips` },
{ name: "Java", lang: "java", code: `class Solution {
    public int minFlips(String s) {
        int n = s.length();
        int flips = 0;
        
        for (int i = 0; i &lt; n / 2; i++) {
            if (s.charAt(i) != s.charAt(n - 1 - i)) {
                flips++;
            }
        }
        
        return flips;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int minFlips(string s) {
        int n = s.length();
        int flips = 0;
        
        for (int i = 0; i &lt; n / 2; i++) {
            if (s[i] != s[n - 1 - i]) {
                flips++;
            }
        }
        
        return flips;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    minFlips(s) {
        const n = s.length;
        let flips = 0;
        
        for (let i = 0; i &lt; Math.floor(n / 2); i++) {
            if (s[i] !== s[n - 1 - i]) {
                flips++;
            }
        }
        
        return flips;
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    minFlips(s: string): number {
        const n = s.length;
        let flips = 0;
        
        for (let i = 0; i &lt; Math.floor(n / 2); i++) {
            if (s[i] !== s[n - 1 - i]) {
                flips++;
            }
        }
        
        return flips;
    }
}` },]} />

**Complexity**
- Time: O(n) - We traverse half the string
- Space: O(1) - Only using constant extra space
- Notes: Clean and intuitive approach

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Number of Flips to Reverse Binary String Solutions","datePublished":"2025-07-19","description":"Find the minimum number of bit flips needed to make a binary string equal to its reverse.","keywords":"LeetCode, Minimum Number of Flips to Reverse Binary String, Easy, Math, Two Pointers, String, Bit Manipulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we traverse the string once with two pointers."}},{"@type":"Question","name":"What is the space complexity of the Direct Comparison approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) because we create and store a reversed copy of the input string."}},{"@type":"Question","name":"Which approach is most space-efficient?","acceptedAnswer":{"@type":"Answer","text":"The Two Pointers and Iterative approaches both have O(1) space complexity, making them the most space-efficient solutions."}}]}).replace(/</g,'\u003c')}
</script>