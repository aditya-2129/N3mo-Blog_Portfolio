---
title: "Distribute Candies Among Children I"
summary: "Count ways to distribute n candies among 3 children with each getting 1 to limit candies."
date: "Dec 21, 2024"
tags:
  - Math
  - Combinatorics
  - Enumeration
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 76.40% | **Paid:** No
> **Topics:** Math, Combinatorics, Enumeration

You are given two positive integers n and limit.

Distribute n candies among 3 children such that:

Each child gets at least one candy.
No child gets more than limit candies.
Return the number of ways to distribute the candies.

- Examples
- Constraints
- [Brute Force Enumeration](#brute-force-enumeration)
- [Mathematical Formula (Inclusion-Exclusion)](#mathematical-formula-inclusion-exclusion)

## Examples

Example 1:
Input: n = 5, limit = 2
Output: 3
Explanation: There are 3 ways to distribute the 5 candies such that each child gets at least one candy and no child gets more than 2 candies: (1,2,2), (2,1,2), (2,2,1).

Example 2:
Input: n = 3, limit = 3
Output: 1
Explanation: There is only 1 way to distribute the 3 candies: (1,1,1).

## Constraints

```text
- 1 <= n <= 50
- 1 <= limit <= 50
```

---
## Brute Force Enumeration

**Intuition**
Iterate through all possible distributions of candies to the first two children and check if the third child gets a valid amount.

**Steps**
- Iterate through possible candies for child 1 (1 to min(limit, n-2))
- For each value, iterate through possible candies for child 2 (1 to min(limit, n-child1-1))
- Calculate candies for child 3 and check if it's valid (1 to limit)
- Count all valid distributions

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def distributeCandies(self, n: int, limit: int) -> int:
        count = 0
        for a in range(1, min(limit, n - 2) + 1):
            for b in range(1, min(limit, n - a - 1) + 1):
                c = n - a - b
                if 1 &lt;= c &lt;= limit:
                    count += 1
        return count` },
{ name: "Java", lang: "java", code: `class Solution {
    public int distributeCandies(int n, int limit) {
        int count = 0;
        for (int a = 1; a &lt;= Math.min(limit, n - 2); a++) {
            for (int b = 1; b &lt;= Math.min(limit, n - a - 1); b++) {
                int c = n - a - b;
                if (c &gt;= 1 && c &lt;= limit) {
                    count++;
                }
            }
        }
        return count;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int distributeCandies(int n, int limit) {
        int count = 0;
        for (int a = 1; a &lt;= min(limit, n - 2); a++) {
            for (int b = 1; b &lt;= min(limit, n - a - 1); b++) {
                int c = n - a - b;
                if (c &gt;= 1 && c &lt;= limit) {
                    count++;
                }
            }
        }
        return count;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    distributeCandies(n, limit) {
        let count = 0;
        for (let a = 1; a &lt;= Math.min(limit, n - 2); a++) {
            for (let b = 1; b &lt;= Math.min(limit, n - a - 1); b++) {
                const c = n - a - b;
                if (c &gt;= 1 && c &lt;= limit) {
                    count++;
                }
            }
        }
        return count;
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    distributeCandies(n: number, limit: number): number {
        let count = 0;
        for (let a = 1; a &lt;= Math.min(limit, n - 2); a++) {
            for (let b = 1; b &lt;= Math.min(limit, n - a - 1); b++) {
                const c = n - a - b;
                if (c &gt;= 1 && c &lt;= limit) {
                    count++;
                }
            }
        }
        return count;
    }
}` },]} />

**Complexity**
- Time: O(limit²)
- Space: O(1)
- Notes: Simple and intuitive, works well for small constraints

---
## Mathematical Formula (Inclusion-Exclusion)

**Intuition**
Use combinatorics with the inclusion-exclusion principle to count valid distributions without enumeration.

**Steps**
- Transform the problem: let yi = xi - 1, so 0 &lt;= yi &lt;= limit - 1 and y1 + y2 + y3 = n - 3
- Count total solutions without upper bound using stars and bars: C(n-1, 2)
- Subtract cases where at least one child exceeds limit using inclusion-exclusion
- Formula: C(n-1, 2) - 3*C(n-1-limit, 2) + 3*C(n-1-2*limit, 2) - C(n-1-3*limit, 2)

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def distributeCandies(self, n: int, limit: int) -> int:
        def C(n, k):
            if n &lt; 0 or n &lt; k:
                return 0
            if k == 0 or k == n:
                return 1
            if k == 1:
                return n
            return n * (n - 1) // 2
        
        return C(n - 1, 2) - 3 * C(n - 1 - limit, 2) + 3 * C(n - 1 - 2 * limit, 2) - C(n - 1 - 3 * limit, 2)` },
{ name: "Java", lang: "java", code: `class Solution {
    public int distributeCandies(int n, int limit) {
        return C(n - 1, 2) - 3 * C(n - 1 - limit, 2) + 3 * C(n - 1 - 2 * limit, 2) - C(n - 1 - 3 * limit, 2);
    }
    
    private int C(int n, int k) {
        if (n &lt; 0 || n &lt; k) {
            return 0;
        }
        if (k == 0 || k == n) {
            return 1;
        }
        if (k == 1) {
            return n;
        }
        return n * (n - 1) / 2;
    }
}` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    int distributeCandies(int n, int limit) {
        return C(n - 1, 2) - 3 * C(n - 1 - limit, 2) + 3 * C(n - 1 - 2 * limit, 2) - C(n - 1 - 3 * limit, 2);
    }
    
private:
    int C(int n, int k) {
        if (n &lt; 0 || n &lt; k) {
            return 0;
        }
        if (k == 0 || k == n) {
            return 1;
        }
        if (k == 1) {
            return n;
        }
        return n * (n - 1) / 2;
    }
};` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    distributeCandies(n, limit) {
        const C = (n, k) =&gt; {
            if (n &lt; 0 || n &lt; k) {
                return 0;
            }
            if (k === 0 || k === n) {
                return 1;
            }
            if (k === 1) {
                return n;
            }
            return n * (n - 1) / 2;
        };
        
        return C(n - 1, 2) - 3 * C(n - 1 - limit, 2) + 3 * C(n - 1 - 2 * limit, 2) - C(n - 1 - 3 * limit, 2);
    }
}` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    distributeCandies(n: number, limit: number): number {
        const C = (n: number, k: number): number =&gt; {
            if (n &lt; 0 || n &lt; k) {
                return 0;
            }
            if (k === 0 || k === n) {
                return 1;
            }
            if (k === 1) {
                return n;
            }
            return n * (n - 1) / 2;
        };
        
        return C(n - 1, 2) - 3 * C(n - 1 - limit, 2) + 3 * C(n - 1 - 2 * limit, 2) - C(n - 1 - 3 * limit, 2);
    }
}` },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: Optimal solution using combinatorial mathematics

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Distribute Candies Among Children I Solutions","datePublished":"2024-12-21","description":"Count ways to distribute n candies among 3 children with each getting 1 to limit candies.","keywords":"LeetCode, Distribute Candies Among Children I, Easy, Math, Combinatorics, Enumeration"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Brute Force Enumeration?","acceptedAnswer":{"@type":"Answer","text":"O(limit²)"}},{"@type":"Question","name":"What is the time complexity of Mathematical Formula (Inclusion-Exclusion)?","acceptedAnswer":{"@type":"Answer","text":"O(1)"}}]}).replace(/</g,'\u003c')}
</script>