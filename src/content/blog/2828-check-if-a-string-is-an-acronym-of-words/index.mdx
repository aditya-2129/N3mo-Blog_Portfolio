---
title: "Check if a String Is an Acronym of Words"
summary: "Determine if a given string is formed by the first letters of an array of words."
date: "Mar 12, 2025"
tags:
  - Array
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 83.00% | **Paid:** No
> **Topics:** Array, String

Given an array of strings words and a string s, determine if s is an acronym of words.

The string s is an acronym of words if it can be formed by concatenating the first character of each string in words in order. For example, ["ab", "c"] forms the acronym "ac".

Return true if s is an acronym of words, and false otherwise.

- Examples
- Constraints
- [Iterative Comparison](#iterative-comparison)
- [Two Pointers](#two-pointers)

## Examples

```text
Input: words = ["alice","bob","charlie"], s = "abc"
Output: true
Explanation: The first character of the words "alice", "bob", and "charlie" are 'a', 'b', and 'c' respectively. s = "abc" is the acronym formed by concatenating these characters, so return true.
```

```text
Input: words = ["an","apple"], s = "a"
Output: false
Explanation: The first character of the words "an" and "apple" are 'a' and 'a' respectively. s = "a" is not the acronym formed by concatenating these characters ('a' + 'a' = "aa"), so return false.
```

```text
Input: words = ["never","gonna","give","up","on","you"], s = "ngguoy"
Output: true
```

## Constraints

```text
1 <= words.length <= 100
1 <= words[i].length <= 10
words[i] consists of lowercase English letters.
1 <= s.length <= 100
s consists of lowercase English letters.
```

---
## Iterative Comparison

**Intuition**
We can construct the acronym string by iterating through the `words` array and appending the first character of each word to a result string. Finally, we compare the constructed string with `s`.

**Steps**
- Initialize an empty string builder or result string.
- Iterate through each word in the `words` array.
- Append the first character of the current word to the result.
- After the loop, compare the result with `s`.
- Return true if they are equal, false otherwise.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isAcronym(self, words: list[str], s: str) -&gt; bool:\n        return ''.join(w[0] for w in words) == s" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isAcronym(List&lt;String&gt; words, String s) {\n        StringBuilder sb = new StringBuilder();\n        for (String word : words) {\n            sb.append(word.charAt(0));\n        }\n        return sb.toString().equals(s);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isAcronym(vector&lt;string&gt;& words, string s) {\n        string res = \"\";\n        for (const string& word : words) {\n            res += word[0];\n        }\n        return res == s;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var isAcronym = function(words, s) {\n    return words.map(w =&gt; w[0]).join('') === s;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isAcronym(words: string[], s: string): boolean {\n    return words.map(w =&gt; w[0]).join('') === s;\n}" },]} />

**Complexity**
- Time: O(N), where N is the total number of characters in the `words` array (specifically the number of words).
- Space: O(N), to store the constructed acronym string.
- Notes: This approach is straightforward and readable, but uses extra space proportional to the length of the acronym.

---
## Two Pointers

**Intuition**
We can optimize space by comparing characters on the fly without building a new string. If the length of `words` is different from the length of `s`, they cannot be acronyms. Otherwise, we check if the first character of each word matches the corresponding character in `s`.

**Steps**
- Check if the length of `words` is equal to the length of `s`. If not, return false.
- Iterate through the indices of the `words` array.
- At each index `i`, check if `words[i][0]` is equal to `s[i]`.
- If any character does not match, return false.
- If the loop completes without mismatches, return true.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isAcronym(self, words: list[str], s: str) -&gt; bool:\n        if len(words) != len(s):\n            return False\n        for i in range(len(words)):\n            if words[i][0] != s[i]:\n                return False\n        return True" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isAcronym(List&lt;String&gt; words, String s) {\n        if (words.size() != s.length()) return false;\n        for (int i = 0; i &lt; words.size(); i++) {\n            if (words.get(i).charAt(0) != s.charAt(i)) return false;\n        }\n        return true;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isAcronym(vector&lt;string&gt;& words, string s) {\n        if (words.size() != s.size()) return false;\n        for (int i = 0; i &lt; words.size(); i++) {\n            if (words[i][0] != s[i]) return false;\n        }\n        return true;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var isAcronym = function(words, s) {\n    if (words.length !== s.length) return false;\n    for (let i = 0; i &lt; words.length; i++) {\n        if (words[i][0] !== s[i]) return false;\n    }\n    return true;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function isAcronym(words: string[], s: string): boolean {\n    if (words.length !== s.length) return false;\n    for (let i = 0; i &lt; words.length; i++) {\n        if (words[i][0] !== s[i]) return false;\n    }\n    return true;\n}" },]} />

**Complexity**
- Time: O(N), where N is the number of words.
- Space: O(1), as we only use a constant amount of extra space for variables.
- Notes: This is the most optimal approach in terms of space complexity.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Check if a String Is an Acronym of Words Solutions","datePublished":"2025-03-12","description":"Determine if a given string is formed by the first letters of an array of words.","keywords":"LeetCode, Check if a String Is an Acronym of Words, Easy, Array, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Comparison approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N), where N is the number of words."}},{"@type":"Question","name":"What is the space complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1), as it does not use extra space proportional to the input size."}},{"@type":"Question","name":"Why check the length of words against the length of s?","acceptedAnswer":{"@type":"Answer","text":"If the number of words does not match the length of the string s, it is impossible for s to be the acronym of the words array."}}]}).replace(/</g,'\u003c')}
</script>