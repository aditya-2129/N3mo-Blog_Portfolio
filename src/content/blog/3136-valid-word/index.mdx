---
title: "Valid Word"
summary: "Determine if a word is valid based on length, character composition, and vowel/consonant requirements."
date: "Apr 12, 2024"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 50.90% | **Paid:** No
> **Topics:** String

A string word is valid if the following conditions are met:

- It contains at least 3 characters.
- It consists only of lowercase English letters.
- It contains at least one vowel ('a', 'e', 'i', 'o', 'u').
- It contains at least one consonant (any letter that is not a vowel).

Given a string word, return true if word is valid, otherwise return false.

- Examples
- Constraints
- [Simple Iteration](#simple-iteration)
- [Regular Expression](#regular-expression)
- [Set-Based Approach](#set-based-approach)

## Examples

### Example 1

**Input:**
```text
word = "234Adas"
```

**Output:**
```text
true
```

**Explanation:**
This word satisfies the conditions.

### Example 2

**Input:**
```text
word = "b3"
```

**Output:**
```text
false
```

**Explanation:**
The length of this word is fewer than 3, and does not have a vowel.

### Example 3

**Input:**
```text
word = "a3$e"
```

**Output:**
```text
false
```

**Explanation:**
This word contains a '$' character and does not have a consonant.

## Constraints

```text
1 <= word.length <= 20
word consists of lowercase English letters and digits.
```

---

## Simple Iteration

**Intuition**
Iterate through each character of the word, checking all conditions: minimum length, valid characters only, presence of at least one vowel and one consonant.

**Steps**
- Check if word length is at least 3
- Initialize flags for vowel and consonant presence
- Iterate through each character:
  - Return false if character is not a lowercase letter
  - Update vowel or consonant flag based on character
- Return true only if both flags are true

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isValid(self, word: str) -&gt; bool:\n        if len(word) &lt; 3:\n            return False\n        \n        vowels = set('aeiou')\n        has_vowel = False\n        has_consonant = False\n        \n        for c in word:\n            if not c.isalpha() or not c.islower():\n                return False\n            if c in vowels:\n                has_vowel = True\n            else:\n                has_consonant = True\n        \n        return has_vowel and has_consonant\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public boolean isValid(String word) {\n        if (word.length() &lt; 3) {\n            return false;\n        }\n        \n        String vowels = \"aeiou\";\n        boolean hasVowel = false;\n        boolean hasConsonant = false;\n        \n        for (int i = 0; i &lt; word.length(); i++) {\n            char c = word.charAt(i);\n            if (!Character.isLetter(c) || !Character.isLowerCase(c)) {\n                return false;\n            }\n            if (vowels.indexOf(c) != -1) {\n                hasVowel = true;\n            } else {\n                hasConsonant = true;\n            }\n        }\n        \n        return hasVowel && hasConsonant;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    bool isValid(string word) {\n        if (word.length() &lt; 3) {\n            return false;\n        }\n        \n        string vowels = \"aeiou\";\n        bool hasVowel = false;\n        bool hasConsonant = false;\n        \n        for (char c : word) {\n            if (!isalpha(c) || !islower(c)) {\n                return false;\n            }\n            if (vowels.find(c) != string::npos) {\n                hasVowel = true;\n            } else {\n                hasConsonant = true;\n            }\n        }\n        \n        return hasVowel && hasConsonant;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} word\n * @return {boolean}\n */\nvar isValid = function(word) {\n    if (word.length &lt; 3) {\n        return false;\n    }\n    \n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    let hasVowel = false;\n    let hasConsonant = false;\n    \n    for (const c of word) {\n        if (!/[a-z]/.test(c)) {\n            return false;\n        }\n        if (vowels.has(c)) {\n            hasVowel = true;\n        } else {\n            hasConsonant = true;\n        }\n    }\n    \n    return hasVowel && hasConsonant;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function isValid(word: string): boolean {\n    if (word.length &lt; 3) {\n        return false;\n    }\n    \n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    let hasVowel = false;\n    let hasConsonant = false;\n    \n    for (const c of word) {\n        if (!/[a-z]/.test(c)) {\n            return false;\n        }\n        if (vowels.has(c)) {\n            hasVowel = true;\n        } else {\n            hasConsonant = true;\n        }\n    }\n    \n    return hasVowel && hasConsonant;\n}\n" },]} />

**Complexity**
- Time: O(n) where n is the length of the word
- Space: O(1) - only using a fixed-size set for vowels
- Notes: Most straightforward approach with clear logic flow

---

## Regular Expression

**Intuition**
Use regex patterns to validate all conditions in a concise manner by matching against specific patterns.

**Steps**
- Check minimum length condition separately
- Create regex pattern for valid characters (lowercase letters only)
- Create regex pattern for at least one vowel
- Create regex pattern for at least one consonant
- All conditions must be satisfied

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import re\n\nclass Solution:\n    def isValid(self, word: str) -&gt; bool:\n        if len(word) &lt; 3:\n            return False\n        \n        # Check all characters are lowercase letters\n        if not re.fullmatch(r'[a-z]+', word):\n            return False\n        \n        # Check at least one vowel\n        if not re.search(r'[aeiou]', word):\n            return False\n        \n        # Check at least one consonant\n        if not re.search(r'[^aeiou]', word):\n            return False\n        \n        return True\n" },
{ name: "Java", lang: "java", code: "import java.util.regex.*;\n\nclass Solution {\n    public boolean isValid(String word) {\n        if (word.length() &lt; 3) {\n            return false;\n        }\n        \n        // Check all characters are lowercase letters\n        if (!word.matches(\"[a-z]+\")) {\n            return false;\n        }\n        \n        // Check at least one vowel\n        if (!word.matches(\".*[aeiou].*\")) {\n            return false;\n        }\n        \n        // Check at least one consonant\n        if (!word.matches(\".*[^aeiou].*\")) {\n            return false;\n        }\n        \n        return true;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;regex&gt;\n\nclass Solution {\npublic:\n    bool isValid(string word) {\n        if (word.length() &lt; 3) {\n            return false;\n        }\n        \n        // Check all characters are lowercase letters\n        if (!regex_match(word, regex(\"[a-z]+\"))) {\n            return false;\n        }\n        \n        // Check at least one vowel\n        if (!regex_search(word, regex(\"[aeiou]\"))) {\n            return false;\n        }\n        \n        // Check at least one consonant\n        if (!regex_search(word, regex(\"[^aeiou]\"))) {\n            return false;\n        }\n        \n        return true;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} word\n * @return {boolean}\n */\nvar isValid = function(word) {\n    if (word.length &lt; 3) {\n        return false;\n    }\n    \n    // Check all characters are lowercase letters\n    if (!/^[a-z]+$/.test(word)) {\n        return false;\n    }\n    \n    // Check at least one vowel\n    if (!/[aeiou]/.test(word)) {\n        return false;\n    }\n    \n    // Check at least one consonant\n    if (!/[^aeiou]/.test(word)) {\n        return false;\n    }\n    \n    return true;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function isValid(word: string): boolean {\n    if (word.length &lt; 3) {\n        return false;\n    }\n    \n    // Check all characters are lowercase letters\n    if (!/^[a-z]+$/.test(word)) {\n        return false;\n    }\n    \n    // Check at least one vowel\n    if (!/[aeiou]/.test(word)) {\n        return false;\n    }\n    \n    // Check at least one consonant\n    if (!/[^aeiou]/.test(word)) {\n        return false;\n    }\n    \n    return true;\n}\n" },]} />

**Complexity**
- Time: O(n) - regex engine scans the string
- Space: O(1) - constant space for regex patterns
- Notes: More concise but may have overhead from regex compilation

---

## Set-Based Approach

**Intuition**
Use set operations to efficiently check for vowel and consonant presence while validating character constraints.

**Steps**
- Check minimum length requirement
- Create a set of all vowels
- Create sets for found vowels and consonants
- Iterate through word, populating respective sets
- Validate all conditions using set operations

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def isValid(self, word: str) -&gt; bool:\n        if len(word) &lt; 3:\n            return False\n        \n        vowels = set('aeiou')\n        found_vowels = set()\n        found_consonants = set()\n        \n        for c in word:\n            if not c.isalpha() or not c.islower():\n                return False\n            if c in vowels:\n                found_vowels.add(c)\n            else:\n                found_consonants.add(c)\n        \n        return len(found_vowels) &gt; 0 and len(found_consonants) &gt; 0\n" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public boolean isValid(String word) {\n        if (word.length() &lt; 3) {\n            return false;\n        }\n        \n        Set&lt;Character&gt; vowels = new HashSet&lt;&gt;(Arrays.asList('a', 'e', 'i', 'o', 'u'));\n        Set&lt;Character&gt; foundVowels = new HashSet&lt;&gt;();\n        Set&lt;Character&gt; foundConsonants = new HashSet&lt;&gt;();\n        \n        for (int i = 0; i &lt; word.length(); i++) {\n            char c = word.charAt(i);\n            if (!Character.isLetter(c) || !Character.isLowerCase(c)) {\n                return false;\n            }\n            if (vowels.contains(c)) {\n                foundVowels.add(c);\n            } else {\n                foundConsonants.add(c);\n            }\n        }\n        \n        return !foundVowels.isEmpty() && !foundConsonants.isEmpty();\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;unordered_set&gt;\n\nclass Solution {\npublic:\n    bool isValid(string word) {\n        if (word.length() &lt; 3) {\n            return false;\n        }\n        \n        unordered_set&lt;char&gt; vowels = {'a', 'e', 'i', 'o', 'u'};\n        unordered_set&lt;char&gt; foundVowels;\n        unordered_set&lt;char&gt; foundConsonants;\n        \n        for (char c : word) {\n            if (!isalpha(c) || !islower(c)) {\n                return false;\n            }\n            if (vowels.count(c)) {\n                foundVowels.insert(c);\n            } else {\n                foundConsonants.insert(c);\n            }\n        }\n        \n        return !foundVowels.empty() && !foundConsonants.empty();\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} word\n * @return {boolean}\n */\nvar isValid = function(word) {\n    if (word.length &lt; 3) {\n        return false;\n    }\n    \n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    const foundVowels = new Set();\n    const foundConsonants = new Set();\n    \n    for (const c of word) {\n        if (!/[a-z]/.test(c)) {\n            return false;\n        }\n        if (vowels.has(c)) {\n            foundVowels.add(c);\n        } else {\n            foundConsonants.add(c);\n        }\n    }\n    \n    return foundVowels.size &gt; 0 && foundConsonants.size &gt; 0;\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function isValid(word: string): boolean {\n    if (word.length &lt; 3) {\n        return false;\n    }\n    \n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    const foundVowels = new Set&lt;string&gt;();\n    const foundConsonants = new Set&lt;string&gt;();\n    \n    for (const c of word) {\n        if (!/[a-z]/.test(c)) {\n            return false;\n        }\n        if (vowels.has(c)) {\n            foundVowels.add(c);\n        } else {\n            foundConsonants.add(c);\n        }\n    }\n    \n    return foundVowels.size &gt; 0 && foundConsonants.size &gt; 0;\n}\n" },]} />

**Complexity**
- Time: O(n) - single pass through the string
- Space: O(1) - sets are bounded by alphabet size (26 letters)
- Notes: Uses more memory than simple iteration but provides clear separation of concerns

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Valid Word Solutions","datePublished":"2024-04-12","description":"Determine if a word is valid based on length, character composition, and vowel/consonant requirements.","keywords":"LeetCode, Valid Word, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Simple Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of the word, as we iterate through each character once."}},{"@type":"Question","name":"What is the space complexity of Regular Expression approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) constant space for regex patterns, though there may be some overhead from regex compilation."}},{"@type":"Question","name":"What is the time complexity of Set-Based approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for a single pass through the string with O(1) set operations for each character."}},{"@type":"Question","name":"Which approach is most efficient for Valid Word problem?","acceptedAnswer":{"@type":"Answer","text":"Simple Iteration is most efficient with O(n) time and O(1) space, with minimal overhead compared to regex or set-based approaches."}}]}).replace(/</g,'\u003c')}
</script>