---
title: "Count Tested Devices After Test Operations"
summary: "Count devices with positive battery after iteratively decrementing subsequent devices."
date: "Mar 12, 2024"
tags:
  - Array
  - Simulation
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 78.90% | **Paid:** No
> **Topics:** Array, Simulation, Counting

You are given a 0-indexed integer array `batteryPercentages` having length `n`, where `batteryPercentages[i]` denotes the battery percentage of the `ith` device.

You are asked to test each device `i` in order from `0` to `n - 1`:

- If `batteryPercentages[i]` is greater than `0`:
    - It has been tested.
    - Decrease the battery percentage of all devices with index `j` (where `i &lt; j &lt; n`) by `1`.

Return an integer denoting the number of devices that have been tested after performing the test operations.

- Examples
- Constraints
- [Approach 1: Simulation](#approach-1-simulation)
- [Approach 2: Optimized One-Pass](#approach-2-optimized-one-pass)

## Examples

**Example 1:**

```text
Input: batteryPercentages = [1,1,2,1,3]
Output: 3
Explanation: 
- Device 0 is tested because batteryPercentages[0] > 0. batteryPercentages becomes [1,0,1,0,2].
- Device 1 is not tested.
- Device 2 is tested because batteryPercentages[2] > 0. batteryPercentages becomes [1,0,1,0,1].
- Device 3 is not tested.
- Device 4 is tested because batteryPercentages[4] > 0. batteryPercentages becomes [1,0,1,0,0].
There are 3 devices tested.
```

**Example 2:**

```text
Input: batteryPercentages = [0,1,2]
Output: 2
Explanation: 
- Device 0 is not tested.
- Device 1 is tested because batteryPercentages[1] > 0. batteryPercentages becomes [0,1,1].
- Device 2 is tested because batteryPercentages[2] > 0. batteryPercentages becomes [0,1,0].
There are 2 devices tested.
```

## Constraints

```text
1 <= n <= 100
0 <= batteryPercentages[i] <= 100
```

---
## Approach 1: Simulation

**Intuition**
We can directly simulate the process described in the problem statement. We iterate through the array, and whenever we find a device with battery percentage greater than 0, we increment our count and decrement the battery percentage of all subsequent devices by 1.

**Steps**
- Initialize a counter `count` to 0.
- Iterate through the array `batteryPercentages` with index `i`.
- If `batteryPercentages[i] &gt; 0`:
    - Increment `count`.
    - Iterate through the array from index `i + 1` to the end, decrementing each element by 1.
- Return `count`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def countTestedDevices(self, batteryPercentages: List[int]) -&gt; int:\r\n        n = len(batteryPercentages)\r\n        count = 0\r\n        for i in range(n):\r\n            if batteryPercentages[i] &gt; 0:\r\n                count += 1\r\n                for j in range(i + 1, n):\r\n                    batteryPercentages[j] -= 1\r\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int countTestedDevices(int[] batteryPercentages) {\r\n        int n = batteryPercentages.length;\r\n        int count = 0;\r\n        for (int i = 0; i &lt; n; i++) {\r\n            if (batteryPercentages[i] &gt; 0) {\r\n                count++;\r\n                for (int j = i + 1; j &lt; n; j++) {\r\n                    batteryPercentages[j]--;\r\n                }\r\n            }\r\n        }\r\n        return count;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n\r\nclass Solution {\r\npublic:\r\n    int countTestedDevices(std::vector&lt;int&gt;& batteryPercentages) {\r\n        int n = batteryPercentages.size();\r\n        int count = 0;\r\n        for (int i = 0; i &lt; n; i++) {\r\n            if (batteryPercentages[i] &gt; 0) {\r\n                count++;\r\n                for (int j = i + 1; j &lt; n; j++) {\r\n                    batteryPercentages[j]--;\r\n                }\r\n            }\r\n        }\r\n        return count;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "var countTestedDevices = function(batteryPercentages) {\r\n    const n = batteryPercentages.length;\r\n    let count = 0;\r\n    for (let i = 0; i &lt; n; i++) {\r\n        if (batteryPercentages[i] &gt; 0) {\r\n            count++;\r\n            for (let j = i + 1; j &lt; n; j++) {\r\n                batteryPercentages[j]--;\r\n            }\r\n        }\r\n    }\r\n    return count;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countTestedDevices(batteryPercentages: number[]): number {\r\n    const n = batteryPercentages.length;\r\n    let count = 0;\r\n    for (let i = 0; i &lt; n; i++) {\r\n        if (batteryPercentages[i] &gt; 0) {\r\n            count++;\r\n            for (let j = i + 1; j &lt; n; j++) {\r\n                batteryPercentages[j]--;\r\n            }\r\n        }\r\n    }\r\n    return count;\r\n}" },]} />

**Complexity**
- Time: O(n²) - In the worst case, for every device, we iterate through the rest of the array.
- Space: O(1) - We modify the array in place and use a constant amount of extra space.
- Notes: This approach is straightforward but not the most efficient for large arrays.

---
## Approach 2: Optimized One-Pass

**Intuition**
Instead of actually modifying the array elements, we can observe that the value of `batteryPercentages[i]` is effectively reduced by the number of devices tested before it. If we keep track of how many devices have been tested so far (`decrement`), we can determine if the current device is testable by checking if `batteryPercentages[i] - decrement &gt; 0`.

**Steps**
- Initialize `count` and `decrement` to 0.
- Iterate through each `val` in `batteryPercentages`.
- If `val - decrement &gt; 0`:
    - Increment `count`.
    - Increment `decrement` (since this device will decrement all subsequent devices).
- Return `count`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def countTestedDevices(self, batteryPercentages: List[int]) -&gt; int:\r\n        count = 0\r\n        decrement = 0\r\n        for val in batteryPercentages:\r\n            if val - decrement &gt; 0:\r\n                count += 1\r\n                decrement += 1\r\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int countTestedDevices(int[] batteryPercentages) {\r\n        int count = 0;\r\n        int decrement = 0;\r\n        for (int val : batteryPercentages) {\r\n            if (val - decrement &gt; 0) {\r\n                count++;\r\n                decrement++;\r\n            }\r\n        }\r\n        return count;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n\r\nclass Solution {\r\npublic:\r\n    int countTestedDevices(std::vector&lt;int&gt;& batteryPercentages) {\r\n        int count = 0;\r\n        int decrement = 0;\r\n        for (int val : batteryPercentages) {\r\n            if (val - decrement &gt; 0) {\r\n                count++;\r\n                decrement++;\r\n            }\r\n        }\r\n        return count;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "var countTestedDevices = function(batteryPercentages) {\r\n    let count = 0;\r\n    let decrement = 0;\r\n    for (const val of batteryPercentages) {\r\n        if (val - decrement &gt; 0) {\r\n            count++;\r\n            decrement++;\r\n        }\r\n    }\r\n    return count;\r\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countTestedDevices(batteryPercentages: number[]): number {\r\n    let count = 0;\r\n    let decrement = 0;\r\n    for (const val of batteryPercentages) {\r\n        if (val - decrement &gt; 0) {\r\n            count++;\r\n            decrement++;\r\n        }\r\n    }\r\n    return count;\r\n}" },]} />

**Complexity**
- Time: O(n) - We traverse the array exactly once.
- Space: O(1) - We only use two integer variables for tracking.
- Notes: This is the optimal solution, avoiding the nested loop of the simulation approach.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Count Tested Devices After Test Operations Solutions","datePublished":"2024-03-12","description":"Count devices with positive battery after iteratively decrementing subsequent devices.","keywords":"LeetCode, Count Tested Devices After Test Operations, Easy, Array, Simulation, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n²) because for each device tested, we iterate through all subsequent devices to decrement their battery percentage."}},{"@type":"Question","name":"What is the time complexity of the Optimized One-Pass approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) as we traverse the array once, tracking the cumulative decrement count instead of modifying the array."}}]}).replace(/</g,'\u003c')}
</script>