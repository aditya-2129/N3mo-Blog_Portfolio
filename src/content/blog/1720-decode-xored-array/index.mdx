---
title: "Decode XORed Array"
summary: "Given an encoded array and the first element of the original array, reconstruct the original array using XOR properties."
date: "Nov 07, 2024"
tags:
  - Array
  - Bit Manipulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 87.40% | **Paid:** No
> **Topics:** Array, Bit Manipulation

There is a hidden integer array arr that consists of n non-negative integers.

It was encoded into another integer array encoded of length n - 1, such that encoded[i] = arr[i] XOR arr[i + 1]. For example, if arr = [1,0,2,1], then encoded = [1,2,3].

You are given the encoded array and an integer first, that is the first element of arr.

Return arr.

- Examples
- Constraints
- [Iterative Reconstruction](#iterative-reconstruction)
- [Brute Force Search](#brute-force-search)

## Examples

```text
Input: encoded = [1,2,3], first = 1
Output: [1,0,2,1]
Explanation: If arr = [1,0,2,1], then first = 1 and encoded = [1 XOR 0, 0 XOR 2, 2 XOR 1] = [1,2,3]
```

```text
Input: encoded = [6,2,7,3], first = 4
Output: [4,2,0,7,4]
```

## Constraints

```text
- 2 <= n <= 10^4
- encoded.length == n - 1
- 0 <= encoded[i] <= 10^5
- 0 <= first <= 10^5
```

---
## Iterative Reconstruction

**Intuition**
The XOR operation has a unique property: if `a ^ b = c`, then `a ^ c = b`. Since we know `encoded[i]` (which is `c`) and `arr[i]` (which is `a`), we can directly compute `arr[i+1]` (which is `b`) by XORing the current element with the corresponding encoded element.

**Steps**
- Initialize the result array with the `first` element.
- Iterate through the `encoded` array.
- For each element, calculate the next value by XORing the current value with the encoded value.
- Append the calculated value to the result array.
- Return the result array.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def decode(self, encoded: List[int], first: int) -&gt; List[int]:\r\n        arr = [first]\r\n        for x in encoded:\r\n            arr.append(arr[-1] ^ x)\r\n        return arr" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int[] decode(int[] encoded, int first) {\r\n        int n = encoded.length;\r\n        int[] arr = new int[n + 1];\r\n        arr[0] = first;\r\n        for (int i = 0; i &lt; n; i++) {\r\n            arr[i + 1] = arr[i] ^ encoded[i];\r\n        }\r\n        return arr;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\r\npublic:\r\n    vector&lt;int&gt; decode(vector&lt;int&gt;& encoded, int first) {\r\n        vector&lt;int&gt; arr;\r\n        arr.push_back(first);\r\n        for (int x : encoded) {\r\n            arr.push_back(arr.back() ^ x);\r\n        }\r\n        return arr;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\r\n    decode(encoded, first) {\r\n        const arr = [first];\r\n        for (const x of encoded) {\r\n            arr.push(arr[arr.length - 1] ^ x);\r\n        }\r\n        return arr;\r\n    }\r\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\r\n    decode(encoded: number[], first: number): number[] {\r\n        const arr: number[] = [first];\r\n        for (const x of encoded) {\r\n            arr.push(arr[arr.length - 1] ^ x);\r\n        }\r\n        return arr;\r\n    }\r\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n) (to store the result)
- Notes: This is the optimal solution, utilizing the mathematical property of XOR to solve the problem in a single pass.

---
## Brute Force Search

**Intuition**
Since the values are constrained to a relatively small range (0 to 10⁵), we can theoretically find the next number by checking every possible integer in that range until we find one that satisfies the XOR condition `arr[i] ^ arr[i+1] == encoded[i]`.

**Steps**
- Initialize the result array with the `first` element.
- Iterate through the `encoded` array.
- For each position, iterate through all possible numbers from 0 to 10⁵.
- Check if `current_val ^ candidate == encoded[i]`.
- If true, append `candidate` to the result and break the inner loop.
- Return the result array.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def decode(self, encoded: List[int], first: int) -&gt; List[int]:\r\n        arr = [first]\r\n        for x in encoded:\r            current = arr[-1]\r\n            next_val = -1\r\n            for candidate in range(100001):\r\n                if current ^ candidate == x:\r\n                    next_val = candidate\r\n                    break\r\n            arr.append(next_val)\r\n        return arr" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int[] decode(int[] encoded, int first) {\r\n        int n = encoded.length;\r\n        int[] arr = new int[n + 1];\r\n        arr[0] = first;\r\n        for (int i = 0; i &lt; n; i++) {\r\n            int current = arr[i];\r\n            int next = -1;\r\n            for (int candidate = 0; candidate &lt;= 100000; candidate++) {\r\n                if ((current ^ candidate) == encoded[i]) {\r\n                    next = candidate;\r\n                    break;\r\n                }\r\n            }\r\n            arr[i + 1] = next;\r\n        }\r\n        return arr;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\r\npublic:\r\n    vector&lt;int&gt; decode(vector&lt;int&gt;& encoded, int first) {\r\n        vector&lt;int&gt; arr;\r\n        arr.push_back(first);\r\n        for (int x : encoded) {\r\n            int current = arr.back();\r\n            int next = -1;\r\n            for (int candidate = 0; candidate &lt;= 100000; candidate++) {\r\n                if ((current ^ candidate) == x) {\r\n                    next = candidate;\r\n                    break;\r\n                }\r\n            }\r\n            arr.push_back(next);\r\n        }\r\n        return arr;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\r\n    decode(encoded, first) {\r\n        const arr = [first];\r\n        for (const x of encoded) {\r\n            const current = arr[arr.length - 1];\r\n            let next = -1;\r\n            for (let candidate = 0; candidate &lt;= 100000; candidate++) {\r\n                if ((current ^ candidate) === x) {\r\n                    next = candidate;\r\n                    break;\r\n                }\r\n            }\r\n            arr.push(next);\r\n        }\r\n        return arr;\r\n    }\r\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\r\n    decode(encoded: number[], first: number): number[] {\r\n        const arr: number[] = [first];\r\n        for (const x of encoded) {\r\n            const current = arr[arr.length - 1];\r\n            let next = -1;\r\n            for (let candidate = 0; candidate &lt;= 100000; candidate++) {\r\n                if ((current ^ candidate) === x) {\r\n                    next = candidate;\r\n                    break;\r\n                }\r\n            }\r\n            arr.push(next);\r\n        }\r\n        return arr;\r\n    }\r\n}" },]} />

**Complexity**
- Time: O(n * m), where m is the maximum value in the array (10⁵).
- Space: O(n)
- Notes: This approach is significantly slower than the optimal solution and is not recommended for large inputs, but it demonstrates a naive search strategy.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Decode XORed Array Solutions","datePublished":"2024-11-07","description":"Given an encoded array and the first element of the original array, reconstruct the original array using XOR properties.","keywords":"LeetCode, Decode XORed Array, Easy, Array, Bit Manipulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Reconstruction approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the encoded array."}},{"@type":"Question","name":"What is the space complexity of the Iterative Reconstruction approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) to store the resulting array."}},{"@type":"Question","name":"Why is the Brute Force approach inefficient?","acceptedAnswer":{"@type":"Answer","text":"The Brute Force approach has a time complexity of O(n * m), where m is the maximum value (10⁵), making it much slower than the O(n) optimal solution."}}]}).replace(/</g,'\u003c')}
</script>