---
title: "Sum of Good Numbers"
summary: "Given an array of integers, return the sum of numbers that are divisible by their digit count."
date: "Apr 11, 2025"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 69.70% | **Paid:** No
> **Topics:** Array

You are given an integer array `nums`. A number `nums[i]` is considered **good** if it is divisible by the number of digits it has. Return the sum of all good numbers in `nums`.

- Examples
- Constraints
- [String Conversion](#string-conversion)
- [Mathematical Logarithm](#mathematical-logarithm)
- [Iterative Digit Counting](#iterative-digit-counting)

## Examples

### Example 1

**Input:**
```text
nums = [1,3,2,1,5,4], k = 2
```

**Output:**
```text
12
```

**Explanation:**
The good numbers are nums[1] = 3, nums[4] = 5, and nums[5] = 4 because they are strictly greater than the numbers at indices i - k and i + k.

### Example 2

**Input:**
```text
nums = [2,1], k = 1
```

**Output:**
```text
2
```

**Explanation:**
The only good number is nums[0] = 2 because it is strictly greater than nums[1].

## Constraints

```text
- 2 <= nums.length <= 100
- 1 <= nums[i] <= 1000
- 1 <= k <= floor(nums.length / 2)
```

---
## String Conversion

**Intuition**
Convert each number to a string to easily count its length (number of digits), then check divisibility.

**Steps**
- Initialize a sum variable to 0.
- Iterate through each number in the array.
- Convert the number to a string and get its length.
- If the number modulo the length is 0, add the number to the sum.
- Return the sum.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfGoodNumbers(self, nums: list[int]) -&gt; int:\n        total = 0\n        for num in nums:\n            digits = len(str(num))\n            if num % digits == 0:\n                total += num\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfGoodNumbers(int[] nums) {\n        int sum = 0;\n        for (int num : nums) {\n            int digits = String.valueOf(num).length();\n            if (num % digits == 0) {\n                sum += num;\n            }\n        }\n        return sum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfGoodNumbers(vector&lt;int&gt;& nums) {\n        int sum = 0;\n        for (int num : nums) {\n            int digits = to_string(num).length();\n            if (num % digits == 0) {\n                sum += num;\n            }\n        }\n        return sum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar sumOfGoodNumbers = function(nums) {\n    let sum = 0;\n    for (let num of nums) {\n        let digits = num.toString().length;\n        if (num % digits === 0) {\n            sum += num;\n        }\n    }\n    return sum;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfGoodNumbers(nums: number[]): number {\n    let sum = 0;\n    for (let num of nums) {\n        let digits = num.toString().length;\n        if (num % digits === 0) {\n            sum += num;\n        }\n    }\n    return sum;\n}" },]} />

**Complexity**
- Time: O(n * d), where n is the number of elements and d is the average number of digits (max 10 for 10⁹).
- Space: O(d) for the string representation.
- Notes: Very readable and concise.

---
## Mathematical Logarithm

**Intuition**
Use the mathematical property that the number of digits in an integer `n` is `floor(log10(n)) + 1`.

**Steps**
- Initialize a sum variable to 0.
- Iterate through each number in the array.
- Calculate digits using `floor(log10(num)) + 1`.
- If the number modulo the digit count is 0, add the number to the sum.
- Return the sum.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import math\n\nclass Solution:\n    def sumOfGoodNumbers(self, nums: list[int]) -&gt; int:\n        total = 0\n        for num in nums:\n            digits = int(math.log10(num)) + 1\n            if num % digits == 0:\n                total += num\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfGoodNumbers(int[] nums) {\n        int sum = 0;\n        for (int num : nums) {\n            int digits = (int) Math.log10(num) + 1;\n            if (num % digits == 0) {\n                sum += num;\n            }\n        }\n        return sum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;cmath&gt;\n\nclass Solution {\npublic:\n    int sumOfGoodNumbers(vector&lt;int&gt;& nums) {\n        int sum = 0;\n        for (int num : nums) {\n            int digits = (int)log10(num) + 1;\n            if (num % digits == 0) {\n                sum += num;\n            }\n        }\n        return sum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar sumOfGoodNumbers = function(nums) {\n    let sum = 0;\n    for (let num of nums) {\n        let digits = Math.floor(Math.log10(num)) + 1;\n        if (num % digits === 0) {\n            sum += num;\n        }\n    }\n    return sum;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfGoodNumbers(nums: number[]): number {\n    let sum = 0;\n    for (let num of nums) {\n        let digits = Math.floor(Math.log10(num)) + 1;\n        if (num % digits === 0) {\n            sum += num;\n        }\n    }\n    return sum;\n}" },]} />

**Complexity**
- Time: O(n).
- Space: O(1).
- Notes: Faster than string conversion but relies on floating point math which can be tricky near powers of 10 (though safe for 10⁹).

---
## Iterative Digit Counting

**Intuition**
Count digits by repeatedly dividing the number by 10 until it becomes 0.

**Steps**
- Initialize a sum variable to 0.
- Iterate through each number in the array.
- Create a temporary copy of the number and a counter initialized to 0.
- While the temporary number is greater than 0, divide it by 10 and increment the counter.
- If the original number modulo the counter is 0, add the number to the sum.
- Return the sum.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def sumOfGoodNumbers(self, nums: list[int]) -&gt; int:\n        total = 0\n        for num in nums:\n            temp = num\n            digits = 0\n            while temp &gt; 0:\n                digits += 1\n                temp //= 10\n            if num % digits == 0:\n                total += num\n        return total" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int sumOfGoodNumbers(int[] nums) {\n        int sum = 0;\n        for (int num : nums) {\n            int temp = num;\n            int digits = 0;\n            while (temp &gt; 0) {\n                digits++;\n                temp /= 10;\n            }\n            if (num % digits == 0) {\n                sum += num;\n            }\n        }\n        return sum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int sumOfGoodNumbers(vector&lt;int&gt;& nums) {\n        int sum = 0;\n        for (int num : nums) {\n            int temp = num;\n            int digits = 0;\n            while (temp &gt; 0) {\n                digits++;\n                temp /= 10;\n            }\n            if (num % digits == 0) {\n                sum += num;\n            }\n        }\n        return sum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number[]} nums\n * @return {number}\n */\nvar sumOfGoodNumbers = function(nums) {\n    let sum = 0;\n    for (let num of nums) {\n        let temp = num;\n        let digits = 0;\n        while (temp &gt; 0) {\n            digits++;\n            temp = Math.floor(temp / 10);\n        }\n        if (num % digits === 0) {\n            sum += num;\n        }\n    }\n    return sum;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function sumOfGoodNumbers(nums: number[]): number {\n    let sum = 0;\n    for (let num of nums) {\n        let temp = num;\n        let digits = 0;\n        while (temp &gt; 0) {\n            digits++;\n            temp = Math.floor(temp / 10);\n        }\n        if (num % digits === 0) {\n            sum += num;\n        }\n    }\n    return sum;\n}" },]} />

**Complexity**
- Time: O(n * d).
- Space: O(1).
- Notes: Pure integer arithmetic, avoiding string allocation and floating point inaccuracies.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Sum of Good Numbers Solutions","datePublished":"2025-04-11","description":"Given an array of integers, return the sum of numbers that are divisible by their digit count.","keywords":"LeetCode, Sum of Good Numbers, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the String Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"O(n * d), where n is the array length and d is the number of digits."}},{"@type":"Question","name":"What is the space complexity of the Mathematical Logarithm approach?","acceptedAnswer":{"@type":"Answer","text":"O(1), as it uses only a constant amount of extra space."}},{"@type":"Question","name":"How do we determine if a number is 'good'?","acceptedAnswer":{"@type":"Answer","text":"A number is good if it is divisible by the total count of its digits."}}]}).replace(/</g,'\u003c')}
</script>