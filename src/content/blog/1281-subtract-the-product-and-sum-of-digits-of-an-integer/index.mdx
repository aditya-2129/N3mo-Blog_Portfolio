---
title: "Subtract the Product and Sum of Digits of an Integer"
summary: "Given an integer n, return the difference between the product of its digits and the sum of its digits."
date: "Aug 13, 2024"
tags:
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 86.60% | **Paid:** No
> **Topics:** Math

Given an integer number n, return the difference between the product of its digits and the sum of its digits.

- Examples
- Constraints
- [Approach 1: Iterative Digit Extraction](#iterative-digit-extraction)
- [Approach 2: String Conversion](#string-conversion)
- [Approach 3: Functional Reduction](#functional-reduction)

## Examples

```text
Input: n = 234
Output: 15
Explanation:
Product of digits = 2 * 3 * 4 = 24
Sum of digits = 2 + 3 + 4 = 9
Result = 24 - 9 = 15
```

```text
Input: n = 4421
Output: 21
Explanation:
Product of digits = 4 * 4 * 2 * 1 = 32
Sum of digits = 4 + 4 + 2 + 1 = 11
Result = 32 - 11 = 21
```

## Constraints

```text
1 <= n <= 10^5
```

---

## Iterative Digit Extraction

**Intuition**
Extract each digit using modulo and division operations, then compute product and sum separately.

**Steps**
- Initialize product to 1 and sum to 0
- While n is greater than 0, extract the last digit using modulo 10
- Multiply product by the digit and add digit to sum
- Remove the last digit by dividing n by 10
- Return the difference between product and sum

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def subtractProductAndSum(self, n: int) -&gt; int:\n        product = 1\n        sum_digits = 0\n        while n &gt; 0:\n            digit = n % 10\n            product *= digit\n            sum_digits += digit\n            n //= 10\n        return product - sum_digits" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int subtractProductAndSum(int n) {\n        int product = 1;\n        int sum = 0;\n        while (n &gt; 0) {\n            int digit = n % 10;\n            product *= digit;\n            sum += digit;\n            n /= 10;\n        }\n        return product - sum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int subtractProductAndSum(int n) {\n        int product = 1;\n        int sum = 0;\n        while (n &gt; 0) {\n            int digit = n % 10;\n            product *= digit;\n            sum += digit;\n            n /= 10;\n        }\n        return product - sum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar subtractProductAndSum = function(n) {\n    let product = 1;\n    let sum = 0;\n    while (n &gt; 0) {\n        const digit = n % 10;\n        product *= digit;\n        sum += digit;\n        n = Math.floor(n / 10);\n    }\n    return product - sum;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function subtractProductAndSum(n: number): number {\n    let product = 1;\n    let sum = 0;\n    while (n &gt; 0) {\n        const digit = n % 10;\n        product *= digit;\n        sum += digit;\n        n = Math.floor(n / 10);\n    }\n    return product - sum;\n}" },]} />

**Complexity**
- Time: O(log n) - number of digits in n
- Space: O(1) - only using constant extra space
- Notes: Most efficient approach with minimal memory usage

---

## String Conversion

**Intuition**
Convert the number to a string to easily iterate over each digit character.

**Steps**
- Convert n to a string representation
- Iterate through each character in the string
- Convert each character back to an integer
- Calculate product and sum of all digits
- Return the difference

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def subtractProductAndSum(self, n: int) -&gt; int:\n        digits = [int(d) for d in str(n)]\n        product = 1\n        sum_digits = 0\n        for d in digits:\n            product *= d\n            sum_digits += d\n        return product - sum_digits" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int subtractProductAndSum(int n) {\n        String s = String.valueOf(n);\n        int product = 1;\n        int sum = 0;\n        for (int i = 0; i &lt; s.length(); i++) {\n            int digit = s.charAt(i) - '0';\n            product *= digit;\n            sum += digit;\n        }\n        return product - sum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\nclass Solution {\npublic:\n    int subtractProductAndSum(int n) {\n        string s = to_string(n);\n        int product = 1;\n        int sum = 0;\n        for (char c : s) {\n            int digit = c - '0';\n            product *= digit;\n            sum += digit;\n        }\n        return product - sum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar subtractProductAndSum = function(n) {\n    const s = n.toString();\n    let product = 1;\n    let sum = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        const digit = parseInt(s[i]);\n        product *= digit;\n        sum += digit;\n    }\n    return product - sum;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function subtractProductAndSum(n: number): number {\n    const s = n.toString();\n    let product = 1;\n    let sum = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        const digit = parseInt(s[i]);\n        product *= digit;\n        sum += digit;\n    }\n    return product - sum;\n}" },]} />

**Complexity**
- Time: O(log n) - number of digits in n
- Space: O(log n) - string representation of n
- Notes: More readable but uses extra memory for string conversion

---

## Functional Reduction

**Intuition**
Use functional programming constructs like reduce to compute product and sum in a declarative way.

**Steps**
- Convert n to string and split into individual digit characters
- Map each character to its integer value
- Use reduce to compute product with initial value 1
- Use reduce or sum to compute total of all digits
- Return the difference

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from functools import reduce\nimport operator\n\nclass Solution:\n    def subtractProductAndSum(self, n: int) -&gt; int:\n        digits = [int(d) for d in str(n)]\n        product = reduce(operator.mul, digits, 1)\n        sum_digits = sum(digits)\n        return product - sum_digits" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\n\nclass Solution {\n    public int subtractProductAndSum(int n) {\n        int[] digits = String.valueOf(n).chars().map(c -&gt; c - '0').toArray();\n        int product = IntStream.of(digits).reduce(1, (a, b) -&gt; a * b);\n        int sum = IntStream.of(digits).sum();\n        return product - sum;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;string&gt;\n#include &lt;numeric&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int subtractProductAndSum(int n) {\n        string s = to_string(n);\n        vector&lt;int&gt; digits;\n        for (char c : s) {\n            digits.push_back(c - '0');\n        }\n        int product = accumulate(digits.begin(), digits.end(), 1, multiplies&lt;int&gt;());\n        int sum = accumulate(digits.begin(), digits.end(), 0);\n        return product - sum;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar subtractProductAndSum = function(n) {\n    const digits = n.toString().split('').map(Number);\n    const product = digits.reduce((acc, d) =&gt; acc * d, 1);\n    const sum = digits.reduce((acc, d) =&gt; acc + d, 0);\n    return product - sum;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function subtractProductAndSum(n: number): number {\n    const digits: number[] = n.toString().split('').map(Number);\n    const product: number = digits.reduce((acc, d) =&gt; acc * d, 1);\n    const sum: number = digits.reduce((acc, d) =&gt; acc + d, 0);\n    return product - sum;\n}" },]} />

**Complexity**
- Time: O(log n) - number of digits in n
- Space: O(log n) - array of digits
- Notes: Clean functional style but with overhead of array allocation

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Subtract the Product and Sum of Digits of an Integer Solutions","datePublished":"2024-08-13","description":"Given an integer n, return the difference between the product of its digits and the sum of its digits.","keywords":"LeetCode, Subtract the Product and Sum of Digits of an Integer, Easy, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Iterative Digit Extraction approach?","acceptedAnswer":{"@type":"Answer","text":"O(log n) where log n represents the number of digits in the integer n."}},{"@type":"Question","name":"What is the space complexity of String Conversion approach?","acceptedAnswer":{"@type":"Answer","text":"O(log n) for storing the string representation of the number."}},{"@type":"Question","name":"Which approach is most memory efficient?","acceptedAnswer":{"@type":"Answer","text":"The Iterative Digit Extraction approach uses O(1) space, making it the most memory efficient."}},{"@type":"Question","name":"What is the time complexity of Functional Reduction approach?","acceptedAnswer":{"@type":"Answer","text":"O(log n) as we need to process each digit exactly once."}}]}).replace(/</g,'\u003c')}
</script>