---
title: "Minimum Number of Chairs in a Waiting Room"
summary: "Find the minimum number of chairs needed in a waiting room given a string of people entering ('E') and leaving ('L')."
date: "Mar 12, 2025"
tags:
  - String
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 79.30% | **Paid:** No
> **Topics:** String, Simulation

You are given a string s representing the sequence of people entering and leaving a waiting room. Each character in s is either 'E' (enter) or 'L' (leave).

When a person enters, they need a chair. When a person leaves, their chair becomes available.

Return the minimum number of chairs needed so that everyone entering has a place to sit.

- Examples
- Constraints
- [Simulation Approach](#simulation-approach)
- [Prefix Sum Approach](#prefix-sum-approach)

## Examples

Example 1:
```text
Input: s = "EEEE"
Output: 4
Explanation: 4 people enter, 0 leave. We need 4 chairs.
```

Example 2:
```text
Input: s = "ELELE"
Output: 1
Explanation: The sequence is: Enter(1), Leave(0), Enter(1), Leave(0), Enter(1).
The maximum number of people at any time is 1, so we need 1 chair.
```

Example 3:
```text
Input: s = "ELEELEELLL"
Output: 3
Explanation: The sequence is: Enter(1), Leave(0), Enter(1), Enter(2), Leave(1), 
Enter(2), Enter(3), Leave(2), Leave(1), Leave(0).
The maximum number of people at any time is 3, so we need 3 chairs.
```

## Constraints

```text
1 <= s.length <= 50
s consists only of 'E' and 'L'
It is guaranteed that at any point, the number of people leaving 
will not exceed the number of people who have entered.
```

---

## Simulation Approach

**Intuition**
Simulate the process by tracking the current number of people in the room and keeping track of the maximum count observed.

**Steps**
- Initialize a counter for current people and a variable for maximum chairs needed
- Iterate through each character in the string
- Increment counter for 'E', decrement for 'L'
- Update maximum whenever current count exceeds it
- Return the maximum value

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumChairs(self, s: str) -&gt; int:\n        current = 0\n        max_chairs = 0\n        for c in s:\n            if c == 'E':\n                current += 1\n            else:\n                current -= 1\n            max_chairs = max(max_chairs, current)\n        return max_chairs" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimumChairs(String s) {\n        int current = 0;\n        int maxChairs = 0;\n        for (int i = 0; i &lt; s.length(); i++) {\n            char c = s.charAt(i);\n            if (c == 'E') {\n                current++;\n            } else {\n                current--;\n            }\n            maxChairs = Math.max(maxChairs, current);\n        }\n        return maxChairs;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimumChairs(string s) {\n        int current = 0;\n        int maxChairs = 0;\n        for (char c : s) {\n            if (c == 'E') {\n                current++;\n            } else {\n                current--;\n            }\n            maxChairs = max(maxChairs, current);\n        }\n        return maxChairs;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {number}\n */\nvar minimumChairs = function(s) {\n    let current = 0;\n    let maxChairs = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        if (s[i] === 'E') {\n            current++;\n        } else {\n            current--;\n        }\n        maxChairs = Math.max(maxChairs, current);\n    }\n    return maxChairs;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumChairs(s: string): number {\n    let current = 0;\n    let maxChairs = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        if (s[i] === 'E') {\n            current++;\n        } else {\n            current--;\n        }\n        maxChairs = Math.max(maxChairs, current);\n    }\n    return maxChairs;\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1) using only constant extra space
- Notes: Most intuitive approach with single pass through the string

---

## Prefix Sum Approach

**Intuition**
Treat 'E' as +1 and 'L' as -1, then find the maximum prefix sum which represents the peak occupancy.

**Steps**
- Initialize prefix sum and maximum variables
- For each character, add +1 for 'E' or -1 for 'L' to running sum
- Track the maximum value of the running sum
- Return the maximum as the answer

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def minimumChairs(self, s: str) -&gt; int:\n        prefix_sum = 0\n        max_prefix = 0\n        for c in s:\n            prefix_sum += 1 if c == 'E' else -1\n            max_prefix = max(max_prefix, prefix_sum)\n        return max_prefix" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int minimumChairs(String s) {\n        int prefixSum = 0;\n        int maxPrefix = 0;\n        for (int i = 0; i &lt; s.length(); i++) {\n            prefixSum += (s.charAt(i) == 'E') ? 1 : -1;\n            maxPrefix = Math.max(maxPrefix, prefixSum);\n        }\n        return maxPrefix;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int minimumChairs(string s) {\n        int prefixSum = 0;\n        int maxPrefix = 0;\n        for (char c : s) {\n            prefixSum += (c == 'E') ? 1 : -1;\n            maxPrefix = max(maxPrefix, prefixSum);\n        }\n        return maxPrefix;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {number}\n */\nvar minimumChairs = function(s) {\n    let prefixSum = 0;\n    let maxPrefix = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        prefixSum += (s[i] === 'E') ? 1 : -1;\n        maxPrefix = Math.max(maxPrefix, prefixSum);\n    }\n    return maxPrefix;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function minimumChairs(s: string): number {\n    let prefixSum = 0;\n    let maxPrefix = 0;\n    for (let i = 0; i &lt; s.length; i++) {\n        prefixSum += (s[i] === 'E') ? 1 : -1;\n        maxPrefix = Math.max(maxPrefix, prefixSum);\n    }\n    return maxPrefix;\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1) using only constant extra space
- Notes: Mathematically equivalent to simulation but framed as a prefix sum problem

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Number of Chairs in a Waiting Room Solutions","datePublished":"2025-03-12","description":"Find the minimum number of chairs needed in a waiting room given a string of people entering ('E') and leaving ('L').","keywords":"LeetCode, Minimum Number of Chairs in a Waiting Room, Easy, String, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string, as we iterate through each character exactly once."}},{"@type":"Question","name":"What is the space complexity of the Prefix Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for tracking the prefix sum and maximum value."}},{"@type":"Question","name":"Why do we track the maximum count instead of just the final count?","acceptedAnswer":{"@type":"Answer","text":"We need the minimum number of chairs to accommodate everyone at any point in time, which is the peak occupancy (maximum count), not the final count."}}]}).replace(/</g,'\u003c')}
</script>