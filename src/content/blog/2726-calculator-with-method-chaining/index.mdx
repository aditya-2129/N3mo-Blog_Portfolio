---
title: "Calculator with Method Chaining"
summary: "Design a Calculator class that supports method chaining for arithmetic operations like add, subtract, multiply, divide, and power."
date: "Jun 24, 2024"
tags:
  - Design
  - Object-Oriented
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 77.60% | **Paid:** No
> **Topics:** N/A

Design a Calculator class. The class should provide the following methods:

- `add(value)`: Adds the given value to the current result and returns the calculator instance for chaining.
- `subtract(value)`: Subtracts the given value from the current result and returns the calculator instance for chaining.
- `multiply(value)`: Multiplies the current result by the given value and returns the calculator instance for chaining.
- `divide(value)`: Divides the current result by the given value and returns the calculator instance for chaining. If the value is 0, throw an error "Division by zero".
- `power(value)`: Raises the current result to the power of the given value and returns the calculator instance for chaining.
- `getResult()`: Returns the current result.

All methods except `getResult()` should support method chaining, meaning they return the calculator instance itself.

- Examples
- Constraints
- [Approach 1: Class with Method Chaining](#approach-1-class-with-method-chaining)
- [Approach 2: Using Builder Pattern](#approach-2-using-builder-pattern)
- [Approach 3: Functional Approach with Closure](#approach-3-functional-approach-with-closure)

## Examples

### Example 1
```text
Input: 
actions = ["Calculator", "add", "subtract", "getResult"], 
values = [[10], [5], [7], []]
Output: 8
Explanation: 
new Calculator(10) // 10
  .add(5)          // 10 + 5 = 15
  .subtract(7)     // 15 - 7 = 8
  .getResult()     // 8
```

### Example 2
```text
Input: 
actions = ["Calculator", "multiply", "power", "getResult"], 
values = [[2], [5], [2], []]
Output: 100
Explanation: 
new Calculator(2)   // 2
  .multiply(5)     // 2 * 5 = 10
  .power(2)        // 10² = 100
  .getResult()     // 100
```

### Example 3
```text
Input: 
actions = ["Calculator", "divide", "add", "getResult"], 
values = [[20], [0], [5], []]
Output: "Division by zero"
Explanation: 
new Calculator(20)  // 20
  .divide(0)        // Error: Division by zero
```

## Constraints

```text
- actions is a valid JSON array of strings
- values is a valid JSON array of numbers
- 2 <= actions.length <= 2 * 10^4
- 1 <= values.length <= 2 * 10^4 - 1
- actions[i] is one of "Calculator", "add", "subtract", "multiply", "divide", "power", and "getResult"
- First action is always "Calculator"
- Last action is always "getResult"
```

---
## Approach 1: Class with Method Chaining

**Intuition**
Create a Calculator class with an instance variable to store the result. Each arithmetic method updates this result and returns `this` to enable method chaining.

**Steps**
- Initialize the Calculator with a starting value in the constructor
- Implement each method (add, subtract, multiply, divide, power) to modify the result
- Return `this` from each method to enable chaining
- Implement getResult() to return the current result value
- Handle division by zero by throwing an error

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Calculator:\n    def __init__(self, value):\n        self.result = value\n    \n    def add(self, value):\n        self.result += value\n        return self\n    \n    def subtract(self, value):\n        self.result -= value\n        return self\n    \n    def multiply(self, value):\n        self.result *= value\n        return self\n    \n    def divide(self, value):\n        if value == 0:\n            raise Exception(\"Division by zero\")\n        self.result /= value\n        return self\n    \n    def power(self, value):\n        self.result **= value\n        return self\n    \n    def getResult(self):\n        return self.result" },
{ name: "Java", lang: "java", code: "class Calculator {\n    private double result;\n    \n    public Calculator(int value) {\n        this.result = value;\n    }\n    \n    public Calculator add(int value) {\n        this.result += value;\n        return this;\n    }\n    \n    public Calculator subtract(int value) {\n        this.result -= value;\n        return this;\n    }\n    \n    public Calculator multiply(int value) {\n        this.result *= value;\n        return this;\n    }\n    \n    public Calculator divide(int value) {\n        if (value == 0) {\n            throw new Error(\"Division by zero\");\n        }\n        this.result /= value;\n        return this;\n    }\n    \n    public Calculator power(int value) {\n        this.result = Math.pow(this.result, value);\n        return this;\n    }\n    \n    public double getResult() {\n        return this.result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Calculator {\nprivate:\n    double result;\npublic:\n    Calculator(int value) {\n        result = value;\n    }\n    \n    Calculator* add(int value) {\n        result += value;\n        return this;\n    }\n    \n    Calculator* subtract(int value) {\n        result -= value;\n        return this;\n    }\n    \n    Calculator* multiply(int value) {\n        result *= value;\n        return this;\n    }\n    \n    Calculator* divide(int value) {\n        if (value == 0) {\n            throw runtime_error(\"Division by zero\");\n        }\n        result /= value;\n        return this;\n    }\n    \n    Calculator* power(int value) {\n        result = pow(result, value);\n        return this;\n    }\n    \n    double getResult() {\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Calculator {\n    constructor(value) {\n        this.result = value;\n    }\n    \n    add(value) {\n        this.result += value;\n        return this;\n    }\n    \n    subtract(value) {\n        this.result -= value;\n        return this;\n    }\n    \n    multiply(value) {\n        this.result *= value;\n        return this;\n    }\n    \n    divide(value) {\n        if (value === 0) {\n            throw new Error(\"Division by zero\");\n        }\n        this.result /= value;\n        return this;\n    }\n    \n    power(value) {\n        this.result = Math.pow(this.result, value);\n        return this;\n    }\n    \n    getResult() {\n        return this.result;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Calculator {\n    private result: number;\n    \n    constructor(value: number) {\n        this.result = value;\n    }\n    \n    add(value: number): Calculator {\n        this.result += value;\n        return this;\n    }\n    \n    subtract(value: number): Calculator {\n        this.result -= value;\n        return this;\n    }\n    \n    multiply(value: number): Calculator {\n        this.result *= value;\n        return this;\n    }\n    \n    divide(value: number): Calculator {\n        if (value === 0) {\n            throw new Error(\"Division by zero\");\n        }\n        this.result /= value;\n        return this;\n    }\n    \n    power(value: number): Calculator {\n        this.result = Math.pow(this.result, value);\n        return this;\n    }\n    \n    getResult(): number {\n        return this.result;\n    }\n}" },]} />

**Complexity**
- Time: O(1) for each operation
- Space: O(1) - only storing the result
- Notes: Simple and intuitive approach, most commonly used in real-world scenarios

---
## Approach 2: Using Builder Pattern

**Intuition**
Separate the calculation logic from the result storage using a builder-like pattern where operations are queued and executed when getResult is called.

**Steps**
- Create a Calculator class that stores the initial value and a list of operations
- Each method adds an operation to the queue instead of modifying the result directly
- getResult() executes all operations in sequence and returns the final result
- This allows for potential operation replay or modification before execution

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Calculator:\n    def __init__(self, value):\n        self.initial = value\n        self.operations = []\n    \n    def add(self, value):\n        self.operations.append(('add', value))\n        return self\n    \n    def subtract(self, value):\n        self.operations.append(('subtract', value))\n        return self\n    \n    def multiply(self, value):\n        self.operations.append(('multiply', value))\n        return self\n    \n    def divide(self, value):\n        if value == 0:\n            raise Exception(\"Division by zero\")\n        self.operations.append(('divide', value))\n        return self\n    \n    def power(self, value):\n        self.operations.append(('power', value))\n        return self\n    \n    def getResult(self):\n        result = self.initial\n        for op, value in self.operations:\n            if op == 'add':\n                result += value\n            elif op == 'subtract':\n                result -= value\n            elif op == 'multiply':\n                result *= value\n            elif op == 'divide':\n                result /= value\n            elif op == 'power':\n                result **= value\n        return result" },
{ name: "Java", lang: "java", code: "import java.util.ArrayList;\nimport java.util.List;\n\nclass Calculator {\n    private double initial;\n    private List&lt;String&gt; operations;\n    private List&lt;Integer&gt; values;\n    \n    public Calculator(int value) {\n        this.initial = value;\n        this.operations = new ArrayList&lt;&gt;();\n        this.values = new ArrayList&lt;&gt;();\n    }\n    \n    public Calculator add(int value) {\n        operations.add(\"add\");\n        values.add(value);\n        return this;\n    }\n    \n    public Calculator subtract(int value) {\n        operations.add(\"subtract\");\n        values.add(value);\n        return this;\n    }\n    \n    public Calculator multiply(int value) {\n        operations.add(\"multiply\");\n        values.add(value);\n        return this;\n    }\n    \n    public Calculator divide(int value) {\n        if (value == 0) {\n            throw new Error(\"Division by zero\");\n        }\n        operations.add(\"divide\");\n        values.add(value);\n        return this;\n    }\n    \n    public Calculator power(int value) {\n        operations.add(\"power\");\n        values.add(value);\n        return this;\n    }\n    \n    public double getResult() {\n        double result = initial;\n        for (int i = 0; i &lt; operations.size(); i++) {\n            String op = operations.get(i);\n            int val = values.get(i);\n            switch (op) {\n                case \"add\": result += val; break;\n                case \"subtract\": result -= val; break;\n                case \"multiply\": result *= val; break;\n                case \"divide\": result /= val; break;\n                case \"power\": result = Math.pow(result, val); break;\n            }\n        }\n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;cmath&gt;\n\nusing namespace std;\n\nclass Calculator {\nprivate:\n    double initial;\n    vector&lt;string&gt; operations;\n    vector&lt;int&gt; values;\npublic:\n    Calculator(int value) {\n        initial = value;\n    }\n    \n    Calculator* add(int value) {\n        operations.push_back(\"add\");\n        values.push_back(value);\n        return this;\n    }\n    \n    Calculator* subtract(int value) {\n        operations.push_back(\"subtract\");\n        values.push_back(value);\n        return this;\n    }\n    \n    Calculator* multiply(int value) {\n        operations.push_back(\"multiply\");\n        values.push_back(value);\n        return this;\n    }\n    \n    Calculator* divide(int value) {\n        if (value == 0) {\n            throw runtime_error(\"Division by zero\");\n        }\n        operations.push_back(\"divide\");\n        values.push_back(value);\n        return this;\n    }\n    \n    Calculator* power(int value) {\n        operations.push_back(\"power\");\n        values.push_back(value);\n        return this;\n    }\n    \n    double getResult() {\n        double result = initial;\n        for (size_t i = 0; i &lt; operations.size(); i++) {\n            string op = operations[i];\n            int val = values[i];\n            if (op == \"add\") result += val;\n            else if (op == \"subtract\") result -= val;\n            else if (op == \"multiply\") result *= val;\n            else if (op == \"divide\") result /= val;\n            else if (op == \"power\") result = pow(result, val);\n        }\n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Calculator {\n    constructor(value) {\n        this.initial = value;\n        this.operations = [];\n    }\n    \n    add(value) {\n        this.operations.push({ op: 'add', value });\n        return this;\n    }\n    \n    subtract(value) {\n        this.operations.push({ op: 'subtract', value });\n        return this;\n    }\n    \n    multiply(value) {\n        this.operations.push({ op: 'multiply', value });\n        return this;\n    }\n    \n    divide(value) {\n        if (value === 0) {\n            throw new Error(\"Division by zero\");\n        }\n        this.operations.push({ op: 'divide', value });\n        return this;\n    }\n    \n    power(value) {\n        this.operations.push({ op: 'power', value });\n        return this;\n    }\n    \n    getResult() {\n        let result = this.initial;\n        for (const { op, value } of this.operations) {\n            switch (op) {\n                case 'add': result += value; break;\n                case 'subtract': result -= value; break;\n                case 'multiply': result *= value; break;\n                case 'divide': result /= value; break;\n                case 'power': result = Math.pow(result, value); break;\n            }\n        }\n        return result;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "type Operation = {\n    op: 'add' | 'subtract' | 'multiply' | 'divide' | 'power';\n    value: number;\n};\n\nclass Calculator {\n    private initial: number;\n    private operations: Operation[];\n    \n    constructor(value: number) {\n        this.initial = value;\n        this.operations = [];\n    }\n    \n    add(value: number): Calculator {\n        this.operations.push({ op: 'add', value });\n        return this;\n    }\n    \n    subtract(value: number): Calculator {\n        this.operations.push({ op: 'subtract', value });\n        return this;\n    }\n    \n    multiply(value: number): Calculator {\n        this.operations.push({ op: 'multiply', value });\n        return this;\n    }\n    \n    divide(value: number): Calculator {\n        if (value === 0) {\n            throw new Error(\"Division by zero\");\n        }\n        this.operations.push({ op: 'divide', value });\n        return this;\n    }\n    \n    power(value: number): Calculator {\n        this.operations.push({ op: 'power', value });\n        return this;\n    }\n    \n    getResult(): number {\n        let result = this.initial;\n        for (const { op, value } of this.operations) {\n            switch (op) {\n                case 'add': result += value; break;\n                case 'subtract': result -= value; break;\n                case 'multiply': result *= value; break;\n                case 'divide': result /= value; break;\n                case 'power': result = Math.pow(result, value); break;\n            }\n        }\n        return result;\n    }\n}" },]} />

**Complexity**
- Time: O(n) for getResult() where n is the number of operations, O(1) for other methods
- Space: O(n) to store all operations
- Notes: Useful when you need to replay or modify operations before execution, but uses more memory

---
## Approach 3: Functional Approach with Closure

**Intuition**
Use closures to encapsulate the state and return an object with methods that can access and modify the enclosed state, enabling method chaining without a traditional class.

**Steps**
- Create a factory function that takes an initial value
- Use a closure to maintain the result state
- Return an object with methods that modify the enclosed state
- Each method returns the object itself to enable chaining

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "def createCalculator(value):\n    result = [value]\n    \n    class Calculator:\n        def add(self, value):\n            result[0] += value\n            return self\n        \n        def subtract(self, value):\n            result[0] -= value\n            return self\n        \n        def multiply(self, value):\n            result[0] *= value\n            return self\n        \n        def divide(self, value):\n            if value == 0:\n                raise Exception(\"Division by zero\")\n            result[0] /= value\n            return self\n        \n        def power(self, value):\n            result[0] **= value\n            return self\n        \n        def getResult(self):\n            return result[0]\n    \n    return Calculator()" },
{ name: "Java", lang: "java", code: "import java.util.function.Supplier;\n\nclass Calculator {\n    private double result;\n    \n    private Calculator(double value) {\n        this.result = value;\n    }\n    \n    public static Calculator create(int value) {\n        return new Calculator(value);\n    }\n    \n    public Calculator add(int value) {\n        this.result += value;\n        return this;\n    }\n    \n    public Calculator subtract(int value) {\n        this.result -= value;\n        return this;\n    }\n    \n    public Calculator multiply(int value) {\n        this.result *= value;\n        return this;\n    }\n    \n    public Calculator divide(int value) {\n        if (value == 0) {\n            throw new Error(\"Division by zero\");\n        }\n        this.result /= value;\n        return this;\n    }\n    \n    public Calculator power(int value) {\n        this.result = Math.pow(this.result, value);\n        return this;\n    }\n    \n    public double getResult() {\n        return this.result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;functional&gt;\n#include &lt;memory&gt;\n\nclass Calculator {\nprivate:\n    std::shared_ptr&lt;double&gt; result;\n    \n    Calculator(double value) {\n        result = std::make_shared&lt;double&gt;(value);\n    }\npublic:\n    static Calculator* create(int value) {\n        return new Calculator(value);\n    }\n    \n    Calculator* add(int value) {\n        *result += value;\n        return this;\n    }\n    \n    Calculator* subtract(int value) {\n        *result -= value;\n        return this;\n    }\n    \n    Calculator* multiply(int value) {\n        *result *= value;\n        return this;\n    }\n    \n    Calculator* divide(int value) {\n        if (value == 0) {\n            throw std::runtime_error(\"Division by zero\");\n        }\n        *result /= value;\n        return this;\n    }\n    \n    Calculator* power(int value) {\n        *result = pow(*result, value);\n        return this;\n    }\n    \n    double getResult() {\n        return *result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "function createCalculator(value) {\n    let result = value;\n    \n    return {\n        add(value) {\n            result += value;\n            return this;\n        },\n        subtract(value) {\n            result -= value;\n            return this;\n        },\n        multiply(value) {\n            result *= value;\n            return this;\n        },\n        divide(value) {\n            if (value === 0) {\n                throw new Error(\"Division by zero\");\n            }\n            result /= value;\n            return this;\n        },\n        power(value) {\n            result = Math.pow(result, value);\n            return this;\n        },\n        getResult() {\n            return result;\n        }\n    };\n}" },
{ name: "TypeScript", lang: "typescript", code: "interface Calculator {\n    add(value: number): Calculator;\n    subtract(value: number): Calculator;\n    multiply(value: number): Calculator;\n    divide(value: number): Calculator;\n    power(value: number): Calculator;\n    getResult(): number;\n}\n\nfunction createCalculator(value: number): Calculator {\n    let result = value;\n    \n    return {\n        add(value: number): Calculator {\n            result += value;\n            return this;\n        },\n        subtract(value: number): Calculator {\n            result -= value;\n            return this;\n        },\n        multiply(value: number): Calculator {\n            result *= value;\n            return this;\n        },\n        divide(value: number): Calculator {\n            if (value === 0) {\n                throw new Error(\"Division by zero\");\n            }\n            result /= value;\n            return this;\n        },\n        power(value: number): Calculator {\n            result = Math.pow(result, value);\n            return this;\n        },\n        getResult(): number {\n            return result;\n        }\n    };\n}" },]} />

**Complexity**
- Time: O(1) for each operation
- Space: O(1) - only storing the result in closure
- Notes: Functional programming style, provides encapsulation without classes, commonly used in JavaScript

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Calculator with Method Chaining Solutions","datePublished":"2024-06-24","description":"Design a Calculator class that supports method chaining for arithmetic operations like add, subtract, multiply, divide, and power.","keywords":"LeetCode, Calculator with Method Chaining, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Class with Method Chaining approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) for each operation, as each arithmetic operation is performed in constant time."}},{"@type":"Question","name":"What is the space complexity of the Builder Pattern approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of operations, as we store all operations in a list before execution."}},{"@type":"Question","name":"What is the advantage of using the Functional Approach with Closure?","acceptedAnswer":{"@type":"Answer","text":"It provides encapsulation without using classes, following functional programming principles, and is commonly used in JavaScript."}},{"@type":"Question","name":"How does method chaining work in the Calculator class?","acceptedAnswer":{"@type":"Answer","text":"Each method returns 'this' (the calculator instance itself), allowing multiple methods to be called in sequence on the same object."}},{"@type":"Question","name":"How is division by zero handled in these implementations?","acceptedAnswer":{"@type":"Answer","text":"All implementations throw an error with the message 'Division by zero' when attempting to divide by zero."}}]}).replace(/</g,'\u003c')}
</script>