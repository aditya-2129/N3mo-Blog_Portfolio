---
title: "Number of Distinct Averages"
summary: "Calculate the number of distinct averages obtained by repeatedly pairing the minimum and maximum numbers in an array."
date: "Jun 15, 2025"
tags:
  - Array
  - Hash Table
  - Two Pointers
  - Sorting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 58.90% | **Paid:** No
> **Topics:** Array, Hash Table, Two Pointers, Sorting

You are given an integer array nums. In one operation, you must:

Choose the minimum and maximum elements from nums.
Remove them from nums.
Calculate the average of the two removed elements.

The average of two numbers a and b is (a + b) / 2.

For example, the average of 2 and 3 is 2.5, and the average of 2 and 2 is 2.

Return the number of distinct averages calculated using the above process.

Notice that when there is a tie for the minimum or maximum element, any element can be removed.

- Examples
- Constraints
- [Sorting and Two Pointers](#sorting-and-two-pointers)
- [Frequency Map (Bucket Sort)](#frequency-map-bucket-sort)
- [Brute Force Simulation](#brute-force-simulation)

## Examples

```text
Input: nums = [9,5,7,3,5,3]
Output: 2
Explanation:
1. Remove min=3 and max=9. Average = (3+9)/2 = 6.0. nums = [5,7,5,3].
2. Remove min=3 and max=7. Average = (3+7)/2 = 5.0. nums = [5,5].
3. Remove min=5 and max=5. Average = (5+5)/2 = 5.0. nums = [].
The distinct averages are 6.0 and 5.0. Return 2.
```

```text
Input: nums = [1,100]
Output: 1
Explanation:
There is only one average to be calculated after removing min=1 and max=100. Average = (1+100)/2 = 50.5.
```

## Constraints

```text
2 <= nums.length <= 100
1 <= nums[i] <= 100
```

---
## Sorting and Two Pointers

**Intuition**
By sorting the array, the minimum and maximum elements are naturally positioned at the start and end. We can then use two pointers to pair these elements efficiently.

**Steps**
- Sort the array in ascending order.
- Initialize a set to store distinct averages.
- Use a left pointer at the start and a right pointer at the end.
- While the left pointer is less than the right pointer:
  - Calculate the average of `nums[left]` and `nums[right]`.
  - Add the average to the set.
  - Increment the left pointer and decrement the right pointer.
- Return the size of the set.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def distinctAverages(self, nums: list[int]) -&gt; int:\n        nums.sort()\n        seen = set()\n        l, r = 0, len(nums) - 1\n        while l &lt; r:\n            avg = (nums[l] + nums[r]) / 2\n            seen.add(avg)\n            l += 1\n            r -= 1\n        return len(seen)\n" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int distinctAverages(int[] nums) {\n        Arrays.sort(nums);\n        Set&lt;Double&gt; seen = new HashSet&lt;&gt;();\n        int l = 0, r = nums.length - 1;\n        while (l &lt; r) {\n            double avg = (nums[l] + nums[r]) / 2.0;\n            seen.add(avg);\n            l++;\n            r--;\n        }\n        return seen.size();\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int distinctAverages(vector&lt;int&gt;& nums) {\n        sort(nums.begin(), nums.end());\n        unordered_set&lt;double&gt; seen;\n        int l = 0, r = nums.size() - 1;\n        while (l &lt; r) {\n            double avg = (nums[l] + nums[r]) / 2.0;\n            seen.insert(avg);\n            l++;\n            r--;\n        }\n        return seen.size();\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var distinctAverages = function(nums) {\n    nums.sort((a, b) =&gt; a - b);\n    const seen = new Set();\n    let l = 0, r = nums.length - 1;\n    while (l &lt; r) {\n        const avg = (nums[l] + nums[r]) / 2;\n        seen.add(avg);\n        l++;\n        r--;\n    }\n    return seen.size();\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function distinctAverages(nums: number[]): number {\n    nums.sort((a, b) =&gt; a - b);\n    const seen = new Set&lt;number&gt;();\n    let l = 0, r = nums.length - 1;\n    while (l &lt; r) {\n        const avg = (nums[l] + nums[r]) / 2;\n        seen.add(avg);\n        l++;\n        r--;\n    }\n    return seen.size();\n}\n" },]} />

**Complexity**
- Time: O(n log n) due to sorting.
- Space: O(n) to store the set of averages.
- Notes: This is the most standard approach for this problem.

---
## Frequency Map (Bucket Sort)

**Intuition**
Since the values in `nums` are constrained to a small range (1 to 100), we can use a frequency array (bucket sort) to count occurrences. This allows us to find the min and max in O(100) time per operation without sorting the original array.

**Steps**
- Create a frequency array `count` of size 101 initialized to 0.
- Populate `count` based on the numbers in `nums`.
- Initialize a set to store distinct averages.
- While there are elements remaining (tracked by a total count or by scanning):
  - Find the minimum index `min` with `count[min] &gt; 0`.
  - Find the maximum index `max` with `count[max] &gt; 0`.
  - Decrement `count[min]` and `count[max]`.
  - Calculate the average and add it to the set.
- Return the size of the set.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def distinctAverages(self, nums: list[int]) -&gt; int:\n        count = [0] * 101\n        for n in nums:\n            count[n] += 1\n            \n        seen = set()\n        remaining = len(nums)\n        \n        while remaining &gt; 0:\n            # Find min\n            min_val = 0\n            for i in range(1, 101):\n                if count[i] &gt; 0:\n                    min_val = i\n                    break\n            \n            # Find max\n            max_val = 0\n            for i in range(100, 0, -1):\n                if count[i] &gt; 0:\n                    max_val = i\n                    break\n                    \n            count[min_val] -= 1\n            count[max_val] -= 1\n            remaining -= 2\n            \n            seen.add((min_val + max_val) / 2)\n            \n        return len(seen)\n" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int distinctAverages(int[] nums) {\n        int[] count = new int[101];\n        for (int n : nums) count[n]++;\n        \n        Set&lt;Double&gt; seen = new HashSet&lt;&gt;();\n        int remaining = nums.length;\n        \n        while (remaining &gt; 0) {\n            int min = 0;\n            for (int i = 1; i &lt;= 100; i++) {\n                if (count[i] &gt; 0) {\n                    min = i;\n                    break;\n                }\n            }\n            \n            int max = 0;\n            for (int i = 100; i &gt;= 1; i--) {\n                if (count[i] &gt; 0) {\n                    max = i;\n                    break;\n                }\n            }\n            \n            count[min]--;\n            count[max]--;\n            remaining -= 2;\n            \n            seen.add((min + max) / 2.0);\n        }\n        return seen.size();\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int distinctAverages(vector&lt;int&gt;& nums) {\n        int count[101] = {0};\n        for (int n : nums) count[n]++;\n        \n        unordered_set&lt;double&gt; seen;\n        int remaining = nums.size();\n        \n        while (remaining &gt; 0) {\n            int minVal = 0;\n            for (int i = 1; i &lt;= 100; ++i) {\n                if (count[i] &gt; 0) {\n                    minVal = i;\n                    break;\n                }\n            }\n            \n            int maxVal = 0;\n            for (int i = 100; i &gt;= 1; --i) {\n                if (count[i] &gt; 0) {\n                    maxVal = i;\n                    break;\n                }\n            }\n            \n            count[minVal]--;\n            count[maxVal]--;\n            remaining -= 2;\n            \n            seen.insert((minVal + maxVal) / 2.0);\n        }\n        return seen.size();\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var distinctAverages = function(nums) {\n    const count = new Array(101).fill(0);\n    for (const n of nums) count[n]++;\n    \n    const seen = new Set();\n    let remaining = nums.length;\n    \n    while (remaining &gt; 0) {\n        let min = 0;\n        for (let i = 1; i &lt;= 100; i++) {\n            if (count[i] &gt; 0) {\n                min = i;\n                break;\n            }\n        }\n        \n        let max = 0;\n        for (let i = 100; i &gt;= 1; i--) {\n            if (count[i] &gt; 0) {\n                max = i;\n                break;\n            }\n        }\n        \n        count[min]--;\n        count[max]--;\n        remaining -= 2;\n        \n        seen.add((min + max) / 2);\n    }\n    return seen.size();\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function distinctAverages(nums: number[]): number {\n    const count = new Array(101).fill(0);\n    for (const n of nums) count[n]++;\n    \n    const seen = new Set&lt;number&gt;();\n    let remaining = nums.length;\n    \n    while (remaining &gt; 0) {\n        let min = 0;\n        for (let i = 1; i &lt;= 100; i++) {\n            if (count[i] &gt; 0) {\n                min = i;\n                break;\n            }\n        }\n        \n        let max = 0;\n        for (let i = 100; i &gt;= 1; i--) {\n            if (count[i] &gt; 0) {\n                max = i;\n                break;\n            }\n        }\n        \n        count[min]--;\n        count[max]--;\n        remaining -= 2;\n        \n        seen.add((min + max) / 2);\n    }\n    return seen.size();\n}\n" },]} />

**Complexity**
- Time: O(n * C), where C is the range of values (100). Since C is constant, this is effectively O(n).
- Space: O(C) for the frequency array, plus O(n) for the set.
- Notes: This approach avoids sorting and is very efficient for small value ranges.

---
## Brute Force Simulation

**Intuition**
Directly simulate the process described in the problem. In each step, scan the array to find the minimum and maximum elements, remove them, and calculate the average.

**Steps**
- Convert the array to a mutable list structure.
- Initialize a set for distinct averages.
- While the list is not empty:
  - Find the minimum value in the list.
  - Find the maximum value in the list.
  - Remove the first occurrence of the minimum value.
  - Remove the first occurrence of the maximum value.
  - Calculate the average and add to the set.
- Return the size of the set.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def distinctAverages(self, nums: list[int]) -&gt; int:\n        seen = set()\n        while nums:\n            mn = min(nums)\n            mx = max(nums)\n            nums.remove(mn)\n            nums.remove(mx)\n            seen.add((mn + mx) / 2)\n        return len(seen)\n" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int distinctAverages(int[] nums) {\n        List&lt;Integer&gt; list = new ArrayList&lt;&gt;();\n        for (int n : nums) list.add(n);\n        \n        Set&lt;Double&gt; seen = new HashSet&lt;&gt;();\n        \n        while (!list.isEmpty()) {\n            int min = Collections.min(list);\n            int max = Collections.max(list);\n            \n            list.remove(Integer.valueOf(min));\n            list.remove(Integer.valueOf(max));\n            \n            seen.add((min + max) / 2.0);\n        }\n        return seen.size();\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;algorithm&gt;\n#include &lt;unordered_set&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int distinctAverages(vector&lt;int&gt;& nums) {\n        vector&lt;int&gt; v = nums;\n        unordered_set&lt;double&gt; seen;\n        \n        while (!v.empty()) {\n            auto minIt = min_element(v.begin(), v.end());\n            int minVal = *minIt;\n            v.erase(minIt);\n            \n            auto maxIt = max_element(v.begin(), v.end());\n            int maxVal = *maxIt;\n            v.erase(maxIt);\n            \n            seen.insert((minVal + maxVal) / 2.0);\n        }\n        return seen.size();\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var distinctAverages = function(nums) {\n    const seen = new Set();\n    let arr = [...nums];\n    \n    while (arr.length &gt; 0) {\n        const min = Math.min(...arr);\n        const max = Math.max(...arr);\n        \n        const minIdx = arr.indexOf(min);\n        arr.splice(minIdx, 1);\n        \n        const maxIdx = arr.indexOf(max);\n        arr.splice(maxIdx, 1);\n        \n        seen.add((min + max) / 2);\n    }\n    return seen.size();\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function distinctAverages(nums: number[]): number {\n    const seen = new Set&lt;number&gt;();\n    let arr = [...nums];\n    \n    while (arr.length &gt; 0) {\n        const min = Math.min(...arr);\n        const max = Math.max(...arr);\n        \n        const minIdx = arr.indexOf(min);\n        arr.splice(minIdx, 1);\n        \n        const maxIdx = arr.indexOf(max);\n        arr.splice(maxIdx, 1);\n        \n        seen.add((min + max) / 2);\n    }\n    return seen.size();\n}\n" },]} />

**Complexity**
- Time: O(n²) because finding min/max takes O(n) and removing elements takes O(n), repeated n/2 times.
- Space: O(n) to store the list and the set.
- Notes: This is the least efficient approach but directly follows the problem statement.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Number of Distinct Averages Solutions","datePublished":"2025-06-15","description":"Calculate the number of distinct averages obtained by repeatedly pairing the minimum and maximum numbers in an array.","keywords":"LeetCode, Number of Distinct Averages, Easy, Array, Hash Table, Two Pointers, Sorting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Sorting and Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n log n) due to the sorting step."}},{"@type":"Question","name":"What is the time complexity of the Frequency Map approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n * C), where C is the range of values (100). Since C is constant, this is effectively O(n)."}},{"@type":"Question","name":"What is the time complexity of the Brute Force Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n²) because finding and removing elements takes linear time for each operation."}}]}).replace(/</g,'\u003c')}
</script>