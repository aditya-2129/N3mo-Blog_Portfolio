---
title: "Average Value of Even Numbers That Are Divisible by Three"
summary: "Calculate the average of numbers in an array that are divisible by both 2 and 3."
date: "May 06, 2025"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 63.20% | **Paid:** No
> **Topics:** Array, Math

Given an integer array nums, return the average value of all even numbers that are divisible by three.

The average value is the integer division of the sum of the elements by the number of elements.

Note that the average of 0 elements is considered to be 0.

- Examples
- Constraints
- [Iterative Single Pass](#iterative-single-pass)
- [Functional Approach](#functional-approach)

## Examples

Example 1:
```text
Input: nums = [1,3,6,10,12]
Output: 9
Explanation: 6 and 12 are even numbers that are divisible by 3. (6 + 12) / 2 = 9.
```

Example 2:
```text
Input: nums = [1,2,4,7,10]
Output: 0
Explanation: There are no even numbers that are divisible by 3 in the given array.
```

## Constraints

```text
1 <= nums.length <= 1000
1 <= nums[i] <= 1000
```

---

## Iterative Single Pass

**Intuition**
We iterate through the array once, checking if each number satisfies the condition of being divisible by both 2 and 3 (which is equivalent to being divisible by 6). We accumulate the sum and count of these valid numbers to compute the average.

**Steps**
- Initialize `total_sum` to 0 and `count` to 0.
- Loop through each number in the input array.
- If the number is divisible by 6, add it to `total_sum` and increment `count`.
- After the loop, if `count` is 0, return 0. Otherwise, return `total_sum / count`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def averageValue(self, nums: List[int]) -&gt; int:\n        total_sum = 0\n        count = 0\n        for num in nums:\n            if num % 6 == 0:\n                total_sum += num\n                count += 1\n        return total_sum // count if count &gt; 0 else 0\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int averageValue(int[] nums) {\n        int sum = 0;\n        int count = 0;\n        for (int num : nums) {\n            if (num % 6 == 0) {\n                sum += num;\n                count++;\n            }\n        }\n        return count == 0 ? 0 : sum / count;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int averageValue(vector&lt;int&gt;& nums) {\n        int sum = 0;\n        int count = 0;\n        for (int num : nums) {\n            if (num % 6 == 0) {\n                sum += num;\n                count++;\n            }\n        }\n        return count == 0 ? 0 : sum / count;\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var averageValue = function(nums) {\n    let sum = 0;\n    let count = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if (nums[i] % 6 === 0) {\n            sum += nums[i];\n            count++;\n        }\n    }\n    return count === 0 ? 0 : Math.floor(sum / count);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function averageValue(nums: number[]): number {\n    let sum = 0;\n    let count = 0;\n    for (const num of nums) {\n        if (num % 6 === 0) {\n            sum += num;\n            count++;\n        }\n    }\n    return count === 0 ? 0 : Math.floor(sum / count);\n}\n" },]} />

**Complexity**
- Time: O(n), where n is the number of elements in the array.
- Space: O(1), as we only use a few variables for storage.
- Notes: This is the most efficient approach in terms of both time and space complexity.

## Functional Approach

**Intuition**
We utilize functional programming constructs like `filter` and `reduce` (or streams) to declaratively process the array. We first filter the numbers that are divisible by 6, then calculate the sum and count of the filtered list.

**Steps**
- Filter the array to keep only numbers divisible by 6.
- Calculate the sum of the filtered numbers.
- Determine the count of the filtered numbers.
- Return the integer division of the sum by the count, or 0 if the count is 0.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def averageValue(self, nums: List[int]) -&gt; int:\n        filtered = [x for x in nums if x % 6 == 0]\n        return sum(filtered) // len(filtered) if filtered else 0\n" },
{ name: "Java", lang: "java", code: "import java.util.IntSummaryStatistics;\nimport java.util.Arrays;\n\nclass Solution {\n    public int averageValue(int[] nums) {\n        IntSummaryStatistics stats = Arrays.stream(nums)\n            .filter(n -&gt; n % 6 == 0)\n            .summaryStatistics();\n        return stats.getCount() == 0 ? 0 : (int) (stats.getSum() / stats.getCount());\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;numeric&gt;\n#include &lt;algorithm&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int averageValue(vector&lt;int&gt;& nums) {\n        vector&lt;int&gt; valid;\n        copy_if(nums.begin(), nums.end(), back_inserter(valid), [](int n){\n            return n % 6 == 0;\n        });\n        if (valid.empty()) return 0;\n        int sum = accumulate(valid.begin(), valid.end(), 0);\n        return sum / valid.size();\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "var averageValue = function(nums) {\n    const filtered = nums.filter(n =&gt; n % 6 === 0);\n    if (filtered.length === 0) return 0;\n    const sum = filtered.reduce((acc, curr) =&gt; acc + curr, 0);\n    return Math.floor(sum / filtered.length);\n};\n" },
{ name: "TypeScript", lang: "typescript", code: "function averageValue(nums: number[]): number {\n    const filtered = nums.filter(n =&gt; n % 6 === 0);\n    if (filtered.length === 0) return 0;\n    const sum = filtered.reduce((acc, curr) =&gt; acc + curr, 0);\n    return Math.floor(sum / filtered.length);\n}\n" },]} />

**Complexity**
- Time: O(n), as we must traverse the array to filter and reduce.
- Space: O(n) in the worst case (if all elements are valid), as functional methods often create intermediate arrays or objects.
- Notes: While concise, this approach may use more memory than the iterative approach due to intermediate collections.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Average Value of Even Numbers That Are Divisible by Three Solutions","datePublished":"2025-05-06","description":"Calculate the average of numbers in an array that are divisible by both 2 and 3.","keywords":"LeetCode, Average Value of Even Numbers That Are Divisible by Three, Easy, Array, Math"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Single Pass approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the number of elements in the array."}},{"@type":"Question","name":"What is the space complexity of the Functional Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) in the worst case, as it creates a new array or stream to store the filtered elements."}},{"@type":"Question","name":"How do we check if a number is even and divisible by three efficiently?","acceptedAnswer":{"@type":"Answer","text":"We can check if a number is divisible by 6, since 6 is the least common multiple of 2 and 3."}}]}).replace(/</g,'\u003c')}
</script>