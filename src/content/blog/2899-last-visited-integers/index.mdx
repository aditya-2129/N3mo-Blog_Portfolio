---
title: "Last Visited Integers"
summary: "Given an array of strings, process integers and 'prev' commands to return the last visited integers."
date: "Jun 20, 2025"
tags:
  - Array
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 62.30% | **Paid:** No
> **Topics:** Array, Simulation

You are given an array of strings words, where words[i] is either a digit string (0-9) or the string "prev".

Starting with an empty array nums, for each string word in words:

- If word is a digit string, push the integer value of word onto nums.
- If word is "prev", let k be the number of consecutive "prev" strings including the current one. Find the kth most recent element from nums. If k is greater than the size of nums, use -1 instead.

Return an integer array containing the answer for every "prev" string in words.

- Examples
- Constraints
- [Simulation with Stack](#simulation-with-stack)
- [Array Indexing Approach](#array-indexing-approach)

## Examples

Example 1:

```text
Input: words = ["1","2","prev","prev","prev"]
Output: [2,1,-1]
Explanation:
- "1" : Push 1 onto nums. nums = [1].
- "2" : Push 2 onto nums. nums = [1,2].
- "prev" : k = 1. The 1st most recent element is 2. Append 2 to answer.
- "prev" : k = 2. The 2nd most recent element is 1. Append 1 to answer.
- "prev" : k = 3. There are only 2 elements in nums. Append -1 to answer.
```

Example 2:

```text
Input: words = ["1","prev","2","prev","prev"]
Output: [1,2,1]
Explanation:
- "1" : Push 1 onto nums. nums = [1].
- "prev" : k = 1. The 1st most recent element is 1. Append 1 to answer.
- "2" : Push 2 onto nums. nums = [1,2].
- "prev" : k = 1. The 1st most recent element is 2. Append 2 to answer.
- "prev" : k = 2. The 2nd most recent element is 1. Append 1 to answer.
```

## Constraints

```text
1 <= words.length <= 100
words[i] is either "prev" or a digit string from "0" to "9".
```

---

## Simulation with Stack

**Intuition**
Maintain a stack of seen integers and track consecutive "prev" occurrences to efficiently retrieve the kth most recent element.

**Steps**
- Initialize an empty stack to store seen integers and a counter for consecutive "prev" strings
- Iterate through each word in the input array
- If the word is a digit, convert it to an integer, push onto the stack, and reset the "prev" counter
- If the word is "prev", increment the counter and check if we have enough elements in the stack
- Append the appropriate value (kth from end or -1) to the result array

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def lastVisitedIntegers(self, words: list[str]) -&gt; list[int]:\n        seen = []\n        result = []\n        prev_count = 0\n        \n        for word in words:\n            if word == \"prev\":\n                prev_count += 1\n                if prev_count &lt;= len(seen):\n                    result.append(seen[-prev_count])\n                else:\n                    result.append(-1)\n            else:\n                prev_count = 0\n                seen.append(int(word))\n        \n        return result" },
{ name: "Java", lang: "java", code: "class Solution {\n    public List&lt;Integer&gt; lastVisitedIntegers(List&lt;String&gt; words) {\n        List&lt;Integer&gt; seen = new ArrayList&lt;&gt;();\n        List&lt;Integer&gt; result = new ArrayList&lt;&gt;();\n        int prevCount = 0;\n        \n        for (String word : words) {\n            if (word.equals(\"prev\")) {\n                prevCount++;\n                if (prevCount &lt;= seen.size()) {\n                    result.add(seen.get(seen.size() - prevCount));\n                } else {\n                    result.add(-1);\n                }\n            } else {\n                prevCount = 0;\n                seen.add(Integer.parseInt(word));\n            }\n        }\n        \n        return result;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; lastVisitedIntegers(vector&lt;string&gt;& words) {\n        vector&lt;int&gt; seen;\n        vector&lt;int&gt; result;\n        int prevCount = 0;\n        \n        for (const string& word : words) {\n            if (word == \"prev\") {\n                prevCount++;\n                if (prevCount &lt;= (int)seen.size()) {\n                    result.push_back(seen[seen.size() - prevCount]);\n                } else {\n                    result.push_back(-1);\n                }\n            } else {\n                prevCount = 0;\n                seen.push_back(stoi(word));\n            }\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    lastVisitedIntegers(words) {\n        const seen = [];\n        const result = [];\n        let prevCount = 0;\n        \n        for (const word of words) {\n            if (word === \"prev\") {\n                prevCount++;\n                if (prevCount &lt;= seen.length) {\n                    result.push(seen[seen.length - prevCount]);\n                } else {\n                    result.push(-1);\n                }\n            } else {\n                prevCount = 0;\n                seen.push(parseInt(word));\n            }\n        }\n        \n        return result;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    lastVisitedIntegers(words: string[]): number[] {\n        const seen: number[] = [];\n        const result: number[] = [];\n        let prevCount = 0;\n        \n        for (const word of words) {\n            if (word === \"prev\") {\n                prevCount++;\n                if (prevCount &lt;= seen.length) {\n                    result.push(seen[seen.length - prevCount]);\n                } else {\n                    result.push(-1);\n                }\n            } else {\n                prevCount = 0;\n                seen.push(parseInt(word));\n            }\n        }\n        \n        return result;\n    }\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of words array
- Space: O(n) for storing seen integers and result
- Notes: Simple and intuitive approach with clear logic flow

---

## Array Indexing Approach

**Intuition**
Use array indexing directly instead of stack operations, treating the seen array as a dynamic list with index-based access.

**Steps**
- Create an array to store integer values and another for results
- Track the current position in the seen array and consecutive "prev" count
- For each word, either add the integer value or compute the index based on prev count
- Handle boundary cases where prev count exceeds available elements

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def lastVisitedIntegers(self, words: list[str]) -&gt; list[int]:\n        nums = []\n        ans = []\n        k = 0\n        \n        for word in words:\n            if word == \"prev\":\n                k += 1\n                idx = len(nums) - k\n                if idx &gt;= 0:\n                    ans.append(nums[idx])\n                else:\n                    ans.append(-1)\n            else:\n                k = 0\n                nums.append(int(word))\n        \n        return ans" },
{ name: "Java", lang: "java", code: "class Solution {\n    public List&lt;Integer&gt; lastVisitedIntegers(List&lt;String&gt; words) {\n        List&lt;Integer&gt; nums = new ArrayList&lt;&gt;();\n        List&lt;Integer&gt; ans = new ArrayList&lt;&gt;();\n        int k = 0;\n        \n        for (String word : words) {\n            if (word.equals(\"prev\")) {\n                k++;\n                int idx = nums.size() - k;\n                if (idx &gt;= 0) {\n                    ans.add(nums.get(idx));\n                } else {\n                    ans.add(-1);\n                }\n            } else {\n                k = 0;\n                nums.add(Integer.parseInt(word));\n            }\n        }\n        \n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; lastVisitedIntegers(vector&lt;string&gt;& words) {\n        vector&lt;int&gt; nums;\n        vector&lt;int&gt; ans;\n        int k = 0;\n        \n        for (const string& word : words) {\n            if (word == \"prev\") {\n                k++;\n                int idx = (int)nums.size() - k;\n                if (idx &gt;= 0) {\n                    ans.push_back(nums[idx]);\n                } else {\n                    ans.push_back(-1);\n                }\n            } else {\n                k = 0;\n                nums.push_back(stoi(word));\n            }\n        }\n        \n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    lastVisitedIntegers(words) {\n        const nums = [];\n        const ans = [];\n        let k = 0;\n        \n        for (const word of words) {\n            if (word === \"prev\") {\n                k++;\n                const idx = nums.length - k;\n                if (idx &gt;= 0) {\n                    ans.push(nums[idx]);\n                } else {\n                    ans.push(-1);\n                }\n            } else {\n                k = 0;\n                nums.push(parseInt(word));\n            }\n        }\n        \n        return ans;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    lastVisitedIntegers(words: string[]): number[] {\n        const nums: number[] = [];\n        const ans: number[] = [];\n        let k = 0;\n        \n        for (const word of words) {\n            if (word === \"prev\") {\n                k++;\n                const idx = nums.length - k;\n                if (idx &gt;= 0) {\n                    ans.push(nums[idx]);\n                } else {\n                    ans.push(-1);\n                }\n            } else {\n                k = 0;\n                nums.push(parseInt(word));\n            }\n        }\n        \n        return ans;\n    }\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of words array
- Space: O(n) for storing nums and ans arrays
- Notes: Similar to stack approach but uses explicit index calculation for clarity

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Last Visited Integers Solutions","datePublished":"2025-06-20","description":"Given an array of strings, process integers and 'prev' commands to return the last visited integers.","keywords":"LeetCode, Last Visited Integers, Easy, Array, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Simulation with Stack approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of words array"}},{"@type":"Question","name":"What is the space complexity of Array Indexing approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for storing nums and ans arrays"}},{"@type":"Question","name":"What data structure is best suited for this problem?","acceptedAnswer":{"@type":"Answer","text":"A stack or dynamic array works well for tracking seen integers in order"}}]}).replace(/</g,'\u003c')}
</script>