---
title: "Count the Number of Vowel Strings in Range"
summary: "You are given a 0-indexed string array words and two integers left and right. Return the number of vowel strings in the range [left, right]."
date: "Oct 28, 2024"
tags:
  - Array
  - String
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 74.30% | **Paid:** No
> **Topics:** Array, String, Counting

You are given a 0-indexed string array words and two integers left and right.

A string is called a vowel string if it starts with a vowel character and ends with a vowel character. The vowel characters are 'a', 'e', 'i', 'o', and 'u'.

Return the number of vowel strings in the range [left, right].

- Examples
- Constraints
- [Iterative Approach](#iterative-approach)
- [Functional Approach](#functional-approach)

## Examples

```text
Input: words = ["how","leto","let","be","code"], left = 0, right = 2
Output: 1
Explanation: 
- "how" starts and ends with consonants.
- "leto" starts and ends with vowels.
- "let" starts and ends with consonants.
There is 1 vowel string in the range.
```

```text
Input: words = ["are","amy","u"], left = 0, right = 2
Output: 2
Explanation: 
- "are" starts and ends with vowels.
- "amy" starts with a vowel and ends with a consonant.
- "u" starts and ends with a vowel.
There are 2 vowel strings in the range.
```

## Constraints

```text
1 <= words.length <= 1000
1 <= words[i].length <= 10
words[i] consists of lowercase English letters.
0 <= left <= right < words.length
```

---

## Iterative Approach

**Intuition**
We simply iterate through the specified range of indices and check each word to see if it satisfies the vowel string condition.

**Steps**
- Define a set or string containing the vowels 'a', 'e', 'i', 'o', 'u'.
- Initialize a counter to 0.
- Loop from index `left` to `right` (inclusive).
- For each word, check if the first character and the last character are present in the vowels set.
- If both are vowels, increment the counter.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def vowelStrings(self, words: list[str], left: int, right: int) -&gt; int:\n        vowels = set('aeiou')\n        count = 0\n        for i in range(left, right + 1):\n            word = words[i]\n            if word[0] in vowels and word[-1] in vowels:\n                count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int vowelStrings(String[] words, int left, int right) {\n        Set&lt;Character&gt; vowels = new HashSet&lt;&gt;(Arrays.asList('a', 'e', 'i', 'o', 'u'));\n        int count = 0;\n        for (int i = left; i &lt;= right; i++) {\n            String word = words[i];\n            if (vowels.contains(word.charAt(0)) && vowels.contains(word.charAt(word.length() - 1))) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int vowelStrings(vector&lt;string&gt;& words, int left, int right) {\n        unordered_set&lt;char&gt; vowels = {'a', 'e', 'i', 'o', 'u'};\n        int count = 0;\n        for (int i = left; i &lt;= right; ++i) {\n            string word = words[i];\n            if (vowels.count(word.front()) && vowels.count(word.back())) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var vowelStrings = function(words, left, right) {\n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    let count = 0;\n    for (let i = left; i &lt;= right; i++) {\n        const word = words[i];\n        if (vowels.has(word[0]) && vowels.has(word[word.length - 1])) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function vowelStrings(words: string[], left: number, right: number): number {\n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    let count = 0;\n    for (let i = left; i &lt;= right; i++) {\n        const word = words[i];\n        if (vowels.has(word[0]) && vowels.has(word[word.length - 1])) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(N), where N is the number of words in the range (right - left + 1).
- Space: O(1), as we only use a fixed-size set for vowels and a counter.
- Notes: This is the most straightforward approach.

---

## Functional Approach

**Intuition**
We utilize functional programming constructs like streams, filters, or list comprehensions to declaratively filter the words that match the criteria and count them.

**Steps**
- Define the vowels.
- Create a sub-list or stream of words from index `left` to `right`.
- Filter this collection to keep only words where the first and last characters are vowels.
- Return the size or count of the filtered collection.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def vowelStrings(self, words: list[str], left: int, right: int) -&gt; int:\n        vowels = set('aeiou')\n        return sum(1 for w in words[left:right+1] if w[0] in vowels and w[-1] in vowels)" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\n\nclass Solution {\n    public int vowelStrings(String[] words, int left, int right) {\n        Set&lt;Character&gt; vowels = new HashSet&lt;&gt;(Arrays.asList('a', 'e', 'i', 'o', 'u'));\n        return (int) IntStream.rangeClosed(left, right)\n            .filter(i -&gt; vowels.contains(words[i].charAt(0)) && vowels.contains(words[i].charAt(words[i].length() - 1)))\n            .count();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n\nclass Solution {\npublic:\n    int vowelStrings(vector&lt;string&gt;& words, int left, int right) {\n        unordered_set&lt;char&gt; vowels = {'a', 'e', 'i', 'o', 'u'};\n        return count_if(words.begin() + left, words.begin() + right + 1, [&](const string& word) {\n            return vowels.count(word.front()) && vowels.count(word.back());\n        });\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var vowelStrings = function(words, left, right) {\n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    return words.slice(left, right + 1).filter(word =&gt; vowels.has(word[0]) && vowels.has(word[word.length - 1])).length;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function vowelStrings(words: string[], left: number, right: number): number {\n    const vowels = new Set(['a', 'e', 'i', 'o', 'u']);\n    return words.slice(left, right + 1).filter(word =&gt; vowels.has(word[0]) && vowels.has(word[word.length - 1])).length;\n}" },]} />

**Complexity**
- Time: O(N), where N is the number of words in the range.
- Space: O(1) auxiliary space, though `slice` in JS/TS creates a shallow copy O(N).
- Notes: More concise syntax, often preferred in modern codebases for readability.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Count the Number of Vowel Strings in Range Solutions","datePublished":"2024-10-28","description":"You are given a 0-indexed string array words and two integers left and right. Return the number of vowel strings in the range [left, right].","keywords":"LeetCode, Count the Number of Vowel Strings in Range, Easy, Array, String, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N), where N is the number of words in the range [left, right]."}},{"@type":"Question","name":"What is the space complexity of the Functional Approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is generally O(1) auxiliary space, but methods like slice in JavaScript may use O(N) space to create the sub-array."}},{"@type":"Question","name":"What characters are considered vowels in this problem?","acceptedAnswer":{"@type":"Answer","text":"The vowel characters are 'a', 'e', 'i', 'o', and 'u'."}}]}).replace(/</g,'\u003c')}
</script>