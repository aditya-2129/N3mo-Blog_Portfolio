---
title: "Create Hello World Function"
summary: "Write a function createHelloWorld that returns a new function which always returns 'Hello World'."
date: "Apr 26, 2024"
tags:
  - JavaScript
  - Function
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 81.90% | **Paid:** No
> **Topics:** N/A

Write a function createHelloWorld. It should return a new function that always returns "Hello World".

- Examples
- Constraints
- [Direct Function Return](#direct-function-return)
- [Arrow/Lambda Function](#arrowlambda-function)
- [Class-based Approach](#class-based-approach)

## Examples

```text
Input:  args = []
Output: "Hello World"
Explanation:
const f = createHelloWorld();
f(); // "Hello World"
```

```text
Input:  args = [{},null,42]
Output: "Hello World"
Explanation:
const f = createHelloWorld();
f({}, null, 42); // "Hello World"
```

## Constraints

```text
0 <= args.length <= 10
```

---

## Direct Function Return

**Intuition**
Create a function that returns another function, where the inner function simply returns the string "Hello World" regardless of any arguments passed to it.

**Steps**
- Define the outer function createHelloWorld
- Inside it, define and return an inner function
- The inner function returns "Hello World"

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "def createHelloWorld():\n    def f():\n        return \"Hello World\"\n    return f" },
{ name: "Java", lang: "java", code: "class Solution {\n    public java.util.function.Supplier&lt;String&gt; createHelloWorld() {\n        return () -&gt; \"Hello World\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    std::function&lt;std::string()&gt; createHelloWorld() {\n        return []() { return \"Hello World\"; };\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var createHelloWorld = function() {\n    return function() {\n        return \"Hello World\";\n    };\n};" },
{ name: "TypeScript", lang: "typescript", code: "function createHelloWorld(): () =&gt; string {\n    return function() {\n        return \"Hello World\";\n    };\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This is the most straightforward approach with minimal overhead.

---

## Arrow/Lambda Function

**Intuition**
Use modern arrow/lambda syntax for a more concise implementation that achieves the same result with cleaner code.

**Steps**
- Define createHelloWorld using arrow/lambda syntax
- Return an arrow/lambda function that returns "Hello World"

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "def createHelloWorld():\n    return lambda: \"Hello World\"" },
{ name: "Java", lang: "java", code: "class Solution {\n    public java.util.function.Supplier&lt;String&gt; createHelloWorld() {\n        return () -&gt; \"Hello World\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    std::function&lt;std::string()&gt; createHelloWorld() {\n        return []() -&gt; std::string { return \"Hello World\"; };\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var createHelloWorld = function() {\n    return () =&gt; \"Hello World\";\n};" },
{ name: "TypeScript", lang: "typescript", code: "const createHelloWorld = (): (() =&gt; string) =&gt; {\n    return () =&gt; \"Hello World\";\n};" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: More concise syntax, functionally identical to the direct approach.

---

## Class-based Approach

**Intuition**
Encapsulate the hello world functionality in a class with a callable method, demonstrating object-oriented programming principles.

**Steps**
- Create a class with a method that returns "Hello World"
- Return an instance or method reference from createHelloWorld

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class HelloWorld:\n    def __call__(self):\n        return \"Hello World\"\n\ndef createHelloWorld():\n    return HelloWorld()" },
{ name: "Java", lang: "java", code: "class Solution {\n    public java.util.function.Supplier&lt;String&gt; createHelloWorld() {\n        return new HelloWorld()::get;\n    }\n}\n\nclass HelloWorld {\n    public String get() {\n        return \"Hello World\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    std::function&lt;std::string()&gt; createHelloWorld() {\n        return HelloWorld();\n    }\n};\n\nclass HelloWorld {\npublic:\n    std::string operator()() const {\n        return \"Hello World\";\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class HelloWorld {\n    constructor() {}\n    call() {\n        return \"Hello World\";\n    }\n}\n\nvar createHelloWorld = function() {\n    const hw = new HelloWorld();\n    return function() {\n        return hw.call();\n    };\n};" },
{ name: "TypeScript", lang: "typescript", code: "class HelloWorld {\n    call(): string {\n        return \"Hello World\";\n    }\n}\n\nfunction createHelloWorld(): () =&gt; string {\n    const hw = new HelloWorld();\n    return () =&gt; hw.call();\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: More verbose but demonstrates OOP patterns; useful for extending functionality.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Create Hello World Function Solutions","datePublished":"2024-04-26","description":"Write a function createHelloWorld that returns a new function which always returns 'Hello World'.","keywords":"LeetCode, Create Hello World Function, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of Direct Function Return approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - The function returns immediately with constant time operations."}},{"@type":"Question","name":"What is the space complexity of Arrow/Lambda Function approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Only stores a reference to the function, no additional data structures."}},{"@type":"Question","name":"What is the time complexity of Class-based Approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) - Object creation and method calls are constant time operations."}}]}).replace(/</g,'\u003c')}
</script>