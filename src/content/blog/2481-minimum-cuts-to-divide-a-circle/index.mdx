---
title: "Minimum Cuts to Divide a Circle"
summary: "Given a circle, find the minimum number of straight cuts needed to divide it into n equal pieces."
date: "Apr 25, 2024"
tags:
  - Math
  - Geometry
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 56.20% | **Paid:** No
> **Topics:** Math, Geometry

Given an integer n, return the minimum number of cuts needed to divide a circle into n equal pieces.

A valid cut is a straight line that passes through the center of the circle and divides the circle into two pieces.

- Examples
- Constraints
- [Mathematical Logic](#mathematical-logic)
- [Bitwise Manipulation](#bitwise-manipulation)

## Examples

### Example 1

**Input:**
```text
n = 4
```

**Output:**
```text
2
```

**Explanation:**
The above figure shows how cutting the circle twice through the middle divides it into 4 equal slices.

### Example 2

**Input:**
```text
n = 3
```

**Output:**
```text
3
```

**Explanation:**
At least 3 cuts are needed to divide the circle into 3 equal slices.
It can be shown that less than 3 cuts cannot result in 3 slices of equal size and shape.
Also note that the first cut will not divide the circle into distinct parts.

## Constraints

```text
- 1 <= n <= 100
```

---
## Mathematical Logic

**Intuition**
The problem relies on the parity of the number of pieces `n`. If `n` is 1, no cuts are needed. If `n` is even, we can cut through the center `n/2` times to create `n` pieces (each cut creates 2 pieces). If `n` is odd, we cannot cut through the center to get an odd number of pieces (except 1), so we need `n` cuts.

**Steps**
- Check if `n` is 1. If so, return 0.
- Check if `n` is even. If so, return `n / 2`.
- Otherwise, `n` is odd, return `n`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numberOfCuts(self, n: int) -&gt; int:\n        if n == 1:\n            return 0\n        if n % 2 == 0:\n            return n // 2\n        return n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numberOfCuts(int n) {\n        if (n == 1) {\n            return 0;\n        }\n        if (n % 2 == 0) {\n            return n / 2;\n        }\n        return n;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numberOfCuts(int n) {\n        if (n == 1) return 0;\n        if (n % 2 == 0) return n / 2;\n        return n;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar numberOfCuts = function(n) {\n    if (n === 1) return 0;\n    if (n % 2 === 0) return n / 2;\n    return n;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfCuts(n: number): number {\n    if (n === 1) return 0;\n    if (n % 2 === 0) return n / 2;\n    return n;\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This is the most straightforward approach using basic arithmetic checks.

---
## Bitwise Manipulation

**Intuition**
We can optimize the parity check and division using bitwise operators. Checking if a number is odd is equivalent to checking if the least significant bit is set (`n & 1`). Dividing an even number by 2 is equivalent to a right shift by 1 (`n &gt;&gt; 1`).

**Steps**
- If `n` is 1, return 0.
- If `n` is odd (`n & 1`), return `n`.
- If `n` is even, return `n &gt;&gt; 1`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def numberOfCuts(self, n: int) -&gt; int:\n        if n == 1:\n            return 0\n        if n & 1:\n            return n\n        return n &gt;&gt; 1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int numberOfCuts(int n) {\n        if (n == 1) {\n            return 0;\n        }\n        if ((n & 1) != 0) {\n            return n;\n        }\n        return n &gt;&gt; 1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int numberOfCuts(int n) {\n        if (n == 1) return 0;\n        if (n & 1) return n;\n        return n &gt;&gt; 1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {number} n\n * @return {number}\n */\nvar numberOfCuts = function(n) {\n    if (n === 1) return 0;\n    if (n & 1) return n;\n    return n &gt;&gt; 1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function numberOfCuts(n: number): number {\n    if (n === 1) return 0;\n    if (n & 1) return n;\n    return n &gt;&gt; 1;\n}" },]} />

**Complexity**
- Time: O(1)
- Space: O(1)
- Notes: This approach is functionally identical to the mathematical logic but uses bitwise operations which can be slightly faster at the CPU level.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Cuts to Divide a Circle Solutions","datePublished":"2024-04-25","description":"Given a circle, find the minimum number of straight cuts needed to divide it into n equal pieces.","keywords":"LeetCode, Minimum Cuts to Divide a Circle, Easy, Math, Geometry"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity for the Mathematical Logic approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(1) as it involves a constant number of arithmetic operations."}},{"@type":"Question","name":"What is the space complexity for the Bitwise Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as no additional data structures are used."}}]}).replace(/</g,'\u003c')}
</script>