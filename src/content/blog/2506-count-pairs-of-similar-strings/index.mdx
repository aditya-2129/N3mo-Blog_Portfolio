---
title: "Count Pairs Of Similar Strings"
summary: "Count pairs of strings that contain the same set of unique characters."
date: "Feb 21, 2026"
tags:
  - Array
  - Hash Table
  - String
  - Bit Manipulation
  - Counting
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 73.70% | **Paid:** No
> **Topics:** Array, Hash Table, String, Bit Manipulation, Counting

You are given a 0-indexed string array `words`.

Two strings are similar if they consist of the same characters.

*   For example, `"abca"` and `"cba"` are similar because they consist of the characters `'a'`, `'b'`, and `'c'`.
*   However, `"abacba"` and `"bcfd"` are not similar because they do not consist of the same characters.

Return the number of pairs `(i, j)` such that `i &lt; j` and `words[i]` and `words[j]` are similar.

- Examples
- Constraints
- [Sorting and Hash Map](#sorting-and-hash-map)
- [Bitmasking](#bitmasking)

## Examples

```text
Input: words = ["aba","aabb","abcd","bac","aabc"]
Output: 2
Explanation: There are 2 pairs that satisfy the conditions:
- (0,1) because both words consist of characters {'a', 'b'}.
- (3,4) because both words consist of characters {'a', 'b', 'c'}.
```

```text
Input: words = ["aabb","ab","ba"]
Output: 3
Explanation: There are 3 pairs that satisfy the conditions:
- (0,1) because both words consist of characters {'a', 'b'}.
- (0,2) because both words consist of characters {'a', 'b'}.
- (1,2) because both words consist of characters {'a', 'b'}.
```

```text
Input: words = ["nba","cba","dba"]
Output: 0
Explanation: Since there are no pairs that satisfy the conditions, we return 0.
```

## Constraints

```text
1 <= words.length <= 100
1 <= words[i].length <= 100
words[i] consist of only lowercase English letters.
```

---
## Sorting and Hash Map

**Intuition**
Two strings are similar if they contain the exact same set of unique characters. We can normalize each string by extracting its unique characters, sorting them, and joining them back into a key. Strings that are similar will produce the same normalized key. We then count the frequency of each key and calculate the number of pairs possible for each frequency using the combination formula n(n-1)/2.

**Steps**
- Initialize a hash map to store the frequency of each normalized string key.
- Iterate through each word in the input array.
- For each word, convert it to a set of characters to remove duplicates, then sort the characters and join them to form a unique key.
- Increment the count for this key in the hash map.
- After processing all words, iterate through the values in the hash map.
- For each count `v`, add `v * (v - 1) / 2` to the result.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from collections import Counter\nfrom typing import List\n\nclass Solution:\n    def similarPairs(self, words: List[str]) -&gt; int:\n        counts = Counter()\n        for w in words:\n            # Create a sorted string of unique characters\n            key = ''.join(sorted(set(w)))\n            counts[key] += 1\n        \n        ans = 0\n        for v in counts.values():\n            ans += v * (v - 1) // 2\n        return ans" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int similarPairs(String[] words) {\n        Map&lt;String, Integer&gt; map = new HashMap&lt;&gt;();\n        for (String w : words) {\n            char[] chars = w.toCharArray();\n            // Use TreeSet to sort and remove duplicates\n            Set&lt;Character&gt; set = new TreeSet&lt;&gt;();\n            for (char c : chars) {\n                set.add(c);\n            }\n            StringBuilder sb = new StringBuilder();\n            for (char c : set) {\n                sb.append(c);\n            }\n            String key = sb.toString();\n            map.put(key, map.getOrDefault(key, 0) + 1);\n        }\n        \n        int ans = 0;\n        for (int v : map.values()) {\n            ans += v * (v - 1) / 2;\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;unordered_map&gt;\n#include &lt;set&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int similarPairs(vector&lt;string&gt;& words) {\n        unordered_map&lt;string, int&gt; counts;\n        for (string& w : words) {\n            set&lt;char&gt; s(w.begin(), w.end());\n            string key(s.begin(), s.end());\n            counts[key]++;\n        }\n        \n        int ans = 0;\n        for (auto& p : counts) {\n            int v = p.second;\n            ans += v * (v - 1) / 2;\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string[]} words\n * @return {number}\n */\nvar similarPairs = function(words) {\n    const map = new Map();\n    for (const w of words) {\n        // Create a sorted string of unique characters\n        const unique = [...new Set(w)].sort().join('');\n        map.set(unique, (map.get(unique) || 0) + 1);\n    }\n    \n    let ans = 0;\n    for (const v of map.values()) {\n        ans += v * (v - 1) / 2;\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function similarPairs(words: string[]): number {\n    const map = new Map&lt;string, number&gt;();\n    for (const w of words) {\n        // Create a sorted string of unique characters\n        const unique = [...new Set(w)].sort().join('');\n        map.set(unique, (map.get(unique) || 0) + 1);\n    }\n    \n    let ans = 0;\n    for (const v of map.values()) {\n        ans += v * (v - 1) / 2;\n    }\n    return ans;\n};" },]} />

**Complexity**
- Time: O(N * K log K), where N is the number of words and K is the average length of a word. The log K factor comes from sorting the unique characters of each word.
- Space: O(N), to store the frequency map.
- Notes: This approach is intuitive but slightly slower due to the sorting step.

---
## Bitmasking

**Intuition**
Since the input strings consist only of lowercase English letters, we can represent the set of characters in a string using a 26-bit integer (bitmask). Each bit in the integer corresponds to a letter (e.g., bit 0 for 'a', bit 1 for 'b'). Two strings are similar if and only if their bitmasks are identical. This allows for very efficient comparison and counting using bitwise operations.

**Steps**
- Initialize a hash map to store the frequency of each bitmask.
- Iterate through each word in the input array.
- For each word, initialize a mask to 0.
- Iterate through each character in the word.
- Calculate the bit position: `charCode - 'a'`.
- Set the corresponding bit in the mask using bitwise OR: `mask |= (1 &lt;&lt; bitPosition)`.
- Increment the count for this mask in the hash map.
- After processing all words, iterate through the values in the hash map.
- For each count `v`, add `v * (v - 1) / 2` to the result.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from collections import Counter\nfrom typing import List\n\nclass Solution:\n    def similarPairs(self, words: List[str]) -&gt; int:\n        counts = Counter()\n        for w in words:\n            mask = 0\n            for c in w:\n                mask |= 1 &lt;&lt; (ord(c) - ord('a'))\n            counts[mask] += 1\n        \n        ans = 0\n        for v in counts.values():\n            ans += v * (v - 1) // 2\n        return ans" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int similarPairs(String[] words) {\n        Map&lt;Integer, Integer&gt; map = new HashMap&lt;&gt;();\n        for (String w : words) {\n            int mask = 0;\n            for (int i = 0; i &lt; w.length(); i++) {\n                mask |= 1 &lt;&lt; (w.charAt(i) - 'a');\n            }\n            map.put(mask, map.getOrDefault(mask, 0) + 1);\n        }\n        \n        int ans = 0;\n        for (int v : map.values()) {\n            ans += v * (v - 1) / 2;\n        }\n        return ans;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;string&gt;\n#include &lt;unordered_map&gt;\n\nusing namespace std;\n\nclass Solution {\npublic:\n    int similarPairs(vector&lt;string&gt;& words) {\n        unordered_map&lt;int, int&gt; counts;\n        for (string& w : words) {\n            int mask = 0;\n            for (char c : w) {\n                mask |= 1 &lt;&lt; (c - 'a');\n            }\n            counts[mask]++;\n        }\n        \n        int ans = 0;\n        for (auto& p : counts) {\n            int v = p.second;\n            ans += v * (v - 1) / 2;\n        }\n        return ans;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string[]} words\n * @return {number}\n */\nvar similarPairs = function(words) {\n    const map = new Map();\n    for (const w of words) {\n        let mask = 0;\n        for (let i = 0; i &lt; w.length; i++) {\n            mask |= 1 &lt;&lt; (w.charCodeAt(i) - 97);\n        }\n        map.set(mask, (map.get(mask) || 0) + 1);\n    }\n    \n    let ans = 0;\n    for (const v of map.values()) {\n        ans += v * (v - 1) / 2;\n    }\n    return ans;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function similarPairs(words: string[]): number {\n    const map = new Map&lt;number, number&gt;();\n    for (const w of words) {\n        let mask = 0;\n        for (let i = 0; i &lt; w.length; i++) {\n            mask |= 1 &lt;&lt; (w.charCodeAt(i) - 97);\n        }\n        map.set(mask, (map.get(mask) || 0) + 1);\n    }\n    \n    let ans = 0;\n    for (const v of map.values()) {\n        ans += v * (v - 1) / 2;\n    }\n    return ans;\n};" },]} />

**Complexity**
- Time: O(N * K), where N is the number of words and K is the average length of a word. We iterate through every character once.
- Space: O(N), to store the frequency map.
- Notes: This is the optimal approach for this problem, avoiding the overhead of sorting and string manipulation.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Count Pairs Of Similar Strings Solutions","datePublished":"2026-02-21","description":"Count pairs of strings that contain the same set of unique characters.","keywords":"LeetCode, Count Pairs Of Similar Strings, Easy, Array, Hash Table, String, Bit Manipulation, Counting"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Sorting and Hash Map approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * K log K), where N is the number of words and K is the average length of a word, due to sorting the unique characters."}},{"@type":"Question","name":"What is the time complexity of the Bitmasking approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * K), where N is the number of words and K is the average length of a word, as we process each character exactly once."}},{"@type":"Question","name":"Why is Bitmasking preferred for this problem?","acceptedAnswer":{"@type":"Answer","text":"Bitmasking is preferred because it avoids the overhead of sorting and string manipulation, using efficient bitwise operations to compare character sets."}}]}).replace(/</g,'\u003c')}
</script>