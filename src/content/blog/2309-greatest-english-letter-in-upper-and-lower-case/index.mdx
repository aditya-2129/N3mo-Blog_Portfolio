---
title: "Greatest English Letter in Upper and Lower Case"
summary: "Find the greatest English letter that appears in both uppercase and lowercase in a given string."
date: "Dec 04, 2025"
tags:
  - Hash Table
  - String
  - Enumeration
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 72.00% | **Paid:** No
> **Topics:** Hash Table, String, Enumeration

Given a string s, return the greatest English letter which occurs as both a lowercase and uppercase letter in s. The returned letter should be in uppercase. If no such letter exists, return an empty string.

An English letter b is greater than another English letter a if b appears after a in the English alphabet.

- Examples
- Constraints
- [Brute Force Enumeration](#brute-force-enumeration)
- [Hash Set Approach](#hash-set-approach)
- [Boolean Array Approach](#boolean-array-approach)

## Examples

Example 1:
```text
Input: s = "arRAzFif"
Output: "R"
Explanation: The letter 'R' is the greatest letter that appears in both lower and upper case in s.
```

Example 2:
```text
Input: s = "AbCdEfGhIjK"
Output: ""
Explanation: There is no letter that appears in both lower and upper case in s.
```

Example 3:
```text
Input: s = "aA"
Output: "A"
Explanation: The letter 'A' is the greatest letter that appears in both lower and upper case in s.
```

## Constraints

```text
1 <= s.length <= 1000
s consists of lowercase and uppercase English letters.
```

---
## Brute Force Enumeration

**Intuition**
Iterate from 'Z' down to 'A' and for each letter, check if both its uppercase and lowercase forms exist in the string.

**Steps**
- Iterate from 'Z' down to 'A'
- For each letter, check if both uppercase and lowercase versions exist in the string
- Return the first letter found (which will be the greatest)
- If no letter is found, return an empty string

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def greatestLetter(self, s: str) -&gt; str:\n        for c in range(ord('Z'), ord('A') - 1, -1):\n            upper = chr(c)\n            lower = chr(c + 32)\n            if upper in s and lower in s:\n                return upper\n        return \"\"" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String greatestLetter(String s) {\n        for (char c = 'Z'; c &gt;= 'A'; c--) {\n            char upper = c;\n            char lower = (char)(c + 32);\n            if (s.indexOf(upper) != -1 && s.indexOf(lower) != -1) {\n                return String.valueOf(upper);\n            }\n        }\n        return \"\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string greatestLetter(string s) {\n        for (char c = 'Z'; c &gt;= 'A'; c--) {\n            char upper = c;\n            char lower = c + 32;\n            if (s.find(upper) != string::npos && s.find(lower) != string::npos) {\n                return string(1, upper);\n            }\n        }\n        return \"\";\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar greatestLetter = function(s) {\n    for (let c = 90; c &gt;= 65; c--) {\n        const upper = String.fromCharCode(c);\n        const lower = String.fromCharCode(c + 32);\n        if (s.includes(upper) && s.includes(lower)) {\n            return upper;\n        }\n    }\n    return \"\";\n};" },
{ name: "TypeScript", lang: "typescript", code: "function greatestLetter(s: string): string {\n    for (let c = 90; c &gt;= 65; c--) {\n        const upper = String.fromCharCode(c);\n        const lower = String.fromCharCode(c + 32);\n        if (s.includes(upper) && s.includes(lower)) {\n            return upper;\n        }\n    }\n    return \"\";\n}" },]} />

**Complexity**
- Time: O(26 * n) = O(n) where n is the length of the string
- Space: O(1)
- Notes: Simple but may have repeated string searches

---
## Hash Set Approach

**Intuition**
Store all characters in a hash set for O(1) lookup, then iterate from 'Z' to 'A' checking if both cases exist.

**Steps**
- Create a set containing all characters from the string
- Iterate from 'Z' down to 'A'
- For each letter, check if both uppercase and lowercase versions are in the set
- Return the first letter found
- If no letter is found, return an empty string

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def greatestLetter(self, s: str) -&gt; str:\n        char_set = set(s)\n        for c in range(ord('Z'), ord('A') - 1, -1):\n            upper = chr(c)\n            lower = chr(c + 32)\n            if upper in char_set and lower in char_set:\n                return upper\n        return \"\"" },
{ name: "Java", lang: "java", code: "import java.util.HashSet;\nimport java.util.Set;\n\nclass Solution {\n    public String greatestLetter(String s) {\n        Set&lt;Character&gt; charSet = new HashSet&lt;&gt;();\n        for (char c : s.toCharArray()) {\n            charSet.add(c);\n        }\n        for (char c = 'Z'; c &gt;= 'A'; c--) {\n            char upper = c;\n            char lower = (char)(c + 32);\n            if (charSet.contains(upper) && charSet.contains(lower)) {\n                return String.valueOf(upper);\n            }\n        }\n        return \"\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;unordered_set&gt;\n\nclass Solution {\npublic:\n    string greatestLetter(string s) {\n        unordered_set&lt;char&gt; charSet(s.begin(), s.end());\n        for (char c = 'Z'; c &gt;= 'A'; c--) {\n            char upper = c;\n            char lower = c + 32;\n            if (charSet.count(upper) && charSet.count(lower)) {\n                return string(1, upper);\n            }\n        }\n        return \"\";\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar greatestLetter = function(s) {\n    const charSet = new Set(s);\n    for (let c = 90; c &gt;= 65; c--) {\n        const upper = String.fromCharCode(c);\n        const lower = String.fromCharCode(c + 32);\n        if (charSet.has(upper) && charSet.has(lower)) {\n            return upper;\n        }\n    }\n    return \"\";\n};" },
{ name: "TypeScript", lang: "typescript", code: "function greatestLetter(s: string): string {\n    const charSet = new Set(s);\n    for (let c = 90; c &gt;= 65; c--) {\n        const upper = String.fromCharCode(c);\n        const lower = String.fromCharCode(c + 32);\n        if (charSet.has(upper) && charSet.has(lower)) {\n            return upper;\n        }\n    }\n    return \"\";\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(n) for the hash set
- Notes: More efficient lookups but uses extra space

---
## Boolean Array Approach

**Intuition**
Use two boolean arrays of size 26 to track which uppercase and lowercase letters exist in the string.

**Steps**
- Create two boolean arrays of size 26 for uppercase and lowercase letters
- Iterate through the string and mark the presence of each letter
- Iterate from index 25 down to 0 (representing 'Z' to 'A')
- Check if both uppercase and lowercase versions exist for each letter
- Return the first letter found
- If no letter is found, return an empty string

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def greatestLetter(self, s: str) -&gt; str:\n        upper = [False] * 26\n        lower = [False] * 26\n        \n        for c in s:\n            if c.isupper():\n                upper[ord(c) - ord('A')] = True\n            else:\n                lower[ord(c) - ord('a')] = True\n        \n        for i in range(25, -1, -1):\n            if upper[i] and lower[i]:\n                return chr(ord('A') + i)\n        return \"\"" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String greatestLetter(String s) {\n        boolean[] upper = new boolean[26];\n        boolean[] lower = new boolean[26];\n        \n        for (char c : s.toCharArray()) {\n            if (c &gt;= 'A' && c &lt;= 'Z') {\n                upper[c - 'A'] = true;\n            } else {\n                lower[c - 'a'] = true;\n            }\n        }\n        \n        for (int i = 25; i &gt;= 0; i--) {\n            if (upper[i] && lower[i]) {\n                return String.valueOf((char)('A' + i));\n            }\n        }\n        return \"\";\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string greatestLetter(string s) {\n        bool upper[26] = {false};\n        bool lower[26] = {false};\n        \n        for (char c : s) {\n            if (c &gt;= 'A' && c &lt;= 'Z') {\n                upper[c - 'A'] = true;\n            } else {\n                lower[c - 'a'] = true;\n            }\n        }\n        \n        for (int i = 25; i &gt;= 0; i--) {\n            if (upper[i] && lower[i]) {\n                return string(1, (char)('A' + i));\n            }\n        }\n        return \"\";\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} s\n * @return {string}\n */\nvar greatestLetter = function(s) {\n    const upper = new Array(26).fill(false);\n    const lower = new Array(26).fill(false);\n    \n    for (const c of s) {\n        if (c &gt;= 'A' && c &lt;= 'Z') {\n            upper[c.charCodeAt(0) - 65] = true;\n        } else {\n            lower[c.charCodeAt(0) - 97] = true;\n        }\n    }\n    \n    for (let i = 25; i &gt;= 0; i--) {\n        if (upper[i] && lower[i]) {\n            return String.fromCharCode(65 + i);\n        }\n    }\n    return \"\";\n};" },
{ name: "TypeScript", lang: "typescript", code: "function greatestLetter(s: string): string {\n    const upper: boolean[] = new Array(26).fill(false);\n    const lower: boolean[] = new Array(26).fill(false);\n    \n    for (const c of s) {\n        if (c &gt;= 'A' && c &lt;= 'Z') {\n            upper[c.charCodeAt(0) - 65] = true;\n        } else {\n            lower[c.charCodeAt(0) - 97] = true;\n        }\n    }\n    \n    for (let i = 25; i &gt;= 0; i--) {\n        if (upper[i] && lower[i]) {\n            return String.fromCharCode(65 + i);\n        }\n    }\n    return \"\";\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the string
- Space: O(1) - fixed size arrays of 26
- Notes: Most space-efficient with constant space

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Greatest English Letter in Upper and Lower Case Solutions","datePublished":"2025-12-04","description":"Find the greatest English letter that appears in both uppercase and lowercase in a given string.","keywords":"LeetCode, Greatest English Letter in Upper and Lower Case, Easy, Hash Table, String, Enumeration"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force Enumeration approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(26 * n) = O(n) where n is the length of the string."}},{"@type":"Question","name":"What is the space complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) for storing all characters in the hash set."}},{"@type":"Question","name":"Which approach is the most space-efficient?","acceptedAnswer":{"@type":"Answer","text":"The Boolean Array approach is the most space-efficient with O(1) space complexity since it uses fixed-size arrays of 26 elements."}},{"@type":"Question","name":"What is the time complexity of the Boolean Array approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the string."}}]}).replace(/</g,'\u003c')}
</script>