---
title: "Maximum Difference Between Adjacent Elements in a Circular Array"
summary: "Find the maximum absolute difference between adjacent elements in a circular array."
date: "Feb 05, 2026"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 75.70% | **Paid:** No
> **Topics:** Array

You are given a circular array of integers. A circular array means that the end of the array connects to the beginning of the array. Your task is to find the maximum absolute difference between any two adjacent elements in the array.

Return the maximum absolute difference between adjacent elements.

- Examples
- Constraints
- [Single Pass with Modulo](#single-pass-with-modulo)
- [Two Pass Approach](#two-pass-approach)
- [Using Iterator Helpers](#using-iterator-helpers)

## Examples

### Example 1

**Input:**
```text
nums = [1,2,4]
```

**Output:**
```text
3
```

**Explanation:**
Because nums is circular, nums[0] and nums[2] are adjacent. They have the maximum absolute difference of |4 - 1| = 3.

### Example 2

**Input:**
```text
nums = [-5,-10,-5]
```

**Output:**
```text
5
```

**Explanation:**
The adjacent elements nums[0] and nums[1] have the maximum absolute difference of |-5 - (-10)| = 5.

## Constraints

```text
2 <= nums.length <= 10⁵
-10⁹ <= nums[i] <= 10⁹
```

---
## Single Pass with Modulo

**Intuition**
Iterate through the array once, using modulo operation to handle the circular nature by wrapping around to the first element when reaching the end.

**Steps**
- Initialize maxDiff to 0
- Iterate through each index from 0 to n-1
- Calculate absolute difference between current element and next element (using modulo for circular access)
- Update maxDiff if current difference is larger
- Return maxDiff

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def maxDifference(self, nums: List[int]) -> int:
        n = len(nums)
        if n &lt; 2:
            return 0
        max_diff = 0
        for i in range(n):
            diff = abs(nums[i] - nums[(i + 1) % n])
            max_diff = max(max_diff, diff)
        return max_diff
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int maxDifference(int[] nums) {
        int n = nums.length;
        if (n &lt; 2) return 0;
        int maxDiff = 0;
        for (int i = 0; i &lt; n; i++) {
            int diff = Math.abs(nums[i] - nums[(i + 1) % n]);
            maxDiff = Math.max(maxDiff, diff);
        }
        return maxDiff;
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <cmath>
#include <algorithm>

using namespace std;

class Solution {
public:
    int maxDifference(vector<int>& nums) {
        int n = nums.size();
        if (n &lt; 2) return 0;
        int maxDiff = 0;
        for (int i = 0; i &lt; n; i++) {
            int diff = abs(nums[i] - nums[(i + 1) % n]);
            maxDiff = max(maxDiff, diff);
        }
        return maxDiff;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    maxDifference(nums) {
        const n = nums.length;
        if (n &lt; 2) return 0;
        let maxDiff = 0;
        for (let i = 0; i &lt; n; i++) {
            const diff = Math.abs(nums[i] - nums[(i + 1) % n]);
            maxDiff = Math.max(maxDiff, diff);
        }
        return maxDiff;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    maxDifference(nums: number[]): number {
        const n = nums.length;
        if (n &lt; 2) return 0;
        let maxDiff = 0;
        for (let i = 0; i &lt; n; i++) {
            const diff = Math.abs(nums[i] - nums[(i + 1) % n]);
            maxDiff = Math.max(maxDiff, diff);
        }
        return maxDiff;
    }
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Single pass with constant extra space, optimal solution

---
## Two Pass Approach

**Intuition**
Separate the circular pair handling from the linear pairs by doing two passes - one for consecutive elements and one for the wrap-around pair.

**Steps**
- Initialize maxDiff to 0
- First pass: iterate through indices 0 to n-2, comparing each element with its next neighbor
- Second pass: handle the circular pair by comparing last element with first element
- Return maxDiff

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def maxDifference(self, nums: List[int]) -> int:
        n = len(nums)
        if n &lt; 2:
            return 0
        max_diff = 0
        # First pass: linear adjacent pairs
        for i in range(n - 1):
            diff = abs(nums[i] - nums[i + 1])
            max_diff = max(max_diff, diff)
        # Second pass: circular pair (last and first)
        diff = abs(nums[n - 1] - nums[0])
        max_diff = max(max_diff, diff)
        return max_diff
` },
{ name: "Java", lang: "java", code: `class Solution {
    public int maxDifference(int[] nums) {
        int n = nums.length;
        if (n &lt; 2) return 0;
        int maxDiff = 0;
        // First pass: linear adjacent pairs
        for (int i = 0; i &lt; n - 1; i++) {
            int diff = Math.abs(nums[i] - nums[i + 1]);
            maxDiff = Math.max(maxDiff, diff);
        }
        // Second pass: circular pair (last and first)
        int diff = Math.abs(nums[n - 1] - nums[0]);
        maxDiff = Math.max(maxDiff, diff);
        return maxDiff;
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <cmath>
#include <algorithm>

using namespace std;

class Solution {
public:
    int maxDifference(vector<int>& nums) {
        int n = nums.size();
        if (n &lt; 2) return 0;
        int maxDiff = 0;
        // First pass: linear adjacent pairs
        for (int i = 0; i &lt; n - 1; i++) {
            int diff = abs(nums[i] - nums[i + 1]);
            maxDiff = max(maxDiff, diff);
        }
        // Second pass: circular pair (last and first)
        int diff = abs(nums[n - 1] - nums[0]);
        maxDiff = max(maxDiff, diff);
        return maxDiff;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    maxDifference(nums) {
        const n = nums.length;
        if (n &lt; 2) return 0;
        let maxDiff = 0;
        // First pass: linear adjacent pairs
        for (let i = 0; i &lt; n - 1; i++) {
            const diff = Math.abs(nums[i] - nums[i + 1]);
            maxDiff = Math.max(maxDiff, diff);
        }
        // Second pass: circular pair (last and first)
        const diff = Math.abs(nums[n - 1] - nums[0]);
        maxDiff = Math.max(maxDiff, diff);
        return maxDiff;
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    maxDifference(nums: number[]): number {
        const n = nums.length;
        if (n &lt; 2) return 0;
        let maxDiff = 0;
        // First pass: linear adjacent pairs
        for (let i = 0; i &lt; n - 1; i++) {
            const diff = Math.abs(nums[i] - nums[i + 1]);
            maxDiff = Math.max(maxDiff, diff);
        }
        // Second pass: circular pair (last and first)
        const diff = Math.abs(nums[n - 1] - nums[0]);
        maxDiff = Math.max(maxDiff, diff);
        return maxDiff;
    }
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Same complexity as single pass, but avoids modulo operation

---
## Using Iterator Helpers

**Intuition**
Leverage language-specific iterator helpers and functional programming constructs to compute differences in a more declarative style.

**Steps**
- Create a rotated version of the array (first element moved to end)
- Zip the original array with the rotated array
- Map each pair to its absolute difference
- Find the maximum of all differences

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `from typing import List

class Solution:
    def maxDifference(self, nums: List[int]) -> int:
        n = len(nums)
        if n &lt; 2:
            return 0
        # Create rotated array and compute differences
        rotated = nums[1:] + [nums[0]]
        return max(abs(a - b) for a, b in zip(nums, rotated))
` },
{ name: "Java", lang: "java", code: `import java.util.stream.IntStream;

class Solution {
    public int maxDifference(int[] nums) {
        int n = nums.length;
        if (n &lt; 2) return 0;
        return IntStream.range(0, n)
            .map(i -> Math.abs(nums[i] - nums[(i + 1) % n]))
            .max()
            .orElse(0);
    }
}
` },
{ name: "C++", lang: "cpp", code: `#include <vector>
#include <cmath>
#include <algorithm>
#include <numeric>

using namespace std;

class Solution {
public:
    int maxDifference(vector<int>& nums) {
        int n = nums.size();
        if (n &lt; 2) return 0;
        int maxDiff = 0;
        for (int i = 0; i &lt; n; i++) {
            maxDiff = max(maxDiff, abs(nums[i] - nums[(i + 1) % n]));
        }
        return maxDiff;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `class Solution {
    maxDifference(nums) {
        const n = nums.length;
        if (n &lt; 2) return 0;
        // Create rotated array
        const rotated = [...nums.slice(1), nums[0]];
        // Compute max difference using reduce
        return nums.reduce((max, curr, i) =&gt; {
            const diff = Math.abs(curr - rotated[i]);
            return Math.max(max, diff);
        }, 0);
    }
}
` },
{ name: "TypeScript", lang: "typescript", code: `class Solution {
    maxDifference(nums: number[]): number {
        const n = nums.length;
        if (n &lt; 2) return 0;
        // Create rotated array
        const rotated = [...nums.slice(1), nums[0]];
        // Compute max difference using reduce
        return nums.reduce((max: number, curr: number, i: number) =&gt; {
            const diff = Math.abs(curr - rotated[i]);
            return Math.max(max, diff);
        }, 0);
    }
}
` },]} />

**Complexity**
- Time: O(n)
- Space: O(n) for rotated array in Python/JS/TS, O(1) for Java/C++
- Notes: More readable but may use extra space depending on implementation

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Maximum Difference Between Adjacent Elements in a Circular Array Solutions","datePublished":"2026-02-05","description":"Find the maximum absolute difference between adjacent elements in a circular array.","keywords":"LeetCode, Maximum Difference Between Adjacent Elements in a Circular Array, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Single Pass with Modulo approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) where n is the length of the array, as we iterate through each element exactly once."}},{"@type":"Question","name":"What is the space complexity of the optimal solution?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a constant amount of extra space for variables."}},{"@type":"Question","name":"How do we handle the circular nature of the array?","acceptedAnswer":{"@type":"Answer","text":"We use the modulo operator (i + 1) % n to wrap around to the first element when we reach the end of the array."}},{"@type":"Question","name":"What edge cases should we consider?","acceptedAnswer":{"@type":"Answer","text":"We should handle arrays with length less than 2 (return 0), arrays with all identical elements (return 0), and arrays with only 2 elements."}}]}).replace(/</g,'\u003c')}
</script>