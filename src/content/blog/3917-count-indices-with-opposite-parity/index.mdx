---
title: "Count Indices With Opposite Parity"
summary: "Count the number of indices where the index and the value at that index have opposite parity."
date: "May 21, 2025"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 81.50% | **Paid:** No
> **Topics:** N/A

You are given a 0-indexed integer array `nums`. An index `i` is called good if `nums[i]` and `i` have opposite parity. Return the number of good indices.

- Examples
- Constraints
- [Approach 1: Linear Iteration](#approach-1-linear-iteration)
- [Approach 2: Functional Programming](#approach-2-functional-programming)

## Examples

### Example 1

**Input:**
```text
nums = [1,2,3,4]
```

**Output:**
```text
[2,1,1,0]
```

**Explanation:**
nums[0] = 1, which is odd. Thus, the indices j = 1 and j = 3 satisfy the conditions, so the score of index 0 is 2.

nums[1] = 2, which is even. Thus, the index j = 2 satisfies the conditions, so the score of index 1 is 1.

nums[2] = 3, which is odd. Thus, the index j = 3 satisfies the conditions, so the score of index 2 is 1.

nums[3] = 4, which is even. Thus, no index satisfies the conditions, so the score of index 3 is 0.

Thus, the answer = [2, 1, 1, 0].

### Example 2

**Input:**
```text
nums = [1]
```

**Output:**
```text
[0]
```

**Explanation:**
There is only one element in nums. Thus, the score of index 0 is 0.

## Constraints

```text
1 <= nums.length <= 100
1 <= nums[i] <= 100
```

---
## Approach 1: Linear Iteration

**Intuition**
We iterate through the array once. For each index, we check if the parity of the index (even/odd) is different from the parity of the value at that index.

**Steps**
- Initialize a counter to 0.
- Loop through the array using the index `i`.
- Check if `(i % 2)` is not equal to `(nums[i] % 2)`.
- If they are different, increment the counter.
- Return the counter.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countOppositeParity(self, nums: list[int]) -&gt; int:\n        count = 0\n        for i, val in enumerate(nums):\n            if (i % 2) != (val % 2):\n                count += 1\n        return count" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int countOppositeParity(int[] nums) {\n        int count = 0;\n        for (int i = 0; i &lt; nums.length; i++) {\n            if ((i % 2) != (nums[i] % 2)) {\n                count++;\n            }\n        }\n        return count;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int countOppositeParity(vector&lt;int&gt;& nums) {\n        int count = 0;\n        for (int i = 0; i &lt; nums.size(); i++) {\n            if ((i % 2) != (nums[i] % 2)) {\n                count++;\n            }\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var countOppositeParity = function(nums) {\n    let count = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if ((i % 2) !== (nums[i] % 2)) {\n            count++;\n        }\n    }\n    return count;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countOppositeParity(nums: number[]): number {\n    let count = 0;\n    for (let i = 0; i &lt; nums.length; i++) {\n        if ((i % 2) !== (nums[i] % 2)) {\n            count++;\n        }\n    }\n    return count;\n}" },]} />

**Complexity**
- Time: O(n), where n is the length of the array.
- Space: O(1), as we only use a single variable for counting.
- Notes: This is the most efficient approach with minimal overhead.

---
## Approach 2: Functional Programming

**Intuition**
We can use built-in functional methods like `filter`, `reduce`, or list comprehensions to condense the logic into a single expression, improving readability for those familiar with functional styles.

**Steps**
- Use a method that iterates over the array with access to the index (e.g., `reduce` in JavaScript, list comprehension in Python).
- Apply the parity check `(i % 2) != (val % 2)`.
- Sum the number of times the condition is true.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def countOppositeParity(self, nums: list[int]) -&gt; int:\n        return sum(1 for i, val in enumerate(nums) if (i % 2) != (val % 2))" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\n\nclass Solution {\n    public int countOppositeParity(int[] nums) {\n        return (int) IntStream.range(0, nums.length)\n                .filter(i -&gt; (i % 2) != (nums[i] % 2))\n                .count();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n#include &lt;vector&gt;\n\nclass Solution {\npublic:\n    int countOppositeParity(vector&lt;int&gt;& nums) {\n        int count = 0;\n        int idx = 0;\n        for (int val : nums) {\n            if ((idx % 2) != (val % 2)) {\n                count++;\n            }\n            idx++;\n        }\n        return count;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var countOppositeParity = function(nums) {\n    return nums.reduce((acc, val, i) =&gt; {\n        return acc + ((i % 2) !== (val % 2) ? 1 : 0);\n    }, 0);\n};" },
{ name: "TypeScript", lang: "typescript", code: "function countOppositeParity(nums: number[]): number {\n    return nums.reduce((acc, val, i) =&gt; {\n        return acc + ((i % 2) !== (val % 2) ? 1 : 0);\n    }, 0);\n}" },]} />

**Complexity**
- Time: O(n), as we must visit every element once.
- Space: O(1) auxiliary space, though some languages may create intermediate objects (like streams) depending on implementation.
- Notes: More concise but may have slight constant-time overhead compared to a raw loop.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Count Indices With Opposite Parity Solutions","datePublished":"2025-05-21","description":"Count the number of indices where the index and the value at that index have opposite parity.","keywords":"LeetCode, Count Indices With Opposite Parity, Easy, N/A"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"O(n), where n is the length of the array."}},{"@type":"Question","name":"What is the space complexity of the Linear Iteration approach?","acceptedAnswer":{"@type":"Answer","text":"O(1), as we only use a counter variable."}},{"@type":"Question","name":"What is the time complexity of the Functional approach?","acceptedAnswer":{"@type":"Answer","text":"O(n), as we iterate through the array once."}},{"@type":"Question","name":"What is the space complexity of the Functional approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) or O(n) depending on the language implementation (e.g., streams), but generally O(1) auxiliary space."}}]}).replace(/</g,'\u003c')}
</script>