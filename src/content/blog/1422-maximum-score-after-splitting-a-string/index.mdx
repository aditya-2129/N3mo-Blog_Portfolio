---
title: "Maximum Score After Splitting a String"
summary: "Given a binary string, split it into left and right substrings to maximize the score of zeros in left plus ones in right."
date: "Aug 06, 2024"
tags:
  - String
  - Prefix Sum
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 65.10% | **Paid:** No
> **Topics:** String, Prefix Sum

Given a string s of zeros and ones, return the maximum score after splitting the string into two non-empty substrings (i.e. left substring and right substring).

The score after splitting a string is the number of zeros in the left substring plus the number of ones in the right substring.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Prefix Sum](#prefix-sum)
- [One Pass Optimization](#one-pass-optimization)

## Examples

Example 1:
```text
Input: s = "011101"
Output: 5
Explanation: 
All possible splits:
"0" | "11101": 1 + 4 = 5
"01" | "1101": 1 + 3 = 4
"011" | "101": 1 + 2 = 3
"0111" | "01": 1 + 1 = 2
"01110" | "1": 1 + 1 = 2
```

Example 2:
```text
Input: s = "00111"
Output: 5
Explanation: 
"0" | "0111": 1 + 3 = 4
"00" | "111": 2 + 3 = 5
"001" | "11": 2 + 2 = 4
"0011" | "1": 2 + 1 = 3
```

Example 3:
```text
Input: s = "1111"
Output: 3
```

## Constraints

```text
2 <= s.length <= 500
s[i] is either '0' or '1'.
```

---
## Brute Force

**Intuition**
Iterate through every possible split point. For each split, count the zeros in the left part and the ones in the right part to calculate the score.

**Steps**
- Iterate through the string from index 1 to length - 1.
- For each index, split the string into left and right substrings.
- Count zeros in the left substring.
- Count ones in the right substring.
- Update the maximum score found so far.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maxScore(self, s: str) -&gt; int:\n        n = len(s)\n        max_score = 0\n        for i in range(1, n):\n            left = s[:i]\n            right = s[i:]\n            score = left.count('0') + right.count('1')\n            max_score = max(max_score, score)\n        return max_score" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxScore(String s) {\n        int n = s.length();\n        int maxScore = 0;\n        for (int i = 1; i &lt; n; i++) {\n            String left = s.substring(0, i);\n            String right = s.substring(i);\n            int zeros = 0;\n            int ones = 0;\n            for (char c : left.toCharArray()) if (c == '0') zeros++;\n            for (char c : right.toCharArray()) if (c == '1') ones++;\n            maxScore = Math.max(maxScore, zeros + ones);\n        }\n        return maxScore;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int maxScore(string s) {\n        int n = s.length();\n        int maxScore = 0;\n        for (int i = 1; i &lt; n; ++i) {\n            string left = s.substr(0, i);\n            string right = s.substr(i);\n            int zeros = count(left.begin(), left.end(), '0');\n            int ones = count(right.begin(), right.end(), '1');\n            maxScore = max(maxScore, zeros + ones);\n        }\n        return maxScore;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var maxScore = function(s) {\n    let n = s.length;\n    let maxScore = 0;\n    for (let i = 1; i &lt; n; i++) {\n        let left = s.substring(0, i);\n        let right = s.substring(i);\n        let zeros = 0, ones = 0;\n        for (let c of left) if (c === '0') zeros++;\n        for (let c of right) if (c === '1') ones++;\n        maxScore = Math.max(maxScore, zeros + ones);\n    }\n    return maxScore;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function maxScore(s: string): number {\n    let n = s.length;\n    let maxScore = 0;\n    for (let i = 1; i &lt; n; i++) {\n        let left = s.substring(0, i);\n        let right = s.substring(i);\n        let zeros = 0, ones = 0;\n        for (let c of left) if (c === '0') zeros++;\n        for (let c of right) if (c === '1') ones++;\n        maxScore = Math.max(maxScore, zeros + ones);\n    }\n    return maxScore;\n}" },]} />

**Complexity**
- Time: O(n²)
- Space: O(1)
- Notes: Simple to implement but inefficient for large strings due to repeated counting.

---
## Prefix Sum

**Intuition**
Precompute the number of zeros up to each index (prefix sum) and the number of ones from each index to the end (suffix sum). This allows O(1) score calculation for any split.

**Steps**
- Create an array `prefixZeros` where `prefixZeros[i]` stores the count of zeros in `s[0...i]`.
- Create an array `suffixOnes` where `suffixOnes[i]` stores the count of ones in `s[i...n-1]`.
- Iterate through all possible split points. For a split at index `i`, the score is `prefixZeros[i-1] + suffixOnes[i]`.
- Track the maximum score.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maxScore(self, s: str) -&gt; int:\n        n = len(s)\n        prefix_zeros = [0] * n\n        suffix_ones = [0] * n\n        \n        count = 0\n        for i in range(n):\n            if s[i] == '0':\n                count += 1\n            prefix_zeros[i] = count\n            \n        count = 0\n        for i in range(n - 1, -1, -1):\n            if s[i] == '1':\n                count += 1\n            suffix_ones[i] = count\n            \n        max_score = 0\n        for i in range(1, n):\n            score = prefix_zeros[i-1] + suffix_ones[i]\n            max_score = max(max_score, score)\n            \n        return max_score" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxScore(String s) {\n        int n = s.length();\n        int[] prefixZeros = new int[n];\n        int[] suffixOnes = new int[n];\n        \n        int count = 0;\n        for (int i = 0; i &lt; n; i++) {\n            if (s.charAt(i) == '0') count++;\n            prefixZeros[i] = count;\n        }\n        \n        count = 0;\n        for (int i = n - 1; i &gt;= 0; i--) {\n            if (s.charAt(i) == '1') count++;\n            suffixOnes[i] = count;\n        }\n        \n        int maxScore = 0;\n        for (int i = 1; i &lt; n; i++) {\n            int score = prefixZeros[i - 1] + suffixOnes[i];\n            maxScore = Math.max(maxScore, score);\n        }\n        return maxScore;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int maxScore(string s) {\n        int n = s.length();\n        vector&lt;int&gt; prefixZeros(n, 0);\n        vector&lt;int&gt; suffixOnes(n, 0);\n        \n        int count = 0;\n        for (int i = 0; i &lt; n; ++i) {\n            if (s[i] == '0') count++;\n            prefixZeros[i] = count;\n        }\n        \n        count = 0;\n        for (int i = n - 1; i &gt;= 0; --i) {\n            if (s[i] == '1') count++;\n            suffixOnes[i] = count;\n        }\n        \n        int maxScore = 0;\n        for (int i = 1; i &lt; n; ++i) {\n            int score = prefixZeros[i - 1] + suffixOnes[i];\n            maxScore = max(maxScore, score);\n        }\n        return maxScore;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var maxScore = function(s) {\n    let n = s.length;\n    let prefixZeros = new Array(n).fill(0);\n    let suffixOnes = new Array(n).fill(0);\n    \n    let count = 0;\n    for (let i = 0; i &lt; n; i++) {\n        if (s[i] === '0') count++;\n        prefixZeros[i] = count;\n    }\n    \n    count = 0;\n    for (let i = n - 1; i &gt;= 0; i--) {\n        if (s[i] === '1') count++;\n        suffixOnes[i] = count;\n    }\n    \n    let maxScore = 0;\n    for (let i = 1; i &lt; n; i++) {\n        let score = prefixZeros[i - 1] + suffixOnes[i];\n        maxScore = Math.max(maxScore, score);\n    }\n    return maxScore;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function maxScore(s: string): number {\n    let n = s.length;\n    let prefixZeros = new Array(n).fill(0);\n    let suffixOnes = new Array(n).fill(0);\n    \n    let count = 0;\n    for (let i = 0; i &lt; n; i++) {\n        if (s[i] === '0') count++;\n        prefixZeros[i] = count;\n    }\n    \n    count = 0;\n    for (let i = n - 1; i &gt;= 0; i--) {\n        if (s[i] === '1') count++;\n        suffixOnes[i] = count;\n    }\n    \n    let maxScore = 0;\n    for (let i = 1; i &lt; n; i++) {\n        let score = prefixZeros[i - 1] + suffixOnes[i];\n        maxScore = Math.max(maxScore, score);\n    }\n    return maxScore;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(n)
- Notes: Faster than brute force but uses extra space for the prefix and suffix arrays.

---
## One Pass Optimization

**Intuition**
We can calculate the score in a single pass. First, count the total number of ones in the string. Then, iterate through the string. Maintain a count of zeros seen so far. As we move the split point, if we see a '0', it adds to the left score. If we see a '1', it means that '1' is moving from the right side to the left side, so we decrement the total ones count (which represents the right side score).

**Steps**
- Count the total number of '1's in the string.
- Initialize `zeros` to 0 and `maxScore` to 0.
- Iterate through the string from index 0 to `n - 2` (to ensure the right substring is non-empty).
- If the current character is '0', increment `zeros`.
- If the current character is '1', decrement `totalOnes`.
- Calculate the current score as `zeros + totalOnes` and update `maxScore`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def maxScore(self, s: str) -&gt; int:\n        total_ones = s.count('1')\n        zeros = 0\n        max_score = 0\n        \n        for i in range(len(s) - 1):\n            if s[i] == '0':\n                zeros += 1\n            else:\n                total_ones -= 1\n            \n            max_score = max(max_score, zeros + total_ones)\n            \n        return max_score" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int maxScore(String s) {\n        int totalOnes = 0;\n        for (char c : s.toCharArray()) {\n            if (c == '1') totalOnes++;\n        }\n        \n        int zeros = 0;\n        int maxScore = 0;\n        \n        for (int i = 0; i &lt; s.length() - 1; i++) {\n            if (s.charAt(i) == '0') {\n                zeros++;\n            } else {\n                totalOnes--;\n            }\n            maxScore = Math.max(maxScore, zeros + totalOnes);\n        }\n        return maxScore;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int maxScore(string s) {\n        int totalOnes = count(s.begin(), s.end(), '1');\n        int zeros = 0;\n        int maxScore = 0;\n        \n        for (int i = 0; i &lt; s.length() - 1; ++i) {\n            if (s[i] == '0') {\n                zeros++;\n            } else {\n                totalOnes--;\n            }\n            maxScore = max(maxScore, zeros + totalOnes);\n        }\n        return maxScore;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var maxScore = function(s) {\n    let totalOnes = 0;\n    for (let c of s) {\n        if (c === '1') totalOnes++;\n    }\n    \n    let zeros = 0;\n    let maxScore = 0;\n    \n    for (let i = 0; i &lt; s.length - 1; i++) {\n        if (s[i] === '0') {\n            zeros++;\n        } else {\n            totalOnes--;\n        }\n        maxScore = Math.max(maxScore, zeros + totalOnes);\n    }\n    return maxScore;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function maxScore(s: string): number {\n    let totalOnes = 0;\n    for (let c of s) {\n        if (c === '1') totalOnes++;\n    }\n    \n    let zeros = 0;\n    let maxScore = 0;\n    \n    for (let i = 0; i &lt; s.length - 1; i++) {\n        if (s[i] === '0') {\n            zeros++;\n        } else {\n            totalOnes--;\n        }\n        maxScore = Math.max(maxScore, zeros + totalOnes);\n    }\n    return maxScore;\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: Optimal solution with linear time complexity and constant space usage.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Maximum Score After Splitting a String Solutions","datePublished":"2024-08-06","description":"Given a binary string, split it into left and right substrings to maximize the score of zeros in left plus ones in right.","keywords":"LeetCode, Maximum Score After Splitting a String, Easy, String, Prefix Sum"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n²) because for each of the n-1 split points, we traverse the string to count zeros and ones."}},{"@type":"Question","name":"What is the time complexity of the Prefix Sum approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) as we precompute the prefix and suffix arrays in two passes and then calculate the score in a third pass."}},{"@type":"Question","name":"What is the time complexity of the One Pass approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n) since we traverse the string only once to calculate the total ones and then once more to find the maximum score."}},{"@type":"Question","name":"What is the space complexity of the One Pass approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) as we only use a few integer variables for counting."}}]}).replace(/</g,'\u003c')}
</script>