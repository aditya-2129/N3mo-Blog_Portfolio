---
title: "Minimum Common Value"
summary: "Given two sorted arrays, find the smallest integer present in both."
date: "Apr 22, 2026"
tags:
  - Array
  - Hash Table
  - Two Pointers
  - Binary Search
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 58.00% | **Paid:** No
> **Topics:** Array, Hash Table, Two Pointers, Binary Search

Given two integer arrays nums1 and nums2 sorted in non-decreasing order, return the minimum integer common to both arrays. If there is no common integer amongst nums1 and nums2, return -1. An integer common to both arrays is considered common if it appears at least once in each array.

- Examples
- Constraints
- [Brute Force](#brute-force)
- [Hash Set](#hash-set)
- [Two Pointers](#two-pointers)
- [Binary Search](#binary-search)

## Examples

Example 1:
```text
Input: nums1 = [1,2,3], nums2 = [2,4]
Output: 2
Explanation: The smallest integer common to both arrays is 2.
```

Example 2:
```text
Input: nums1 = [1,2,3,6], nums2 = [7,1]
Output: 1
Explanation: The smallest integer common to both arrays is 1.
```

## Constraints

```text
1 <= nums1.length, nums2.length <= 10^5
1 <= nums1[i], nums2[i] <= 10^9
Both nums1 and nums2 are sorted in non-decreasing order.
```

---
## Brute Force

**Intuition**
Iterate through every element in the first array and, for each element, check if it exists in the second array. Since the arrays are sorted, we can optimize the inner loop slightly by breaking early if the current element in `nums2` exceeds the target.

**Steps**
- Iterate through each element `x` in `nums1`.
- For each `x`, iterate through `nums2` to find a match.
- If a match is found, return `x` immediately (since `nums1` is sorted, this is the minimum).
- If the loop finishes without finding a match, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def getCommon(self, nums1: list[int], nums2: list[int]) -&gt; int:\n        for x in nums1:\n            for y in nums2:\n                if x == y:\n                    return x\n                if y &gt; x:\n                    break\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int getCommon(int[] nums1, int[] nums2) {\n        for (int x : nums1) {\n            for (int y : nums2) {\n                if (x == y) {\n                    return x;\n                }\n                if (y &gt; x) {\n                    break;\n                }\n            }\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int getCommon(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        for (int x : nums1) {\n            for (int y : nums2) {\n                if (x == y) return x;\n                if (y &gt; x) break;\n            }\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var getCommon = function(nums1, nums2) {\n    for (let x of nums1) {\n        for (let y of nums2) {\n            if (x === y) return x;\n            if (y &gt; x) break;\n        }\n    }\n    return -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function getCommon(nums1: number[], nums2: number[]): number {\n    for (let x of nums1) {\n        for (let y of nums2) {\n            if (x === y) return x;\n            if (y &gt; x) break;\n        }\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(n * m) in the worst case, where n and m are the lengths of the arrays.
- Space: O(1)
- Notes: This approach is inefficient for large inputs but serves as a baseline.

---
## Hash Set

**Intuition**
Store all elements of the first array in a hash set for O(1) lookups. Then iterate through the second array and return the first element found in the set.

**Steps**
- Create a set from `nums1`.
- Iterate through `nums2`.
- If an element from `nums2` exists in the set, return it.
- If the loop ends, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def getCommon(self, nums1: list[int], nums2: list[int]) -&gt; int:\n        s = set(nums1)\n        for x in nums2:\n            if x in s:\n                return x\n        return -1" },
{ name: "Java", lang: "java", code: "import java.util.HashSet;\nimport java.util.Set;\n\nclass Solution {\n    public int getCommon(int[] nums1, int[] nums2) {\n        Set&lt;Integer&gt; set = new HashSet&lt;&gt;();\n        for (int num : nums1) {\n            set.add(num);\n        }\n        for (int num : nums2) {\n            if (set.contains(num)) {\n                return num;\n            }\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;unordered_set&gt;\n\nclass Solution {\npublic:\n    int getCommon(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        unordered_set&lt;int&gt; s(nums1.begin(), nums1.end());\n        for (int x : nums2) {\n            if (s.find(x) != s.end()) return x;\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var getCommon = function(nums1, nums2) {\n    let s = new Set(nums1);\n    for (let x of nums2) {\n        if (s.has(x)) return x;\n    }\n    return -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function getCommon(nums1: number[], nums2: number[]): number {\n    let s = new Set(nums1);\n    for (let x of nums2) {\n        if (s.has(x)) return x;\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(n + m)
- Space: O(n) to store the set.
- Notes: Fast time complexity but trades off memory usage proportional to the size of the first array.

---
## Two Pointers

**Intuition**
Since both arrays are sorted, we can traverse them simultaneously using two pointers. Compare the elements at the current pointers; move the pointer pointing to the smaller value forward to try and find a match.

**Steps**
- Initialize two pointers, `i` and `j`, to 0.
- While both pointers are within their array bounds:
    - If `nums1[i] == nums2[j]`, return the value.
    - If `nums1[i] &lt; nums2[j]`, increment `i`.
    - Otherwise, increment `j`.
- Return -1 if no match is found.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def getCommon(self, nums1: list[int], nums2: list[int]) -&gt; int:\n        i = j = 0\n        n, m = len(nums1), len(nums2)\n        while i &lt; n and j &lt; m:\n            if nums1[i] == nums2[j]:\n                return nums1[i]\n            elif nums1[i] &lt; nums2[j]:\n                i += 1\n            else:\n                j += 1\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int getCommon(int[] nums1, int[] nums2) {\n        int i = 0, j = 0;\n        int n = nums1.length, m = nums2.length;\n        while (i &lt; n && j &lt; m) {\n            if (nums1[i] == nums2[j]) {\n                return nums1[i];\n            } else if (nums1[i] &lt; nums2[j]) {\n                i++;\n            } else {\n                j++;\n            }\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int getCommon(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        int i = 0, j = 0;\n        int n = nums1.size(), m = nums2.size();\n        while (i &lt; n && j &lt; m) {\n            if (nums1[i] == nums2[j]) return nums1[i];\n            else if (nums1[i] &lt; nums2[j]) i++;\n            else j++;\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var getCommon = function(nums1, nums2) {\n    let i = 0, j = 0;\n    const n = nums1.length, m = nums2.length;\n    while (i &lt; n && j &lt; m) {\n        if (nums1[i] === nums2[j]) return nums1[i];\n        else if (nums1[i] &lt; nums2[j]) i++;\n        else j++;\n    }\n    return -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function getCommon(nums1: number[], nums2: number[]): number {\n    let i = 0, j = 0;\n    const n = nums1.length, m = nums2.length;\n    while (i &lt; n && j &lt; m) {\n        if (nums1[i] === nums2[j]) return nums1[i];\n        else if (nums1[i] &lt; nums2[j]) i++;\n        else j++;\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(n + m)
- Space: O(1)
- Notes: This is the optimal approach for sorted arrays, utilizing constant extra space.

---
## Binary Search

**Intuition**
Iterate through the smaller array (or just `nums1`) and perform a binary search for each element in the second array. Since `nums2` is sorted, binary search is efficient.

**Steps**
- Iterate through each element `x` in `nums1`.
- Perform binary search for `x` in `nums2`.
- If found, return `x` (since `nums1` is sorted, the first found is the minimum).
- If the loop finishes, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import bisect\n\nclass Solution:\n    def getCommon(self, nums1: list[int], nums2: list[int]) -&gt; int:\n        for x in nums1:\n            idx = bisect.bisect_left(nums2, x)\n            if idx &lt; len(nums2) and nums2[idx] == x:\n                return x\n        return -1" },
{ name: "Java", lang: "java", code: "import java.util.Arrays;\n\nclass Solution {\n    public int getCommon(int[] nums1, int[] nums2) {\n        for (int x : nums1) {\n            int idx = Arrays.binarySearch(nums2, x);\n            if (idx &gt;= 0) {\n                return x;\n            }\n        }\n        return -1;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;algorithm&gt;\n\nclass Solution {\npublic:\n    int getCommon(vector&lt;int&gt;& nums1, vector&lt;int&gt;& nums2) {\n        for (int x : nums1) {\n            if (std::binary_search(nums2.begin(), nums2.end(), x)) {\n                return x;\n            }\n        }\n        return -1;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "var getCommon = function(nums1, nums2) {\n    const binarySearch = (arr, target) =&gt; {\n        let left = 0, right = arr.length - 1;\n        while (left &lt;= right) {\n            const mid = Math.floor((left + right) / 2);\n            if (arr[mid] === target) return true;\n            if (arr[mid] &lt; target) left = mid + 1;\n            else right = mid - 1;\n        }\n        return false;\n    };\n    \n    for (let x of nums1) {\n        if (binarySearch(nums2, x)) return x;\n    }\n    return -1;\n};" },
{ name: "TypeScript", lang: "typescript", code: "function getCommon(nums1: number[], nums2: number[]): number {\n    const binarySearch = (arr: number[], target: number): boolean =&gt; {\n        let left = 0, right = arr.length - 1;\n        while (left &lt;= right) {\n            const mid = Math.floor((left + right) / 2);\n            if (arr[mid] === target) return true;\n            if (arr[mid] &lt; target) left = mid + 1;\n            else right = mid - 1;\n        }\n        return false;\n    };\n\n    for (let x of nums1) {\n        if (binarySearch(nums2, x)) return x;\n    }\n    return -1;\n}" },]} />

**Complexity**
- Time: O(n log m) or O(min(n, m) log max(n, m))
- Space: O(1)
- Notes: Useful if one array is significantly smaller than the other, though generally slower than the Two Pointers approach for similar sizes.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Minimum Common Value Solutions","datePublished":"2026-04-22","description":"Given two sorted arrays, find the smallest integer present in both.","keywords":"LeetCode, Minimum Common Value, Easy, Array, Hash Table, Two Pointers, Binary Search"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"O(n * m) in the worst case."}},{"@type":"Question","name":"What is the most space-efficient approach?","acceptedAnswer":{"@type":"Answer","text":"The Two Pointers approach uses O(1) space."}},{"@type":"Question","name":"What is the time complexity of the Two Pointers approach?","acceptedAnswer":{"@type":"Answer","text":"O(n + m), where n and m are the lengths of the arrays."}}]}).replace(/</g,'\u003c')}
</script>