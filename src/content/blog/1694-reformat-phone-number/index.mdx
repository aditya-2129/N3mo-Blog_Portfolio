---
title: "Reformat Phone Number"
summary: "Reformat a phone number string by removing non-digits and grouping into blocks of 3, with special handling for the last block."
date: "Oct 05, 2025"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 67.70% | **Paid:** No
> **Topics:** String

You are given a phone number as a string number. number consists of digits, spaces ' ', and/or dashes '-'.

You would like to reformat the phone number in a certain manner. Specifically, the digits should be grouped in blocks of length 3, separated by dashes, except possibly for the last block, which could be of length 2 or 3, but not 1. If the last block has length 4, it should be split into two blocks of length 2.

Return the reformatted phone number.

- Examples
- Constraints
- [String Manipulation](#string-manipulation)
- [Queue-based Approach](#queue-based-approach)
- [Modular Arithmetic](#modular-arithmetic)

## Examples

### Example 1

**Input:**
```text
number = "1-23-45 6"
```

**Output:**
```text
"123-456"
```

**Explanation:**
The digits are "123456".
Step 1: There are more than 4 digits, so group the next 3 digits. The 1st block is "123".
Step 2: There are 3 digits remaining, so put them in a single block of length 3. The 2nd block is "456".
Joining the blocks gives "123-456".

### Example 2

**Input:**
```text
number = "123 4-567"
```

**Output:**
```text
"123-45-67"
```

**Explanation:**
The digits are "1234567".
Step 1: There are more than 4 digits, so group the next 3 digits. The 1st block is "123".
Step 2: There are 4 digits left, so split them into two blocks of length 2. The blocks are "45" and "67".
Joining the blocks gives "123-45-67".

### Example 3

**Input:**
```text
number = "123 4-5678"
```

**Output:**
```text
"123-456-78"
```

**Explanation:**
The digits are "12345678".
Step 1: The 1st block is "123".
Step 2: The 2nd block is "456".
Step 3: There are 2 digits left, so put them in a single block of length 2. The 3rd block is "78".
Joining the blocks gives "123-456-78".

## Constraints

```text
2 <= number.length <= 100
number consists of digits, spaces ' ', and/or dashes '-'.
```

---

## String Manipulation

**Intuition**
Clean the input string by removing non-digit characters, then iterate through the cleaned string building groups of 3 digits, with special handling when 4 digits remain.

**Steps**
- Remove all non-digit characters from the input string
- Iterate through the cleaned string with a pointer
- When 4 digits remain, split into two groups of 2
- When 2 or 3 digits remain, take all remaining digits
- Otherwise, take groups of 3 digits
- Join all groups with dashes

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def reformatNumber(self, number: str) -&gt; str:\n        cleaned = ''.join(c for c in number if c.isdigit())\n        \n        result = []\n        i = 0\n        n = len(cleaned)\n        \n        while i &lt; n:\n            if n - i == 4:\n                result.append(cleaned[i:i+2])\n                result.append(cleaned[i+2:i+4])\n                i += 4\n            elif n - i &lt;= 3:\n                result.append(cleaned[i:])\n                i = n\n            else:\n                result.append(cleaned[i:i+3])\n                i += 3\n        \n        return '-'.join(result)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String reformatNumber(String number) {\n        StringBuilder cleaned = new StringBuilder();\n        for (char c : number.toCharArray()) {\n            if (Character.isDigit(c)) {\n                cleaned.append(c);\n            }\n        }\n        \n        StringBuilder result = new StringBuilder();\n        int n = cleaned.length();\n        int i = 0;\n        \n        while (i &lt; n) {\n            if (n - i == 4) {\n                result.append(cleaned.substring(i, i + 2));\n                result.append(\"-\");\n                result.append(cleaned.substring(i + 2, i + 4));\n                i += 4;\n            } else if (n - i &lt;= 3) {\n                result.append(cleaned.substring(i));\n                i = n;\n            } else {\n                result.append(cleaned.substring(i, i + 3));\n                result.append(\"-\");\n                i += 3;\n            }\n        }\n        \n        return result.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string reformatNumber(string number) {\n        string cleaned;\n        for (char c : number) {\n            if (isdigit(c)) {\n                cleaned += c;\n            }\n        }\n        \n        string result;\n        int n = cleaned.length();\n        int i = 0;\n        \n        while (i &lt; n) {\n            if (n - i == 4) {\n                result += cleaned.substr(i, 2);\n                result += \"-\";\n                result += cleaned.substr(i + 2, 2);\n                i += 4;\n            } else if (n - i &lt;= 3) {\n                result += cleaned.substr(i);\n                i = n;\n            } else {\n                result += cleaned.substr(i, 3);\n                result += \"-\";\n                i += 3;\n            }\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} number\n * @return {string}\n */\nvar reformatNumber = function(number) {\n    const cleaned = number.replace(/\\D/g, '');\n    \n    const result = [];\n    let i = 0;\n    const n = cleaned.length;\n    \n    while (i &lt; n) {\n        if (n - i === 4) {\n            result.push(cleaned.substring(i, i + 2));\n            result.push(cleaned.substring(i + 2, i + 4));\n            i += 4;\n        } else if (n - i &lt;= 3) {\n            result.push(cleaned.substring(i));\n            i = n;\n        } else {\n            result.push(cleaned.substring(i, i + 3));\n            i += 3;\n        }\n    }\n    \n    return result.join('-');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function reformatNumber(number: string): string {\n    const cleaned = number.replace(/\\D/g, '');\n    \n    const result: string[] = [];\n    let i = 0;\n    const n = cleaned.length;\n    \n    while (i &lt; n) {\n        if (n - i === 4) {\n            result.push(cleaned.substring(i, i + 2));\n            result.push(cleaned.substring(i + 2, i + 4));\n            i += 4;\n        } else if (n - i &lt;= 3) {\n            result.push(cleaned.substring(i));\n            i = n;\n        } else {\n            result.push(cleaned.substring(i, i + 3));\n            i += 3;\n        }\n    }\n    \n    return result.join('-');\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the input string
- Space: O(n) for storing the cleaned string and result
- Notes: Simple and intuitive approach with clear logic flow

---

## Queue-based Approach

**Intuition**
Use a queue to store digits and process them in groups, taking 3 at a time until 4 or fewer remain, then handle the special cases.

**Steps**
- Create a queue containing only digit characters
- While queue has more than 4 elements, dequeue 3 and add as a group
- Handle remaining 2, 3, or 4 digits appropriately
- Join all groups with dashes

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from collections import deque\n\nclass Solution:\n    def reformatNumber(self, number: str) -&gt; str:\n        queue = deque(c for c in number if c.isdigit())\n        \n        result = []\n        \n        while len(queue) &gt; 4:\n            result.append(''.join([queue.popleft() for _ in range(3)]))\n        \n        remaining = ''.join(queue)\n        if len(remaining) == 4:\n            result.append(remaining[:2])\n            result.append(remaining[2:])\n        else:\n            result.append(remaining)\n        \n        return '-'.join(result)" },
{ name: "Java", lang: "java", code: "import java.util.LinkedList;\nimport java.util.Queue;\n\nclass Solution {\n    public String reformatNumber(String number) {\n        Queue&lt;Character&gt; queue = new LinkedList&lt;&gt;();\n        for (char c : number.toCharArray()) {\n            if (Character.isDigit(c)) {\n                queue.offer(c);\n            }\n        }\n        \n        StringBuilder result = new StringBuilder();\n        \n        while (queue.size() &gt; 4) {\n            result.append(queue.poll());\n            result.append(queue.poll());\n            result.append(queue.poll());\n            result.append(\"-\");\n        }\n        \n        int remaining = queue.size();\n        if (remaining == 4) {\n            result.append(queue.poll());\n            result.append(queue.poll());\n            result.append(\"-\");\n            result.append(queue.poll());\n            result.append(queue.poll());\n        } else {\n            while (!queue.isEmpty()) {\n                result.append(queue.poll());\n            }\n        }\n        \n        return result.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;queue&gt;\n\nclass Solution {\npublic:\n    string reformatNumber(string number) {\n        queue&lt;char&gt; q;\n        for (char c : number) {\n            if (isdigit(c)) {\n                q.push(c);\n            }\n        }\n        \n        string result;\n        \n        while (q.size() &gt; 4) {\n            for (int i = 0; i &lt; 3; i++) {\n                result += q.front();\n                q.pop();\n            }\n            result += \"-\";\n        }\n        \n        int remaining = q.size();\n        if (remaining == 4) {\n            result += q.front(); q.pop();\n            result += q.front(); q.pop();\n            result += \"-\";\n            result += q.front(); q.pop();\n            result += q.front(); q.pop();\n        } else {\n            while (!q.empty()) {\n                result += q.front();\n                q.pop();\n            }\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} number\n * @return {string}\n */\nvar reformatNumber = function(number) {\n    const digits = number.replace(/\\D/g, '').split('');\n    \n    const result = [];\n    \n    while (digits.length &gt; 4) {\n        result.push(digits.splice(0, 3).join(''));\n    }\n    \n    if (digits.length === 4) {\n        result.push(digits.splice(0, 2).join(''));\n        result.push(digits.splice(0, 2).join(''));\n    } else {\n        result.push(digits.join(''));\n    }\n    \n    return result.join('-');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function reformatNumber(number: string): string {\n    const digits: string[] = number.replace(/\\D/g, '').split('');\n    \n    const result: string[] = [];\n    \n    while (digits.length &gt; 4) {\n        result.push(digits.splice(0, 3).join(''));\n    }\n    \n    if (digits.length === 4) {\n        result.push(digits.splice(0, 2).join(''));\n        result.push(digits.splice(0, 2).join(''));\n    } else {\n        result.push(digits.join(''));\n    }\n    \n    return result.join('-');\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the input string
- Space: O(n) for the queue and result storage
- Notes: Uses queue semantics which naturally model the problem of processing digits sequentially

---

## Modular Arithmetic

**Intuition**
Use the remainder when dividing by 3 to determine the optimal grouping strategy upfront, then build the result accordingly.

**Steps**
- Clean the input string to extract only digits
- Calculate n % 3 to determine the first group size
- If remainder is 0, first group is 3; if 1, first group is 4 (split as 2-2); if 2, first group is 2
- Process the first group with special handling for size 4
- Process remaining digits in groups of 3
- Join all groups with dashes

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def reformatNumber(self, number: str) -&gt; str:\n        cleaned = ''.join(c for c in number if c.isdigit())\n        n = len(cleaned)\n        \n        remainder = n % 3\n        if remainder == 0:\n            first_group = 3\n        elif remainder == 1:\n            first_group = 4\n        else:\n            first_group = 2\n        \n        result = []\n        i = 0\n        \n        if first_group == 4:\n            result.append(cleaned[i:i+2])\n            result.append(cleaned[i+2:i+4])\n            i += 4\n        else:\n            result.append(cleaned[i:i+first_group])\n            i += first_group\n        \n        while i &lt; n:\n            result.append(cleaned[i:i+3])\n            i += 3\n        \n        return '-'.join(result)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String reformatNumber(String number) {\n        StringBuilder cleaned = new StringBuilder();\n        for (char c : number.toCharArray()) {\n            if (Character.isDigit(c)) {\n                cleaned.append(c);\n            }\n        }\n        \n        int n = cleaned.length();\n        \n        int remainder = n % 3;\n        int firstGroup;\n        if (remainder == 0) {\n            firstGroup = 3;\n        } else if (remainder == 1) {\n            firstGroup = 4;\n        } else {\n            firstGroup = 2;\n        }\n        \n        StringBuilder result = new StringBuilder();\n        int i = 0;\n        \n        if (firstGroup == 4) {\n            result.append(cleaned.substring(i, i + 2));\n            result.append(\"-\");\n            result.append(cleaned.substring(i + 2, i + 4));\n            i += 4;\n        } else {\n            result.append(cleaned.substring(i, i + firstGroup));\n            i += firstGroup;\n        }\n        \n        while (i &lt; n) {\n            if (result.length() &gt; 0) {\n                result.append(\"-\");\n            }\n            result.append(cleaned.substring(i, i + 3));\n            i += 3;\n        }\n        \n        return result.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string reformatNumber(string number) {\n        string cleaned;\n        for (char c : number) {\n            if (isdigit(c)) {\n                cleaned += c;\n            }\n        }\n        \n        int n = cleaned.length();\n        \n        int remainder = n % 3;\n        int firstGroup;\n        if (remainder == 0) {\n            firstGroup = 3;\n        } else if (remainder == 1) {\n            firstGroup = 4;\n        } else {\n            firstGroup = 2;\n        }\n        \n        string result;\n        int i = 0;\n        \n        if (firstGroup == 4) {\n            result += cleaned.substr(i, 2);\n            result += \"-\";\n            result += cleaned.substr(i + 2, 2);\n            i += 4;\n        } else {\n            result += cleaned.substr(i, firstGroup);\n            i += firstGroup;\n        }\n        \n        while (i &lt; n) {\n            if (!result.empty()) {\n                result += \"-\";\n            }\n            result += cleaned.substr(i, 3);\n            i += 3;\n        }\n        \n        return result;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "/**\n * @param {string} number\n * @return {string}\n */\nvar reformatNumber = function(number) {\n    const cleaned = number.replace(/\\D/g, '');\n    const n = cleaned.length;\n    \n    const remainder = n % 3;\n    let firstGroup;\n    if (remainder === 0) {\n        firstGroup = 3;\n    } else if (remainder === 1) {\n        firstGroup = 4;\n    } else {\n        firstGroup = 2;\n    }\n    \n    const result = [];\n    let i = 0;\n    \n    if (firstGroup === 4) {\n        result.push(cleaned.substring(i, i + 2));\n        result.push(cleaned.substring(i + 2, i + 4));\n        i += 4;\n    } else {\n        result.push(cleaned.substring(i, i + firstGroup));\n        i += firstGroup;\n    }\n    \n    while (i &lt; n) {\n        result.push(cleaned.substring(i, i + 3));\n        i += 3;\n    }\n    \n    return result.join('-');\n};" },
{ name: "TypeScript", lang: "typescript", code: "function reformatNumber(number: string): string {\n    const cleaned = number.replace(/\\D/g, '');\n    const n = cleaned.length;\n    \n    const remainder = n % 3;\n    let firstGroup: number;\n    if (remainder === 0) {\n        firstGroup = 3;\n    } else if (remainder === 1) {\n        firstGroup = 4;\n    } else {\n        firstGroup = 2;\n    }\n    \n    const result: string[] = [];\n    let i = 0;\n    \n    if (firstGroup === 4) {\n        result.push(cleaned.substring(i, i + 2));\n        result.push(cleaned.substring(i + 2, i + 4));\n        i += 4;\n    } else {\n        result.push(cleaned.substring(i, i + firstGroup));\n        i += firstGroup;\n    }\n    \n    while (i &lt; n) {\n        result.push(cleaned.substring(i, i + 3));\n        i += 3;\n    }\n    \n    return result.join('-');\n}" },]} />

**Complexity**
- Time: O(n) where n is the length of the input string
- Space: O(n) for storing the cleaned string and result
- Notes: Mathematical approach that determines grouping strategy upfront using modular arithmetic

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Reformat Phone Number Solutions","datePublished":"2025-10-05","description":"Reformat a phone number string by removing non-digits and grouping into blocks of 3, with special handling for the last block.","keywords":"LeetCode, Reformat Phone Number, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of String Manipulation approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the length of the input string"}},{"@type":"Question","name":"What is the space complexity of Queue-based approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) for the queue and result storage"}},{"@type":"Question","name":"How does Modular Arithmetic approach determine grouping?","acceptedAnswer":{"@type":"Answer","text":"It uses n % 3 to determine the first group size: 0→3, 1→4 (split as 2-2), 2→2"}}]}).replace(/</g,'\u003c')}
</script>