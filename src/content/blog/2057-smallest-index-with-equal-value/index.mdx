---
title: "Smallest Index With Equal Value"
summary: "Find the smallest index i where i % 10 equals nums[i], or return -1."
date: "Dec 30, 2025"
tags:
  - Array
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 73.20% | **Paid:** No
> **Topics:** Array

Given a 0-indexed integer array nums, return the smallest index i such that i % 10 == nums[i]. If there is no such index, return -1.

- Examples
- Constraints
- [Linear Scan](#linear-scan)
- [Functional / Stream Approach](#functional--stream-approach)

## Examples

```text
Input: nums = [0,2,3,4,0,1,5]
Output: 0
Explanation: 0 % 10 == 0 == nums[0].
```

```text
Input: nums = [4,3,2,1]
Output: 2
Explanation: 2 % 10 == 2 == nums[2].
```

```text
Input: nums = [1,2,3,4,5,6,7,8,9,0]
Output: -1
Explanation: There is no index i that satisfies i % 10 == nums[i].
```

## Constraints

```text
1 <= nums.length <= 100
0 <= nums[i] <= 1000
```

---
## Linear Scan

**Intuition**
Since we are looking for the smallest index, we can iterate through the array from left to right. The first index that satisfies the condition is the answer.

**Steps**
- Iterate through the array using a loop from index 0 to n-1.
- For each index i, check if i % 10 equals nums[i].
- If the condition is met, return i immediately.
- If the loop finishes without finding a match, return -1.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def smallestEqual(self, nums: List[int]) -&gt; int:\r\n        for i in range(len(nums)):\r\n            if i % 10 == nums[i]:\r\n                return i\r\n        return -1" },
{ name: "Java", lang: "java", code: "class Solution {\r\n    public int smallestEqual(int[] nums) {\r\n        for (int i = 0; i &lt; nums.length; i++) {\r\n            if (i % 10 == nums[i]) {\r\n                return i;\r\n            }\r\n        }\r\n        return -1;\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n\r\nclass Solution {\r\npublic:\r\n    int smallestEqual(std::vector&lt;int&gt;& nums) {\r\n        for (int i = 0; i &lt; nums.size(); i++) {\r\n            if (i % 10 == nums[i]) {\r\n                return i;\r\n            }\r\n        }\r\n        return -1;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\r\n    smallestEqual(nums) {\r\n        for (let i = 0; i &lt; nums.length; i++) {\r\n            if (i % 10 === nums[i]) {\r\n                return i;\r\n            }\r\n        }\r\n        return -1;\r\n    }\r\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\r\n    smallestEqual(nums: number[]): number {\r\n        for (let i = 0; i &lt; nums.length; i++) {\r\n            if (i % 10 === nums[i]) {\r\n                return i;\r\n            }\r\n        }\r\n        return -1;\r\n    }\r\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: This is the most optimal approach as we must check every element in the worst case.

---
## Functional / Stream Approach

**Intuition**
Utilize built-in functional programming features like streams, generators, or higher-order functions to find the index declaratively.

**Steps**
- Use the language's specific functional utility to iterate and filter.
- Python: Use a generator expression with enumerate and next().
- Java: Use IntStream to generate indices and filter.
- JavaScript/TypeScript: use findIndex which accepts a predicate.
- C++: Use std::find_if with a lambda.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "from typing import List\r\n\r\nclass Solution:\r\n    def smallestEqual(self, nums: List[int]) -&gt; int:\r\n        return next((i for i, n in enumerate(nums) if i % 10 == n), -1)" },
{ name: "Java", lang: "java", code: "import java.util.stream.IntStream;\r\n\r\nclass Solution {\r\n    public int smallestEqual(int[] nums) {\r\n        return IntStream.range(0, nums.length)\r\n                        .filter(i -&gt; i % 10 == nums[i])\r\n                        .findFirst()\r\n                        .orElse(-1);\r\n    }\r\n}" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\r\n#include &lt;algorithm&gt;\r\n#include &lt;iterator&gt;\r\n\r\nclass Solution {\r\npublic:\r\n    int smallestEqual(std::vector&lt;int&gt;& nums) {\r\n        int idx = 0;\r\n        auto it = std::find_if(nums.begin(), nums.end(), [&idx](int val) {\r\n            bool match = (idx % 10 == val);\r\n            idx++;\r\n            return match;\r\n        });\r\n        return it != nums.end() ? std::distance(nums.begin(), it) : -1;\r\n    }\r\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\r\n    smallestEqual(nums) {\r\n        return nums.findIndex((n, i) =&gt; i % 10 === n);\r\n    }\r\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\r\n    smallestEqual(nums: number[]): number {\r\n        return nums.findIndex((n, i) =&gt; i % 10 === n);\r\n    }\r\n}" },]} />

**Complexity**
- Time: O(n)
- Space: O(1)
- Notes: While syntactically different, the underlying complexity remains linear. Some functional implementations may have slight constant-time overhead compared to a raw loop.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Smallest Index With Equal Value Solutions","datePublished":"2025-12-30","description":"Find the smallest index i where i % 10 equals nums[i], or return -1.","keywords":"LeetCode, Smallest Index With Equal Value, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the array, as we may need to check every element."}},{"@type":"Question","name":"What is the space complexity of the Linear Scan approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(1) because we only use a constant amount of extra space for variables."}},{"@type":"Question","name":"Why do we use modulo 10 in the condition?","acceptedAnswer":{"@type":"Answer","text":"The problem specifically asks to find the index i such that the last digit of i (i % 10) is equal to the value at that index."}}]}).replace(/</g,'\u003c')}
</script>