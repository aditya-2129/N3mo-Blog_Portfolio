---
title: "Maximum Area of Longest Diagonal Rectangle"
summary: "Find the area of the rectangle with the longest diagonal length. If multiple rectangles have the same longest diagonal, return the maximum area among them."
date: "Oct 16, 2025"
tags:
  - Array
  - Math
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 45.90% | **Paid:** No
> **Topics:** Array

You are given a 2D array dimensions where dimensions[i] = [length_i, width_i] represents the length and width of the ith rectangle.

The length of the diagonal of the ith rectangle is `sqrt(length_i^2 + width_i^2)`.

Return the area of the rectangle having the longest diagonal. If there are multiple rectangles with the same longest diagonal, return the area of the rectangle having the maximum area.

- Examples
- Constraints
- [Approach 1: Iterative Comparison](#approach-1-iterative-comparison)
- [Approach 2: Sorting](#approach-2-sorting)

## Examples

**Example 1:**

```text
Input: dimensions = [[9,3],[8,6]]
Output: 48
Explanation:
For rectangle 1: length = 9, width = 3, diagonal = sqrt(9² + 3²) = sqrt(90), area = 27.
For rectangle 2: length = 8, width = 6, diagonal = sqrt(8² + 6²) = sqrt(100), area = 48.
The longest diagonal is sqrt(100), so we return the area of rectangle 2, which is 48.
```

**Example 2:**

```text
Input: dimensions = [[3,4],[4,3]]
Output: 12
Explanation:
Both rectangles have a diagonal of sqrt(3² + 4²) = 5.
Since both have the same longest diagonal, we return the maximum area, which is 12.
```

## Constraints

```text
- 1 <= dimensions.length <= 100
- dimensions[i].length == 2
- 1 <= dimensions[i][0], dimensions[i][1] <= 100
```

---
## Approach 1: Iterative Comparison

**Intuition**
We can iterate through the list of rectangles once. For each rectangle, we calculate the squared length of the diagonal to avoid floating-point precision issues. We keep track of the maximum diagonal squared found so far and the corresponding area. If we find a rectangle with a larger diagonal, we update our maximums. If we find a rectangle with the same diagonal length but a larger area, we update the area.

**Steps**
- Initialize `max_diag_sq` to -1 and `max_area` to 0.
- Iterate through each rectangle in the `dimensions` array.
- For each rectangle, extract `length` and `width`.
- Calculate `diag_sq` as `length * length + width * width`.
- Calculate `area` as `length * width`.
- If `diag_sq` is greater than `max_diag_sq`, update `max_diag_sq` to `diag_sq` and `max_area` to `area`.
- Else if `diag_sq` is equal to `max_diag_sq` and `area` is greater than `max_area`, update `max_area` to `area`.
- Return `max_area`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def areaOfMaxDiagonal(self, dimensions: list[list[int]]) -> int:\n        max_diag_sq = -1\n        max_area = 0\n        for l, w in dimensions:\n            d = l * l + w * w\n            a = l * w\n            if d > max_diag_sq:\n                max_diag_sq = d\n                max_area = a\n            elif d == max_diag_sq:\n                if a > max_area:\n                    max_area = a\n        return max_area' },
{ name: "Java", lang: "java", code: 'class Solution {\n    public int areaOfMaxDiagonal(int[][] dimensions) {\n        long maxDiagSq = -1;\n        int maxArea = 0;\n        for (int[] dim : dimensions) {\n            int l = dim[0];\n            int w = dim[1];\n            long d = (long) l * l + (long) w * w;\n            int a = l * w;\n            if (d &gt; maxDiagSq) {\n                maxDiagSq = d;\n                maxArea = a;\n            } else if (d == maxDiagSq && a &gt; maxArea) {\n                maxArea = a;\n            }\n        }\n        return maxArea;\n    }\n}' },
{ name: "C++", lang: "cpp", code: 'class Solution {\npublic:\n    int areaOfMaxDiagonal(vector<vector<int>&gt;& dimensions) {\n        long long maxDiagSq = -1;\n        int maxArea = 0;\n        for (auto& dim : dimensions) {\n            int l = dim[0];\n            int w = dim[1];\n            long long d = (long long) l * l + (long long) w * w;\n            int a = l * w;\n            if (d &gt; maxDiagSq) {\n                maxDiagSq = d;\n                maxArea = a;\n            } else if (d == maxDiagSq && a &gt; maxArea) {\n                maxArea = a;\n            }\n        }\n        return maxArea;\n    }\n};' },
{ name: "JavaScript", lang: "javascript", code: 'var areaOfMaxDiagonal = function(dimensions) {\n    let maxDiagSq = -1;\n    let maxArea = 0;\n    for (let i = 0; i &lt; dimensions.length; i++) {\n        let l = dimensions[i][0];\n        let w = dimensions[i][1];\n        let d = l * l + w * w;\n        let a = l * w;\n        if (d &gt; maxDiagSq) {\n            maxDiagSq = d;\n            maxArea = a;\n        } else if (d === maxDiagSq && a &gt; maxArea) {\n            maxArea = a;\n        }\n    }\n    return maxArea;\n};' },
{ name: "TypeScript", lang: "typescript", code: 'function areaOfMaxDiagonal(dimensions: number[][]): number {\n    let maxDiagSq: number = -1;\n    let maxArea: number = 0;\n    for (let i = 0; i &lt; dimensions.length; i++) {\n        let l: number = dimensions[i][0];\n        let w: number = dimensions[i][1];\n        let d: number = l * l + w * w;\n        let a: number = l * w;\n        if (d &gt; maxDiagSq) {\n            maxDiagSq = d;\n            maxArea = a;\n        } else if (d === maxDiagSq && a &gt; maxArea) {\n            maxArea = a;\n        }\n    }\n    return maxArea;\n}' },]} />

**Complexity**
- Time: O(N), where N is the number of rectangles. We iterate through the list once.
- Space: O(1), as we only use a few variables to store the maximums.
- Notes: This is the most optimal approach as it requires a single pass and constant extra space.

---
## Approach 2: Sorting

**Intuition**
We can sort the array of rectangles based on two criteria: primarily by the squared length of the diagonal in descending order, and secondarily by the area in descending order. After sorting, the first element in the array will be the rectangle with the longest diagonal (and the largest area if there are ties). We simply return its area.

**Steps**
- Sort the `dimensions` array using a custom comparator.
- The comparator should first compare the squared diagonal ($l^2 + w^2$). If they are different, sort in descending order.
- If the squared diagonals are the same, compare the area ($l \times w$) and sort in descending order.
- Return the area of the first rectangle in the sorted array.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: 'class Solution:\n    def areaOfMaxDiagonal(self, dimensions: list[list[int]]) -> int:\n        # Sort by diagonal squared (desc), then by area (desc)\n        dimensions.sort(key=lambda x: (x[0]*x[0] + x[1]*x[1], x[0]*x[1]), reverse=True)\n        return dimensions[0][0] * dimensions[0][1]' },
{ name: "Java", lang: "java", code: 'import java.util.Arrays;\n\nclass Solution {\n    public int areaOfMaxDiagonal(int[][] dimensions) {\n        Arrays.sort(dimensions, (a, b) -> {\n            long diagA = (long) a[0] * a[0] + (long) a[1] * a[1];\n            long diagB = (long) b[0] * b[0] + (long) b[1] * b[1];\n            if (diagB != diagA) {\n                return Long.compare(diagB, diagA);\n            }\n            int areaA = a[0] * a[1];\n            int areaB = b[0] * b[1];\n            return Integer.compare(areaB, areaA);\n        });\n        return dimensions[0][0] * dimensions[0][1];\n    }\n}' },
{ name: "C++", lang: "cpp", code: '#include <algorithm>\n#include <vector>\n\nclass Solution {\npublic:\n    int areaOfMaxDiagonal(vector<vector<int>&gt;& dimensions) {\n        sort(dimensions.begin(), dimensions.end(), [](const vector<int>& a, const vector<int>& b) {\n            long long diagA = (long long) a[0] * a[0] + (long long) a[1] * a[1];\n            long long diagB = (long long) b[0] * b[0] + (long long) b[1] * b[1];\n            if (diagA != diagB) {\n                return diagA &gt; diagB;\n            }\n            int areaA = a[0] * a[1];\n            int areaB = b[0] * b[1];\n            return areaA &gt; areaB;\n        });\n        return dimensions[0][0] * dimensions[0][1];\n    }\n};' },
{ name: "JavaScript", lang: "javascript", code: 'var areaOfMaxDiagonal = function(dimensions) {\n    dimensions.sort((a, b) =&gt; {\n        const diagA = a[0] * a[0] + a[1] * a[1];\n        const diagB = b[0] * b[0] + b[1] * b[1];\n        if (diagA !== diagB) {\n            return diagB - diagA;\n        }\n        const areaA = a[0] * a[1];\n        const areaB = b[0] * b[1];\n        return areaB - areaA;\n    });\n    return dimensions[0][0] * dimensions[0][1];\n};' },
{ name: "TypeScript", lang: "typescript", code: 'function areaOfMaxDiagonal(dimensions: number[][]): number {\n    dimensions.sort((a, b) =&gt; {\n        const diagA: number = a[0] * a[0] + a[1] * a[1];\n        const diagB: number = b[0] * b[0] + b[1] * b[1];\n        if (diagA !== diagB) {\n            return diagB - diagA;\n        }\n        const areaA: number = a[0] * a[1];\n        const areaB: number = b[0] * b[1];\n        return areaB - areaA;\n    });\n    return dimensions[0][0] * dimensions[0][1];\n}' },]} />

**Complexity**
- Time: O(N log N), due to the sorting algorithm.
- Space: O(1) or O(N), depending on the sorting algorithm's implementation (e.g., Timsort in Python uses O(N), in-place sort in C++ uses O(log N) stack space).
- Notes: While concise, this approach is less efficient than the iterative comparison for large inputs due to the sorting overhead.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Maximum Area of Longest Diagonal Rectangle Solutions","datePublished":"2025-10-16","description":"Find the area of the rectangle with the longest diagonal length. If multiple rectangles have the same longest diagonal, return the maximum area among them.","keywords":"LeetCode, Maximum Area of Longest Diagonal Rectangle, Easy, Array"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Comparison approach?","acceptedAnswer":{"@type":"Answer","text":"O(N), where N is the number of rectangles."}},{"@type":"Question","name":"What is the space complexity of the Iterative Comparison approach?","acceptedAnswer":{"@type":"Answer","text":"O(1), as we only use a few variables to store the maximums."}},{"@type":"Question","name":"What is the time complexity of the Sorting approach?","acceptedAnswer":{"@type":"Answer","text":"O(N log N), due to the sorting algorithm."}}]}).replace(/</g,'\u003c')}
</script>