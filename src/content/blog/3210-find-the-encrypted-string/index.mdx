---
title: "Find the Encrypted String"
summary: "Encrypt a string by replacing each character with the character k positions ahead, wrapping around if necessary."
date: "Jan 27, 2026"
tags:
  - String
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 68.20% | **Paid:** No
> **Topics:** String

You are given a string `s` and an integer `k`. The encryption rules are as follows:

For each character `s[i]` in the string, replace it with the character that is `k` positions ahead of it in the string.
If moving `k` positions ahead goes past the end of the string, wrap around to the beginning.

Return the resulting encrypted string.

- Examples
- Constraints
- [Iterative Simulation](#iterative-simulation)
- [String Slicing](#string-slicing)

## Examples

### Example 1

**Input:**
```text
s = "dart", k = 3
```

**Output:**
```text
"tdar"
```

**Explanation:**
For i = 0, the 3^rd character after 'd' is 't'.

For i = 1, the 3^rd character after 'a' is 'd'.

For i = 2, the 3^rd character after 'r' is 'a'.

For i = 3, the 3^rd character after 't' is 'r'.

### Example 2

**Input:**
```text
s = "aaa", k = 1
```

**Output:**
```text
"aaa"
```

**Explanation:**
As all the characters are the same, the encrypted string will also be the same.

## Constraints

```text
1 <= s.length <= 100
1 <= k <= 10⁴
s consists of lowercase English letters.
```

---
## Iterative Simulation

**Intuition**
We can directly simulate the encryption process by iterating through each character of the string and calculating the index of the character that should replace it using modulo arithmetic.

**Steps**
- Get the length `n` of the string `s`.
- Initialize an empty result string (or builder).
- Loop through each index `i` from `0` to `n - 1`.
- Calculate the target index as `(i + k) % n`.
- Append the character at the target index to the result.
- Return the result.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def getEncryptedString(self, s: str, k: int) -&gt; str:\n        n = len(s)\n        res = []\n        for i in range(n):\n            res.append(s[(i + k) % n])\n        return ''.join(res)" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String getEncryptedString(String s, int k) {\n        int n = s.length();\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i &lt; n; i++) {\n            sb.append(s.charAt((i + k) % n));\n        }\n        return sb.toString();\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string getEncryptedString(string s, int k) {\n        int n = s.length();\n        string res = \"\";\n        for (int i = 0; i &lt; n; i++) {\n            res += s[(i + k) % n];\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    getEncryptedString(s, k) {\n        const n = s.length;\n        let res = '';\n        for (let i = 0; i &lt; n; i++) {\n            res += s[(i + k) % n];\n        }\n        return res;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    getEncryptedString(s: string, k: number): string {\n        const n = s.length;\n        let res = '';\n        for (let i = 0; i &lt; n; i++) {\n            res += s[(i + k) % n];\n        }\n        return res;\n    }\n}" },]} />

**Complexity**
- Time: O(n), where n is the length of the string. We iterate through the string once.
- Space: O(n) to store the result string.
- Notes: This approach is straightforward and works well for the given constraints.

---
## String Slicing

**Intuition**
The encryption process effectively rotates the string to the left by `k` positions. The character at index `i` in the result comes from index `(i + k) % n` in the original string. This is equivalent to taking the substring starting from index `k` and appending the substring ending at index `k`.

**Steps**
- Calculate `k = k % n` to handle cases where `k` is larger than the string length.
- Return the concatenation of `s[k:]` and `s[:k]`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def getEncryptedString(self, s: str, k: int) -&gt; str:\n        n = len(s)\n        k %= n\n        return s[k:] + s[:k]" },
{ name: "Java", lang: "java", code: "class Solution {\n    public String getEncryptedString(String s, int k) {\n        int n = s.length();\n        k = k % n;\n        return s.substring(k) + s.substring(0, k);\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    string getEncryptedString(string s, int k) {\n        int n = s.length();\n        k = k % n;\n        return s.substr(k) + s.substr(0, k);\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    getEncryptedString(s, k) {\n        const n = s.length;\n        k = k % n;\n        return s.slice(k) + s.slice(0, k);\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    getEncryptedString(s: string, k: number): string {\n        const n = s.length;\n        k = k % n;\n        return s.slice(k) + s.slice(0, k);\n    }\n}" },]} />

**Complexity**
- Time: O(n) for creating the new string (slicing and concatenation).
- Space: O(n) to store the result.
- Notes: This is the most idiomatic and concise solution in languages that support string slicing.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find the Encrypted String Solutions","datePublished":"2026-01-27","description":"Encrypt a string by replacing each character with the character k positions ahead, wrapping around if necessary.","keywords":"LeetCode, Find the Encrypted String, Easy, String"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Iterative Simulation approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(n), where n is the length of the string, as we iterate through each character once."}},{"@type":"Question","name":"What is the space complexity of the String Slicing approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) to store the resulting string, as strings are immutable in most languages."}}]}).replace(/</g,'\u003c')}
</script>