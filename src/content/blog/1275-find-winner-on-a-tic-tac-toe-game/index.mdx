---
title: "Find Winner on a Tic Tac Toe Game"
summary: "Determine the winner of a Tic Tac Toe game given a sequence of moves, or if the game is a draw or pending."
date: "Oct 30, 2025"
tags:
  - Array
  - Hash Table
  - Matrix
  - Simulation
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 54.60% | **Paid:** No
> **Topics:** Array, Hash Table, Matrix, Simulation

Tic-tac-toe is played by two players A and B on a 3 x 3 grid.

The rules of Tic-Tac-Toe are:

- Players take turns placing characters into empty squares ' '.
- The first player A always places 'X' characters, while the second player B always places 'O' characters.
- 'X' and 'O' characters are always placed into empty squares, never on filled ones.
- The game ends when there are three of the same (non-empty) character filling any row, column, or diagonal.
- The game also ends if all squares are non-empty.
- No more moves can be played after the game has ended.

Given a 2D integer array moves where moves[i] = [rowi, coli] indicates that the ith move will be played on grid[rowi][coli]. return the winner of the game if it exists (A or B). In case the game ends in a draw return "Draw". If there are still movements to play return "Pending".

You can assume that moves is valid (i.e., it follows the rules of Tic-Tac-Toe), the grid is initially empty, and A will play first.

- Examples
- Constraints
- [Simulation with Matrix](#simulation-with-matrix)
- [Row/Column Counting](#rowcolumn-counting)
- [Hash Set Tracking](#hash-set-tracking)

## Examples

### Example 1

**Input:**
```text
moves = [[0,0],[2,0],[1,1],[2,1],[2,2]]
```

**Output:**
```text
"A"
```

**Explanation:**
A wins, they always play first.

### Example 2

**Input:**
```text
moves = [[0,0],[1,1],[0,1],[0,2],[1,0],[2,0]]
```

**Output:**
```text
"B"
```

**Explanation:**
B wins.

### Example 3

**Input:**
```text
moves = [[0,0],[1,1],[2,0],[1,0],[1,2],[2,1],[0,1],[0,2],[2,2]]
```

**Output:**
```text
"Draw"
```

**Explanation:**
The game ends in a draw since there are no moves to make.

## Constraints

```text
1 <= moves.length <= 9
moves[i].length == 2
0 <= rowi, coli <= 2
There are no repeated moves.
```

---
## Simulation with Matrix

**Intuition**
Simulate the game by creating a 3x3 board and placing each move alternately for players A and B, then check for winning conditions.

**Steps**
- Initialize a 3x3 board with empty values
- Iterate through moves, placing 'X' for A (even indices) and 'O' for B (odd indices)
- After each move, check if the current player has won by checking rows, columns, and diagonals
- If no winner after all moves, return "Draw" if board is full, otherwise "Pending"

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def tictactoe(self, moves: List[List[int]]) -> str:
        board = [[0] * 3 for _ in range(3)]
        
        def check_winner(player):
            # Check rows
            for i in range(3):
                if all(board[i][j] == player for j in range(3)):
                    return True
            # Check columns
            for j in range(3):
                if all(board[i][j] == player for i in range(3)):
                    return True
            # Check diagonals
            if all(board[i][i] == player for i in range(3)):
                return True
            if all(board[i][2 - i] == player for i in range(3)):
                return True
            return False
        
        for i, (row, col) in enumerate(moves):
            player = 1 if i % 2 == 0 else 2
            board[row][col] = player
            if check_winner(player):
                return "A" if player == 1 else "B"
        
        return "Draw" if len(moves) == 9 else "Pending"
` },
{ name: "Java", lang: "java", code: `class Solution {
    public String tictactoe(int[][] moves) {
        int[][] board = new int[3][3];
        
        for (int i = 0; i &lt; moves.length; i++) {
            int player = (i % 2 == 0) ? 1 : 2;
            int row = moves[i][0];
            int col = moves[i][1];
            board[row][col] = player;
            
            if (checkWinner(board, player)) {
                return (player == 1) ? "A" : "B";
            }
        }
        
        return (moves.length == 9) ? "Draw" : "Pending";
    }
    
    private boolean checkWinner(int[][] board, int player) {
        // Check rows
        for (int i = 0; i &lt; 3; i++) {
            if (board[i][0] == player && board[i][1] == player && board[i][2] == player) {
                return true;
            }
        }
        // Check columns
        for (int j = 0; j &lt; 3; j++) {
            if (board[0][j] == player && board[1][j] == player && board[2][j] == player) {
                return true;
            }
        }
        // Check diagonals
        if (board[0][0] == player && board[1][1] == player && board[2][2] == player) {
            return true;
        }
        if (board[0][2] == player && board[1][1] == player && board[2][0] == player) {
            return true;
        }
        return false;
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    string tictactoe(vector<vector<int>&gt;& moves) {
        vector<vector<int>&gt; board(3, vector<int>(3, 0));
        
        for (int i = 0; i &lt; moves.size(); i++) {
            int player = (i % 2 == 0) ? 1 : 2;
            int row = moves[i][0];
            int col = moves[i][1];
            board[row][col] = player;
            
            if (checkWinner(board, player)) {
                return (player == 1) ? "A" : "B";
            }
        }
        
        return (moves.size() == 9) ? "Draw" : "Pending";
    }
    
private:
    bool checkWinner(vector<vector<int>&gt;& board, int player) {
        // Check rows
        for (int i = 0; i &lt; 3; i++) {
            if (board[i][0] == player && board[i][1] == player && board[i][2] == player) {
                return true;
            }
        }
        // Check columns
        for (int j = 0; j &lt; 3; j++) {
            if (board[0][j] == player && board[1][j] == player && board[2][j] == player) {
                return true;
            }
        }
        // Check diagonals
        if (board[0][0] == player && board[1][1] == player && board[2][2] == player) {
            return true;
        }
        if (board[0][2] == player && board[1][1] == player && board[2][0] == player) {
            return true;
        }
        return false;
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[][]} moves
 * @return {string}
 */
var tictactoe = function(moves) {
    const board = Array(3).fill(null).map(() =&gt; Array(3).fill(0));
    
    const checkWinner = (player) =&gt; {
        // Check rows
        for (let i = 0; i &lt; 3; i++) {
            if (board[i][0] === player && board[i][1] === player && board[i][2] === player) {
                return true;
            }
        }
        // Check columns
        for (let j = 0; j &lt; 3; j++) {
            if (board[0][j] === player && board[1][j] === player && board[2][j] === player) {
                return true;
            }
        }
        // Check diagonals
        if (board[0][0] === player && board[1][1] === player && board[2][2] === player) {
            return true;
        }
        if (board[0][2] === player && board[1][1] === player && board[2][0] === player) {
            return true;
        }
        return false;
    };
    
    for (let i = 0; i &lt; moves.length; i++) {
        const player = (i % 2 === 0) ? 1 : 2;
        const row = moves[i][0];
        const col = moves[i][1];
        board[row][col] = player;
        
        if (checkWinner(player)) {
            return (player === 1) ? "A" : "B";
        }
    }
    
    return (moves.length === 9) ? "Draw" : "Pending";
};
` },
{ name: "TypeScript", lang: "typescript", code: `function tictactoe(moves: number[][]): string {
    const board: number[][] = Array(3).fill(null).map(() =&gt; Array(3).fill(0));
    
    const checkWinner = (player: number): boolean =&gt; {
        // Check rows
        for (let i = 0; i &lt; 3; i++) {
            if (board[i][0] === player && board[i][1] === player && board[i][2] === player) {
                return true;
            }
        }
        // Check columns
        for (let j = 0; j &lt; 3; j++) {
            if (board[0][j] === player && board[1][j] === player && board[2][j] === player) {
                return true;
            }
        }
        // Check diagonals
        if (board[0][0] === player && board[1][1] === player && board[2][2] === player) {
            return true;
        }
        if (board[0][2] === player && board[1][1] === player && board[2][0] === player) {
            return true;
        }
        return false;
    };
    
    for (let i = 0; i &lt; moves.length; i++) {
        const player = (i % 2 === 0) ? 1 : 2;
        const row = moves[i][0];
        const col = moves[i][1];
        board[row][col] = player;
        
        if (checkWinner(player)) {
            return (player === 1) ? "A" : "B";
        }
    }
    
    return (moves.length === 9) ? "Draw" : "Pending";
}
` },]} />

**Complexity**
- Time: O(n) where n is the number of moves (at most 9)
- Space: O(1) for the fixed 3x3 board
- Notes: Simple and intuitive approach with constant space

---
## Row/Column Counting

**Intuition**
Instead of maintaining a full board, track counts for each row, column, and diagonal. A player wins when any count reaches 3.

**Steps**
- Initialize arrays for row counts, column counts, and variables for diagonals
- For each move, increment for player A and decrement for player B
- After each move, check if any count reaches 3 or -3
- If no winner after all moves, return "Draw" if 9 moves were made, otherwise "Pending"

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def tictactoe(self, moves: List[List[int]]) -> str:
        rows = [0] * 3
        cols = [0] * 3
        diag1 = 0  # top-left to bottom-right
        diag2 = 0  # top-right to bottom-left
        
        for i, (row, col) in enumerate(moves):
            player = 1 if i % 2 == 0 else -1
            rows[row] += player
            cols[col] += player
            
            if row == col:
                diag1 += player
            if row + col == 2:
                diag2 += player
            
            if abs(rows[row]) == 3 or abs(cols[col]) == 3 or abs(diag1) == 3 or abs(diag2) == 3:
                return "A" if player == 1 else "B"
        
        return "Draw" if len(moves) == 9 else "Pending"
` },
{ name: "Java", lang: "java", code: `class Solution {
    public String tictactoe(int[][] moves) {
        int[] rows = new int[3];
        int[] cols = new int[3];
        int diag1 = 0;  // top-left to bottom-right
        int diag2 = 0;  // top-right to bottom-left
        
        for (int i = 0; i &lt; moves.length; i++) {
            int player = (i % 2 == 0) ? 1 : -1;
            int row = moves[i][0];
            int col = moves[i][1];
            
            rows[row] += player;
            cols[col] += player;
            
            if (row == col) {
                diag1 += player;
            }
            if (row + col == 2) {
                diag2 += player;
            }
            
            if (Math.abs(rows[row]) == 3 || Math.abs(cols[col]) == 3 || 
                Math.abs(diag1) == 3 || Math.abs(diag2) == 3) {
                return (player == 1) ? "A" : "B";
            }
        }
        
        return (moves.length == 9) ? "Draw" : "Pending";
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    string tictactoe(vector<vector<int>&gt;& moves) {
        vector<int> rows(3, 0);
        vector<int> cols(3, 0);
        int diag1 = 0;  // top-left to bottom-right
        int diag2 = 0;  // top-right to bottom-left
        
        for (int i = 0; i &lt; moves.size(); i++) {
            int player = (i % 2 == 0) ? 1 : -1;
            int row = moves[i][0];
            int col = moves[i][1];
            
            rows[row] += player;
            cols[col] += player;
            
            if (row == col) {
                diag1 += player;
            }
            if (row + col == 2) {
                diag2 += player;
            }
            
            if (abs(rows[row]) == 3 || abs(cols[col]) == 3 || 
                abs(diag1) == 3 || abs(diag2) == 3) {
                return (player == 1) ? "A" : "B";
            }
        }
        
        return (moves.size() == 9) ? "Draw" : "Pending";
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[][]} moves
 * @return {string}
 */
var tictactoe = function(moves) {
    const rows = [0, 0, 0];
    const cols = [0, 0, 0];
    let diag1 = 0;  // top-left to bottom-right
    let diag2 = 0;  // top-right to bottom-left
    
    for (let i = 0; i &lt; moves.length; i++) {
        const player = (i % 2 === 0) ? 1 : -1;
        const row = moves[i][0];
        const col = moves[i][1];
        
        rows[row] += player;
        cols[col] += player;
        
        if (row === col) {
            diag1 += player;
        }
        if (row + col === 2) {
            diag2 += player;
        }
        
        if (Math.abs(rows[row]) === 3 || Math.abs(cols[col]) === 3 || 
            Math.abs(diag1) === 3 || Math.abs(diag2) === 3) {
            return (player === 1) ? "A" : "B";
        }
    }
    
    return (moves.length === 9) ? "Draw" : "Pending";
};
` },
{ name: "TypeScript", lang: "typescript", code: `function tictactoe(moves: number[][]): string {
    const rows: number[] = [0, 0, 0];
    const cols: number[] = [0, 0, 0];
    let diag1: number = 0;  // top-left to bottom-right
    let diag2: number = 0;  // top-right to bottom-left
    
    for (let i = 0; i &lt; moves.length; i++) {
        const player: number = (i % 2 === 0) ? 1 : -1;
        const row: number = moves[i][0];
        const col: number = moves[i][1];
        
        rows[row] += player;
        cols[col] += player;
        
        if (row === col) {
            diag1 += player;
        }
        if (row + col === 2) {
            diag2 += player;
        }
        
        if (Math.abs(rows[row]) === 3 || Math.abs(cols[col]) === 3 || 
            Math.abs(diag1) === 3 || Math.abs(diag2) === 3) {
            return (player === 1) ? "A" : "B";
        }
    }
    
    return (moves.length === 9) ? "Draw" : "Pending";
}
` },]} />

**Complexity**
- Time: O(n) where n is the number of moves
- Space: O(1) using fixed-size arrays
- Notes: More efficient than full board simulation, uses counting instead of checking all cells

---
## Hash Set Tracking

**Intuition**
Track all positions occupied by each player using sets, then check if any winning combination is fully contained in a player's set.

**Steps**
- Create sets for both players A and B
- Define all 8 winning combinations (3 rows, 3 columns, 2 diagonals)
- After placing all moves, check if any winning combination is a subset of either player's positions
- Return the winner, "Draw" if board is full, or "Pending" otherwise

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: `class Solution:
    def tictactoe(self, moves: List[List[int]]) -> str:
        player_a = set()
        player_b = set()
        
        # All winning combinations
        win_combinations = [
            # Rows
            {(0, 0), (0, 1), (0, 2)},
            {(1, 0), (1, 1), (1, 2)},
            {(2, 0), (2, 1), (2, 2)},
            # Columns
            {(0, 0), (1, 0), (2, 0)},
            {(0, 1), (1, 1), (2, 1)},
            {(0, 2), (1, 2), (2, 2)},
            # Diagonals
            {(0, 0), (1, 1), (2, 2)},
            {(0, 2), (1, 1), (2, 0)}
        ]
        
        for i, (row, col) in enumerate(moves):
            if i % 2 == 0:
                player_a.add((row, col))
            else:
                player_b.add((row, col))
        
        for combo in win_combinations:
            if combo.issubset(player_a):
                return "A"
            if combo.issubset(player_b):
                return "B"
        
        return "Draw" if len(moves) == 9 else "Pending"
` },
{ name: "Java", lang: "java", code: `import java.util.*;

class Solution {
    public String tictactoe(int[][] moves) {
        Set<String> playerA = new HashSet&lt;&gt;();
        Set<String> playerB = new HashSet&lt;&gt;();
        
        // All winning combinations
        List<Set<String>&gt; winCombinations = new ArrayList&lt;&gt;();
        
        // Rows
        winCombinations.add(Set.of("0,0", "0,1", "0,2"));
        winCombinations.add(Set.of("1,0", "1,1", "1,2"));
        winCombinations.add(Set.of("2,0", "2,1", "2,2"));
        
        // Columns
        winCombinations.add(Set.of("0,0", "1,0", "2,0"));
        winCombinations.add(Set.of("0,1", "1,1", "2,1"));
        winCombinations.add(Set.of("0,2", "1,2", "2,2"));
        
        // Diagonals
        winCombinations.add(Set.of("0,0", "1,1", "2,2"));
        winCombinations.add(Set.of("0,2", "1,1", "2,0"));
        
        for (int i = 0; i &lt; moves.length; i++) {
            int row = moves[i][0];
            int col = moves[i][1];
            String pos = row + "," + col;
            
            if (i % 2 == 0) {
                playerA.add(pos);
            } else {
                playerB.add(pos);
            }
        }
        
        for (Set<String> combo : winCombinations) {
            if (playerA.containsAll(combo)) {
                return "A";
            }
            if (playerB.containsAll(combo)) {
                return "B";
            }
        }
        
        return (moves.length == 9) ? "Draw" : "Pending";
    }
}
` },
{ name: "C++", lang: "cpp", code: `class Solution {
public:
    string tictactoe(vector<vector<int>&gt;& moves) {
        set<pair<int, int>&gt; playerA;
        set<pair<int, int>&gt; playerB;
        
        // All winning combinations
        vector<vector<pair<int, int>&gt;&gt; winCombinations = {
            // Rows
            {{0, 0}, {0, 1}, {0, 2}},
            {{1, 0}, {1, 1}, {1, 2}},
            {{2, 0}, {2, 1}, {2, 2}},
            // Columns
            {{0, 0}, {1, 0}, {2, 0}},
            {{0, 1}, {1, 1}, {2, 1}},
            {{0, 2}, {1, 2}, {2, 2}},
            // Diagonals
            {{0, 0}, {1, 1}, {2, 2}},
            {{0, 2}, {1, 1}, {2, 0}}
        };
        
        for (int i = 0; i &lt; moves.size(); i++) {
            int row = moves[i][0];
            int col = moves[i][1];
            
            if (i % 2 == 0) {
                playerA.insert({row, col});
            } else {
                playerB.insert({row, col});
            }
        }
        
        for (auto& combo : winCombinations) {
            bool aWins = true;
            bool bWins = true;
            for (auto& pos : combo) {
                if (playerA.find(pos) == playerA.end()) {
                    aWins = false;
                }
                if (playerB.find(pos) == playerB.end()) {
                    bWins = false;
                }
            }
            if (aWins) return "A";
            if (bWins) return "B";
        }
        
        return (moves.size() == 9) ? "Draw" : "Pending";
    }
};
` },
{ name: "JavaScript", lang: "javascript", code: `/**
 * @param {number[][]} moves
 * @return {string}
 */
var tictactoe = function(moves) {
    const playerA = new Set();
    const playerB = new Set();
    
    // All winning combinations
    const winCombinations = [
        // Rows
        [[0, 0], [0, 1], [0, 2]],
        [[1, 0], [1, 1], [1, 2]],
        [[2, 0], [2, 1], [2, 2]],
        // Columns
        [[0, 0], [1, 0], [2, 0]],
        [[0, 1], [1, 1], [2, 1]],
        [[0, 2], [1, 2], [2, 2]],
        // Diagonals
        [[0, 0], [1, 1], [2, 2]],
        [[0, 2], [1, 1], [2, 0]]
    ];
    
    const posKey = (row, col) =&gt; row + ',' + col;
    
    for (let i = 0; i &lt; moves.length; i++) {
        const row = moves[i][0];
        const col = moves[i][1];
        const key = posKey(row, col);
        
        if (i % 2 === 0) {
            playerA.add(key);
        } else {
            playerB.add(key);
        }
    }
    
    for (const combo of winCombinations) {
        const aWins = combo.every(([r, c]) =&gt; playerA.has(posKey(r, c)));
        const bWins = combo.every(([r, c]) =&gt; playerB.has(posKey(r, c)));
        
        if (aWins) return "A";
        if (bWins) return "B";
    }
    
    return (moves.length === 9) ? "Draw" : "Pending";
};
` },
{ name: "TypeScript", lang: "typescript", code: `function tictactoe(moves: number[][]): string {
    const playerA: Set<string> = new Set();
    const playerB: Set<string> = new Set();
    
    // All winning combinations
    const winCombinations: number[][][] = [
        // Rows
        [[0, 0], [0, 1], [0, 2]],
        [[1, 0], [1, 1], [1, 2]],
        [[2, 0], [2, 1], [2, 2]],
        // Columns
        [[0, 0], [1, 0], [2, 0]],
        [[0, 1], [1, 1], [2, 1]],
        [[0, 2], [1, 2], [2, 2]],
        // Diagonals
        [[0, 0], [1, 1], [2, 2]],
        [[0, 2], [1, 1], [2, 0]]
    ];
    
    const posKey = (row: number, col: number): string =&gt; row + ',' + col;
    
    for (let i = 0; i &lt; moves.length; i++) {
        const row: number = moves[i][0];
        const col: number = moves[i][1];
        const key: string = posKey(row, col);
        
        if (i % 2 === 0) {
            playerA.add(key);
        } else {
            playerB.add(key);
        }
    }
    
    for (const combo of winCombinations) {
        const aWins: boolean = combo.every(([r, c]) =&gt; playerA.has(posKey(r, c)));
        const bWins: boolean = combo.every(([r, c]) =&gt; playerB.has(posKey(r, c)));
        
        if (aWins) return "A";
        if (bWins) return "B";
    }
    
    return (moves.length === 9) ? "Draw" : "Pending";
}
` },]} />

**Complexity**
- Time: O(n) where n is the number of moves
- Space: O(n) for storing player positions
- Notes: More intuitive for checking winning conditions but uses more space than counting approach

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find Winner on a Tic Tac Toe Game Solutions","datePublished":"2025-10-30","description":"Determine the winner of a Tic Tac Toe game given a sequence of moves, or if the game is a draw or pending.","keywords":"LeetCode, Find Winner on a Tic Tac Toe Game, Easy, Array, Hash Table, Matrix, Simulation"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Simulation with Matrix approach?","acceptedAnswer":{"@type":"Answer","text":"O(n) where n is the number of moves (at most 9). We iterate through all moves and check winning conditions in constant time."}},{"@type":"Question","name":"What is the space complexity of the Row/Column Counting approach?","acceptedAnswer":{"@type":"Answer","text":"O(1) as we only use fixed-size arrays of length 3 and a few integer variables regardless of input size."}},{"@type":"Question","name":"Which approach is most efficient for this problem?","acceptedAnswer":{"@type":"Answer","text":"The Row/Column Counting approach is most efficient with O(n) time and O(1) space, avoiding the need to check all cells for winning conditions."}},{"@type":"Question","name":"How do we determine if the game is a draw vs pending?","acceptedAnswer":{"@type":"Answer","text":"If all 9 moves are made with no winner, it's a draw. If fewer than 9 moves are made with no winner, the game is pending."}}]}).replace(/</g,'\u003c')}
</script>