---
title: "Smallest Absent Positive Greater Than Average"
summary: "Find the smallest positive integer not present in the array that is strictly greater than the array's average."
date: "Aug 01, 2025"
tags:
  - Array
  - Hash Table
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 34.30% | **Paid:** No
> **Topics:** Array, Hash Table

Given an integer array nums, return the smallest positive integer that is strictly greater than the average of the array and is not present in the array.

The average is calculated as the sum of all elements divided by the number of elements. It is guaranteed that an answer exists.

## Examples

### Example 1:
```text
Input: nums = [1, 2, 3]
Output: 4
Explanation: The average is (1 + 2 + 3) / 3 = 2.
The integers greater than 2 are 3, 4, 5, ...
3 is present in the array.
4 is not present in the array and is positive.
Thus, the answer is 4.
```

### Example 2:
```text
Input: nums = [3, 4, 5]
Output: 6
Explanation: The average is (3 + 4 + 5) / 3 = 4.
The integers greater than 4 are 5, 6, 7, ...
5 is present in the array.
6 is not present in the array and is positive.
Thus, the answer is 6.
```

### Example 3:
```text
Input: nums = [-1, -2, -3]
Output: 1
Explanation: The average is (-1 - 2 - 3) / 3 = -2.
The integers greater than -2 are -1, 0, 1, 2, ...
The smallest positive integer not in the array is 1.
```

## Constraints

```text
1 <= nums.length <= 10⁵
-10⁹ <= nums[i] <= 10⁹
It is guaranteed that an answer exists.
```

- Examples
- Constraints
- [Approach 1: Brute Force](#approach-1-brute-force)
- [Approach 2: Hash Set](#approach-2-hash-set)
- [Approach 3: Sorting and Binary Search](#approach-3-sorting-and-binary-search)

## Examples

### Example 1:
```text
Input: nums = [1, 2, 3]
Output: 4
```

### Example 2:
```text
Input: nums = [3, 4, 5]
Output: 6
```

### Example 3:
```text
Input: nums = [-1, -2, -3]
Output: 1
```

## Constraints

```text
1 <= nums.length <= 10⁵
-10⁹ <= nums[i] <= 10⁹
```

---
## Approach 1: Brute Force

**Intuition**
Calculate the average, then iterate through integers starting from the floor of the average plus one. For each integer, check if it exists in the array and if it is positive.

**Steps**
- Calculate the average of the array.
- Initialize a candidate integer `x` to `floor(average) + 1`.
- Loop indefinitely:
  - Check if `x` is positive and not present in `nums`.
  - If both conditions are met, return `x`.
  - Otherwise, increment `x` and repeat.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestAbsentPositive(self, nums: list[int]) -&gt; int:\n        avg = sum(nums) / len(nums)\n        x = int(avg) + 1\n        while True:\n            if x &gt; 0 and x not in nums:\n                return x\n            x += 1\n" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int smallestAbsentPositive(int[] nums) {\n        double sum = 0;\n        for (int n : nums) sum += n;\n        double avg = sum / nums.length;\n        int x = (int) Math.floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && !contains(nums, x)) {\n                return x;\n            }\n            x++;\n        }\n    }\n    private boolean contains(int[] nums, int target) {\n        for (int n : nums) {\n            if (n == target) return true;\n        }\n        return false;\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    int smallestAbsentPositive(vector&lt;int&gt;& nums) {\n        double sum = accumulate(nums.begin(), nums.end(), 0.0);\n        double avg = sum / nums.size();\n        int x = (int)floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && find(nums.begin(), nums.end(), x) == nums.end()) {\n                return x;\n            }\n            x++;\n        }\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    smallestAbsentPositive(nums) {\n        const sum = nums.reduce((a, b) =&gt; a + b, 0);\n        const avg = sum / nums.length;\n        let x = Math.floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && !nums.includes(x)) {\n                return x;\n            }\n            x++;\n        }\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    smallestAbsentPositive(nums: number[]): number {\n        const sum = nums.reduce((a, b) =&gt; a + b, 0);\n        const avg = sum / nums.length;\n        let x = Math.floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && !nums.includes(x)) {\n                return x;\n            }\n            x++;\n        }\n    }\n}\n" },]} />

**Complexity**
- Time: O(N * M), where N is the length of the array and M is the value of the answer. In the worst case, we scan the array for every candidate integer.
- Space: O(1)
- Notes: Simple to implement but inefficient for large arrays or large gaps in numbers.

---
## Approach 2: Hash Set

**Intuition**
Optimize the lookup process by storing all array elements in a Hash Set. This allows us to check for the presence of a candidate integer in O(1) time on average.

**Steps**
- Calculate the average of the array.
- Store all elements of `nums` in a Hash Set.
- Initialize a candidate integer `x` to `floor(average) + 1`.
- Loop indefinitely:
  - Check if `x` is positive and not present in the Hash Set.
  - If both conditions are met, return `x`.
  - Otherwise, increment `x` and repeat.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def smallestAbsentPositive(self, nums: list[int]) -&gt; int:\n        avg = sum(nums) / len(nums)\n        num_set = set(nums)\n        x = int(avg) + 1\n        while True:\n            if x &gt; 0 and x not in num_set:\n                return x\n            x += 1\n" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int smallestAbsentPositive(int[] nums) {\n        double sum = 0;\n        for (int n : nums) sum += n;\n        double avg = sum / nums.length;\n        \n        Set&lt;Integer&gt; set = new HashSet&lt;&gt;();\n        for (int n : nums) set.add(n);\n        \n        int x = (int) Math.floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && !set.contains(x)) {\n                return x;\n            }\n            x++;\n        }\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include &lt;vector&gt;\n#include &lt;unordered_set&gt;\n#include &lt;numeric&gt;\nusing namespace std;\n\nclass Solution {\npublic:\n    int smallestAbsentPositive(vector&lt;int&gt;& nums) {\n        double sum = accumulate(nums.begin(), nums.end(), 0.0);\n        double avg = sum / nums.size();\n        unordered_set&lt;int&gt; num_set(nums.begin(), nums.end());\n        int x = (int)floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && num_set.find(x) == num_set.end()) {\n                return x;\n            }\n            x++;\n        }\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    smallestAbsentPositive(nums) {\n        const sum = nums.reduce((a, b) =&gt; a + b, 0);\n        const avg = sum / nums.length;\n        const numSet = new Set(nums);\n        let x = Math.floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && !numSet.has(x)) {\n                return x;\n            }\n            x++;\n        }\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    smallestAbsentPositive(nums: number[]): number {\n        const sum = nums.reduce((a, b) =&gt; a + b, 0);\n        const avg = sum / nums.length;\n        const numSet = new Set(nums);\n        let x = Math.floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && !numSet.has(x)) {\n                return x;\n            }\n            x++;\n        }\n    }\n}\n" },]} />

**Complexity**
- Time: O(N + M), where N is the length of the array (to build the set) and M is the value of the answer (to find the missing number).
- Space: O(N) to store the Hash Set.
- Notes: Significantly faster than brute force for lookups, trading space for time.

---
## Approach 3: Sorting and Binary Search

**Intuition**
Sort the array to organize the numbers. Then, iterate through candidate integers starting from `floor(average) + 1`. For each candidate, use binary search to check if it exists in the sorted array.

**Steps**
- Sort the array `nums`.
- Calculate the average.
- Initialize `x` to `floor(average) + 1`.
- Loop indefinitely:
  - Use binary search to check if `x` exists in `nums`.
  - If `x` is positive and not found, return `x`.
  - Otherwise, increment `x`.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "import bisect\n\nclass Solution:\n    def smallestAbsentPositive(self, nums: list[int]) -&gt; int:\n        nums.sort()\n        avg = sum(nums) / len(nums)\n        x = int(avg) + 1\n        while True:\n            if x &gt; 0:\n                idx = bisect.bisect_left(nums, x)\n                if idx == len(nums) or nums[idx] != x:\n                    return x\n            x += 1\n" },
{ name: "Java", lang: "java", code: "import java.util.*;\n\nclass Solution {\n    public int smallestAbsentPositive(int[] nums) {\n        Arrays.sort(nums);\n        double sum = 0;\n        for (int n : nums) sum += n;\n        double avg = sum / nums.length;\n        \n        int x = (int) Math.floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && Arrays.binarySearch(nums, x) &lt; 0) {\n                return x;\n            }\n            x++;\n        }\n    }\n}\n" },
{ name: "C++", lang: "cpp", code: "#include <vector>\n#include <algorithm>\n#include <numeric>\nusing namespace std;\n\nclass Solution {\npublic:\n    int smallestAbsentPositive(vector<int>& nums) {\n        sort(nums.begin(), nums.end());\n        double sum = accumulate(nums.begin(), nums.end(), 0.0);\n        double avg = sum / nums.size();\n        int x = (int)floor(avg) + 1;\n        while (true) {\n            if (x &gt; 0 && !binary_search(nums.begin(), nums.end(), x)) {\n                return x;\n            }\n            x++;\n        }\n    }\n};\n" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    smallestAbsentPositive(nums) {\n        nums.sort((a, b) =&gt; a - b);\n        const sum = nums.reduce((a, b) =&gt; a + b, 0);\n        const avg = sum / nums.length;\n        let x = Math.floor(avg) + 1;\n        \n        const binarySearch = (arr, target) =&gt; {\n            let left = 0, right = arr.length;\n            while (left &lt; right) {\n                const mid = Math.floor((left + right) / 2);\n                if (arr[mid] &lt; target) left = mid + 1;\n                else right = mid;\n            }\n            return left &lt; arr.length && arr[left] === target;\n        };\n\n        while (true) {\n            if (x &gt; 0 && !binarySearch(nums, x)) {\n                return x;\n            }\n            x++;\n        }\n    }\n}\n" },
{ name: "TypeScript", lang: "typescript", code: "class Solution {\n    smallestAbsentPositive(nums: number[]): number {\n        nums.sort((a, b) =&gt; a - b);\n        const sum = nums.reduce((a, b) =&gt; a + b, 0);\n        const avg = sum / nums.length;\n        let x = Math.floor(avg) + 1;\n\n        const binarySearch = (arr: number[], target: number): boolean =&gt; {\n            let left = 0, right = arr.length;\n            while (left &lt; right) {\n                const mid = Math.floor((left + right) / 2);\n                if (arr[mid] &lt; target) left = mid + 1;\n                else right = mid;\n            }\n            return left &lt; arr.length && arr[left] === target;\n        };\n\n        while (true) {\n            if (x &gt; 0 && !binarySearch(nums, x)) {\n                return x;\n            }\n            x++;\n        }\n    }\n}\n" },]} />

**Complexity**
- Time: O(N log N + M log N), where N is the length of the array (for sorting) and M is the value of the answer.
- Space: O(1) or O(N) depending on the sorting algorithm's space complexity.
- Notes: Useful if the array is already sorted or if memory is constrained and O(N) space for a Hash Set is undesirable, though generally slower than the Hash Set approach due to the log N factor for each check.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Smallest Absent Positive Greater Than Average Solutions","datePublished":"2025-08-01","description":"Find the smallest positive integer not present in the array that is strictly greater than the array's average.","keywords":"LeetCode, Smallest Absent Positive Greater Than Average, Easy, Array, Hash Table"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N * M), where N is the array length and M is the value of the answer."}},{"@type":"Question","name":"What is the time complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity is O(N + M), where N is the array length and M is the value of the answer."}},{"@type":"Question","name":"What is the space complexity of the Hash Set approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(N) to store the elements in the set."}}]}).replace(/</g,'\u003c')}
</script>