---
title: "Find the Distinct Difference Array"
summary: "Calculate an array where each element is the count of distinct numbers in the prefix minus the count of distinct numbers in the suffix."
date: "Sep 09, 2025"
tags:
  - Array
  - Hash Table
draft: false
difficulty: "Easy"
---

import CodeTabs from "@components/CodeTabs"

> **Difficulty:** Easy | **Acceptance:** 77.20% | **Paid:** No
> **Topics:** Array, Hash Table

You are given a 0-indexed array nums of length n.

The distinct difference array of nums is an array diff of length n such that diff[i] equals the number of distinct elements in the suffix nums[i + 1 ... n - 1] subtracted from the number of distinct elements in the prefix nums[0 ... i].

Return the distinct difference array of nums.

- Examples
- Constraints
- [Approach 1: Brute Force](#approach-1-brute-force)
- [Approach 2: Prefix and Suffix Sets](#approach-2-prefix-and-suffix-sets)

## Examples

Example 1:
```text
Input: nums = [1,2,3,4,5]
Output: [-3,-1,1,3,5]
Explanation: 
- For index 0, the prefix is [1] and the suffix is [2,3,4,5]. There is 1 distinct element in the prefix and 4 distinct elements in the suffix. The difference is 1 - 4 = -3.
- For index 1, the prefix is [1,2] and the suffix is [3,4,5]. There are 2 distinct elements in the prefix and 3 distinct elements in the suffix. The difference is 2 - 3 = -1.
- For index 2, the prefix is [1,2,3] and the suffix is [4,5]. There are 3 distinct elements in the prefix and 2 distinct elements in the suffix. The difference is 3 - 2 = 1.
- For index 3, the prefix is [1,2,3,4] and the suffix is [5]. There are 4 distinct elements in the prefix and 1 distinct element in the suffix. The difference is 4 - 1 = 3.
- For index 4, the prefix is [1,2,3,4,5] and the suffix is []. There are 5 distinct elements in the prefix and 0 distinct elements in the suffix. The difference is 5 - 0 = 5.
```

Example 2:
```text
Input: nums = [3,2,3,4,2]
Output: [-2,-1,0,2,3]
Explanation: 
- For index 0, the prefix is [3] and the suffix is [2,3,4,2]. There is 1 distinct element in the prefix and 3 distinct elements in the suffix. The difference is 1 - 3 = -2.
- For index 1, the prefix is [3,2] and the suffix is [3,4,2]. There are 2 distinct elements in the prefix and 3 distinct elements in the suffix. The difference is 2 - 3 = -1.
- For index 2, the prefix is [3,2,3] and the suffix is [4,2]. There are 2 distinct elements in the prefix and 2 distinct elements in the suffix. The difference is 2 - 2 = 0.
- For index 3, the prefix is [3,2,3,4] and the suffix is [2]. There are 3 distinct elements in the prefix and 1 distinct element in the suffix. The difference is 3 - 1 = 2.
- For index 4, the prefix is [3,2,3,4,2] and the suffix is []. There are 3 distinct elements in the prefix and 0 distinct elements in the suffix. The difference is 3 - 0 = 3.
```

## Constraints

```text
1 <= n <= 50
1 <= nums[i] <= 50
```

---
## Approach 1: Brute Force

**Intuition**
Since the constraints are very small ($n \le 50$), we can afford to recalculate the distinct elements for the prefix and suffix at every index from scratch.

**Steps**
- Iterate through the array from index `i = 0` to `n-1`.
- For each `i`, create a set from `nums[0...i]` to get distinct prefix elements.
- Create another set from `nums[i+1...n-1]` to get distinct suffix elements.
- Calculate the difference between the sizes of these two sets and store it in the result array.

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def distinctDifferenceArray(self, nums: list[int]) -&gt; list[int]:\n        n = len(nums)\n        res = []\n        for i in range(n):\n            prefix = set(nums[:i+1])\n            suffix = set(nums[i+1:])\n            res.append(len(prefix) - len(suffix))\n        return res" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] distinctDifferenceArray(int[] nums) {\n        int n = nums.length;\n        int[] res = new int[n];\n        for (int i = 0; i &lt; n; i++) {\n            java.util.Set&lt;Integer&gt; prefix = new java.util.HashSet&lt;&gt;();\n            java.util.Set&lt;Integer&gt; suffix = new java.util.HashSet&lt;&gt;();\n            for (int j = 0; j &lt;= i; j++) prefix.add(nums[j]);\n            for (int j = i + 1; j &lt; n; j++) suffix.add(nums[j]);\n            res[i] = prefix.size() - suffix.size();\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; distinctDifferenceArray(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        vector&lt;int&gt; res(n);\n        for (int i = 0; i &lt; n; ++i) {\n            unordered_set&lt;int&gt; prefix(nums.begin(), nums.begin() + i + 1);\n            unordered_set&lt;int&gt; suffix(nums.begin() + i + 1, nums.end());\n            res[i] = prefix.size() - suffix.size();\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    distinctDifferenceArray(nums) {\n        const n = nums.length;\n        const res = new Array(n);\n        for (let i = 0; i &lt; n; i++) {\n            const prefix = new Set(nums.slice(0, i + 1));\n            const suffix = new Set(nums.slice(i + 1));\n            res[i] = prefix.size - suffix.size;\n        }\n        return res;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "function distinctDifferenceArray(nums: number[]): number[] {\n    const n = nums.length;\n    const res: number[] = new Array(n);\n    for (let i = 0; i &lt; n; i++) {\n        const prefix = new Set(nums.slice(0, i + 1));\n        const suffix = new Set(nums.slice(i + 1));\n        res[i] = prefix.size - suffix.size;\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: $O(n²)$
- Space: $O(n)$
- Notes: Simple to implement but inefficient for large inputs.

---
## Approach 2: Prefix and Suffix Sets

**Intuition**
We can optimize the solution by precomputing the number of distinct elements for every prefix and every suffix in a single pass each. This reduces the time complexity to linear.

**Steps**
- Create a `prefix` array where `prefix[i]` stores the count of distinct elements in `nums[0...i]`. Iterate forward, maintaining a set of seen elements.
- Create a `suffix` array where `suffix[i]` stores the count of distinct elements in `nums[i...n-1]`. Iterate backward, maintaining a set of seen elements.
- Iterate through the array again to calculate the result. For index `i`, the result is `prefix[i] - suffix[i+1]`. Handle the edge case where `i+1` is out of bounds (suffix is empty, count is 0).

<CodeTabs client:load tabs={[{ name: "Python3", lang: "python", code: "class Solution:\n    def distinctDifferenceArray(self, nums: list[int]) -&gt; list[int]:\n        n = len(nums)\n        prefix = [0] * n\n        seen = set()\n        for i in range(n):\n            seen.add(nums[i])\n            prefix[i] = len(seen)\n        \n        suffix = [0] * (n + 1)\n        seen = set()\n        for i in range(n - 1, -1, -1):\n            suffix[i] = len(seen)\n            seen.add(nums[i])\n        \n        res = []\n        for i in range(n):\n            res.append(prefix[i] - suffix[i+1])\n        return res" },
{ name: "Java", lang: "java", code: "class Solution {\n    public int[] distinctDifferenceArray(int[] nums) {\n        int n = nums.length;\n        int[] prefix = new int[n];\n        java.util.Set&lt;Integer&gt; set = new java.util.HashSet&lt;&gt;();\n        for (int i = 0; i &lt; n; i++) {\n            set.add(nums[i]);\n            prefix[i] = set.size();\n        }\n        \n        int[] suffix = new int[n + 1];\n        set.clear();\n        for (int i = n - 1; i &gt;= 0; i--) {\n            suffix[i] = set.size();\n            set.add(nums[i]);\n        }\n        \n        int[] res = new int[n];\n        for (int i = 0; i &lt; n; i++) {\n            res[i] = prefix[i] - suffix[i + 1];\n        }\n        return res;\n    }\n}" },
{ name: "C++", lang: "cpp", code: "class Solution {\npublic:\n    vector&lt;int&gt; distinctDifferenceArray(vector&lt;int&gt;& nums) {\n        int n = nums.size();\n        vector&lt;int&gt; prefix(n);\n        unordered_set&lt;int&gt; seen;\n        for (int i = 0; i &lt; n; ++i) {\n            seen.insert(nums[i]);\n            prefix[i] = seen.size();\n        }\n        \n        vector&lt;int&gt; suffix(n + 1, 0);\n        seen.clear();\n        for (int i = n - 1; i &gt;= 0; --i) {\n            suffix[i] = seen.size();\n            seen.insert(nums[i]);\n        }\n        \n        vector&lt;int&gt; res(n);\n        for (int i = 0; i &lt; n; ++i) {\n            res[i] = prefix[i] - suffix[i + 1];\n        }\n        return res;\n    }\n};" },
{ name: "JavaScript", lang: "javascript", code: "class Solution {\n    distinctDifferenceArray(nums) {\n        const n = nums.length;\n        const prefix = new Array(n);\n        const seen = new Set();\n        for (let i = 0; i &lt; n; i++) {\n            seen.add(nums[i]);\n            prefix[i] = seen.size;\n        }\n        \n        const suffix = new Array(n + 1).fill(0);\n        seen.clear();\n        for (let i = n - 1; i &gt;= 0; i--) {\n            suffix[i] = seen.size;\n            seen.add(nums[i]);\n        }\n        \n        const res = new Array(n);\n        for (let i = 0; i &lt; n; i++) {\n            res[i] = prefix[i] - suffix[i + 1];\n        }\n        return res;\n    }\n}" },
{ name: "TypeScript", lang: "typescript", code: "function distinctDifferenceArray(nums: number[]): number[] {\n    const n = nums.length;\n    const prefix: number[] = new Array(n);\n    const seen = new Set&lt;number&gt;();\n    for (let i = 0; i &lt; n; i++) {\n        seen.add(nums[i]);\n        prefix[i] = seen.size;\n    }\n    \n    const suffix: number[] = new Array(n + 1).fill(0);\n    seen.clear();\n    for (let i = n - 1; i &gt;= 0; i--) {\n        suffix[i] = seen.size;\n        seen.add(nums[i]);\n    }\n    \n    const res: number[] = new Array(n);\n    for (let i = 0; i &lt; n; i++) {\n        res[i] = prefix[i] - suffix[i + 1];\n    }\n    return res;\n}" },]} />

**Complexity**
- Time: $O(n)$
- Space: $O(n)$
- Notes: Optimal time complexity with a trade-off of using extra space for the prefix and suffix arrays.

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"TechArticle","headline":"LeetCode Find the Distinct Difference Array Solutions","datePublished":"2025-09-09","description":"Calculate an array where each element is the count of distinct numbers in the prefix minus the count of distinct numbers in the suffix.","keywords":"LeetCode, Find the Distinct Difference Array, Easy, Array, Hash Table"}).replace(/</g,'\u003c')}
</script>

<script is:inline type="application/ld+json">
{JSON.stringify({"@context":"https://schema.org","@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is the time complexity of the Brute Force approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Brute Force approach is O(n²)."}},{"@type":"Question","name":"What is the time complexity of the Prefix and Suffix Sets approach?","acceptedAnswer":{"@type":"Answer","text":"The time complexity of the Prefix and Suffix Sets approach is O(n)."}},{"@type":"Question","name":"What is the space complexity of the Prefix and Suffix Sets approach?","acceptedAnswer":{"@type":"Answer","text":"The space complexity is O(n) to store the prefix and suffix arrays."}}]}).replace(/</g,'\u003c')}
</script>